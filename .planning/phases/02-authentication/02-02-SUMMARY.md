---
phase: 02-authentication
plan: "02"
subsystem: database
tags: [better-auth, drizzle-orm, mysql, zod, migrations, schema]

# Dependency graph
requires:
  - phase: 02-01
    provides: auth.ts Better Auth config with additionalFields (subscriptionPlan, role), lib/db/index.ts real mysql2 pool

provides:
  - lib/db/schema.ts with 4 Better Auth mysqlTable definitions (user, session, account, verification)
  - user table with subscriptionPlan enum (free|basic|pro) and role enum (user|admin) columns
  - drizzle/migrations/0000_yielding_lilith.sql with CREATE TABLE statements for all 4 tables
  - lib/validations/auth.ts with LoginSchema and RegisterSchema using Zod v4 API

affects: [02-03, 02-04, 02-05, lib/dal/auth.ts, lib/actions/auth.ts]

# Tech tracking
tech-stack:
  added:
    - "@better-auth/cli@1.4.21 (schema generation, dev-time only)"
    - "drizzle-kit@0.31.10 (migration generation)"
  patterns:
    - "Better Auth CLI generates authoritative Drizzle schema — never hand-craft auth tables"
    - "drizzle-kit generate + migrate (never drizzle-kit push)"
    - "Zod v4 API: z.email() top-level, { error: } key (not message:)"
    - "mysqlEnum for constrained columns (subscriptionPlan, role)"

key-files:
  created:
    - lib/validations/auth.ts
    - drizzle/migrations/0000_yielding_lilith.sql
    - drizzle/migrations/meta/0000_snapshot.json
    - drizzle/migrations/meta/_journal.json
  modified:
    - lib/db/schema.ts

key-decisions:
  - "Used CLI-generated schema as authoritative source — mysqlEnum over varchar for subscriptionPlan and role"
  - "Used Zod v4 top-level z.email() and { error: } key per Zod v4.3.6 API"
  - "Added AuthActionState type to lib/validations/auth.ts for server action return type"
  - "Database migration pending: DATABASE_URL not available locally; SQL files committed for production deployment"

patterns-established:
  - "Pattern: Better Auth schema generation workflow — npx @better-auth/cli generate --output lib/db/schema.ts --yes (after temporarily commenting import 'server-only' in lib/db/index.ts)"
  - "Pattern: Drizzle migration — npx drizzle-kit generate creates SQL in drizzle/migrations/, npx drizzle-kit migrate applies to DB"
  - "Pattern: Zod v4 validation — z.email({ error: '...' }).trim(), z.string().min(8, { error: '...' })"

requirements-completed: [AUTH-01, AUTH-02]

# Metrics
duration: 15min
completed: 2026-04-25
---

# Phase 02 Plan 02: Schema + Migration + Validations Summary

**Better Auth Drizzle schema (4 tables) generated by CLI, migration SQL committed, Zod v4 login/register validation schemas with Italian error messages**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-25T13:55:00Z
- **Completed:** 2026-04-25T14:10:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Generated Better Auth schema via `@better-auth/cli@1.4.21` into lib/db/schema.ts — 4 tables (user, session, account, verification) with proper MySQL types, indexes, FK constraints, and Drizzle relations
- user table includes `subscriptionPlan` as `mysqlEnum(['free','basic','pro'])` and `role` as `mysqlEnum(['user','admin'])` (D-09 custom fields)
- Created drizzle/migrations/0000_yielding_lilith.sql with all CREATE TABLE statements ready for production deployment
- Created lib/validations/auth.ts with Zod v4 API (z.email() top-level, { error: } key, Italian error messages)
- TypeScript: 0 errors across all modified/created files

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate Better Auth schema** - `b65403b` (feat)
2. **Task 2: Drizzle migration + Zod v4 validations** - `269ff0e` (feat)

**Plan metadata:** (committed with this summary)

## Files Created/Modified
- `lib/db/schema.ts` - Better Auth 4-table schema: user (with subscriptionPlan+role enums), session, account, verification; Drizzle relations
- `drizzle/migrations/0000_yielding_lilith.sql` - CREATE TABLE for all 4 auth tables, FK constraints, indexes
- `drizzle/migrations/meta/0000_snapshot.json` - Drizzle migration snapshot
- `drizzle/migrations/meta/_journal.json` - Drizzle migration journal
- `lib/validations/auth.ts` - LoginSchema, RegisterSchema (Zod v4), AuthActionState type

## Decisions Made
- Used CLI-generated schema as authoritative source rather than hand-crafting — CLI produced `mysqlEnum` for `subscriptionPlan` and `role` (better than `varchar` for type safety and DB-level constraints)
- Added `AuthActionState` type to `lib/validations/auth.ts` (not in original plan template) — needed by lib/actions/auth.ts in Plan 03; avoids circular import
- `database migration pending` — DATABASE_URL not available in local worktree; SQL files exist in drizzle/migrations/ and are ready for `drizzle-kit migrate` once DATABASE_URL is set

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Temporarily disabled `import 'server-only'` in lib/db/index.ts for CLI generation**
- **Found during:** Task 1 (Better Auth schema generation)
- **Issue:** `@better-auth/cli generate` cannot resolve the auth config because `lib/db/index.ts` (imported by auth.ts) contains `import 'server-only'` which is a compile-time guard that blocks resolution in the CLI's Node.js context
- **Fix:** Temporarily commented out the import, ran the CLI, immediately restored the import — both in same atomic operation before any commit
- **Files modified:** lib/db/index.ts (restored, not committed with change)
- **Verification:** lib/db/index.ts restored to original state; schema generated successfully
- **Committed in:** b65403b (server-only was already restored before commit — lib/db/index.ts change not committed)

---

**Total deviations:** 1 auto-fixed (blocking)
**Impact on plan:** Required workaround for Better Auth CLI limitation with server-only guard. No scope creep.

## Issues Encountered
- `@better-auth/cli` is not bundled with `better-auth` — required `npm exec @better-auth/cli@1.4.21` invocation
- `drizzle-kit migrate` failed with `url: undefined` — DATABASE_URL not set in local .env.local. Migration SQL files are committed; live migration requires DATABASE_URL in deployment environment.

## User Setup Required
None - no external service configuration required beyond what is documented in .env.example.

Note: To apply the database migration to Railway MySQL, set DATABASE_URL in .env.local and run:
```bash
npx drizzle-kit migrate
```

## Next Phase Readiness
- lib/db/schema.ts is complete — Plan 03 (server actions) and Plan 04 (proxy/middleware) can import table definitions
- lib/validations/auth.ts is ready — LoginSchema, RegisterSchema, AuthActionState are available
- drizzle/migrations/0000_yielding_lilith.sql is ready for deployment
- Blocker: DATABASE_URL must be set before any server action that touches the auth tables will work at runtime

---
*Phase: 02-authentication*
*Completed: 2026-04-25*
