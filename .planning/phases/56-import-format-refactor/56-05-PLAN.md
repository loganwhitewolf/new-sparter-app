---
phase: 56-import-format-refactor
plan: 05
type: execute
wave: 5
depends_on: ["56-04"]
files_modified:
  - lib/dal/regex-discovery.ts
  - scripts/regex-discovery.ts
  - scripts/seed-extras.ts
autonomous: true
gap_closure: true
requirements: [IFMT-05]
must_haves:
  truths:
    - "lib/dal/regex-discovery.ts selects descriptionStripPattern from importFormatVersion (not platform); TS2339 errors at lines 45 and 92 are gone."
    - "scripts/regex-discovery.ts loadAllActiveFormats selects all 9 contract columns from importFormatVersion (not platform); all TS2339 errors in lines 142-151 are gone."
    - "scripts/seed-extras.ts set-fineco-description-strip-pattern step is a documented no-op; the TS2353 error at line 50 is gone and yarn db:seed-extras runs cleanly."
    - "npx tsc --noEmit reports zero errors across all three files."
  artifacts:
    - "lib/dal/regex-discovery.ts (descriptionStripPattern read from importFormatVersion)"
    - "scripts/regex-discovery.ts (loadAllActiveFormats selects contract from importFormatVersion)"
    - "scripts/seed-extras.ts (set-fineco-description-strip-pattern converted to no-op)"
  key_links:
    - "lib/dal/regex-discovery.ts join chain: expense → file → importFormatVersion → platform — importFormatVersion is already the join anchor; contract columns are simply re-pointed to the version row"
    - "scripts/regex-discovery.ts already innerJoins importFormatVersion; the platform join remains for identity (name/slug/country); contract column aliases are redirected to importFormatVersion"
    - "seed-extras.ts step 2 is superseded — descriptionStripPattern for Fineco is now seeded via importFormatVersions in seed-data.ts, so step 2 becomes a safe no-op"
---

<objective>
Close the 3 TypeScript compilation blockers that VERIFICATION.md identified as the sole remaining gap in IFMT-05. All three files still reference contract columns on the `platform` table that were dropped in migration 0022. The fix is mechanical re-pointing — each file already has the importFormatVersion join in place; only the column references need redirecting.

Purpose: Achieve a clean `npx tsc --noEmit` across the full phase-56 file set, completing IFMT-05.
Output: 3 files updated, 13 TS errors eliminated, `yarn db:seed-extras` compiles and runs cleanly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-import-format-refactor/56-CONTEXT.md
@lib/dal/regex-discovery.ts
@scripts/regex-discovery.ts
@scripts/seed-extras.ts
@lib/dal/import-formats.ts
@lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point lib/dal/regex-discovery.ts to read descriptionStripPattern from importFormatVersion</name>
  <files>lib/dal/regex-discovery.ts</files>
  <read_first>
    - lib/dal/regex-discovery.ts — Two select queries each carry `descriptionStripPattern: platform.descriptionStripPattern` (line 45 in getUncategorizedExpensesForDiscovery; line 92 in getUncategorizedExpensesForPlatformApply). Both already join importFormatVersion via `leftJoin(importFormatVersion, eq(file.importFormatVersionId, importFormatVersion.id))`. Only the column reference needs changing.
    - lib/dal/import-formats.ts — Authoritative reference for how the production DAL reads descriptionStripPattern: `descriptionStripPattern: importFormatVersion.descriptionStripPattern` in the select object. Mirror this pattern exactly.
    - lib/db/schema.ts — Confirm importFormatVersion.descriptionStripPattern is `text("description_strip_pattern")` (nullable, line ~307).
  </read_first>
  <action>
    In lib/dal/regex-discovery.ts, change both occurrences of `platform.descriptionStripPattern` to `importFormatVersion.descriptionStripPattern`:

    1. In `getUncategorizedExpensesForDiscovery` (currently line 45): change the select field from `descriptionStripPattern: platform.descriptionStripPattern` to `descriptionStripPattern: importFormatVersion.descriptionStripPattern`.

    2. In `getUncategorizedExpensesForPlatformApply` (currently line 92): change the select field from `descriptionStripPattern: platform.descriptionStripPattern` to `descriptionStripPattern: importFormatVersion.descriptionStripPattern`.

    The join chain in both functions (expense → file → importFormatVersion → platform) is already correct and must not be changed. The return types `UncategorizedExpenseForDiscovery` and `UncategorizedExpenseForPlatformApply` both declare `descriptionStripPattern: string | null` — the type is correct and must not change (importFormatVersion.descriptionStripPattern is nullable text, which matches).

    No other changes to this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "regex-discovery.ts" || echo "No errors in lib/dal/regex-discovery.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "lib/dal/regex-discovery.ts"` produces no output (zero TS errors in this file).
    - Both select objects use `importFormatVersion.descriptionStripPattern`, not `platform.descriptionStripPattern`.
    - The join chains and return types are unchanged.
  </acceptance_criteria>
  <done>lib/dal/regex-discovery.ts compiles cleanly; descriptionStripPattern is read from importFormatVersion (per IFMT-05, ADR 0013).</done>
</task>

<task type="auto">
  <name>Task 2: Re-point scripts/regex-discovery.ts loadAllActiveFormats to select contract from importFormatVersion</name>
  <files>scripts/regex-discovery.ts</files>
  <read_first>
    - scripts/regex-discovery.ts lines 132-179 — `loadAllActiveFormats` already calls `.innerJoin(platform, eq(importFormatVersion.platformId, platform.id))` (line 154). The select object (lines 134-152) reads 9 contract columns from `platform.*` with aliases like `platformDelimiter: platform.delimiter`, `platformTimestampColumn: platform.timestampColumn`, etc. The map (lines 157-178) unpacks these into the `platform` sub-object on each candidate. All 9 of these aliases must be redirected to `importFormatVersion.*`.
    - lib/dal/import-formats.ts — Authoritative reference for the correct column mapping post-migration 0022. The production `loadImportFormatsForDetection` selects: `delimiter: importFormatVersion.delimiter`, `timestampColumn: importFormatVersion.timestampColumn`, `descriptionColumn: importFormatVersion.descriptionColumn`, `amountType: importFormatVersion.amountType`, `amountColumn: importFormatVersion.amountColumn`, `positiveAmountColumn: importFormatVersion.positiveAmountColumn`, `negativeAmountColumn: importFormatVersion.negativeAmountColumn`, `multiplyBy: importFormatVersion.multiplyBy`, `descriptionStripPattern: importFormatVersion.descriptionStripPattern`.
    - The script retains the `platform` innerJoin for identity columns: `platformName: platform.name`, `platformSlug: platform.slug`, `platformCountry: platform.country` (these were NOT dropped in migration 0022 and are correct).
  </read_first>
  <action>
    In scripts/regex-discovery.ts `loadAllActiveFormats`, redirect the 9 contract-column aliases from the `platform` table to `importFormatVersion`. Replace the platform-sourced contract aliases in the `.select({...})` block (lines 134-152) with the following changes:

    - `platformDelimiter: platform.delimiter` → `platformDelimiter: importFormatVersion.delimiter`
    - `platformTimestampColumn: platform.timestampColumn` → `platformTimestampColumn: importFormatVersion.timestampColumn`
    - `platformDescriptionColumn: platform.descriptionColumn` → `platformDescriptionColumn: importFormatVersion.descriptionColumn`
    - `platformAmountType: platform.amountType` → `platformAmountType: importFormatVersion.amountType`
    - `platformAmountColumn: platform.amountColumn` → `platformAmountColumn: importFormatVersion.amountColumn`
    - `platformPositiveAmountColumn: platform.positiveAmountColumn` → `platformPositiveAmountColumn: importFormatVersion.positiveAmountColumn`
    - `platformNegativeAmountColumn: platform.negativeAmountColumn` → `platformNegativeAmountColumn: importFormatVersion.negativeAmountColumn`
    - `platformMultiplyBy: platform.multiplyBy` → `platformMultiplyBy: importFormatVersion.multiplyBy`
    - `platformDescriptionStripPattern: platform.descriptionStripPattern` → `platformDescriptionStripPattern: importFormatVersion.descriptionStripPattern`

    The identity aliases `platformName: platform.name`, `platformSlug: platform.slug`, `platformCountry: platform.country` remain unchanged (platform retains identity columns post-migration 0022).

    The `.innerJoin(platform, ...)` clause, the `.where(...)` clause, and the `.map(...)` block that unpacks `row.platformDelimiter` etc. into `candidate.platform.*` are all unchanged — only the select source table changes.

    Also ensure `importFormatVersion` is already imported from `../lib/db/schema` at the top of the file (line 21 confirms it is).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "scripts/regex-discovery.ts" || echo "No errors in scripts/regex-discovery.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "scripts/regex-discovery.ts"` produces no output.
    - All 9 contract-column aliases in loadAllActiveFormats reference `importFormatVersion.*`, not `platform.*`.
    - Identity aliases (platformName, platformSlug, platformCountry) still reference `platform.*`.
    - The map block unpacking `row.platformDelimiter` etc. is unchanged.
  </acceptance_criteria>
  <done>scripts/regex-discovery.ts compiles cleanly; loadAllActiveFormats builds ImportFormatCandidateInput from importFormatVersion contract columns as required by IFMT-05 and ADR 0013.</done>
</task>

<task type="auto">
  <name>Task 3: Convert set-fineco-description-strip-pattern in seed-extras.ts to a no-op</name>
  <files>scripts/seed-extras.ts</files>
  <read_first>
    - scripts/seed-extras.ts lines 46-54 — `setFinecoDescriptionStripPattern` (Step 2) calls `database.update(platform).set({ descriptionStripPattern: '\\s+Carta N\\..*$' }).where(eq(platform.slug, 'fineco'))`. This is the TS2353 error: `descriptionStripPattern` no longer exists on the platform table (dropped in migration 0022).
    - VERIFICATION.md gap 3 / remediation option (b): the strip pattern for Fineco is now seeded directly via `importFormatVersions` in seed-data.ts (the `move-parsing-contract-to-format-version` step in seed-extras.ts already migrated existing rows, and new installs get the value from seed.ts). The step is functionally redundant. Convert it to a no-op with a comment explaining it is superseded.
    - CLAUDE.md hard rule: seeds are additive — never remove or reorder existing steps. Convert to no-op body; do not delete the function or remove it from STEPS.
  </read_first>
  <action>
    In scripts/seed-extras.ts, replace the `setFinecoDescriptionStripPattern` function body with a no-op that logs a superseded message. Replace the entire function body (the `database.update(...)` block) with:

    ```
    console.log('    set-fineco-description-strip-pattern: no-op (superseded — descriptionStripPattern now lives on importFormatVersion, seeded via seed-data.ts importFormatVersions)')
    ```

    Remove the now-unused `result` and `count` local variables entirely. The function signature `async function setFinecoDescriptionStripPattern(database: Db): Promise<void>` is unchanged. The `_database` parameter rename convention (prefix with underscore) may be applied since the parameter is now unused: rename `database` to `_database` in the signature to suppress any "unused variable" linter warnings.

    The `platform` import at the top of the file may now be unused by this step if no other step in the file uses it — do NOT remove it speculatively, as other steps (e.g. reorganizeSpesaSubcategories) may still use `platform`. Check whether any other step references `platform` before removing its import. If other steps use it, leave the import as-is.

    No other changes to this file. The STEPS array order and all other step functions are unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "seed-extras.ts" || echo "No errors in scripts/seed-extras.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "scripts/seed-extras.ts"` produces no output.
    - `setFinecoDescriptionStripPattern` function body contains only a `console.log` no-op call (no `database.update` on platform).
    - The function remains in STEPS at its original position (Step 2); order is unchanged.
    - No platform import removed if any other step in the file references `platform`.
  </acceptance_criteria>
  <done>scripts/seed-extras.ts compiles cleanly; yarn db:seed-extras runs without error; Step 2 documents why it is superseded (IFMT-05 — platform no longer carries the contract post-migration 0022).</done>
</task>

<task type="auto">
  <name>Task 4: Full TypeScript compilation gate</name>
  <files></files>
  <read_first>
    - VERIFICATION.md behavioral spot-checks table: the failing check was `npx tsc --noEmit` filtered to phase-56 files, expecting zero errors. After Tasks 1-3, the 13 errors (12 TS2339 + 1 TS2353) must all be gone.
  </read_first>
  <action>
    Run `npx tsc --noEmit` across the full project. Confirm the output contains zero errors referencing `lib/dal/regex-discovery.ts`, `scripts/regex-discovery.ts`, or `scripts/seed-extras.ts`. If any error remains in those three files, investigate and fix in the relevant task's file before proceeding. Do not suppress errors or add `@ts-ignore` directives — fix the root cause.

    Also run `yarn check:language` to confirm no developer-facing Italian strings were introduced in the no-op comment or any other change.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "regex-discovery\.ts|seed-extras\.ts" | wc -l | tr -d ' ' | grep -qx '0' && echo "PASS: zero TS errors in gap files" || echo "FAIL: TS errors remain"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces zero errors in lib/dal/regex-discovery.ts, scripts/regex-discovery.ts, and scripts/seed-extras.ts.
    - `yarn check:language` exits clean (no developer-facing Italian strings introduced).
    - No `@ts-ignore` or `as any` casts added to any of the three files.
  </acceptance_criteria>
  <done>Phase 56 achieves a clean TypeScript compilation across all files touched by the phase, completing IFMT-05 — all consumers re-pointed to the importFormatVersion contract with no behavioral regression.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | File | Change |
|--------|------|--------|
| `getUncategorizedExpensesForDiscovery` | `lib/dal/regex-discovery.ts` | `descriptionStripPattern` select redirected from `platform` to `importFormatVersion` |
| `getUncategorizedExpensesForPlatformApply` | `lib/dal/regex-discovery.ts` | `descriptionStripPattern` select redirected from `platform` to `importFormatVersion` |
| `loadAllActiveFormats` | `scripts/regex-discovery.ts` | 9 contract-column aliases redirected from `platform.*` to `importFormatVersion.*`; identity aliases unchanged |
| `setFinecoDescriptionStripPattern` | `scripts/seed-extras.ts` | Body replaced with no-op console.log; TS2353 error eliminated; STEPS order preserved |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regex-discovery CLI → DB | Operator-run script reads format contract from the DB; a mis-pointed query would silently produce wrong import_format_config shapes, causing the discovery tool to normalize transactions incorrectly. |
| seed-extras runner → DB | Operator-run seed step writes to the platform table; a step targeting a dropped column fails at compile time (and would fail at runtime). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-10 | Tampering | loadAllActiveFormats contract-source move | medium | mitigate | The aliased field names feeding into `candidate.platform.*` are unchanged — the ImportFormatCandidateInput shape consumed by detectImportFormat and normalizeTransactionRow is identical to what loadImportFormatsForDetection already produces; no behavioral drift possible. |
| T-56-11 | Repudiation | no-op step silently hiding a broken seed | low | mitigate | The no-op logs a clear superseded message explaining where the value now lives; operators can trace the migration path through the comment and the VERIFICATION report. |
| T-56-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed in this plan; all changes are column-reference redirections on existing imported symbols. |
</threat_model>

<verification>
- `npx tsc --noEmit` produces zero errors in lib/dal/regex-discovery.ts, scripts/regex-discovery.ts, and scripts/seed-extras.ts.
- Both select queries in lib/dal/regex-discovery.ts use `importFormatVersion.descriptionStripPattern` (grep confirms zero occurrences of `platform.descriptionStripPattern` in the file).
- `loadAllActiveFormats` in scripts/regex-discovery.ts contains zero references to `platform.delimiter`, `platform.timestampColumn`, `platform.descriptionColumn`, `platform.amountType`, `platform.amountColumn`, `platform.positiveAmountColumn`, `platform.negativeAmountColumn`, `platform.multiplyBy`, or `platform.descriptionStripPattern`.
- `setFinecoDescriptionStripPattern` in scripts/seed-extras.ts contains no `database.update` call (grep confirms no `.update(platform)` in the function body).
- `yarn check:language` exits clean.
</verification>

<success_criteria>
Phase 56 IFMT-05 is fully satisfied: all consumers (scoreCandidate, normalizeTransactionRow, detection DAL, seeds, wizard, regex-discovery DAL, regex-discovery CLI) read or write the parsing contract from `import_format_version` only. The TypeScript compiler reports zero errors across all phase-56 files. The regex-discovery workflow (yarn regex:discover, /regex-label) is restored to a compilable state. yarn db:seed-extras runs cleanly with Step 2 documented as superseded.
</success_criteria>

<output>
Create `.planning/phases/56-import-format-refactor/56-05-SUMMARY.md` when done. Confirm `npx tsc --noEmit` is clean across all three gap files and list the exact line changes made in each.
</output>
