---
phase: 60-seed-slug-linkage-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTEXT.md
  - lib/services/regex-discovery.ts
  - lib/services/pattern-application.ts
autonomous: true
requirements:
  - PLAT-06
must_haves:
  truths:
    - "CONTEXT.md DescriptionStripPattern glossary entry correctly attributes the field to import_format_version (ADR 0013), not to platform"
    - "JSDoc and inline comments in lib/services/regex-discovery.ts reference import format version as the source of descriptionStripPattern"
    - "Inline comment in lib/services/pattern-application.ts references import format version, not platform, as the strip-pattern source"
  artifacts:
    - "CONTEXT.md — DescriptionStripPattern entry first sentence updated to reference import_format_version and ADR 0013"
    - "lib/services/regex-discovery.ts — JSDoc and step-3 inline comment updated"
    - "lib/services/pattern-application.ts — filter inline comment updated"
  key_links:
    - "ADR 0013 (Phase 56): descriptionStripPattern moved from platform to import_format_version; all developer-visible docs must reflect this"
---

<objective>
Correct every developer-facing reference that still attributes DescriptionStripPattern to the `platform` table rather than to `import_format_version` where it lives since Phase 56 (ADR 0013) — closing PLAT-06.

Purpose: Stale "per Platform" attribution in CONTEXT.md and three service-layer code comments conflicts with ADR 0013 and creates confusion for future contributors. These are documentation/comment fixes only; no runtime behavior changes.

Output:
- CONTEXT.md — DescriptionStripPattern glossary entry first sentence updated
- lib/services/regex-discovery.ts — two comments updated
- lib/services/pattern-application.ts — one comment updated
</objective>

<execution_context>
@/Users/andreabernardini/.claude/gsd-core/workflows/execute-plan.md
@/Users/andreabernardini/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DescriptionStripPattern glossary entry in CONTEXT.md</name>
  <files>CONTEXT.md</files>
  <read_first>
    Read CONTEXT.md lines 33–36 — the DescriptionStripPattern glossary entry.
    Current first sentence (stale): the attribution says the regex is configured for the Platform table.
    The correct attribution (ADR 0013): the field lives on `import_format_version` (`description_strip_pattern` column), not on `platform`.
  </read_first>
  <action>
    Edit CONTEXT.md. Locate the DescriptionStripPattern glossary entry (under "### Transazioni e movimenti"). Its first sentence currently attributes the regex to the platform entity.

    Replace only the first sentence of the DescriptionStripPattern body — the sentence that names the table/entity the field belongs to — with:
    "Regex nullable configurata per Import Format (campo `description_strip_pattern` su `import_format_version`, ADR 0013)."

    Leave every subsequent sentence of the entry unchanged: the sentence describing when the pattern is applied (before normalizeDescription and hash calculation), the sentence about boilerplate removal, and the sentence about rawRow preservation all remain as-is.

    Do not change the entry heading, the _Avoid_ line, or any other part of CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q 'configurata per Import Format' CONTEXT.md && echo 'PASS' || echo 'FAIL'</automated>
    Expected output: PASS
  </verify>
  <acceptance_criteria>
    - The DescriptionStripPattern entry first sentence states the field lives on `import_format_version` and cites ADR 0013
    - No sentence in that entry attributes the field to `platform`
    - All other content in CONTEXT.md is untouched
  </acceptance_criteria>
  <done>CONTEXT.md DescriptionStripPattern entry first sentence references `import_format_version` and ADR 0013. grep confirms the corrected phrase is present.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale code comments in regex-discovery.ts and pattern-application.ts</name>
  <files>lib/services/regex-discovery.ts, lib/services/pattern-application.ts</files>
  <read_first>
    Read lib/services/regex-discovery.ts lines 28–95:
    - Line ~34: JSDoc comment on the `applyStrip` function — says the source of the strip pattern is the "platform".
    - Line ~87: inline comment in the `discoverRegexCandidates` step-3 block — describes the strip pattern as a "platform-level" attribute.

    Read lib/services/pattern-application.ts lines 165–180:
    - Line ~170: inline comment in the filter block — says the strip pattern being applied is a "platform" attribute.
  </read_first>
  <action>
    Make three targeted comment edits. Do NOT change any executable code — only the comment text on each of the three lines.

    1. lib/services/regex-discovery.ts — `applyStrip` JSDoc (line ~34):
       Update the phrase that describes the origin of the strip value. Where the comment currently names "the platform" as the source of the strip pattern (a "seed/operator-controlled value"), change the attribution to name the import format version: "Applies the import format version descriptionStripPattern (`import_format_version.description_strip_pattern`, ADR 0013), a seed/operator-controlled value..."
       Preserve the remainder of the JSDoc sentence (the T-51-07 note about never deriving from user-supplied free text).

    2. lib/services/regex-discovery.ts — step-3 inline comment (line ~87):
       The parenthetical that describes why all rows share the same strip value currently calls it a "platform-level value". Change this to "import format version value" (`import_format_version.description_strip_pattern`). The surrounding sentence structure and context remain unchanged.

    3. lib/services/pattern-application.ts — filter inline comment (line ~170):
       The CR-02 mirror comment says "apply platform descriptionStripPattern before normalizing". Change the attribution from "platform" to "import format version": "apply import format version descriptionStripPattern before normalizing".
       Preserve the rest of the comment line.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Expected: exits 0 (comment-only changes cannot break TypeScript, but run to confirm no accidental edits slipped into code).
    Also: grep -q 'import format version' lib/services/regex-discovery.ts && echo 'PASS'
    Expected: PASS
  </verify>
  <acceptance_criteria>
    - The `applyStrip` JSDoc in regex-discovery.ts attributes the strip pattern to "import format version" (not "platform")
    - The step-3 inline comment in regex-discovery.ts calls it an "import format version value" (not "platform-level")
    - The CR-02 comment in pattern-application.ts says "import format version descriptionStripPattern" (not "platform descriptionStripPattern")
    - No executable code lines are changed in either file
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>Three comments updated across two service files. All three now reference import format version as the source of descriptionStripPattern. tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → codebase | CONTEXT.md and service comments are internal developer documentation; no user-facing or runtime exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-60-04 | Information Disclosure | CONTEXT.md exposes internal schema details | low | accept | CONTEXT.md is private developer documentation, not served to end users |
| T-60-05 | Tampering | Accidental code edit during comment fix | low | mitigate | tsc --noEmit in verify catches any inadvertent code change; comment-only edits scoped to specific lines |
</threat_model>

<verification>
After executing both tasks:

  grep -q 'configurata per Import Format' CONTEXT.md && echo 'CONTEXT.md OK'
  grep -q 'import format version' lib/services/regex-discovery.ts && echo 'regex-discovery.ts OK'
  grep -q 'import format version' lib/services/pattern-application.ts && echo 'pattern-application.ts OK'
  npx tsc --noEmit && echo 'TypeScript OK'
</verification>

<success_criteria>
1. CONTEXT.md DescriptionStripPattern entry first sentence: "Regex nullable configurata per Import Format (campo `description_strip_pattern` su `import_format_version`, ADR 0013)."
2. lib/services/regex-discovery.ts: both comments (applyStrip JSDoc and step-3 inline) reference "import format version" as the strip pattern source.
3. lib/services/pattern-application.ts: CR-02 comment references "import format version descriptionStripPattern".
4. TypeScript compiles clean (no accidental code edits).
</success_criteria>

<output>
Create .planning/phases/60-seed-slug-linkage-and-docs/60-02-SUMMARY.md when done.
</output>
