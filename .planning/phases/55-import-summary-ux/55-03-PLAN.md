---
phase: 55-import-summary-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/import/suggestion-section.tsx
  - app/(app)/import/[fileId]/suggestions/page.tsx
  - tests/import-suggestions-page.test.tsx
autonomous: true
requirements:
  - SUMUI-02
  - SUMUI-03

must_haves:
  truths:
    - "La suggestions page ha un paragrafo descrittivo sotto l'h1 che comunica scope platform e entry point ricontrolla (SUMUI-03)"
    - "I due gruppi nella SuggestionSection hanno heading distinti con testo introduttivo di 1 riga ciascuno (SUMUI-02)"
    - "I aria-label sulle <section> sono preservati invariati"
    - "I single-categorization items rimangono read-only — nessuna CTA aggiunta"
    - "Il test per il copy della suggestions page è aggiornato al testo SUMUI-03"
  artifacts:
    - path: "components/import/suggestion-section.tsx"
      provides: "Section headings polish con intro text per regex candidates e single-cat groups"
    - path: "app/(app)/import/[fileId]/suggestions/page.tsx"
      provides: "Paragrafo SUMUI-03 sotto h1 — scope platform + entry point ricontrolla"
    - path: "tests/import-suggestions-page.test.tsx"
      provides: "Test aggiornati per SUMUI-02 headings e SUMUI-03 sub-heading copy"
  key_links:
    - from: "app/(app)/import/[fileId]/suggestions/page.tsx"
      to: "components/import/suggestion-section.tsx"
      via: "page passes candidates + singleSuggestions props; SuggestionSection renders two sections"
      pattern: "<SuggestionSection"
---

<objective>
Polish UI della suggestions page post-import:
- SUMUI-02 (D-03): intestazioni di sezione distinte con intro text in SuggestionSection per regex candidates vs single-categorization
- SUMUI-03 (D-01, D-02): paragrafo descrittivo sotto l'h1 nella suggestions page che comunica (a) scope platform, (b) entry point ricontrolla dal tab Importazioni
- Aggiornare i test correlati

Purpose: L'utente che atterra sulla suggestions page capisce immediatamente che (a) i suggerimenti vengono dalla sua piattaforma specifica, (b) può ricontrollare, e (c) i due gruppi hanno scopi diversi.
Output: suggestion-section.tsx e suggestions/page.tsx con copy aggiornato; test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-import-summary-ux/55-CONTEXT.md
@.planning/phases/55-import-summary-ux/55-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish SuggestionSection headings con intro text (SUMUI-02, D-03, D-04)</name>
  <files>components/import/suggestion-section.tsx</files>

  <read_first>
    - components/import/suggestion-section.tsx (intero file — capire struttura delle due section, posizione degli h2, valori degli aria-label, rendering di suggestions e singleSuggestions)
    - .planning/phases/55-import-summary-ux/55-PATTERNS.md (sezione "suggestion-section.tsx" — esempi esatti di come aggiungere il div wrapper con h2 + p)
    - CONTEXT.md (repo root — tono informativo vs promozionale per copy italiana)
  </read_first>

  <action>
    In components/import/suggestion-section.tsx, modificare le due section:

    Section 1 — regex candidates (aria-label="Suggerimenti pattern"):
    1. Trovare l'h2 attuale: Suggerimenti pattern ({suggestions.length}).
    2. Sostituirlo con un div wrapper contenente:
       - h2 className="text-base font-semibold" con testo: Pattern proposti
       - p className="mt-1 text-sm text-muted-foreground" con testo: Crea un pattern per categorizzare automaticamente queste transazioni nelle importazioni future.
    3. Il count suggestions.length può essere omesso dall'heading (i count sono già visibili nelle card individuali) — lasciare heading neutro come da discretion.
    4. Preservare aria-label="Suggerimenti pattern" sull'elemento section invariato.

    Section 2 — single-categorization (aria-label="Transazioni identiche senza categoria"):
    1. Trovare l'h2 attuale: Transazioni identiche ({singleSuggestions.length}).
    2. Sostituirlo con un div wrapper contenente:
       - h2 className="text-base font-semibold" con testo: Transazioni identiche ({singleSuggestions.length}) — mantenere il count qui perché aiuta l'utente a calibrare l'effort manuale (come da RESEARCH.md open question 1 recommendation)
       - p className="mt-1 text-sm text-muted-foreground" con testo: Queste descrizioni compaiono più volte ma non generano un pattern automatizzabile. Categorizzale manualmente dalla pagina Spese.
    3. Preservare aria-label="Transazioni identiche senza categoria" sull'elemento section invariato.
    4. Verificare che i single-cat items non abbiano CTA o link aggiunti (D-04 — read-only).

    Eseguire yarn check:language dopo le modifiche.
  </action>

  <verify>
    <automated>grep -c "aria-label=\"Suggerimenti pattern\"" components/import/suggestion-section.tsx | grep -q "^1$" && echo "PASS: aria-label section 1 preservato" && grep -c "aria-label=\"Transazioni identiche senza categoria\"" components/import/suggestion-section.tsx | grep -q "^1$" && echo "PASS: aria-label section 2 preservato" && grep -c "Pattern proposti" components/import/suggestion-section.tsx | grep -q "^1$" && echo "PASS: heading section 1 aggiornato" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo "PASS: TypeScript clean"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "aria-label=\"Suggerimenti pattern\"" components/import/suggestion-section.tsx restituisce 1 (preservato)
    - grep -c "aria-label=\"Transazioni identiche senza categoria\"" components/import/suggestion-section.tsx restituisce 1 (preservato)
    - grep -c "Pattern proposti" components/import/suggestion-section.tsx restituisce 1 (heading section 1 aggiornato)
    - grep -c "Categorizzale manualmente" components/import/suggestion-section.tsx restituisce 1 (intro text section 2 presente)
    - grep -c "Crea un pattern per categorizzare automaticamente" components/import/suggestion-section.tsx restituisce 1 (intro text section 1 presente)
    - Il file non contiene nuovi elementi CTA, button, o link nelle section single-cat (D-04 read-only)
    - npx tsc --noEmit esce con codice 0
  </acceptance_criteria>

  <done>SuggestionSection ha heading distinti con intro text per i due gruppi; aria-label preservati; single-cat items read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere paragrafo SUMUI-03 nella suggestions page (D-01, D-02)</name>
  <files>app/(app)/import/[fileId]/suggestions/page.tsx</files>

  <read_first>
    - app/(app)/import/[fileId]/suggestions/page.tsx (intero file — capire struttura del blocco header con h1 + p attuale, auth guard, rendering di SuggestionSection)
    - .planning/phases/55-import-summary-ux/55-PATTERNS.md (sezione "suggestions/page.tsx" — struttura attuale del blocco header righe 34–39 e pattern di modifica)
    - .planning/phases/55-import-summary-ux/55-CONTEXT.md (D-01, D-02 — posizione e contenuto del paragrafo SUMUI-03)
  </read_first>

  <action>
    In app/(app)/import/[fileId]/suggestions/page.tsx:

    1. Trovare il blocco div con h1 "Suggerimenti pattern" e il p.text-muted-foreground esistente (righe ~34–39). Il testo attuale del p è: "Crea pattern per categorizzare automaticamente transazioni simili nelle prossime importazioni."

    2. Sostituire il contenuto del p.text-muted-foreground con il testo SUMUI-03 (D-02):
       "I suggerimenti sono stati rilevati dalle transazioni non categorizzate di questa piattaforma dopo l'importazione. Puoi ricontrollare i pattern in qualsiasi momento dal tab Importazioni."
       Usare l'entità HTML &apos; per l'apostrofo in JSX (l'importazione → l&apos;importazione) se il progetto usa regole JSX strict; in alternativa usare l'apostrofo diretto Unicode se già usato altrove nella pagina.

    3. Mantenere invariati: auth guard (verifySession + getFileForUser + notFound), il resto del layout, il rendering di SuggestionSection, il flusso recheckRegexAction.

    4. Eseguire yarn check:language dopo la modifica.

    Il paragrafò comunica: (a) scope platform ("transazioni non categorizzate di questa piattaforma"), (b) entry point ricontrolla ("dal tab Importazioni") — entrambi richiesti da D-02.
  </action>

  <verify>
    <automated>grep -c "rilevati dalle transazioni non categorizzate di questa piattaforma" "app/(app)/import/[fileId]/suggestions/page.tsx" | grep -q "^1$" && echo "PASS: copy SUMUI-03 presente" && grep -c "tab Importazioni" "app/(app)/import/[fileId]/suggestions/page.tsx" | grep -q "^1$" && echo "PASS: entry point ricontrolla menzionato" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo "PASS: TypeScript clean"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "rilevati dalle transazioni non categorizzate di questa piattaforma" app/(app)/import/[fileId]/suggestions/page.tsx restituisce 1
    - grep -c "tab Importazioni" app/(app)/import/[fileId]/suggestions/page.tsx restituisce 1
    - grep -c "Suggerimenti pattern" app/(app)/import/[fileId]/suggestions/page.tsx restituisce almeno 1 (h1 preservato)
    - Il testo "Crea pattern per categorizzare automaticamente transazioni simili nelle prossime importazioni" non è più presente (sostituito)
    - npx tsc --noEmit esce con codice 0
  </acceptance_criteria>

  <done>La suggestions page ha il paragrafo SUMUI-03 che comunica scope platform e entry point ricontrolla; il testo vecchio è sostituito.</done>
</task>

<task type="auto">
  <name>Task 3: Aggiornare tests/import-suggestions-page.test.tsx per SUMUI-02 e SUMUI-03</name>
  <files>tests/import-suggestions-page.test.tsx</files>

  <read_first>
    - tests/import-suggestions-page.test.tsx (intero file — capire struttura della suite, test D-08 copy su subtitle, test su heading "Suggerimenti pattern", helper di mock)
    - app/(app)/import/[fileId]/suggestions/page.tsx (versione già modificata nel Task 2 — copy esatto del p SUMUI-03)
    - components/import/suggestion-section.tsx (versione già modificata nel Task 1 — heading "Pattern proposti" e intro text)
  </read_first>

  <action>
    In tests/import-suggestions-page.test.tsx:

    1. Test D-08 copy (riga ~237, "page contains required heading and subtitle"):
       - Il test verifica attualmente: html.toContain('Crea pattern per categorizzare automaticamente transazioni simili nelle prossime importazioni')
       - Aggiornare la stringa verificata con il nuovo testo SUMUI-03: usare una sottostringa caratteristica come "rilevati dalle transazioni non categorizzate di questa piattaforma" o "tab Importazioni" (evitare di copiare l'intera frase con apostrofi che potrebbero differire tra entity HTML e stringa JS).
       - Mantenere la verifica che l'h1 "Suggerimenti pattern" sia presente.

    2. Test SUMUI-02 headings (aggiungere o aggiornare):
       - Se il test file ha un test che verifica le heading della SuggestionSection, aggiornarlo per verificare "Pattern proposti" (heading section 1) e "Transazioni identiche" (heading section 2).
       - Se non esiste un test dedicato ai heading, aggiungerne uno nominato 'SUMUI-02: SuggestionSection shows distinct headings for regex and single-cat groups' che verifica la presenza di "Pattern proposti" e "Categorizzale manualmente dalla pagina Spese" nel rendering.

    3. Verificare che i test su "Suggerimenti pattern (X)" (il vecchio heading con count) siano aggiornati o rimossi — il nuovo heading regex è "Pattern proposti" senza count.

    Eseguire yarn test tests/import-suggestions-page.test.tsx dopo le modifiche.
  </action>

  <verify>
    <automated>yarn test tests/import-suggestions-page.test.tsx 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - yarn test tests/import-suggestions-page.test.tsx esce con codice 0 (tutti i test passano)
    - grep -c "rilevati dalle transazioni non categorizzate" tests/import-suggestions-page.test.tsx restituisce almeno 1 (test SUMUI-03 aggiornato)
    - grep -c "Crea pattern per categorizzare automaticamente transazioni simili nelle prossime importazioni" tests/import-suggestions-page.test.tsx restituisce 0 (vecchio testo rimosso dalla assertion)
    - Il test per il copy della suggestions page (D-08 copy) passa
  </acceptance_criteria>

  <done>Test della suggestions page aggiornati al copy SUMUI-03 e ai nuovi heading SUMUI-02; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nessuna nuova superficie | Modifiche a copy UI e struttura heading; auth guard suggestions page invariato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Information Disclosure | suggestions/page.tsx — copy che menziona "piattaforma" | accept | Il riferimento alla piattaforma è già implicito nel flusso; nessun dato sensibile esposto dal copy |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | Nessun pacchetto installato in questa phase |
</threat_model>

<verification>
yarn test tests/import-suggestions-page.test.tsx deve essere green.
yarn check:language deve passare dopo le modifiche al copy UI.
npx tsc --noEmit deve uscire con zero errori.
Navigare su /import/[fileId]/suggestions (con fileId valido) e verificare manualmente: paragrafo SUMUI-03 visibile sotto l'h1, due section con heading "Pattern proposti" e "Transazioni identiche" ciascuna con il proprio testo introduttivo.
</verification>

<success_criteria>
- suggestions/page.tsx contiene il paragrafo SUMUI-03 che comunica scope platform + entry point ricontrolla — per SUMUI-03, D-01, D-02
- SuggestionSection ha heading distinti con intro text per i due gruppi — per SUMUI-02, D-03
- Single-cat items rimangono read-only senza CTA — per D-04
- aria-label sulle section preservati invariati
- Test suite import-suggestions-page green
</success_criteria>

## Artifacts this phase produces

**Symbols modified (non creati):**
- `SuggestionSection` component in `components/import/suggestion-section.tsx` — heading h2 aggiornati, p intro text aggiunti per entrambe le section
- `suggestions/page.tsx` RSC — paragrafo p.text-muted-foreground aggiornato con copy SUMUI-03

**Copy prodotto (Italian product surface):**
- Section 1 heading: "Pattern proposti"
- Section 1 intro: "Crea un pattern per categorizzare automaticamente queste transazioni nelle importazioni future."
- Section 2 heading: "Transazioni identiche ({count})"
- Section 2 intro: "Queste descrizioni compaiono più volte ma non generano un pattern automatizzabile. Categorizzale manualmente dalla pagina Spese."
- SUMUI-03 sub-heading: "I suggerimenti sono stati rilevati dalle transazioni non categorizzate di questa piattaforma dopo l'importazione. Puoi ricontrollare i pattern in qualsiasi momento dal tab Importazioni."

<output>
Create `.planning/phases/55-import-summary-ux/55-03-SUMMARY.md` when done
</output>
