---
phase: 57-pdf-import-trade-republic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - scripts/seed-data.ts
  - tests/fixtures/import/trade-republic-sample.pdf
  - tests/trade-republic-pdf-parser.test.ts
autonomous: false
requirements: [PDF-04, PDF-05]
user_setup: []

must_haves:
  truths:
    - "A real Trade Republic PDF fixture exists at tests/fixtures/import/trade-republic-sample.pdf containing the TRANSAZIONI SUL CONTO section, at least one PANORAMICA TRANSAZIONI mirror section, at least 3 movement rows, a running-balance column, and at least one savings-plan row with a quantity: token"
    - "X-coordinate column boundary constants are calibrated from the real fixture and committed as named constants with a comment citing the fixture"
    - "scripts/seed-data.ts contains a Trade Republic platform row (id 8) and a matching import_format_version row with synthetic headers data, descrizione, importo_entrata, importo_uscita and delimiter ',' (per D-08)"
    - "The TR import_format_version uses amountType 'separate' with positiveAmountColumn 'importo_entrata' and negativeAmountColumn 'importo_uscita' (per D-07)"
    - "The TR import_format_version carries descriptionStripPattern that strips the quantity: token and its value (per D-06, PDF-05)"
    - "yarn db:seed re-run is idempotent and inserts the TR platform and format version without duplicating existing rows"
  artifacts:
    - tests/fixtures/import/trade-republic-sample.pdf
    - tests/trade-republic-pdf-parser.test.ts
    - scripts/seed-data.ts
  key_links:
    - "headerSignatureFor in scripts/seed.ts joins [timestampColumn, descriptionColumn, amountColumn, positiveAmountColumn, negativeAmountColumn] with delimiter — for TR this MUST yield 'data,descrizione,importo_entrata,importo_uscita'"
    - "unpdf must be a runtime dependency in package.json for Wave 2 (parser) to import it"
---

<objective>
Wave 0 prerequisite for PDF import. Install unpdf, obtain a real Trade Republic PDF fixture, calibrate X-coordinate column boundaries from it, scaffold the parser test file, and add the Trade Republic platform + import_format_version to the canonical seed data with synthetic headers.

Purpose: Every downstream plan depends on (a) a real fixture to calibrate against and assert behavior, (b) the unpdf dependency, and (c) a seeded TR format version the detector can match against. Without a real fixture the X-coordinate thresholds are guesses (research Open Question 1, Anti-Pattern: hardcoding X without a fixture).
Output: unpdf installed; fixture committed; calibration constants module; failing parser test scaffold; TR seed rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pdf-import-trade-republic/57-CONTEXT.md
@.planning/phases/57-pdf-import-trade-republic/57-RESEARCH.md
@docs/adr/0014-pdf-import-per-bank-template.md
@scripts/seed-data.ts
@scripts/seed.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Obtain a real Trade Republic PDF fixture</name>
  <action>Human supplies a Trade Republic statement PDF at tests/fixtures/import/trade-republic-sample.pdf (see how-to-verify). Claude cannot generate a real bank PDF with authentic X-coordinate layout, so this is a human-only step.</action>
  <what-built>Nothing automated yet — this requires a human to supply a real Trade Republic statement PDF. Claude cannot generate a real bank PDF with authentic X-coordinate layout.</what-built>
  <how-to-verify>
    1. Export or download a Trade Republic account statement PDF (Italian locale). Redact personal data if needed but PRESERVE the column layout, the "TRANSAZIONI SUL CONTO" section, the "PANORAMICA TRANSAZIONI" section, the running-balance column, and at least one savings-plan row containing a "quantity:" token.
    2. Save it to tests/fixtures/import/trade-republic-sample.pdf
    3. Confirm the file is under 5 MB and has fewer than 50 pages.
    If no real PDF is available, instead provide a faithful synthetic PDF that reproduces the same column X positions, both sections, the balance column, and a quantity: row — and say so in the resume signal so the calibration task knows the source.
  </how-to-verify>
  <resume-signal>Type "fixture ready" once tests/fixtures/import/trade-republic-sample.pdf exists, and note whether it is a real export or synthetic.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install unpdf and calibrate X-coordinate column boundaries from the fixture</name>
  <files>package.json, yarn.lock, tests/trade-republic-pdf-parser.test.ts</files>
  <read_first>
    - package.json (confirm unpdf absent; confirm yarn workspace)
    - .planning/phases/57-pdf-import-trade-republic/57-RESEARCH.md (Pattern 1 unpdf token extraction; Pattern 2 positional column assignment; Q1 unpdf API; Pitfall 1 Y-coordinate inversion)
    - tests/import-detector.test.ts (existing test style — describe/it, vitest imports, fixture loading conventions)
  </read_first>
  <action>
    Run `yarn add unpdf` (research Package Legitimacy Audit: approved, unjs/unpdf v1.6.2, MIT). Verify it lands in dependencies (not devDependencies) since the parser runs at runtime in lib/services.
    Write a calibration probe inside tests/trade-republic-pdf-parser.test.ts (vitest) that loads tests/fixtures/import/trade-republic-sample.pdf, calls getDocumentProxy(new Uint8Array(bytes)) then extractTextItems(pdf), and logs each StructuredTextItem str and x for the rows in the TRANSAZIONI SUL CONTO section. Use the logged x values to determine the left-edge x bands for the four columns plus the running-balance column. Sort items by y descending for reading order (Pitfall 1: PDF y=0 is page bottom; do NOT sort ascending).
    Record the calibrated thresholds as named exported constants in the test file scaffold for now (they migrate into the parser in Wave 2): CREDIT_X_MIN, CREDIT_X_MAX, DEBIT_X_MIN, DEBIT_X_MAX, BALANCE_X_MIN, with a comment line citing tests/fixtures/import/trade-republic-sample.pdf as the calibration source. Do NOT invent the numbers — derive them from the actual logged x positions of this fixture.
    Leave the test file's behavioral tests as failing/skipped placeholders that name the Wave 2 expectations (section extraction, sign attribution, balance chain pass, balance chain fail, quantity strip) so Wave 2 has a RED target. Mark behavioral placeholders with it.todo or it.skip so this plan's verify can pass on the calibration probe alone.
  </action>
  <verify>
    <automated>yarn test tests/trade-republic-pdf-parser.test.ts 2>&1 | grep -Eq "passed|todo|skipped" &amp;&amp; node -e "const p=require('./package.json'); if(!p.dependencies||!p.dependencies.unpdf) throw new Error('unpdf not in dependencies'); console.log('unpdf '+p.dependencies.unpdf)"</automated>
  </verify>
  <done>unpdf is in package.json dependencies; tests/trade-republic-pdf-parser.test.ts loads the fixture, logs token x positions, and exports calibrated X-boundary constants with a comment citing the fixture; behavioral expectations exist as todo/skip placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Trade Republic platform and import_format_version with synthetic headers</name>
  <files>scripts/seed-data.ts</files>
  <read_first>
    - scripts/seed-data.ts (platforms array ends id 7 Fineco at line ~941; importFormatVersions array, Fineco entry platformId 7 at lines ~1053-1071 is the amountType 'separate' reference)
    - scripts/seed.ts (headerSignatureFor lines 59-69 — confirms which columns form the signature and that they join with delimiter; the insert mapping lines 104-129)
    - lib/db/schema.ts (importFormatVersion columns: delimiter notNull, amountType, positiveAmountColumn, negativeAmountColumn, timestampColumn notNull, descriptionColumn notNull, descriptionStripPattern nullable; platform columns id/name/slug/country)
  </read_first>
  <action>
    Append a Trade Republic platform entry to the `platforms` array in scripts/seed-data.ts: id 8, name "Trade Republic", slug "trade-republic", country "IT". This is additive — do NOT edit existing platform rows (project rule: seeds are additive; onConflictDoNothing makes re-seed idempotent).
    Append a matching entry to the `importFormatVersions` array: platformId 8, version 1, delimiter "," (placeholder — the column is NOT NULL per Pitfall 2; "," is the join char used by headerSignatureFor so synthetic-header matching works), descriptionColumn "descrizione", amountType "separate" as AmountType, amountColumn null, positiveAmountColumn "importo_entrata", negativeAmountColumn "importo_uscita", timestampColumn "data", dateFormat null (parseBankDate auto-handles Italian month names per D-07), dateReplace false, decimalReplace false, multiplyBy 1, descriptionStripPattern set to the regex that strips the quantity token and its value (research Q7 recommends quantity:\s*[\d.,]+\s* — write it correctly escaped for a TS string literal so it strips "quantity: 3" entirely, per D-06 and PDF-05), notes "Initial Trade Republic PDF import contract".
    The four synthetic header names (data, descrizione, importo_entrata, importo_uscita) are Claude's discretion per D-08 and must exactly match the headers the Wave 2 parser will emit and the columns referenced above so headerSignatureFor yields "data,descrizione,importo_entrata,importo_uscita".
    Do NOT add a parserKind column or any new schema field (D-08 defers it; deferred idea). Do NOT create a migration — the TR row reuses existing importFormatVersion columns.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/seed-data.ts','utf8');if(!/slug:\s*\"trade-republic\"/.test(s))throw new Error('TR platform missing');if(!/positiveAmountColumn:\s*\"importo_entrata\"/.test(s))throw new Error('TR positive col missing');if(!/negativeAmountColumn:\s*\"importo_uscita\"/.test(s))throw new Error('TR negative col missing');if(!/timestampColumn:\s*\"data\"/.test(s))throw new Error('TR timestamp col missing');console.log('TR seed rows present')"</automated>
  </verify>
  <done>scripts/seed-data.ts has a Trade Republic platform (id 8, slug trade-republic) and an importFormatVersions entry (platformId 8, amountType separate, importo_entrata/importo_uscita columns, timestamp data, quantity strip pattern); existing rows are untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Run migration and seed; verify TR format version persisted</name>
  <files>scripts/seed-data.ts</files>
  <read_first>
    - CLAUDE.md (Seed Data run order: db:migrate, db:seed, db:seed-extras, db:seed-patterns; never drizzle-kit push in prod)
    - scripts/seed.ts (the seed runner reads platforms and importFormatVersions and computes headerSignature)
  </read_first>
  <action>
    No schema change was made, so no `drizzle-kit generate` is needed. Run `yarn db:seed` against the local/dev database. onConflictDoNothing makes this idempotent — existing platforms and format versions are preserved; the new TR rows (id 8) are inserted.
    Confirm the TR import_format_version was created with headerSignature equal to "data,descrizione,importo_entrata,importo_uscita" (headerSignatureFor joins the five candidate columns, filtering nulls, with delimiter ","; for TR the non-null columns in signature order are timestampColumn data, descriptionColumn descrizione, positiveAmountColumn importo_entrata, negativeAmountColumn importo_uscita — amountColumn is null and filtered out).
    Do not run db:seed-extras or db:seed-patterns for this plan — no taxonomy migration or regex pattern changes are introduced.
  </action>
  <verify>
    <automated>yarn db:seed 2>&1 | grep -Eq "format versions inserted|seed_succeeded"</automated>
  </verify>
  <done>yarn db:seed completes successfully and reports format versions inserted; the Trade Republic platform and import_format_version exist in the dev database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→repo (fixture file) | A binary PDF fixture is committed to the repo; it must not contain real personal financial data |
| package manager → dependency tree | `yarn add unpdf` pulls a third-party package into runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01-01 | Information Disclosure | tests/fixtures/import/trade-republic-sample.pdf | medium | mitigate | Task 1 instructs redaction of personal data while preserving layout; reviewer confirms no real PII before commit |
| T-57-01-SC | Tampering | `yarn add unpdf` | high | mitigate | Package Legitimacy Audit in RESEARCH.md verdict OK (unjs/unpdf, 1.3M/wk, 3yr, MIT) — APPROVED, no checkpoint required |
</threat_model>

<verification>
- unpdf present in package.json dependencies
- tests/fixtures/import/trade-republic-sample.pdf exists and is a valid PDF under 5 MB
- X-boundary constants in tests/trade-republic-pdf-parser.test.ts are derived from the fixture (comment cites it)
- scripts/seed-data.ts TR rows present and additive
- yarn db:seed idempotent and TR format version persisted with correct headerSignature
</verification>

<success_criteria>
unpdf installed; real TR PDF fixture committed; X-coordinate column boundaries calibrated and exported; TR platform + import_format_version seeded with synthetic headers data/descrizione/importo_entrata/importo_uscita and the quantity strip pattern; downstream waves have a concrete fixture and seeded format to build against.
</success_criteria>

<output>
Create `.planning/phases/57-pdf-import-trade-republic/57-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `tests/fixtures/import/trade-republic-sample.pdf` (NEW) — real/synthetic TR statement fixture
- `tests/trade-republic-pdf-parser.test.ts` (NEW, scaffold) — calibration probe + exported X-boundary constants `CREDIT_X_MIN`, `CREDIT_X_MAX`, `DEBIT_X_MIN`, `DEBIT_X_MAX`, `BALANCE_X_MIN`; todo/skip behavioral placeholders
- `scripts/seed-data.ts` (MODIFIED) — TR platform row (id 8, slug `trade-republic`) appended to `platforms`; TR `importFormatVersions` entry (platformId 8, `amountType: "separate"`, `positiveAmountColumn: "importo_entrata"`, `negativeAmountColumn: "importo_uscita"`, `timestampColumn: "data"`, `descriptionColumn: "descrizione"`, quantity strip pattern)
- `package.json` / `yarn.lock` (MODIFIED) — `unpdf` runtime dependency
