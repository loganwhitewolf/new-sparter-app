---
phase: 47-taxonomy-seed-rework
plan: 05
type: execute
wave: 4
depends_on: ["47-04"]
files_modified:
  - tests/category-settings-seed.ts
  - .planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03]
tags: [R-FN-03, build-gate, phase-close]

must_haves:
  truths:
    - "R-FN-03 todos in category-settings-seed.ts enabled and GREEN"
    - "Full yarn test + yarn build pass — phase gate"
    - "47-VALIDATION.md nyquist_compliant true"
  artifacts:
    - path: "tests/category-settings-seed.ts"
      provides: "R-FN-03 nature assignment assertions on seed-data"
  key_links:
    - from: "category-settings-seed R-FN-03 tests"
      to: "scripts/seed-data.ts subCategories[].natureId"
      via: "import subCategories + NATURE_BY_ID map"
      pattern: "natureId"
---

<objective>
Abilitare test R-FN-03 e chiudere il gate di fase: suite completa + build green (D-05 done = scripts + tests, no DB).

Purpose: Conferma TAX-01/02/03 end-to-end a livello statico; Nyquist sign-off su 47-VALIDATION.md.

Output: Phase 47 pronta per `/gsd-verify-work 47` e handoff Phase 48.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-taxonomy-seed-rework/47-04-SUMMARY.md
@.planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md
@tests/category-settings-seed.ts
@tests/seed-taxonomy.test.ts
@tests/seed-extras-steps.test.ts
@scripts/seed-data.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

- **`tests/category-settings-seed.ts`** — R-FN-03 block enabled:
  - Import `subCategories`, `natures` from seed-data
  - Replace `it.todo` with real tests: at least one sub with natureId 3 (essential), one with 4 (discretionary), transfer subs with natureId 6 (D-13 supersedes old "ignore null" todo)
  - Assert via NATURE_BY_ID map, not stale FlowNature on row

- **`.planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md`** — update frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, task status rows ✅

- **Phase gate evidence:** `yarn test` + `yarn build` green (no db:seed)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Enable R-FN-03 seed nature assignment tests</name>
  <read_first>
    - tests/category-settings-seed.ts (R-FN-03 scaffold L238-254)
    - scripts/seed-data.ts (subCategories with natureId)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (enable pattern Wave 4)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-11, D-13)
  </read_first>
  <action>
    In tests/category-settings-seed.ts, enable the three `it.todo` tests under `seed nature assignment (R-FN-03)`.

    Add imports: `subCategories`, `natures` from `../scripts/seed-data`. Build `NATURE_BY_ID` map from natures array.

    Test 1: `at least 1 system subcategory has nature essential` — expect some sub with natureId where NATURE_BY_ID[natureId] === 'essential'

    Test 2: `at least 1 system subcategory has nature discretionary` — same for discretionary

    Test 3 (update intent per D-13): `transfer subcategories have nature transfer` — filter subs with slug in ['trasferimento-tra-conti','addebito-carta-di-credito','contante']; assert all have natureId 6 / code transfer. Remove obsolete "ignore-category null" wording.

    Keep existing FlowNature regression guard test unchanged.

    Run yarn test tests/category-settings-seed.ts
  </action>
  <verify>
    <automated>yarn test tests/category-settings-seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero it.todo remaining in R-FN-03 describe block
    - All three nature assignment tests pass
    - Transfer subs assert natureId 6 (D-13)
    - Tests import from seed-data, not inline mocks
  </acceptance_criteria>
  <done>R-FN-03 enabled and GREEN (TAX-03 validation dimension).</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full test suite + build + validation sign-off</name>
  <read_first>
    - .planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-05 no DB apply)
    - CLAUDE.md (yarn check:language if touching comments/strings)
  </read_first>
  <action>
    Run phase gate commands in order:
    1. `yarn test tests/seed-taxonomy.test.ts`
    2. `yarn test tests/seed-extras-steps.test.ts` (if exists from Plan 04)
    3. `yarn test tests/category-settings-seed.ts`
    4. `yarn test` (full suite)
    5. `yarn build`

    If any failure, fix within Phase 47 scope only (seed/tests). Do NOT run db:seed or db:seed-extras (D-05).

    Update `.planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md`:
    - frontmatter: nyquist_compliant: true, wave_0_complete: true
    - Mark per-task rows ✅ green
    - Wave 0 checklist items checked

    Run `yarn check:language` if any developer-facing English strings were added in this plan wave.
  </action>
  <verify>
    <automated>yarn test && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - yarn test exits 0
    - yarn build exits 0
    - 47-VALIDATION.md nyquist_compliant: true
    - No drizzle-kit generate / db:seed / db:seed-extras executed (D-05)
    - directions/natures blocks in seed-data.ts still untouched from Phase 46 (D-04)
  </acceptance_criteria>
  <done>Phase 47 gate green; ready for verify-work and Phase 48 migration apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test/build gate only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | accept | No new attack surface |

T-47-SC: N/A
</threat_model>

<verification>
yarn test && yarn build green. 47-VALIDATION.md signed off. All TAX-* requirements covered by automated tests.
</verification>

<success_criteria>
- R-FN-03 tests enabled
- Full CI-equivalent gate passes
- TAX-01, TAX-02, TAX-03 validated statically
- Phase 47 complete without DB apply
</success_criteria>

<output>
Create `.planning/phases/47-taxonomy-seed-rework/47-05-SUMMARY.md` when done.
</output>
