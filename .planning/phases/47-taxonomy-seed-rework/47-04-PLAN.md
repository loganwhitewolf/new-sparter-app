---
phase: 47-taxonomy-seed-rework
plan: 04
type: execute
wave: 3
depends_on: ["47-03"]
files_modified:
  - scripts/seed-extras.ts
  - tests/seed-extras-steps.test.ts
autonomous: true
requirements: [TAX-02, TAX-03]
tags: [seed-extras, backfill, deployed-db, merge-migration]

must_haves:
  truths:
    - "setSubcategoryNature step 1 is no-op (D-06) — no UPDATE on removed nature column"
    - "STEPS 6+ append v2 remap: insert, migrate merges, rename, deactivate, backfill nature_id"
    - "Backfill resolves nature by code via SELECT from nature table (D-12)"
    - "user_subcategory_override.nature_id backfill step exists"
    - "seed-extras.ts compiles; STEPS registry test passes"
  artifacts:
    - path: "scripts/seed-extras.ts"
      provides: "STEPS 6+ v2 deployed-DB transforms + step 1 no-op"
      contains: "v2-backfill-nature-id"
    - path: "tests/seed-extras-steps.test.ts"
      provides: "Static STEPS registry smoke test"
  key_links:
    - from: "v2-backfill-nature-id step"
      to: "nature.code lookup"
      via: "sql subquery SET nature_id = (SELECT id FROM nature WHERE code = ...)"
      pattern: "SELECT id FROM nature WHERE code"
---

<objective>
Riparare step 1 rotto (D-06) e appendere STEPS 6+ in `seed-extras.ts` per DB deployati v1: remap slug, merge expense/pattern pointers, backfill `nature_id` (D-08, D-09, D-12).

Purpose: TAX-02 dissolutions/renames su righe esistenti; TAX-03 additive backfill senza editare STEPS 2-5 bodies salvo build-survival (A2).

Output: seed-extras pronto per Phase 48 operator run; no DB apply in Phase 47 (D-05).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-taxonomy-seed-rework/47-03-SUMMARY.md
@.planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md
@.planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md
@.planning/phases/47-taxonomy-seed-rework/47-RESEARCH.md (STEP design table, ordering)
@.planning/nature-remapping-WORKING.md
@scripts/seed-extras.ts
@tests/fixtures/v2-taxonomy-manifest.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

- **Step 1 no-op (D-06, D-09):** `setSubcategoryNature` body replaced with log-only; keep STEPS name `set-subcategory-nature`; retain `NATURE_SLUGS` as documentation comment only

- **STEPS 6+ (append only, D-08):**
  - `v2-insert-categories-subcategories` — INSERT net-new cats/subs absent from v1 (pensioni-e-sussidi, servizi-professionali, animali, cultura-e-tempo-libero, entrate-straordinarie, new subs) with slug-exists guards
  - `v2-migrate-merges-out` — OUT wrapper dissolutions + spesa/ristorazione/shopping merges using step 3 idiom (expense → pattern dedupe → deactivate source)
  - `v2-migrate-merges-in-allocation-transfer` — IN dividend merge, ALLOCATION titoli merge, TRANSFER bonifici→trasferimento-tra-conti; extend step 4 idempotently (no duplicate)
  - `v2-rename-categories-subcategories` — category/sub renames with idempotent rename guard (target exists → deactivate source)
  - `v2-deactivate-pruned` — isActive=false on DROPPED slugs + dissolved cats AFTER migrations
  - `v2-backfill-nature-id` — Record&lt;natureCode, slugs[]&gt; UPDATE via `(SELECT id FROM nature WHERE code = ...)` (D-12)
  - `v2-backfill-override-nature-id` — UPDATE user_subcategory_override FROM sub_category join

- **Build-survival (A2 discretion):** If STEPS 3-5 contain `SET nature =` raw SQL that fails compile/runtime post-Phase 48, minimal swap to no-op OR `SET nature_id` — document in SUMMARY; do NOT rewrite step 3-4 merge logic bodies otherwise (D-09)

- **`tests/seed-extras-steps.test.ts`** — assert STEPS registry includes step names: v2-backfill-nature-id, v2-backfill-override-nature-id, v2-deactivate-pruned
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: No-op step 1 + append insert/merge/rename STEPS 6-9</name>
  <read_first>
    - scripts/seed-extras.ts (setSubcategoryNature L206-223, reorganizeSpesaSubcategories L239-346, reorganizeTransferRimborsiCategories L352-509, STEPS registry L534+)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-06, D-08, D-09, D-10)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (merge migration core, rename guard, INSERT idiom)
    - tests/fixtures/v2-taxonomy-manifest.ts (merge/dissolve slug lists)
    - .planning/nature-remapping-WORKING.md
  </read_first>
  <action>
    Per D-06 replace `setSubcategoryNature` body with no-op log: `set-subcategory-nature: no-op (Phase 47 — superseded by v2-backfill-nature-id)`. Remove raw SQL `UPDATE sub_category SET nature =`. Keep function signature and STEPS entry name.

    Append new step functions AFTER existing steps 2-5 (D-09 — do not edit steps 2-5 bodies except compile failure):

    **`v2InsertCategoriesSubcategories`:** INSERT categories/subs that exist in v2 seed-data manifest but not in v1 DB. Use slug-exists SELECT guard before INSERT (pattern from step 4 L397-420). categoryId from resolved category slug/id. Do NOT hardcode nature_id integers — backfill step sets them (D-12).

    **`v2MigrateMergesOut`:** Implement OUT merges from working doc using step 3 four-step loop per slug pair: resolve IDs by slug → UPDATE expense → pattern conflict DELETE + UPDATE → deactivate source. Include wrapper dissolution migrations (assicurazioni→distributed subs, abbonamenti→distributed, famiglia→distributed). Log skip when source/target absent (idempotent).

    **`v2MigrateMergesInAllocationTransfer`:** IN merges (dividendi-*→dividendi), ALLOCATION (azioni/obbligazioni/fondi→titoli-e-fondi), TRANSFER (bonifico-* + ricariche→trasferimento-tra-conti, prelievo-contante→contante rename). Extend step 4 work idempotently — check before duplicating transfer inserts.

    **`v2RenameCategoriesSubcategories`:** Category renames (bollette-e-utilita→utenze, income-finanziari→rendite, tasse→imposte-e-oneri, regali→regali-e-donazioni, ignore→trasferimenti). Sub renames per working doc. Use rename guard: if target slug exists, deactivate source instead of rename (PATTERNS.md C).

    Register all four in STEPS array after step 5. English developer comments; Italian slug strings in data.

    Scope guard: `isNull(subCategory.userId)` on all system-row mutations.
  </action>
  <verify>
    <automated>grep -q 'no-op' scripts/seed-extras.ts && grep -q 'v2-insert-categories-subcategories' scripts/seed-extras.ts && grep -q 'v2-migrate-merges-out' scripts/seed-extras.ts && grep -q 'v2-migrate-merges-in-allocation-transfer' scripts/seed-extras.ts && grep -q 'v2-rename-categories-subcategories' scripts/seed-extras.ts && yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - setSubcategoryNature is no-op; no SET nature = in that function (D-06)
    - STEPS registry includes v2-insert, v2-migrate-merges-out, v2-migrate-merges-in-allocation-transfer, v2-rename steps
    - Steps 2-5 bodies unchanged unless tsc forced edit (document if so)
    - All new SQL uses parameterized drizzle sql template or typed update — no string concat of operator input
    - Merge order: migrate before deactivate in merge steps (not in deactivate step yet)
  </acceptance_criteria>
  <done>Step 1 no-op + insert/merge/rename STEPS appended (D-08 first half, TAX-02).</done>
</task>

<task type="auto">
  <name>Task 2: Append deactivate + nature_id backfill STEPS + registry test</name>
  <read_first>
    - scripts/seed-extras.ts (STEPS registry, step 1 from Task 1)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (backfill D-12, override backfill F)
    - tests/fixtures/v2-taxonomy-manifest.ts (DISSOLVED, DROPPED, V2_SUBCATEGORY_MANIFEST nature codes)
    - lib/db/schema.ts (userSubcategoryOverride.natureId)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Append remaining STEPS:

    **`v2DeactivatePruned`:** AFTER all migrations, set isActive=false on DISSOLVED_CATEGORY_SLUGS (categories) and DROPPED_SUBCATEGORY_SLUGS + merged-away source slugs (subs). Use inArray + isNull(userId). Log rowCount.

    **`v2BackfillNatureId` (D-12, TAX-03):** Define `NATURE_SLUG_MAP: Record<string, string[]>` mapping nature.code → slug list sourced from V2_SUBCATEGORY_MANIFEST / working doc. For each code, execute:
    `UPDATE sub_category SET nature_id = (SELECT id FROM nature WHERE code = ${code}) WHERE slug = ANY(${slugs}) AND user_id IS NULL`
    Never hardcode nature_id integers. Include transfer subs (D-13). Run after renames complete.

    **`v2BackfillOverrideNatureId`:** 
    `UPDATE user_subcategory_override uso SET nature_id = sc.nature_id FROM sub_category sc WHERE uso.sub_category_id = sc.id AND sc.user_id IS NULL AND uso.nature_id IS NULL AND sc.nature_id IS NOT NULL`

    Export STEPS name array or add test-only export if needed for registry test (minimal surface).

    Create `tests/seed-extras-steps.test.ts` importing STEPS names (export `STEP_NAMES` from seed-extras if needed — smallest change). Assert includes: `v2-backfill-nature-id`, `v2-backfill-override-nature-id`, `v2-deactivate-pruned`, `set-subcategory-nature`.

    **A2 build-survival:** Grep steps 3-5 for `SET nature =`. If present and would fail on fresh DB, replace with no-op log OR nature_id equivalent — minimal line change only; note in SUMMARY.

    Do NOT run yarn db:seed-extras (D-05).
  </action>
  <verify>
    <automated>yarn test tests/seed-extras-steps.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - STEPS include v2-deactivate-pruned, v2-backfill-nature-id, v2-backfill-override-nature-id
    - Backfill uses nature.code lookup, not hardcoded ids (D-12)
    - Deactivate runs after merge steps in registry order
    - tests/seed-extras-steps.test.ts passes or STEPS names verified via grep gate
    - seed-extras.ts typechecks clean
    - No db:seed-extras executed (D-05)
  </acceptance_criteria>
  <done>Full v2 seed-extras chain authored; TAX-03 additive backfill ready for Phase 48 apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → PostgreSQL | seed-extras runs with DATABASE_URL; mutates taxonomy rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Tampering | seed-extras SQL | mitigate | Drizzle parameterized sql`` templates; isNull(userId) scope guard on system rows |
| T-47-05 | DoS | Production seed misfire | accept | Existing PRODUCTION_MIGRATION_CONFIRM gate in db-config (operator discipline) |
| T-47-06 | Integrity | Double-apply STEPS | mitigate | Idempotent slug guards + skip logs from step 3-4 patterns |

T-47-SC: N/A
</threat_model>

<verification>
seed-extras compiles. Step 1 no-op. STEPS 6+ registered. Registry test passes. No live DB run.
</verification>

<success_criteria>
- D-06 step 1 fixed
- D-08/D-09 additive STEPS complete
- D-12 code-based backfill
- TAX-02 deployed transforms + TAX-03 backfill steps authored
</success_criteria>

<output>
Create `.planning/phases/47-taxonomy-seed-rework/47-04-SUMMARY.md` when done.
</output>
