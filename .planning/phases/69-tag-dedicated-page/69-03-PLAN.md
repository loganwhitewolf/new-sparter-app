---
phase: 69-tag-dedicated-page
plan: 03
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - components/tags/tag-settings-panel.tsx
  - tests/tag-settings-panel.test.tsx
  - components/dashboard/tag-ranking-list.tsx
  - lib/actions/tags.ts
autonomous: false
requirements: [TAG-11, TAG-12]
must_haves:
  truths:
    - "/tags is an index: each tag in the list is a link to /tags/[id]; the inline TagDetailView (KPI + count + tx list) is gone; CreateTagDialog, the active/archived grouping and archived badges remain (D2, TAG-12)."
    - "/dashboard/tags ranking links the tag name (primary) to /tags/[id]; a secondary 'vedi transazioni' affordance to /transactions?tag={id} may remain (D6, TAG-12)."
    - "getTagDetailAction (and TagDetailResult) are removed from lib/actions/tags.ts — the dedicated RSC reads the DAL directly, so the client action is orphaned (D2); tsc proves no dangling references."
    - "updateTagAction and archiveTagAction additionally revalidatePath(APP_ROUTES.tagDetail(id)) so an in-place edit/archive on the page reflects without a hard reload; the user stays on the page after archive and the Archiviato badge updates (D5)."
    - "The dashboard /transactions?tag= wiring is untouched — that removal is Phase 70 (scope fence)."
  artifacts:
    - "components/tags/tag-settings-panel.tsx — index list of links, no detail pane."
    - "components/dashboard/tag-ranking-list.tsx — name link re-pointed to tagDetail()."
    - "lib/actions/tags.ts — getTagDetailAction/TagDetailResult removed; detail-route revalidation added."
    - "tests/tag-settings-panel.test.tsx — updated for the index (links, no detail pane)."
  key_links:
    - "/tags list item → tagDetail(tag.id) and /dashboard/tags name → tagDetail(item.tagId): the two entry points (D6, TAG-12)."
    - "updateTagAction/archiveTagAction → revalidatePath(tagDetail(id)): in-place refresh of the dedicated page (D5)."
---

<objective>
Complete Phase 69: turn `/tags` into an index whose items link to `/tags/[id]` and remove the now-
orphaned inline detail (D2), re-point the `/dashboard/tags` ranking name link to `/tags/[id]` (D6),
remove the orphaned `getTagDetailAction`/`TagDetailResult` and add detail-route revalidation to the
edit/archive actions (D5), then verify the whole feature end-to-end with a human checkpoint —
reachability from both entry points (TAG-12) and cross-query reconciliation of the page totals
against /dashboard/tags (TAG-07).

Scope fence (LOCKED): do NOT touch the dashboard `/transactions?tag=` filter wiring — that removal
is Phase 70 (TAG-13). The transactions-table `?tag=` filter stays.

Purpose: make the dedicated page the canonical, reachable view of a tag and retire the superseded
inline surface.
Output: both entry points live, orphaned action gone, in-place edit/archive, feature verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-tag-dedicated-page/CONTEXT.md
@.planning/phases/69-tag-dedicated-page/69-01-SUMMARY.md

# The panel to convert to an index (remove TagDetailView + its getTagDetailAction fetch; list items → links)
@components/tags/tag-settings-panel.tsx
# The dashboard ranking whose primary name link is re-pointed (keep the optional secondary transactions link)
@components/dashboard/tag-ranking-list.tsx
# The actions file: remove getTagDetailAction/TagDetailResult, add detail-route revalidation to update/archive
@lib/actions/tags.ts
# Route helper (tagDetail added in 69-01) + APP_ROUTES
@lib/routes.ts
# Existing panel test to update for the index shape
@tests/tag-settings-panel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert /tags panel into an index of links (remove inline TagDetailView)</name>
  <files>components/tags/tag-settings-panel.tsx, tests/tag-settings-panel.test.tsx</files>
  <action>
Refold the inline detail into the dedicated page (D2). In components/tags/tag-settings-panel.tsx:

- Delete the `TagDetailView` component, its `getTagDetailAction` import, the on-demand fetch effect, and all the KPI/count/tx-list formatting helpers that existed only to serve it (StatCard, the amount/date formatters, tone/count-label helpers) — that rendering now lives in tag-detail-report.tsx.
- Remove the selection state (`selectedId`/`selectedTag`) and the two-column `grid-cols-[200px_1fr]` layout with its right-hand detail pane. Replace `TagListItem`'s `<button onSelect>` with a Next.js `<Link href={tagDetail(tag.id)}>` (import `tagDetail` from '@/lib/routes'); keep the item markup (name truncate + "Archiviato" badge). The component becomes a single-column list.
- KEEP: `CreateTagDialog`, the empty-state copy, the active-vs-archived grouping with the "Archiviati" sub-heading, and the archived badges (D2).
- The component may no longer need "use client" (no local state left). If nothing client-only remains, drop the directive so it renders as a server component; otherwise leave it. Use Claude's discretion but keep it building.
- Remove the now-unused EditTagDialog/ArchiveTagDialog import from the panel (they moved to the page header in 69-01) if they are no longer referenced here.

Update tests/tag-settings-panel.test.tsx: the mock of tag-mutation-dialogs and the empty-state / mixed active+archived / archived-badge / "Nessun intervallo" cases that still apply stay; remove the assertions about the selected detail pane rendering Edit + Archive triggers (that pane is gone). Add an assertion that each tag row is a link whose href is `/tags/{id}` (e.g. the rendered HTML contains `href="/tags/1"` and `href="/tags/2"`). Keep it a renderToStaticMarkup test (no jsdom).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/tag-settings-panel.test.tsx</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/eslint components/tags/tag-settings-panel.tsx tests/tag-settings-panel.test.tsx</automated>
    <automated>yarn check:language</automated>
  </verify>
  <done>
/tags renders a single-column index; every tag row links to /tags/[id]; TagDetailView and its inline fetch are gone; CreateTagDialog, active/archived grouping and archived badges remain. The panel test asserts the row links and the retained cases; tsc, vitest, ESLint, check:language clean.
  </done>
  <reversibility rating="reversible">UI refactor of an existing panel; revertible via git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Re-point dashboard entry point and retire the orphaned action</name>
  <files>components/dashboard/tag-ranking-list.tsx, lib/actions/tags.ts</files>
  <action>
Two independent edits, both gated by tsc.

1. components/dashboard/tag-ranking-list.tsx (D6): change the primary tag-name `<Link>` `href` from `/transactions?tag=${item.tagId}` to `tagDetail(item.tagId)` (import `tagDetail` from '@/lib/routes'); update its `aria-label` to reflect "apri il tag" rather than "apri transazioni". A secondary "vedi transazioni" affordance pointing to `/transactions?tag=${item.tagId}` MAY be added but is optional (Claude's discretion). Do NOT remove or alter any dashboard period/`?tag=` filter wiring elsewhere — that is Phase 70 (scope fence).

2. lib/actions/tags.ts (D2 + D5): remove the `getTagDetailAction` function and the `TagDetailResult` type (orphaned once the panel stopped fetching inline — the dedicated RSC calls the DAL directly). Drop now-unused imports from '@/lib/dal/tags' (`getTagDetail`, `TagDetail`, and `resolveOwnedTagId` IF no other export in this file still uses them — leave any that remain in use). Then add `revalidatePath(APP_ROUTES.tagDetail(parsed.data.id))` to BOTH `updateTagAction` and `archiveTagAction`, alongside their existing `revalidatePath(APP_ROUTES.tags)` and `revalidatePath(APP_ROUTES.dashboardTags)` calls, so an in-place edit/archive on the dedicated page refreshes without a hard reload (D5). `createTagAction` needs no detail-route revalidation (no existing detail page for a brand-new tag).
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/vitest run tests/tag-actions.test.ts</automated>
    <automated>./node_modules/.bin/eslint components/dashboard/tag-ranking-list.tsx lib/actions/tags.ts</automated>
    <automated>yarn check:language</automated>
  </verify>
  <done>
The /dashboard/tags name link points to /tags/[id]; the orphaned action and its result type are removed (tsc passing proves no dangling importers remain); updateTagAction and archiveTagAction revalidate the detail route in addition to /tags and /dashboard/tags. The existing tag-actions tests still pass; the dashboard `/transactions?tag=` filter wiring is untouched. tsc, vitest, ESLint, check:language clean.
  </done>
  <reversibility rating="reversible">Link re-point + removal of an unused export + additive revalidation; revertible via git.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The dedicated tag page (/tags/[id]) with reconciled KPIs, per-category CSS-bar breakdown, compact
transaction list and in-header edit/archive, reachable from both /tags (index) and /dashboard/tags
(ranking name link).
  </what-built>
  <how-to-verify>
Run the app (`yarn dev`), sign in to an account that has at least one tag with transactions.
1. Go to /tags — confirm it is a list; click a tag → you land on /tags/{id} showing that tag's page (TAG-12, D2).
2. Go to /dashboard/tags — click a tag's name → you land on the same /tags/{id} page (TAG-12, D6). Note the tag's total on /dashboard/tags.
3. On /tags/{id}, confirm the "Valore finale" KPI equals the total you saw for that tag on /dashboard/tags — they must reconcile (TAG-07). Confirm "Entrate"/"Uscite" are sign-colored, the "{n} transazioni incluse" count matches the number of rows in the transaction list (TAG-08), the "Per categoria" bars render with sign-based color and are sorted largest-first (TAG-09), and the transaction list is date-descending (TAG-10).
4. Click "Modifica", rename the tag, save → the header name updates in place. If the tag is active, click "Archivia", confirm → you stay on the page and an "Archiviato" badge appears (TAG-11, D5).
5. Manually hit /tags/999999 (a non-existent/foreign id) and /tags/abc → both should render the not-found page (D1).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe what did not reconcile / render.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | edit/archive submit a user-supplied tag `id` via updateTagAction/archiveTagAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-05 | Information Disclosure | lib/actions/tags.ts (update/archive) | high | mitigate | Unchanged existing defense: both actions call verifySession() then a service scoped by (userId, tagId), so a foreign id updates/archives zero rows. This plan only adds revalidatePath(tagDetail(id)) — a cache hint, not a data read — introducing no new authorization surface. |
| T-69-06 | Denial of Service | revalidatePath(tagDetail(id)) | low | accept | id is already validated/owned by the action's service call; revalidating a single owned detail path is bounded and cheap. |
| T-69-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `./node_modules/.bin/tsc --noEmit` clean — proves getTagDetailAction/TagDetailResult removal left no dangling importers and the re-pointed links type-check.
- `./node_modules/.bin/vitest run tests/tag-settings-panel.test.tsx tests/tag-actions.test.ts` green.
- ESLint + `yarn check:language` clean on touched files.
- Human-verify checkpoint confirms reachability from both entry points (TAG-12), cross-query reconciliation of the page net against /dashboard/tags (TAG-07), in-place edit/archive (TAG-11), and notFound on bad/foreign ids (D1).
</verification>

<success_criteria>
- /tags is an index linking to /tags/[id]; the inline detail is retired (D2, TAG-12).
- /dashboard/tags name link opens /tags/[id] (D6, TAG-12).
- getTagDetailAction/TagDetailResult removed; edit/archive revalidate the detail route for in-place refresh (D2, D5, TAG-11).
- Dashboard `/transactions?tag=` wiring untouched (Phase 70 scope fence honored).
- End-to-end feature verified by the human checkpoint (TAG-07, TAG-11, TAG-12).
</success_criteria>

<output>
Create `.planning/phases/69-tag-dedicated-page/69-03-SUMMARY.md` when done.
</output>
