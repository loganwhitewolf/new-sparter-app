---
phase: 80-dashboard-accrual-lens
plan: 07
type: execute
wave: 4
depends_on: ["80-04", "80-05", "80-06"]
files_modified:
  - tests/dashboard.spec.ts
autonomous: true
requirements: [LENS-01, LENS-02, LENS-04, LENS-05]

must_haves:
  truths:
    - "The switch is visible and functional (URL updates on click) on /dashboard/overview, /dashboard/categories, and /dashboard/categories/[id]"
    - "The switch is visible but disabled with the note on /dashboard/tags"
    - "/tags/[id] renders with no switch at all and has zero file diff across the whole phase (D-06)"
    - "The lens survives navigation between all three tabbed dashboard sub-routes"
    - "The full real-Postgres regression suite (LENS-03 cash byte-identical + accrual instalment behavior across all 8 lens-aware aggregation functions) is green"
  artifacts: []
  key_links:
    - "End-to-end: LensSwitch click -> URL -> RSC re-render -> DAL ledgerRowSource -> Postgres view -> rendered totals, proven live across all four routes via Playwright"
---

<objective>
Close out Phase 80 with end-to-end Playwright coverage across all four dashboard sub-routes and a
final full-suite regression run. This is the phase's last wave: every DAL function and every route
wired in Plans 80-01 through 80-06 gets exercised together, live, in a browser — proving the
`LensSwitch` control actually renders, flips the URL, and behaves correctly (enabled + functional
on Overview/Categories/CategoryDetail; disabled + noted on Tags; entirely absent on `/tags/[id]`).

Purpose: the individual plans proved their own slice in isolation (unit tests, real-Postgres DAL
regression); this plan proves the four routes compose correctly as ONE coherent global switch
(D-03), and that the phase leaves no gap between the eight migrated DAL functions and the UI
surfaces that call them.
Output: a green Playwright suite covering the lens across all four routes; a green full regression
suite (vitest) confirming LENS-03 one final time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-dashboard-accrual-lens/80-CONTEXT.md
@.planning/phases/80-dashboard-accrual-lens/80-01-SUMMARY.md
@.planning/phases/80-dashboard-accrual-lens/80-04-SUMMARY.md
@.planning/phases/80-dashboard-accrual-lens/80-05-SUMMARY.md
@.planning/phases/80-dashboard-accrual-lens/80-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright coverage for the lens switch across all four dashboard sub-routes</name>
  <files>tests/dashboard.spec.ts</files>
  <read_first>
    tests/dashboard.spec.ts lines 1-30 (openDashboard/openDashboardPath helpers, the
    `test.describe('Dashboard - DASH-NN: ...')` naming convention, the `x-staging-key` header pattern
    every test uses);
    tests/dashboard.spec.ts lines 84-108 (DASH-02's existing tab-navigation + URL-persistence test —
    the exact style/assertions this task's new block follows).
  </read_first>
  <action>
    Add a new `test.describe('Dashboard - LENS: cassa/competenza switch', () => { ... })` block to
    tests/dashboard.spec.ts, following the file's existing `openDashboardPath`/`x-staging-key`
    convention. Add these test cases: (1) "LENS switch renders on /dashboard/overview and flipping
    it updates the URL" — goto `/dashboard/overview`, locate the two switch buttons via
    `page.getByRole('button', { name: 'Competenza' })` (and 'Cassa'), click 'Competenza', assert
    `await expect(page).toHaveURL(/\?lens=competenza/)` and the button's `aria-pressed` attribute is
    `'true'`. (2) "LENS switch renders and is functional on /dashboard/categories and
    /dashboard/categories/[id]" — same click+URL-assertion pattern on both routes. (3) "LENS switch
    is disabled with the explanatory note on /dashboard/tags" — goto `/dashboard/tags`, assert both
    switch buttons `toBeDisabled()` and the note text `page.getByText('i tag sono all-time: la
    lente non cambia i totali')` is visible. (4) "lens survives tab navigation" — set
    `/dashboard/overview?lens=competenza`, click the 'Categorie' tab link, assert the resulting URL
    still contains `lens=competenza`. (5) "no switch exists on /tags/[id]" — goto a tag detail route
    (reuse an existing tag id fixture from the staging seed, or fall back gracefully like the
    existing category-detail test's `if (count > 0) ... else ...` pattern at lines 131-141 if none
    exists) and assert `page.getByRole('button', { name: 'Cassa' })` has count 0 on that page.
  </action>
  <acceptance_criteria>
    - New `test.describe('Dashboard - LENS: ...')` block exists with the five cases above
    - All five tests pass against the staging deployment (or are structured to degrade gracefully
      exactly like the file's existing empty-state fallback pattern, never a hard crash)
  </acceptance_criteria>
  <verify>
    <automated>yarn test:e2e tests/dashboard.spec.ts -g "LENS"</automated>
  </verify>
  <done>
    The lens switch's presence, enabled/disabled state, URL wiring, and tab-persistence are proven
    live across all four dashboard sub-routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final full-suite regression close-out (LENS-03 + all lens-aware functions)</name>
  <files>N/A — verification only, no files modified</files>
  <read_first>
    tests/reimbursement-regression.test.ts (the full existing byte-identical suite);
    tests/amortization-lens-regression.test.ts (this phase's accrual/cash proof for the
    dashboard.ts functions, built across Plans 80-01 and 80-02);
    tests/amortization-lens-regression-overview.test.ts (this phase's accrual/cash proof for the
    overview.ts functions, built in Plan 80-03).
  </read_first>
  <action>
    Run the complete vitest suite (`yarn test`) and the complete real-Postgres regression trio
    (`yarn db:up && yarn test tests/reimbursement-regression.test.ts
    tests/amortization-lens-regression.test.ts tests/amortization-lens-regression-overview.test.ts`)
    one final time to confirm zero regressions accumulated across the phase's six prior plans. If
    any failure surfaces, it indicates a gap between two plans' assumptions (e.g. a shared file
    edited by two plans in sequence that conflicts) — fix in place before considering the phase
    complete. This task performs no new code changes when the suite is already green; it exists as
    the phase's explicit closing gate.
  </action>
  <acceptance_criteria>
    - `yarn test` (full suite) exits 0
    - `yarn db:up && yarn test tests/reimbursement-regression.test.ts tests/amortization-lens-regression.test.ts tests/amortization-lens-regression-overview.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>yarn test && yarn db:up && yarn test tests/reimbursement-regression.test.ts tests/amortization-lens-regression.test.ts tests/amortization-lens-regression-overview.test.ts</automated>
  </verify>
  <done>
    The full test suite and the real-Postgres regression pair are green; Phase 80 is regression-clean.
  </done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/dashboard.spec.ts` — new `test.describe('Dashboard - LENS: ...')` Playwright block
  covering all four dashboard sub-routes' switch behavior
- No new source files — this plan closes the phase's verification loop only

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright client -> staging deployment | E2E tests exercise the real deployed lens wiring end-to-end, the same trust boundary as every other `x-staging-key`-gated test in this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-10 | Information Disclosure | Staging key exposure in test headers | low | accept | Pre-existing pattern (`process.env.STAGING_KEY ?? 'test-staging-key'`) used unchanged by every test in this file; not introduced or altered by this phase |

</threat_model>

<verification>
`yarn test:e2e tests/dashboard.spec.ts -g "LENS"` and `yarn test && yarn db:up && yarn test
tests/reimbursement-regression.test.ts tests/amortization-lens-regression.test.ts
tests/amortization-lens-regression-overview.test.ts` — all green.
</verification>

<success_criteria>
The lens switch is proven live across all four dashboard sub-routes; the full regression suite
(unit + real-Postgres) is green, closing LENS-01, LENS-02, LENS-03 (still), LENS-04, and LENS-05.
</success_criteria>

<output>
Create `.planning/phases/80-dashboard-accrual-lens/80-07-SUMMARY.md` when done
</output>
