---
phase: 80-dashboard-accrual-lens
plan: 06
type: execute
wave: 3
depends_on: ["80-01"]
files_modified:
  - app/(app)/dashboard/tags/page.tsx
autonomous: true
requirements: [LENS-01]

must_haves:
  truths:
    - "/dashboard/tags always renders the LensSwitch (D-04: no conditional gate) but disabled, with the note 'i tag sono all-time: la lente non cambia i totali' (D-05)"
    - "getTagTotals on /dashboard/tags is never called with a ledgerRowSource argument — tags remain lens-invariant by construction, not by a disabled UI layered on top of a lens-aware query"
    - "/tags/[id] (app/(app)/tags/[id]/page.tsx) receives zero changes in this phase (D-06) — verified by an explicit untouched-file check"
  artifacts:
    - "app/(app)/dashboard/tags/page.tsx parses ?lens= (for display only) and renders <LensSwitch lens={lens} disabled note=\"...\" />"
  key_links:
    - "?lens= -> parseLensParam -> <LensSwitch lens={lens} disabled note=...> (display-only; never reaches getTagTotals)"
---

<objective>
Wire `/dashboard/tags` to render the global `LensSwitch` in its disabled+noted state (D-05): the
switch is always visible (D-04) so the control's presence is predictable across all four
sub-routes, but flipping it here is a structural no-op — `getTagTotals`/`getTagDetail` are all-time
aggregates (v2.7 model) that never take a `ledgerRowSource` and were never modified in this phase.
Confirm `/tags/[id]` (outside the `(app)/dashboard` route group) receives zero changes (D-06).

Purpose: complete D-03's "one global switch, shared identically across all four dashboard
sub-routes" — the Tags route's switch instance is inert by design, not by omission, and must not
confuse the user about whether the feature is broken.
Output: /dashboard/tags shows a disabled switch with an explanatory note; /tags/[id] is untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-dashboard-accrual-lens/80-CONTEXT.md
@.planning/phases/80-dashboard-accrual-lens/80-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the disabled+noted LensSwitch on /dashboard/tags</name>
  <files>app/(app)/dashboard/tags/page.tsx</files>
  <read_first>
    app/(app)/dashboard/tags/page.tsx (current structure — reads NO searchParams today, per its own
    comment at line 9: "TAG-05's per-tag total is all-time and does not subscribe to the dashboard's
    global filters");
    components/dashboard/lens-switch.tsx (LensSwitch component, from Plan 80-01 — accepts
    `disabled`/`note` props already);
    .planning/phases/80-dashboard-accrual-lens/80-CONTEXT.md D-05 (the exact Italian note copy).
  </read_first>
  <action>
    In app/(app)/dashboard/tags/page.tsx, add a `searchParams: Promise<{ lens?: string | string[]
    }>` prop to `DashboardTagsPage` (currently takes no props). Parse `const lens =
    parseLensParam((await searchParams).lens)` (import from `@/lib/utils/search-params`) — used
    ONLY to set the switch's visual pressed state so it reflects whatever lens the user arrived with
    via tab navigation; it must NEVER be passed to `getTagTotals` (which stays exactly
    `getTagTotals(userId)`, zero changes, per D-05). Render `<LensSwitch lens={lens} disabled
    note="i tag sono all-time: la lente non cambia i totali" />` (import from
    `@/components/dashboard/lens-switch`) in the page's header block, next to the existing
    `<h1>Tag</h1>` (lines 42-48).
  </action>
  <acceptance_criteria>
    - `/dashboard/tags` renders a switch with the HTML `disabled` attribute on both buttons and the
      Italian note text visible
    - `getTagTotals` call site is unchanged: `grep -c "getTagTotals(userId)" app/(app)/dashboard/tags/page.tsx`
      is 1, and no `ledgerRowSource`/`lens` argument is ever passed to it
    - `git diff --quiet HEAD -- 'app/(app)/tags/[id]/page.tsx'` reports no changes (D-06) — this file
      is never touched by any task in this phase
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
    /dashboard/tags shows the disabled, noted switch; getTagTotals is untouched; /tags/[id] has zero diff.
  </done>
</task>

</tasks>

## Artifacts this phase produces

- `app/(app)/dashboard/tags/page.tsx` — renders `LensSwitch` in the `disabled` + noted state (D-05);
  `getTagTotals` call site remains unchanged (no `ledgerRowSource`/`lens` argument ever added)
- `app/(app)/tags/[id]/page.tsx` — explicitly verified untouched (D-06), zero diff across the whole phase

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> RSC | `?lens=` on `/dashboard/tags`, used only for the switch's display state, never reaching the DAL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-09 | Tampering | A future edit accidentally threading `lens`/`ledgerRowSource` into `getTagTotals`/`getTagDetail` | low | accept | D-05 is structural (these functions were never given the parameter at all in this phase, unlike the ten netting-fragment sites) — the acceptance criteria's grep gate catches an accidental future wiring attempt on this exact call site |

</threat_model>

<verification>
`node_modules/.bin/tsc --noEmit`. Manually visit /dashboard/tags, confirm the switch is visibly
disabled with the note, and that /tags/[id] is unaffected.
</verification>

<success_criteria>
/dashboard/tags's switch is always visible, disabled, and noted; tag totals never change with the
lens; /tags/[id] remains completely untouched.
</success_criteria>

<output>
Create `.planning/phases/80-dashboard-accrual-lens/80-06-SUMMARY.md` when done
</output>
