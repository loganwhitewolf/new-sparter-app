---
phase: 50-transaction-pairing
plan: 04
type: execute
wave: 2
depends_on: ["50-01", "50-02"]
files_modified:
  - lib/dal/dashboard.ts
  - lib/dal/overview.ts
  - lib/dal/transactions.ts
autonomous: true
requirements: [PAIR-02, PAIR-03]
tags: [transaction-pairing, netting, dashboard, dal, aggregation]

must_haves:
  truths:
    - "Every dashboard/overview aggregation excludes the paired secondary and attributes the algebraic net to the primary's month + direction bucket"
    - "Unpaired transactions are aggregated exactly as before (ADR 0004 algebraic-sum baseline unchanged)"
    - "The transaction list select exposes pairedWithId, pairedNetAmount, pairedDescription, pairedOccurredAt"
    - "The shared isNotSecondary() + effectiveAmount() helpers are reused at all 8 sites — never re-derived per query"
  artifacts:
    - path: "lib/dal/dashboard.ts"
      provides: "Netting applied to 6 aggregation queries via shared helpers"
      contains: "isNotSecondary"
    - path: "lib/dal/overview.ts"
      provides: "Netting applied to getOverviewChart + getMonthOverMonthCategoryChanges"
      contains: "effectiveAmount"
    - path: "lib/dal/transactions.ts"
      provides: "pairedWithId / pairedNetAmount / pairedDescription / pairedOccurredAt on transactionListSelect + TransactionListRow"
      contains: "pairedWithId"
  key_links:
    - from: "lib/dal/dashboard.ts"
      to: "lib/dal/transaction-pairs-sql.ts"
      via: "import { isNotSecondary, effectiveAmount }"
      pattern: "from '@/lib/dal/transaction-pairs-sql'"
    - from: "lib/dal/overview.ts"
      to: "lib/dal/transaction-pairs-sql.ts"
      via: "import { isNotSecondary, effectiveAmount }"
      pattern: "from '@/lib/dal/transaction-pairs-sql'"
    - from: "lib/dal/transactions.ts (transactionListSelect)"
      to: "transaction_pair"
      via: "correlated subqueries for pairedWithId / pairedNetAmount"
      pattern: "transaction_pair"
---

<objective>
Make explicit pairing change every dashboard aggregation consistently (D-05–D-08): the secondary transaction is excluded from its own month/bucket via the shared `isNotSecondary()` WHERE fragment, and the primary's contribution becomes the algebraic net (primary.amount + secondary.amount) via the shared `effectiveAmount()` SUM-CASE substitution — applied identically at all 8 aggregation sites and surfaced in the transaction list. Unpaired transactions remain untouched (PAIR-03: ADR 0004 baseline preserved).

The netting applies consistently at ALL aggregation sites with no partial netting (D-07); the secondary is excluded from its own month/bucket (D-06) while the primary carries the net. CRITICAL ANTI-PATTERN GUARD: do NOT write per-query bespoke netting CASE logic. Both helpers come from `lib/dal/transaction-pairs-sql.ts` (Plan 02) and are applied together at every site (Pitfalls 1 & 2 — exclude-without-net is the common bug).

Purpose: Turn the Plan 01 netting + select-shape tests GREEN and supply the list fields the Plan 05 badge/popover render.
Output: edits to dashboard.ts (6 sites), overview.ts (2 sites), transactions.ts (select + type).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-transaction-pairing/50-RESEARCH.md
@.planning/phases/50-transaction-pairing/50-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- Netting integrated into `getOverviewAmountTotals`, `getCategoriesBreakdown`, `getCategoryRanking`, `getCategoryDeviations` (reference + baseline), `getCategoryDetail` (trend + subcategories + topTransactions), `getMonthlyTrendByNature` (lib/dal/dashboard.ts).
- Netting integrated into `getOverviewChart`, `getMonthOverMonthCategoryChanges` (current + previous) (lib/dal/overview.ts).
- New `TransactionListRow` fields + `transactionListSelect` keys: `pairedWithId`, `pairedNetAmount`, `pairedDescription`, `pairedOccurredAt` (lib/dal/transactions.ts).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply netting helpers to the 6 dashboard.ts aggregation sites</name>
  <files>lib/dal/dashboard.ts</files>
  <read_first>
    - lib/dal/transaction-pairs-sql.ts (created Plan 02) — the `isNotSecondary()` + `effectiveAmount()` helpers to import and reuse (NEVER re-derive the CASE/NOT EXISTS logic inline at any site)
    - lib/dal/dashboard.ts — all 6 aggregation functions: `getOverviewAmountTotals` (~432), `getCategoriesBreakdown` (~899), `getCategoryRanking` (~959), `getCategoryDeviations` (~1018, two sub-queries), `getCategoryDetail` (~1124, three sub-queries), `getMonthlyTrendByNature` (~1340); read each query's WHERE `and(...)` and its SUM CASE expressions so the substitution is surgical
    - .planning/phases/50-transaction-pairing/50-RESEARCH.md §1 (exact current shapes of every query) and §3 (the apply-both-together rule) and §Common Pitfalls 1–3
    - .planning/phases/50-transaction-pairing/50-PATTERNS.md (lib/dal/dashboard.ts section — the 6-site list and the before/after WHERE + SUM substitution)
    - tests/dashboard-dal.test.ts (extended in Plan 01) — the netting scenarios this code makes GREEN
  </read_first>
  <action>
    Add `import { isNotSecondary, effectiveAmount } from '@/lib/dal/transaction-pairs-sql'` to dashboard.ts. At EACH of the 6 sites, make two changes together: (1) add `isNotSecondary()` as an additional condition inside the existing WHERE `and(...)`; (2) replace every `${transactionTable.amount}` reference inside SUM CASE expressions with `${effectiveAmount()}` so the primary contributes the net (primary.amount + secondary.amount). Sites: `getOverviewAmountTotals` (totalIn/totalOut/totalAllocation SUM CASE); `getCategoriesBreakdown` (`abs(sum(amount))`); `getCategoryRanking` (`abs(sum(amount))`); `getCategoryDeviations` — apply to BOTH the reference-period and baseline-period sub-queries; `getCategoryDetail` — apply to the trend sub-query, the subcategories sub-query, AND the topTransactions sub-query (for topTransactions, add `isNotSecondary()` to its WHERE so paired secondaries never appear in the top-5, and use the netted amount for sorting/display per 50-RESEARCH.md Open Question 3); `getMonthlyTrendByNature` (uses leftJoin for uncategorized — `isNotSecondary()` references `${transactionTable.id}` and resolves regardless; replace the amount in the SUM with `effectiveAmount()`). Because the primary keeps its OWN `occurredAt` and direction join, the net automatically lands in the primary's month + direction bucket (D-05) and the secondary never contributes to its own month (D-06). This pairing change is read-only over the categorization model — no expense/subcategory row is touched (D-04). Do NOT alter the `ne(direction.code, 'transfer')` / `eq(direction.includedInTotals, true)` conditions — they stay. Preserve the `cache` wrappers and existing GROUP BY columns. No native arithmetic — the addition happens inside `effectiveAmount()`'s SQL (`::numeric`).
  </action>
  <verify>
    <automated>yarn test tests/dashboard-dal.test.ts && yarn lint lib/dal/dashboard.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/dal/dashboard.ts` imports `isNotSecondary` and `effectiveAmount` from `@/lib/dal/transaction-pairs-sql`, and contains NO inline `transaction_pair` CASE/NOT EXISTS re-derivation (the only reference is via the helpers).
    - Source assertion: `effectiveAmount()` appears in the SUM expressions of all six functions; `isNotSecondary()` appears in each function's WHERE (grep both helpers across the file, filtering out comment lines).
    - Behavior assertion (50-VALIDATION.md PAIR-03 netting + baseline regression): `yarn test tests/dashboard-dal.test.ts` GREEN — paired secondary excluded, primary shows the net, and the pre-existing unpaired baseline cases still pass.
    - Command: `yarn check:language` passes; `yarn lint lib/dal/dashboard.ts` clean.
  </acceptance_criteria>
  <done>All six dashboard aggregations apply both shared helpers together; secondaries are excluded and primaries net; ADR 0004 baseline regression GREEN; no per-query bespoke netting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Netting in overview.ts (2 sites) + paired fields on transactionListSelect</name>
  <files>lib/dal/overview.ts, lib/dal/transactions.ts</files>
  <read_first>
    - lib/dal/transaction-pairs-sql.ts — the helpers (reused, not re-derived)
    - lib/dal/overview.ts — `getOverviewChart` (~423, correlated subselects for nature/direction, GROUP BY month/nature/directionCode) and `getMonthOverMonthCategoryChanges` (~170, current + previous month sub-queries); confirm `getOverview` (~112) delegates to `getOverviewAmountTotals` and needs NO edit (Pitfall 3 — double-counting guard)
    - lib/dal/transactions.ts — `transactionListSelect` (lines 70–92) and `TransactionListRow` (lines 100–121); `buildTransactionOrderBy` must remain unchanged (correlated subqueries, not a JOIN, avoid fan-out)
    - .planning/phases/50-transaction-pairing/50-RESEARCH.md §1.7/§1.8 (overview query shapes), §4 (the four correlated-subquery select fragments), Open Questions 1 & 2
    - .planning/phases/50-transaction-pairing/50-PATTERNS.md (lib/dal/overview.ts + lib/dal/transactions.ts sections — exact fragments)
    - tests/transactions-dal.test.ts + tests/dashboard-dal.test.ts (extended in Plan 01) — the select-shape + overview netting cases this makes GREEN
  </read_first>
  <action>
    overview.ts: add `import { isNotSecondary, effectiveAmount } from '@/lib/dal/transaction-pairs-sql'`. In `getOverviewChart`, add `isNotSecondary()` to its WHERE `and(...)` and replace the `${transactionTable.amount}` inside its `sum(...)` with `${effectiveAmount()}` (the correlated-subselect nature/direction idiom is unaffected — `isNotSecondary()`/`effectiveAmount()` reference `${transactionTable.id}`/`${transactionTable.amount}` which resolve to the same alias this file imports, per Pitfall 3). In `getMonthOverMonthCategoryChanges`, apply both helpers to BOTH the current-month and previous-month sub-queries. Leave `getOverview` untouched — it inherits netting through `getOverviewAmountTotals` (editing it would double-count).
    transactions.ts: extend `transactionListSelect` with four correlated-subquery fields (NO LEFT JOIN — preserves `buildTransactionOrderBy`): `pairedWithId` (`sql<string | null>` returning the counterpart id via CASE on which FK matches `${transaction.id}`, LIMIT 1); `pairedNetAmount` (`sql<string | null>` returning `(${transaction.amount}::numeric + counterpart.amount::numeric)::text` — the SAME signed net on both legs per Open Question 1); `pairedDescription` (`sql<string | null>` counterpart description for the popover); `pairedOccurredAt` (`sql<Date | null>` counterpart `occurred_at`). Add the four nullable fields to the `TransactionListRow` type after `categoryType`. The net arithmetic stays inside the SQL `::numeric` addition (no native JS arithmetic). These fields feed the Plan 05 badge/popover (PAIR-02).
  </action>
  <verify>
    <automated>yarn test tests/overview-dal.test.ts tests/transactions-dal.test.ts tests/dashboard-dal.test.ts 2>&1 | grep -qiE "pass|✓" && yarn lint lib/dal/overview.ts lib/dal/transactions.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/dal/overview.ts` imports both helpers; `isNotSecondary()` + `effectiveAmount()` appear in `getOverviewChart` and both sub-queries of `getMonthOverMonthCategoryChanges`; `getOverview` is unchanged (no helper added there).
    - Source assertion: `lib/dal/transactions.ts` `transactionListSelect` contains `pairedWithId`, `pairedNetAmount`, `pairedDescription`, `pairedOccurredAt` via correlated subqueries referencing `transaction_pair` (no new LEFT JOIN); `TransactionListRow` declares all four as nullable.
    - Behavior assertion (50-VALIDATION.md PAIR-02 select-shape + PAIR-03 overview netting): `yarn test tests/transactions-dal.test.ts` and the overview/dashboard netting cases GREEN; `buildTransactionOrderBy` unchanged.
    - Command: `yarn check:language` + `yarn lint lib/dal/overview.ts lib/dal/transactions.ts` clean.
  </acceptance_criteria>
  <done>overview's 2 sites net via shared helpers (getOverview untouched); the four paired fields are on the list select + row type via correlated subqueries; select-shape + overview netting tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL read → client (transaction list + dashboard) | All read queries are already `verifySession`-scoped via `dateScopedTransactions(userId, …)` / existing user filters; netting adds no new boundary, only a netting transform on already-scoped rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Information Disclosure | correlated subqueries on `transaction_pair` in `transactionListSelect` | mitigate | The outer query is already user-scoped (`getTransactions` filters by session userId); the correlated subqueries resolve only this user's pair rows because the cascade FKs originate from the user's own transactions. No counterpart from another user can surface (a cross-user pair is impossible — Plan 03 blocks creation). Acceptance: existing user-scope filters preserved; no new unscoped table exposure. |
| T-50-07 | Tampering (incorrect aggregation) | exclude-without-net bug (Pitfalls 1 & 2) | mitigate | Both helpers applied together at every site; the Plan 01 netting test pins `totalOut` to the algebraic net so an exclude-only regression turns the suite RED. Acceptance: dashboard netting tests GREEN. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs (50-RESEARCH.md §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `yarn test tests/dashboard-dal.test.ts tests/overview-dal.test.ts tests/transactions-dal.test.ts` — GREEN (netting + select-shape + baseline regression).
- `yarn lint lib/dal/dashboard.ts lib/dal/overview.ts lib/dal/transactions.ts` — clean.
- `yarn check:language` — clean.
</verification>

<success_criteria>
PAIR-03 netting is consistent across all 8 aggregation sites via the two shared helpers (no per-query re-derivation); unpaired transactions match the ADR 0004 baseline; PAIR-02 list fields (`pairedWithId`/`pairedNetAmount`/`pairedDescription`/`pairedOccurredAt`) are present on the select + row type via correlated subqueries; `getOverview` is not double-netted.
</success_criteria>

<output>
Create `.planning/phases/50-transaction-pairing/50-04-SUMMARY.md` when done.
</output>
