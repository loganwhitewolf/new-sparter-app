---
phase: 69-proto-design-variants
plan: 03
type: execute
wave: 3
depends_on: ["69-02"]
files_modified:
  - app/proto/branding/NOTES.md
autonomous: false
requirements: [BRAND-02]

must_haves:
  truths:
    - "app/proto/branding/NOTES.md exists as design-lock source of truth with Domanda, Come provarlo, Varianti a/b/c descriptions, and Verdetto PO fields (Winner / Steal from losers / Do not ship) per D-07"
    - "After human Preview review, Verdetto PO records the winning variant id and handoff notes for Phase 71 — REQUIREMENTS checkbox is not the SoT (D-07, BRAND-02)"
    - "Proto remains throwaway Preview-gated noindex; no production marketing extraction in this phase (D-09)"
  artifacts:
    - path: app/proto/branding/NOTES.md
      provides: "BRAND-02 PO verdict template + filled verdict after checkpoint"
      contains: "Verdetto PO"
  key_links:
    - "NOTES.md Verdetto → Phase 71 components/marketing promotion input"
    - "Come provarlo documents PROTOTYPES_ENABLED local + Vercel Preview paths"
---

<objective>
Close BRAND-02: ship the NOTES.md winner ritual template and pause for PO/stakeholder Preview compare so the production design direction is recorded before any `(public)` work (Phases 70–73).

Purpose: Without a written verdict, Phase 71 has no locked direction — this plan is the design-lock gate.

Output: `NOTES.md` template committed; human fills Winner / Steal / Do not ship after reviewing `/proto/branding?variant=a|b|c`.
</objective>

## Artifacts this phase produces

- `app/proto/branding/NOTES.md` — Design-lock handoff for Phase 71 (D-07 / BRAND-02). Template in Task 1; Verdetto filled in Task 2 checkpoint.

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-proto-design-variants/69-CONTEXT.md
@.planning/phases/69-proto-design-variants/69-RESEARCH.md
@.planning/phases/69-proto-design-variants/69-PATTERNS.md
@.planning/REQUIREMENTS.md
@app/proto/branding/page.tsx
@app/proto/branding/variant-a.tsx
@app/proto/branding/variant-b.tsx
@app/proto/branding/variant-c.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Write NOTES.md design-lock template</name>
  <files>app/proto/branding/NOTES.md</files>
  <read_first>
    .planning/phases/69-proto-design-variants/69-RESEARCH.md NOTES.md template section (BRAND-02 handoff).
    .planning/phases/69-proto-design-variants/69-PATTERNS.md NOTES.md required sections + historical table-toolbar Verdetto pattern.
    .planning/phases/69-proto-design-variants/69-CONTEXT.md D-07.
    app/proto/branding/variant-a.tsx variant-b.tsx variant-c.tsx — one-line axis summaries for the Varianti section.
  </read_first>
  <action>
    Create `app/proto/branding/NOTES.md` as the BRAND-02 design-lock source of truth (D-07). Required sections exactly:

    - Title banner: PROTOTYPE — branding variants · NOTES + throwaway / Phase 71 handoff notice
    - Domanda: which A+B direction becomes the production homepage
    - Come provarlo: local `PROTOTYPES_ENABLED=1 yarn dev` → `/proto/branding?variant=a`; Preview URL + `/proto/branding` (Preview-scoped env)
    - Varianti: bullets for a Shot-as-plane, b Editorial split, c Type-led stack — one sentence each describing the structural difference actually shipped
    - Verdetto PO (compilare dopo review): blank fields Winner, Steal from losers, Do not ship

    Leave Winner blank for the human checkpoint. Do not flip REQUIREMENTS.md checkboxes here (verification / milestone tracking later). Do not create production marketing routes.
  </action>
  <acceptance_criteria>
    - `app/proto/branding/NOTES.md` exists and contains headings or labels: Domanda, Come provarlo, Varianti, Verdetto PO, Winner, Steal from losers, Do not ship (Italian or English field labels matching RESEARCH template).
    - File mentions `/proto/branding` and `PROTOTYPES_ENABLED`.
    - File mentions variant keys a, b, and c.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/proto/branding/NOTES.md && grep -q 'Verdetto PO' app/proto/branding/NOTES.md && grep -q 'PROTOTYPES_ENABLED' app/proto/branding/NOTES.md && grep -q '/proto/branding' app/proto/branding/NOTES.md && grep -q 'Shot-as-plane' app/proto/branding/NOTES.md && grep -q 'Editorial split' app/proto/branding/NOTES.md && grep -q 'Type-led stack' app/proto/branding/NOTES.md</automated>
  </verify>
  <done>NOTES.md template ready for PO verdict (D-07 scaffold).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>PO Preview compare + fill NOTES verdict</name>
  <files>app/proto/branding/NOTES.md</files>
  <read_first>
    app/proto/branding/NOTES.md — template from Task 1.
    app/proto/layout.tsx — confirm gate still intact before sharing Preview.
  </read_first>
  <what-built>
    Throwaway branding hub at `/proto/branding` with three structural A+B variants (a shot-as-plane, b editorial split, c type-led stack), floating switcher, Italian import-first copy, Registrati/Entra CTAs, Preview-gated + noindex via existing proto layout. NOTES.md template awaiting Verdetto PO (BRAND-01 complete; BRAND-02 pending human fill).
  </what-built>
  <how-to-verify>
    1. Local: `PROTOTYPES_ENABLED=1 yarn dev` → open http://localhost:3000/proto/branding — confirm switcher cycles a/b/c and layouts differ.
    2. Optional: open Vercel Preview URL + `/proto/branding` (env must be Preview-scoped).
    3. Confirm without env / Production: `/proto/branding` returns 404.
    4. Pick a winner; edit `app/proto/branding/NOTES.md` Verdetto PO: set Winner to `a`, `b`, or `c`; note Steal from losers; note Do not ship.
    5. Confirm no desire to extract `components/marketing/*` yet (that is Phase 71).
  </how-to-verify>
  <resume-signal>Type "approved" when NOTES.md Verdetto is filled, or describe issues / requested variant tweaks</resume-signal>
  <acceptance_criteria>
    - After approval, `NOTES.md` Verdetto Winner field is a non-blank value equal to a, b, or c (case-insensitive).
    - Steal from losers and Do not ship lines are present (may say "nessuno" / none).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human checkpoint fills NOTES.md; executor after resume runs: grep -E 'Winner:[[:space:]]*[abcABC]' app/proto/branding/NOTES.md</automated>
  </verify>
  <done>BRAND-02 satisfied: winning variant id recorded in NOTES.md as Phase 71 handoff SoT (D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human PO ↔ NOTES.md | Trusted operator writing design verdict; file is not a public API |
| Preview URL sharing | Stakeholder must use Preview, not confuse with Production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-10 | Information Disclosure | Shared Preview URL | medium | mitigate | NOTES Come provarlo stresses Preview-only; Production 404 without env |
| T-69-11 | Tampering | NOTES.md verdict | low | accept | Repo-controlled markdown; PO is trusted; no runtime trust in NOTES |
| T-69-01 | Information Disclosure | proto gate | high | mitigate | Checkpoint re-confirms Production 404; do not disable layout gate |
| T-69-SC | Tampering | npm installs | high | accept | No installs |
</threat_model>

<verification>
NOTES.md template committed; after human approval Winner is a|b|c; phase ready for verify-work / Phase 70 planning.
</verification>

<success_criteria>
BRAND-02 locked in NOTES.md; proto remains throwaway; no (public) work started.
</success_criteria>

<output>
Create `.planning/phases/69-proto-design-variants/69-03-SUMMARY.md` when done
</output>
