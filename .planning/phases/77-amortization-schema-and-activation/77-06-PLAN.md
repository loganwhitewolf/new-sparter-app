---
phase: 77-amortization-schema-and-activation
plan: 06
type: execute
wave: 3
depends_on: ["77-01", "77-04", "77-05"]
files_modified:
  - tests/dashboard-dal.test.ts
  - tests/overview-dal.test.ts
  - tests/tags-dal.test.ts
  - tests/expense-group-invariance.test.ts
  - tests/reimbursement-regression.test.ts
autonomous: true
requirements: [LENS-03]

must_haves:
  truths:
    - "Every one of the 10 aggregation functions tests/helpers/reimbursement-test-db.ts::captureAggregationSnapshot exercises (getOverviewAmountTotals, getCategoriesBreakdown, getCategoryRanking, getCategoryDeviations, getCategoryDetail, getMonthlyTrendByNature, getMonthOverMonthCategoryChanges, getOverviewChart, getTagTotals, getTagDetail) is proven byte-identical under the cash lens once amortization plans and instalments exist, with the FULL suite (not just the regression file) green — the complete LENS-03 gate (D-12)."
    - "No production call site outside lib/db/schema.ts's two view definitions calls effectiveAmount() or isNotSecondary() directly anymore — the ~16-call-site fragment pair has structurally collapsed into the ledger_entry_cash/ledger_entry_accrual seam (D-11), verified by a repo-wide grep."
    - "Every pre-existing mocked unit test for dashboard.ts/overview.ts/tags.ts (dashboard-dal.test.ts, overview-dal.test.ts, tags-dal.test.ts, expense-group-invariance.test.ts) still passes after the seam migration — collateral breakage from the row-source swap, if any, is fixed here rather than left for a later phase to discover."
    - { statement: "A transaction whose occurredAt falls exactly on a dateRange boundary (from/to) is included/excluded identically by ledger_entry_cash's inherited gte/lte predicates as it was by the pre-seam direct-transaction query — boundary inclusion is unchanged by the seam.", verification: backstop }
    - { statement: "Aggregation queries for a date range with zero transactions and zero instalments return the same zero/COALESCE-defaulted values under the cash lens after the seam lands as they did before.", verification: backstop }
    - { statement: "List-returning cash-lens aggregations (e.g. getCategoryDetail's top transactions, getTagDetail's transaction list) keep their pre-existing ORDER BY unchanged after the seam — instalment rows never appear in ledger_entry_cash's result set, so no new tie-break can interact with existing ordering.", verification: backstop }
  artifacts:
    - tests/reimbursement-regression.test.ts (final closing full-suite proof)
  key_links:
    - "Full `node_modules/.bin/vitest run` -> every DAL unit test + the real-Postgres regression suite, all green -> LENS-03 phase gate closed"
---

<objective>
Close the LENS-03 phase gate: run the FULL test suite (not just the regression file) to catch any
collateral breakage the seam migration (Plans 77-01/77-04/77-05) introduced in the pre-existing
mocked unit tests for dashboard.ts/overview.ts/tags.ts, fix what breaks, and do the final
structural verification that zero production call sites still reach effectiveAmount()/
isNotSecondary() directly.

Purpose: LENS-03 is not "the regression file passes" alone — it is "the whole test suite still
proves cash-lens behavior is untouched," which is what actually gates the phase before Phase 78+
can build on the seam.
Output: full suite green; structural proof the fragment pair has collapsed into the seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/db/schema.ts
@lib/dal/dashboard.ts
@lib/dal/overview.ts
@lib/dal/tags.ts
@tests/dashboard-dal.test.ts
@tests/overview-dal.test.ts
@tests/tags-dal.test.ts
@tests/expense-group-invariance.test.ts
@tests/reimbursement-regression.test.ts
</context>

<tasks>

<task type="auto">
  <name>Diagnose and fix collateral unit-test breakage from the seam migration</name>
  <precondition>Plans 77-01, 77-04, and 77-05 are complete — dashboard.ts, overview.ts, and tags.ts's 10 gated functions all read from ledger_entry_cash.</precondition>
  <files>tests/dashboard-dal.test.ts, tests/overview-dal.test.ts, tests/tags-dal.test.ts, tests/expense-group-invariance.test.ts</files>
  <read_first>
    tests/dashboard-dal.test.ts lines 1-60 (the `vi.mock('@/lib/db/schema', () => ({...}))` factory
    — a flat plain-string-keyed stand-in for real Drizzle table proxies; `.from()` in this file's
    mocked db chain is argument-agnostic (`vi.fn(() => chain)`, ignores what table object it
    receives) — so the most likely (and possibly only) needed fix is adding a `ledgerEntryCash: {
    id: 'ledgerEntryCash.id', userId: 'ledgerEntryCash.userId', occurredAt:
    'ledgerEntryCash.occurredAt', expenseId: 'ledgerEntryCash.expenseId', amount:
    'ledgerEntryCash.amount' }` entry to the mock factory, mirroring the existing `expense`/
    `transaction` entries' shape — needed only because dashboard.ts now does `import {
    ledgerEntryCash, ... } from '@/lib/db/schema'` and would otherwise destructure `undefined`),
    tests/overview-dal.test.ts lines 1-64 (a DIFFERENT mock shape — mocks `drizzle-orm` primitives
    directly and does NOT mock `@/lib/db/schema` at all, so overview.ts's `import { ledgerEntryCash }
    from '@/lib/db/schema'` resolves against the REAL schema.ts module, which itself calls the real
    `pgView(...).as(sql\`...\`)` from drizzle-orm/pg-core using the MOCKED `sql`/`relations` functions
    from this file's `vi.mock('drizzle-orm', ...)` factory — check whether this evaluates cleanly by
    running the file; schema.ts's PRE-EXISTING `uniqueIndex(...).where(sql\`...\`)` usage on
    `category`/`subCategory` already exercises this exact same mocked-sql-under-real-schema.ts path
    today, so pgView should behave the same way unless it internally validates its `.as()` argument
    differently — if it throws, the fix is adding whatever additional `drizzle-orm` export
    `pgView`/`pgMaterializedView`'s internals need to the mock factory), tests/tags-dal.test.ts lines
    100-160 and tests/expense-group-invariance.test.ts lines 40-100 (their own
    `@/lib/db/schema`/`drizzle-orm` mock factories — same diagnosis approach).
  </read_first>
  <action>
    Run `node_modules/.bin/vitest run` (the full suite, not a single file). For every failure in
    tests/dashboard-dal.test.ts, tests/overview-dal.test.ts, tests/tags-dal.test.ts, or
    tests/expense-group-invariance.test.ts, diagnose against the read-first guidance above: if the
    failure is "Cannot read properties of undefined (reading 'expenseId'/'id'/'amount'/...)" or
    similar, the fix is adding a `ledgerEntryCash` (and, only if a failure specifically needs it,
    `ledgerEntryAccrual`) entry to that file's schema/table mock, with plain string-keyed columns
    matching the pattern every other mocked table in that same file already uses. If a failure is a
    genuine assertion mismatch (not a missing-mock crash), the seam migration changed observable
    behavior somewhere it should not have — treat this as a real bug in Plans 77-01/77-04/77-05's
    migrated query, not a test to loosen; fix the production query to restore the exact prior
    behavior (cross-reference the specific dashboard.ts/overview.ts/tags.ts function named in the
    failing test against this plan's read_first files to find the mismatch). Do not touch any test
    assertion's EXPECTED VALUE — only mock setup and (if a real bug is found) production code.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run</automated>
  </verify>
  <acceptance_criteria>
    `node_modules/.bin/vitest run` exits 0 for the entire suite. No test assertion's expected value
    changed in this task — only mock setup additions and (if applicable) production-code fixes.
  </acceptance_criteria>
  <done>Full suite green; the seam migration introduced zero observable behavior change anywhere in
  the codebase's existing test coverage.</done>
</task>

<task type="auto">
  <name>Structural D-11 verification + final LENS-03 closing proof</name>
  <precondition>The previous task's full-suite run is green.</precondition>
  <files>lib/db/schema.ts, tests/reimbursement-regression.test.ts</files>
  <read_first>
    lib/dal/dashboard.ts, lib/dal/overview.ts, lib/dal/tags.ts (the three files that used to call
    effectiveAmount()/isNotSecondary() directly — the grep target for this task), lib/db/schema.ts
    (the two view definitions — the ONLY place these fragment's logic should now exist, duplicated
    inline per Plan 77-01's Step 2 documented tradeoff), tests/reimbursement-regression.test.ts (the
    full amortization describe block built across Plans 77-01/77-04/77-05 — this task adds the
    closing structural proof only, no new numeric assertions).
  </read_first>
  <action>
    Run a repo-wide check: `grep -rn "effectiveAmount()\|isNotSecondary()" lib/dal/dashboard.ts
    lib/dal/overview.ts lib/dal/tags.ts` — expect ZERO matches (getUncategorizedCount never called
    either fragment to begin with, so this is a true zero, not a filtered exception). Confirm
    `lib/dal/transaction-pairs-sql.ts` itself is UNCHANGED and still exports both functions (kept for
    the SS8/AMORT-06 realization work Phase 78 reuses, and for its own direct fragment-contract unit
    tests in tests/dashboard-dal.test.ts, which stay green per this task's dependency on the previous
    task). Add a short code comment to `ledgerEntryAccrual`'s definition in lib/db/schema.ts (if not
    already present from Plan 77-01) explicitly stating: unconsumed in Phase 77 (Phase 80 wires the
    accrual-lens dashboard reads); its transaction-branch netting assumption (non-amortized
    transactions still apply effectiveAmount()-equivalent netting; instalment rows never do, since
    D-04 blocks amortization on any reimbursement-involved transaction in this phase) will need
    re-verification once Phase 78's AMORT-06 (reimbursement reduces an open plan's remaining
    instalments) ships. Run `node_modules/.bin/vitest run tests/reimbursement-regression.test.ts`
    once more as the final, isolated confirmation of the LENS-03 gate (redundant with the full-suite
    run in the previous task, but this is the phase's formal acceptance artifact — its own green run
    is what the phase's SUMMARY should cite).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/reimbursement-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Zero grep hits for effectiveAmount()/isNotSecondary() in dashboard.ts/overview.ts/tags.ts.
    tests/reimbursement-regression.test.ts exits 0 in isolation. lib/db/schema.ts's
    ledgerEntryAccrual definition carries the Phase-80-readiness/Phase-78-revisit comment.
  </acceptance_criteria>
  <done>The ledger_entry seam (D-11) has structurally replaced every direct fragment-pair call site;
  LENS-03's cash-lens byte-identical guarantee is fully proven across all 10 gated functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan modifies only test fixtures/mocks and adds a documentation comment; it introduces no new runtime code path or trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-77-15 | Tampering | Test-only changes | low | accept | This plan touches only test mock factories and one code comment; no production write path or query changes — the STRIDE surface of Plans 77-01/77-04/77-05 already covers the real production risk |

</threat_model>

<verification>
`node_modules/.bin/vitest run` (full suite) exits 0. `grep -rn "effectiveAmount()\|isNotSecondary()"
lib/dal/dashboard.ts lib/dal/overview.ts lib/dal/tags.ts` shows zero matches.
</verification>

<success_criteria>
The full test suite is green after the seam migration. LENS-03's byte-identical cash-lens guarantee
is proven across all 10 gated aggregation functions, and the fragment pair has structurally
collapsed into the ledger_entry seam with no remaining direct call sites.
</success_criteria>

<output>
Create `.planning/phases/77-amortization-schema-and-activation/77-06-SUMMARY.md` when done
</output>
