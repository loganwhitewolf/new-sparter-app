---
phase: 77-amortization-schema-and-activation
plan: 05
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - lib/dal/overview.ts
  - lib/dal/tags.ts
  - tests/reimbursement-regression.test.ts
autonomous: true
requirements: [LENS-03]

must_haves:
  truths:
    - "getMonthOverMonthCategoryChanges (both its allocation-grain and category-grain sub-queries, current and previous month each) and getOverviewChart read their amount from ledger_entry_cash.amount and no longer call effectiveAmount()/isNotSecondary() directly (D-11)."
    - "getTagTotals (rooted FROM tag, LEFT JOIN down to transactions — the inverted join direction) keeps surfacing a zero-transaction tag via its LEFT JOIN chain unchanged; its exclusion FILTER's isNotSecondary() term becomes 'the dual-joined ledger_entry_cash row is not null', applied uniformly across count/minDate/maxDate/total so a refund transaction is excluded from all four exactly as it was before."
    - "getTagDetail keeps displaying each transaction's raw description/occurredAt from `transaction` while its `amount` column (the signed value shown per row) comes from the dual-joined ledger_entry_cash.amount — the same dual-join pattern Plan 77-04 established for getCategoryDetail's top-transactions query, needed here because ledger_entry_cash carries no description column."
    - "All four functions' pre-existing regression assertions stay green unmodified, and the new amortization-scenario assertions (added in this plan) prove all 10 of the phase's gated aggregation functions are now migrated and byte-identical (LENS-03 — full 10-function coverage reached by this plan plus Plan 77-01/77-04)."
  artifacts:
    - lib/dal/overview.ts (getMonthOverMonthCategoryChanges, getOverviewChart — migrated to ledger_entry_cash)
    - lib/dal/tags.ts (getTagTotals, getTagDetail — migrated to ledger_entry_cash via the dual-join pattern)
  key_links:
    - "getMonthOverMonthCategoryChanges/getOverviewChart -> ledger_entry_cash -> byte-identical outputs"
    - "getTagTotals/getTagDetail -> tag/transactionTag LEFT/INNER JOIN transaction dual-joined with ledger_entry_cash by id -> byte-identical outputs"
---

<objective>
Complete the ledger_entry seam migration (D-11) for `lib/dal/overview.ts`'s two aggregation
functions and `lib/dal/tags.ts`'s two aggregation functions — the last 4 of the 10 gated
aggregation call sites (the other 6 are Plan 77-01's `getOverviewAmountTotals` and Plan 77-04's
five `dashboard.ts` functions).

Purpose: completes LENS-03's full 10-function coverage — every site
`tests/helpers/reimbursement-test-db.ts::captureAggregationSnapshot` exercises is migrated after
this plan.
Output: overview.ts and tags.ts fully migrated; regression assertions extended for all four.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/dal/overview.ts
@lib/dal/tags.ts
@lib/dal/dashboard-filters.ts
@lib/db/schema.ts
@tests/reimbursement-regression.test.ts
@tests/fixtures/reimbursement-seed.ts
</context>

<tasks>

<task type="auto">
  <name>Migrate getMonthOverMonthCategoryChanges and getOverviewChart (lib/dal/overview.ts)</name>
  <precondition>lib/db/schema.ts exports ledgerEntryCash and lib/dal/dashboard-filters.ts exports the generalized dateScopedTransactions (Plan 77-01 completed).</precondition>
  <files>lib/dal/overview.ts</files>
  <read_first>
    lib/dal/overview.ts lines 188-380 (getMonthOverMonthCategoryChanges — four sub-queries: allocation
    curr/prev, category curr/prev, all following the same safe aggregate-only pattern, no raw-amount
    display concern), lib/dal/overview.ts lines 473-533 (getOverviewChart — one aggregate query, same
    pattern, with an inline correlated direction-code subquery in the WHERE clause that is UNRELATED to
    the amount seam and stays untouched), lib/dal/dashboard.ts lines 458-522 (getOverviewAmountTotals —
    Plan 77-01's reference transformation this task repeats).
  </read_first>
  <action>
    For all four sub-queries in getMonthOverMonthCategoryChanges and the one query in
    getOverviewChart: change `.from(transactionTable)` to `.from(ledgerEntryCash)`; change every join
    anchored on `transactionTable.expenseId` to `eq(ledgerEntryCash.expenseId, expense.id)`; replace
    every `${effectiveAmount()}` with `${ledgerEntryCash.amount}`; remove `isNotSecondary()` from
    every WHERE clause; update `dateScopedTransactions(userId, ...)` calls to
    `dateScopedTransactions(ledgerEntryCash, userId, ...)`. getOverviewChart's `monthSql`
    (`to_char(${transactionTable.occurredAt}, 'YYYY-MM')`) becomes
    `to_char(${ledgerEntryCash.occurredAt}, 'YYYY-MM')`. Leave the `natureSql`/`directionCodeSql`
    correlated subqueries, the transfer-exclusion correlated subquery in getOverviewChart's WHERE, and
    every other join/GROUP BY/try-catch-fallback structure unchanged — none of them reference
    transaction amount or the pair-secondary predicate.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <automated>node_modules/.bin/vitest run tests/reimbursement-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "effectiveAmount\|isNotSecondary" lib/dal/overview.ts` shows no hits between lines
    188-533. tests/reimbursement-regression.test.ts's pre-existing
    getMonthOverMonthCategoryChanges/getOverviewChart assertions remain green unmodified.
  </acceptance_criteria>
  <done>overview.ts's two gated aggregation functions read from ledger_entry_cash.</done>
</task>

<task type="auto">
  <name>Migrate getTagTotals and getTagDetail via the dual-join pattern (lib/dal/tags.ts)</name>
  <precondition>The previous task in this plan is complete.</precondition>
  <files>lib/dal/tags.ts, tests/reimbursement-regression.test.ts</files>
  <read_first>
    lib/dal/tags.ts lines 205-252 (getTagTotals — rooted FROM tag, LEFT JOIN down through
    transactionTag/transactionTable/expense/... to direction; the `tagTotalExclusion` FILTER
    predicate applied uniformly to count/minDate/maxDate/total), lib/dal/tags.ts lines 353-396
    (getTagDetail — rooted FROM transactionTable, INNER JOIN transactionTag; SELECTs raw description
    and occurredAt alongside the netted `amount`), lib/dal/dashboard.ts lines 1361-1406 (Plan 77-04's
    getCategoryDetail top-transactions dual-join — the SAME pattern this task applies here, for a
    different reason: ledger_entry_cash carries no `description` column at all, so any function
    needing description alongside the netted amount must dual-join by id rather than swap `.from()`).
  </read_first>
  <action>
    getTagTotals: keep `.from(tag)` and its existing LEFT JOIN chain
    (`transactionTag -> transactionTable -> expense -> subCategory -> category ->
    userSubcategoryOverride -> nature -> direction`) completely unchanged — the inverted join
    direction is what keeps a zero-transaction tag surfacing a row, and must not be disturbed. ADD one
    more `.leftJoin(ledgerEntryCash, eq(ledgerEntryCash.id, transactionTable.id))` after the existing
    transactionTable join. In `tagTotalExclusion`, replace the `${isNotSecondary()}` term with
    `sql\`${ledgerEntryCash.id} IS NOT NULL\`` (a refund transaction has no matching ledger_entry_cash
    row, so this LEFT JOIN naturally nulls out for it — identical exclusion effect, now expressed via
    the join instead of a NOT EXISTS fragment) — this term stays inside the SAME FILTER expression
    used by all four aggregates (count/minDate/maxDate/total), so a refund transaction is still
    excluded from all four, not just total. Replace `${effectiveAmount()}` in the `total` SELECT
    expression with `${ledgerEntryCash.amount}`. `count`/`minDate`/`maxDate` keep reading
    `transactionTable.id`/`transactionTable.occurredAt` unchanged (those are not netted amounts).

    getTagDetail: keep `.from(transactionTable)` and its existing INNER JOIN chain unchanged (needed
    for `description`, which ledger_entry_cash does not carry). ADD
    `.innerJoin(ledgerEntryCash, eq(ledgerEntryCash.id, transactionTable.id))`. Remove
    `isNotSecondary()` from the WHERE clause (the INNER JOIN itself now excludes a refund transaction,
    since it has no matching ledger_entry_cash row — same effect, expressed via the join). Replace the
    `amount: sql\`(${effectiveAmount()})::text\`` SELECT expression with
    `amount: sql\`(${ledgerEntryCash.amount})::text\``. `transactionId`, `occurredAt`, `description`,
    `subCategoryName`, `categoryName` all keep reading from `transactionTable`/`subCategory`/`category`
    unchanged.

    tests/reimbursement-regression.test.ts: extend the amortization describe block (Plan
    77-01/77-04) with the final two assertions — getTagTotals and getTagDetail stay byte-identical
    between the pre- and post-amortization snapshots — completing all 10 functions'
    amortization-scenario coverage in this one describe block. Add a closing assertion in the same
    block that explicitly re-asserts the ORIGINAL N=1 reimbursement scenario's existing values (from
    the file's very first describeIfReachable block) are STILL correct when captured via a snapshot
    that also has amortization data present elsewhere in the same fixture set — proving the two
    features (reimbursement netting and amortization) do not interact or leak into each other's
    totals.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <automated>node_modules/.bin/vitest run tests/reimbursement-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "effectiveAmount\|isNotSecondary" lib/dal/tags.ts` shows no hits between lines 205-396.
    tests/reimbursement-regression.test.ts's pre-existing getTagTotals/getTagDetail assertions
    (including the zero-transaction-tag case) remain green unmodified. The new closing
    cross-feature-non-interaction assertion passes.
  </acceptance_criteria>
  <done>tags.ts's two gated aggregation functions migrated via the dual-join pattern; all 10 of the
  phase's gated aggregation functions are now on ledger_entry_cash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ledger_entry_cash view -> getTagTotals/getTagDetail dual joins | No new user input; tagId/userId scoping is unchanged from today |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-77-13 | Tampering | getTagTotals zero-transaction-tag path | medium | mitigate | The LEFT JOIN chain (tag -> ... -> ledger_entry_cash) is preserved unchanged in root/direction; only one additional LEFT JOIN is added, verified by the existing zero-transaction-tag regression assertion staying green |
| T-77-14 | Information Disclosure | getTagDetail dual join | low | accept | The join key (`ledgerEntryCash.id = transactionTable.id`) carries no user-controlled value; userId/tagId scoping in the outer WHERE is unchanged |

</threat_model>

<verification>
`node_modules/.bin/tsc --noEmit` is clean. tests/reimbursement-regression.test.ts exits 0 with all
pre-existing assertions plus the full 10-function amortization-scenario coverage plus the
cross-feature non-interaction assertion. `grep -rn "effectiveAmount\|isNotSecondary"
lib/dal/dashboard.ts lib/dal/overview.ts lib/dal/tags.ts` shows zero hits outside
`getUncategorizedCount` (explicitly out of scope, unrelated to amount).
</verification>

<success_criteria>
All 10 of the phase's gated aggregation call sites read from ledger_entry_cash; no direct
effectiveAmount()/isNotSecondary() call remains in dashboard.ts, overview.ts, or tags.ts.
</success_criteria>

<output>
Create `.planning/phases/77-amortization-schema-and-activation/77-05-SUMMARY.md` when done
</output>
