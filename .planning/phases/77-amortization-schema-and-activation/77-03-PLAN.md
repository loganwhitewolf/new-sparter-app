---
phase: 77-amortization-schema-and-activation
plan: 03
type: execute
wave: 3
depends_on: ["77-01"]
files_modified:
  - lib/dal/transactions.ts
  - lib/actions/transactions.ts
  - lib/validations/transactions.ts
  - components/transactions/transaction-form-dialog.tsx
  - tests/amortization-manual-entry.test.ts
autonomous: true
requirements: [AMORT-01]

must_haves:
  truths:
    - "The manual create-transaction form offers an 'Ammortizza questa transazione' checkbox that, when checked, reveals a Mesi input and — once N is valid — a compact inline preview table, inheriting the exact same validation and preview math as the row/detail-page activation dialog (D-10)."
    - "Checking the box and submitting creates the transaction, detaches it, creates the plan, and materialises the instalments in one atomic write — never a transaction that exists without its plan, or a plan that exists without its instalments."
    - "The amortization checkbox is only meaningfully actionable once the amount field represents an outflow (a negative value); the same not-outflow/too-small validation the row dialog enforces applies here before submission."
    - "The inline preview (E4) inherits E1's empty/invalid/populated/loading resolutions from Plan 77-01's dialog, rendered as the compact variant (smaller font, tighter spacing) per the UI-SPEC."
    - "The submit button reads 'Crea e ammortizza' when the checkbox is checked and 'Crea transazione' otherwise; a successful combined submission toasts 'Transazione creata e ammortizzata su {N} mesi.'"
  artifacts:
    - lib/dal/transactions.ts (insertManualTransactionTx — tx-composable core; insertManualTransaction becomes a thin db.transaction wrapper around it)
    - lib/validations/transactions.ts (CreateTransactionSchema extended with amortizationEnabled/amortizationMonths)
    - lib/actions/transactions.ts (createTransaction extended with the atomic combined path)
    - tests/amortization-manual-entry.test.ts
  key_links:
    - "transaction-form-dialog.tsx checkbox+months -> createTransaction (extended) -> one db.transaction wrapping insertManualTransactionTx + amortization-activation.ts::activatePlanTx (unmodified, called against the just-inserted transactionId within the same tx)"
---

<objective>
Close AMORT-01's third entry point: an inline "Ammortizza questa transazione" option on the manual
create-transaction form (D-10), atomically combining transaction creation with the same
detach+plan+instalment write Plan 77-01 built, reusing `activatePlanTx` unmodified.

Purpose: AMORT-01 requires activation from the row, the detail page, AND manual entry — this plan
delivers the third.
Output: manual entry can create-and-amortize in a single atomic submission.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-amortization-schema-and-activation/77-CONTEXT.md
@.planning/phases/77-amortization-schema-and-activation/77-UI-SPEC.md
@lib/dal/transactions.ts
@lib/actions/transactions.ts
@lib/validations/transactions.ts
@lib/services/amortization-activation.ts
@lib/services/amortization-math.ts
@components/transactions/transaction-form-dialog.tsx
</context>

<!--
Wave note: this plan is wave 3 (not wave 2) purely because it shares lib/dal/transactions.ts with
Plan 77-02 (wave 2) — a file-conflict wave bump, not a functional dependency on 77-02's undo work.
This plan only depends on 77-01's activatePlanTx/amortization-math.ts.
-->

<tasks>

<task type="auto" tdd="true">
  <name>Atomic combined create+amortize action (D-10)</name>
  <precondition>lib/services/amortization-activation.ts::activatePlanTx exists (Plan 77-01 completed).</precondition>
  <files>lib/dal/transactions.ts, lib/actions/transactions.ts, lib/validations/transactions.ts, tests/amortization-manual-entry.test.ts</files>
  <read_first>
    lib/dal/transactions.ts lines 580-643 (insertManualTransaction's current body — it opens its own
    db.transaction internally today, which this task must split into a tx-composable core so it can
    run inside the SAME transaction as activatePlanTx), lib/services/amortization-activation.ts
    (activatePlanTx's exact input shape — {userId, transactionId, months} — and that it runs guard
    checks as its own first step, so it works correctly against a transaction inserted moments earlier
    in the SAME tx with no special-casing needed), lib/actions/transactions.ts lines 41-77
    (createTransaction's current body — the schema parse + verifySession + try/catch shape this task
    extends), lib/validations/transactions.ts (CreateTransactionSchema — the fields this task adds
    to), lib/services/transaction-detach.ts::applyDetachCleanupTx (the pattern of a tx-core function
    plus a thin db.transaction-opening public wrapper — insertManualTransaction follows the identical
    split).
  </read_first>
  <behavior>
    tests/amortization-manual-entry.test.ts (real-Postgres harness):
    - createTransaction with amortizationEnabled='on', amortizationMonths='3', description, a
      negative amount, and a valid date creates: one transaction (detached into a new Standalone
      Expense, synthetic descriptionHash), one amortization_plan row (months=3), and 3
      amortization_instalment rows summing to the entered amount.
    - createTransaction with amortizationEnabled='on' but a POSITIVE amount returns {error: 'Puoi
      ammortizzare solo transazioni in uscita.'} and creates NOTHING (not even the transaction) —
      the whole operation is one atomic unit, not "create transaction, then fail to amortize it".
    - createTransaction with amortizationEnabled='on' and amortizationMonths='1' returns {error:
      'Minimo 2 mesi.'} and creates nothing.
    - createTransaction with amortizationEnabled unset (or 'off') behaves EXACTLY as before this
      plan — creates a plain transaction, no plan, no instalments (regression safety for the
      existing manual-entry path).
  </behavior>
  <action>
    lib/dal/transactions.ts: extract insertManualTransaction's body into a new exported
    `insertManualTransactionTx(tx: DbOrTx, data: ManualTransactionData): Promise<{transactionId:
    string; expenseId: string}>` that performs the SAME two inserts (expense, transaction) against
    the passed-in `tx` — no internal db.transaction call. Redefine `insertManualTransaction(data)`
    as `db.transaction(tx => insertManualTransactionTx(tx, data))` — a pure signature-preserving
    refactor; every existing caller of insertManualTransaction keeps working unchanged.

    lib/validations/transactions.ts: extend CreateTransactionSchema with
    `amortizationEnabled: z.preprocess((v) => v === 'on' || v === true, z.boolean()).optional()
    .default(false)` and `amortizationMonths: z.preprocess((v) => (v === '' || v == null ? undefined
    : Number(v)), z.number().int().min(2).optional())`, plus a `.superRefine` that requires
    amortizationMonths to be present and valid when amortizationEnabled is true (error message
    'Minimo 2 mesi.' when missing/invalid, matching the row dialog's message).

    lib/actions/transactions.ts::createTransaction: after parsing, when
    parsed.data.amortizationEnabled is true, replace the existing `await insertManualTransaction(...)`
    call with `await db.transaction(async (tx) => { const {transactionId} =
    await insertManualTransactionTx(tx, {...}); await activatePlanTx(tx, {userId, transactionId,
    months: parsed.data.amortizationMonths!}) })` — ONE db.transaction wrapping both writes, so a
    guard failure (not-outflow, too-small — the only guards that can fire against a brand-new
    transaction with no reimbursement/group/plan history yet) or any write error rolls back the
    transaction insert too. Catch activatePlanTx's typed guard error the same way
    lib/actions/amortization.ts does (Plan 77-01) and surface its Italian message. When
    amortizationEnabled is false, behavior is UNCHANGED (still calls the plain
    insertManualTransaction). On success with amortization, return a distinguishable success signal
    (extend ActionState or return an additional `amortized: boolean` field) so the dialog can show
    the amortization-specific success toast.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/amortization-manual-entry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    tests/amortization-manual-entry.test.ts exits 0 covering all 4 <behavior> cases against a real
    Postgres harness. The positive-amount and N=1 failure cases leave zero rows in `transaction` for
    that description (proving the atomic rollback, not a partial create-then-fail).
  </acceptance_criteria>
  <done>Manual entry can create and amortize a transaction in one atomic write, and the existing
  non-amortized manual-entry path is unchanged.</done>
</task>

<task type="auto">
  <name>Manual-entry form: checkbox, months input, compact inline preview (D-10)</name>
  <files>components/transactions/transaction-form-dialog.tsx</files>
  <read_first>
    components/transactions/transaction-form-dialog.tsx (full file — the useActionState/FormData
    submit pattern this task extends), components/transactions/activate-amortization-dialog.tsx
    (Plan 77-01 — the preview-table rendering and validateMonthsForAmount/materializeInstalments
    client-side usage this task reuses in compact form), .planning/phases/77-amortization-schema-and-activation/77-UI-SPEC.md
    (the "Activation on Manual Entry Form (D-10)" copywriting block).
  </read_first>
  <action>
    components/transactions/transaction-form-dialog.tsx: add a Checkbox (shadcn) labeled "Ammortizza
    questa transazione" beneath the amount/date grid. When checked, render an Input named
    "amortizationMonths" (label "Mesi", placeholder "2–{maxMonths}" where maxMonths is computed via
    maxMonthsForAmount(amount) from the current amount field value, parsed the same way the action
    parses it: `amount.replace(',', '.')`). Once the amount parses to a negative Decimal AND months
    passes validateMonthsForAmount, render the compact preview: same Data/Importo table structure as
    ActivateAmortizationDialog's preview but smaller font/tighter row padding, no separate scroll
    virtualization needed here (manual-entry N is expected to be small; if it grows large the same
    bounded-height + incremental-render approach from Plan 77-01 applies). When the amount is NOT
    negative, show the checkbox but keep it functionally inert (no months input renders) — matches
    D-01's outflow-only constraint, mirrors the row-guard's amount-sign check from Plan 77-01. Change
    the submit button's label to "Crea e ammortizza" when the checkbox is checked, "Crea transazione"
    otherwise (state.error-driven Alert unchanged). On success, branch the toast: if the last
    submission had amortizationEnabled checked, toast.success(`Transazione creata e ammortizzata su
    ${months} mesi.`); else keep the existing "Transazione creata con successo." toast.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Checking the box with a negative amount and a valid N shows the compact preview matching the
    same math as the row dialog. Checking the box with a positive (or empty) amount shows no months
    input. Submitting with the box checked and valid N shows the amortization-specific success toast.
  </acceptance_criteria>
  <done>Manual entry offers create-and-amortize inline, visually consistent with the row/detail
  activation dialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> createTransaction Server Action (extended) | Untrusted amortizationEnabled/amortizationMonths cross here alongside the existing description/amount/date fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-77-09 | Tampering | amortizationMonths input | medium | mitigate | CreateTransactionSchema's superRefine rejects a missing/non-integer/too-small value before any DB read, identical validation tier to Plan 77-01's CreateAmortizationPlanSchema |
| T-77-10 | Tampering | combined-write atomicity | high | mitigate | insertManualTransactionTx + activatePlanTx run inside ONE db.transaction; a guard failure (not-outflow, too-small) or any write error rolls back the transaction insert too — never a plain transaction left behind after a failed amortize-on-create |

</threat_model>

<verification>
tests/amortization-manual-entry.test.ts exits 0. `node_modules/.bin/tsc --noEmit` is clean. Manual:
open "Nuova transazione", check "Ammortizza questa transazione", enter a negative amount and 3
months, confirm the preview and submission both match the row dialog's math.
</verification>

<success_criteria>
A user can create and amortize a transaction in one atomic submission from the manual entry form,
with validation and preview consistent with the other two entry points.
</success_criteria>

<output>
Create `.planning/phases/77-amortization-schema-and-activation/77-03-SUMMARY.md` when done
</output>
