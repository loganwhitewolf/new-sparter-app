---
phase: 79-amortizations-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dal/amortization.ts
  - app/(app)/amortizations/page.tsx
  - components/amortizations/amortization-table.tsx
  - components/amortizations/amortization-summary-header.tsx
  - lib/utils/amortizations-table-config.ts
  - lib/utils/table-config.ts
  - lib/routes.ts
  - components/layout/sidebar.tsx
  - tests/amortization-registry-dal.test.ts
  - tests/amortization-registry-table.test.ts
autonomous: true
requirements: [REG-01, REG-03]

must_haves:
  truths:
    - "getAmortizationPlanList(userId) returns every amortization_plan row (open AND closed) owned by userId, scoped via WHERE p.user_id = ${userId} server-side — a foreign-owned plan never appears regardless of any client-supplied filter param (IDOR-safe by construction)."
    - "consumedAmount is computed as an explicit SUM(amortization_instalment.amount WHERE occurred_at < CURRENT_DATE), never derived as initialAmount minus netValue or any other residual arithmetic — a partially-reimbursed-and-re-spread plan's consumed total stays historically accurate."
    - "Every derived monetary value (initialAmount, consumedAmount, netValue) is Decimal-precise: SQL-side numeric arithmetic inside the DAL query (matching the getReimbursementAggregates precedent, lib/dal/reimbursement.ts), and any cross-row JS aggregation (the D-B1 summary, Plan 79-01 Task 2) uses Decimal.js (toDecimal/.plus()) exclusively — never native +/-/* on a DECIMAL-as-string value (REG-01 precision edge)."
    - "Default sort is remainingMonths ascending (D-C2); two plans sharing an identical remainingMonths value preserve deterministic order — p.id ASC tie-break in the DAL's own ORDER BY, then Array.prototype.sort's spec-guaranteed stability in sortAmortizationRows — never left to unspecified ordering (REG-01 ordering edge)."
    - "A plan whose remainingMonths reaches 0 (every instalment has already occurred) still renders with status='open' until the user explicitly closes it (Plan 79-02, D-A1/D-A2) — remainingMonths reaching zero is never itself treated as a closed-status signal (REG-01 boundary edge)."
    - "A plan whose netValue is exactly '0.00' (Decimal-equal, not float-near-zero) renders with amountToneClass's neutral/zero tone, never the negative/residual warning tone (REG-01 adjacency edge)."
    - "An account with zero amortization plans renders EmptyState('no-data') on the RSC page and mounts NEITHER the summary header nor the table; an account whose plans are ALL closed still mounts the summary header, showing 'Netto residuo aperto: €0,00' (E1/E6 empty edges) — 'no plans at all' and 'no OPEN plans' are two distinct, separately-renderable states (REG-01 empty edge)."
    - "The registry's client-side status filter defaults to status==='open' when the URL's status param is absent (resolveEffectiveStatusFilter(null) === 'open') — a deliberate override of the shared DataTableToolbar's generic 'no param = show all' convention (D-C1); the toolbar's own Select control may visually show 'Tutte' selected in this state, which is an accepted UI-SPEC tradeoff, not a bug to 'fix' toward showing all plans by default."
    - "Closed plans render with Badge variant='secondary' ('Chiuso') and open plans with variant='default' ('Aperto') — the only visual states this element has (D-C3/E5, flagged assumption: a stateless display-only badge; no loading/error/partial state applies to it beyond these two already-specified variants)."
    - "Clicking a row's description link navigates to transactionDetailHref(row.transactionId) — the amortized transaction's own detail page, which already hosts the full Phase 78 lifecycle UI (D-D1); no /amortizations/[id] plan-detail page is built."
    - { statement: "The registry table, the summary KPI card, and the toolbar/empty-state surfaces (E1/E2/E3/E6) have a defined loading/skeleton treatment for the RSC fetch — none is built in this phase (a plain synchronous RSC fetch, no Suspense boundary); this is a held-out UI-state check for human/manual verification, not a silently-passed automated assertion.", verification: backstop }
    - { statement: "The summary KPI figure and every monetary table cell render correctly (no overflow/clip) at very large magnitudes, and the table's row/count copy reads correctly at zero, one, and many plans with no singular/plural mismatch (E1/E2) — not exercised by an automated test in this phase.", verification: backstop }
    - { statement: "The toolbar (search + status filter) reflows without clipping on narrow/mobile viewports, handles a very long search query without breaking layout, and every filter/sort combination behaves correctly in combination including the zero/one/many-available-options case (E3) — not exercised by an automated test in this phase; relies on the shared DataTableToolbar's existing responsive behavior.", verification: backstop }
    - { statement: "The empty-state surface (no-data / no-result) does not flash the wrong variant during RSC loading, degrades gracefully on a data-load error distinct from the no-data state, and does not overflow on small viewports (E6) — not exercised by an automated test in this phase.", verification: backstop }
  artifacts:
    - lib/dal/amortization.ts
    - app/(app)/amortizations/page.tsx
    - components/amortizations/amortization-table.tsx
    - components/amortizations/amortization-summary-header.tsx
    - lib/utils/amortizations-table-config.ts
    - tests/amortization-registry-dal.test.ts
    - tests/amortization-registry-table.test.ts
  key_links:
    - "AmortizationsPage (app/(app)/amortizations/page.tsx) calls verifySession() then getAmortizationPlanList(userId) — the IDOR boundary lives entirely in the DAL's WHERE clause, never re-checked client-side."
    - "AmortizationTable's client-side status filter (resolveEffectiveStatusFilter, absent param -> 'open') and default sort (remainingMonths ASC via AMORTIZATIONS_TABLE_CONFIG.defaultSort) both implement D-C1/D-C2 without a second server round-trip — DataTableToolbar/useToolbarSort/useTableUrl are reused unmodified from the /reimbursements stack."
    - "lib/utils/table-config.ts's TableConfig['id'] union must include 'amortizations' before AMORTIZATIONS_TABLE_CONFIG type-checks — a one-line widening of an existing type, not a new type."
  prohibitions:
    - "MUST NOT perturb the LENS-03 byte-identical cash-lens regression — getAmortizationPlanList issues SELECT-only statements; it must never INSERT/UPDATE/DELETE against transaction, amortization_plan, or amortization_instalment."
    - "MUST NOT write a synthetic/placeholder transaction anywhere in this plan's scope (ADR 0019 §8) — this plan is read-only; no transaction-creation code exists in it at all."
---

<objective>
Deliver REG-01 and REG-03 (79-CONTEXT.md): a dedicated `/amortizations` registry page listing every
amortization plan (open and closed) with description, transaction date, initial amount, consumed
amount, net value, and remaining months (D-B2's `X/N` + progress-bar rendering), sorted by remaining
months ascending by default (D-C2), defaulting to an open-only view with a status filter that
reveals closed plans (D-C1), and a visually unambiguous open/closed distinction (D-C3). Row
navigation goes to the amortized transaction's own detail page (D-D1) — no new plan-detail page.

This is the phase's TRACER (Task 1): the thinnest end-to-end read path — DAL query -> RSC page ->
interactive client table -> route -> nav entry — wired for real and verified via a real-Postgres DAL
test before any expansion. Task 2 expands with the D-B1 summary header, an additive widget that
changes nothing about the already-proven read path.

Purpose: give the user one place to see every amortization plan's derived state at a glance, reusing
the proven `/reimbursements` structural stack (RSC -> DAL -> interactive table) rather than inventing
a new pattern.
Output: lib/dal/amortization.ts (getAmortizationPlanList), app/(app)/amortizations/page.tsx,
components/amortizations/amortization-table.tsx (full interactive table, no row actions yet — those
are Plan 79-02/REG-02), components/amortizations/amortization-summary-header.tsx, the
AMORTIZATIONS_TABLE_CONFIG, a widened TableConfig['id'] union, the `/amortizations` route + nav
entry, and a real-Postgres DAL test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-amortizations-registry/79-CONTEXT.md
@lib/db/schema.ts
@lib/dal/reimbursement.ts
@app/(app)/reimbursements/page.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Amortizations registry — DAL, page, interactive table, route, nav (D-B2/C1/C2/C3/D1)</name>
  <files>lib/dal/amortization.ts, app/(app)/amortizations/page.tsx, components/amortizations/amortization-table.tsx, lib/utils/amortizations-table-config.ts, lib/utils/table-config.ts, lib/routes.ts, components/layout/sidebar.tsx, tests/amortization-registry-dal.test.ts, tests/amortization-registry-table.test.ts</files>
  <read_first>
    lib/dal/reimbursement.ts lines 453-551 (getReimbursementList — the exact raw-SQL alias
    convention this DAL query mirrors: `p`/`t`-style table aliases to avoid Postgres
    bare-column-name ambiguity inside correlated subqueries; SQL-side numeric arithmetic for
    per-row derived values, distinct from the CLAUDE.md JS-layer Decimal.js rule which governs
    application code operating on already-fetched DECIMAL-as-string values)
    lib/utils/reimbursement-format.ts lines 15-17 (resolveReimbursementDisplayTitle — the
    `.trim() || fallback` precedence pattern to mirror for this DAL's own displayTitle:
    (customTitle ?? '').trim() || description)
    lib/db/schema.ts lines 653-715 (amortizationPlan: id/userId/transactionId/months/status/
    totalAmount; amortizationInstalment: id/userId/planId/instalmentNumber/expenseId/amount/
    occurredAt) and lines 418-445 (transaction: id/userId/description/customTitle/occurredAt)
    app/(app)/reimbursements/page.tsx (RSC page structure: verifySession -> DAL -> EmptyState
    vs. real component, exact JSX shell to mirror)
    components/reimbursements/reimbursement-table.tsx (interactive table structure:
    useToolbarSort + DataTableToolbar wiring, exported pure sort-helper convention, the
    max-w-0 w-full + inner truncate no-horizontal-scroll pattern on the link column, EmptyState
    variant="no-result")
    lib/utils/reimbursements-table-config.ts (TableConfig object shape: id/search/filters/
    sortable/defaultSort)
    lib/utils/table-config.ts (TableConfig['id'] is currently a closed union of
    'transactions' | 'expenses' | 'files' | 'reimbursements' — MUST widen to also include
    'amortizations' or AMORTIZATIONS_TABLE_CONFIG will fail to type-check; config.id is a
    compile-time literal only, never read at runtime by useTableUrl/DataTableToolbar)
    lib/routes.ts (APP_ROUTES const shape + reimbursementHref/transactionDetailHref helper
    pattern to mirror for amortizations/amortizationDetailHref)
    components/layout/sidebar.tsx lines 6-19 and 44-53 (lucide-react icon imports + topNavItems
    array — add the new entry immediately after the Rimborsi entry)
    lib/utils/decimal.ts, lib/utils/amount-tone.ts, lib/utils/format-amount.ts (toDecimal/
    toDbDecimal, amountToneClass's zero/positive/negative tone classes, formatAbsoluteAmount's
    non-finite fallback convention — reuse this shared utility for Initial/Consumed Amount
    rather than a new local absolute-value formatter)
    tests/reimbursement-list.test.ts (real-Postgres DAL test conventions: connectReimbursementTestDb,
    resetReimbursementFixtures, describeIfReachable/requireHarnessDb skip-gracefully pattern)
    tests/fixtures/reimbursement-seed.ts lines 358-402 (seedAmortizationPlan — takes
    userId/transactionId/expenseId/months/instalments/optional totalAmount, inserts the plan row
    with status:'open' plus one instalment row per entry) and seedExpenseWithTransaction/
    seedUser/seedMinimalTaxonomy signatures
    lib/services/amortization-math.ts (materializeInstalments(amount, date, months) — returns
    Array<{date, amount}> with Decimal-precise remainder-on-first-instalment rounding; use it to
    build deterministic fixtures instead of hand-computing amounts)
    tests/reimbursement-table-sort.test.ts (jsdom-free pure-function test pattern for an exported
    sort helper — this repo has no jsdom, so ALL new client-logic tests must be pure exported
    functions, never simulated clicks/renders)
  </read_first>
  <behavior>
    - getAmortizationPlanList(userId) returns ONLY rows where amortization_plan.user_id = userId —
      a plan seeded for a different user never appears, regardless of how many plans exist total.
    - For a plan with N months where only some instalments have occurred (occurred_at < today):
      consumedAmount equals the Decimal sum of exactly those past instalment amounts; netValue
      equals initialAmount minus that same consumedAmount; remainingMonths equals the count of
      instalments with occurred_at >= today. All three stay internally consistent even after a
      plan's totalAmount snapshot has been re-spread (Phase 78 reducePlanTx) — consumed is never
      derived from total/net.
    - A plan with status manually set to 'closed' still appears in the list (both statuses are
      returned; filtering to open-only is a CLIENT-side default, D-C1, not a DAL-level filter).
    - Two plans with an identical remainingMonths value order deterministically by plan id
      ascending (DAL ORDER BY tie-break).
    - A transaction with a non-empty customTitle resolves displayTitle to that customTitle
      (trimmed); a transaction with a null/empty customTitle falls back to its raw description.
    - A brand-new user with zero plans resolves to an empty array, never undefined/throw.
  </behavior>
  <action>
    In lib/dal/amortization.ts (new, 'server-only'), export type AmortizationPlanListRow = { id:
    string; transactionId: string; description: string; displayTitle: string; transactionDate:
    Date; initialAmount: string; consumedAmount: string; netValue: string; remainingMonths:
    number; totalMonths: number; status: 'open' | 'closed' }. Export async function
    getAmortizationPlanList(userId: string): Promise<AmortizationPlanListRow[]> issuing ONE
    db.execute(sql`...`) statement aliased `p` (amortization_plan) and `t` (transaction), mirroring
    getReimbursementList's alias convention exactly. SELECT: p.id, p.transaction_id AS
    transaction_id, t.description, t.custom_title AS custom_title, t.occurred_at AS
    transaction_date, p.total_amount AS initial_amount, p.months AS total_months, p.status; a
    correlated scalar subquery consumed_amount = COALESCE((SELECT SUM(ai.amount::numeric)::text
    FROM amortization_instalment ai WHERE ai.plan_id = p.id AND ai.occurred_at < CURRENT_DATE),
    '0.00'); net_value computed in the SAME statement as (p.total_amount::numeric -
    COALESCE((SELECT SUM(ai2.amount::numeric) FROM amortization_instalment ai2 WHERE ai2.plan_id =
    p.id AND ai2.occurred_at < CURRENT_DATE), 0))::text — this in-database numeric subtraction
    mirrors the getReimbursementAggregates precedent and is distinct from the JS-layer Decimal.js
    rule; remaining_months = a correlated scalar subquery COUNT(*) FROM amortization_instalment
    ai3 WHERE ai3.plan_id = p.id AND ai3.occurred_at >= CURRENT_DATE. FROM amortization_plan p
    INNER JOIN transaction t ON t.id = p.transaction_id WHERE p.user_id = ${userId} (the ONLY WHERE
    predicate — the IDOR boundary, server-side from verifySession, never client-supplied). ORDER BY
    remaining_months ASC, p.id ASC (D-C2 default + deterministic tie-break). Map result.rows to
    AmortizationPlanListRow, computing displayTitle as (row.custom_title ?? '').trim() ||
    row.description (mirrors resolveReimbursementDisplayTitle's precedent, applied inline as a
    single expression rather than a new shared helper) and coercing transaction_date to a real
    Date.

    In lib/utils/table-config.ts, widen the TableConfig['id'] union literal from 'transactions' |
    'expenses' | 'files' | 'reimbursements' to also include 'amortizations' — a one-line type
    change, config.id is compile-time-only (never read at runtime by useTableUrl or
    DataTableToolbar, which both key exclusively off the `route` string argument).

    Create lib/utils/amortizations-table-config.ts exporting AMORTIZATIONS_TABLE_CONFIG: id:
    'amortizations'; search: { key: 'q', placeholder: 'Cerca per descrizione…' }; filters: one
    entry { key: 'status', label: 'Stato', type: 'status', options: [{value:'open',label:'Aperto'},
    {value:'closed',label:'Chiuso'}], toChip: v => (v==='open'?'Aperto':v==='closed'?'Chiuso':v) };
    sortable: remainingMonths ('Rate rimanenti'), description ('Descrizione'), transactionDate
    ('Data'), initialAmount ('Importo iniziale'), consumedAmount ('Consumato'), netValue ('Netto');
    defaultSort: { key: 'remainingMonths', dir: 'asc' } (D-C2).

    In lib/routes.ts, add amortizations: '/amortizations' to the APP_ROUTES const, and export
    function amortizationDetailHref(planId: string) returning
    `${APP_ROUTES.amortizations}/${encodeURIComponent(planId)}` positioned near reimbursementHref
    (this href is for future use only — D-D1 explicitly defers a plan-detail page; row clicks in
    this plan target transactionDetailHref instead).

    In components/layout/sidebar.tsx, import CalendarClock from lucide-react alongside the
    existing icon imports, and add { href: APP_ROUTES.amortizations, label: 'Ammortamenti', icon:
    CalendarClock } to topNavItems immediately after the Rimborsi entry.

    Create app/(app)/amortizations/page.tsx: export const metadata = { title: 'Ammortamenti' };
    export default async function AmortizationsPage(): call const { userId } = await
    verifySession(), const plans = await getAmortizationPlanList(userId); render a `flex flex-col
    gap-6 p-6` wrapper with an h1 'Ammortamenti' + subtitle 'Tutte le rate dei tuoi ammortamenti.'
    (mirrors the reimbursements page shell exactly); when plans.length === 0, render EmptyState
    variant="no-data" message="Nessun ammortamento" hint="Non hai ancora nessun ammortamento
    attivo. Quando ammortizzerai una spesa, vedrai qui tutte le tue rate."; otherwise render
    `<AmortizationTable plans={plans} route={APP_ROUTES.amortizations} />` (Task 2 inserts the
    summary header above this same call, inside the same non-empty branch).

    Create components/amortizations/amortization-table.tsx ('use client'). Export
    resolveEffectiveStatusFilter(statusParam: string | null): 'open' | 'closed' — returns 'closed'
    only when statusParam === 'closed', 'open' in every other case (including null/undefined/'open')
    — this is the D-C1 default-to-open override of the shared toolbar's generic "absent param =
    show all" convention; document inline that the toolbar's own Select may visually show 'Tutte'
    selected while this function still resolves 'open' (an accepted UI-SPEC tradeoff, not a bug).
    Export sortAmortizationRows(rows, sort, dir): numeric keys (initialAmount/consumedAmount/
    netValue) via toDecimal(...).comparedTo(...); remainingMonths via plain numeric subtraction (a
    count, not money — exempt from the Decimal.js rule); description via localeCompare;
    transactionDate (default) via getTime() subtraction; ties preserve input order (stable sort).
    Export function AmortizationTable({ plans, route }: { plans: AmortizationPlanListRow[]; route:
    string }): read searchParams via useSearchParams(); const effectiveStatus =
    resolveEffectiveStatusFilter(searchParams.get('status')); const q =
    searchParams.get('q')?.trim().toLowerCase() ?? ''; filter plans to
    row.status===effectiveStatus AND (q === '' || row.displayTitle.toLowerCase().includes(q));
    resolve sortKey/sortDir from useToolbarSort(route), falling back to
    AMORTIZATIONS_TABLE_CONFIG.defaultSort when no URL sort param is set; sort via
    sortAmortizationRows. Render DataTableToolbar config={AMORTIZATIONS_TABLE_CONFIG} route={route}
    above the table; when the sorted/filtered set is empty render EmptyState variant="no-result";
    otherwise render a Table with columns, in order: Descrizione (HeaderSortButton key
    'description', cell is a Link to transactionDetailHref(row.transactionId) with the max-w-0
    w-full + inner truncate pattern and a title= tooltip, text is row.displayTitle — D-D1), Data
    (HeaderSortButton 'transactionDate', row.transactionDate.toLocaleDateString('it-IT')), Importo
    iniziale (HeaderSortButton 'initialAmount', align right, formatAbsoluteAmount(row.initialAmount)
    — imported from lib/utils/format-amount.ts, no sign), Consumato (HeaderSortButton
    'consumedAmount', align right, formatAbsoluteAmount(row.consumedAmount)), Netto (HeaderSortButton
    'netValue', align right, a SIGNED local formatSignedAmount(row.netValue) helper mirroring
    reimbursement-table.tsx's own local formatSignedAmount exactly — including its non-finite
    fallback `${value} EUR` — with className amountToneClass(row.netValue) for the D-B2/adjacency
    zero-tone requirement), Rate rimanenti (HeaderSortButton 'remainingMonths', cell shows
    `${row.remainingMonths}/${row.totalMonths}` text plus a light div-based progress bar whose fill
    width is `${((row.totalMonths - row.remainingMonths) / row.totalMonths) * 100}%` — the label
    text uses the REMAINING count per UI-SPEC's explicit sort-key-mapping/edge-case formula, while
    the bar visually fills as CONSUMED progresses; this reading is chosen over 79-CONTEXT.md D-B2's
    looser parenthetical "(consumed/total)" phrasing because it matches the column header 'Rate
    rimanenti', the ROADMAP/REQUIREMENTS.md REG-01 field name "remaining months", and UI-SPEC's own
    explicit formula + "0/20, bar full" zero-remaining edge case), Stato (plain TableHead, a Badge
    variant={row.status==='open' ? 'default' : 'secondary'} showing 'Aperto'/'Chiuso' — D-C3). Do
    NOT render an Azioni column in this task — row actions are Plan 79-02/REG-02.

    Create tests/amortization-registry-dal.test.ts — a real-Postgres suite mirroring
    tests/reimbursement-list.test.ts's harness conventions exactly (connectReimbursementTestDb,
    resetReimbursementFixtures, describeIfReachable, requireHarnessDb, afterAll pool.end()). Seed
    via seedUser/seedMinimalTaxonomy/seedExpenseWithTransaction/seedAmortizationPlan/
    materializeInstalments (tests/fixtures/reimbursement-seed.ts, lib/services/amortization-math.ts).
    For "past vs. future instalment" fixtures, compute startDate relative to `new Date()` at test
    time (e.g. 4 months before now) rather than a hardcoded calendar date, since the DAL query's
    CURRENT_DATE is the real wall clock — a hardcoded past/future boundary would eventually go
    stale. Cover every <behavior> case above, plus: two plans for DIFFERENT users only returns the
    calling user's plan (IDOR); a manually status='closed' plan (direct db.update after
    seedAmortizationPlan, which always inserts status='open') still appears with status:'closed'.

    Create tests/amortization-registry-table.test.ts — jsdom-free pure-function tests (mirrors
    tests/reimbursement-table-sort.test.ts). Cover: sortAmortizationRows sorts netValue via Decimal
    comparison, not string comparison (e.g. '-100.00' vs '20.00'); sorts remainingMonths
    numerically; sorts description via localeCompare; ties preserve input order (stable);
    resolveEffectiveStatusFilter(null) === 'open', resolveEffectiveStatusFilter('open') === 'open',
    resolveEffectiveStatusFilter('closed') === 'closed'; amountToneClass('0.00') resolves to the
    neutral/zero tone class (not positive/negative) as a lightweight regression proof of the
    REG-01 adjacency edge for this feature's own usage.
  </action>
  <reversibility rating="reversible">Surfaces existing schema/services on a brand-new read-only
  page — no data migration, no write path, fully removable without unwinding any other
  decision.</reversibility>
  <verify>
    <automated>yarn vitest run tests/amortization-registry-dal.test.ts tests/amortization-registry-table.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/dal/amortization.ts exports getAmortizationPlanList and AmortizationPlanListRow; the
      query's only WHERE predicate is p.user_id = ${userId} (grep confirms no second WHERE clause).
    - `yarn vitest run tests/amortization-registry-dal.test.ts` passes (or skips gracefully with the
      standard "Local Postgres unreachable" warning when Docker is unavailable).
    - `yarn vitest run tests/amortization-registry-table.test.ts` passes, covering
      sortAmortizationRows and resolveEffectiveStatusFilter with zero jsdom dependency.
    - lib/utils/table-config.ts's TableConfig['id'] union includes 'amortizations' (grep confirms
      the literal string); `yarn tsc --noEmit` is clean.
    - lib/routes.ts exports APP_ROUTES.amortizations === '/amortizations' and
      amortizationDetailHref.
    - components/layout/sidebar.tsx's topNavItems array contains an entry with href:
      APP_ROUTES.amortizations (grep confirms 'Ammortamenti' string present).
    - components/amortizations/amortization-table.tsx contains zero occurrences of "Azioni" or a
      row-action Button/Link — confirming REG-02 wiring is deliberately deferred to Plan 79-02.
  </acceptance_criteria>
  <done>A user with amortization plans can navigate to /amortizations from the sidebar and see every
  plan (open + closed), sorted by remaining months ascending by default, with correct Decimal-precise
  derived values, an open/closed badge, and a status filter that defaults to open-only while still
  allowing closed plans to be revealed; clicking a plan's description navigates to its transaction
  detail page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Summary header — total open net residual (D-B1)</name>
  <files>components/amortizations/amortization-summary-header.tsx, app/(app)/amortizations/page.tsx, tests/amortization-registry-table.test.ts</files>
  <read_first>
    .planning/phases/79-amortizations-registry/79-UI-SPEC.md section "Summary Header (D-B1)" (the
    single-KPI contract: label 'Netto residuo aperto', card markup `rounded-lg border bg-card p-4`
    + `text-sm text-muted-foreground` label + `text-2xl font-semibold tracking-tight` value,
    €0,00 fallback when no open plans, raw-value-suffixed-EUR fallback on non-finite arithmetic)
    lib/utils/decimal.ts (toDecimal/.plus() — this is a NEW cross-row JS aggregation over
    already-fetched DECIMAL-as-string netValue fields, so it MUST use Decimal.js, unlike Task 1's
    single-row SQL-side arithmetic)
    components/reimbursements/reimbursement-table.tsx lines 33-39 (the local formatSignedAmount
    helper's non-finite fallback convention `${value} EUR` — mirror this exact fallback shape for
    the KPI's own signed currency formatter, matching Task 1's Netto column formatter)
  </read_first>
  <behavior>
    - computeTotalOpenResidual(plans) with three open plans (netValue '-33.33', '-33.33',
      '-33.34') and one closed plan (netValue '-500.00') returns exactly '-100.00' — the closed
      plan's value is excluded, and Decimal.js prevents any float rounding drift across the three
      open values.
    - computeTotalOpenResidual(plans) with zero open plans (all closed, or the array is empty)
      returns exactly '0.00', never NaN or an empty string.
  </behavior>
  <action>
    Create components/amortizations/amortization-summary-header.tsx ('use client'). Export
    computeTotalOpenResidual(plans: AmortizationPlanListRow[]): string — plans.filter(p =>
    p.status === 'open').reduce((sum, p) => sum.plus(toDecimal(p.netValue)), toDecimal('0')),
    returning .toFixed(2). Export function AmortizationSummaryHeader({ plans }: { plans:
    AmortizationPlanListRow[] }): compute const totalOpenResidual =
    computeTotalOpenResidual(plans); format it via a local Intl.NumberFormat('it-IT', {style:
    'currency', currency: 'EUR'}) formatter applied to Number(totalOpenResidual), with the SAME
    non-finite fallback convention as Task 1's formatSignedAmount (`${totalOpenResidual} EUR` when
    Number.isFinite fails — surfaces an upstream bug rather than silently showing €0,00). Render a
    `rounded-lg border bg-card p-4` div containing a `text-sm text-muted-foreground` label 'Netto
    residuo aperto' and a `text-2xl font-semibold tracking-tight` value line with the formatted
    amount.

    In app/(app)/amortizations/page.tsx, import AmortizationSummaryHeader and mount it immediately
    above the existing `<AmortizationTable .../>` call, inside the SAME plans.length > 0 branch (the
    summary header never renders in the EmptyState('no-data') branch — Task 1's must_haves empty
    edge: "no plans at all" vs "no OPEN plans" are distinct states, and this task's own
    computeTotalOpenResidual already correctly returns '0.00' for the latter without any extra
    branching in the page component).

    Extend tests/amortization-registry-table.test.ts with computeTotalOpenResidual cases covering
    both <behavior> scenarios above, using the SAME makeRow-style fixture-builder convention already
    established in Task 1's tests for this file.
  </action>
  <reversibility rating="reversible">A new additive KPI widget layered on an already-working page —
  removing it later unwinds nothing else (D-B1).</reversibility>
  <verify>
    <automated>yarn vitest run tests/amortization-registry-table.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - components/amortizations/amortization-summary-header.tsx exports computeTotalOpenResidual and
      AmortizationSummaryHeader; both computeTotalOpenResidual test cases pass.
    - app/(app)/amortizations/page.tsx renders AmortizationSummaryHeader only inside the non-empty
      branch (grep confirms it does NOT appear inside the EmptyState('no-data') JSX block).
    - The label string 'Netto residuo aperto' appears exactly once in
      amortization-summary-header.tsx.
  </acceptance_criteria>
  <done>The registry page shows a single KPI — total open net residual — computed with Decimal.js
  precision, showing €0,00 when there are no open plans, mounted only when the account has at least
  one plan of any status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page -> getAmortizationPlanList | server-side call only; userId comes from verifySession(), never a client-supplied value |
| browser -> AmortizationTable (search/status/sort URL params) | client-supplied filter params narrow an ALREADY userId-scoped row set fetched once by the RSC page; they never trigger a re-query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Information Disclosure | getAmortizationPlanList | high | mitigate | WHERE p.user_id = ${userId} scoped server-side from verifySession(), never from a client-supplied filter; the INNER JOIN to transaction carries no additional leak surface since transaction rows are only reachable via a plan already scoped to userId |
| T-79-02 | Tampering | client-side status/q filter params | low | accept | filtering happens entirely client-side over an already userId-scoped row set fetched once by the RSC page; a tampered status/q URL param can only narrow or fail to narrow that SAME set, never fetch another user's rows |
| T-79-03 | Information Disclosure | transaction.description/customTitle rendered in the table | low | accept | rendered via React's default text interpolation (auto-escaped), never dangerouslySetInnerHTML — matches every other table in the app (transactions/expenses/reimbursements) |

</threat_model>

<verification>
- `yarn vitest run tests/amortization-registry-dal.test.ts tests/amortization-registry-table.test.ts`
- `yarn tsc --noEmit`
- `yarn check:language` (new Italian strings only in page/table copy, no dev-facing string in Italian)
</verification>

<success_criteria>
- REG-01 fully delivered: every amortization plan (open + closed) is visible at /amortizations with
  description, transaction date, initial amount, consumed amount, net value, and remaining months,
  all Decimal-precise.
- REG-03 fully delivered: open vs closed plans are unambiguously distinguished (badge), the registry
  defaults to an open-only view, and a status filter reveals closed plans.
- The summary header (D-B1) shows the total open net residual, computed with Decimal.js.
</success_criteria>

<output>
Create `.planning/phases/79-amortizations-registry/79-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `lib/dal/amortization.ts` — new file; exports `getAmortizationPlanList(userId)`, type
  `AmortizationPlanListRow`.
- `app/(app)/amortizations/page.tsx` — new RSC route; exports default `AmortizationsPage`, `metadata`.
- `components/amortizations/amortization-table.tsx` — new file; exports `AmortizationTable`,
  `sortAmortizationRows`, `resolveEffectiveStatusFilter`.
- `components/amortizations/amortization-summary-header.tsx` — new file; exports
  `AmortizationSummaryHeader`, `computeTotalOpenResidual`.
- `lib/utils/amortizations-table-config.ts` — new file; exports `AMORTIZATIONS_TABLE_CONFIG`.
- `lib/utils/table-config.ts` — modified (existing file); `TableConfig['id']` union widened to
  include `'amortizations'`.
- `lib/routes.ts` — modified (existing file); adds `APP_ROUTES.amortizations`,
  `amortizationDetailHref(planId)`.
- `components/layout/sidebar.tsx` — modified (existing file); adds an `Ammortamenti` entry to
  `topNavItems`.
- `tests/amortization-registry-dal.test.ts`, `tests/amortization-registry-table.test.ts` — new test
  files.
