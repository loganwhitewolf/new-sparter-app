---
phase: 74-group-anchor-and-reconciliation
plan: 03
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - lib/services/transaction-edit.ts
  - tests/transaction-edit.test.ts
  - tests/pair-guard-message.test.ts
autonomous: true
requirements: [RMB-09]

must_haves:
  truths:
    - "An edit that breaks the reimbursement's opposite-sign/nonzero invariant is still rejected before any write, exactly as today -- all 5 pre-existing DET-03 tests pass unmodified."
    - "An edit bringing an amount to exactly 0.00 is blocked -- the existing sign-check already covers this (0 is neither gt(0) nor lt(0)); a dedicated test now asserts it explicitly (RMB-09/boundary)."
    - "Editing a transaction that participates in no reimbursement never trips the guard (RMB-09/empty, pre-existing behavior, reconfirmed by the untouched existing test)."
    - "An edit that flips the required opposite sign is blocked before any write (RMB-09/precision, pre-existing behavior, reconfirmed by the untouched existing tests)."
    - "When N>1 linked refunds exist on the blocking reimbursement, the thrown message names it by title -- e.g. Scollega prima il rimborso \"Cena di gruppo\" (RMB-09/encoding)."
    - "When N<=1 linked refunds exist, the message is the exact unchanged string 'Scollega prima il rimborso' -- no regression in the common case."
  artifacts:
    - lib/services/transaction-edit.ts (buildPairGuardMessage added; 2 throw sites updated)
    - tests/transaction-edit.test.ts (schema mock extended; new N>1 and zero-edit cases added)
    - tests/pair-guard-message.test.ts (new, DB-mock-free pure-function unit test)
  key_links:
    - "updateTransaction()'s existing reimbursementId resolution (unchanged, T-73-10) -> reimbursement.title + refundCount lookup (new) -> buildPairGuardMessage()"
  prohibitions:
    - "The guard never auto-reconciles or auto-nudges totals to make an edit fit -- D-04 explicitly rejects this; the only change in this plan is the thrown message's text, never the block condition or a write path."
---

<objective>
Improve the amount-edit pair guard's message when N>1 linked refunds exist on the blocking
reimbursement (D-04, RMB-09): identify WHICH reimbursement is blocking by name, so the user knows
what to unlink. The guard's block condition itself is untouched (it already generalizes correctly
to any N, per Phase 73) -- hard-block, no auto-reconciliation, exactly as today for N<=1.

Sequenced in Wave 2, after Plan 74-01's tracer is verified, per TRACER_MODE's explicit ordering
("...before expanding to N>1 Group cases, residual, and the guard message") -- not a file
dependency (this plan touches lib/services/transaction-edit.ts only, untouched by 74-01).

Purpose: closes RMB-09's only remaining gap -- a clearer message, never a behavior change to the
block itself.
Output: an enriched, still-hard-blocking guard message; a small, directly unit-testable pure
helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-group-anchor-and-reconciliation/74-CONTEXT.md

@lib/services/transaction-edit.ts
@lib/db/schema.ts
@tests/transaction-edit.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich the N>1 pair-guard message with the blocking reimbursement's title</name>
  <files>lib/services/transaction-edit.ts, tests/transaction-edit.test.ts</files>
  <read_first>lib/services/transaction-edit.ts, tests/transaction-edit.test.ts (especially the DET-03 pair guard describe block and the reimbursement schema mock), lib/db/schema.ts (reimbursement.title)</read_first>
  <behavior>
    - Anchor-edit guard trip, reimbursement has exactly 1 linked refund -> message is EXACTLY 'Scollega prima il rimborso' (unchanged from today).
    - Anchor-edit guard trip, reimbursement has 2 linked refunds -> message contains BOTH 'Scollega prima il rimborso' AND the reimbursement's title (e.g. 'Cena di gruppo').
    - Refund-edit guard trip, reimbursement has 2 linked refunds -> same title enrichment.
    - An edit that would bring the amount to exactly '0.00' is blocked (0 is neither gt(0) nor lt(0), so the existing oppositeSign check already rejects it -- this is a new explicit test of existing logic, RMB-09/boundary).
  </behavior>
  <action>
Add an exported pure function to lib/services/transaction-edit.ts, near the top of the file after
the imports and before updateTransaction: `buildPairGuardMessage(input: { refundCount: number;
reimbursementTitle: string }): string` -- returns the literal 'Scollega prima il rimborso' when
`input.refundCount <= 1`; else returns `Scollega prima il rimborso "${input.reimbursementTitle}"`
(the title double-quoted, interpolated) when `input.refundCount > 1`. Document in a comment above
it: naming a SPECIFIC blocking refund is not meaningful for an anchor edit (the invariant is a SUM
across all N refunds, not any single one), so the reimbursement's own title is the actionable
identifier; for a refund-edit conflict the user already knows which refund they're editing, so no
further "which refund" disambiguation is needed there either.

In the anchor-edit branch's existing sumRows query (`.select({ refundsSum: ... }).from(reimbursement)
.where(eq(reimbursement.id, reimbursementId))`), add two more selected fields: `reimbursementTitle:
reimbursement.title` (a plain Drizzle column reference -- reimbursement is already the base table
of this query, no new join needed) and `refundCount: sql<number>` counting every linked refund
(`SELECT COUNT(*)::int FROM reimbursement_refund WHERE reimbursement_id = ${reimbursementId}`).
Replace the existing `throw new Error('Scollega prima il rimborso')` in this branch with `throw new
Error(buildPairGuardMessage({ refundCount: sumRow?.refundCount ?? 0, reimbursementTitle:
sumRow?.reimbursementTitle ?? '' }))`.

In the refund-edit branch's existing sumRows query (`.select({ anchorAmount: ..., otherRefundsSum:
... }).from(reimbursement).where(eq(reimbursement.id, reimbursementId))`), add the identical two
fields (`reimbursementTitle`, and a `refundCount` counting ALL linked refunds on that
reimbursement, not excluding the one being edited -- the total N is what determines ambiguity).
Replace this branch's `throw new Error('Scollega prima il rimborso')` the same way.

Update tests/transaction-edit.test.ts's mocked `@/lib/db/schema` module: add `title:
'reimbursement.title'` to the mocked `reimbursement` object (currently only `{ id, expenseId }`).
Add the 4 `<behavior>` cases as new `it()`s inside the existing 'DET-03 -- pair guard' describe
block, extending the relevant `dbSelectChain.mockImplementation` return rows with
`reimbursementTitle`/`refundCount` fields for the NEW cases only -- the 5 pre-existing DET-03
tests' mock rows are left exactly as they are (their `refundCount` comes back `undefined`, which
`> 1` evaluates to false, so they correctly fall back to the plain unchanged message and keep
passing via their existing `.rejects.toThrow('Scollega prima il rimborso')` substring assertion,
with zero modification to those test bodies).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/transaction-edit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/services/transaction-edit.ts exports buildPairGuardMessage -- confirm via reading the
      file's export statements.
    - node_modules/.bin/vitest run tests/transaction-edit.test.ts exits 0, including all 5
      pre-existing DET-03 tests UNMODIFIED plus the 4 new `<behavior>` cases.
    - The N>1 anchor-edit and refund-edit test cases assert the thrown message contains the
      reimbursement's title, not just the substring 'Scollega prima il rimborso'.
    - The zero-amount-edit test asserts the guard throws (blocks), proving the pre-existing
      oppositeSign check already covers this boundary with no code change needed there.
  </acceptance_criteria>
  <done>Both guard throw sites enrich their message with the blocking reimbursement's title when
  N>1 linked refunds exist; the N<=1 case and all pre-existing DET-03 behavior are unchanged; the
  exact-zero-amount boundary is explicitly proven blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Dedicated pure-function unit test for buildPairGuardMessage</name>
  <files>tests/pair-guard-message.test.ts</files>
  <read_first>lib/services/transaction-edit.ts (Task 1's buildPairGuardMessage export)</read_first>
  <action>
Add tests/pair-guard-message.test.ts -- a DB-mock-free unit test file (no vi.mock needed at all,
mirroring this codebase's established "exported pure function for direct unit testing" pattern,
e.g. computeMergeEligibility/isGroupTitleValid) importing buildPairGuardMessage directly from
lib/services/transaction-edit.ts and asserting: (a) `{ refundCount: 1, reimbursementTitle: 'Cena
di gruppo' }` returns EXACTLY 'Scollega prima il rimborso' (title never interpolated at N=1); (b)
`{ refundCount: 0, reimbursementTitle: '' }` (the defensive fallback shape) ALSO returns the plain
message; (c) `{ refundCount: 2, reimbursementTitle: 'Cena di gruppo' }` returns a string
containing BOTH the substring 'Scollega prima il rimborso' AND 'Cena di gruppo' (RMB-09/encoding --
the message names the blocking reimbursement, not a generic string); (d) `{ refundCount: 3,
reimbursementTitle: 'Regalo per "Mario"' }` (a title containing embedded double-quote characters)
still returns a string containing the literal title substring, unmangled -- document in a comment
that title text is plain Italian display copy, never re-parsed as a structured format, so no
escaping/sanitization is attempted or required here.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/pair-guard-message.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/pair-guard-message.test.ts imports buildPairGuardMessage with no `vi.mock` calls at
      all -- confirm via reading the file (no mock setup boilerplate).
    - node_modules/.bin/vitest run tests/pair-guard-message.test.ts exits 0, covering all 4 cases.
  </acceptance_criteria>
  <done>buildPairGuardMessage's N<=1/N>1 branching and title interpolation are proven via a fast,
  DB-free pure-function test, independent of the DAL-mocking test in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| updateTransaction() input.amount -> guard | User-supplied via a Server Action; the guard is the last line of defense against a write that would silently corrupt a reimbursement's net. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-08 | Tampering | The guard's block condition itself | high | mitigate | All 5 pre-existing DET-03 tests must keep passing UNMODIFIED (only the message text branches) -- any code change that weakened the block would surface as a regression in those exact, untouched assertions, not only in the new ones. |
| T-74-09 | Repudiation | Silent auto-reconciliation | high | mitigate (by omission) | D-04 explicitly rejects auto-reconciliation; this plan adds no code path that mutates amounts to "fix" the invariant -- the guard remains hard-block-only, confirmed by no new `.update(transaction)` call site being introduced by this plan (only the `throw new Error(...)` argument changes). |
| T-74-10 | Information Disclosure | reimbursement.title interpolated into the thrown message | low | accept | The title always belongs to the SAME editing user's own reimbursement (ownership already scoped by the pre-existing roleRows/sumRows queries via `transaction.userId`); no cross-user data can appear in the message. |
</threat_model>

<assumption_delta_decision>
Noun: outflow anchor (Expense XOR Expense Group).
Decision: no-change -- the XOR anchor model is locked upstream (schema migration 0029, Phase 73,
ADR 0018) and is not re-opened by this phase.
Rationale: the guard's message enrichment reads the SAME reimbursement row the existing guard
already resolves; it introduces no new anchor identity or resolution path.
</assumption_delta_decision>

## Artifacts this phase produces

- `effectiveAmount()` (modified) -- lib/dal/transaction-pairs-sql.ts -- proportional-spread rewrite (Plan 74-01)
- `isNotSecondary()` (confirmed unchanged, no modification) -- lib/dal/transaction-pairs-sql.ts (Plan 74-01)
- `seedSecondEssentialCategory()` / `seedExpenseGroup()` / `seedReimbursementOnGroup()` (new) -- tests/fixtures/reimbursement-seed.ts (Plan 74-01)
- `getReimbursementAggregates()` (new) -- lib/dal/reimbursement.ts (new file, Plan 74-02)
- `computeReimbursementResidual()` (new) -- lib/services/reimbursement.ts (new file, Plan 74-02)
- `buildPairGuardMessage()` (new) -- lib/services/transaction-edit.ts (this plan, 74-03)
- `updateTransaction()`'s two guard throw sites (modified) -- lib/services/transaction-edit.ts (this plan, 74-03)
- No schema/migration changes -- the message enrichment reads an existing column
  (reimbursement.title) and adds a COUNT(*) subquery over an already-indexed FK
  (reimbursement_refund_reimbursementId_idx); no new index required.

<verification>
- node_modules/.bin/vitest run tests/transaction-edit.test.ts exits 0 (5 pre-existing + 4 new
  cases, all in the DET-03 describe block).
- node_modules/.bin/vitest run tests/pair-guard-message.test.ts exits 0 (4 cases, DB-mock-free).
- `git diff -- lib/services/transaction-edit.ts` shows only the two throw sites' argument and the
  new exported helper changed -- no change to the block condition (`oppositeSign` logic) itself.
</verification>

<success_criteria>
- The pair guard still hard-blocks every invariant-breaking edit exactly as before (proven by
  untouched pre-existing tests).
- When N>1, the message names the blocking reimbursement by title; when N<=1, the message is
  byte-identical to today's.
- No auto-reconciliation code path exists anywhere in this plan's diff.
</success_criteria>

<output>
Create `.planning/phases/74-group-anchor-and-reconciliation/74-03-SUMMARY.md` when done
</output>
