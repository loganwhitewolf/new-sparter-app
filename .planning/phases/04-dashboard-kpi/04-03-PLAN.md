---
phase: "04"
plan: "03"
type: "implementation"
---

<objective>
04-dashboard-kpi 02
</objective>

<context>
Phase: 04 (dashboard-kpi) — Milestone: M001: Migration

Build the reusable dashboard UI components for filters, KPI cards, category breakdown, monthly trend, and loading skeletons.

Purpose: This plan creates the visible dashboard component layer while keeping data fetching in Server Components and chart interactivity isolated to Client Components.
Output: `components/dashboard/` component set used by the final `/dashboard` page.
</context>

<must_haves>
- "D-03: DashboardFilters writes `type` and `preset` to `/dashboard` URL search params without full page reload"
- "D-04: KpiCards renders exactly five KPI cards with labels: Totale entrate, Totale uscite, Bilancio, Tasso risparmio, Da categorizzare"
- "D-05: KPI card grid is 5 columns on desktop and 2 columns on mobile with the fifth card centered"
- "D-02: CategoryBreakdownChart is a Client Component using Recharts horizontal BarChart with drill-down state"
- "D-06: KPI value and chart colors use total-in emerald, total-out red, balance slate, and conditional savings colors"
- "D-07: MonthlyTrendChart is a Client Component using grouped bars for totalIn, totalOut, totalNc, totalIgn"
- "D-08: MonthlyTrendChart exposes clickable legend state to show/hide individual series"
- "Chart components do not import `lib/dal/dashboard.ts` at runtime; they receive data by props"
</must_haves>
