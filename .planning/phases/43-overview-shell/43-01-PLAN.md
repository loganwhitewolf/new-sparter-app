---
phase: 43-overview-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/overview/format.ts
  - components/dashboard/overview/kpi-card-reading.tsx
  - components/dashboard/overview/kpi-row.tsx
  - components/dashboard/overview/overview-header.tsx
autonomous: true
requirements: [HEAD-01, HEAD-02, HEAD-03, KPI-01, KPI-02, KPI-03, KPI-04]
must_haves:
  truths:
    - "User sees the page title with an inline year-selector pill on the same row"
    - "The year selector lists only years that have transaction data"
    - "User sees four KPI cards (Totale entrate, Totale uscite, Bilancio, Tasso risparmio) with a 'vs anno prec.' delta badge and a sentiment-colored qualitative reading line"
    - "The delta badge is hidden when its delta is null"
  artifacts:
    - path: "components/dashboard/overview/format.ts"
      provides: "Production EUR + compact k-notation formatters (replace proto eur/eurCompact)"
      exports: ["formatEur", "formatEurCompact"]
    - path: "components/dashboard/overview/kpi-card-reading.tsx"
      provides: "ReadingKpiCard + Reading type"
      exports: ["ReadingKpiCard", "Reading"]
    - path: "components/dashboard/overview/kpi-row.tsx"
      provides: "4 KPI cards with savings/balance/trend reading helpers, consuming OverviewData"
      exports: ["KpiRow"]
    - path: "components/dashboard/overview/overview-header.tsx"
      provides: "Title + inline year-pill Select with ?year= router.replace wiring"
      exports: ["OverviewHeader"]
  key_links:
    - from: "components/dashboard/overview/kpi-row.tsx"
      to: "lib/dal/dashboard.ts OverviewData"
      via: "props typed as OverviewData"
      pattern: "OverviewData"
    - from: "components/dashboard/overview/overview-header.tsx"
      to: "?year= URL param"
      via: "router.replace with scroll:false"
      pattern: "router\\.replace"
---

<objective>
Port the PO-approved proto KPI row, ReadingKpiCard, and HeaderH1 into production
components under `components/dashboard/overview/`, plus a production number formatter
to replace the throwaway proto `eur`/`eurCompact`. Strip nothing here (these proto
files contain no P44/P45 bits) — port the reading-line thresholds verbatim, rewire the
mock-data imports to real DAL types and the production formatter.

Purpose: Delivers the header (HEAD-01..03) and the 4 KPI cards (KPI-01..04) — two of
the three UI pieces of the redesigned overview shell, as self-contained presentation
components consumed later by the page integration (Plan 03).

Output: `format.ts`, `kpi-card-reading.tsx`, `kpi-row.tsx`, `overview-header.tsx` under
`components/dashboard/overview/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-overview-shell/43-CONTEXT.md
@CLAUDE.md
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production number formatters</name>
  <files>components/dashboard/overview/format.ts</files>
  <read_first>
    - app/proto/overview/mock-data.ts (proto `eur` lines 197-198 and `eurCompact` lines 201-202 — the throwaway shapes to reproduce in production)
    - components/dashboard/category-ranking-list.tsx (lines 55-62 — existing `Intl.NumberFormat('it-IT', { style:'currency', currency:'EUR' })` convention in this codebase; the DAL returns DECIMAL strings, parse before formatting)
    - lib/utils/decimal.ts (toDecimal — use for parsing DAL DECIMAL strings if any client-side coercion is needed; formatting here only)
  </read_first>
  <action>
    Create `components/dashboard/overview/format.ts` (developer-facing code in English).
    Export `formatEur(value: string | number): string` using
    `Intl.NumberFormat('it-IT', { style: 'currency', currency: 'EUR', maximumFractionDigits: 0 })`
    — matches proto `eur` and the codebase convention; accept the DAL's DECIMAL string by
    coercing through `Number()`/`toDecimal` so KPI/tooltip values render as "€1.234".
    Export `formatEurCompact(value: string | number): string` reproducing proto `eurCompact`:
    values ≥ 1000 render as k-notation with `it-IT` locale and `maximumFractionDigits: 1`
    (e.g. "2,5k"); below 1000 render the rounded integer. Used for always-on bar labels in
    Plan 02. Do NOT reintroduce `eurSigned` (that is P45 movers copy — out of scope).
    Keep instantiation module-scoped (one formatter instance), as `category-ranking-list.tsx` does.
  </action>
  <verify>
    <automated>grep -q "export function formatEur" components/dashboard/overview/format.ts && grep -q "export function formatEurCompact" components/dashboard/overview/format.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^$" | grep -i "overview/format" | head; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/dashboard/overview/format.ts` exists and exports `formatEur` and `formatEurCompact`.
    - `formatEur` uses `Intl.NumberFormat('it-IT', …, currency: 'EUR')` and accepts a string input (DAL DECIMAL strings).
    - `formatEurCompact` renders k-notation for values ≥ 1000 (locale it-IT) and a rounded integer otherwise.
    - No `eurSigned` / movers-related export is present.
    - `npx tsc --noEmit` reports no error originating from `overview/format.ts`.
  </acceptance_criteria>
  <done>Both formatters exported, typed for string|number, type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: ReadingKpiCard + KpiRow (port verbatim, rewire to OverviewData)</name>
  <files>components/dashboard/overview/kpi-card-reading.tsx, components/dashboard/overview/kpi-row.tsx</files>
  <read_first>
    - app/proto/overview/kpi-card-reading.tsx (the ReadingKpiCard component + Reading type + sentimentColor / valueColor / deltaColor / formatDelta helpers — port verbatim)
    - app/proto/overview/kpi-row.tsx (KpiRow + savingsReading 50/30/20 / balanceReading sign / trendReading helpers — port reading thresholds verbatim per D-CONTEXT discretion note)
    - lib/dal/dashboard.ts (lines 31-44 — OverviewData type: totalIn/totalOut/balance are strings; savingsRate is number; deltas.* are number | null)
    - components/dashboard/overview/format.ts (formatEur from Task 1 — replaces proto `eur` import)
    - components/ui/card.tsx, components/ui/badge.tsx (Card/CardContent, Badge — reused as-is)
    - CONTEXT.md §"Dashboard e analisi" (domain copy rules — "variazione" banned; the proto copy already complies)
  </read_first>
  <action>
    Create `components/dashboard/overview/kpi-card-reading.tsx`: port `ReadingKpiCard` and
    the `Reading` type verbatim from the proto, removing the "PROTOTYPE — wipe me" header
    comment. Keep the existing `delta !== null` guard that hides the Badge when delta is null
    (D-06 / KPI-02). Keep the "vs {prevYear}" badge suffix; CONTEXT decision relabels it to
    "vs {anno prec.}" — render the literal Italian "vs {prevYear}" exactly as the proto does
    (prevYear is the numeric prior year, which IS "anno prec."); do not invent new copy.
    Create `components/dashboard/overview/kpi-row.tsx`: port `savingsReading`, `balanceReading`,
    `trendReading` verbatim. Change the `KpiRow` signature from the proto's mock `getKpis(year, …)`
    to accept real data: `KpiRow({ data, year }: { data: OverviewData; year: number })`. Map
    `OverviewData` fields onto the four `ReadingKpiCard`s — `formatEur(data.totalIn)`,
    `formatEur(data.totalOut)`, `formatEur(data.balance)`, and `` `${data.savingsRate}%` `` for
    Tasso risparmio. Feed `data.deltas.totalIn/totalOut/balance/savingsRate` (number | null) to
    each card's `delta` prop. For the reading helpers that need numeric balance/savingsRate, coerce
    `data.balance` via `Number()` (or toDecimal) and use `data.savingsRate` directly. Drop the
    5th "Da categorizzare" concern entirely (out of scope, replaced by the P44 nudge).
    No FilterBar/MoversList exist in these proto files — nothing to strip.
  </action>
  <verify>
    <automated>grep -q "export function ReadingKpiCard" components/dashboard/overview/kpi-card-reading.tsx && grep -q "export function KpiRow" components/dashboard/overview/kpi-row.tsx && grep -q "OverviewData" components/dashboard/overview/kpi-row.tsx && grep -q "delta !== null" components/dashboard/overview/kpi-card-reading.tsx && ! grep -qi "PROTOTYPE" components/dashboard/overview/kpi-card-reading.tsx components/dashboard/overview/kpi-row.tsx && echo "ported-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `ReadingKpiCard` and `Reading` exported from `kpi-card-reading.tsx`; the `delta !== null` badge-hiding guard is present.
    - `KpiRow` exported from `kpi-row.tsx`, typed `{ data: OverviewData; year: number }`, importing `OverviewData` from `@/lib/dal/dashboard`.
    - `savingsReading` thresholds are exactly ≥20 / ≥10 / ≥0 / else with the proto's Italian strings; `balanceReading` and `trendReading` ported verbatim.
    - The four cards read Totale entrate, Totale uscite, Bilancio, Tasso risparmio and pull values + deltas from `OverviewData`.
    - No "PROTOTYPE" comment remains; no FilterBar/MoversList/getKpis import remains.
    - `formatEur` (Task 1) is used for the three monetary values.
  </acceptance_criteria>
  <done>KPI cards render the four real OverviewData metrics with reading lines and null-safe delta badges; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: OverviewHeader (port HeaderH1, wire ?year= against real years)</name>
  <files>components/dashboard/overview/overview-header.tsx</files>
  <read_first>
    - app/proto/overview/header-variants.tsx §HeaderH1 (lines 35-61 — the title + inline year-pill Select + ?year= router.replace({ scroll:false }) wiring; ignore H2-H5 and useYearNav, they are unused)
    - app/proto/overview/year-select.tsx (year selector reference — same Select pattern)
    - components/ui/select.tsx (Select/SelectTrigger/SelectValue/SelectContent/SelectItem — reused as-is)
    - lib/dal/overview.ts (getYearsWithData returns string[] DESC — this component receives those years as a prop; it does NOT call the DAL itself)
  </read_first>
  <action>
    Create `components/dashboard/overview/overview-header.tsx` as a `'use client'` component.
    Port HeaderH1 verbatim (title `Panoramica delle tue finanze` + inline year-pill Select with
    `usePathname`/`useRouter`/`useSearchParams` and `router.replace(..., { scroll: false })` — D-05,
    no history spam). Replace the proto's `AVAILABLE_YEARS` mock import with a `years: string[]` prop
    (the real years from `getYearsWithData()`, passed by the page in Plan 03). Signature:
    `OverviewHeader({ year, years }: { year: number; years: string[] })`. Render one `SelectItem`
    per year. `Select value` is `String(year)` and `onValueChange` calls the existing `update(next)`
    that sets `?year=`. This satisfies HEAD-01 (inline pill on title row) and HEAD-03 (only
    data-bearing years listed — the page supplies only `getYearsWithData()` results). Remove the
    "PROTOTYPE — wipe me" comment. Keep `aria-label="Anno"` on the trigger.
  </action>
  <verify>
    <automated>grep -q "export function OverviewHeader" components/dashboard/overview/overview-header.tsx && grep -q "'use client'" components/dashboard/overview/overview-header.tsx && grep -q "scroll: false" components/dashboard/overview/overview-header.tsx && grep -q "years: string\[\]\|years\b" components/dashboard/overview/overview-header.tsx && ! grep -q "AVAILABLE_YEARS" components/dashboard/overview/overview-header.tsx && echo "header-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `OverviewHeader` exported from `overview-header.tsx`, a `'use client'` component typed `{ year: number; years: string[] }`.
    - Title and year-pill Select render on the same flex row (HEAD-01); the title reads "Panoramica delle tue finanze".
    - Selecting a year calls `router.replace` with `?year=` set and `{ scroll: false }` (D-05).
    - The Select options come from the `years` prop, not a mock `AVAILABLE_YEARS` import (HEAD-03).
    - `aria-label="Anno"` present; no "PROTOTYPE" comment remains.
  </acceptance_criteria>
  <done>Header renders title + inline year pill driven by the real years prop; changing the pill updates ?year= via router.replace(scroll:false).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `components/dashboard/overview/format.ts`
- `components/dashboard/overview/kpi-card-reading.tsx`
- `components/dashboard/overview/kpi-row.tsx`
- `components/dashboard/overview/overview-header.tsx`

Exported symbols:
- `formatEur(value: string | number): string`
- `formatEurCompact(value: string | number): string`
- `ReadingKpiCard` (component) + `Reading` (type)
- `KpiRow` (component; props `{ data: OverviewData; year: number }`)
- `OverviewHeader` (component; props `{ year: number; years: string[] }`)

Helper functions (co-located, ported verbatim):
- `savingsReading(rate)`, `balanceReading(balance)`, `trendReading(delta, prevYear, kind)` in kpi-row.tsx
- `formatDelta`, `valueColor`, `deltaColor`, `sentimentColor` in kpi-card-reading.tsx
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → client component | `?year=` URL param read via `useSearchParams`; year pill writes `?year=` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | OverviewHeader `?year=` write | mitigate | Select options are constrained to the `years: string[]` prop (only `getYearsWithData()` values); arbitrary years cannot be chosen via the pill. Server-side resolution (Plan 03, D-04) defends against a hand-edited URL. |
| T-43-02 | Injection (XSS) | KpiRow / ReadingKpiCard rendered copy | accept | All rendered values are formatter output (numbers) and static Italian strings; `prevYear` is a number. React escapes by default. No `dangerouslySetInnerHTML`. No attacker-controlled string is rendered. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — all deps (recharts, shadcn ui, decimal.js) already present. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the four new files.
- `yarn check:language` passes (developer code English; only intentional Italian product copy).
- Reading thresholds match the proto exactly (50/30/20, balance sign, trend vs prior year).
</verification>

<success_criteria>
- Header pill + KPI cards exist as importable production components consuming real DAL types.
- Null deltas hide the badge; reading lines are sentiment-colored.
- No mock-data or PROTOTYPE references remain in the new files.
</success_criteria>

<output>
Create `.planning/phases/43-overview-shell/43-01-SUMMARY.md` when done.
</output>
