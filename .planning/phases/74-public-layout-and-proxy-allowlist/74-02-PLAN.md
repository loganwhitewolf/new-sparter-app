---
phase: 74-public-layout-and-proxy-allowlist
plan: 02
type: execute
wave: 2
depends_on: [74-01]
files_modified:
  - app/(public)/_components/site-header.tsx
  - app/(public)/_components/site-footer.tsx
  - app/(public)/layout.tsx
autonomous: true
requirements: [BRAND-03]

must_haves:
  truths:
    - "Every (public) page renders sticky SiteHeader + SiteFooter chrome with Italian labels and English hrefs from named route constants (BRAND-03, D-06, D-12)"
    - "Desktop header exposes Come funziona + Entra + Registrati; legal links stay footer-only (UI-SPEC)"
    - "Mobile Sheet (side right) lists Come funziona → Entra → Registrati → separator → Privacy → Termini → Dashboard, closes on navigate (D-08, D-09)"
    - "Footer always shows Dashboard text link to APP_ROUTES.dashboard (D-08); anon click is gated by proxy to login"
  artifacts:
    - path: app/(public)/_components/site-header.tsx
      provides: "Sticky desktop nav + mobile Sheet island"
      contains: "SiteHeader"
    - path: app/(public)/_components/site-footer.tsx
      provides: "Footer link inventory including Dashboard (D-08)"
      contains: "Dashboard"
    - path: app/(public)/layout.tsx
      provides: "Wires SiteHeader + main + SiteFooter; removes temporary tracer chrome"
      contains: "SiteHeader"
  key_links:
    - "Header/footer/Sheet hrefs import MARKETING_ROUTES, AUTH_PAGE_ROUTES, APP_ROUTES — no hard-coded path string literals (D-06)"
    - "layout.tsx imports colocated _components only — never authenticated app shell"
  prohibitions:
    - "No authenticated-app layout chrome, sidebar, bottom nav, or onboarding gate inside (public) (BRAND-03)"
    - "No components/marketing/* ; no Fraunces / branding display font (D-12)"
    - "No Pricing link anywhere in public chrome"
    - "No session-aware hide of Registrati (Phase 77 / BRAND-12 deferred)"
  assumptions:
    - "UI Considerations from 74-UI-SPEC are all covered (static RSC chrome; Sheet collapses overflow; footer flex-wrap; fixed Italian labels) — no unresolved UI states"
    - "Sheet order discretion: Dashboard last after legal links (RESEARCH recommendation)"
---

<objective>
Expand the tracer `(public)` shell into full UI-SPEC chrome: colocated `SiteHeader` (desktop + mobile Sheet) and `SiteFooter` with D-08 Dashboard link, wired through `app/(public)/layout.tsx` — Geist-only, no authenticated app chrome (BRAND-03, D-06, D-08, D-09, D-12).

Purpose: Make marketing navigation end-to-end clickable before stub bodies land in Plan 03.

Output: Sticky header/nav/footer on all `(public)` routes; mobile Sheet with full inventory including Dashboard.
</objective>

## Artifacts this phase produces

- `SiteHeader` (`app/(public)/_components/site-header.tsx`) — sticky desktop nav + client Sheet mobile menu.
- `SiteFooter` (`app/(public)/_components/site-footer.tsx`) — text-link footer including Dashboard (D-08).
- Updated `PublicLayout` — SiteHeader + `main.flex-1` + SiteFooter; temporary tracer wordmark removed.

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-public-layout-and-proxy-allowlist/74-01-SUMMARY.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-RESEARCH.md
@app/(public)/layout.tsx
@app/(auth)/layout.tsx
@components/ui/button.tsx
@components/ui/sheet.tsx
@components/layout/mobile-more-sheet.tsx
@lib/routes.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Build SiteHeader — desktop nav + mobile Sheet</name>
  <precondition>Plan 74-01 complete: app/(public)/layout.tsx exists and lib/routes.ts exports MARKETING_ROUTES, AUTH_PAGE_ROUTES, APP_ROUTES</precondition>
  <files>app/(public)/_components/site-header.tsx</files>
  <read_first>
    .planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md — Header desktop/mobile slots, sticky classes, Sheet a11y, typography, spacing.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md — site-header pattern; Sheet side right; close-on-navigate; Button asChild.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md — D-06 named constants; D-08 Dashboard in Sheet; D-09 always-visible Entra/Registrati; D-12 no marketing components dir.
    components/layout/mobile-more-sheet.tsx — Link inventory + onOpenChange(false) pattern only; do not copy side=bottom.
    components/ui/sheet.tsx — default side is right.
    lib/routes.ts — MARKETING_ROUTES, AUTH_PAGE_ROUTES, APP_ROUTES.
  </read_first>
  <action>
    Create `'use client'` `app/(public)/_components/site-header.tsx` exporting `SiteHeader` per UI-SPEC + D-08:

    Desktop (`md+`): sticky bar (`sticky top-0 z-40 border-b … h-14 max-w-6xl` inner); wordmark Sparter → `MARKETING_ROUTES.home` (`text-2xl font-semibold tracking-tight`); nav text link Come funziona → `MARKETING_ROUTES.howItWorks`; Entra outline `Button asChild size="sm"` → `AUTH_PAGE_ROUTES.login`; Registrati primary `Button asChild size="sm"` → `AUTH_PAGE_ROUTES.register`. No Privacy/Termini/Pricing in desktop header.

    Active nav treatment (D-12 / UI-SPEC Typography — pick one, do not combine): when the current pathname matches a nav href, apply `text-foreground font-semibold` (inactive: `text-muted-foreground`, hover → `text-foreground`). Use `usePathname()` (or equivalent) so Come funziona / Sheet product+legal links reflect the active route. Do not add underline in addition to semibold.

    Mobile (viewport below `md`): wordmark left; Registrati `size="sm"` always visible; Menu icon trigger `aria-label="Apri menu"` opens shadcn Sheet with `side="right"`. Sheet link order: Come funziona → Entra → Registrati → separator → Privacy → Termini → Dashboard (Dashboard last per discretion + D-08). Each Link closes the sheet on navigate. Close control `aria-label="Chiudi menu"`. Touch rows ≥ 44px. Use lucide `Menu` only. Href values only from named route constants (D-06) — including `APP_ROUTES.dashboard` for Dashboard. No session reads (D-09). Semantic color tokens only.
  </action>
  <acceptance_criteria>
    - File exports `SiteHeader` and is marked `'use client'`.
    - File imports `MARKETING_ROUTES`, `AUTH_PAGE_ROUTES`, and `APP_ROUTES` from `@/lib/routes`.
    - File contains `aria-label="Apri menu"` and Sheet usage with right side (explicit `side="right"` or default SheetContent without `side="bottom"`).
    - Visible Italian labels include Come funziona, Entra, Registrati, Privacy, Termini, and Dashboard.
    - Active route nav links use `text-foreground` + `font-semibold` (not underline) per UI-SPEC / D-12.
    - File has zero matches for the path substring `/pricing`.
  </acceptance_criteria>
  <verify>
    <automated>test -f 'app/(public)/_components/site-header.tsx' && grep -q 'SiteHeader' 'app/(public)/_components/site-header.tsx' && grep -q "use client" 'app/(public)/_components/site-header.tsx' && grep -q 'MARKETING_ROUTES' 'app/(public)/_components/site-header.tsx' && grep -q 'AUTH_PAGE_ROUTES' 'app/(public)/_components/site-header.tsx' && grep -q 'APP_ROUTES' 'app/(public)/_components/site-header.tsx' && grep -q 'Apri menu' 'app/(public)/_components/site-header.tsx' && grep -q 'font-semibold' 'app/(public)/_components/site-header.tsx' && grep -q 'usePathname' 'app/(public)/_components/site-header.tsx' && ! grep -q '/pricing' 'app/(public)/_components/site-header.tsx' && ! grep -q 'side="bottom"' 'app/(public)/_components/site-header.tsx'</automated>
  </verify>
  <done>SiteHeader ships sticky desktop nav and right Sheet mobile menu with D-08 Dashboard in Sheet inventory (D-06, D-08, D-09, D-12).</done>
</task>

<task type="auto" tdd="false">
  <name>Build SiteFooter + wire PublicLayout chrome</name>
  <precondition>SiteHeader exists at app/(public)/_components/site-header.tsx</precondition>
  <files>app/(public)/_components/site-footer.tsx, app/(public)/layout.tsx</files>
  <read_first>
    .planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md — Footer structure, link inventory, copyright, text-link styling.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md — D-08 Dashboard in footer (desktop) and Sheet (already in header task).
    .planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md — site-footer RSC pattern; layout shell adapt from auth.
    app/(public)/layout.tsx — replace temporary tracer chrome with SiteHeader + SiteFooter.
    app/(auth)/layout.tsx — wordmark type continuity reference only.
  </read_first>
  <action>
    1) Create RSC `app/(public)/_components/site-footer.tsx` exporting `SiteFooter`: `border-t border-border py-8` with inner `max-w-6xl mx-auto px-4 sm:px-6`. Wordmark link Sparter → `MARKETING_ROUTES.home`. Text links (`text-sm text-muted-foreground hover:text-foreground`, `flex-wrap gap-x-4 gap-y-2`): Come funziona, Privacy, Termini, Entra, Registrati, Dashboard — hrefs from `MARKETING_ROUTES` / `AUTH_PAGE_ROUTES` / `APP_ROUTES.dashboard` only (D-06, D-08). Copyright `© {new Date().getFullYear()} Sparter`. No Pricing. Prefer text links over primary Buttons in footer (accent reserved for header/shell CTAs).

    2) Update `app/(public)/layout.tsx` to import `SiteHeader` and `SiteFooter` from `./_components/*` and render: outer `flex min-h-screen flex-col bg-background` → SiteHeader → `main.flex-1` children → SiteFooter. Remove any temporary tracer-only wordmark chrome. Still no session reads; still no imports from authenticated app layout modules.
  </action>
  <acceptance_criteria>
    - `site-footer.tsx` exports `SiteFooter` and includes label Dashboard with href from `APP_ROUTES.dashboard` (or `APP_ROUTES` member access).
    - `site-footer.tsx` includes Come funziona, Privacy, Termini, Entra, Registrati labels and has zero `/pricing` matches.
    - `layout.tsx` imports both `SiteHeader` and `SiteFooter` and contains `main` with `flex-1`.
    - Under `app/(public)/`, zero import lines reference `app-shell`, `sidebar-provider`, or `dal/auth`.
    - Directory `components/marketing` does not exist.
  </acceptance_criteria>
  <verify>
    <automated>test -f 'app/(public)/_components/site-footer.tsx' && grep -q 'SiteFooter' 'app/(public)/_components/site-footer.tsx' && grep -q 'Dashboard' 'app/(public)/_components/site-footer.tsx' && grep -q 'APP_ROUTES' 'app/(public)/_components/site-footer.tsx' && grep -q 'SiteHeader' 'app/(public)/layout.tsx' && grep -q 'SiteFooter' 'app/(public)/layout.tsx' && ! grep -R --include='*.tsx' --include='*.ts' -E 'app-shell|sidebar-provider|dal/auth' 'app/(public)' && test ! -d components/marketing && yarn check:language && yarn vitest run tests/proxy-auth.test.ts</automated>
  </verify>
  <done>Public layout ships full UI-SPEC chrome with D-08 Dashboard in footer; BRAND-03 isolation held (D-06, D-08, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public chrome Links → routes | Href targets must match allowlisted English slugs or intentional app/auth destinations |
| Anon click Dashboard | Leaves public surface; proxy enforces login redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-07 | Elevation of Privilege | chrome href drift | high | mitigate | Named constants only (D-06); Plan 03 stubs for every marketing link target |
| T-74-08 | Spoofing | fake CTA destinations | medium | mitigate | Entra/Registrati only AUTH_PAGE_ROUTES; Dashboard only APP_ROUTES.dashboard |
| T-74-09 | Information Disclosure | session in chrome | low | accept | Phase 74 intentionally shows Entra+Registrati always (D-09); no session fetch |
| T-74-SC | Tampering | npm installs | high | accept | Zero new packages — reuse Button/Sheet |
</threat_model>

<verification>
Header/footer render on `/`; mobile Sheet includes Dashboard last; `yarn vitest run tests/proxy-auth.test.ts` still green; language check clean.
</verification>

<success_criteria>
BRAND-03 chrome contract met for header/nav/footer; D-06/D-08/D-09/D-12 satisfied; no authenticated shell leak.
</success_criteria>

<output>
Create `.planning/phases/74-public-layout-and-proxy-allowlist/74-02-SUMMARY.md` when done
</output>
