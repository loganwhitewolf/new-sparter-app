---
phase: 74-public-layout-and-proxy-allowlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/routes.ts
  - proxy.ts
  - app/page.tsx
  - app/(public)/layout.tsx
  - app/(public)/page.tsx
  - tests/proxy-auth.test.ts
autonomous: true
requirements: [BRAND-04, BRAND-05]

must_haves:
  truths:
    - "Anonymous GET / passes proxy without 307 to /login and renders the (public) homepage shell (brand + import-first supporting line + Registrati/Entra) — BRAND-04, BRAND-05, D-07, D-11"
    - "Authenticated GET / returns 307 Location /dashboard from proxy only — never from the RSC page (BRAND-05, D-01, D-02)"
    - "Authenticated GET /how-it-works is not bounced by AUTH_ROUTES (passes with 200 / no Location) — D-03"
    - "Anonymous GET /dashboard still 307 to /login (deny-by-default preserved) — BRAND-04, D-07"
  artifacts:
    - path: lib/routes.ts
      provides: "PUBLIC_MARKETING_ROUTES, AUTH_ROUTES, PUBLIC_ROUTES + MARKETING_ROUTES / AUTH_PAGE_ROUTES named maps + isPublicPath / isAuthPath"
      contains: "PUBLIC_MARKETING_ROUTES"
    - path: proxy.ts
      provides: "SoT-imported allowlist + smart-root auth / → dashboard; staging and next-action early returns preserved"
      contains: "isPublicPath"
    - path: app/(public)/page.tsx
      provides: "Minimal type-led homepage shell owning URL /"
      contains: "Carica i tuoi estratti"
    - path: app/(public)/layout.tsx
      provides: "Sibling public flex column shell (no authenticated app layout imports)"
      contains: "flex min-h-screen"
    - path: tests/proxy-auth.test.ts
      provides: "D-07 Vitest cases for anon home, auth home, auth deep link, gated path"
      contains: "authenticated home"
  key_links:
    - "proxy.ts imports isPublicPath / isAuthPath / MARKETING_ROUTES / APP_ROUTES from lib/routes.ts — local PUBLIC_ROUTES/AUTH_ROUTES arrays deleted (D-05)"
    - "path === MARKETING_ROUTES.home && isAuthenticated → 307 APP_ROUTES.dashboard after session, before final next() (D-01, D-02)"
    - "Same changeset: app/page.tsx deleted and app/(public)/page.tsx added (single owner of /)"
  prohibitions:
    - "No smart-root redirect() inside app/(public)/page.tsx or any (public) RSC (D-01)"
    - "No prefix match for marketing/auth paths — exact includes only; /proto keeps startsWith (D-04)"
    - "No authenticated-app layout chrome, sidebar provider, or verifySession hard-fail imported into (public) (BRAND-03)"
    - "No components/marketing/* directory; no Fraunces / branding display font load (D-12)"
    - "No fake legal prose on any page created in this plan"
  assumptions:
    - "Specless probe left BRAND-03/04/05 unclassified — predicates authored from CONTEXT D-01…D-12 + RESEARCH validation map"
    - "A1: session mock { user: { id } } satisfies !!session?.user in proxy tests"
    - "A2: next.config has no trailingSlash — exact path match expects no trailing slash on marketing routes"
---

<objective>
Prove the production-quality tracer path for BRAND-04 + BRAND-05: `lib/routes.ts` allowlist SoT wired into `proxy.ts` (exact match + smart root), and anonymous `/` owned by `app/(public)/page.tsx` after deleting the redirect-only root page — locked by D-07 Vitest cases.

Purpose: Catch dual-`/` ownership, missing smart-root, and allowlist drift on the first commit before chrome/stubs expand.

Output: Anon `/` reachable under `(public)`; auth `/` → `/dashboard` via proxy only; existing `next-action` / gated-path regressions green.

Unresolved edge note (specless): BRAND-04/05 probe items unresolved — covered here via D-01…D-07 + D-11 must_haves (not deferred).
</objective>

## Artifacts this phase produces

- `MARKETING_ROUTES` / `AUTH_PAGE_ROUTES` named maps — chrome href SoT (D-06); feed mandated arrays.
- `PUBLIC_MARKETING_ROUTES` / `AUTH_ROUTES` / `PUBLIC_ROUTES` — proxy membership arrays (D-05 exact names).
- `isPublicPath(path)` / `isAuthPath(path)` — exact-membership helpers for proxy (D-04).
- Smart-root branch in `proxy()` — auth `MARKETING_ROUTES.home` → `APP_ROUTES.dashboard` (D-01, D-02, BRAND-05).
- `PublicLayout` default export in `app/(public)/layout.tsx` — flex min-h-screen column shell (BRAND-03 skeleton).
- Homepage shell default export in `app/(public)/page.tsx` — type-led minimal `/` (D-11).
- D-07 cases in `tests/proxy-auth.test.ts` — anon home, auth home, auth `/how-it-works`, gated path regression.
- Deleted `app/page.tsx` — removes conflicting owner of `/`.

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-RESEARCH.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-VALIDATION.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-COVERAGE.md
@lib/routes.ts
@proxy.ts
@app/page.tsx
@tests/proxy-auth.test.ts
@app/(auth)/layout.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>End-to-end public / tracer — SoT allowlist + smart root + (public) home</name>
  <reversibility rating="costly">Implements D-01 (proxy-only smart root) and D-05 (shared PUBLIC_* / AUTH_* exports); undoing later requires coordinated proxy + chrome + test migration.</reversibility>
  <files>lib/routes.ts, proxy.ts, app/page.tsx, app/(public)/layout.tsx, app/(public)/page.tsx, tests/proxy-auth.test.ts</files>
  <read_first>
    .planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md — D-01…D-07, D-11 locked; do not reopen.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-RESEARCH.md — Pattern 1 routes SoT; Pattern 2 proxy order; Pitfall 1–2 dual-/ and smart-root; D-07 Vitest examples.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md — exact export shapes and proxy import list.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md — homepage shell copy + CTA pair only for this tracer (full header/footer = Plan 02).
    lib/routes.ts — keep APP_ROUTES untouched; add parallel maps (do not fold marketing into APP_ROUTES).
    proxy.ts — preserve STAGING_KEY and next-action blocks verbatim at top; delete local arrays.
    app/page.tsx — current redirect-only owner of /; must be deleted in this same task.
    tests/proxy-auth.test.ts — harness + existing three cases to keep.
  </read_first>
  <action>
    1) Extend `lib/routes.ts` per D-05/D-06 and RESEARCH Pattern 1: export `MARKETING_ROUTES` (`home: '/'`, `howItWorks: '/how-it-works'`, `privacy: '/privacy'`, `terms: '/terms'`), `AUTH_PAGE_ROUTES` (`login: '/login'`, `register: '/register'`), derived `PUBLIC_MARKETING_ROUTES`, `AUTH_ROUTES`, `PUBLIC_ROUTES` with those exact array export names, plus `isPublicPath` / `isAuthPath` using readonly-string includes. Do not add marketing keys onto `APP_ROUTES`.

    2) Refactor `proxy.ts` per D-01/D-02/D-03/D-04/D-05: import `APP_ROUTES`, `AUTH_PAGE_ROUTES`, `MARKETING_ROUTES`, `isAuthPath`, `isPublicPath` from `@/lib/routes`. Delete the file-local allowlist/auth arrays. Keep staging-key and next-action early returns first and unchanged in behavior. After session: bounce authenticated users on auth paths to `APP_ROUTES.dashboard`; then if path equals `MARKETING_ROUTES.home` and authenticated, redirect to `APP_ROUTES.dashboard` (smart root — must run even though home is public); then gate anon with `isPublicPath(path) || path.startsWith('/proto')`. Exact membership only for marketing/auth (D-04). Use `AUTH_PAGE_ROUTES.login` for the anon login redirect target.

    3) In one changeset: delete `app/page.tsx` and create `app/(public)/layout.tsx` + `app/(public)/page.tsx`. Layout: `flex min-h-screen flex-col bg-background` with a minimal temporary top wordmark link to `MARKETING_ROUTES.home` and `main.flex-1` children (Plan 02 replaces temporary chrome with SiteHeader/SiteFooter). Homepage shell per D-11 / UI-SPEC: centered `max-w-xl` column, brand Sparter (Geist only), supporting line exactly `Carica i tuoi estratti. Sparter li categorizza — senza collegare la banca.`, CTA group primary Registrati → `AUTH_PAGE_ROUTES.register` and outline Entra → `AUTH_PAGE_ROUTES.login` via `Button asChild` + `Link`, `size="lg"`, `gap-4`. No `redirect()` call in the page (D-01). No session reads in layout.

    4) Extend `tests/proxy-auth.test.ts` with D-07 cases (keep existing three): anon `/` status 200 and null Location; auth `/` status 307 Location `https://app.example.test/dashboard`; auth `/how-it-works` status 200 null Location; keep anon `/dashboard` → login regression. Mock shape `{ user: { id: 'u1' } }` is enough (RESEARCH A1).
  </action>
  <acceptance_criteria>
    - `lib/routes.ts` exports identifiers `PUBLIC_MARKETING_ROUTES`, `AUTH_ROUTES`, `PUBLIC_ROUTES`, `MARKETING_ROUTES`, `AUTH_PAGE_ROUTES`, `isPublicPath`, `isAuthPath`.
    - `proxy.ts` imports from `@/lib/routes` and has zero matches for a local `const PUBLIC_ROUTES =` or `const AUTH_ROUTES =` declaration.
    - `proxy.ts` compares home via `MARKETING_ROUTES.home` (or equivalent path === '/') for the authenticated dashboard redirect after session.
    - `app/page.tsx` does not exist; `app/(public)/page.tsx` and `app/(public)/layout.tsx` exist.
    - `app/(public)/page.tsx` contains the supporting-line Italian copy and does not call `redirect(`.
    - `tests/proxy-auth.test.ts` includes cases covering anonymous marketing home, authenticated home → dashboard, and authenticated marketing deep link.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f app/page.tsx && test -f 'app/(public)/page.tsx' && test -f 'app/(public)/layout.tsx' && grep -q 'PUBLIC_MARKETING_ROUTES' lib/routes.ts && grep -q 'isPublicPath' proxy.ts && ! grep -q 'redirect(' 'app/(public)/page.tsx' && yarn vitest run tests/proxy-auth.test.ts</automated>
  </verify>
  <done>Tracer green: SoT allowlist + proxy smart root + (public) owns `/`; D-07 Vitest suite passes (D-01, D-02, D-03, D-04, D-05, D-07, D-11).</done>
</task>

<task type="auto" tdd="false">
  <name>Isolation + language gates for tracer artifacts</name>
  <files>lib/routes.ts, proxy.ts, app/(public)/layout.tsx, app/(public)/page.tsx</files>
  <read_first>
    app/(public)/layout.tsx — confirm no imports from app/(app) layout or layout chrome modules.
    app/(public)/page.tsx — Geist-only shell; no marketing display font wiring.
    CLAUDE.md / AGENTS.md — English identifiers; Italian UI copy only.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-RESEARCH.md — Anti-Patterns: AppShell leak, next-action order.
  </read_first>
  <action>
    1) Confirm `proxy.ts` still short-circuits when `next-action` header is present before calling `getAuthSessionOrNull`, and still honors staging-key bypass before session (Pitfall 4). Do not reorder those blocks.

    2) Confirm `app/(public)/**` does not import authenticated-app shell modules (`components/layout/app-shell`, sidebar provider, or `verifySession` from dal/auth). Temporary layout chrome in this plan may stay inline; do not create a top-level marketing components directory.

    3) Run `yarn check:language` after route/string edits. Fix any new violations in touched files only.
  </action>
  <acceptance_criteria>
    - `proxy.ts` still contains the `next-action` header check before the `getAuthSessionOrNull` call site.
    - Under `app/(public)/`, zero import lines reference `app-shell`, `sidebar-provider`, or `dal/auth`.
    - Directory `components/marketing` does not exist.
    - `yarn check:language` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>! grep -R --include='*.tsx' --include='*.ts' -E 'app-shell|sidebar-provider|dal/auth' 'app/(public)' && test ! -d components/marketing && yarn check:language && yarn vitest run tests/proxy-auth.test.ts</automated>
  </verify>
  <done>Public tracer isolated from authenticated chrome; language check clean; proxy early returns intact (BRAND-03 isolation fence for later expansion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `proxy.ts` | Untrusted pathname; deny-by-default session gate |
| `proxy.ts` → RSC `(public)` | Only allowlisted paths reach marketing HTML anonymously |
| Staging header `x-staging-key` | Privileged bypass when `STAGING_KEY` env set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-01 | Elevation of Privilege | proxy.ts allowlist | high | mitigate | Exact SoT `isPublicPath` only; D-07 anon gated-path test; no marketing prefix match (D-04) |
| T-74-02 | Elevation of Privilege | AUTH_ROUTES misuse | high | mitigate | Keep AUTH_ROUTES = login/register only (D-03); auth deep-link test |
| T-74-03 | Tampering | x-pathname header | medium | mitigate | Keep existing overwrite of x-pathname/x-search on every next() |
| T-74-04 | Tampering | next-action bypass | high | mitigate | Preserve next-action short-circuit before session; existing Vitest case |
| T-74-05 | Spoofing / Open Redirect | smart-root redirect | medium | mitigate | Fixed destination `APP_ROUTES.dashboard` only (D-02) |
| T-74-06 | Elevation of Privilege | STAGING_KEY | high | mitigate | Existing rule: never set STAGING_KEY on Railway prod; bypass stays first |
| T-74-SC | Tampering | npm installs | high | accept | Zero new packages — no install surface |
</threat_model>

<verification>
`yarn vitest run tests/proxy-auth.test.ts` green; `app/page.tsx` absent; anon `/` renderable under `(public)`; auth `/` redirect only in proxy.
</verification>

<success_criteria>
BRAND-04 + BRAND-05 tracer truths hold; D-01…D-05 + D-07 + D-11 implemented; no dual owner of `/`.
</success_criteria>

<output>
Create `.planning/phases/74-public-layout-and-proxy-allowlist/74-01-SUMMARY.md` when done
</output>
