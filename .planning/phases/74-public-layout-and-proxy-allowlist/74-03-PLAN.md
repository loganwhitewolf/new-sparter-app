---
phase: 74-public-layout-and-proxy-allowlist
plan: 03
type: execute
wave: 3
depends_on: [74-02]
files_modified:
  - app/(public)/how-it-works/page.tsx
  - app/(public)/privacy/page.tsx
  - app/(public)/terms/page.tsx
autonomous: false
requirements: [BRAND-03]

must_haves:
  truths:
    - "Anon visitor can open /how-it-works, /privacy, /terms under (public) chrome and see heading + body 'Contenuto in arrivo.' + Torna alla home → / (D-10, BRAND-03)"
    - "Chrome nav/footer links to those paths do not 404 (UI-SPEC stub destinations; UI Considerations error coverage)"
    - "No fake legal/GDPR prose on stub pages (D-10)"
  artifacts:
    - path: app/(public)/how-it-works/page.tsx
      provides: "Come funziona stub RSC"
      contains: "Contenuto in arrivo"
    - path: app/(public)/privacy/page.tsx
      provides: "Privacy stub RSC"
      contains: "Contenuto in arrivo"
    - path: app/(public)/terms/page.tsx
      provides: "Termini stub RSC"
      contains: "Contenuto in arrivo"
  key_links:
    - "Stub Torna alla home → MARKETING_ROUTES.home (D-06, D-10)"
    - "Stub routes are members of PUBLIC_MARKETING_ROUTES so proxy allows anon (BRAND-04 already in Plan 01)"
  prohibitions:
    - "No fake legal prose / GDPR claims on stubs (D-10)"
    - "No full how-it-works explainer body (Phase 75 deferred)"
    - "No legal MDX bodies (Phase 76 deferred)"
    - "No components/marketing/* ; no Fraunces (D-12)"
  assumptions:
    - "Specless BRAND-03 unclassified edge resolved by D-10 stub contract + UI-SPEC stub table + Plan 02 chrome"
---

<objective>
Ship D-10 stub pages for every chrome-linked marketing path (`/how-it-works`, `/privacy`, `/terms`) so nav never 404s, then human-verify the full Phase 74 chrome + allowlist + smart-root experience (BRAND-03 completion).

Purpose: Close the link inventory loop after Plan 02 chrome; prove UI-SPEC stub + isolation visually.

Output: Three stub RSCs + approved human checkpoint for phase gate.
</objective>

## Artifacts this phase produces

- `HowItWorksPage` — `app/(public)/how-it-works/page.tsx` stub (heading Come funziona).
- `PrivacyPage` — `app/(public)/privacy/page.tsx` stub (heading Privacy).
- `TermsPage` — `app/(public)/terms/page.tsx` stub (heading Termini).
- Shared stub body copy — `Contenuto in arrivo.` + `Torna alla home` → `MARKETING_ROUTES.home`.

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-public-layout-and-proxy-allowlist/74-02-SUMMARY.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md
@.planning/phases/74-public-layout-and-proxy-allowlist/74-VALIDATION.md
@lib/routes.ts
@app/(public)/layout.tsx
@app/(public)/_components/site-header.tsx
@app/(public)/_components/site-footer.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Ship stub pages for how-it-works, privacy, terms</name>
  <precondition>Plan 74-02 complete: SiteHeader/SiteFooter wired in app/(public)/layout.tsx; MARKETING_ROUTES exported</precondition>
  <files>app/(public)/how-it-works/page.tsx, app/(public)/privacy/page.tsx, app/(public)/terms/page.tsx</files>
  <read_first>
    .planning/phases/74-public-layout-and-proxy-allowlist/74-CONTEXT.md — D-10 stub contract; D-06 constants; D-12 scope fence.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-UI-SPEC.md — Stub table + Heading/Body typography roles; Torna alla home optional text link.
    .planning/phases/74-public-layout-and-proxy-allowlist/74-PATTERNS.md — shared stub RSC pattern.
    lib/routes.ts — MARKETING_ROUTES.home for back link.
  </read_first>
  <action>
    Create three RSC pages under `app/(public)/` sharing the D-10 stub pattern from PATTERNS:

    - `how-it-works/page.tsx` — h1 `Come funziona`
    - `privacy/page.tsx` — h1 `Privacy`
    - `terms/page.tsx` — h1 `Termini`

    Each: container `mx-auto max-w-xl px-4 py-12 sm:px-6`; body paragraph exactly `Contenuto in arrivo.`; text link `Torna alla home` → `MARKETING_ROUTES.home` with muted hover classes. Typography: heading `text-xl font-semibold`; body `text-base`. Italian UI only; English route segments. Do not invent legal/GDPR paragraphs. Do not add marketing component extractions or display-font imports.
  </action>
  <acceptance_criteria>
    - All three page files exist under `app/(public)/how-it-works/`, `privacy/`, and `terms/`.
    - Each file contains the exact stub body string `Contenuto in arrivo.` and link label `Torna alla home`.
    - Each file imports `MARKETING_ROUTES` (or uses it for the home href).
    - Across the three stub files, zero matches for substrings `GDPR`, `sub-processor`, or `trattamento dei dati`.
    - Directory `components/marketing` does not exist.
  </acceptance_criteria>
  <verify>
    <automated>test -f 'app/(public)/how-it-works/page.tsx' && test -f 'app/(public)/privacy/page.tsx' && test -f 'app/(public)/terms/page.tsx' && grep -q 'Contenuto in arrivo' 'app/(public)/how-it-works/page.tsx' && grep -q 'Contenuto in arrivo' 'app/(public)/privacy/page.tsx' && grep -q 'Contenuto in arrivo' 'app/(public)/terms/page.tsx' && grep -q 'Torna alla home' 'app/(public)/how-it-works/page.tsx' && ! grep -E 'GDPR|sub-processor|trattamento dei dati' 'app/(public)/how-it-works/page.tsx' 'app/(public)/privacy/page.tsx' 'app/(public)/terms/page.tsx' && test ! -d components/marketing && yarn check:language && yarn vitest run tests/proxy-auth.test.ts</automated>
  </verify>
  <done>Three D-10 stubs render under (public) chrome with Torna alla home; no fake legal copy (BRAND-03, D-10).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify public chrome + smart root + stubs</name>
  <what-built>
    Phase 74 end-to-end: SoT allowlist + proxy smart root (Plan 01), SiteHeader/SiteFooter with D-08 Dashboard (Plan 02), stub pages for Come funziona / Privacy / Termini (this plan).
  </what-built>
  <how-to-verify>
    1. Incognito (anon): open `/` — marketing homepage shell (brand + supporting line + Registrati/Entra); sticky header + footer; no sidebar / bottom nav / onboarding.
    2. Anon: click Come funziona, Privacy, Termini from chrome — each shows `Contenuto in arrivo.` + Torna alla home; still public chrome.
    3. Anon: click Dashboard in footer — expect redirect to `/login` (proxy gate).
    4. Logged-in: open `/` — expect redirect to `/dashboard` (not marketing shell).
    5. Logged-in: open `/how-it-works` — page loads (no bounce to dashboard).
    6. Mobile width: open Sheet — order includes Dashboard after legal links; closes on navigate.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stub legal routes | Public HTML must not claim policies before Phase 76 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-74-10 | Repudiation / Compliance | stub legal pages | medium | mitigate | Exact D-10 copy only — `Contenuto in arrivo.`; acceptance grep blocks GDPR-like prose |
| T-74-11 | Information Disclosure | stub pages | low | accept | Intentionally empty until Phase 75/76 |
| T-74-SC | Tampering | npm installs | high | accept | Zero new packages |
</threat_model>

<verification>
Automated stub + proxy + language gates green; human checkpoint confirms chrome isolation and smart root.
</verification>

<success_criteria>
BRAND-03 stub destinations live; D-10 honored; human approves phase UX for chrome + allowlist + smart root.
</success_criteria>

<output>
Create `.planning/phases/74-public-layout-and-proxy-allowlist/74-03-SUMMARY.md` when done
</output>
