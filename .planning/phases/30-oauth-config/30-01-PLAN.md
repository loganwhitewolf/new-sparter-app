---
phase: 30-oauth-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/registration.ts
  - lib/actions/auth.ts
  - app/api/auth/[...all]/route.ts
  - tests/registration-config.test.ts
  - tests/auth-actions-registration.test.ts
  - tests/auth-route-registration.test.ts
  - tests/production-smoke.test.ts
  - tests/production-smoke.spec.ts
autonomous: true
requirements:
  - REG-01
tags:
  - auth
  - registration
  - cleanup

must_haves:
  truths:
    - "lib/auth/registration.ts no longer exists"
    - "signUpAction in lib/actions/auth.ts proceeds directly to RegisterSchema.safeParse with no registration guard"
    - "app/api/auth/[...all]/route.ts is reduced to a single destructured export from toNextJsHandler(auth)"
    - "No file in lib/ or app/ imports from @/lib/auth/registration"
    - "yarn tsc --noEmit passes"
    - "yarn test passes (registration test files are deleted, not failing)"
  artifacts:
    - path: "lib/actions/auth.ts"
      provides: "signUpAction without registration guard"
      contains: "export async function signUpAction"
    - path: "app/api/auth/[...all]/route.ts"
      provides: "Better Auth catch-all route"
      contains: "export const { GET, POST } = toNextJsHandler(auth)"
  key_links:
    - from: "lib/actions/auth.ts"
      to: "auth.api.signUpEmail"
      via: "direct call after RegisterSchema parse — no guard"
      pattern: "auth\\.api\\.signUpEmail"
    - from: "app/api/auth/[...all]/route.ts"
      to: "auth"
      via: "toNextJsHandler(auth) destructured export"
      pattern: "toNextJsHandler\\(auth\\)"
---

<objective>
Eliminate the registration guardrail entirely per REG-01 / D-01 / D-02.

Purpose: Remove the `isRegistrationEnabled()` / `REGISTRATION_DISABLED_MESSAGE` / `RegistrationEnv` module and every consumer of it, so any user can register via email/password or (after Phase 31) OAuth. No residual references, comments, or TODOs.

Output:
- `lib/auth/registration.ts` deleted
- 3 registration test files deleted
- `lib/actions/auth.ts` — registration import + guard removed
- `app/api/auth/[...all]/route.ts` — simplified to canonical Better Auth Next.js form
- `tests/production-smoke.test.ts` — disabled-signup test blocks removed
- `tests/production-smoke.spec.ts` — disabled-registration helper + test removed; enabled-registration skip guard removed
</objective>

<execution_context>
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-oauth-config/30-CONTEXT.md
@.planning/phases/30-oauth-config/30-RESEARCH.md
@.planning/phases/30-oauth-config/30-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Current state of files being modified. Read these before editing. -->

From lib/auth/registration.ts (TO BE DELETED — 30 lines):
- `export type RegistrationEnv = { REGISTRATION_ENABLED?: string | undefined }`
- `export const REGISTRATION_DISABLED_MESSAGE = 'Le registrazioni sono temporaneamente disabilitate.'`
- `export function isRegistrationEnabled(env?: RegistrationEnv): boolean`

From lib/actions/auth.ts:
- Line 7: `import { isRegistrationEnabled, REGISTRATION_DISABLED_MESSAGE } from '@/lib/auth/registration'`
- Lines 39–41 (inside `signUpAction`): `if (!isRegistrationEnabled()) { return { error: REGISTRATION_DISABLED_MESSAGE } }`
- Followed by blank line 42 before `const parsed = RegisterSchema.safeParse(...)` on line 43.

From app/api/auth/[...all]/route.ts (27 lines):
- Line 1: `import { auth } from '@/auth'`
- Line 2: `import { REGISTRATION_DISABLED_MESSAGE, isRegistrationEnabled } from '@/lib/auth/registration'`
- Line 3: `import { toNextJsHandler } from 'better-auth/next-js'`
- Line 5: `const authHandlers = toNextJsHandler(auth)`
- Lines 7–9: `function isEmailSignUpRequest(request: Request): boolean { ... }`
- Line 11: `export const GET = authHandlers.GET`
- Lines 13–27: `export function POST(request: Request) { if (!isRegistrationEnabled() && isEmailSignUpRequest(request)) { return Response.json({ error: { code: 'registration_disabled', message: REGISTRATION_DISABLED_MESSAGE } }, { status: 403 }) } return authHandlers.POST(request) }`

Target final shape (after edit) — exactly these three lines:
```typescript
import { auth } from '@/auth'
import { toNextJsHandler } from 'better-auth/next-js'

export const { GET, POST } = toNextJsHandler(auth)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete registration module + 3 dedicated test files</name>
  <files>
    lib/auth/registration.ts (delete),
    tests/registration-config.test.ts (delete),
    tests/auth-actions-registration.test.ts (delete),
    tests/auth-route-registration.test.ts (delete)
  </files>
  <read_first>
    - lib/auth/registration.ts (confirm exports before deletion)
    - tests/registration-config.test.ts (confirm it only tests isRegistrationEnabled)
    - tests/auth-actions-registration.test.ts (confirm it only tests the removed guard)
    - tests/auth-route-registration.test.ts (confirm it only tests the removed route guard)
  </read_first>
  <action>
    Delete the four files using `rm` (no replacements, no rename):

    ```bash
    rm lib/auth/registration.ts
    rm tests/registration-config.test.ts
    rm tests/auth-actions-registration.test.ts
    rm tests/auth-route-registration.test.ts
    ```

    Do NOT keep stub/empty replacement files. Do NOT leave comments referring to the deleted module elsewhere. After this task, importing from `@/lib/auth/registration` MUST be a TypeScript error in the consumer files (Tasks 2 and 3 fix those imports in the same plan).

    Per D-01: full elimination — no residual references, no comments, no TODOs.
  </action>
  <verify>
    <automated>test ! -e lib/auth/registration.ts && test ! -e tests/registration-config.test.ts && test ! -e tests/auth-actions-registration.test.ts && test ! -e tests/auth-route-registration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -e lib/auth/registration.ts` returns exit code 1 (file does not exist)
    - `test -e tests/registration-config.test.ts` returns exit code 1
    - `test -e tests/auth-actions-registration.test.ts` returns exit code 1
    - `test -e tests/auth-route-registration.test.ts` returns exit code 1
    - `ls lib/auth/ 2>/dev/null | grep -c '^registration\.ts$'` equals 0
  </acceptance_criteria>
  <done>Four files removed from the working tree; no replacements created.</done>
</task>

<task type="auto">
  <name>Task 2: Remove registration guard from signUpAction</name>
  <files>lib/actions/auth.ts</files>
  <read_first>
    - lib/actions/auth.ts (current file — confirm line 7 import and lines 39–41 guard block exist exactly as documented)
  </read_first>
  <action>
    Apply two edits to `lib/actions/auth.ts`:

    **Edit A — Delete the import on line 7.** Remove this exact line:
    ```typescript
    import { isRegistrationEnabled, REGISTRATION_DISABLED_MESSAGE } from '@/lib/auth/registration'
    ```
    Do NOT replace it. The line count drops by 1.

    **Edit B — Delete the guard block inside `signUpAction`.** Remove these exact three lines (current lines 39–41) and the blank line 42 that separates them from `const parsed`:
    ```typescript
      if (!isRegistrationEnabled()) {
        return { error: REGISTRATION_DISABLED_MESSAGE }
      }

    ```
    After the edit, the body of `signUpAction` MUST start (after the function signature and opening brace) with:
    ```typescript
      const parsed = RegisterSchema.safeParse({
        email: formData.get('email'),
        password: formData.get('password'),
      })
    ```

    Do NOT modify `signInAction`, `signOutAction`, error handling, or `getSafeSignUpErrorMessage`/`logSanitizedAuthError` imports. Do NOT add a TODO/FIXME/comment referencing the removed guard.
  </action>
  <verify>
    <automated>grep -v '^#' lib/actions/auth.ts | grep -cE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|@/lib/auth/registration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' lib/actions/auth.ts | grep -cE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|@/lib/auth/registration"` equals 0
    - `grep -c "export async function signUpAction" lib/actions/auth.ts` equals 1
    - `grep -A1 "export async function signUpAction" lib/actions/auth.ts | grep -c "_prev"` equals 1 (function signature unchanged)
    - `grep -c "const parsed = RegisterSchema.safeParse" lib/actions/auth.ts` equals 1
    - `grep -c "auth.api.signUpEmail" lib/actions/auth.ts` equals 1 (call site preserved)
  </acceptance_criteria>
  <done>signUpAction no longer references the registration module; signature, schema parse, and sign-up call are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Simplify the Better Auth catch-all route handler</name>
  <files>app/api/auth/[...all]/route.ts</files>
  <read_first>
    - app/api/auth/[...all]/route.ts (current 27-line file — confirm guard logic and toNextJsHandler import already present)
  </read_first>
  <action>
    Replace the entire contents of `app/api/auth/[...all]/route.ts` with exactly these four lines (including a trailing newline):

    ```typescript
    import { auth } from '@/auth'
    import { toNextJsHandler } from 'better-auth/next-js'

    export const { GET, POST } = toNextJsHandler(auth)
    ```

    This deletes:
    - The `@/lib/auth/registration` import (line 2)
    - The `authHandlers` const (line 5)
    - The `isEmailSignUpRequest` helper (lines 7–9)
    - The named `GET` export (line 11)
    - The named `POST(request: Request)` function with its guard branch (lines 13–27)

    The destructured export form `export const { GET, POST } = toNextJsHandler(auth)` is the canonical Better Auth Next.js App Router shape (per RESEARCH.md §4 and Pitfall 4). Do not add fallback exports, comments, or wrapper functions.
  </action>
  <verify>
    <automated>test "$(wc -l < app/api/auth/[...all]/route.ts)" -le 4 && grep -c "toNextJsHandler(auth)" app/api/auth/[...all]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l < 'app/api/auth/[...all]/route.ts'` is less than or equal to 4
    - `grep -c "export const { GET, POST } = toNextJsHandler(auth)" 'app/api/auth/[...all]/route.ts'` equals 1
    - `grep -cE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|isEmailSignUpRequest|registration_disabled" 'app/api/auth/[...all]/route.ts'` equals 0
    - `grep -c "import { auth } from '@/auth'" 'app/api/auth/[...all]/route.ts'` equals 1
    - `grep -c "import { toNextJsHandler } from 'better-auth/next-js'" 'app/api/auth/[...all]/route.ts'` equals 1
  </acceptance_criteria>
  <done>Route file contains only the two imports and the destructured handler export; all guard logic gone.</done>
</task>

<task type="auto">
  <name>Task 4: Strip disabled-signup blocks from production smoke tests</name>
  <files>tests/production-smoke.test.ts, tests/production-smoke.spec.ts</files>
  <read_first>
    - tests/production-smoke.test.ts (current 273-line file — confirm the two blocks below exist)
    - tests/production-smoke.spec.ts (current 189-line file — confirm helper and disabled-registration test block)
  </read_first>
  <action>
    **File A: `tests/production-smoke.test.ts`** — delete these two blocks (verbatim, including their surrounding blank lines):

    1. The single `it('passes healthy runtime and disabled-signup rejection while printing only safe fields', async () => { ... })` block (around lines 142–188 per PATTERNS.md). It is the one that calls `runSmoke([..., '--expect-disabled-signup'])` and asserts a `disabled_signup` phase. Locate it by `grep -n "expect-disabled-signup\|disabled-signup rejection" tests/production-smoke.test.ts`.
    2. The entire `describe('production-smoke CLI disabled-signup phase', () => { ... })` block (around lines 235–273 per PATTERNS.md). Locate by `grep -n "production-smoke CLI disabled-signup phase" tests/production-smoke.test.ts`.

    Delete the blocks only. Preserve every other test (config validation, health phase healthy/degraded/malformed, enabled signup, R2 health) untouched.

    **File B: `tests/production-smoke.spec.ts`** — apply three edits:

    1. Delete the `expectDisabledSignup` helper function (around lines 66–79). Locate by `grep -n "function expectDisabledSignup\|expectDisabledSignup" tests/production-smoke.spec.ts`.
    2. Delete the `test('disabled registration rejects direct signup while existing login still works', ...)` block (around lines 147–159). Locate by `grep -n "disabled registration rejects direct signup" tests/production-smoke.spec.ts`.
    3. Inside the `test('enabled registration creates a disposable user...')` block, delete the single line that reads `test.skip(envFlag('PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED'), ...)` (around line 133). Locate by `grep -n "PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED" tests/production-smoke.spec.ts`. The test must remain intact otherwise.

    Do NOT remove the `'enabled registration creates a disposable user and reaches dashboard'` test body, the `'optional R2 browser import reaches analyze route...'` test, or any other test/imports. Do NOT add new tests.

    After edits, `tests/production-smoke.spec.ts` must still compile and reference only existing helpers.
  </action>
  <verify>
    <automated>grep -cE "expect-disabled-signup|disabled-signup rejection|production-smoke CLI disabled-signup phase" tests/production-smoke.test.ts | grep -q '^0$' && grep -cE "expectDisabledSignup|PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED|disabled registration rejects direct signup" tests/production-smoke.spec.ts | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "expect-disabled-signup|disabled-signup rejection|production-smoke CLI disabled-signup phase" tests/production-smoke.test.ts` equals 0
    - `grep -cE "expectDisabledSignup|PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED|disabled registration rejects direct signup" tests/production-smoke.spec.ts` equals 0
    - `grep -c "enabled registration creates a disposable user" tests/production-smoke.spec.ts` equals 1 (test preserved)
    - `grep -c "optional R2 browser import" tests/production-smoke.spec.ts` equals 1 (test preserved)
    - `grep -c "registration_disabled" tests/production-smoke.test.ts` equals 0
  </acceptance_criteria>
  <done>Both smoke files are free of disabled-signup references; preserved tests still present.</done>
</task>

<task type="auto">
  <name>Task 5: Verify no residual references and gates pass</name>
  <files>(verification only — touches no source files)</files>
  <read_first>
    - lib/actions/auth.ts (post-edit)
    - app/api/auth/[...all]/route.ts (post-edit)
  </read_first>
  <action>
    Run the residual-reference audit from RESEARCH.md §Acceptance Validation Commands plus the project gates. All commands MUST return the expected output. Do NOT modify code in this task — if a check fails, return to the prior task that owns the file and fix it.

    Commands to run:

    ```bash
    # 1. No registration imports in lib/ or app/
    grep -rE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|RegistrationEnv|@/lib/auth/registration" lib/ app/ --include="*.ts" --include="*.tsx"
    # Expected: zero matches (exit code 1)

    # 2. No registration tests left under tests/
    ls tests/ | grep -E "^(registration-config|auth-actions-registration|auth-route-registration)\.test\.ts$"
    # Expected: zero matches (exit code 1)

    # 3. TypeScript gate
    yarn tsc --noEmit
    # Expected: exit code 0

    # 4. Test gate (project-wide)
    yarn test
    # Expected: exit code 0
    ```

    Note: the `tests/auth-errors.test.ts` description containing the word "registration" is INTENTIONAL (it describes generic sign-up error handling, per RESEARCH.md §6) — do NOT modify that file.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|RegistrationEnv|@/lib/auth/registration" lib/ app/ --include="*.ts" --include="*.tsx" | wc -l` equals 0
    - `ls tests/ 2>/dev/null | grep -cE "^(registration-config|auth-actions-registration|auth-route-registration)\.test\.ts$"` equals 0
    - `yarn tsc --noEmit` exits with code 0
    - `yarn test` exits with code 0
  </acceptance_criteria>
  <done>All gates green; zero residual references to the removed registration module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → `/api/auth/sign-up/email` | Untrusted public sign-up requests now reach Better Auth directly (the guardrail that returned 403 is gone) |
| anonymous client → `signUpAction` server action | Untrusted form submissions now reach Better Auth directly through the server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Spoofing (S) | `signUpAction` in `lib/actions/auth.ts` | mitigate | `RegisterSchema.safeParse` is preserved on the immediately-following line after guard removal; Better Auth's `signUpEmail` performs the actual identity creation; generic error messaging via `getSafeSignUpErrorMessage` is unchanged to avoid account enumeration. |
| T-30-02 | Tampering (T) | `app/api/auth/[...all]/route.ts` | mitigate | The destructured `export const { GET, POST } = toNextJsHandler(auth)` is the canonical Better Auth shape — it preserves all of Better Auth's request validation, CSRF/session handling, and method routing. No custom handler logic that could be bypassed. |
| T-30-03 | Repudiation (R) | sign-up error logging | accept | `logSanitizedAuthError('sign_up', error)` call in `signUpAction` is preserved; audit logging behavior is unchanged from the pre-guard state. |
| T-30-04 | Information disclosure (I) | sign-up error responses | mitigate | `getSafeSignUpErrorMessage` (preserved, unchanged) returns generic messages; no field-level Zod errors leak; no Better Auth internals exposed. Comment on line ~63 of `lib/actions/auth.ts` ("D-06: keep credential/account errors generic...") remains accurate. |
| T-30-05 | Denial of service (D) | open public sign-up endpoint | accept | Removing the guardrail is the explicit phase intent (REG-01). Rate-limiting / CAPTCHA are not in scope for this milestone (no requirement exists). Documented as accepted risk; revisit if abuse appears. Better Auth's `minPasswordLength: 8` and `autoSignIn` config remain in `auth.ts`. |
| T-30-06 | Elevation of privilege (E) | sign-up role assignment | mitigate | `auth.ts` `additionalFields.role` defaults to `'user'` with `input: false` (existing config, unchanged). A new sign-up cannot self-assign `admin`. `subscriptionPlan` defaults to `'free'` with `input: false`. No regression from guard removal. |
</threat_model>

<verification>
- `grep -rE "isRegistrationEnabled|REGISTRATION_DISABLED_MESSAGE|RegistrationEnv|@/lib/auth/registration" lib/ app/ tests/ --include="*.ts" --include="*.tsx"` returns zero lines
- `yarn tsc --noEmit` exits 0
- `yarn test` exits 0
- The four deleted files do not exist on disk
- `app/api/auth/[...all]/route.ts` has at most 4 lines and contains the canonical destructured export
</verification>

<success_criteria>
1. `lib/auth/registration.ts`, `tests/registration-config.test.ts`, `tests/auth-actions-registration.test.ts`, `tests/auth-route-registration.test.ts` no longer exist.
2. `lib/actions/auth.ts` imports nothing from `@/lib/auth/registration` and `signUpAction` proceeds directly to schema parse.
3. `app/api/auth/[...all]/route.ts` is exactly the canonical Better Auth Next.js shape (≤4 lines).
4. `tests/production-smoke.test.ts` and `tests/production-smoke.spec.ts` contain no `disabled-signup`, `expectDisabledSignup`, or `PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED` references; preserved tests still pass.
5. `yarn tsc --noEmit` and `yarn test` both exit 0.
6. REG-01 acceptance: any user can hit the sign-up server action / route without encountering an ALLOWED_EMAIL / registration_disabled error.
</success_criteria>

<output>
After completion, create `.planning/phases/30-oauth-config/30-01-SUMMARY.md` documenting the residual-reference audit results, gate output, and any preserved tests in the modified smoke files.
</output>
