---
phase: 73-proto-design-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/proto/layout.tsx
  - app/proto/branding/page.tsx
  - app/proto/branding/prototype-switcher.tsx
  - app/proto/branding/variant-a.tsx
  - app/proto/branding/fonts.ts
  - app/proto/branding/assets/overview-hero-placeholder.svg
autonomous: true
requirements: [BRAND-01]
user_setup:
  - service: local-proto-gate
    why: "Local smoke of /proto/branding requires PROTOTYPES_ENABLED at request time"
    env_vars:
      - name: PROTOTYPES_ENABLED
        source: "Export PROTOTYPES_ENABLED=1 before yarn dev (Vercel Preview already Preview-scoped)"

must_haves:
  truths:
    - "With PROTOTYPES_ENABLED set, visitor opens /proto/branding and sees Variant A (shot-as-plane) end-to-end: Sparter brand hero signal, one Italian import-first headline, one supporting line, Registrati+Entra CTAs, dominant product visual plane (D-01, D-02, D-04, D-05)"
    - "Floating PrototypeSwitcher is always rendered under the proto gate and cycles ?variant= via router.replace; invalid/missing variant defaults to a (D-05)"
    - "Proto layout still force-dynamic + robots noindex + notFound without PROTOTYPES_ENABLED; main chrome no longer applies p-4/md:p-6 so heroes can be full-bleed (D-09)"
  artifacts:
    - path: app/proto/layout.tsx
      provides: "Full-bleed proto main (padding removed); gate + noindex unchanged"
      contains: "PROTOTYPES_ENABLED"
    - path: app/proto/branding/page.tsx
      provides: "Hub RSC awaiting searchParams Promise; mounts active variant + switcher"
      contains: "searchParams"
    - path: app/proto/branding/prototype-switcher.tsx
      provides: "Client floating compare chrome (proto tool, not design under review)"
      contains: "PrototypeSwitcher"
    - path: app/proto/branding/variant-a.tsx
      provides: "Shot-as-plane A+B first viewport + two benefits + closing CTA"
      contains: "VariantA"
    - path: app/proto/branding/fonts.ts
      provides: "Scoped marketing display face CSS variable"
      contains: "brandingDisplay"
    - path: app/proto/branding/assets/overview-hero-placeholder.svg
      provides: "Labeled placeholder until real overview capture replaces it"
  key_links:
    - "page.tsx await searchParams → whitelist a|b|c → VariantA | stub B/C → PrototypeSwitcher"
    - "brandingDisplay.variable applied only on branding wrapper (root Geist untouched)"
    - "CTA Links hardcoded to /register and /login via shadcn Button asChild"
---

<objective>
Ship the production-quality tracer path for BRAND-01: full-bleed proto shell + hub `/proto/branding?variant=` + always-visible switcher + Variant A (shot-as-plane) inside locked A+touch-of-B direction (D-01), with scoped marketing typography and a labeled hero placeholder.

Purpose: Prove Preview-gated compare UX end-to-end on one structural axis before expanding B/C. Catch padding / NODE_ENV / scope-leak failures on the first commit.

Output: Runnable `/proto/branding` (default variant a) under existing `PROTOTYPES_ENABLED` gate; Variant B/C may stub until Plan 02.

Planning note (specless probe): `init.plan-phase` returned `phase_req_ids: null` because ROADMAP lacked `### Phase 73:` detail — probe predicates intentionally not generated; coverage via CONTEXT D-01…D-09 + REQUIREMENTS BRAND-01/02 must_haves instead.
</objective>

## Artifacts this phase produces

- `app/proto/branding/` hub (page + switcher + three variants + fonts + assets + NOTES.md) — Compare surface for BRAND-01; NOTES handoff for BRAND-02 (Plans 01–03).
- Full-bleed `app/proto/layout.tsx` chrome — Enables edge-to-edge heroes without inventing a second gate.
- No `app/(public)/`, no `proxy.ts` allowlist expansion, no drizzle schema — Phase 74–73 / schema out of scope.

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-proto-design-variants/73-CONTEXT.md
@.planning/phases/73-proto-design-variants/73-RESEARCH.md
@.planning/phases/73-proto-design-variants/73-PATTERNS.md
@.planning/phases/73-proto-design-variants/73-VALIDATION.md
@.planning/phases/73-proto-design-variants/73-COVERAGE.md
@app/proto/layout.tsx
@app/fonts.ts
@components/ui/button.tsx
@~/.claude/skills/prototype/UI.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>End-to-end /proto/branding tracer — Variant A shot-as-plane</name>
  <reversibility rating="costly">Implements locked D-01 A+touch-of-B; Phase 75 promotes the eventual winner into components/marketing — undoing after promotion rewrites production marketing UI.</reversibility>
  <files>app/proto/layout.tsx, app/proto/branding/page.tsx, app/proto/branding/prototype-switcher.tsx, app/proto/branding/variant-a.tsx</files>
  <read_first>
    app/proto/layout.tsx — keep dynamic/force-dynamic, robots noindex, PROTOTYPES_ENABLED notFound; only change main className padding (Pitfall 1 / 73-PATTERNS).
    .planning/phases/73-proto-design-variants/73-PATTERNS.md — hub page pattern, overview switcher @ cbafe29 (always visible), CTA Button asChild pattern, D-02/D-03 composition constraints.
    .planning/phases/73-proto-design-variants/73-CONTEXT.md — D-01…D-09 locked decisions (do not reopen).
    .planning/phases/73-proto-design-variants/73-RESEARCH.md — structural axis a = Shot-as-plane; never hide switcher on build-mode checks; whitelist variant.
    app/(auth)/login/page.tsx — living searchParams Promise await pattern.
    components/ui/button.tsx — asChild + Slot confirmed.
    app/(app)/onboarding/_components/step-5-outro.tsx — Italian primary/secondary CTA pair analog.
  </read_first>
  <action>
    1) Edit `app/proto/layout.tsx`: remove `p-4 md:p-6` from the wrapping main so children own inset; do not alter the env gate, `dynamic`, or robots metadata (D-09).

    2) Create hub `app/proto/branding/page.tsx` as an async RSC. Props type uses searchParams as a Promise of an object with optional variant string (Next.js 16 page convention). Await params; whitelist so only `b` and `c` select those keys, otherwise `a` (D-05). Never echo the raw query string into markup. Render `VariantA` when active; for `b`/`c` render a minimal labeled stub panel saying the axis name so the switcher path works before Plan 02 fills them. Mount `PrototypeSwitcher` with `current={variant}`. Top-of-file comment: PROTOTYPE — wipe me. Stay under app/proto/branding only — do not edit proxy.ts.

    3) Create `app/proto/branding/prototype-switcher.tsx` as `'use client'`. Adapt overview switcher @ cbafe29: VARIANTS keys `a|b|c` with English axis names Shot-as-plane / Editorial split / Type-led stack; `router.replace(pathname + ?variant=, { scroll: false })`; ArrowLeft/ArrowRight with input/textarea/contentEditable guard; fixed bottom-center high-contrast mono pill clearly labeled as proto tool chrome (D-05 discretion). Always render under proto — do not gate visibility on build-mode environment checks (Preview runs production mode). Italian aria-labels on prev/next buttons.

    4) Create `app/proto/branding/variant-a.tsx` exporting `VariantA` — structural axis shot-as-plane (D-01, D-06 discretion): full-bleed product visual as atmosphere plane; quiet band for brand wordmark Sparter (hero-level), one Italian outcome headline, one import-first supporting line (estratti/file framing — privacy-aligned per D-04), CTA group primary Registrati → `/register` and secondary Entra → `/login` via `Button asChild` + `Link` (D-02). Below fold: exactly two short Italian benefit blocks + closing CTA repeating Registrati (D-03). Composition must not use card grids, stat strips, floating badges/overlays on the media, or offer/tier language. For this tracer task, the product visual may be a full-bleed colored frame with visible Italian label that it is a placeholder for the overview capture — Plan 01 Task 2 swaps in the SVG/`next/image` asset. Optional subtle CSS/`tw-animate-css` entrance only; no new packages. Distinct marketing type may use Tailwind serif utilities temporarily until fonts.ts lands in Task 2 (D-08).
  </action>
  <acceptance_criteria>
    - `app/proto/layout.tsx` still contains `PROTOTYPES_ENABLED`, `force-dynamic`, and `robots`; main className no longer includes both `p-4` and `md:p-6`.
    - `app/proto/branding/page.tsx` awaits `searchParams` and defaults unknown variants to `a`.
    - `app/proto/branding/prototype-switcher.tsx` exports `PrototypeSwitcher` and contains `router.replace`; file has zero matches for the identifier `NODE_ENV` (build-mode hide forbidden).
    - `app/proto/branding/variant-a.tsx` exports `VariantA` and includes Link hrefs `/register` and `/login` with visible labels Registrati and Entra.
    - Directory `app/(public)` does not exist after this task.
  </acceptance_criteria>
  <verify>
    <automated>test ! -d 'app/(public)' && test -f app/proto/branding/page.tsx && test -f app/proto/branding/variant-a.tsx && test -f app/proto/branding/prototype-switcher.tsx && ! grep -q 'NODE_ENV' app/proto/branding/prototype-switcher.tsx && grep -q 'PROTOTYPES_ENABLED' app/proto/layout.tsx && ! grep -E 'p-4 md:p-6' app/proto/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Tracer path compiles: full-bleed proto layout + hub with switcher + VariantA shot-as-plane A+B composition runnable under PROTOTYPES_ENABLED=1 at /proto/branding (D-01, D-02, D-03, D-04, D-05, D-09).</done>
</task>

<task type="auto" tdd="false">
  <name>Scoped branding fonts + labeled hero placeholder asset</name>
  <files>app/proto/branding/fonts.ts, app/proto/branding/assets/overview-hero-placeholder.svg, app/proto/branding/variant-a.tsx, app/proto/branding/page.tsx</files>
  <read_first>
    app/fonts.ts — next/font/google shape with `variable` + `display: 'swap'` (do not modify root fonts or app/layout.tsx).
    .planning/phases/73-proto-design-variants/73-RESEARCH.md Pattern 2 + Pattern 3 — scoped display face; next/image priority; D-08 anti-cliché list.
    app/proto/branding/variant-a.tsx — replace temporary visual frame with shared asset.
    app/proto/branding/page.tsx — apply brandingDisplay.variable on wrapper only.
  </read_first>
  <action>
    1) Create `app/proto/branding/fonts.ts` exporting `brandingDisplay` from `next/font/google` with CSS variable `--font-branding-display`, subsets latin, display swap. Pick one editorial face that avoids the D-08 cliché stacks (no purple-gradient marketing look; no warm-cream terracotta Playfair-style pairing; no broadsheet hairline system). Document the chosen family name in a one-line comment. Apply `brandingDisplay.variable` on the branding page wrapper only — never change `app/layout.tsx` Geist wiring (D-08).

    2) Create `app/proto/branding/assets/overview-hero-placeholder.svg` (or webp if a real capture is available in-repo — prefer real `/dashboard/overview` capture when present; otherwise SVG placeholder). The placeholder must include visible text that it is a temporary overview stand-in (Italian or English developer label OK on the asset itself). Wire it into `VariantA` via `next/image` with `priority` for LCP; no floating badges/overlays on the media (D-02, D-04). Shared asset path must be importable by Plan 02 variants later.

    3) Zero new npm packages. Do not touch drizzle schema/migrations.
  </action>
  <acceptance_criteria>
    - `app/proto/branding/fonts.ts` exports `brandingDisplay` and defines variable `--font-branding-display`.
    - `app/proto/branding/page.tsx` references `brandingDisplay.variable`.
    - At least one file exists under `app/proto/branding/assets/` and `variant-a.tsx` imports that path through `next/image` (default: `overview-hero-placeholder.svg`; real capture under `assets/` also valid).
    - `package.json` dependency block unchanged this task (no new packages).
    - No files under `drizzle/` modified.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/proto/branding/fonts.ts && grep -q 'brandingDisplay' app/proto/branding/fonts.ts && grep -q 'brandingDisplay' app/proto/branding/page.tsx && ls app/proto/branding/assets/* >/dev/null && grep -q 'next/image' app/proto/branding/variant-a.tsx && grep -qE 'assets/' app/proto/branding/variant-a.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Marketing display font scoped to branding hub; VariantA uses next/image hero asset (placeholder or real capture) with priority (D-01, D-04, D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview browser → `/proto/*` | Unauthenticated throwaway UI; gated by PROTOTYPES_ENABLED in layout |
| Query string `?variant=` | Untrusted input; must be whitelisted before render selection |
| CTA navigation | Leaves proto for auth routes; paths must be fixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-73-01 | Information Disclosure | app/proto/layout.tsx | high | mitigate | Keep existing robots noindex + force-dynamic env gate; do not remove PROTOTYPES_ENABLED check; Production stays 404 |
| T-73-02 | Tampering | app/proto/branding/page.tsx variant param | medium | mitigate | Whitelist only a\|b\|c; never interpolate raw query into HTML |
| T-73-03 | Spoofing | VariantA CTA Links | medium | mitigate | Hardcode href `/register` and `/login` only |
| T-69-04 | Elevation of Privilege (social) | PrototypeSwitcher chrome | low | mitigate | High-contrast mono pill reads as proto tool; NOTES documents Preview-only access |
| T-69-05 | Denial of Service | N/A static RSC | low | accept | Static marketing markup; no heavy server work |
| T-69-SC | Tampering | npm installs | high | accept | Zero new packages this phase — no install surface |
</threat_model>

<verification>
With PROTOTYPES_ENABLED=1, open /proto/branding — Variant A full-bleed hero renders, switcher visible, CTAs resolve to auth routes, tsc clean. Without env, /proto/branding 404s.
</verification>

<success_criteria>
Tracer BRAND-01 path works end-to-end for variant a; layout padding fixed; fonts+placeholder wired; no (public) scope leak.
</success_criteria>

<output>
Create `.planning/phases/73-proto-design-variants/73-01-SUMMARY.md` when done
</output>
