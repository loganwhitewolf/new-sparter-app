---
phase: 69-proto-design-variants
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - app/proto/branding/variant-b.tsx
  - app/proto/branding/variant-c.tsx
  - app/proto/branding/page.tsx
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "Visitor can flip among three structurally different A+B variants (shot-as-plane, editorial split, type-led stack) via switcher / ?variant=a|b|c — not three color tweaks (D-01, D-05, D-06)"
    - "Each variant keeps first-viewport contract: Sparter brand + one Italian headline + import-first sub + Registrati/Entra + one dominant product visual; below fold two benefits + closing CTA (D-02, D-03, D-04)"
    - "Scroll silhouette differs across a/b/c (hierarchy/visual weight), sharing fonts/asset helpers only — no shared Layout shell that collapses structural difference (D-06, D-08)"
  artifacts:
    - path: app/proto/branding/variant-b.tsx
      provides: "Editorial-split VariantB"
      contains: "VariantB"
    - path: app/proto/branding/variant-c.tsx
      provides: "Type-led-stack VariantC"
      contains: "VariantC"
    - path: app/proto/branding/page.tsx
      provides: "Hub mounts real B/C instead of stubs"
  key_links:
    - "page.tsx variant===b → VariantB; variant===c → VariantC"
    - "Shared hero asset from Plan 01 assets/ reused without overlays"
---

<objective>
Expand the tracer hub to full BRAND-01 compare surface: replace B/C stubs with two more A+touch-of-B variants on locked structural axes — editorial split and type-led stack (D-01, D-06).

Purpose: Give PO three radically different layouts on one Preview URL so design direction can lock before Phase 71.

Output: Complete `VariantB` + `VariantC`; hub page wires all three; stubs removed.
</objective>

## Artifacts this phase produces

- `app/proto/branding/variant-b.tsx` — Editorial-split marketing composition (asymmetric type column + large product frame).
- `app/proto/branding/variant-c.tsx` — Type-led stack (oversized brand/headline; product band under type).
- Updated `app/proto/branding/page.tsx` — Real three-way mount (no stubs).

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-proto-design-variants/69-CONTEXT.md
@.planning/phases/69-proto-design-variants/69-RESEARCH.md
@.planning/phases/69-proto-design-variants/69-PATTERNS.md
@.planning/phases/69-proto-design-variants/69-01-PLAN.md
@app/proto/branding/page.tsx
@app/proto/branding/variant-a.tsx
@app/proto/branding/fonts.ts
@app/proto/branding/assets/overview-hero-placeholder.svg
@components/ui/button.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Variant B — editorial split axis</name>
  <reversibility rating="costly">Part of D-01 A+B compare set; winner may be promoted in Phase 71.</reversibility>
  <files>app/proto/branding/variant-b.tsx, app/proto/branding/page.tsx</files>
  <read_first>
    app/proto/branding/variant-a.tsx — composition contract + CTA + image import pattern to mirror semantically, not layout.
    app/proto/branding/page.tsx — replace stub for key b with VariantB import.
    .planning/phases/69-proto-design-variants/69-RESEARCH.md Recommended Structural Axes table — axis b Editorial split.
    .planning/phases/69-proto-design-variants/69-CONTEXT.md D-02 D-03 D-04 D-06 D-08.
    ~/.claude/skills/prototype/UI.md — variants must be structurally different; avoid sharing a Layout shell.
  </read_first>
  <action>
    Create `app/proto/branding/variant-b.tsx` exporting `VariantB`. Axis: editorial split — asymmetric magazine hierarchy with a strong type column and a large product frame beside/offset (not the same full-bleed band silhouette as VariantA). Still A+touch-of-B (D-01): real/placeholder overview visual + editorial typography via `--font-branding-display`.

    First viewport (D-02): Sparter as hero-level brand signal; one Italian outcome headline; one import-first supporting line (file/estratti framing, privacy-aligned — D-04); CTA group Registrati primary → `/register`, Entra outline → `/login` via Button asChild; one dominant product visual using the shared Plan 01 asset through next/image; no card grids, no stat strips, no badges/overlays on the media, no offer/tier language.

    Below fold (D-03): exactly two short Italian benefit blocks + closing CTA. Copy may differ from VariantA but stay honest/import-first. Reuse tiny helpers (CtaGroup / image) if useful; do not share a page Layout component with A/C.

    Update `page.tsx` to import and render `VariantB` when variant is `b` (remove stub). PROTOTYPE wipe-me header comment. No drizzle, no proxy, no packages.
  </action>
  <acceptance_criteria>
    - `app/proto/branding/variant-b.tsx` exports `VariantB` and includes `/register` and `/login` Link targets with Registrati and Entra labels.
    - `app/proto/branding/page.tsx` imports VariantB and renders it for variant `b`.
    - `variant-b.tsx` references the shared hero asset path under `app/proto/branding/assets/` or the same import used by VariantA.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/proto/branding/variant-b.tsx && grep -q 'export function VariantB' app/proto/branding/variant-b.tsx && grep -q 'VariantB' app/proto/branding/page.tsx && grep -q '/register' app/proto/branding/variant-b.tsx && grep -q '/login' app/proto/branding/variant-b.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Editorial-split VariantB live on ?variant=b with D-02/D-03/D-04 composition (D-01, D-06).</done>
</task>

<task type="auto" tdd="false">
  <name>Variant C — type-led stack axis</name>
  <reversibility rating="costly">Part of D-01 A+B compare set; winner may be promoted in Phase 71.</reversibility>
  <files>app/proto/branding/variant-c.tsx, app/proto/branding/page.tsx</files>
  <read_first>
    app/proto/branding/variant-a.tsx and variant-b.tsx — ensure C silhouette differs from both (type-led stack vs shot-as-plane vs editorial split).
    app/proto/branding/page.tsx — replace stub for key c.
    .planning/phases/69-proto-design-variants/69-RESEARCH.md axis c Type-led stack.
    .planning/phases/69-proto-design-variants/69-CONTEXT.md D-02 D-03 D-04 D-06 D-08 D-09.
  </read_first>
  <action>
    Create `app/proto/branding/variant-c.tsx` exporting `VariantC`. Axis: type-led stack — oversized brand + headline dominate the first screen; product visual is a full-width band immediately under the type/CTA block (distinct scroll silhouette from A and B).

    Same content contracts as B: Italian import-first copy (D-04); Sparter brand hero signal; Registrati/Entra CTAs to `/register`/`/login`; shared hero asset without overlays; two benefit blocks + closing CTA below fold (D-02, D-03). Stay inside A+touch-of-B (D-01). Avoid D-08 cliché atmospheres (purple gradients, warm-cream terracotta serif pairing, broadsheet hairlines, glow, emoji clusters).

    Update `page.tsx` so variant `c` mounts `VariantC` and no labeled stub panels remain for b/c. Confirm all three axes switchable. Throwaway only — do not extract components/marketing (D-09).
  </action>
  <acceptance_criteria>
    - `app/proto/branding/variant-c.tsx` exports `VariantC` with `/register` and `/login` CTAs labeled Registrati and Entra.
    - `app/proto/branding/page.tsx` imports VariantA, VariantB, and VariantC.
    - Files `variant-a.tsx`, `variant-b.tsx`, `variant-c.tsx` all exist.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/proto/branding/variant-c.tsx && grep -q 'export function VariantC' app/proto/branding/variant-c.tsx && grep -q 'VariantC' app/proto/branding/page.tsx && grep -q 'VariantA' app/proto/branding/page.tsx && grep -q 'VariantB' app/proto/branding/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Three structural variants mounted on hub; BRAND-01 compare surface complete (D-05, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query `?variant=` | Same whitelist as Plan 01 |
| Italian marketing copy | Must not invent bank-linking or paid-tier promises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-06 | Spoofing | VariantB/C CTAs | medium | mitigate | Same hardcoded `/register` `/login` only |
| T-69-07 | Tampering | variant param | medium | mitigate | Existing page whitelist unchanged |
| T-69-08 | Information Disclosure | proto pages | high | mitigate | Inherit layout noindex + env gate; do not weaken |
| T-69-09 | Repudiation | marketing claims | medium | mitigate | Import-first / privacy-aligned Italian copy only (D-04); no bank-link or tier claims |
| T-69-SC | Tampering | npm installs | high | accept | Zero new packages |
</threat_model>

<verification>
PROTOTYPES_ENABLED=1 yarn dev — open /proto/branding and cycle a→b→c; each layout silhouette differs; CTAs work; tsc/lint clean on app/proto/branding/**.
</verification>

<success_criteria>
BRAND-01 fully satisfied locally: three structural A+B variants on one hub with switcher.
</success_criteria>

<output>
Create `.planning/phases/69-proto-design-variants/69-02-SUMMARY.md` when done
</output>
