---
phase: 84-category-detail-and-cleanup
plan: 3
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - lib/dal/dashboard.ts
  - tests/helpers/reimbursement-test-db.ts
  - tests/amortization-lens-regression.test.ts
  - tests/reimbursement-regression.test.ts
  - tests/dashboard-dal.test.ts
autonomous: true
requirements: [RETIRE-02]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "getCategoriesBreakdown, getCategoryRanking and getCategoryDetail take an explicit {from,to,type} range instead of a DashboardFilters/preset object; getMonthlyTrendByNature takes an explicit {from,to} range instead of a bare preset string — no caller anywhere in app/lib/components/tests still passes a preset-shaped argument to any of the four (RETIRE-02, D-15)."
    - "getCategoryDeviations, getDeviationDateRanges, buildDeviationDataset, DEVIATION_NOISE_THRESHOLD, and the dead getOverview/getOverviewComparisonRanges/previousDashboardPresetDateRange chain no longer exist in lib/dal/dashboard.ts."
    - "The regression harness's captureAggregationSnapshot passes the SAME already-in-scope dateRange to every function it calls (never re-deriving it), and the amortization/reimbursement regression suites assert byte-identical expected values before and after this plan — RETIRE-02's own gate."
    - "tests/pace-engine-lens-regression.test.ts (the Phase 82 RETIRE-05 canary) requires zero code changes and still passes, proving this plan touched nothing it depends on."
  artifacts:
    - "lib/dal/dashboard.ts: getCategoriesBreakdown/getCategoryRanking/getCategoryDetail/getMonthlyTrendByNature re-signed to explicit-range parameters"
    - "tests/helpers/reimbursement-test-db.ts: lastMonthRange() test-local helper (byte-identical to dashboardPresetToDateRange('last-month'))"
  key_links:
    - "tests/amortization-lens-regression.test.ts / tests/reimbursement-regression.test.ts -> tests/helpers/reimbursement-test-db.ts (captureAggregationSnapshot) -> lib/dal/dashboard.ts (re-signed functions) — the D-16 byte-identical gate"
---

<objective>
Retirement part 1 (D-17): change the shared aggregation DAL's filter shape from preset-based to explicit date-range (D-15) on the four functions the Categories detail page's OLD code path used, delete the now-truly-dead Deviation machinery and the already-dead `getOverview`/`getOverviewComparisonRanges` chain from `lib/dal/dashboard.ts`, and rewire every regression-suite call site accordingly (D-16) — with every surviving expected value staying byte-identical, since the covered date range is provably unchanged. This plan does NOT delete any file, does NOT touch `lib/utils/date.ts`/`lib/utils/dashboard.ts`/`lib/validations/dashboard.ts`/`lib/routes.ts`/`CONTEXT.md`, and does NOT run the D-19 exit grep — that is Plan 84-04's job. By the end of Plan 84-02, no app/component code called any of the four functions with the OLD signature; this plan's job is purely the DAL/test layer that D-17 explicitly sequences AFTER the build.

Purpose: make the D-14 hard deletions in Plan 84-04 possible without touching a still-live signature.
Output: `getCategoriesBreakdown`/`getCategoryRanking`/`getCategoryDetail`/`getMonthlyTrendByNature` take explicit ranges; the regression gate passes unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-category-detail-and-cleanup/84-CONTEXT.md
@.planning/phases/84-category-detail-and-cleanup/84-RESEARCH.md
@lib/dal/dashboard.ts
@lib/utils/date.ts
@tests/helpers/reimbursement-test-db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-sign the 4 DAL functions; delete Deviation machinery and the dead getOverview chain from lib/dal/dashboard.ts</name>
  <files>lib/dal/dashboard.ts</files>
  <read_first>
    lib/dal/dashboard.ts lines 1-53 (imports — you will drop `dashboardPresetToDateRange`, `DashboardFilters`, `DashboardPreset`, `buildDeviationMap` from these; KEEP `computeBreakdownPercentages`, `computeDeltaPercent`, `computeSavingsRate`, `monthLabel`, `monthsBetween`, `DateRange`);
    lib/dal/dashboard.ts lines 207-221 (`DeviationData`, `DeviationDateRanges`, `CategoryDeviationsInput` types — deleted alongside their only producer/consumer);
    lib/dal/dashboard.ts lines 326-406 (`previousDashboardPresetDateRange`, `getOverviewComparisonRanges`, `DEVIATION_NOISE_THRESHOLD`, `getDeviationDateRanges`, `buildDeviationDataset` — all deleted; `getOverviewComparisonRanges` has exactly one live import, `tests/dashboard-dal.test.ts`, which Task 3 of this plan updates);
    lib/dal/dashboard.ts lines 1161-1179 (`getOverview` — dead, zero importers verified in 84-RESEARCH.md §"getOverview in dashboard.ts is dead code" — delete outright, the live Overview DAL is `lib/dal/overview.ts:130` and is untouched);
    lib/dal/dashboard.ts lines 1181-1244 (`getCategoriesBreakdown`) and 1246-1308 (`getCategoryRanking`) — both currently `(filters: DashboardFilters, ledgerRowSource?)`, both call `dashboardPresetToDateRange(filters.preset)` and `filters.type === 'all' ? undefined : eq(direction.code, filters.type)`;
    lib/dal/dashboard.ts lines 1397-1508 (`getCategoryDeviations` — deleted outright) and lines 1510-1754 (`getCategoryDetail` — currently `(categoryId, filters: DashboardFilters, ledgerRowSource?)`, calls `dashboardPresetToDateRange(filters.preset)` at line ~1517 and NEVER reads `filters.type` anywhere else in its body);
    lib/dal/dashboard.ts lines 1756-1815 (`getMonthlyTrendByNature` — currently `(preset: DashboardPreset, ledgerRowSource?)`, calls `dashboardPresetToDateRange(preset)`; this one groups by nature and has no `type` filter at all — its new signature has no `type` field, unlike the other three).
  </read_first>
  <action>
    Change `getCategoriesBreakdown`'s and `getCategoryRanking`'s first parameter from `filters: DashboardFilters` to `{ from, to, type }: { from: Date; to: Date; type: 'in'|'out'|'all' }`; delete the `dashboardPresetToDateRange(filters.preset)` line in each, using the destructured `from`/`to` directly; change every remaining `filters.type` reference to `type`. Change `getCategoryDetail`'s second parameter from `filters: DashboardFilters` to `{ from, to, type }: { from: Date; to: Date; type: 'in'|'out'|'all' }` (the `type` field is accepted for signature symmetry with the two functions above but genuinely unused inside this function's body, exactly as `filters.type` was never read before this change — this is a byte-identical, not merely equivalent, change); delete its `dashboardPresetToDateRange(filters.preset)` line, destructure `from`/`to` directly. Change `getMonthlyTrendByNature`'s first parameter from `preset: DashboardPreset` to `{ from, to }: { from: Date; to: Date }`; delete its `dashboardPresetToDateRange(preset)` line, destructure `from`/`to` directly.

    Delete, in their entirety: `previousDashboardPresetDateRange`, `getOverviewComparisonRanges`, `getOverview`, `DEVIATION_NOISE_THRESHOLD`, `getDeviationDateRanges`, `buildDeviationDataset`, `getCategoryDeviations`, and the types `DeviationData`, `DeviationDateRanges`, `CategoryDeviationsInput`. Drop the now-unused `dashboardPresetToDateRange`, `DashboardFilters`, `DashboardPreset`, `buildDeviationMap` imports; keep every other import untouched (`computeBreakdownPercentages`, `computeDeltaPercent`, `computeSavingsRate`, `monthLabel`, `monthsBetween`, `DateRange`, everything from `@/lib/db/schema`, `@/lib/dal/dashboard-filters`, `@/lib/dal/covered-months`, `@/lib/services/pace-and-projection`).
  </action>
  <reversibility rating="costly">Every call site across the app, this file's own regression-test consumers, and both Task 2/3 of this plan depend on the new shape landing correctly in one atomic edit — reverting requires restoring the old signatures AND the deleted symbols in lock-step with the test rewiring in Tasks 2-3.</reversibility>
  <verify>
    <automated>yarn typecheck 2>&1 | grep -c "lib/dal/dashboard.ts" ; echo "expected: TypeScript errors here are expected until Task 2/3 update the test call sites in the same plan"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DashboardFilters\|DashboardPreset\|dashboardPresetToDateRange\|buildDeviationMap\|getCategoryDeviations\|getDeviationDateRanges\|buildDeviationDataset\|DEVIATION_NOISE_THRESHOLD\|previousDashboardPresetDateRange\|getOverviewComparisonRanges" lib/dal/dashboard.ts` returns zero matches.
    - `grep -n "export const getOverview = " lib/dal/dashboard.ts` returns zero matches (the dead preset-shaped `getOverview` is gone; `lib/dal/overview.ts`'s own `getOverview` is a different file, untouched).
    - `getCategoriesBreakdown`, `getCategoryRanking`, `getCategoryDetail` each declare a first (or second, for `getCategoryDetail`) parameter destructuring `from`, `to`, `type`; `getMonthlyTrendByNature` declares `from`, `to` with no `type` field.
  </acceptance_criteria>
  <done>The four aggregation functions and lib/dal/dashboard.ts's dead/Deviation symbols are re-signed/removed; the file compiles once Tasks 2-3 update the remaining call sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire captureAggregationSnapshot and the amortization regression suite to the new signatures</name>
  <files>tests/helpers/reimbursement-test-db.ts, tests/amortization-lens-regression.test.ts</files>
  <read_first>
    tests/helpers/reimbursement-test-db.ts lines 254-333 (`CaptureAggregationSnapshotInput`, `captureAggregationSnapshot` — the local `filters` object at line ~293, the `Promise.all` array at lines ~297-319, and the returned object literal at lines ~321-332 — both must lose the `getCategoryDeviations` entry TOGETHER, keeping every other entry positionally aligned; an off-by-one here silently reassigns every subsequent snapshot value);
    tests/amortization-lens-regression.test.ts lines 1-40 (imports, `dashboardPresetToDateRange` import at line 13) and lines 160-215 (the two direct `dashboardModule.getCategoriesBreakdown(filters)`/`getCategoryRanking(filters)`/`getCategoryDeviations(...)`/`getCategoryDetail(...)`/`getMonthlyTrendByNature(filters.preset)` blocks — cash arm ~175-181, accrual arm ~201-207 — both call `dashboardPresetToDateRange('last-month')` earlier in the file (line ~66) to build `dateRange`, which is ALREADY the exact range these two blocks should use instead of re-deriving `filters`).
  </read_first>
  <behavior>
    - lastMonthRange() must return byte-identical values to lib/utils/date.ts's current `dashboardPresetToDateRange('last-month')` branch: `{ from: new Date(now.getFullYear(), now.getMonth()-1, 1), to: new Date(now.getFullYear(), now.getMonth(), 0, 23,59,59,999) }` — copy this exact arithmetic, do not "simplify" it, since D-16 forbids any change in the covered period.
  </behavior>
  <action>
    Add an exported `lastMonthRange(now: Date = new Date()): { from: Date; to: Date }` to `tests/helpers/reimbursement-test-db.ts`, byte-identical to `dashboardPresetToDateRange('last-month')`'s current branch (per `<behavior>`). In `captureAggregationSnapshot`, delete the local `const filters = {...}` line; build `const range = { from: dateRange.from, to: dateRange.to, type: 'all' as const }` from the ALREADY-IN-SCOPE `dateRange` parameter; change the 4 call sites `getCategoriesBreakdown(filters)` -> `getCategoriesBreakdown(range)`, `getCategoryRanking(filters)` -> `getCategoryRanking(range)`, `getCategoryDetail(categoryId, filters)` -> `getCategoryDetail(categoryId, range)`, `getMonthlyTrendByNature(filters.preset)` -> `getMonthlyTrendByNature({ from: dateRange.from, to: dateRange.to })`; DELETE the `getCategoryDeviations` entry from BOTH the `Promise.all` destructuring array AND the returned object literal (snapshot goes from 10 keys to 9) — keep every other array position and object key exactly where it already is, do not reorder or rename anything else.

    In `tests/amortization-lens-regression.test.ts`, replace both `const filters = { preset: 'last-month' as const, type: 'all' as const, sort: 'amount' as const }` blocks (cash arm and accrual arm) with `const range = { from: dateRange.from, to: dateRange.to, type: 'all' as const }` (the `dateRange` local already in scope from line ~66's `dashboardPresetToDateRange('last-month')` call — do NOT replace THAT call in this file with `lastMonthRange()`, since it is not inside `captureAggregationSnapshot` and this file's own `dashboardPresetToDateRange` import stays valid until Plan 84-04 deletes the production symbol; only the call-SHAPE passed to the 4 DAL functions changes here). Update the two `Promise.all` blocks' 5 call sites (`getCategoriesBreakdown`, `getCategoryRanking`, `getCategoryDetail`, `getMonthlyTrendByNature`, and drop `getCategoryDeviations` entirely) to the new `range`/`{from,to}` shapes, in both the cash arm (no `ledgerRowSource` arg) and the accrual arm (`ledgerEntryAccrual` second arg). Delete the 2 `cashDeviation`/`deviationsAccrual`-derived assertions that read the now-removed `getCategoryDeviations` result.
  </action>
  <reversibility rating="costly">Regression-suite call sites and Task 1's DAL signatures must land together for the suite to compile at all — an isolated revert of this task alone would break the build.</reversibility>
  <verify>
    <automated>yarn test tests/amortization-lens-regression.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test tests/amortization-lens-regression.test.ts --run` passes with the SAME expected amounts already hardcoded in the file ('30.00' cash, '10.00' accrual) — unchanged by this task, since the covered period is provably identical.
    - `grep -n "getCategoryDeviations" tests/amortization-lens-regression.test.ts tests/helpers/reimbursement-test-db.ts` returns zero matches.
    - `captureAggregationSnapshot`'s returned object has exactly 9 keys (was 10); every surviving key's position in both the `Promise.all` array and the object literal is unchanged relative to the original file.
  </acceptance_criteria>
  <done>The amortization lens regression suite passes unchanged against the new signatures; getCategoryDeviations no longer appears in the harness or that suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire reimbursement-regression.test.ts and dashboard-dal.test.ts's dead-code assertions</name>
  <files>tests/reimbursement-regression.test.ts, tests/dashboard-dal.test.ts</files>
  <read_first>
    tests/reimbursement-regression.test.ts (grep your own read for `dashboardPresetToDateRange('last-month')` — roughly 14 call sites building a local `dateRange` before calling `captureAggregationSnapshot({..., dateRange, ...})` — each of THESE call sites is unaffected by Task 2's change, since `captureAggregationSnapshot` already accepts `dateRange` as an input and Task 2 made it forward that value correctly; the 3 places that need editing in THIS file are the ones reading `snapshot.getCategoryDeviations`/`before.getCategoryDeviations`/`after.getCategoryDeviations` — locate them by name, not by line number, since the file is long);
    tests/dashboard-dal.test.ts lines 110-155 (the top-level `getOverviewComparisonRanges`/`getDeviationDateRanges`/`buildDeviationDataset` imports at lines 116-121, the `getOverviewComparisonRanges` describe block at ~131-155) and the `buildDeviationDataset`/`getDeviationDateRanges` describe blocks further down (grep for `describe('buildDeviationDataset` and `describe('getDeviationDateRanges` to locate them) — all three describe blocks die with Task 1's deletions; the `dashboardPresetToDateRange` test at ~157-162 and the `DASHBOARD_TOTAL_EXPENSE_STATUSES` test below it are UNRELATED and stay untouched (that production symbol is not deleted until Plan 84-04).
  </read_first>
  <action>
    In `tests/reimbursement-regression.test.ts`: delete every assertion/type annotation that reads a `getCategoryDeviations` key off a `captureAggregationSnapshot` result (the per-snapshot `Map<number, {...}>` typed reads, and the before/after byte-identical diff comparison of that key) — leave every OTHER assertion (`getOverviewAmountTotals`, `getCategoriesBreakdown`, `getCategoryRanking`, `getCategoryDetail`, `getMonthlyTrendByNature`, `getMonthOverMonthCategoryChanges`, `getOverviewChart`, `getTagTotals`, `getTagDetail`) completely untouched — their expected numeric values must not change, and none of their own call shapes changed (Task 2 already made `captureAggregationSnapshot` do the range-forwarding internally; this file calls that helper, not the 4 DAL functions directly, so it needs edits ONLY where it reads the deleted key).

    In `tests/dashboard-dal.test.ts`: remove `getOverviewComparisonRanges`, `getDeviationDateRanges`, `buildDeviationDataset` from the top `await import('../lib/dal/dashboard')` destructure; delete the 3 describe blocks that exercise them (`'dashboard DAL amount mapping'`'s `getOverviewComparisonRanges` assertions, `'buildDeviationDataset (D-02, D-03, D-05)'`, `'getDeviationDateRanges (D-02, D-03)'`) in their entirety. Leave the `dashboardPresetToDateRange` test and every other describe block in this 40KB file untouched.
  </action>
  <verify>
    <automated>yarn test tests/reimbursement-regression.test.ts tests/dashboard-dal.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getCategoryDeviations\|getOverviewComparisonRanges\|getDeviationDateRanges\|buildDeviationDataset" tests/reimbursement-regression.test.ts tests/dashboard-dal.test.ts` returns zero matches.
    - `yarn test tests/reimbursement-regression.test.ts --run` passes with every surviving expected value byte-identical to before this plan (no numeric assertion in this file changes).
    - `yarn test --run` (full suite) is green, including `tests/pace-engine-lens-regression.test.ts` (the Phase 82 RETIRE-05 canary) with zero edits made to that file.
    - `yarn typecheck` is clean.
  </acceptance_criteria>
  <done>The full suite (including the RETIRE-05 canary) is green with byte-identical expected values, and no test file still references the deleted dead-code symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only surface | This plan touches only DAL signatures and their test call sites — no new user-facing input path, no change to `verifySession()`/ownership scoping in any of the 4 re-signed functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-08 | Tampering | Regression-suite numeric drift | high | mitigate | D-16's core guarantee: `captureAggregationSnapshot` forwards the SAME `dateRange` object to every re-signed function instead of re-deriving it, making a period mismatch structurally impossible, not merely tested for |
| T-84-09 | Repudiation | Silent snapshot-shape misalignment (10→9 keys) | medium | mitigate | Task 2's acceptance criteria explicitly re-verify positional alignment of the `Promise.all` array against the returned object literal after the `getCategoryDeviations` removal |

</threat_model>

<verification>
- `yarn test tests/amortization-lens-regression.test.ts tests/reimbursement-regression.test.ts tests/dashboard-dal.test.ts --run`
- `yarn test --run` (full suite, including `tests/pace-engine-lens-regression.test.ts`)
- `yarn typecheck`
</verification>

<success_criteria>
- The four aggregation functions take explicit date ranges; no caller anywhere passes a preset-shaped argument to any of them.
- Every regression suite that exercises them (amortization, reimbursement, the Phase 82 RETIRE-05 canary) is green with unchanged expected values.
</success_criteria>

<output>
Create `.planning/phases/84-category-detail-and-cleanup/84-03-SUMMARY.md` when done
</output>
