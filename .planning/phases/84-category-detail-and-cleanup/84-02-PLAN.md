---
phase: 84-category-detail-and-cleanup
plan: 2
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - lib/dal/category-detail-year-window.ts
  - components/dashboard/category-detail-table.tsx
  - components/dashboard/category-subcategory-breakdown.tsx
  - components/dashboard/category-detail-difference-chart.tsx
  - components/dashboard/category-detail-skeleton.tsx
  - app/(app)/dashboard/categories/[id]/page.tsx
  - tests/category-detail-year-window-dal.test.ts
  - tests/category-detail-table.test.ts
  - tests/category-subcategory-breakdown.test.tsx
autonomous: true
requirements: [CDET-01, CDET-02, CDET-04, CDET-05, CDET-07]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A previous-year row renders directly below the current-year row, showing the homologous window's 12 (or window-sliced) plain amounts, whenever the previous year has at least one Covered Month inside the homologous window; when it has none, a stated-reason line replaces the row entirely — never a silent gap (D-11, CDET-02)."
    - "A Differenza row's summary column shows Totale/Media deltas labelled 'Rispetto al {anno-1}'; the Totale delta is replaced by a stated reason when the previous year has fewer than PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS covered months in the homologous window, while the Media delta always renders regardless of that gate (D-10, D-12, CDET-04, CDET-07)."
    - "Subcategories are listed ordered by current-window weight, each carrying a contribution (current minus previous) computed via Decimal.js; the contributions sum EXACTLY to the parent category's total difference, including a subcategory present only in the previous period (negative contribution, 0% weight) or only in the current period (D-16, CDET-05)."
    - "The difference chart renders bars of computeComparison(currentMonth, homologousPreviousMonth) per displayed month, colored via resolveComparisonJudgement, with no sign glyph anywhere and a legend stating what above/below the zero line means; it consumes the SAME series object the table renders, never a separate query (D-08, D-09)."
    - "The window-scoped topTransactions list shows at most 5 transactions drawn only from the selected window's date range, reusing the existing top-transactions query shape parameterized on window.from/window.to (D-05)."
  artifacts:
    - components/dashboard/category-detail-difference-chart.tsx
    - "CategoryDetailYearWindowData.previousYear (real shape, replacing Plan 84-01's null placeholder)"
    - "CategoryDetailYearWindowData.subcategories (real shape, replacing Plan 84-01's [] placeholder)"
    - "CategoryDetailYearWindowData.topTransactions (real shape, replacing Plan 84-01's [] placeholder)"
  key_links:
    - "lib/dal/category-detail-year-window.ts (getCategoryDetailYearWindow) -> lib/services/pace-and-projection.ts (canShowPreviousYearTotalDifference, PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS, computeComparison, resolveComparisonJudgement)"
    - "components/dashboard/category-detail-difference-chart.tsx -> the SAME data.current/data.previousYear series category-detail-table.tsx renders (D-08 no second query)"
    - "components/dashboard/category-subcategory-breakdown.tsx (reshaped) -> CategoryDetailYearWindowData.subcategories (weight + contribution, no more DeviationBadge import)"
---

<objective>
Complete the category detail page's build half: the previous-year comparison row and gated Differenza summary (D-10/D-11/D-12, CDET-02/CDET-04/CDET-07), the subcategory contribution rewrite that provably sums to the parent's difference (D-16, CDET-05), the month-by-month difference chart (D-08/D-09), and the window-scoped top-transactions list (D-05). This plan fills in the three placeholder fields Plan 84-01 left on `CategoryDetailYearWindowData` (`previousYear`, `subcategories`, `topTransactions`) — no architecture change, only real data replacing typed stubs. It still does not touch any retired symbol (`DashboardFilters`, `getCategoryDeviations`, `dashboard-filters.tsx`, `deviation-badge.tsx`, `dashboardPresetToDateRange`) — those remain fully intact everywhere in the codebase; retirement is Plan 84-03/84-04 (D-17).

Purpose: deliver the full CDET-01…07 reading experience on the detail page.
Output: the category detail page shows the complete 3-row table (current/previous/Differenza), the difference chart, the rewritten subcategory contribution table, and window-scoped top transactions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-category-detail-and-cleanup/84-CONTEXT.md
@.planning/phases/84-category-detail-and-cleanup/84-RESEARCH.md
@.planning/phases/84-category-detail-and-cleanup/84-PATTERNS.md
@lib/services/pace-and-projection.ts
@lib/dal/dashboard.ts
@components/dashboard/category-subcategory-breakdown.tsx
@components/dashboard/category-detail-trend-chart.tsx
@.scratch/dashboard-categories/detail-table.html
@.scratch/dashboard-categories/detail-chart.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DAL — previous-year comparison, subcategory contributions, window-scoped top transactions</name>
  <files>lib/dal/category-detail-year-window.ts, tests/category-detail-year-window-dal.test.ts</files>
  <read_first>
    lib/services/pace-and-projection.ts (`canShowPreviousYearTotalDifference`, `PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS`, `computeComparison`, `buildYearSeries`);
    lib/dal/dashboard.ts lines ~1587-1751 (the EXACT `topTransactionRows` query — joins, `coalesce(transactionTable.amount, ledgerRowSource.amount)`, `orderBy(desc(abs(amount)), desc(occurredAt), id)`, `.limit(5)` — replicate this verbatim, only parameterizing `from`/`to` on the window instead of the old preset range) and the `CategoryDetailTopTransaction`/`buildCategoryDetailData`'s topTransactions-building logic (title fallback chain `customTitle ?? groupTitle ?? description`) — REUSE the existing exported `CategoryDetailTopTransaction` type from `@/lib/dal/dashboard`, do not redefine it;
    lib/utils/decimal.ts (`toDecimal`, `toDbDecimal` — round once at the return boundary, never per-operand, per CDET-05's exact-sum requirement);
    lib/dal/category-detail-year-window.ts (Plan 84-01's current state — `getCategoryDetailYearWindow`, `CategoryDetailWindowSeries`, the three placeholder fields you are replacing);
    .scratch/dashboard-categories/detail-table.html lines 104-133 (previous-year row + Differenza row markup) and lines 144-188 (subcategory table with the summing Totale row, the "nuova nel 2026"/"solo nel 2025" cases).
  </read_first>
  <behavior>
    - Previous-year gate (D-11): compute `previousCoveredMonths = getCoveredMonthsInYear(year-1)`, filter to the homologous window's month-of-year set. If that filtered set is empty -> `previousYear: {status:'unavailable'}`. Otherwise build the previous year's window series (plain amounts, buildYearSeries total/average, no per-cell deltas) and `totalDifference`: `canShowPreviousYearTotalDifference(filteredSet.length)` true -> `{status:'shown', value: computeComparison(current.total, previous.total)}`, false -> `{status:'insufficient', coveredMonthCount: filteredSet.length}`; `averageDifference` is ALWAYS `computeComparison(current.average, previous.average)` regardless of that gate.
    - Subcategory contributions (CDET-05/D-16): for every subcategory appearing in EITHER the current-window totals OR the previous-homologous-window totals (union of ids), `contribution = computeComparison(currentAmount, previousAmount)` (missing side defaults to '0.00'); `weightPercentage` from `computeBreakdownPercentages`-style division of `currentAmount` over the current-window subcategory total (0 for a previous-only subcategory); `presence` is 'current-only'/'previous-only'/'both'. Sum of all `contribution` values (as unrounded Decimal, summed then rounded once) MUST equal `computeComparison(current.total, previous.total)` exactly — this is the on-screen verification property, not an approximation.
  </behavior>
  <action>
    In `lib/dal/category-detail-year-window.ts`, replace the `previousYear: null` placeholder with the real `CategoryDetailPreviousYearComparison` union described in `<behavior>` (query `getCategoryMonthlyAmounts(categoryId, year-1, ledgerRowSource)` + `getCoveredMonthsInYear(year-1)`, slice to the SAME start/length indices as the current window). Replace `subcategories: []` with a new Drizzle query mirroring `lib/dal/dashboard.ts`'s existing `getCategoryDetail` subcategoryRows query shape (same joins: `ledgerRowSource -> expense -> subCategory -> category`, `leftJoin(userSubcategoryOverride, ...)`, `innerJoin(nature,...)`, `innerJoin(direction,...)`, `eq(direction.includedInTotals, true)`) run TWICE — once scoped to the current window's `{from,to}`, once to the previous-homologous-window's `{from,to}` — then composed into `CategoryDetailSubcategoryContribution[]` per `<behavior>` above, sorted by `currentAmount` descending. Replace `topTransactions: []` with the exact query pattern from `lib/dal/dashboard.ts`'s `getCategoryDetail` topTransactionRows block, parameterized on the CURRENT window's `{from,to}` (never the full year) — reuse the imported `CategoryDetailTopTransaction` type verbatim, do not redeclare it.

    Compute the window's `{from,to}` Date boundaries once (shared by both new subquery pairs and the top-transactions query) from `window.from`/`window.months` using the SAME no-modulo start+length-1 arithmetic Example 2 of 84-RESEARCH.md corrects (never `(startMonth + months - 1) % 12`), since D-03 guarantees the window never crosses the year boundary.
  </action>
  <reversibility rating="reversible">Additive fields on an already-shipped type; no existing caller reads these fields yet outside this plan's own table/chart components.</reversibility>
  <verify>
    <automated>yarn test tests/category-detail-year-window-dal.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - A fixture where the previous year has zero Covered Months inside the homologous window yields `previousYear: {status:'unavailable'}`.
    - A fixture with 3 Covered Months in the homologous window (below the 6-month `PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS` threshold) yields `previousYear.status === 'available'`, `previousYear.totalDifference.status === 'insufficient'`, AND a non-null `previousYear.averageDifference`.
    - A fixture with one subcategory present only in the previous period and one present only in the current period: `contributions.reduce((sum,c) => sum.plus(toDecimal(c.contribution)), toDecimal(0))` equals `computeComparison(current.total, previous.total)` to the cent, verified via `toDecimal(...).equals(...)`, never `===` on strings.
    - `topTransactions` in the returned data never includes a transaction dated outside `window.from`/`window.to`.
  </acceptance_criteria>
  <done>previousYear, subcategories, and topTransactions are real, tested data (no longer placeholders), each honoring their respective D-11/D-16/D-05 gates and invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table — previous-year row, Differenza row; subcategory breakdown rewrite</name>
  <files>components/dashboard/category-detail-table.tsx, components/dashboard/category-subcategory-breakdown.tsx, app/(app)/dashboard/categories/[id]/page.tsx, tests/category-detail-table.test.ts, tests/category-subcategory-breakdown.test.tsx</files>
  <read_first>
    components/dashboard/category-detail-table.tsx (Plan 84-01's current-year-only row — extend, do not rewrite from scratch);
    components/dashboard/category-subcategory-breakdown.tsx (current shape — `deviations` prop + `DeviationBadge` import you are removing);
    .scratch/dashboard-categories/detail-table.html lines 104-133 (previous-year row: muted text color, no per-cell delta; Differenza row: `colspan="12"` explanatory text in the month cells, real numbers ONLY in the summary column) and lines 144-188 (subcategory table columns: name, weight bar, "Totale {year} · {pct}%", "Contributo alla differenza"; `.gone` row styling for a previous-only subcategory; `.total` row with a 2px top border);
    lib/services/pace-and-projection.ts (`resolveComparisonJudgement` — reuse for both the Differenza row and every subcategory contribution's color).
  </read_first>
  <action>
    Extend `CategoryDetailTable` with a second `<tr>` rendering `data.previousYear` when `status === 'available'`: same 12/window columns, plain amounts (no delta line), muted text color, row header "{year-1} (stessa finestra)"; summary column stacked Totale/`previousYear.series.total`, Media/`previousYear.series.average`. When `status === 'unavailable'`, render ONE row spanning all columns with a stated-reason line (Claude's Discretion for exact Italian copy — e.g. "Nessun mese coperto nel {year-1} per questa finestra") instead of the data row — never a silently blank row. Add a third `<tr>` "Differenza": the 12/window month cells render one `colspan` explanatory note (per the prototype's own text), never per-month numbers; the summary column renders `data.previousYear.totalDifference` — `{status:'shown'}` -> magnitude+word via `resolveComparisonJudgement`, label "Rispetto al {year-1}" (D-12); `{status:'insufficient'}` -> a stated reason (Claude's Discretion, e.g. "Dati insufficienti nel {year-1}: {coveredMonthCount} mesi coperti su 6 richiesti") — and `averageDifference`, ALWAYS rendered as magnitude+word with the SAME "Rispetto al {year-1}" label, regardless of the Totale gate (CDET-07). Only render the Differenza row at all when `data.previousYear.status === 'available'`.

    Rewrite `components/dashboard/category-subcategory-breakdown.tsx`: drop the `deviations`/`DeviationBadge` prop and import entirely; new props `{ contributions: CategoryDetailSubcategoryContribution[]; type: 'in'|'out' }`. Render as a table (not the current `<ul>` card list) with columns Sottocategoria / Peso (bar, `weightPercentage`) / "Totale {year}" (with inline `· {pct}%`) / "Contributo alla differenza" (magnitude+word via `resolveComparisonJudgement(contribution, type)`, muted styling + a "— nuova nel {year}"/"— solo nel {year-1}" suffix on the name for `presence !== 'both'` rows, per the prototype's `.gone`/plain-new styling). Add a final Totale row (2px top border, bold) whose two right-hand cells are the sum of `currentAmount` and the sum of `contribution` — this row is the on-screen proof of CDET-05's exact-sum property, not decoration; compute it by summing the ALREADY-provided `contribution`/`currentAmount` strings via Decimal.js, never by re-deriving from `data.previousYear`.

    In `app/(app)/dashboard/categories/[id]/page.tsx`, pass `data.subcategories` to the rewritten `CategorySubcategoryBreakdown` (drop the old `deviations` prop entirely — no more `getCategoryDeviations` import remains anywhere in this file, confirming Plan 84-01's grep guarantee still holds).
  </action>
  <verify>
    <automated>yarn test tests/category-detail-table.test.ts tests/category-subcategory-breakdown.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - A `previousYear: {status:'unavailable'}` fixture renders a stated-reason line and asserts zero `<td>` cells with a per-month amount in that row.
    - A `previousYear.totalDifference: {status:'insufficient', coveredMonthCount:3}` fixture renders a stated reason containing "3" in the Differenza row's Totale summary cell AND a real magnitude+word value in its Media summary cell (never both insufficient).
    - `CategorySubcategoryBreakdown` renders zero occurrences of the strings "Deviazione"/"DeviationBadge" (`grep -c` on the rendered output, filtered of comments) and its rendered Totale row's two right-hand values equal the sum of the fixture's `currentAmount`/`contribution` fields exactly.
    - A `presence: 'previous-only'` row renders its name suffixed with "solo nel {year-1}" and a 0% weight bar.
  </acceptance_criteria>
  <done>The table shows all 3 rows (current/previous/Differenza) with correct gating, and the subcategory table's Totale row visibly proves the exact-sum contribution property.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Difference chart, skeleton reshape, page wiring</name>
  <files>components/dashboard/category-detail-difference-chart.tsx, components/dashboard/category-detail-skeleton.tsx, app/(app)/dashboard/categories/[id]/page.tsx</files>
  <read_first>
    components/dashboard/category-detail-trend-chart.tsx (SVG dimensions/viewBox/buildChartPoints pattern — reuse the chart-library SETUP, this is a role-match analog per 84-PATTERNS.md, not a literal reuse: this is a line chart of absolutes, the new one is a zero-centered bar chart of deltas);
    .scratch/dashboard-categories/detail-chart.html (kept as the REJECTED-variant rationale record only — do NOT copy its arrow glyphs or absolute-value bars; read it to understand what NOT to build, per D-08/D-09's explicit rejection of that shape);
    lib/services/pace-and-projection.ts (`resolveComparisonJudgement`);
    components/dashboard/category-detail-skeleton.tsx (current 3-KPI-card + line-chart + 2-column skeleton — being replaced with a table+chart+subcategory-table skeleton shape);
    components/dashboard/category-year-ranking-skeleton.tsx (the `animate-pulse` placeholder-grid convention used elsewhere in this feature area).
  </read_first>
  <action>
    Create `components/dashboard/category-detail-difference-chart.tsx` exporting `CategoryDetailDifferenceChart({ data }: { data: CategoryDetailYearWindowData })`. Derive its series ENTIRELY from `data.current.months`/`data.previousYear` (never a new query — D-08): for each displayed month index i, `delta = data.previousYear.status === 'available' ? computeComparison(data.current.months[i].amount ?? '0.00', data.previousYear.series.months[i].amount) : null`. Render one SVG bar per month, centered on a zero baseline, height proportional to `|delta|`, positioned above the line when `delta` is positive and below when negative; fill color from `resolveComparisonJudgement(delta, data.category.type)` mapped to the existing `--total-in`/`--total-out`/muted CSS vars (no new color tokens); a month with `delta === null` (previousYear unavailable, or the month itself has no current-year amount) renders a flat zero-height marker, never omitted from the axis. No arrow glyphs, no `+`/`-` sign text anywhere — bar position (above/below) is the only "sign" signal, per D-09. Add a `<title>`/tooltip per bar stating the magnitude+word ("107,90 in più di {month label} {year-1}") and a short legend line beneath the chart stating what above/below the zero line means for this category's direction. Render this chart ABOVE the table in the page.

    Reshape `components/dashboard/category-detail-skeleton.tsx` to placeholder the new layout: a table-shaped skeleton (sticky first column + N month-column placeholders + summary column, 3 row-height bars, mirroring `category-year-ranking-skeleton.tsx`'s `animate-pulse` grid convention) above which sits a chart-shaped placeholder (bar-height rectangles, zero-centered) and below which sits a subcategory-table-shaped placeholder (name/bar/amount/contribution columns). Remove the old 3-KPI-card skeleton section entirely (D-07: no separate KPI header exists anymore).

    In `app/(app)/dashboard/categories/[id]/page.tsx`, render `<CategoryDetailDifferenceChart data={data} />` above `<CategoryDetailTable data={data} />`, and pass `data.topTransactions` to the existing `CategoryTopTransactions` component (unchanged import/props — only its data source changed, from the old `getCategoryDetail` call to the new window-scoped `data.topTransactions`). Confirm no remaining import of `CategoryDetailSummary` or `CategoryDetailTrendChart` in this file (D-07's sticky-column summary subsumes the KPI header; the difference chart replaces the old trend chart) — do not delete those two component FILES yet, that is Plan 84-04's removals-only job (D-17).
  </action>
  <reversibility rating="reversible">D-09's no-sign-glyph rule is a presentation-only choice with no data-shape consequence — reversible if a future ADR amends it.</reversibility>
  <verify>
    <automated>yarn test tests/category-detail-table.test.ts --run && yarn typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryDetailDifferenceChart`'s rendered SVG/markup contains zero occurrences of "▲"/"▼"/"+"/"-" glyphs attached to an amount (grep the rendered output for those literal characters adjacent to a digit).
    - A month with `previousYear.status === 'unavailable'` renders a zero-height marker for that bar, never a thrown error or an omitted column.
    - `grep -n "CategoryDetailSummary\|CategoryDetailTrendChart" "app/(app)/dashboard/categories/[id]/page.tsx"` returns zero matches.
    - `yarn typecheck` passes with the reshaped skeleton and chart wired in.
  </acceptance_criteria>
  <done>The difference chart renders above the table with no sign glyphs, consuming the table's own series; the skeleton matches the new layout; the page no longer imports the two retired-from-this-page components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DAL -> subcategory/top-transaction queries | New Drizzle queries parameterized on window `{from,to}` derived from user-controlled `?months=&from=`, scoped to the same `categoryId`/`userId` ownership checks as Plan 84-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-05 | Information Disclosure | window-scoped topTransactions query | high | mitigate | Reuses the exact ownership-scoped join chain (`ledgerRowSource -> expense -> subCategory -> category`, `dateScopedTransactions(..., userId, ...)`) already regression-proven in `getCategoryDetail` — no new join path introduced |
| T-84-06 | Tampering | Subcategory contribution arithmetic | medium | mitigate | All summation goes through `@/lib/utils/decimal` (`toDecimal`/`toDbDecimal`), rounded once at the return boundary — never native `+`/`-` on money strings, per CLAUDE.md's hard rule and CDET-05's exact-sum contract |
| T-84-07 | Repudiation | Chart/table numeric divergence | low | mitigate | The chart derives its series from the SAME `data.current`/`data.previousYear` object the table renders (D-08) — structurally impossible for the two to disagree, not merely tested for |

</threat_model>

<verification>
- `yarn test tests/category-detail-year-window-dal.test.ts tests/category-detail-table.test.ts tests/category-subcategory-breakdown.test.tsx --run`
- `yarn typecheck`
- Manual: load a category detail with a previous year having partial coverage and confirm the Differenza row's Totale shows a stated reason while Media shows a real number; confirm the chart's legend and tooltip wording read as intended.
</verification>

<success_criteria>
- The category detail page shows all 3 table rows (current/previous/Differenza), the difference chart, the rewritten subcategory contribution table (with a verifiable summing Totale row), and window-scoped top transactions.
- No retired symbol (`DashboardFilters`, `getCategoryDeviations`, `DeviationBadge`) is imported anywhere in the files this plan touches.
</success_criteria>

<output>
Create `.planning/phases/84-category-detail-and-cleanup/84-02-SUMMARY.md` when done
</output>
