---
phase: 76-reimbursements-section
plan: 03
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - lib/dal/transactions.ts
  - tests/transactions-dal.test.ts
  - components/transactions/reimbursement-row-indicator.tsx
  - components/transactions/transaction-table.tsx
  - components/layout/sidebar.tsx
autonomous: true
requirements: [RMB-10]

must_haves:
  truths:
    - "The transactions table's reimbursement indicator badge is a Link to /reimbursements/[id] for the SPECIFIC reimbursement that row belongs to (D-06) — not a generic non-interactive icon."
    - "The sidebar exposes a top-level 'Rimborsi' nav item (peer of Tag/Spese) routing to APP_ROUTES.reimbursements (D-05)."
  artifacts:
    - lib/dal/transactions.ts's transactionListSelect/TransactionListRow expose a new reimbursementId field, resolved via the existing pairedReimbursementIdExpr()
    - components/transactions/reimbursement-row-indicator.tsx's ReimbursementRowIndicator accepts reimbursementId and renders a Link
  key_links:
    - "transaction-table.tsx row -> reimbursementHref(transaction.reimbursementId): every transaction that is part of ANY reimbursement (anchor or refund role) deep-links straight to the dedicated management page."
    - "components/layout/sidebar.tsx topNavItems -> APP_ROUTES.reimbursements: the section is reachable from the primary nav, not only by direct URL."
---

<objective>
Make the dedicated Rimborsi section reachable from the two places a user already is: the sidebar
(D-05) and the transactions table's existing reimbursement badge (D-06) — the badge stops being a
dead icon and becomes a Link straight to the owning reimbursement's page.

Purpose: RMB-10 requires the section to be a genuine "first-class dedicated section," which means
entry points, not just a URL that works if typed directly.

Output: a `reimbursementId` field on the transactions list DAL, a linking `ReimbursementRowIndicator`,
and a new "Rimborsi" sidebar item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-reimbursements-section/76-CONTEXT.md
@.planning/phases/76-reimbursements-section/76-PATTERNS.md
@lib/dal/transactions.ts
@components/transactions/reimbursement-row-indicator.tsx
@components/transactions/transaction-table.tsx
@components/layout/sidebar.tsx
@lib/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose reimbursementId on the transactions list DAL</name>
  <files>lib/dal/transactions.ts, tests/transactions-dal.test.ts</files>
  <read_first>
    lib/dal/transactions.ts (`pairedReimbursementIdExpr()` — ALREADY resolves the reimbursement id
    for a transaction in EITHER role, anchor or refund; `transactionListSelect`'s existing
    `pairedWithId`/`pairedNetAmount`/`pairedAmount`/`pairedDescription`/`pairedOccurredAt` fields, all
    built the same way — `sql\`${pairedCounterpartIdExpr()}\`` etc — and `TransactionListRow`'s
    matching type fields); tests/transactions-dal.test.ts lines ~1218-1316 (the existing
    "transaction pairing select-shape contract" describe block — the exact assertion style: `toHaveProperty`,
    `toMatchObject({op:'sql'})`, and the `collectSqlText` recursive-flatten helper that proves a
    fragment's SQL text mentions `reimbursement` and never `transaction_pair`).
  </read_first>
  <action>
    In lib/dal/transactions.ts, add a new field to `transactionListSelect`:
    `reimbursementId: sql<number | null>\`${pairedReimbursementIdExpr()}\`` (placed alongside the
    other `paired*` fields — this is a NEW field, not a rename of any existing one). Add the matching
    `reimbursementId: number | null` field to the `TransactionListRow` type. No new SQL logic is
    needed — `pairedReimbursementIdExpr()` already resolves the correct reimbursement id for both the
    anchor role and the refund role via its existing two-branch CASE.
  </action>
  <verify>
    <automated>yarn vitest run tests/transactions-dal.test.ts -t "reimbursementId"</automated>
  </verify>
  <acceptance_criteria>
    - Extend the existing `describe("transaction pairing select-shape contract (Phase 50 — PAIR-02)"`
      block in tests/transactions-dal.test.ts with a new `it` asserting: `transactionListSelect` has
      property `reimbursementId`; the fragment matches `{op: 'sql'}`; via the SAME `collectSqlText`
      helper already defined in that file, the flattened SQL text contains `reimbursement` and does
      NOT contain `transaction_pair`.
    - `grep -c "reimbursementId" lib/dal/transactions.ts` shows the field added in both
      `transactionListSelect` and `TransactionListRow`.
  </acceptance_criteria>
  <done>transactionListSelect/TransactionListRow expose reimbursementId, proven by the extended test; full test file still passes.</done>
</task>

<task type="auto">
  <name>Task 2: ReimbursementRowIndicator becomes a link to the dedicated page</name>
  <files>components/transactions/reimbursement-row-indicator.tsx, components/transactions/transaction-table.tsx</files>
  <read_first>
    components/transactions/reimbursement-row-indicator.tsx (current no-props `Badge`-only
    component); components/transactions/transaction-table.tsx (the exact call site: `
    {transaction.pairedNetAmount != null && <ReimbursementRowIndicator />}` inside the title-line
    flex row, alongside `TransactionTagsChip` — same `shrink-0` placement convention).
  </read_first>
  <action>
    Rewrite components/transactions/reimbursement-row-indicator.tsx to accept `{ reimbursementId:
    number }` and wrap the existing `Badge` in a `Link` (from `next/link`) to
    `reimbursementHref(reimbursementId)` (import from `@/lib/routes`); keep the existing
    `Link2`/`Badge` visuals, `aria-label="Rimborso collegato"`, and add
    `title="Clicca per gestire il rimborso"` plus a `hover:bg-muted`/`cursor-pointer` affordance on
    the Badge so it visually reads as interactive (D-06).

    In components/transactions/transaction-table.tsx, change the render condition from
    `{transaction.pairedNetAmount != null && <ReimbursementRowIndicator />}` to
    `{transaction.reimbursementId != null && <ReimbursementRowIndicator
    reimbursementId={transaction.reimbursementId} />}` (the semantically-direct signal now that
    Task 1 exposes it, replacing the `pairedNetAmount != null` proxy check).
  </action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ReimbursementRowIndicator`'s exported function signature takes a `reimbursementId: number` prop
      (no longer a zero-arg component) — `grep -c "reimbursementId" components/transactions/reimbursement-row-indicator.tsx`
      is at least 2 (prop type + usage in the href).
    - `components/transactions/transaction-table.tsx`'s call site passes
      `reimbursementId={transaction.reimbursementId}` and gates on `transaction.reimbursementId != null`,
      not `pairedNetAmount`.
  </acceptance_criteria>
  <reversibility rating="costly">Undo re-touches the shipped Phase 75 row-indicator component and its transaction-table host wiring (D-06, per CONTEXT.md).</reversibility>
  <done>Clicking the reimbursement badge on any paired transaction row navigates to /reimbursements/[id] for that transaction's reimbursement.</done>
</task>

<task type="auto">
  <name>Task 3: "Rimborsi" sidebar nav item</name>
  <files>components/layout/sidebar.tsx</files>
  <read_first>
    components/layout/sidebar.tsx (the `topNavItems` array — `{href, label, icon}` tuples rendered
    via `ClientMountIcon`; the existing import list already includes `Tags`/`Regex`/`Receipt` from
    `lucide-react`, alongside `Link` from `next/link` used elsewhere in this file for the user-menu —
    note `components/transactions/reimbursement-panel.tsx` already imports `Link2` from
    `lucide-react` alongside `next/link`'s `Link` with no naming collision, same pattern to follow
    here).
  </read_first>
  <action>
    In components/layout/sidebar.tsx, add `Link2` to the existing `lucide-react` import list, and
    insert `{ href: APP_ROUTES.reimbursements, label: 'Rimborsi', icon: Link2 }` into `topNavItems`
    immediately after the `Spese` entry (D-05 — Claude's discretion on exact position; "after Spese"
    groups it near the two entities a reimbursement anchors/refunds against). No other changes to
    this file.
  </action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Rimborsi" components/layout/sidebar.tsx` is 1; the new entry appears in `topNavItems`
      between the `Spese`/`APP_ROUTES.expenses` entry and the `Importazioni`/`APP_ROUTES.import` entry.
  </acceptance_criteria>
  <done>The sidebar renders a "Rimborsi" item linking to /reimbursements, positioned after Spese (final visual confirmation deferred to the Plan 76-06 checkpoint — this repo has no jsdom for sidebar render assertions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| transaction-table.tsx row -> ReimbursementRowIndicator | `reimbursementId` is server-resolved (Task 1's DAL field), never client-supplied — the Link target is trustworthy by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-07 | Information Disclosure | reimbursementId link target | low | accept | Following the link only lands on `/reimbursements/[id]`, which Plan 76-05's own IDOR guard (`getReimbursement(userId, id)` -> `notFound()`) re-validates ownership independently — the row-indicator link itself grants no new access beyond what that page already enforces. |
</threat_model>

<verification>
`yarn vitest run tests/transactions-dal.test.ts -t "reimbursementId"` and `yarn tsc --noEmit`.
Manual confirmation of the badge link + sidebar nav item is part of the Plan 76-06 checkpoint.
</verification>

<success_criteria>
- Every transactions-table row that participates in a reimbursement (anchor or refund) shows a
  clickable badge linking to that reimbursement's dedicated page.
- The sidebar has a first-class "Rimborsi" entry.
</success_criteria>

<output>
Create `.planning/phases/76-reimbursements-section/76-03-SUMMARY.md` when done.
</output>
