---
phase: 03-expense-management
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/expenses.spec.ts
  - app/layout.tsx
  - lib/utils/decimal.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03

must_haves:
  truths:
    - "Test stubs exist for all 8 EXP-01/02/03 behaviors (fixme — not yet passing)"
    - "Sonner Toaster is mounted in root layout so toast() calls are visible in the DOM"
    - "shadcn Table component is installed at components/ui/table.tsx"
    - "toDecimal() and toDbDecimal() utils exist and are correctly typed"
  artifacts:
    - path: "tests/expenses.spec.ts"
      provides: "Playwright test stubs for EXP-01/02/03"
      contains: "test.fixme"
    - path: "app/layout.tsx"
      provides: "Root layout with Toaster mounted"
      contains: "<Toaster"
    - path: "lib/utils/decimal.ts"
      provides: "Monetary arithmetic utils (STATE.md blocker)"
      exports: ["toDecimal", "toDbDecimal"]
    - path: "components/ui/table.tsx"
      provides: "shadcn Table component"
  key_links:
    - from: "app/layout.tsx"
      to: "sonner"
      via: "import { Toaster } from 'sonner'"
      pattern: "Toaster"
    - from: "lib/utils/decimal.ts"
      to: "decimal.js"
      via: "import Decimal from 'decimal.js'"
      pattern: "toDecimal|toDbDecimal"
---

<objective>
Wave 0 prerequisites for Phase 3: test stubs, shadcn Table install, Sonner Toaster wiring, and decimal utility creation.

Purpose: Establishes the test contract before any implementation, installs the missing Table component, enables toast feedback globally, and clears the STATE.md blocker on monetary arithmetic utils.
Output: tests/expenses.spec.ts with 8 fixme stubs, app/layout.tsx with Toaster, components/ui/table.tsx installed, lib/utils/decimal.ts created.
</objective>

<execution_context>
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-expense-management/03-CONTEXT.md
@.planning/phases/03-expense-management/03-RESEARCH.md
@.planning/phases/03-expense-management/03-PATTERNS.md

<interfaces>
<!-- From tests/auth.spec.ts — exact stub pattern to replicate -->
```typescript
import { expect, test } from '@playwright/test'

test.describe('Auth - AUTH-01: Registration', () => {
  test('register happy path: valid email+password redirects to /dashboard', async () => {
    test.fixme()
    // AUTH-01: POST /register with valid email+password → 200 redirect → /dashboard
    // Implement when Plan 03 (server actions) is complete
  })
```

<!-- From app/layout.tsx — current state before modification -->
```typescript
<body className={`${geistSans.className} antialiased`}>{children}</body>
```

<!-- lib/utils/decimal.ts — no analog, implement verbatim from RESEARCH.md -->
```typescript
import Decimal from 'decimal.js'
export function toDecimal(value: string | number): Decimal { return new Decimal(value) }
export function toDbDecimal(value: Decimal): string { return value.toFixed(2) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expenses.spec.ts with 8 fixme test stubs</name>
  <files>tests/expenses.spec.ts</files>
  <read_first>
    - tests/auth.spec.ts — exact stub template: import style, test.describe grouping by requirement ID, test.fixme() pattern, comment convention
  </read_first>
  <action>
Create `tests/expenses.spec.ts` with the following structure — mirror tests/auth.spec.ts exactly (same import, same describe/fixme pattern):

```typescript
import { expect, test } from '@playwright/test'

test.describe('Expenses - EXP-01: Create', () => {
  test('create expense: form submit creates row visible in list', async () => {
    test.fixme()
    // EXP-01: "Nuova spesa" dialog → fill titolo + subcategoria → "Salva spesa" → row appears in table
    // Implement when Plan 04 (page + components) is complete
  })

  test('edit expense: modal pre-fills existing data, update persists', async () => {
    test.fixme()
    // EXP-01: Three-dots menu → "Modifica" → dialog opens with existing values pre-filled → "Aggiorna spesa" → table row updated
    // Implement when Plan 04 (page + components) is complete
  })

  test('delete expense: confirm dialog removes row from list', async () => {
    test.fixme()
    // EXP-01: Three-dots menu → "Elimina" → confirm dialog → "Elimina" button → row removed from table
    // Implement when Plan 04 (page + components) is complete
  })
})

test.describe('Expenses - EXP-02: Filters', () => {
  test('filter by status "Da categorizzare" shows only status=1 expenses', async () => {
    test.fixme()
    // EXP-02: Select "Da categorizzare" in Stato filter → URL gains ?status=uncategorized → only uncategorized expenses shown
    // Implement when Plan 04 (page + components) is complete
  })

  test('filter by period "Questo mese" excludes older expenses', async () => {
    test.fixme()
    // EXP-02: Select "Questo mese" → URL gains ?period=this-month → expenses older than current month not shown
    // Implement when Plan 04 (page + components) is complete
  })

  test('filter params survive page refresh', async ({ page }) => {
    test.fixme()
    // EXP-02: Set ?category=ristorazione&status=uncategorized → reload page → same URL → same results
    // Implement when Plan 04 (page + components) is complete
  })
})

test.describe('Expenses - EXP-03: Bulk Categorization', () => {
  test('select N rows: floating action bar appears with correct count', async () => {
    test.fixme()
    // EXP-03: Check 2 checkboxes → FAB appears with text "2 selezionate" and button "Categorizza (2)"
    // Implement when Plan 04 (page + components) is complete
  })

  test('bulk categorize: assigns subCategory + status=3 to all selected expenses', async () => {
    test.fixme()
    // EXP-03: Select 2 expenses → "Categorizza (2)" → bulk dialog → pick subcategory → "Conferma" → both rows show status=3 + new category badge
    // Implement when Plan 04 (page + components) is complete
  })
})
```
  </action>
  <verify>
    <automated>grep -c "test.fixme" /Users/andreabernardini/ai-projects/new-sparter-app/tests/expenses.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/expenses.spec.ts` exists
    - `grep -c "test.fixme" tests/expenses.spec.ts` returns 8
    - `grep "EXP-01\|EXP-02\|EXP-03" tests/expenses.spec.ts` returns matches for all three requirement IDs
    - File uses `import { expect, test } from '@playwright/test'` on line 1
    - Three `test.describe` blocks present: EXP-01, EXP-02, EXP-03
  </acceptance_criteria>
  <done>tests/expenses.spec.ts exists with 8 test.fixme() stubs grouped by requirement ID, matching auth.spec.ts conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn Table + wire Sonner Toaster in root layout</name>
  <files>components/ui/table.tsx, app/layout.tsx</files>
  <read_first>
    - app/layout.tsx — current state (lines 1-20): body tag structure to modify, existing imports
  </read_first>
  <action>
Step 1 — Install shadcn Table component:
```bash
npx shadcn@latest add table
```
This generates `components/ui/table.tsx` with the New York style (matches components.json preset).

Step 2 — Modify `app/layout.tsx` to add Toaster. Add import at top and `<Toaster />` inside `<body>`:

Current body (line 17):
```typescript
<body className={`${geistSans.className} antialiased`}>{children}</body>
```

Target state:
```typescript
import { Toaster } from 'sonner'
// ...
<body className={`${geistSans.className} antialiased`}>
  {children}
  <Toaster />
</body>
```

The `import { Toaster } from 'sonner'` line goes after the existing `import './globals.css'` line. No other changes to layout.tsx.
  </action>
  <verify>
    <automated>test -f /Users/andreabernardini/ai-projects/new-sparter-app/components/ui/table.tsx && grep -l "Toaster" /Users/andreabernardini/ai-projects/new-sparter-app/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/table.tsx` exists (created by npx shadcn@latest add table)
    - `grep "Toaster" app/layout.tsx` returns at least 2 matches (import + usage)
    - `grep "from 'sonner'" app/layout.tsx` returns a match
    - `grep "<Toaster" app/layout.tsx` returns a match
    - app/layout.tsx is otherwise unchanged (same html lang, className, metadata)
  </acceptance_criteria>
  <done>shadcn Table component installed at components/ui/table.tsx. Sonner Toaster imported and mounted in root layout body. Toast calls will now be visible in the DOM.</done>
</task>

<task type="auto">
  <name>Task 3: Create lib/utils/decimal.ts (STATE.md blocker clearance)</name>
  <files>lib/utils/decimal.ts</files>
  <read_first>
    - lib/utils.ts — existing utils file, verify decimal.js is accessible from the project
    - .planning/STATE.md — lines 77-78 confirming the blocker requirement
  </read_first>
  <action>
Create `lib/utils/decimal.ts` with the following content (exact from RESEARCH.md — no deviation):

```typescript
import Decimal from 'decimal.js'

/**
 * Parse a DB DECIMAL string or JS number to a Decimal instance.
 * Use this whenever reading an amount from the database (Drizzle returns DECIMAL as string).
 */
export function toDecimal(value: string | number): Decimal {
  return new Decimal(value)
}

/**
 * Convert a Decimal to a string suitable for DB insertion into a DECIMAL(10,2) column.
 * Never insert raw JS numbers — always use toDbDecimal() before writing amounts.
 */
export function toDbDecimal(value: Decimal): string {
  return value.toFixed(2)
}
```

Note: These utils are NOT used by Phase 3 expense operations (no amount fields on Expense per D-01). They are created now to clear the STATE.md blocker so Phase 5 can consume them without a planning gap.
  </action>
  <verify>
    <automated>cd /Users/andreabernardini/ai-projects/new-sparter-app && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "decimal" | head -5; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/utils/decimal.ts` exists
    - `grep "export function toDecimal" lib/utils/decimal.ts` returns a match
    - `grep "export function toDbDecimal" lib/utils/decimal.ts` returns a match
    - `grep "import Decimal from 'decimal.js'" lib/utils/decimal.ts` returns a match
    - TypeScript compilation of the file produces no errors (tsc --noEmit)
    - Function signatures: `toDecimal(value: string | number): Decimal` and `toDbDecimal(value: Decimal): string`
  </acceptance_criteria>
  <done>lib/utils/decimal.ts exists with toDecimal() and toDbDecimal() exported. STATE.md blocker on monetary arithmetic utils is cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Wave 0 creates test stubs and utility files — no user input, no DB access, no API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-00-01 | Tampering | decimal.ts toDbDecimal output | accept | toFixed(2) returns a well-formed string; no DB writes in Phase 3 — used in Phase 5 where parameterized queries prevent injection |
</threat_model>

<verification>
Run after completing all tasks:
```bash
# Test stub count
grep -c "test.fixme" tests/expenses.spec.ts
# Expected: 8

# Toaster in layout
grep "Toaster" app/layout.tsx
# Expected: 2 lines (import + JSX)

# Decimal utils
grep "toDecimal\|toDbDecimal" lib/utils/decimal.ts
# Expected: 2 function definitions

# Table component installed
test -f components/ui/table.tsx && echo "OK"
# Expected: OK
```
</verification>

<success_criteria>
- tests/expenses.spec.ts exists with exactly 8 test.fixme() stubs grouped by EXP-01/02/03
- app/layout.tsx contains `<Toaster />` from sonner
- components/ui/table.tsx exists (installed via shadcn)
- lib/utils/decimal.ts exports toDecimal() and toDbDecimal() with correct signatures
- TypeScript compiles without errors on the new/modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-expense-management/03-00-SUMMARY.md`
</output>
