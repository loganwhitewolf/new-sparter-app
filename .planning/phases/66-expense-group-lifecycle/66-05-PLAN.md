---
phase: 66-expense-group-lifecycle
plan: 05
type: execute
wave: 3
depends_on: ["66-02"]
files_modified:
  - app/(app)/expenses/groups/[groupId]/page.tsx
  - components/expenses/group-detail-client.tsx
  - components/expenses/remove-group-member-button.tsx
  - tests/group-detail-page.test.tsx
autonomous: true
requirements: [GRP-05, GRP-07]

must_haves:
  truths:
    - "The group detail page's Categoria section becomes an editable SubcategoryPicker trigger (D-01a) — the second of the two GRP-05 recategorize surfaces (the first is Plan 66-04's expenses-table group row)."
    - "Each member row on the group detail page gets a 'Rimuovi dal gruppo' control (D-07) that calls removeExpenseFromGroupAction; the freed member becomes a standalone expense (no more group-row collapsing) and the page refreshes to reflect the new member count."
    - "The group detail page gets a 'Scomponi gruppo' control (D-07) that calls dissolveExpenseGroupAction and navigates to /expenses on success (the group page itself no longer exists after dissolution)."
    - "GRP-05's two surfaces (this plan's group detail edit + Plan 66-04's table group-row edit) both invoke the SAME single categorizeExpenseGroup action (D-01) — no divergent recategorize logic is introduced here."
  artifacts:
    - "components/expenses/remove-group-member-button.tsx exports RemoveGroupMemberButton"
    - "app/(app)/expenses/groups/[groupId]/page.tsx fetches getCategories()/getMostUsedSubcategories(...) and passes categories/mostUsed to GroupDetailClient"
  key_links:
    - "GroupDetailClient's Props type gains categories/mostUsed (sourced from the RSC page, same allowedCategoryTypes convention as the expenses list page: ['in','out','transfer','allocation']) so its inline SubcategoryPicker has the data it needs without a second round trip."
    - "Dissolve navigates via useRouter().push(APP_ROUTES.expenses) — the router already imported in group-detail-client.tsx for GroupTitleEdit's router.refresh()."
  prohibitions:
    - statement: "Dissolution never stores or restores a per-member pre-merge subcategory (D-09) — this plan's dissolve control must not add any client-side 'preview restored state' UI implying a revert."
      status: enforced
      verification: "review: the confirm-dissolve copy states the group will be dissolved and expenses become independent — it must not claim any 'undo' or 'restore previous category' behavior"
---

<objective>
Complete the group detail page's lifecycle controls: an editable subcategory (GRP-05's second
surface, D-01a), a per-member "Rimuovi dal gruppo" button (GRP-07 single-member removal, D-07),
and a "Scomponi gruppo" dissolve control (GRP-07 whole-group dissolution, D-07/D-08). All three
call the Plan 66-02 server actions directly — no new backend logic in this plan.

Purpose: the group detail page is the ONLY surface for remove/dissolve this phase (D-07 —
explicitly not added to the expenses-table bulk bar) and the primary surface for recategorizing
a group when a user is already looking at its composition.

Output: `GroupDetailClient` gains an editable Categoria control, per-member remove buttons, and a
dissolve control; the RSC page fetches and passes categories/mostUsed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-expense-group-lifecycle/66-CONTEXT.md
@.planning/phases/66-expense-group-lifecycle/66-RESEARCH.md
@.planning/phases/66-expense-group-lifecycle/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Editable group subcategory on the detail page (GRP-05, D-01a)</name>
  <files>app/(app)/expenses/groups/[groupId]/page.tsx, components/expenses/group-detail-client.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    app/(app)/expenses/groups/[groupId]/page.tsx (full file in context) — currently only fetches
    `getExpenseGroupForDetail`; must ALSO fetch categories/mostUsed, same shapes as
    app/(app)/expenses/page.tsx's `getCategories()` (no args) and
    `getMostUsedSubcategories(['in','out','transfer','allocation'])` (both imported from
    `@/lib/dal/categories` and `@/lib/dal/subcategory-usage` respectively).
    components/expenses/group-detail-client.tsx (full file in context) — the read-only
    `categoriaSection` block (L68-89) is what becomes editable; `GroupTitleEdit`'s
    `router.refresh()`-on-success pattern (L97) is the pattern to mirror for the new control.
    components/expenses/expense-categorize-dialog.tsx (full file in context) — reuse this exact
    SubcategoryPicker-wrapping shape for the new inline control (do NOT import
    GroupCategorizeDialog from Plan 66-04 — that file lives in a parallel plan with no
    dependency edge to this one; build an independent, equally-thin inline trigger here instead,
    calling the same `categorizeExpenseGroup` action).
    tests/group-detail-page.test.tsx (full file in context) — the
    `'renders the shared subcategory read-only with no "Cambia categoria" control anywhere'` test
    (L152-159) currently asserts the OLD (Phase 65) invariant and must be updated in this task —
    Phase 66 deliberately adds this control, so the assertion flips to confirm its presence.
  </read_first>
  <behavior>
    - The group detail page's Categoria card shows the current subcategory (as before) plus a
      "Cambia categoria" trigger; tapping it opens a SubcategoryPicker sheet; picking a
      subcategory calls categorizeExpenseGroup with {groupId, subCategoryId}; on success, the
      page refreshes (router.refresh()) so the new category and any member total nuances
      re-resolve from the server.
  </behavior>
  <action>
    In `app/(app)/expenses/groups/[groupId]/page.tsx`: import `getCategories` from
    `@/lib/dal/categories` and `getMostUsedSubcategories` from `@/lib/dal/subcategory-usage`.
    After resolving `group` (and confirming it's not undefined), fetch
    `Promise.all([getCategories(), getMostUsedSubcategories(['in','out','transfer','allocation'])])`
    and pass the results as new `categories`/`mostUsed` props to `<GroupDetailClient group=
    {group} categories={categories} mostUsed={mostUsed} />`.
    In `components/expenses/group-detail-client.tsx`: extend `Props` to add `categories:
    CategoryWithSubCategories[]` and `mostUsed: MostUsedSubcategory[]` (import both types from
    `@/lib/dal/categories` / `@/lib/dal/subcategory-usage`). Import `useState`, `useTransition`
    from react, `toast` from sonner, `SubcategoryPicker` from
    `@/components/categorization/subcategory-picker`, `categorizeExpenseGroup` from
    `@/lib/actions/expenses`. Add local state `const [categorizeOpen, setCategorizeOpen] =
    useState(false)` and `const [isPending, startTransition] = useTransition()`. Replace the
    `categoriaSection` block (L68-89): keep the existing read display (subCategoryName/
    categoryName or "Non assegnata" badge), and add a "Cambia categoria" button below it
    (`aria-label="Cambia categoria"`, `onClick={() => setCategorizeOpen(true)}`). After the
    `categoriaSection` JSX (or anywhere in the component body, since SubcategoryPicker portals to
    body via its own Sheet), render `<SubcategoryPicker open={categorizeOpen}
    onOpenChange={setCategorizeOpen} categories={categories} mostUsed={mostUsed}
    allowedCategoryTypes={['in','out','transfer','allocation']} defaultType={null}
    onChange={(subCategoryId) => { startTransition(async () => { const fd = new FormData();
    fd.set('groupId', String(group.id)); fd.set('subCategoryId', subCategoryId); const result =
    await categorizeExpenseGroup({error: null}, fd); if (result.error) { toast.error(
    result.error); return } toast.success('Gruppo categorizzato.'); setCategorizeOpen(false);
    router.refresh() }) }} pending={isPending} />` — `router` is already declared at the top of
    the component (L59) for `GroupTitleEdit`'s onSuccess, reuse it.
    In tests/group-detail-page.test.tsx, update the existing test (L152-159): remove the two
    `.not.toContain('Cambia categoria')` / `.not.toContain('Assegna categoria')` assertions and
    replace with `expect(html).toContain('Cambia categoria')` (the trigger button must now be
    present). Rename the `it` description to drop "with no ... control anywhere". Add a test for
    the `renderGroupDetailClient` helper (extend it to pass `categories: []` and `mostUsed: []`
    as additional props in every existing call site in this file, since `Props` is now stricter)
    confirming the page still renders with empty category/mostUsed arrays (cold-start safety,
    matching SubcategoryPicker's own empty-array tolerance).
  </action>
  <acceptance_criteria>
    - `vitest tests/group-detail-page.test.tsx --run` passes, including the updated Categoria assertion.
    - `app/(app)/expenses/groups/[groupId]/page.tsx` fetches categories/mostUsed only after confirming the group exists (not on the notFound() path) — no wasted queries for a 404.
    - GroupDetailClient's inline categorize control calls `categorizeExpenseGroup` (not `categorizeExpense` or `bulkCategorize`) with `groupId` (not `id`/`ids`).
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/group-detail-page.test.tsx --run</automated>
  </verify>
  <done>The group detail page's Categoria section is editable via SubcategoryPicker, wired to categorizeExpenseGroup, refreshing the page on success; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-member "Rimuovi dal gruppo" control (GRP-07, D-07/D-08)</name>
  <files>components/expenses/remove-group-member-button.tsx, components/expenses/group-detail-client.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    components/expenses/group-detail-client.tsx `collegamentiCard` member-row rendering
    (L103-127, already read in Task 1's context) — currently the ENTIRE row is one `<Link>`; this
    task restructures it to a flex row containing the Link (title+amount) plus a sibling remove
    button, to avoid a button nested inside an anchor.
    components/expense-table.tsx's `DeleteExpenseMenuItem` (L534-617, already read earlier in
    this session) — the Dialog-wrapped confirm pattern (open state, confirm text, destructive
    button, toast on result) is the shape to mirror for a standalone confirm control (not a
    dropdown item here, since member rows have no existing dropdown menu — a plain button
    triggering a Dialog is simplest).
  </read_first>
  <behavior>
    - Each member row shows its title/amount (unchanged, still a Link to the member's own
      expense detail page) plus a "Rimuovi dal gruppo" button. Clicking it opens a confirmation
      Dialog ("Rimuovere questa spesa dal gruppo? Diventerà una spesa indipendente."); confirming
      calls removeExpenseFromGroupAction with {groupId, expenseId}; on success, toasts and calls
      onSuccess (parent refreshes the page so the member list/count updates, including the
      auto-dissolve case where the group itself may no longer exist).
  </behavior>
  <action>
    Create `components/expenses/remove-group-member-button.tsx`: `'use client'`; import
    `useState` from react, `toast` from sonner, `removeExpenseFromGroupAction` from
    `@/lib/actions/expenses`, `Button` from `@/components/ui/button`, `Dialog, DialogContent,
    DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogClose` from
    `@/components/ui/dialog`. `Props = { groupId: number; expenseId: string; expenseTitle:
    string; onSuccess: () => void }`. Local `open`/`pending` state. `handleRemove`: build
    FormData `{groupId: String(groupId), expenseId}`, call `removeExpenseFromGroupAction({error:
    null}, fd)`; on error toast it and keep the dialog open; on success `toast.success('Spesa
    rimossa dal gruppo.')`, close the dialog, call `onSuccess()`. Render a small ghost/outline
    `Button` (e.g. `aria-label="Rimuovi {expenseTitle} dal gruppo"`, text "Rimuovi") that opens
    the Dialog, whose body confirms with the expense title and the "diventerà una spesa
    indipendente" copy, footer has "Annulla" (DialogClose) and a destructive "Rimuovi" button
    calling handleRemove.
    In `components/expenses/group-detail-client.tsx`: import `RemoveGroupMemberButton`.
    Restructure the `collegamentiCard`'s member-row markup (L112-123): replace the single
    `<Link>` wrapping the whole row with a flex container `<div className="flex items-center
    justify-between gap-2 rounded-md border p-2 text-sm hover:bg-muted/50">` containing (a) a
    `<Link href={expenseDetailHref(member.id)} className="min-w-0 flex-1 truncate">` wrapping
    just the title text, (b) the existing amount `<span>`, and (c) a new
    `<RemoveGroupMemberButton groupId={group.id} expenseId={member.id} expenseTitle=
    {member.title} onSuccess={() => router.refresh()} />` as a sibling (not nested inside the
    Link). Keep the `key={member.id}` on the outer container.
    In tests/group-detail-page.test.tsx, extend the `'GroupDetailClient'` describe block: add a
    case asserting each member row renders a `Rimuovi` control (e.g. `aria-label` containing
    "dal gruppo") without breaking the existing "renders each member with its OWN title and OWN
    total, linking to its own expense detail page" assertions (the `href="/expenses/exp-1"` check
    must still pass with the restructured markup).
  </action>
  <acceptance_criteria>
    - `vitest tests/group-detail-page.test.tsx --run` passes, including the new Rimuovi assertion and all pre-existing member-row assertions.
    - The Rimuovi button is never nested inside the member's title `&lt;Link&gt;` (no button-in-anchor a11y violation) — verifiable by checking the rendered HTML has a sibling structure, not nested.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/group-detail-page.test.tsx --run</automated>
  </verify>
  <done>components/expenses/remove-group-member-button.tsx exists and is wired into each member row; confirming removal calls removeExpenseFromGroupAction and refreshes; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "Scomponi gruppo" dissolve control (GRP-07, D-07/D-08/D-09)</name>
  <files>components/expenses/group-detail-client.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    components/expenses/group-detail-client.tsx (already read) — `DetailPageShell`'s
    `overflowMenu` prop (see components/detail-pages/detail-page-shell.tsx, already read this
    session) is the header slot to use for the dissolve control; the component does not
    currently pass `overflowMenu` at all.
    components/expenses/expense-table.tsx's `DeleteExpenseMenuItem` (already read) — the
    Dialog-inside-DropdownMenuItem shape (DialogTrigger asChild wrapping a DropdownMenuItem) is
    the exact pattern to mirror for a single destructive header action.
  </read_first>
  <behavior>
    - The group detail page header shows a "..." menu with one destructive item, "Scomponi
      gruppo". Selecting it opens a confirmation Dialog explaining the group will be dissolved
      and its expenses become independent (no mention of restoring/reverting anything — D-09).
      Confirming calls dissolveExpenseGroupAction with {groupId}; on success, navigates to
      /expenses (the group page no longer exists); on error, toasts and stays on the page.
  </behavior>
  <action>
    In `components/expenses/group-detail-client.tsx`: import `DropdownMenu, DropdownMenuContent,
    DropdownMenuItem, DropdownMenuTrigger` from `@/components/ui/dropdown-menu`, `Button` from
    `@/components/ui/button` (if not already imported), `MoreHorizontal` from `lucide-react`,
    `dissolveExpenseGroupAction` from `@/lib/actions/expenses`. Add local state
    `dissolveDialogOpen`/`dissolvePending` (or reuse a `useTransition`). Build a
    `dissolveControl` JSX block: a `DropdownMenu` with a ghost icon-button trigger
    (`aria-label="Azioni gruppo"`, `MoreHorizontal` icon) whose `DropdownMenuContent` contains
    one destructive `DropdownMenuItem` (`onSelect={(e) => { e.preventDefault();
    setDissolveDialogOpen(true) }}`, text "Scomponi gruppo"), plus a `Dialog` (controlled by
    `dissolveDialogOpen`) with title "Sciogliere il gruppo?", description "Il gruppo verrà
    sciolto e le spese torneranno indipendenti, mantenendo la categoria attuale." (explicitly NOT
    implying any revert — D-09), footer "Annulla" (closes dialog) + destructive "Scomponi" button
    whose handler builds FormData `{groupId: String(group.id)}`, calls
    `dissolveExpenseGroupAction({error: null}, fd)`; on error, toast it and keep the dialog open;
    on success, `toast.success('Gruppo sciolto.')` then `router.push(APP_ROUTES.expenses)` (import
    `APP_ROUTES` from `@/lib/routes` if not already present — it likely already is, since
    `backHref={APP_ROUTES.expenses}` is passed to DetailPageShell). Pass
    `overflowMenu={dissolveControl}` to the existing `<DetailPageShell ... />` invocation
    (currently has no `overflowMenu` prop).
    In tests/group-detail-page.test.tsx, extend the `'GroupDetailClient'` describe block: add a
    case asserting the rendered HTML contains "Scomponi gruppo" (the dropdown item text) and does
    NOT contain any "ripristina"/"restore"/"annulla lo scioglimento" wording (D-09 guard against
    implying reversibility).
  </action>
  <acceptance_criteria>
    - `vitest tests/group-detail-page.test.tsx --run` passes, including the new Scomponi gruppo assertion.
    - The dissolve confirm copy contains no wording implying restoration/undo of the pre-merge state.
    - dissolveExpenseGroupAction is called with exactly `{groupId}` — no expenseId, no subCategoryId.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/group-detail-page.test.tsx --run</automated>
    <human-check>Open an existing group's detail page in the browser. Confirm "Cambia categoria" opens the picker and updates the category everywhere (table row + detail page) after refresh. Remove one member and confirm it becomes a standalone row in the expenses table. Dissolve a group and confirm you land back on /expenses with all former members now standalone.</human-check>
  </verify>
  <done>The group detail page header exposes a "Scomponi gruppo" destructive action; confirming calls dissolveExpenseGroupAction and navigates to /expenses on success; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GroupDetailClient (client) → server actions | group.id comes from the RSC page's already-ownership-verified `getExpenseGroupForDetail` load; member.id values likewise come from that same trusted server read, not raw client input — but the server actions still re-verify ownership independently (Plan 66-01/66-02), since a stale/cached client render could reference since-removed data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-12 | Tampering | RemoveGroupMemberButton / dissolve control | medium | mitigate | Both ultimately call removeExpenseFromGroupAction/dissolveExpenseGroupAction (Plan 66-02), which re-verify group + expense ownership server-side before any delete — a manipulated client cannot remove/dissolve another user's group even by forging groupId/expenseId values in the request. |
| T-66-13 | Information Disclosure | dissolve navigation | low | accept | After a successful dissolve, the client navigates away (router.push) before any stale group data could be re-rendered from cache; no group data is displayed post-dissolution. |
</threat_model>

<verification>
Run `vitest tests/group-detail-page.test.tsx --run` — full file green, including all new
Categoria/Rimuovi/Scomponi assertions.
</verification>

<success_criteria>
- The group detail page's Categoria section is editable (SubcategoryPicker -> categorizeExpenseGroup).
- Each member row has a working "Rimuovi dal gruppo" control (removeExpenseFromGroupAction).
- The page header exposes a working "Scomponi gruppo" control (dissolveExpenseGroupAction), navigating to /expenses on success.
- tests/group-detail-page.test.tsx is green, including the flipped Categoria assertion and new Rimuovi/Scomponi coverage.
</success_criteria>

## Artifacts this phase produces (this plan)

- `components/expenses/remove-group-member-button.tsx`: `RemoveGroupMemberButton({ groupId, expenseId, expenseTitle, onSuccess })`.
- `components/expenses/group-detail-client.tsx`: extended `Props` (`categories`, `mostUsed`); new local state (`categorizeOpen`, `dissolveDialogOpen`); new `overflowMenu` passed to `DetailPageShell`.
- `app/(app)/expenses/groups/[groupId]/page.tsx`: fetches and passes `categories`/`mostUsed`.

<output>
Create `.planning/phases/66-expense-group-lifecycle/66-05-SUMMARY.md` when done
</output>
