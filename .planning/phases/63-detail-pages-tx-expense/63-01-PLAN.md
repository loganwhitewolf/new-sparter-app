---
phase: 63-detail-pages-tx-expense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dal/transactions.ts
  - lib/dal/expenses.ts
  - lib/routes.ts
  - components/detail-pages/detail-page-shell.tsx
  - tests/detail-page-shell.test.tsx
  - tests/transaction-detail-dal.test.ts
  - tests/expense-detail-dal.test.ts
autonomous: true
requirements: [DET-05, DET-06]

must_haves:
  truths:
    - "getTransactionForDetail(userId, id) returns undefined for a non-owned or non-existent transaction id — no data leak, no throw"
    - "getExpenseForDetail(userId, id) returns undefined for a non-owned or non-existent expense id, and includes its linked transactions when found"
    - "DetailPageShell renders a header (title/amount/actions) and named card slots without assuming which entity it wraps"
  artifacts:
    - lib/dal/transactions.ts (getTransactionForDetail export + TransactionDetailRow type)
    - lib/dal/expenses.ts (getExpenseForDetail export + ExpenseDetailRow type)
    - components/detail-pages/detail-page-shell.tsx
    - lib/routes.ts (transactionDetail, expenseDetail route builders)
  key_links:
    - "getTransactionForDetail scopes its WHERE to (id, userId) exactly like getExpenseById — an IDOR here would leak other users' bank data"
    - "getExpenseForDetail's linked-transactions sub-query scopes to (expenseId, userId) matching getTransactionsByExpenseId's existing double-userId guard"
---

<objective>
Build the read-side foundation Plans 02/03 will consume: ownership-scoped DAL queries
returning every field the two detail pages need to render (including immutable fields
and pair state), the shared `DetailPageShell` layout component (D-02), and the two new
route-builder constants in `lib/routes.ts`.

Purpose: isolate the data-shape and layout decisions in one small, easily-reviewed wave
before any page wiring happens, so Plans 02 and 03 can run in parallel without touching
the same files.

Output: `getTransactionForDetail`, `getExpenseForDetail`, `DetailPageShell`, and
`transactionDetailHref` / `expenseDetailHref`.
</objective>

## Artifacts this phase produces

- `lib/dal/transactions.ts` — `export const getTransactionForDetail` (new), `export type TransactionDetailRow` (new)
- `lib/dal/expenses.ts` — `export const getExpenseForDetail` (new), `export type ExpenseDetailRow` (new)
- `lib/routes.ts` — `export function transactionDetailHref(id: string)` (new), `export function expenseDetailHref(id: string)` (new)
- `components/detail-pages/detail-page-shell.tsx` — new file, `export function DetailPageShell(...)`
- `tests/transaction-detail-dal.test.ts`, `tests/expense-detail-dal.test.ts`, `tests/detail-page-shell.test.tsx` — new test files

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-detail-pages-tx-expense/63-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getTransactionForDetail + getExpenseForDetail DAL queries</name>
  <files>lib/dal/transactions.ts, lib/dal/expenses.ts, tests/transaction-detail-dal.test.ts, tests/expense-detail-dal.test.ts</files>
  <read_first>
    lib/dal/transactions.ts (transactionListSelect object lines 71-150, getTransactions lines 269-383, getTransactionsByExpenseId lines 557-581, TransactionListRow type lines 158-186)
    lib/dal/expenses.ts (getExpenseById lines 260-294, ExpenseRow type lines 53-66, getExpenseImportContext lines 296-327)
    lib/db/schema.ts (transaction, expense, transactionPair table definitions — column names for the pairing sub-queries)
    tests/transaction-edit.test.ts (mocking style: hoisted drizzle-orm / schema mocks, thenable select-chain mock)
  </read_first>
  <behavior>
    - `getTransactionForDetail({ userId, id })` returns `undefined` when the id does not
      exist OR belongs to another user (WHERE `and(eq(transaction.id, id), eq(transaction.userId, userId))`)
    - `getTransactionForDetail` returns a full row when found: `id, description,
      transactionHash, descriptionHash, customTitle, amount, currency, occurredAt,
      rowIndex, expenseId, expenseTitle, expenseStatus, expenseNotes,
      expenseSubCategoryId, subCategoryName, categoryName, categorySlug,
      expenseTransactionCount, fileId, fileName, platformName, pairedWithId,
      pairedAmount, pairedDescription, pairedOccurredAt, pairedNetAmount`
    - `getExpenseForDetail({ userId, id })` returns `undefined` for a non-owned or
      missing id (WHERE `and(eq(expense.id, id), eq(expense.userId, userId))`)
    - `getExpenseForDetail` returns the same fields as `ExpenseRow` (id, title, status,
      notes, createdAt, totalAmount, transactionCount, subCategoryId, subCategoryName,
      categoryName, categorySlug, platformName) plus `sourceFile: { id, name } | null`
      and `transactions: ExpenseTransactionRow[]` (reuse the existing
      `ExpenseTransactionRow` type from `lib/dal/transactions.ts`, ordered by
      `desc(transaction.occurredAt)`, same query shape as `getTransactionsByExpenseId`)
    - Both functions are ownership-scoped identically to their list-page analogs — a
      test with a mismatched `userId` on an otherwise-valid `id` must return `undefined`,
      never throw
  </behavior>
  <action>
    In `lib/dal/transactions.ts`: add `export type TransactionDetailRow` and
    `export const getTransactionForDetail = cache(async ({ userId, id }: { userId: string; id: string }): Promise<TransactionDetailRow | undefined> => {...})`.
    Build the select object by extending the existing `transactionListSelect` shape (do
    not duplicate the pairing sub-queries — reuse the same `sql` templates already
    defined for `pairedWithId`/`pairedNetAmount`/`pairedAmount`/`pairedDescription`/`pairedOccurredAt`
    in the module) plus new fields not in `transactionListSelect`:
    `transactionHash: transaction.transactionHash`, `descriptionHash: transaction.descriptionHash`,
    `expenseNotes: expense.notes`, `expenseSubCategoryId: expense.subCategoryId`.
    Join chain: same as `getTransactions` (importFile, importFormatVersion, platform,
    expense, subCategory, category, userSubcategoryOverride) — the file/platform/category
    joins already exist there; do not invent new join paths. `.where(and(eq(transaction.id, id), eq(transaction.userId, userId))).limit(1)`,
    return `rows[0]`.

    In `lib/dal/expenses.ts`: add `export type ExpenseDetailRow = ExpenseRow & { sourceFile: ExpenseSourceFile | null; transactions: ExpenseTransactionRow[] }`
    (import `ExpenseTransactionRow` from `@/lib/dal/transactions`). Add
    `export const getExpenseForDetail = cache(async ({ userId, id }: { userId: string; id: string }): Promise<ExpenseDetailRow | undefined> => {...})`
    that: (1) runs the same select/join/where as `getExpenseById` but scoped by the
    passed `userId` parameter instead of calling `verifySession()` internally (the RSC
    page already calls `verifySession()` once; do not duplicate the session call inside
    this new function — accept `userId` as an argument, unlike `getExpenseById` which
    self-verifies); (2) returns `undefined` immediately if no row found; (3) when found,
    runs a second query for linked transactions scoped to
    `and(eq(transaction.expenseId, id), eq(transaction.userId, userId))` ordered by
    `desc(transaction.occurredAt)`, selecting `{ id, description, customTitle, amount,
    currency, occurredAt }`; (4) resolves `sourceFile` from the same `file`/`importFormatVersion`
    join already present in the row query (`row.fileId`/`displayName`/`originalName`
    exactly as `getExpenseImportContext` does) — do not call `getExpenseImportContext`
    separately, fold its logic into this one query to avoid a second round-trip.

    Both new exports are additive — do not modify `getExpenseById`, `getTransactions`,
    or `getTransactionsByExpenseId`.
  </action>
  <verify>
    <automated>yarn vitest run tests/transaction-detail-dal.test.ts tests/expense-detail-dal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const getTransactionForDetail" lib/dal/transactions.ts` matches
    - `grep -n "export const getExpenseForDetail" lib/dal/expenses.ts` matches
    - `grep -n "eq(transaction.userId, userId)" lib/dal/transactions.ts` — the detail
      query's WHERE includes this exact ownership condition (not just `eq(transaction.id, id)` alone)
    - `grep -n "eq(expense.userId, userId)" lib/dal/expenses.ts` — same ownership guard
      present in `getExpenseForDetail`
    - `yarn vitest run tests/transaction-detail-dal.test.ts tests/expense-detail-dal.test.ts` — all pass
    - Test file asserts a mismatched-userId call returns `undefined` (not a thrown error) for both functions
    - `yarn tsc --noEmit` introduces no new errors attributable to these two files
  </acceptance_criteria>
  <done>Both DAL functions exist, are ownership-scoped, exported, and covered by passing tests proving the 404-shape (undefined) contract for non-owned rows.</done>
</task>

<task type="auto">
  <name>Task 2: DetailPageShell component + route constants</name>
  <files>components/detail-pages/detail-page-shell.tsx, lib/routes.ts, tests/detail-page-shell.test.tsx</files>
  <read_first>
    lib/routes.ts (APP_ROUTES object, existing route-builder function pattern e.g. dashboardCategoryDetail)
    components/transactions/transaction-title-edit.tsx (pencil-edit visual conventions: text sizing, muted-foreground, Pencil icon usage — for shell typography consistency)
    tests/category-detail-components.test.tsx (renderToStaticMarkup test pattern for presentational components)
  </read_first>
  <action>
    In `lib/routes.ts`, add two route-builder functions following the existing
    `dashboardCategoryDetail(id)` pattern (not a static string, since these take a
    dynamic id): `export function transactionDetailHref(id: string) { return \`${APP_ROUTES.transactions}/${encodeURIComponent(id)}\` }`
    and `export function expenseDetailHref(id: string) { return \`${APP_ROUTES.expenses}/${encodeURIComponent(id)}\` }`.
    Do not add these as `APP_ROUTES` object keys (the object holds only static route
    strings today — mixing in functions breaks the `as const` shape); export them as
    standalone functions from the same file, matching `dashboardCategoryDetail`'s
    placement and export style.

    Create `components/detail-pages/detail-page-shell.tsx` as a client component
    (`'use client'`) exporting `DetailPageShell` with props: `backHref: string`,
    `title: React.ReactNode`, `amount?: React.ReactNode`, `primaryAction?: React.ReactNode`,
    `overflowMenu?: React.ReactNode`, and named card slots as optional `React.ReactNode`
    props: `datiCard`, `categoriaCard`, `collegamentiCard`, `riepilogoCard`,
    `transactionsCard` (the last two are expense-page-only; the transaction page passes
    `undefined` for them). Render: a back link (`Link` from `next/link` to `backHref`,
    ArrowLeft icon from lucide-react), then a header with `title` (h1) + `amount`
    beneath it, `primaryAction` and `overflowMenu` as header-right buttons, then a
    single-column stacked list of the non-undefined card slots in the fixed order
    `datiCard, categoriaCard, collegamentiCard, riepilogoCard, transactionsCard`, each
    wrapped in the existing shadcn `Card`/`CardContent` components (`@/components/ui/card`)
    for visual consistency with the rest of the app (do not hand-roll `<div className="card">`).
    Mobile-first single column, no sidebar grid (D-01).
  </action>
  <verify>
    <automated>yarn vitest run tests/detail-page-shell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function transactionDetailHref" lib/routes.ts` matches
    - `grep -n "export function expenseDetailHref" lib/routes.ts` matches
    - `transactionDetailHref('abc')` returns `/transactions/abc`; `expenseDetailHref('abc')` returns `/expenses/abc` (asserted in test)
    - `grep -n "'use client'" components/detail-pages/detail-page-shell.tsx` matches (first line)
    - `grep -n "export function DetailPageShell" components/detail-pages/detail-page-shell.tsx` matches
    - `yarn vitest run tests/detail-page-shell.test.tsx` passes — test renders the shell
      with only `datiCard` set and asserts `categoriaCard`/`collegamentiCard` content is
      absent from output, then renders again with all five card slots and asserts all
      five appear in DOM order
    - `yarn tsc --noEmit` introduces no new errors attributable to these two files
  </acceptance_criteria>
  <done>DetailPageShell renders header + conditional card slots in the fixed order; transactionDetailHref/expenseDetailHref produce correct encoded paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| RSC page → DAL | `userId` passed into `getTransactionForDetail`/`getExpenseForDetail` must originate from `verifySession()`, never from route params or client input |
| DAL → DB | `id` route param is attacker-controlled; must never be trusted as an ownership proof on its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Spoofing/Elevation (IDOR) | `getTransactionForDetail`, `getExpenseForDetail` | high | mitigate | Ownership condition (`eq(*.userId, userId)`) is a mandatory AND clause in every WHERE, verified in Task 1 acceptance criteria via grep + test |
| T-63-02 | Information Disclosure | `getExpenseForDetail` linked-transactions sub-query | medium | mitigate | Sub-query scopes on both `expense.userId` and `transaction.userId`, matching `getTransactionsByExpenseId`'s existing double-guard — a cross-user expenseId cannot leak another user's transaction rows |
| T-63-03 | Tampering | DetailPageShell | low | accept | Pure presentational component with no data mutation or trust boundary crossing |
</threat_model>

<verification>
- `yarn vitest run tests/transaction-detail-dal.test.ts tests/expense-detail-dal.test.ts tests/detail-page-shell.test.tsx`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- `getTransactionForDetail` and `getExpenseForDetail` exist, are ownership-scoped, and return `undefined` (not throw) for non-owned/missing ids.
- `DetailPageShell` renders header + card slots in fixed order, mobile-first single column.
- `transactionDetailHref`/`expenseDetailHref` produce the exact paths Plans 02-04 will link to.
</success_criteria>

<output>
Create `.planning/phases/63-detail-pages-tx-expense/63-01-SUMMARY.md` when done
</output>
