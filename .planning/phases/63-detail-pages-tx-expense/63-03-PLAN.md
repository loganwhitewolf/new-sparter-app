---
phase: 63-detail-pages-tx-expense
plan: 03
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - app/(app)/expenses/[id]/page.tsx
  - components/expenses/expense-detail-client.tsx
  - components/expenses/expense-notes-edit.tsx
  - tests/expense-detail-page.test.tsx
autonomous: true
requirements: [DET-06]

must_haves:
  truths:
    - "Visiting /expenses/[id] for an owned expense shows title, notes, category, readonly derived totals (totalAmount, transactionCount, first/last dates), and the linked-transactions list, each row linking to /transactions/[id]"
    - "Visiting /expenses/[id] for a non-owned or non-existent id returns a 404 (notFound())"
    - "An uncategorized expense (subCategoryId null) shows an amber 'Categorizza' CTA in the header; a categorized expense does not"
    - "Editing title or notes successfully triggers router.refresh() with no toast/notice"
  artifacts:
    - app/(app)/expenses/[id]/page.tsx
    - components/expenses/expense-detail-client.tsx
    - components/expenses/expense-notes-edit.tsx
  key_links:
    - "ExpenseDetailClient invokes updateExpense (lib/actions/expenses.ts, Phase 62-extended) for title/notes/category edits — no new write path is introduced"
    - "Linked-transactions rows link to transactionDetailHref(tx.id), giving the two new pages a working cross-reference loop"
    - "Source file and platform cross-refs reuse the sourceFile/platformName fields already assembled by getExpenseForDetail (Plan 01) — no duplicate query"
---

<objective>
Build `/expenses/[id]` — merging today's "dettagli" and "modifica" expense dialogs into
a single route page (DET-06), with pencil-inline edit for title/notes/category, readonly
derived totals, the linked-transactions cross-reference table, and the existing action
set (cerca su internet, categorizza, elimina).

Purpose: this is the page DET-07 will point the expense table's "Dettagli" menu entry
at, replacing `expense-transactions-dialog.tsx` and the edit-mode `expense-form-dialog.tsx`
invocation.

Output: a working authenticated route page and a client wrapper reusing
`ExpenseTitleEdit`, `SubcategoryPicker`, and the existing delete-confirmation pattern.
</objective>

## Artifacts this phase produces

- `app/(app)/expenses/[id]/page.tsx` — new file, `export default async function ExpenseDetailPage(...)`
- `components/expenses/expense-detail-client.tsx` — new file, `export function ExpenseDetailClient(...)`
- `components/expenses/expense-notes-edit.tsx` — new file, `export function ExpenseNotesEdit(...)`
- `tests/expense-detail-page.test.tsx` — new test file
- Consumes (not produced here): `getExpenseForDetail`, `DetailPageShell`, `transactionDetailHref` (63-01); `categorizeExpense`, `updateExpense`, `deleteExpense` (existing/Phase 62)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-detail-pages-tx-expense/63-CONTEXT.md
@.planning/phases/62-transaction-edit-core/62-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notes inline-edit component</name>
  <files>components/expenses/expense-notes-edit.tsx, tests/expense-detail-page.test.tsx</files>
  <read_first>
    components/expenses/expense-title-edit.tsx (the exact pencil/Enter(submit)/Esc/useActionState pattern to replicate for a textarea field — D-05)
    lib/actions/expenses.ts (updateExpense action: reads `id`, `title`, `subCategoryId`, `notes` from FormData — note `title` is required by `UpdateExpenseSchema`, so the notes-only edit must also submit the expense's current `title` unchanged)
    lib/validations/expense.ts (UpdateExpenseSchema field constraints for notes/title)
  </read_first>
  <behavior>
    - `ExpenseNotesEdit` displays existing notes text (or a muted "Aggiungi note"
      placeholder when empty) with a pencil affordance; clicking enters edit mode with
      a `<textarea>` pre-filled with the current notes value
    - Submitting calls `updateExpense` (from `@/lib/actions/expenses`) with `id`,
      `title` (the expense's current title, passed through unchanged via a hidden
      input — `UpdateExpenseSchema` requires it), and `notes` set to the new value;
      `subCategoryId` is omitted from the FormData entirely so the three-state
      contract (DET-04) leaves category/status untouched
    - On success (`state.error === null`), exits edit mode and calls `onSuccess`
    - Escape cancels without submitting; error text renders in red below the textarea
      exactly like `ExpenseTitleEdit`
  </behavior>
  <action>
    Create `components/expenses/expense-notes-edit.tsx` (`'use client'`) exporting
    `ExpenseNotesEdit({ id, title, notes, onSuccess }: { id: string; title: string;
    notes: string | null; onSuccess?: (newNotes: string) => void })`. Follow
    `expense-title-edit.tsx`'s `useActionState(updateExpense, { error: null })` +
    `submittedRef` + `pendingValueRef` + `useEffect` structure exactly, swapping the
    input for a `<textarea name="notes">` and adding `<input type="hidden" name="title"
    value={title} />` so the required `title` field is always present in the submitted
    FormData even though only notes changed. Placeholder copy for empty notes:
    "Aggiungi note" in muted-foreground text, matching the visual weight of
    `ExpenseTitleEdit`'s non-editing button state.
  </action>
  <verify>
    <automated>yarn vitest run tests/expense-detail-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function ExpenseNotesEdit" components/expenses/expense-notes-edit.tsx` matches
    - `grep -n "updateExpense" components/expenses/expense-notes-edit.tsx` — imports and calls the existing action (no new server action created)
    - `grep -n 'name="title"' components/expenses/expense-notes-edit.tsx` — hidden title field present, confirming the three-state `subCategoryId` contract stays untouched on a notes-only submit
    - `yarn vitest run tests/expense-detail-page.test.tsx` includes and passes a case
      asserting the submitted FormData for a notes edit has no `subCategoryId` key
    - `yarn tsc --noEmit` introduces no new errors attributable to this file
  </acceptance_criteria>
  <done>ExpenseNotesEdit exists, reuses updateExpense, and never sends subCategoryId on a notes-only save.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /expenses/[id] RSC page + client wrapper</name>
  <files>app/(app)/expenses/[id]/page.tsx, components/expenses/expense-detail-client.tsx, tests/expense-detail-page.test.tsx</files>
  <read_first>
    app/(app)/import/[fileId]/suggestions/page.tsx (ownership-gate RSC page pattern)
    components/expenses/expense-table.tsx (lines 280-465: DeleteExpenseMenuItem confirmation dialog body, IgnoreExpenseMenuItem, ExpenseCategorizeDialog wiring, "Da categorizzare" amber CTA pattern)
    components/expenses/expense-transactions-dialog.tsx (linked-transactions table columns/formatting to lift into a card — # / Descrizione / Importo / Data — and the platformName/sourceFile cross-ref block above it; this component is NOT deleted in this plan, only its structure is replicated)
    components/expenses/expense-categorize-dialog.tsx (props: open, onOpenChange, expense: {id, title}, categories, mostUsed, onSuccess)
    lib/dal/expenses.ts (getExpenseForDetail from Plan 01 — ExpenseDetailRow field list, including transactions[] and sourceFile)
    lib/routes.ts (transactionDetailHref, expenseDetailHref from Plan 01; APP_ROUTES.expenses for the delete-redirect and back link)
    components/detail-pages/detail-page-shell.tsx (Plan 01 — props contract)
    lib/utils/format-amount.ts (formatAbsoluteAmount — totalAmount display; expense page shows real sign like the table's totalAmount column already does via a sign-aware class, see expense-table.tsx line ~283)
  </read_first>
  <behavior>
    - GET `/expenses/{ownedId}` renders 200 with title (editable), notes (editable),
      category (editable via SubcategoryPicker or amber CTA if uncategorized), readonly
      Riepilogo card (totalAmount signed, transactionCount, createdAt — matches
      `ExpenseRow` fields; there is no separate first/lastTransactionAt field exposed
      by `ExpenseRow`/`getExpenseForDetail`, so display what the DAL actually returns,
      do not invent fields), a Transazioni card listing every linked transaction (date,
      description, amount) each linking to `transactionDetailHref(tx.id)`, and a
      Collegamenti section showing `sourceFile` (link to the existing
      `${APP_ROUTES.import}?fileId=...`-style filtered import view per D-16) and
      `platformName`
    - GET `/expenses/{nonOwnedId}` and `/expenses/{missingId}` both call `notFound()`
    - Header shows "Cerca su internet" as a primary button + overflow menu for the rest
      (D-09): Google search using
      `expense.title`, identical construction to `expense-transactions-dialog.tsx`'s
      `handleSearchOnline`) and an overflow menu with Elimina (destructive,
      confirmation dialog matching `DeleteExpenseMenuItem`'s body exactly, including
      the linked-transactions warning copy, redirecting to `APP_ROUTES.expenses` with a
      success toast per D-11)
    - When `subCategoryId` is null, an amber "Categorizza" button appears in the header
      (D-12) opening `SubcategoryPicker`/`categorizeExpense`; when categorized, no
      header button — the Categoria card itself opens the picker for recategorization
    - Category card tap opens `SubcategoryPicker` calling the existing
      `categorizeExpense` action (same as `ExpenseCategorizeDialog`), then
      `router.refresh()`
  </behavior>
  <action>
    Create `app/(app)/expenses/[id]/page.tsx` as an async RSC page:
    `export default async function ExpenseDetailPage({ params }: { params:
    Promise<{ id: string }> })`. `const { id } = await params`, `const { userId } =
    await verifySession()`, `const exp = await getExpenseForDetail({ userId, id })`,
    `if (!exp) notFound()`. Fetch `categories`/`mostUsed` in parallel via `Promise.all`
    (same two calls `expenses/page.tsx` already makes for its dialogs). Render
    `<ExpenseDetailClient expense={exp} categories={categories} mostUsed={mostUsed} />`.

    Create `components/expenses/expense-detail-client.tsx` (`'use client'`) exporting
    `ExpenseDetailClient({ expense, categories, mostUsed })`. Use `useRouter()` for
    `router.refresh()` (silent, D-08) and `router.push(APP_ROUTES.expenses)` after
    delete. Hold local `useState` booleans for `categoryPickerOpen` and `deleteOpen`.
    Wire:
    - `ExpenseTitleEdit` (existing, reused unmodified) with `onSuccess={() =>
      router.refresh()}`
    - `ExpenseNotesEdit` (Plan 03 Task 1) with `title={expense.title}`,
      `notes={expense.notes}`, `onSuccess={() => router.refresh()}`
    - `SubcategoryPicker` calling `categorizeExpense` from `@/lib/actions/expenses`
      exactly as `ExpenseCategorizeDialog` does (`allowedCategoryTypes={['in','out','transfer','allocation']}`,
      `defaultType={null}`), on success `toast.success('Spesa categorizzata.')` then
      `router.refresh()`
    - Delete: replicate `DeleteExpenseMenuItem`'s dialog body (title, warning copy,
      `deleteLinkedTransactions` checkbox shown only when `expense.transactionCount >
      0`) calling `deleteExpense` from `@/lib/actions/expenses`; on success
      `toast.success('Spesa eliminata.')` then `router.push(APP_ROUTES.expenses)`
    - Transazioni card (D-04 — inline table in a card): render `expense.transactions` in a `Table` matching
      `expense-transactions-dialog.tsx`'s column set (#, Descrizione, Importo, Data),
      with each row's description wrapped in a `Link` to
      `transactionDetailHref(tx.id)` (this is the cross-ref DET-06 requires — "each
      linking to its page")
    - Riepilogo card: `totalAmount` formatted with sign-aware styling matching
      `expense-table.tsx`'s existing `text-total-out`/`text-total-in` class pattern,
      `transactionCount`, `createdAt` (formatted `it-IT` date)
    - Collegamenti card: `sourceFile` as a `Link` to
      `${APP_ROUTES.import}?fileId=${encodeURIComponent(sourceFile.id)}` when present
      (D-16 — route through a single constant so Phase 64's repoint is one-line; do
      not hardcode the query string a second way elsewhere), else omit the row;
      `platformName` as plain text when present
  </action>
  <verify>
    <automated>yarn vitest run tests/expense-detail-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notFound()" "app/(app)/expenses/[id]/page.tsx"` matches
    - `grep -n "getExpenseForDetail" "app/(app)/expenses/[id]/page.tsx"` matches
    - `grep -n "transactionDetailHref" components/expenses/expense-detail-client.tsx` matches — linked-transaction rows are real links, not plain text
    - `yarn vitest run tests/expense-detail-page.test.tsx` passes, covering: 200 render
      with all fields, 404 for a mismatched-owner fixture, amber Categorizza CTA
      present when `subCategoryId` is null and absent when set, linked-transactions
      rows render an `<a href="/transactions/...">` per row
    - `yarn tsc --noEmit` introduces no new errors attributable to these two files
  </acceptance_criteria>
  <done>/expenses/[id] renders all required fields, actions, and cross-refs (including working links to /transactions/[id]), 404s for non-owned ids, and reuses categorizeExpense/deleteExpense/ExpenseTitleEdit unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → RSC page | `id` route param is attacker-controlled |
| Client wrapper → server actions | Category/delete/notes forms submit `id` values sourced from already-ownership-checked page props; actions re-verify ownership server-side regardless |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-08 | Spoofing/Elevation (IDOR) | `app/(app)/expenses/[id]/page.tsx` | high | mitigate | `getExpenseForDetail` ownership-scoped query (Plan 01) + `notFound()` on undefined |
| T-63-09 | Tampering | `ExpenseNotesEdit` | medium | mitigate | Writes route through the existing atomic `updateExpense` DAL (Phase 62), which re-scopes to `(id, userId)`; the three-state `subCategoryId` contract prevents an accidental category clear on a notes-only submit |
| T-63-10 | Information Disclosure | Linked-transactions list | medium | mitigate | `getExpenseForDetail`'s transaction sub-query double-scopes on `expense.userId` and `transaction.userId` (Plan 01), matching `getTransactionsByExpenseId` |
| T-63-11 | Repudiation | Delete action | low | accept | Existing confirmation dialog + toast pattern already accepted at table level; unchanged here |
</threat_model>

<verification>
- `yarn vitest run tests/expense-detail-page.test.tsx`
- `yarn tsc --noEmit`
- Manual: navigate to `/expenses/{id}` for a multi-transaction expense, click a linked-transaction row, confirm navigation to `/transactions/{id}`; navigate back and confirm the browser back button returns to the expense page (route-page benefit vs. the old dialog)
</verification>

<success_criteria>
- DET-06 fully satisfied: title/notes/category pencil-inline editable, readonly derived totals, linked-transactions list with working per-row links, cerca su internet + categorizza + elimina actions, source file + platform cross-refs.
- Zero new server actions created — everything routes through existing `categorizeExpense`/`updateExpense`/`deleteExpense`.
</success_criteria>

<output>
Create `.planning/phases/63-detail-pages-tx-expense/63-03-SUMMARY.md` when done
</output>
