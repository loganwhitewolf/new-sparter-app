---
phase: 73-reimbursement-schema-and-netting
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - lib/services/reimbursement-invariant.ts
  - tests/reimbursement-invariant.test.ts
  - tests/reimbursement-regression.test.ts
  - tests/migration-backfill.test.ts
  - tests/fixtures/reimbursement-seed.ts
autonomous: true
requirements: [RMB-03, RMB-04, RMB-05]

must_haves:
  truths:
    - "createReimbursement-shaped input with an outflow (negative-amount) anchor and inflow (positive-amount) refunds is accepted; an inflow anchor is rejected; an outflow-signed refund is rejected (D-02, RMB-03)"
    - { statement: "The dinner case (1 outflow anchor, N=3 inflow refunds summing to less than the anchor) nets correctly: effectiveAmount() = anchor + sum(all 3 refunds), all 3 refunds excluded from their own months, identical before/after the same regression comparison used in 73-01", verification: covered }
    - { statement: "Adjacency — refunds summing to EXACTLY the anchor's magnitude: effectiveAmount() returns exactly 0 for the anchor, and the anchor row remains present in aggregation (isNotSecondary() still true for it) rather than disappearing", verification: covered }
    - { statement: "Adjacency — refunds summing to MORE than the anchor's magnitude: effectiveAmount() flips sign; the dashboard totalOut computation treats this identically before and after migration (this is pre-existing 1:1 behavior being carried forward unchanged, not a new fix)", verification: covered }
    - { statement: "Ordering — refund linking order does not affect the SUM used by effectiveAmount(): two refunds of equal amount linked in either order produce the same net", verification: covered }
    - "Q3 (multi-transaction Expense anchor): an Expense with 2 transactions (earlier + later), reimbursement anchored on that expenseId with 1 refund linked, nets ONLY the earliest transaction (the documented Q2/Q3 tie-break); the later sibling transaction's effectiveAmount is untouched"
    - "Every existing transaction_pair row maps to exactly one reimbursement + one reimbursement_refund row; row counts reconcile with zero orphans; a transaction_pair table with zero rows backfills to zero reimbursement rows without error"
  artifacts:
    - lib/services/reimbursement-invariant.ts
    - tests/reimbursement-invariant.test.ts
    - tests/migration-backfill.test.ts
  key_links:
    - "lib/services/reimbursement-invariant.ts assertOutflowAnchorAmount/assertInflowRefundAmount -> reused by Plan 73-04's repointed createPair (defense-in-depth alongside the XOR/uniqueness DB constraints)"
    - "tests/fixtures/reimbursement-seed.ts dinner/adjacency/ordering/Q3 builders -> shared by both reimbursement-regression.test.ts and migration-backfill.test.ts"
---

<objective>
Expand Plan 73-01's narrow N=1 tracer proof into the full regression matrix ADR 0018 and the
spec-less probe fallback require, and add the D-02 invariant enforcement (outflow-anchor /
inflow-refund) that Plan 73-01 deferred. This plan does not touch any new production consumer code
— lib/dal/dashboard.ts, overview.ts, tags.ts already pick up 73-01's generalized
effectiveAmount()/isNotSecondary() with zero changes (per 73-PATTERNS.md: "no changes to query
shape needed at these call sites").

Purpose: prove Mondo Netto (D-04) holds for every real scenario surfaced in ADR 0018 and
73-CONTEXT.md (dinner N>1, adjacency at and past the anchor's magnitude, refund-order determinism,
the Q3 multi-transaction-Expense edge) — not just the N=1 case — and give D-02 a concrete,
testable, reusable enforcement point before Plan 73-04 wires it into the live write path.
Output: `lib/services/reimbursement-invariant.ts`, an expanded regression suite covering 5 real
scenarios, and a dedicated migration-backfill correctness suite (row-count reconciliation, zero
orphans, zero-row safety).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-reimbursement-schema-and-netting/73-CONTEXT.md
@docs/adr/0018-reimbursement-1n-linking-supersedes-net-by-subcategory.md
@lib/services/transaction-pairs.ts
@lib/utils/decimal.ts
.planning/phases/73-reimbursement-schema-and-netting/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 invariant enforcement module</name>
  <read_first>
    lib/services/transaction-pairs.ts lines 107-116 (the existing opposite-sign `oppositeSign`
    check pattern this generalizes — same Decimal.js gt/lt(0) idiom, same "throw a localized
    Italian Error" convention), lib/utils/decimal.ts (toDecimal signature).
  </read_first>
  <behavior>
    - assertOutflowAnchorAmount('-100.00') does not throw.
    - assertOutflowAnchorAmount('50.00') throws an Error with an Italian message naming the anchor/outflow requirement.
    - assertOutflowAnchorAmount('0.00') throws (zero is neither an outflow nor an inflow — same CR-03 convention as the existing pair code).
    - assertInflowRefundAmount('50.00') does not throw.
    - assertInflowRefundAmount('-30.00') throws an Error with an Italian message naming the refund/inflow requirement.
    - assertInflowRefundAmount('0.00') throws.
    - assertReimbursementAmounts({ anchorAmount: '-100.00', refundAmounts: ['30.00', '20.00'] }) does not throw (dinner-shaped input).
    - assertReimbursementAmounts({ anchorAmount: '-100.00', refundAmounts: ['30.00', '-20.00'] }) throws — one bad refund in a multi-refund set is still rejected, not silently dropped.
  </behavior>
  <files>lib/services/reimbursement-invariant.ts, tests/reimbursement-invariant.test.ts</files>
  <action>
    Create lib/services/reimbursement-invariant.ts exporting `assertOutflowAnchorAmount(amount:
    string): void` (throws unless toDecimal(amount).lt(0)), `assertInflowRefundAmount(amount:
    string): void` (throws unless toDecimal(amount).gt(0)), and `assertReimbursementAmounts(input:
    { anchorAmount: string; refundAmounts: string[] }): void` calling both across the full set —
    this is the D-02 invariant (RMB-03) as a small, dependency-free, directly reusable module (no DB
    access; pure string-in Decimal.js checks), mirroring the existing opposite-sign convention in
    lib/services/transaction-pairs.ts rather than inventing a new pattern. Error messages in
    Italian, distinct wording for the anchor case vs the refund case so a caller can tell which side
    failed. This module is consumed by Plan 73-04's repointed createPair — do not wire it into
    createPair here (that repoint is 73-04's task), just build and unit-test it standalone.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/reimbursement-invariant.test.ts</automated>
  </verify>
  <done>All 8 behavior cases pass; the module has zero DB/service dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression matrix + migration-backfill correctness</name>
  <read_first>
    tests/reimbursement-regression.test.ts (73-01's N=1 scenario — the harness/fixture pattern to
    extend, not replace), tests/fixtures/reimbursement-seed.ts (73-01's builders),
    lib/dal/dashboard.ts getOverviewAmountTotals/getCategoriesBreakdown (the queries every scenario
    below calls), 73-RESEARCH.md "Answers to Open Questions" Q2/Q3 section.
  </read_first>
  <files>tests/reimbursement-regression.test.ts, tests/migration-backfill.test.ts, tests/fixtures/reimbursement-seed.ts</files>
  <action>
    Extend tests/fixtures/reimbursement-seed.ts with: `seedReimbursement({ userId, expenseId,
    refundTransactionIds })` inserting directly into reimbursement + reimbursement_refund (for
    scenarios built natively in the new schema rather than migrated from a legacy pair), and a
    helper to seed K independent legacy transaction_pair rows for bulk backfill testing.

    Add 5 scenarios to tests/reimbursement-regression.test.ts, each computing expected values via
    Decimal.js arithmetic on the same seeded input amounts (never hand-typed expected totals):
    (1) Dinner — one outflow anchor (e.g. -90.00), three inflow refunds (30.00, 30.00, 30.00)
    seeded natively via seedReimbursement; assert effectiveAmount() for the anchor equals
    toDecimal('-90.00').plus(90.00) = 0 and each refund is excluded via isNotSecondary(). (2)
    Adjacency-exact — refunds summing to exactly the anchor's magnitude (already covered by
    scenario 1's own math, but assert explicitly that the anchor transaction still appears in
    getCategoriesBreakdown's row set with amount 0, not silently dropped). (3) Adjacency-exceeds —
    refunds summing to MORE than the anchor magnitude (e.g. anchor -50.00, refunds 30.00 + 40.00);
    assert the resulting effectiveAmount() (positive 20.00) flows into
    getOverviewAmountTotals.totalOut identically whether computed via the frozen pre-migration
    fragment (seeded as a legacy transaction_pair... note: a legacy 1:1 pair cannot express 2
    refunds, so seed this case natively via seedReimbursement and instead assert internal
    consistency — SUM(CASE WHEN direction='out' THEN effectiveAmount...) reflects the signed 20.00
    correctly, not that it matches some 1:1-shaped "before") or via the current code (this
    scenario's "before/after" comparison is about the NEW model's internal correctness at N=2, not
    a legacy-shape diff — document this distinction in the test's describe block). (4) Ordering —
    seed 2 equal-amount refunds (25.00 and 25.00) linked at different `createdAt` timestamps in
    both possible insert orders across two sub-tests; assert the resulting SUM/effectiveAmount() is
    identical regardless of order (trivial for SUM, but pin it explicitly per the ordering probe).
    (5) Q3 — one Expense with 2 transactions (occurredAt earlier -50.00, occurredAt later -50.00),
    a reimbursement anchored on that expenseId, one refund transaction (50.00) linked; assert the
    EARLIER transaction's effectiveAmount() becomes 0 (earlier.amount + refund) and the LATER
    transaction's effectiveAmount() remains -50.00 unchanged — this is the concrete, tested
    resolution of Q3's tie-break rule from Plan 73-01 Task 2.

    Create tests/migration-backfill.test.ts (same real-Postgres harness from
    tests/helpers/reimbursement-test-db.ts, same graceful-skip-if-unreachable pattern as 73-01):
    seed K=5 independent legacy transaction_pair rows across different users; run the backfill
    migration; assert reimbursement row count == 5, reimbursement_refund row count == 5, every
    reimbursement.expenseId matches its originating pair's primary transaction's expenseId, every
    reimbursement_refund.transactionId matches its originating pair's transaction_b_id (zero
    orphans, zero cross-wiring). Separately assert: an empty transaction_pair table backfills to
    zero reimbursement rows with no error (0-row safety).
  </action>
  <verify>
    <automated>yarn db:up && ./node_modules/.bin/vitest run tests/reimbursement-regression.test.ts tests/migration-backfill.test.ts</automated>
  </verify>
  <done>All 5 regression scenarios and the migration-backfill row-count/orphan checks pass with
  Docker Postgres running; the whole suite still gracefully skips (not fails) without it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| reimbursement-invariant module -> any future caller | A pure validation function; its correctness is the only thing standing between "amount sign" and D-02's outflow/inflow rule once Plan 73-04 wires it in |
| regression/backfill test harness -> Postgres | Same real-DB boundary introduced in 73-01, reused here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-73-06 | Tampering | assertOutflowAnchorAmount/assertInflowRefundAmount (D-02) | high | mitigate | 8 unit-tested behavior cases including the zero-amount edge (CR-03 precedent); reused verbatim by Plan 73-04 rather than re-implemented, avoiding drift between two invariant checks |
| T-73-07 | Repudiation / silent-corruption | Netting math at N>1 (dinner, adjacency, ordering, Q3) | high | mitigate | Every scenario computes expected values via Decimal.js from the same seeded inputs (never hand-typed), against a real Postgres connection — proves the actual SQL, not a mock |
| T-73-08 | Tampering | migration-backfill.test.ts running against the wrong database | high | mitigate | Reuses 73-01's host-guarded tests/helpers/reimbursement-test-db.ts unchanged — no new connection path introduced |
| T-73-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No new package dependencies this plan |

</threat_model>

<verification>
- `./node_modules/.bin/vitest run tests/reimbursement-invariant.test.ts` — 8/8 green.
- With `yarn db:up`: `./node_modules/.bin/vitest run tests/reimbursement-regression.test.ts tests/migration-backfill.test.ts` — all 5 regression scenarios + backfill row-count/orphan/zero-row checks green.
- Full suite (`./node_modules/.bin/vitest run`) stays green with and without Docker running.
</verification>

<success_criteria>
- D-02 (RMB-03) has a concrete, reusable, unit-tested enforcement point.
- The regression proof covers dinner (N=3), both adjacency directions, refund-order determinism,
  and the Q3 multi-transaction-Expense tie-break — not just N=1.
- Migration backfill is proven correct at K=5 rows and at 0 rows, with zero orphans.
</success_criteria>

<output>
Create `.planning/phases/73-reimbursement-schema-and-netting/73-02-SUMMARY.md` when done
</output>
