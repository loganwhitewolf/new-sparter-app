---
phase: 73-reimbursement-schema-and-netting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - drizzle/migrations/00XX_reimbursement_schema.sql
  - drizzle/migrations/00XX_reimbursement_backfill.sql
  - lib/dal/transaction-pairs-sql.ts
  - tests/dashboard-dal.test.ts
  - tests/helpers/reimbursement-test-db.ts
  - tests/fixtures/reimbursement-seed.ts
  - tests/reimbursement-regression.test.ts
autonomous: false
requirements: [RMB-01, RMB-04, RMB-05]

must_haves:
  truths:
    - "reimbursement + reimbursement_refund tables exist with expenseId XOR expenseGroupId enforced by a DB CHECK constraint (D-03)"
    - "effectiveAmount()/isNotSecondary() read only reimbursement/reimbursement_refund — no production code path nets against transaction_pair anymore (D-05, D-06)"
    - "A migrated N=1 pair (Amazon order/refund case) produces byte-identical entrate/uscite/per-category totals before and after migration, compared via Decimal.js, not string equality (D-07)"
    - { statement: "A reimbursement anchor with zero linked refunds: effectiveAmount() returns the anchor's own raw amount unchanged, and the anchor transaction is never excluded from any aggregation (empty-input probe, RMB-04)", verification: covered }
    - { statement: "The migrated N=1 case reproduces today's 1:1 pairing numbers exactly — the former transaction_pair pair is a strict special case of the new model, not a reinterpretation", verification: covered }
  artifacts:
    - lib/db/schema.ts
    - drizzle/migrations (schema-creation migration + backfill migration)
    - lib/dal/transaction-pairs-sql.ts
    - tests/helpers/reimbursement-test-db.ts
    - tests/fixtures/reimbursement-seed.ts
    - tests/reimbursement-regression.test.ts
  key_links:
    - "effectiveAmount()/isNotSecondary() are consumed unchanged by lib/dal/dashboard.ts, lib/dal/overview.ts, lib/dal/tags.ts — generalizing the shared fragment fixes all 8 call sites without touching those files"
    - "backfill migration: reimbursement.expenseId <- transaction_pair primary's expense_id; reimbursement_refund.transactionId <- transaction_pair.transaction_b_id"
  prohibitions:
    - { statement: "A reimbursement must never be creatable or persisted with a non-negative (inflow-signed) anchor amount or a non-positive (outflow-signed) refund amount", verification: unverified, flagged: true }
    - { statement: "transaction_pair must never remain a second live write path once reimbursement/reimbursement_refund exist — no code path may insert new rows into transaction_pair after this phase ships", verification: unverified, flagged: true }
    - { statement: "Dashboard, per-category, and tag totals must never be allowed to silently diverge between pre- and post-migration snapshots — any numeric difference fails the regression test loudly, never rounded or tolerated", verification: unverified, flagged: true }
    - { statement: "The regression test harness must never connect to, seed, or truncate any database other than the explicitly-designated local throwaway test target — never the app's ambient DATABASE_URL, never staging or production", verification: unverified, flagged: true }
---

<objective>
Generalize the 1:1 `transaction_pair` model into the locked 1:N reimbursement schema (ADR 0018,
D-01/D-03), migrate existing pairs into it, generalize the shared `effectiveAmount()` /
`isNotSecondary()` netting fragment to read the new tables (D-05/D-06), and prove — with a real,
runnable regression test against a real Postgres — that the narrowest possible case (one migrated
1:1 pair, N=1) produces byte-identical dashboard totals before and after. This is this phase's
tracer: every layer the phase touches (schema, migration, shared netting SQL, aggregation call
sites) gets wired end-to-end for the thinnest slice before Plan 73-02 expands the proof matrix.

Purpose: prove the architecture holds before investing in the full N-refund matrix, the downstream
consumer repoints, and the live write-path repoint — exactly the risk ADR 0018 and 73-RESEARCH.md
flag as "the sharp edge" of this phase.
Output: `reimbursement` + `reimbursement_refund` tables, a migration that backfills every existing
`transaction_pair` row, a generalized `effectiveAmount()`/`isNotSecondary()`, and a regression
harness proving N=1 correctness.

**Flagged assumptions carried from the spec-less probe fallback** (RMB-01, RMB-03, RMB-05 came back
`unclassified` from the deterministic probe — per protocol they are surfaced here explicitly, not
silently resolved): this plan's `must_haves.truths` and Plan 73-02's invariant tests are this
project's concrete answer to those three, sourced from ADR 0018 D-01/D-02/D-06 directly rather than
from the generic probe. RMB-04's three classified probes (empty/adjacency/ordering) are split
across this plan (empty/N=1) and Plan 73-02 (adjacency, ordering, dinner N=3, Q3 multi-transaction
Expense).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

## Artifacts This Phase Produces

Phase-wide inventory (all 4 plans, 73-01 through 73-04) — every new symbol/table/file this phase
introduces, plus every existing production file it modifies in place.

**New DB tables** (lib/db/schema.ts, Plan 73-01): `reimbursement`, `reimbursement_refund`.

**New exported functions:**
- `lib/services/reimbursement-invariant.ts` (Plan 73-02, NEW file): `assertOutflowAnchorAmount`,
  `assertInflowRefundAmount`, `assertReimbursementAmounts`.
- `lib/dal/transaction-pairs-sql.ts` (Plan 73-01, MODIFIED): `effectiveAmount()`,
  `isNotSecondary()` — names/signatures unchanged, implementation rewritten.
- `tests/helpers/reimbursement-test-db.ts` (Plan 73-01, NEW file): connect/migrate/truncate helpers
  for the real-Postgres regression harness.
- `tests/fixtures/reimbursement-seed.ts` (Plans 73-01/73-02, NEW file): `seedUser`,
  `seedMinimalTaxonomy`, `seedExpenseWithTransaction`, `seedLegacyPair`, `seedReimbursement`.

**New file paths:**
- `lib/services/reimbursement-invariant.ts`
- `tests/helpers/reimbursement-test-db.ts`
- `tests/fixtures/reimbursement-seed.ts`
- `tests/reimbursement-regression.test.ts`
- `tests/reimbursement-invariant.test.ts`
- `tests/migration-backfill.test.ts`

**New migration filenames** (drizzle/migrations/, auto-named at generate time):
- Schema migration (Plan 73-01): `CREATE TABLE reimbursement` / `CREATE TABLE reimbursement_refund`
  with the XOR CHECK + partial unique indexes.
- Backfill migration (Plan 73-01): hand-authored, migrates every `transaction_pair` row.
- Optional drop migration (Plan 73-04, Task 3): only if the Task 1 checkpoint selects option-b.

**Existing production files modified in place** (no new exports, behavior repointed):
`lib/db/schema.ts` (73-01: new tables; 73-04 conditionally: drop `transactionPair`),
`lib/dal/transactions.ts` (73-03: `transactionListSelect`'s 5 `paired*` fields),
`lib/services/transaction-edit.ts` (73-03: the amount-edit pair guard),
`lib/services/transaction-pairs.ts` (73-04: `createPair`/`deletePairByTransactionId`),
`lib/dal/transaction-pairs.ts` (73-04: `getEligibleCounterparts`'s exclusion predicate).

**Existing test files modified in place:** `tests/dashboard-dal.test.ts` (73-01, fragment-contract
assertions), `tests/transactions-dal.test.ts` (73-03), `tests/transaction-edit.test.ts` (73-03),
`tests/transaction-pairs-service.test.ts` (73-04), `tests/transaction-pairs-dal.test.ts` (73-04).

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-reimbursement-schema-and-netting/73-CONTEXT.md
@docs/adr/0018-reimbursement-1n-linking-supersedes-net-by-subcategory.md
@lib/dal/transaction-pairs-sql.ts
@lib/db/schema.ts
@drizzle/migrations/0020_transaction_pair.sql
@drizzle/migrations/0022_wonderful_eternals.sql
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way migration and choose transaction_pair's post-migration fate</name>
  <decision>
    Task 2 of this plan runs a data migration that backfills every existing `transaction_pair`
    row into `reimbursement` + `reimbursement_refund`, then repoints the shared
    `effectiveAmount()`/`isNotSecondary()` netting fragment to read ONLY the new tables (D-06).
    After this runs, `transaction_pair` stops being the live netting source. Undoing this requires
    a hand-written reverse migration (re-deriving transaction_pair rows from reimbursement data)
    and reverting the SQL fragment — not a simple git revert, because real rows move.
  </decision>
  <context>
    D-06/D-07 (ADR 0018) require this migration to happen in this phase. CONTEXT.md's Claude's
    Discretion section explicitly leaves the table's POST-migration fate open: "whether
    transaction_pair is dropped in this phase or left as a dormant table." The actual DROP (if
    chosen) is scheduled for the LAST task of this phase (Plan 73-04, Task 3) — only once every
    remaining consumer (lib/dal/transactions.ts, lib/services/transaction-edit.ts,
    lib/services/transaction-pairs.ts, lib/dal/transaction-pairs.ts) has been repointed away from
    it, so the full test suite never goes red mid-phase from a dropped-but-still-referenced table.
    This checkpoint only needs to record the CHOICE now; Task 2 below does not drop anything.
  </context>
  <options>
    <option id="option-a">
      <name>Keep transaction_pair as a dormant table (recommended)</name>
      <pros>Fully reversible — the old rows remain inspectable/restorable; no second migration
      needed in this phase; lowest risk given this is explicitly flagged as the phase's sharp edge.</pros>
      <cons>One extra unused table lingers in the schema until a future cleanup phase explicitly
      removes it.</cons>
    </option>
    <option id="option-b">
      <name>Drop transaction_pair once every consumer is repointed (Plan 73-04, Task 3)</name>
      <pros>Cleaner final schema — exactly one netting mechanism, structurally enforced (D-01).</pros>
      <cons>One-way: once dropped, restoring old-shape data requires a DB backup/restore, not a
      code revert.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (keep dormant) or option-b (drop at phase end) — or "stop" to pause before any schema change</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: reimbursement + reimbursement_refund schema, migration, and netting generalization</name>
  <read_first>
    lib/dal/transaction-pairs-sql.ts (the exact effectiveAmount()/isNotSecondary() idiom to
    generalize), lib/db/schema.ts lines 379-522 (expense, transaction, transactionPair,
    expenseGroup, expenseGroupMembership — the FK/index/unique conventions to replicate),
    drizzle/migrations/0020_transaction_pair.sql (CREATE TABLE + FK + index style),
    drizzle/migrations/0022_wonderful_eternals.sql (hand-authored backfill-then-ALTER migration
    style — the precedent for a custom data-migration file), 73-RESEARCH.md "Answers to Open
    Questions" Q2/Q3.
  </read_first>
  <reversibility rating="one-way">D-06: this backfills real transaction_pair rows into
  reimbursement/reimbursement_refund and repoints the live netting source. Undo requires a
  hand-written reverse migration, not a code revert (gated by Task 1's checkpoint).</reversibility>
  <files>lib/db/schema.ts, drizzle/migrations/00XX_reimbursement_schema.sql, drizzle/migrations/00XX_reimbursement_backfill.sql, lib/dal/transaction-pairs-sql.ts</files>
  <action>
    Schema (D-03): add `reimbursement` pgTable — id serial PK; userId text NOT NULL references
    user.id ON DELETE CASCADE; title text NOT NULL; expenseId text NULL references expense.id ON
    DELETE CASCADE; expenseGroupId integer NULL references expenseGroup.id ON DELETE CASCADE;
    createdAt timestamp with tz NOT NULL default now(). Add a table-level CHECK constraint named
    `reimbursement_anchor_xor` via drizzle-orm/pg-core's `check()` builder (import it alongside the
    existing pg-core imports) asserting `(expense_id IS NOT NULL) <> (expense_group_id IS NOT
    NULL)` — the XOR from D-03. Add indexes on userId, expenseId, expenseGroupId, PLUS two partial
    unique indexes via `uniqueIndex(...).on(...).where(sql...)`: one on expenseId WHERE expense_id
    IS NOT NULL, one on expenseGroupId WHERE expense_group_id IS NOT NULL — each anchor (Expense or
    Expense Group) may have at most one reimbursement, mirroring transactionPair's old
    at-most-once-per-side uniqueness. Add `reimbursementRefund` pgTable ("reimbursement_refund") —
    id serial PK; reimbursementId integer NOT NULL references reimbursement.id ON DELETE CASCADE;
    transactionId text NOT NULL references transaction.id ON DELETE CASCADE; createdAt timestamp
    with tz NOT NULL default now(). Add a plain unique constraint on transactionId (a transaction
    refunds at most one reimbursement — generalizes transactionPair's transaction_b_id uniqueness)
    plus indexes on reimbursementId and transactionId. Add `reimbursementRelations` /
    `reimbursementRefundRelations` mirroring the existing `transactionPairRelations` /
    `expenseGroupRelations` style (one user, one expense, one expenseGroup, many refunds; refund ->
    one reimbursement, one transaction).

    Migration 1 (schema): run `yarn db:generate` to produce the CREATE TABLE migration from the
    schema diff.

    Migration 2 (backfill, custom): scaffold a second migration file (follow the
    0022_wonderful_eternals.sql precedent — a hand-authored data migration, not auto-generated) and
    write: for every row in `transaction_pair`, INSERT one `reimbursement` row with expenseId =
    the primary transaction's (transaction_a_id's) expense_id and title = that expense's own title
    (COALESCE-safe, expense.title is NOT NULL so a direct copy is safe); INSERT one
    `reimbursement_refund` row per migrated pair with reimbursementId = the newly-created
    reimbursement's id and transactionId = transaction_b_id. Do NOT drop or alter `transaction_pair`
    in this migration regardless of Task 1's checkpoint choice — see Task 1 rationale on timing.
    Apply both migrations against the local `DATABASE_URL` via `yarn db:migrate` (never
    `drizzle-kit push`).

    Netting generalization (D-05): rewrite `isNotSecondary()` in
    lib/dal/transaction-pairs-sql.ts to `NOT EXISTS (SELECT 1 FROM reimbursement_refund rr WHERE
    rr.transaction_id = transaction.id)` — exported name/signature unchanged so every existing call
    site (dashboard.ts, overview.ts, tags.ts) picks it up transparently. Rewrite
    `effectiveAmount()`: a transaction T is the ANCHOR of a reimbursement when a `reimbursement`
    row's expense_id equals T's expense_id AND T is the earliest transaction of that expense
    (`ORDER BY occurred_at ASC, id ASC LIMIT 1` — this is the concrete, tested resolution of Q3
    since D-03's anchor FK is Expense-level, not transaction-level, and netting must still resolve
    to one specific transaction; document this tie-break in a code comment citing Q3). When T is
    the anchor: effectiveAmount = T.amount + COALESCE(SUM of every linked refund transaction's
    amount for that reimbursement, 0). Otherwise: T.amount unchanged. Add a code comment noting the
    expense_group_id branch is intentionally NOT netted here — no code path creates
    expenseGroupId-anchored rows until Phase 74 (RMB-02), so this is a documented, not silent, gap.
    Keep the "always apply together" doc comment linking effectiveAmount()/isNotSecondary().

    Update the two Phase-50 SQL-fragment-contract assertions in tests/dashboard-dal.test.ts
    (`isNotSecondary() SQL fragment` / `effectiveAmount() SQL fragment`, currently asserting the
    generated text matches `/transaction_pair|tp\.transaction_b_id/` and
    `/case|when|exists|transaction_pair/`) to instead assert the text now references
    `reimbursement_refund`/`reimbursement` — these are RED against the old code the moment
    effectiveAmount()/isNotSecondary() change, and must go GREEN against the new fragment text in
    this same task.
  </action>
  <verify>
    <automated>yarn db:generate exits 0 and emits a migration containing CREATE TABLE "reimbursement" and CREATE TABLE "reimbursement_refund"; yarn db:migrate exits 0 against local DATABASE_URL; ./node_modules/.bin/vitest run exits 0 (dashboard-dal.test.ts fragment-contract tests pass against the new SQL text)</automated>
  </verify>
  <done>reimbursement + reimbursement_refund tables exist in the local dev database with the XOR
  CHECK and partial-unique constraints; every existing transaction_pair row has a corresponding
  reimbursement + reimbursement_refund row; effectiveAmount()/isNotSecondary() read only the new
  tables; the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: N=1 regression proof — real-Postgres harness, byte-identical before/after</name>
  <read_first>
    tests/dashboard-dal.test.ts (mocking conventions for reference — this task's harness
    deliberately does NOT follow that mock pattern, see action), lib/dal/dashboard.ts
    getOverviewAmountTotals (~line 458) and getCategoriesBreakdown (~line 973) for the exact query
    shape the harness must call, docker-compose.yml (the local Postgres container this harness
    targets), .env.example (documents DATABASE_URL default = the docker-compose Postgres),
    package.json `db:up`/`db:down` scripts.
  </read_first>
  <precondition>local Docker Postgres reachable via `yarn db:up` (docker-compose's `sparter-postgres`
  service) — this task's test suite skips gracefully with a console warning if unreachable rather
  than failing the whole-suite run, see action</precondition>
  <files>tests/helpers/reimbursement-test-db.ts, tests/fixtures/reimbursement-seed.ts, tests/reimbursement-regression.test.ts</files>
  <action>
    Create tests/helpers/reimbursement-test-db.ts: resolve a connection string from
    `process.env.TEST_DATABASE_URL`, defaulting to the exact docker-compose URL
    `postgres://postgres:sparter@localhost:5432/sparter`. Hard-guard: refuse to connect (throw) if
    the resolved host is not `localhost`/`127.0.0.1` — this harness must never be pointable at a
    real dev/staging/production database, even via env override. Attempt connection with a short
    timeout (~1.5s); on failure return `{ ok: false }` so the test file below can skip (not fail)
    with a clear "run `yarn db:up`" console message — this keeps `./node_modules/.bin/vitest run`
    green in sandboxes without Docker while still running the real proof on the developer's
    machine. On success, programmatically run drizzle-orm's migrator against
    `drizzle/migrations` (idempotent, safe to re-run) so the harness works against a completely
    fresh container. Export a `resetReimbursementFixtures(db)` helper that TRUNCATEs (CASCADE) the
    tables this harness seeds: user, direction, nature, category, sub_category, platform,
    import_format_version, file, expense, expense_group, expense_group_membership, transaction,
    transaction_pair, reimbursement, reimbursement_refund.

    Create tests/fixtures/reimbursement-seed.ts: exported builder functions — `seedUser`,
    `seedMinimalTaxonomy` (one 'out'-direction essential category/subcategory pair, one
    'in'-direction income category/subcategory pair, matching the direction/nature schema at
    lib/db/schema.ts lines 221-251), `seedExpenseWithTransaction({ userId, subCategoryId, amount,
    occurredAt })` returning `{ expenseId, transactionId }`, `seedLegacyPair({ primaryTransactionId,
    secondaryTransactionId })` inserting directly into `transaction_pair` (old-shape data, for the
    "before" snapshot). All monetary inputs/outputs go through toDecimal()/toDbDecimal() from
    @/lib/utils/decimal — never a raw JS string concatenation of an amount.

    Create tests/reimbursement-regression.test.ts: `beforeAll` connects via the helper above;
    `describe.skip` the whole suite with a console warning if unreachable. `beforeEach` calls
    `resetReimbursementFixtures`. One scenario: the Amazon order/refund (N=1) — seed an outflow
    Expense/transaction (e.g. -100.00) and an inflow transaction (+50.00, unlinked), seed a legacy
    `transaction_pair` row linking them (primary = the outflow, matching D-10's sign-based
    resolution, not the old magnitude rule). Capture the "before" snapshot by calling
    getOverviewAmountTotals and getCategoriesBreakdown directly against this seeded state using a
    FROZEN, inline copy of the pre-Phase-73 effectiveAmount()/isNotSecondary() SQL (a literal `sql`
    template referencing transaction_pair, written once in this test file and labeled "frozen
    regression baseline — do not update when production code changes") — this proves what "before"
    meant without requiring the old production code to still exist. Then call the migration
    backfill logic against this same seeded row (either by invoking the actual migration SQL file's
    statements, or by calling a small exported migration-support function if Task 2 created one —
    read Task 2's migration file to confirm) to populate reimbursement/reimbursement_refund. Then
    call the REAL, current getOverviewAmountTotals/getCategoriesBreakdown (importing from
    lib/dal/dashboard.ts normally) for the "after" snapshot. Assert every compared total is equal
    via toDecimal(before).equals(toDecimal(after)) — never string `===`. Also assert the
    zero-refund case directly against effectiveAmount(): an anchor Expense with a reimbursement row
    but zero reimbursement_refund rows returns its own raw amount and is not excluded from
    aggregation (the empty-input probe).
  </action>
  <verify>
    <automated>yarn db:up && ./node_modules/.bin/vitest run tests/reimbursement-regression.test.ts exits 0 with the N=1 and empty-refund assertions passing (not skipped); ./node_modules/.bin/vitest run exits 0 for the full suite when Docker is unavailable (graceful skip, no false failure)</automated>
  </verify>
  <done>Running with `yarn db:up` active, the regression test proves the migrated N=1 case and the
  zero-refund case produce identical dashboard totals to the frozen pre-migration baseline, using
  Decimal.js comparison throughout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| service layer -> reimbursement/reimbursement_refund | No userId column on either table (mirrors transaction_pair) — ownership is only enforceable by joining through transaction.userId/expense.userId |
| test harness -> Postgres | A new automated test process that connects to and TRUNCATEs a real Postgres database — the only place in this suite that does so |
| migration SQL -> stored data | Backfill runs against real, possibly-production-shaped local data; a mistake here changes stored rows, not just code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-73-01 | Tampering | Migration backfill (Task 2) | high | mitigate | Backfill only INSERTs derived rows from existing transaction_pair data (no destructive UPDATE/DELETE on transaction/expense); Task 3's regression harness independently reconciles row counts and totals before/after |
| T-73-02 | Repudiation / silent-corruption | effectiveAmount()/isNotSecondary() rewrite | high | mitigate | Task 3's real-Postgres regression proof (Decimal.js comparison, never string equality) is the acceptance gate; a divergence fails loudly, not silently |
| T-73-03 | Tampering | Regression test harness accidentally targeting a non-throwaway database | high | mitigate | Hard host-guard (localhost/127.0.0.1 only) in tests/helpers/reimbursement-test-db.ts; explicit TEST_DATABASE_URL, never inherits the app's ambient DATABASE_URL |
| T-73-04 | Information Disclosure / Elevation of Privilege | reimbursement/reimbursement_refund (no userId column) | high | mitigate | Deferred to Plan 73-04 (the only phase-73 code path that WRITES these tables via user input); this plan only writes via a migration script, not user-facing input — no IDOR surface yet, but flagged so 73-04 does not skip the ownership check |
| T-73-05 | Tampering (supply chain) | npm/pip/cargo package installs | n/a | accept | No new package dependencies introduced this plan — schema/migration/SQL changes only, using already-installed drizzle-orm/drizzle-kit |

</threat_model>

<verification>
- `yarn db:generate` produces a migration containing `CREATE TABLE "reimbursement"` and `CREATE TABLE "reimbursement_refund"` with the XOR CHECK constraint text visible in the generated SQL.
- `yarn db:migrate` applies cleanly against the local dev database with zero errors.
- `./node_modules/.bin/vitest run` — full suite green (baseline 139 files / 1727 tests, minus any intentionally-updated assertions, plus new tests from Task 3).
- With `yarn db:up` running: `./node_modules/.bin/vitest run tests/reimbursement-regression.test.ts` passes with real (non-skipped) assertions proving N=1 and empty-refund correctness.
</verification>

<success_criteria>
- reimbursement + reimbursement_refund exist with the D-03 XOR shape.
- Every existing transaction_pair row has been backfilled into the new tables.
- effectiveAmount()/isNotSecondary() read only the new tables.
- A real-Postgres regression test proves the N=1 and empty-refund cases are numerically identical
  to the pre-migration baseline.
- transaction_pair still exists, unmodified in row content, pending Plan 73-04's finalization per
  Task 1's checkpoint choice.
</success_criteria>

<output>
Create `.planning/phases/73-reimbursement-schema-and-netting/73-01-SUMMARY.md` when done
</output>
