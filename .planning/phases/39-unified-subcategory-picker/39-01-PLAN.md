---
phase: 39-unified-subcategory-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/ui/drawer.tsx
  - lib/dal/subcategory-usage.ts
  - lib/categorization/subcategory-options.ts
  - tests/subcategory-usage-dal.test.ts
  - tests/subcategory-options.test.ts
autonomous: false
requirements: [R-UP-04, R-UP-08]
user_setup: []

must_haves:
  truths:
    - "vaul is installed and a shadcn Drawer primitive exists in components/ui/drawer.tsx"
    - "A DAL query returns the user's top ~6 most-used subcategories scoped to allowed category types, empty when the user has no categorized expenses"
    - "buildCategoryOptions / filterCategoryOptions exist in a categorization-owned module independent of the soon-to-be-deleted category-combobox"
  artifacts:
    - path: "components/ui/drawer.tsx"
      provides: "vaul-based Drawer primitive (Drawer, DrawerContent, DrawerTrigger, etc.)"
      contains: "vaul"
    - path: "lib/dal/subcategory-usage.ts"
      provides: "getMostUsedSubcategories(allowedTypes) DAL query"
      exports: ["getMostUsedSubcategories"]
    - path: "lib/categorization/subcategory-options.ts"
      provides: "buildCategoryOptions, filterCategoryOptions, CategoryOption type with categoryType"
      exports: ["buildCategoryOptions", "filterCategoryOptions"]
  key_links:
    - from: "lib/dal/subcategory-usage.ts"
      to: "expense table grouped by subCategoryId"
      via: "drizzle groupBy count scoped to userId + allowed category types"
      pattern: "groupBy|group by"
---

<objective>
Lay the foundation for the unified subcategory picker: add the `vaul` dependency and a shadcn Drawer primitive (the real bottom-sheet container per D-02/R-UP-08), add the "most used" DAL query (D-06/R-UP-04), and extract the pure option helpers (`buildCategoryOptions`/`filterCategoryOptions`) into a categorization-owned module so the picker can consume them without depending on `category-combobox.tsx` (which is deleted in 39-05).

Purpose: Every downstream plan (component build, adoption, cleanup) depends on these primitives. Isolating them in Wave 1 keeps the component plan focused on layout.
Output: `vaul` in package.json, `components/ui/drawer.tsx`, `lib/dal/subcategory-usage.ts`, `lib/categorization/subcategory-options.ts`, plus unit tests for the DAL query and the pure helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-unified-subcategory-picker/39-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify vaul package legitimacy before install</name>
  <read_first>
    - .planning/phases/39-unified-subcategory-picker/39-CONTEXT.md (D-02 mandates vaul)
    - package.json (confirm vaul not already present)
  </read_first>
  <what-built>No code yet — this gate confirms the `vaul` npm package is legitimate before it is added as a dependency. RESEARCH.md has no Package Legitimacy Audit for this phase, so `vaul` is treated as [ASSUMED] and must be human-verified.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vaul
    2. Confirm it is the Emil Kowalski drawer library for React (maintainer `emilkowalski`, repo github.com/emilkowalski/vaul), widely used (millions of weekly downloads), and is the package shadcn/ui's Drawer is built on.
    3. Confirm the latest version is compatible with the React 19 / Next.js 16 stack in package.json.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms `vaul` on npmjs.com is the expected drawer library, not a typosquat.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add vaul + shadcn Drawer primitive</name>
  <files>package.json, components/ui/drawer.tsx</files>
  <read_first>
    - package.json (dependency versions, yarn is the package manager)
    - components/ui/dialog.tsx (existing shadcn primitive conventions: cn usage, data-slot/forwardRef style, exports)
    - app/(app)/prototype/subcategory-picker/_prototype.tsx (the throwaway BottomSheet shows the target shape: fixed height ~80vh mobile / ~600px desktop, bottom-docked on all viewports, constrained desktop width centred)
  </read_first>
  <action>
    Add `vaul` via `yarn add vaul` (per D-02/R-UP-08; the prototype faked the sheet, the real build uses vaul). Create `components/ui/drawer.tsx` as the standard shadcn Drawer wrapper around vaul's `Drawer` namespace: export `Drawer`, `DrawerTrigger`, `DrawerPortal`, `DrawerOverlay`, `DrawerContent`, `DrawerHeader`, `DrawerFooter`, `DrawerTitle`, `DrawerDescription`, `DrawerClose`. Match the existing shadcn primitive style used in `dialog.tsx` (use `cn`, `data-slot` attributes, the project's class conventions). `DrawerContent` must support a fixed-height, bottom-anchored sheet that does NOT grow/shrink with content — the height is set by the consumer; only inner regions scroll. Keep all developer-facing strings/comments in English (Italian only later in the product-facing picker copy). Do not wire it to any surface yet.
  </action>
  <verify>
    <automated>yarn tsc --noEmit 2>&1 | grep -v node_modules | head; grep -q "vaul" package.json &amp;&amp; grep -q "vaul" components/ui/drawer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `vaul` appears in package.json dependencies.
    - `components/ui/drawer.tsx` imports from `vaul` and re-exports the Drawer primitive set.
    - `yarn tsc --noEmit` reports no new type errors from drawer.tsx.
  </acceptance_criteria>
  <done>vaul installed; Drawer primitive available for the picker component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract pure option helpers + add most-used DAL query</name>
  <files>lib/categorization/subcategory-options.ts, lib/dal/subcategory-usage.ts, tests/subcategory-options.test.ts, tests/subcategory-usage-dal.test.ts</files>
  <read_first>
    - components/expenses/category-combobox.tsx (current buildCategoryOptions/filterCategoryOptions/CategoryOption — copy logic, add categoryType to the option)
    - lib/dal/categories.ts (CategoryWithSubCategories shape: type, isOwned, subCategories[].isOwned/name/originalName/slug)
    - lib/dal/expenses.ts (DAL conventions: server-only, verifySession, db query style, scoping by userId)
    - tests/categories-dal.test.ts (DAL unit-test mocking pattern: vi.hoisted db mock, selectResults queue)
    - lib/db/schema.ts (expense table: userId, subCategoryId, status; subCategory.categoryId; category.type)
    - docs/adr/0008-pattern-amount-sign-derived-from-subcategory.md (category type semantics)
  </read_first>
  <behavior>
    - buildCategoryOptions: flattens CategoryWithSubCategories[] to options, optional allowedCategoryTypes filter, and now carries `categoryType` on each option (needed for type-chip filtering in the picker per R-UP-02). Empty categories produce no options.
    - filterCategoryOptions: case-insensitive match across label, originalName, categoryName, slug; empty query returns all.
    - getMostUsedSubcategories(allowedTypes): returns up to 6 subcategory ids (with name + categoryName) ordered by per-user count of expenses currently assigned to that subcategory, scoped to the calling user and to categories whose `type` is in allowedTypes; returns [] when the user has no categorized expenses (cold-start / onboarding) per D-06.
  </behavior>
  <action>
    Create `lib/categorization/subcategory-options.ts` exporting `CategoryOption` (add field `categoryType: CategoryWithSubCategories['type']`), `buildCategoryOptions`, `filterCategoryOptions` — port the existing logic from `category-combobox.tsx`, adding `categoryType` to each built option. Do NOT yet modify or delete `category-combobox.tsx` (39-05 handles that).
    Create `lib/dal/subcategory-usage.ts` (`server-only`) exporting `getMostUsedSubcategories(allowedTypes: Array<CategoryWithSubCategories['type']>)`: call `verifySession`, query `expense` joined to `subCategory`/`category`, group by `subCategory.id`, count, filter `expense.userId = session.userId` and `category.type IN allowedTypes`, order by count desc, limit 6, return `{ subCategoryId, name, categoryName }[]`. Per D-06 this is global-per-user (no contextual/per-amount weighting) and hidden-when-empty (return empty array, the component decides not to render). Decimal/native-arithmetic rules N/A (counting rows). Keep dev-facing code/comments English.
    Write `tests/subcategory-options.test.ts` (pure helpers: build with/without allowedTypes including categoryType assertions; filter case-insensitivity and empty-query passthrough) and `tests/subcategory-usage-dal.test.ts` (mock db per categories-dal.test.ts pattern: assert userId scoping, allowedTypes filter on category.type, limit 6, empty-result passthrough).
  </action>
  <verify>
    <automated>yarn vitest run tests/subcategory-options.test.ts tests/subcategory-usage-dal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - `buildCategoryOptions` output objects include `categoryType`.
    - `getMostUsedSubcategories` returns [] for a user with no categorized expenses and caps results at 6.
    - `category-combobox.tsx` is unchanged.
  </acceptance_criteria>
  <done>Pure helpers and the most-used DAL query exist with green unit tests; ready for the picker component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→DAL (getMostUsedSubcategories) | userId comes from server session, never client input |
| npm install (vaul) | new third-party dependency enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information disclosure | getMostUsedSubcategories | mitigate | Scope every query to `session.userId` via verifySession; never accept a userId argument from the client |
| T-39-02 | Spoofing | allowedTypes filter | mitigate | Filter on `category.type IN allowedTypes` server-side; the result only contains subcategories visible to the user (existing category visibility join) |
| T-39-SC | Tampering | npm install (vaul) | mitigate | Blocking human checkpoint (Task 1) verifies vaul legitimacy on npmjs.com before install |
</threat_model>

<verification>
- `yarn vitest run tests/subcategory-options.test.ts tests/subcategory-usage-dal.test.ts` green.
- `yarn tsc --noEmit` shows no new errors.
- `grep vaul package.json` and `grep vaul components/ui/drawer.tsx` both match.
</verification>

<success_criteria>
vaul + Drawer primitive available; most-used DAL query and pure option helpers exist with passing tests; no existing surface modified.
</success_criteria>

<output>
Create `.planning/phases/39-unified-subcategory-picker/39-01-SUMMARY.md` when done.
</output>
