---
phase: 39-unified-subcategory-picker
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - components/categorization/subcategory-picker.tsx
requirements: [R-UP-01, R-UP-02, R-UP-03, R-UP-08]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A single shared SubcategoryPicker opens as a fixed-height bottom sheet on every viewport and returns one subCategoryId on tap"
    - "Type chips (Entrate/Uscite/Trasferimenti) filter the rail, are preselected from a passed default sign, and exclude system (still reachable under Tutte/search)"
    - "Two-column master-detail: left rail = Più usate + categories of active type; right pane = subcategory tiles; mobile drills left→right with back; search collapses to a flat tile list"
    - "The sheet height does not change when switching type or filtering — only inner lists scroll"
  artifacts:
    - path: "components/categorization/subcategory-picker.tsx"
      provides: "SubcategoryPicker client component (variant E productionized over vaul Drawer)"
      exports: ["SubcategoryPicker"]
      min_lines: 120
  key_links:
    - from: "components/categorization/subcategory-picker.tsx"
      to: "components/ui/drawer.tsx"
      via: "import vaul-based Drawer for the bottom sheet container"
      pattern: "from '@/components/ui/drawer'"
    - from: "components/categorization/subcategory-picker.tsx"
      to: "lib/categorization/subcategory-options.ts"
      via: "buildCategoryOptions/filterCategoryOptions for option building + search"
      pattern: "subcategory-options"
---

<objective>
Build the single reusable `SubcategoryPicker` — the productionized winning prototype variant E — over the real `vaul` Drawer. This is the heart of the phase (D-01, D-02, D-03, D-04, D-05, R-UP-01/02/03/08): a fixed-height bottom sheet on all viewports with type chips, "Più usate", a two-column master-detail with D-style tiles, mobile drill-in, and search-collapse. It outputs exactly one `subCategoryId` via `onChange`; the caller decides commit-vs-fill (D-07, wired in 39-03/39-04).

Purpose: Replace three divergent selection UIs with one. Every adoption surface in 39-03/39-04 consumes this component's contract.
Output: `components/categorization/subcategory-picker.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-unified-subcategory-picker/39-CONTEXT.md
@CLAUDE.md
@app/(app)/prototype/subcategory-picker/_prototype.tsx
@app/(app)/prototype/subcategory-picker/NOTES.md
@lib/categorization/subcategory-options.ts
@lib/dal/subcategory-usage.ts
@components/ui/drawer.tsx
@lib/dal/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the SubcategoryPicker contract + container</name>
  <files>components/categorization/subcategory-picker.tsx</files>
  <read_first>
    - app/(app)/prototype/subcategory-picker/_prototype.tsx (VariantE, TypeChips, Tile, RailItem, ColList, SearchInput, Chip, Empty, BottomSheet, SheetHeader — the reference layout)
    - app/(app)/prototype/subcategory-picker/NOTES.md (decisions #4, #5, #7, #8 and the variant E verdict)
    - .planning/phases/39-unified-subcategory-picker/39-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-07; Claude's Discretion on props shape)
    - components/ui/drawer.tsx (the vaul Drawer primitive — fixed-height bottom-sheet container)
    - lib/categorization/subcategory-options.ts (CategoryOption with categoryType, buildCategoryOptions, filterCategoryOptions)
    - lib/dal/categories.ts (CategoryWithSubCategories)
  </read_first>
  <action>
    Create `components/categorization/subcategory-picker.tsx` (`'use client'`). Define and export the props contract (planner's call per Claude's Discretion):
    `SubcategoryPicker({ open, onOpenChange, categories, mostUsed, allowedCategoryTypes, defaultType, onChange })` where
    `categories: CategoryWithSubCategories[]`,
    `mostUsed: Array<{ subCategoryId: number; name: string; categoryName: string }>` (already type-scoped by the caller via the DAL; empty = hide "Più usate" content per D-06),
    `allowedCategoryTypes: Array<CategoryWithSubCategories['type']>` (the call site's allowed types — categorize sites pass all four `['in','out','transfer','system']`),
    `defaultType: 'in' | 'out' | 'transfer' | null` (preselected type chip from the row's amount sign per D-03; `null` → "Tutte"),
    `onChange: (subCategoryId: string) => void` (tap = select + close + return; the caller commits or fills per D-07).
    Build the container with the `vaul` Drawer from `components/ui/drawer.tsx`: bottom-anchored on all viewports, constrained + centred width on desktop, **fixed height** (~80vh mobile / ~600px desktop) that never grows/shrinks on type switch or filter — only inner columns scroll (`min-h-0 overflow-y-auto`) per D-02/R-UP-08. Header title "Categorizza" with a close control. Do not implement the list internals in this task beyond an empty body placeholder; keep it compiling. Product-facing copy is Italian (domain surface); code/comments English.
  </action>
  <verify>
    <automated>yarn tsc --noEmit 2>&1 | grep -v node_modules | grep "subcategory-picker" | head; test -f components/categorization/subcategory-picker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports `SubcategoryPicker` with the props above.
    - The container imports the Drawer from `@/components/ui/drawer` and sets a fixed height (literal `80vh` and `600px` or equivalent tokens), not a content-sized `max-h`.
    - `yarn tsc --noEmit` shows no errors originating in subcategory-picker.tsx.
  </acceptance_criteria>
  <done>Picker contract + fixed-height vaul sheet container compile; ready for the variant E body.</done>
</task>

<task type="auto">
  <name>Task 2: Implement variant E body (D-04) — type chips, two-column master-detail, search-collapse</name>
  <files>components/categorization/subcategory-picker.tsx</files>
  <read_first>
    - components/categorization/subcategory-picker.tsx (the contract + container from Task 1)
    - app/(app)/prototype/subcategory-picker/_prototype.tsx (VariantE + Tile/RailItem/ColList/TypeChips/SearchInput/Empty — port these)
    - app/(app)/prototype/subcategory-picker/NOTES.md (decision #5 type-chip-only filter, #8 row content rules)
    - lib/categorization/subcategory-options.ts (filterCategoryOptions for search; CategoryOption.categoryType for type filtering)
  </read_first>
  <action>
    Port variant E into the picker body (D-04 two-column master-detail layout). Top region: `SearchInput` + `TypeChips` (Tutte / Entrate `in` / Uscite `out` / Trasferimenti `transfer`) — **no system chip** (D-03); system subcategories stay reachable under "Tutte" and via search. The active type chip initializes from `defaultType`; changing it resets the active rail item to "Più usate". Body: when search is non-empty, collapse both columns into a flat list of `Tile`s filtered by `filterCategoryOptions` then by active type. When search is empty, render `grid sm:grid-cols-[190px_1fr]`: left rail = "⭐ Più usate" (first item, always present) + categories of the active type with subcategory counts; right pane = D-style `Tile`s for the active rail item (Più usate → the `mostUsed` entries; a category → its subcategories). Row/tile content per D-05: subcategory name + (for Più usate) category group, and a "Personale" badge when the option `isOwned` — **no FlowNature badge**. Mobile: hide the left rail when a rail item is active, hide the right pane at root, show a "‹ Categorie" back control (port from variant E). On tap of a subcategory tile, call `onChange(String(subCategoryId))` then `onOpenChange(false)` (select + close + return per D-07). The mostUsed entries map to tiles by `subCategoryId` — resolve display via the built options or the mostUsed name/categoryName directly. Build options once via `buildCategoryOptions(categories, allowedCategoryTypes)`. Keep product copy Italian, code English.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The component renders search + TypeChips with exactly Tutte/Entrate/Uscite/Trasferimenti (no Sistema chip).
    - Source contains a two-column grid (`sm:grid-cols-[190px_1fr]`) and a flat-list branch gated on non-empty search.
    - Tapping a subcategory tile invokes `onChange` with the subcategory id string and closes the sheet.
    - "Personale" badge rendered for owned options; no FlowNature/NATURE_LABELS reference in the file.
    - `yarn build` exits 0.
  </acceptance_criteria>
  <done>SubcategoryPicker is a complete, productionized variant E ready for adoption across surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker→caller (onChange) | the picker returns a subCategoryId string; the caller is responsible for server-side authorization on commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering | onChange subCategoryId | accept | Picker is presentation only; the subCategoryId is re-validated server-side by the existing categorize/create actions (isSubCategoryVisibleToUser / Zod). No new trust boundary introduced here. |
| T-39-04 | Information disclosure | categories/mostUsed props | mitigate | Both are already user-scoped (getCategories / getMostUsedSubcategories run under verifySession); the picker renders only what the server supplied. |
</threat_model>

<verification>
- `yarn build` exits 0.
- `grep -n "grid-cols-\[190px_1fr\]" components/categorization/subcategory-picker.tsx` matches.
- No `NATURE_LABELS` import in the picker.
</verification>

<success_criteria>
One reusable variant-E SubcategoryPicker exists: fixed-height bottom sheet, type chips preselected by sign, two-column master-detail with tiles, search-collapse, mobile drill, single-subCategoryId output.
</success_criteria>

<output>
Create `.planning/phases/39-unified-subcategory-picker/39-02-SUMMARY.md` when done.
</output>
