---
phase: 61-standalone-expense
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - components/transactions/detach-expense-dialog.tsx
  - components/transactions/transaction-table.tsx
autonomous: false
requirements: [STEXP-01, STEXP-02]

must_haves:
  truths:
    - "On ANY transaction in the categorization flow, the user can invoke a 'treat as a standalone expense / do not aggregate' action — the action is general, not gated on transaction count and not a counterparty category (STEXP-01)"
    - "The standalone action captures a title AND a subcategory in one step and detaches the transaction into a dedicated expense with that subcategory (STEXP-01)"
    - "The action works even when the transaction's expense holds only that one transaction — the previous expenseTransactionCount > 1 UI gate is lifted (STEXP-02)"
    - "After a successful standalone detach, the row reflects the new dedicated expense and its chosen category without a full reload"
  artifacts:
    - "components/transactions/detach-expense-dialog.tsx — title + subcategory capture, forwards subCategoryId to detachTransaction"
    - "components/transactions/transaction-table.tsx — standalone action available on any transaction; optimistic row update carries the chosen subcategory"
  key_links:
    - "detach-expense-dialog → detachTransaction action (61-01) → detachTransactionToDedicatedExpense — the subCategoryId must flow through unchanged"
    - "SubcategoryPicker (components/categorization/subcategory-picker.tsx) is the subcategory-capture surface — reuse, do not build a new picker"
    - "transaction-table markExpenseDetached / markExpenseCategorized — optimistic list update must reflect the standalone expense + its category"
---

<objective>
Surface the standalone-expense affordance inline in the categorization flow and capture the subcategory in one step, on ANY transaction. This is the UI slice of Phase 61: a general "treat as a standalone expense / do not aggregate" action (STEXP-01) that is no longer gated on the source expense having more than one transaction (STEXP-02), wiring the title + subcategory into the subcategory-aware detach action delivered by Plan 01.

Purpose: Make the isolation capability reachable where the user categorizes — a shared-subscription reimbursement (or any ambiguous person-to-person inflow) can be peeled out of the sender's `descriptionHash` aggregate and categorized on its own, capturing both a human title and the subcategory without a second step.

Output: An inline standalone action + a title-and-subcategory capture dialog wired to the Plan 01 action, with the transaction-count UI gate removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-standalone-expense/61-CONTEXT.md
@docs/adr/0016-shared-costs-net-by-subcategory-inflows-isolated-per-transaction.md
@CLAUDE.md

# Plan 01 output (read the SUMMARY for the exact action/service signatures)
@.planning/phases/61-standalone-expense/61-01-SUMMARY.md

# Existing UI this plan extends (read before editing)
@components/transactions/detach-expense-dialog.tsx
@components/transactions/transaction-table.tsx
@components/expenses/expense-categorize-dialog.tsx
@components/categorization/subcategory-picker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add subcategory capture to the standalone-expense dialog (title + subcategory in one step)</name>
  <files>components/transactions/detach-expense-dialog.tsx</files>
  <read_first>
    - components/transactions/detach-expense-dialog.tsx (current title-only dialog; calls detachTransaction({ transactionId, title }); onSuccess returns { newExpenseId, newExpenseTitle })
    - components/expenses/expense-categorize-dialog.tsx (canonical pattern for driving SubcategoryPicker: onChange(subCategoryId) → startTransition → server action → toast → onSuccess)
    - components/categorization/subcategory-picker.tsx (SubcategoryPickerProps: open, onOpenChange, categories, mostUsed, allowedCategoryTypes, defaultType, onChange, pending)
    - .planning/phases/61-standalone-expense/61-01-SUMMARY.md (the detachTransaction action now accepts subCategoryId)
  </read_first>
  <action>
    Rework `DetachExpenseDialog` so it captures BOTH a title and a subcategory before detaching, in one flow, and forwards `subCategoryId` to `detachTransaction`.

    Recommended two-step-in-one-dialog shape (choose the cleanest fit for the existing components — this is a Claude's-discretion UI detail per CONTEXT.md): keep the existing title `Input`, then present a "pick subcategory" affordance that opens the reused `SubcategoryPicker` (Sheet). On subcategory selection, call `detachTransaction({ transactionId, title: trimmedTitle, subCategoryId })`, show the success toast, and call `onSuccess` with `{ newExpenseId, newExpenseTitle, subCategoryId }` (extend the onSuccess payload to include the chosen subCategoryId so the table can render the category optimistically).

    Add `categories` and `mostUsed` props to the dialog (typed as in `expense-categorize-dialog.tsx`) so it can render the picker. Reuse the SubcategoryPicker with `allowedCategoryTypes={['in','out','transfer','allocation']}` and `defaultType={null}`. Do NOT build a new picker.

    Preserve the empty-title guard (toast + block) and the pending state. Keep the copy user-facing Italian; make the title/description read as "one-off / do not aggregate" (e.g. title captures the standalone name, helper text explains it will not aggregate with other transactions from the same description). Dev-facing identifiers/comments stay English.

    Update the `Props.onSuccess` type to `(result: { newExpenseId: string; newExpenseTitle: string; subCategoryId?: number }) => void`.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn check:language</automated>
  </verify>
  <done>
    The dialog captures a title and a subcategory (via the reused SubcategoryPicker) and forwards subCategoryId to detachTransaction; onSuccess carries the chosen subCategoryId; types compile; language check passes.
  </done>
  <acceptance_criteria>
    - `DetachExpenseDialog` renders the reused `SubcategoryPicker` (no new picker component).
    - It calls `detachTransaction({ transactionId, title, subCategoryId })` with the chosen subcategory.
    - `onSuccess` payload includes `subCategoryId`.
    - `yarn tsc --noEmit` and `yarn check:language` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make the standalone action general (any transaction) and update the optimistic row</name>
  <files>components/transactions/transaction-table.tsx</files>
  <read_first>
    - components/transactions/transaction-table.tsx (the dropdown menu "Separa in spesa dedicata" item currently gated by `transaction.expenseId && (transaction.expenseTransactionCount ?? 0) > 1`; the detachTarget state; the DetachExpenseDialog render at the bottom; markExpenseDetached and markExpensesCategorized helpers)
    - components/transactions/detach-expense-dialog.tsx (updated onSuccess payload from Task 1)
  </read_first>
  <action>
    Lift the transaction-count gate (STEXP-02): change the standalone menu-item condition from `transaction.expenseId && (transaction.expenseTransactionCount ?? 0) > 1` to `transaction.expenseId` (available on ANY transaction that has a linked expense, regardless of count). Keep the action out of the menu only when there is no linked expense (nothing to detach). Relabel the action so it reads as "treat as a standalone expense / do not aggregate" rather than a mechanical "split" — user-facing Italian copy; keep the `Split` icon or choose a clearer one. The action stays general — do NOT scope it to a counterparty/person.

    Pass `categories` and `mostUsed` (already props of `TransactionTable`) into `DetachExpenseDialog`.

    Update the `DetachExpenseDialog` `onSuccess` handler to consume the new `subCategoryId`: call `markExpenseDetached(...)` for the new expense, and when `subCategoryId` is present, immediately reflect the category on the row (reuse `markExpenseCategorized` / `markExpensesCategorized` with the new expense id and the chosen subCategoryId) instead of re-opening a separate categorize dialog. Remove the old behavior that opened `ExpenseCategorizeDialog` as a mandatory second step after detach — the subcategory is now captured in the standalone dialog. (The standalone row should land already categorized.)

    Do NOT change aggregation, netting, or dashboard code. Do NOT add a persisted per-sender rule.
  </action>
  <verify>
    <automated>yarn tsc --noEmit && yarn vitest run --dir tests 2>/dev/null || yarn vitest run</automated>
  </verify>
  <done>
    The standalone action is offered on any transaction with a linked expense (single- or multi-transaction), captures title + subcategory in one dialog, and the row updates optimistically to the categorized standalone expense; the suite is GREEN.
  </done>
  <acceptance_criteria>
    - The standalone menu item is gated only on `transaction.expenseId` (the `> 1` count gate is removed).
    - `categories`/`mostUsed` are passed to `DetachExpenseDialog`.
    - On success the row shows the new standalone expense already carrying the chosen subcategory (no mandatory second categorize step).
    - `yarn tsc --noEmit` passes and the vitest suite is GREEN.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — standalone action on any transaction</name>
  <action>Human verifies the inline standalone-expense action (see how-to-verify steps below).</action>
  <what-built>
    An inline "standalone expense / do not aggregate" action on any transaction in the categorization flow (Transazioni table row menu), capturing a title and a subcategory in one dialog and detaching the transaction into a dedicated, already-categorized expense — including when the source expense holds only that single transaction.
  </what-built>
  <how-to-verify>
    1. Run the app: `yarn dev` and open the Transazioni page (the transactions table).
    2. Find a transaction whose linked expense holds MULTIPLE transactions (same bank description as others). Open its row menu → invoke the standalone action. Confirm you are asked for a title AND a subcategory, then confirm. Expected: the row now shows a dedicated expense with the chosen category; the other same-description transactions are unaffected.
    3. Find a transaction whose linked expense holds ONLY that one transaction. Open its row menu → the standalone action must be present (previously it was hidden). Invoke it, pick a subcategory, confirm. Expected: it succeeds (no "cannot separate the only transaction" error), the same expense row is re-hashed in place, and the row shows the chosen category.
    4. Confirm the action reads as "do not aggregate / one-off", not as a "money from a person" category, and is available generally.
    5. (Isolation spot-check, optional) After isolating a transaction, importing/creating a later transaction with the SAME original bank description should produce its own fresh uncategorized expense — it must NOT merge into the standalone one.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. missing on single-tx rows, subcategory not captured, wrong copy)</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `DetachExpenseDialog` — now captures title + subcategory (reuses `SubcategoryPicker`), forwards `subCategoryId` to `detachTransaction`, `onSuccess` payload gains `subCategoryId`; new `categories`/`mostUsed` props.
- `transaction-table.tsx` — standalone action available on any transaction with a linked expense (count gate removed); optimistic row update reflects the chosen category; no mandatory second categorize step.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (dialog) → server action `detachTransaction` | Untrusted `transactionId`, `title`, `subCategoryId`; validated + session-scoped server-side (Plan 01) |
| picker selection → dialog state | Client-only selection; the authoritative ownership/visibility checks live server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-04 | Elevation of Privilege / IDOR | `detachTransaction` invoked from the dialog | high | mitigate | The action (Plan 01) calls `verifySession()` and the service scopes every read/write by `userId`; the client cannot detach a transaction it does not own. UI passes only ids; server re-validates. |
| T-61-05 | Tampering | client-chosen `subCategoryId` | medium | mitigate | Server-side Zod (positive integer, Plan 01) plus the FK constraint; the picker only lists user-visible subcategories. A forged foreign id fails validation/FK. |
| T-61-06 | Information Disclosure | optimistic row render after detach | low | accept | The optimistic update mirrors only data the user already supplied (title, chosen subcategory) and the returned expense id; no cross-user data is exposed. |
| T-61-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package installs (edits to existing components only). No `[ASSUMED]`/`[SUS]` packages; slopcheck N/A. |
</threat_model>

<verification>
- `yarn tsc --noEmit` — types compile with the new dialog props and onSuccess payload.
- `yarn vitest run` — existing suite GREEN (no regressions in transaction/table/detach tests).
- `yarn check:language` — no non-English dev-facing strings introduced.
- Human checkpoint (Task 3) — the inline standalone action works on both single- and multi-transaction expenses and captures title + subcategory in one step.
</verification>

<success_criteria>
- A general "standalone expense / do not aggregate" action is reachable on any transaction with a linked expense in the categorization flow.
- The action captures a title and a subcategory in one step and produces a dedicated, already-categorized expense.
- The single-transaction case works (no guard error); the row updates without a full reload.
- No aggregation/netting/dashboard code touched; no per-sender rule persisted.
</success_criteria>

<output>
Create `.planning/phases/61-standalone-expense/61-02-SUMMARY.md` when done. Record the final placement/copy chosen for the affordance (Claude's-discretion resolution) and the onSuccess payload shape.
</output>
