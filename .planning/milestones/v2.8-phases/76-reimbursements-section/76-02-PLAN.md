---
phase: 76-reimbursements-section
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - lib/utils/table-config.ts
  - lib/utils/reimbursements-table-config.ts
  - components/reimbursements/reimbursement-table.tsx
  - app/(app)/reimbursements/page.tsx
autonomous: true
requirements: [RMB-10]

must_haves:
  truths:
    - "A user with exactly one reimbursement sees exactly one correct row in the table (RMB-10 empty/single-element boundary)."
    - "The list defaults to anchor-date descending (D-02); clicking the Titolo/Data/Netto column headers cycles DESC -> ASC -> off (default) per the shared HeaderSortButton convention, applied client-side over the full in-memory row set."
    - "The list table renders exactly the five columns Titolo (title with anchor-description fallback per D-03), Anchor, Netto, Residuo/stato, and Data (D-04), defined by REIMBURSEMENTS_TABLE_CONFIG within the unified-table pattern."
    - "The status filter (owed/settled/surplus) and the free-text search (title or anchor title substring, case-insensitive) narrow the SAME in-memory row set the page already fetched — no second server round-trip."
    - "When the account has zero reimbursements, the page shows the no-data EmptyState without mounting the toolbar; when filters exclude every row, the table shows a no-result EmptyState instead (RMB-10 empty, both branches)."
    - "A reimbursement whose residual is exactly 0.00 always renders the 'Saldato' badge (never 'Dovuti'/'Surplus') in the list — the boundary is exact (RMB-10 adjacency)."
  artifacts:
    - lib/utils/reimbursements-table-config.ts exports REIMBURSEMENTS_TABLE_CONFIG
    - components/reimbursements/reimbursement-table.tsx exports ReimbursementTable
  key_links:
    - "ReimbursementTable -> DataTableToolbar/useToolbarSort/useSearchParams: the SAME unified toolbar/sort system Transactions/Expenses use, reused rather than a bespoke filter UI (RMB-10 'unified table + RSC scaffolding')."
    - "ReimbursementTable's status filter -> row.state: filtering compares the EXACT state string ('owed'|'settled'|'surplus') the Plan 76-01 DAL already derived via computeReimbursementResidual's underlying logic — never a re-derived threshold."
  prohibitions:
    - "Must never recompute residual/net using separate arithmetic instead of the value already returned by getReimbursementList (row.residual/row.state) — filtering/sorting reads those fields as-is."
---

<objective>
Turn Plan 76-01's plain tracer list into the full RMB-10 experience: search, a status filter
(owed/settled/surplus, D-11), and sortable columns (title/anchor date/net, D-02 default anchor-date
desc), reusing the SAME `DataTableToolbar`/`useToolbarSort` unified system Transactions/Expenses
already use.

Purpose: give the "canonical, complete list" (D-01 — all reimbursements, volume managed by
filter/sort, never by hiding rows) a real, navigable, filterable presentation.

Output: `REIMBURSEMENTS_TABLE_CONFIG` + `ReimbursementTable`, wired into `/reimbursements`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-reimbursements-section/76-CONTEXT.md
@.planning/phases/76-reimbursements-section/76-PATTERNS.md
@lib/utils/table-config.ts
@components/data-table/DataTableToolbar.tsx
@components/data-table/HeaderSortButton.tsx
@components/data-table/EmptyState.tsx
@app/(app)/expenses/expenses.table.ts
@lib/dal/reimbursement.ts
@lib/utils/reimbursement-format.ts
@app/(app)/reimbursements/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table config + ReimbursementTable client component</name>
  <files>lib/utils/table-config.ts, lib/utils/reimbursements-table-config.ts, components/reimbursements/reimbursement-table.tsx</files>
  <read_first>
    lib/utils/table-config.ts (the `TableConfig.id` union — currently `'transactions' | 'expenses' |
    'files'`, must widen to include `'reimbursements'`; the `FilterField`/`SortColumn` shapes);
    components/data-table/DataTableToolbar.tsx (the `status` FilterFieldType branch — renders a
    Select with `field.options` when provided, defaulting to the categorized/uncategorized pair
    otherwise; `useToolbarSort(route)`'s returned `{activeSort, activeDir, onSort, isRestoring}`);
    components/data-table/HeaderSortButton.tsx (exact `Props` shape: `column: SortColumn`,
    `activeSort`, `activeDir`, `align`, `className`, `onSort`); app/(app)/expenses/expenses.table.ts
    (a real `TableConfig` object for the exact shape to mirror); tests/reimbursement-panel.test.ts's
    precedent — this repo has no jsdom, so component render/interaction is not directly unit-tested;
    the pure formatting helpers this task introduces ARE.
  </read_first>
  <action>
    In lib/utils/table-config.ts: widen `TableConfig['id']` to
    `'transactions' | 'expenses' | 'files' | 'reimbursements'` — this is the only change to this file.

    Create lib/utils/reimbursements-table-config.ts exporting `REIMBURSEMENTS_TABLE_CONFIG: TableConfig`:
    `id: 'reimbursements'`, `search: { key: 'q', placeholder: 'Cerca per titolo o ancora…' }`,
    `filters: [{ key: 'status', label: 'Stato', type: 'status', options: [{value:'owed',label:'Dovuti'},
    {value:'settled',label:'Saldato'},{value:'surplus',label:'Surplus'}], toChip: (v) => ({owed:'Dovuti',
    settled:'Saldato',surplus:'Surplus'}[v] ?? v) }]` (D-11), `sortable: [{key:'anchorDate',label:'Data'},
    {key:'title',label:'Titolo'},{key:'residual',label:'Netto'}]`, `defaultSort: {key:'anchorDate',dir:'desc'}`
    (D-02).

    Create components/reimbursements/reimbursement-table.tsx (`'use client'`) exporting
    `ReimbursementTable({ reimbursements, route }: { reimbursements: ReimbursementListRow[]; route:
    string })`. Import `ReimbursementListRow`/`ReimbursementResidualState` (type-only) from
    `@/lib/dal/reimbursement`, `formatResidualBadgeLabel`/`residualBadgeClassName` from
    `@/lib/utils/reimbursement-format`, `reimbursementHref`/`expenseDetailHref` from `@/lib/routes`,
    `useToolbarSort`/`DataTableToolbar` from `@/components/data-table/DataTableToolbar`,
    `HeaderSortButton` from `@/components/data-table/HeaderSortButton`, `EmptyState` from
    `@/components/data-table/EmptyState`, `REIMBURSEMENTS_TABLE_CONFIG` from
    `@/lib/utils/reimbursements-table-config`, `useSearchParams` from `next/navigation`,
    `amountToneClass` from `@/lib/utils/amount-tone`. Read `q`/`status` directly via `useSearchParams()`
    (the component's own read access — separate from `useToolbarSort`'s write-only `onSort`, since
    filtering/sorting here narrows the ALREADY-FETCHED `reimbursements` prop in-memory, not a fresh
    server fetch, per D-01's "canonical, complete list, volume managed by filter/sort" and this
    repo's tags-detail precedent of client-side array filtering). Filter: `q` matches
    `row.displayTitle` OR `row.anchorTitle` (lowercase substring); `status` matches `row.state`
    exactly. Sort: derive from `useToolbarSort(route)`'s `activeSort`/`activeDir`, falling back to
    `REIMBURSEMENTS_TABLE_CONFIG.defaultSort` when unset; sort keys map to `row.anchorDate` (Date
    compare), `row.displayTitle` (localeCompare), `row.residual` (`toDecimal` compare) — implement as
    a small local pure `sortReimbursementRows(rows, sort, dir)` function (unit-testable without
    jsdom, mirrors the `computeMergeEligibility`-as-standalone-export precedent in
    components/expenses/expense-table.tsx). Render `<DataTableToolbar config=
    {REIMBURSEMENTS_TABLE_CONFIG} route={route} />` then, if the FILTERED array is empty while
    `reimbursements` (the raw prop) is non-empty, `<EmptyState variant="no-result" .../>`; otherwise a
    `Table` with columns Titolo (HeaderSortButton key `title`, cell = Link to
    `reimbursementHref(row.id)` showing `row.displayTitle`), Ancora (plain `TableHead`, cell = Link to
    `expenseDetailHref(row.anchorExpenseId)` showing `row.anchorTitle`), Netto (HeaderSortButton key
    `residual`, align right, cell = signed amount via `amountToneClass(row.residual)` +
    `formatAbsoluteAmount`-with-sign — write a small local `formatSignedAmount(value: string): string`
    using `Intl.NumberFormat('it-IT', {style:'currency',currency:'EUR'})` over `Number(value)`, the
    SAME local-helper pattern `components/tags/tag-detail-report.tsx` already uses rather than a new
    shared util), Stato (Badge with `className={residualBadgeClassName(row.state)}`, text =
    `formatResidualBadgeLabel(row.residual, row.state)`), Data (HeaderSortButton key `anchorDate`,
    cell = `row.anchorDate.toLocaleDateString('it-IT')`).
  </action>
  <verify>
    <automated>yarn vitest run tests/reimbursement-table-sort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Export `sortReimbursementRows` from reimbursement-table.tsx (or a co-located pure module) so it
      is directly unit-testable; create tests/reimbursement-table-sort.test.ts asserting: sorting by
      `residual` ascending places a `-100.00` row before a `20.00` row (Decimal comparison, not string
      comparison — a naive string sort would misorder `'-100.00'` vs `'20.00'`); sorting by
      `anchorDate` descending with two equal dates preserves array order (stable sort, matching
      `Array.prototype.sort`'s spec-guaranteed stability — no bespoke tie-break needed here since the
      DAL already delivered a deterministic order upstream); `formatResidualBadgeLabel('0.00',
      'settled')` returns exactly `'Saldato'` (RMB-10 adjacency: the exact-zero boundary), never a
      string containing "Dovuti" or "Surplus".
    - `grep -c "id: 'reimbursements'" lib/utils/reimbursements-table-config.ts` is 1.
    - `lib/utils/table-config.ts`'s `TableConfig['id']` union includes `'reimbursements'` (verify via
      `yarn tsc --noEmit` passing with no type errors on the new config file's `id` field).
  </acceptance_criteria>
  <done>REIMBURSEMENTS_TABLE_CONFIG and ReimbursementTable exist, compile, and their pure sort/format helpers are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ReimbursementTable into the /reimbursements page</name>
  <files>app/(app)/reimbursements/page.tsx</files>
  <read_first>
    app/(app)/reimbursements/page.tsx (Plan 76-01's tracer render — the inline Table markup this task
    replaces); app/(app)/expenses/page.tsx (the exact split: EmptyState('no-data') when the raw
    fetched array is empty, otherwise mount the interactive table component).
  </read_first>
  <action>
    Replace the tracer's inline `Table`/`TableRow` markup in app/(app)/reimbursements/page.tsx with:
    if `reimbursements.length === 0`, render `<EmptyState variant="no-data" message="Nessun rimborso"
    hint="Non hai ancora nessun rimborso collegato. Collega un rimborso dalla pagina di una spesa in
    uscita per vederlo qui." />` (RMB-10 empty — this is the ACCOUNT-level empty state, distinct from
    Task 1's filtered-to-zero `no-result` case, which lives inside ReimbursementTable itself);
    otherwise render `<ReimbursementTable reimbursements={reimbursements} route=
    {APP_ROUTES.reimbursements} />`. Import `ReimbursementTable` from
    `@/components/reimbursements/reimbursement-table` and `APP_ROUTES` from `@/lib/routes`.
  </action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/(app)/reimbursements/page.tsx` no longer contains an inline `TableHeader` block
      (`grep -c "TableHeader" "app/(app)/reimbursements/page.tsx"` is 0) — rendering is fully
      delegated to `ReimbursementTable`.
    - `grep -c "ReimbursementTable" "app/(app)/reimbursements/page.tsx"` is at least 1.
  </acceptance_criteria>
  <done>The page renders EmptyState for a zero-reimbursement account, or the full interactive ReimbursementTable otherwise; `yarn tsc --noEmit` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser URL params (`q`, `status`, `sort`, `dir`) -> ReimbursementTable | untrusted, user-controlled query-string values read via `useSearchParams()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-06 | Tampering | ReimbursementTable's `status`/`q` params | low | accept | These params only narrow an ALREADY user-scoped, already-fetched array (`reimbursements`, IDOR-safe per Plan 76-01) — no value they can take ever changes which rows the server sent; worst case is an empty filtered view, never data disclosure. |
</threat_model>

<verification>
`yarn vitest run tests/reimbursement-table-sort.test.ts` and `yarn tsc --noEmit`. Manually confirm
search/filter/sort interactions in a running dev server as part of the Plan 76-06 checkpoint.
</verification>

<success_criteria>
- The list supports search (title/anchor), a status filter, and sortable Titolo/Netto/Data columns,
  all operating over the already-fetched row set with no extra server round-trip.
- Zero-reimbursement and filtered-to-zero states each show the correct, distinct EmptyState variant.
</success_criteria>

<output>
Create `.planning/phases/76-reimbursements-section/76-02-SUMMARY.md` when done.
</output>
