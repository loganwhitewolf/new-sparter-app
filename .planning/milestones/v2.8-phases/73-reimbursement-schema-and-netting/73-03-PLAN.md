---
phase: 73-reimbursement-schema-and-netting
plan: 03
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - lib/dal/transactions.ts
  - tests/transactions-dal.test.ts
  - lib/services/transaction-edit.ts
  - tests/transaction-edit.test.ts
autonomous: true
requirements: [RMB-04, RMB-05]

must_haves:
  truths:
    - "lib/dal/transactions.ts's 5 paired-* fields (pairedWithId, pairedNetAmount, pairedAmount, pairedDescription, pairedOccurredAt) read reimbursement/reimbursement_refund, not transaction_pair — the transaction-list pair popover keeps working for every migrated pair with zero visible change (gate surface #4, 73-VALIDATION.md)"
    - { statement: "Ordering — when a reimbursement has N>1 refunds, pairedWithId/pairedAmount/pairedDescription/pairedOccurredAt pick a deterministic single counterpart (earliest-linked refund by reimbursement_refund.created_at, then transactionId) instead of an arbitrary LIMIT 1 row — today's LIMIT 1 over transaction_a_id/transaction_b_id was 1:1-safe but not meaningfully ordered; the new query is explicitly ordered", verification: covered }
    - "pairedNetAmount reflects the FULL reimbursement net (anchor amount + SUM of every linked refund), shown identically whether the viewed row is the anchor or any one of its refunds — generalizing the old symmetric single-counterpart net"
    - "The amount-edit pair guard in lib/services/transaction-edit.ts fires against reimbursement/reimbursement_refund, not transaction_pair — an edit that would flip the reimbursement's opposite-sign invariant is still blocked with 'Scollega prima il rimborso' after migration (closes the orchestrator-flagged risk: the guard silently going dead while dashboard totals stay correct)"
  artifacts:
    - lib/dal/transactions.ts (rewritten paired-* correlated subqueries)
    - lib/services/transaction-edit.ts (rewritten guard)
  key_links:
    - "transactionListSelect.paired* -> components/transactions/transaction-table.tsx, components/transactions/transaction-detail-client.tsx (existing UI consumers, unchanged field names/types)"
    - "updateTransaction's guard -> reimbursement_refund SUM (generalization of the old single-counterpart oppositeSign check)"
---

<objective>
Repoint the two downstream consumers the 73-RESEARCH.md "Inventory Correction" found reading
`transaction_pair` directly (bypassing the `effectiveAmount()`/`isNotSecondary()` helpers Plan
73-01 already generalized): `lib/dal/transactions.ts`'s 5 raw-SQL `paired*` correlated subqueries
feeding the transaction-list pair popover, and `lib/services/transaction-edit.ts`'s amount-edit
pair guard. Both are existing, live, user-facing behavior today — this is not new UI, it is keeping
existing UI and an existing safety guard correct against the new schema.

Purpose: close the two gaps the orchestrator explicitly flagged as invisible to a totals-only
regression gate — a stale `LIMIT 1` popover showing wrong/no data, and a safety guard that goes
silently dead (still compiles, never throws) the moment `transaction_pair` stops being written to.
Output: `lib/dal/transactions.ts` paired-* fields and `lib/services/transaction-edit.ts`'s guard
both read the new reimbursement tables; gate surfaces #4 and #5 from 73-VALIDATION.md are closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-reimbursement-schema-and-netting/73-CONTEXT.md
@lib/dal/transaction-pairs-sql.ts
.planning/phases/73-reimbursement-schema-and-netting/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint transactions.ts paired-* correlated subqueries</name>
  <read_first>
    lib/dal/transactions.ts lines 80-163 (transactionListSelect — the exact 5 fields and their
    current transaction_pair-based CASE/LIMIT-1 shape to replace), lib/dal/transaction-pairs-sql.ts
    (73-01's rewritten effectiveAmount()/isNotSecondary() — the anchor-resolution tie-break idiom
    — earliest transaction of the anchor Expense — to reuse here for "is THIS transaction the
    anchor"), tests/transactions-dal.test.ts lines 1220-1280 (the existing fragment-presence
    assertions for these 5 fields — update, do not delete).
  </read_first>
  <behavior>
    - A transaction that is a refund: pairedWithId resolves to its reimbursement's anchor
      transaction id (earliest transaction of the anchor's expenseId); pairedAmount/pairedDescription/
      pairedOccurredAt resolve to that anchor transaction's own fields.
    - A transaction that is an anchor: pairedWithId resolves to the earliest-LINKED refund
      (reimbursement_refund.created_at ASC, transactionId ASC) — deterministic even when N>1 exists.
    - pairedNetAmount for EITHER an anchor row or any one of its refund rows equals the anchor's own
      amount + SUM of every linked refund amount (the reimbursement's total net) — identical
      regardless of which participant row is being viewed.
    - An unpaired transaction: all 5 fields are null (unchanged from today).
  </behavior>
  <files>lib/dal/transactions.ts, tests/transactions-dal.test.ts</files>
  <action>
    Rewrite the 5 `sql` fragments in `transactionListSelect` (pairedWithId, pairedNetAmount,
    pairedAmount, pairedDescription, pairedOccurredAt) to read `reimbursement` +
    `reimbursement_refund` instead of `transaction_pair`, preserving field names and TypeScript
    types (`string | null` / `Date | null`) exactly so `TransactionListRow` and every consumer
    (components/transactions/transaction-table.tsx, transaction-detail-client.tsx,
    counterpart-picker-dialog.tsx) needs zero changes. Resolve "is this transaction id the anchor"
    via the same earliest-transaction-of-the-expense tie-break Plan 73-01 built into
    effectiveAmount() (reuse that exact SQL shape here rather than inventing a second tie-break
    rule). For an anchor row, resolve "the displayed counterpart" as the earliest-LINKED refund
    (`ORDER BY reimbursement_refund.created_at ASC, reimbursement_refund.transaction_id ASC LIMIT
    1`) — document in a code comment that this shows only ONE refund when N>1 exist (a documented,
    accepted display limitation; a proper multi-refund popover is Phase 75/76 scope, not silently
    dropped). pairedNetAmount computes the anchor's amount plus the SUM of every refund linked to
    that same reimbursement (not just the one displayed counterpart) so the number is always
    correct even though the popover only names one counterpart. Update the fragment-presence
    assertions at tests/transactions-dal.test.ts lines ~1220-1280 to match the new SQL text
    (reimbursement/reimbursement_refund references instead of transaction_pair).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/transactions-dal.test.ts</automated>
  </verify>
  <done>All 5 paired-* fields read the new tables; existing fragment-presence tests updated and
  green; TransactionListRow's shape is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint the amount-edit pair guard to reimbursement/reimbursement_refund</name>
  <read_first>
    lib/services/transaction-edit.ts lines 71-106 (the exact guard to generalize — its
    single-counterpart lookup and oppositeSign check), tests/transaction-edit.test.ts "DET-03 —
    pair guard" describe block (~line 249) — the 3 existing tests this task must keep green.
  </read_first>
  <behavior>
    - Editing a transaction that is NOT linked to any reimbursement (anchor or refund): guard does
      not run, edit proceeds (unchanged from today).
    - Editing a refund transaction: the "other side" sum = anchor amount + every OTHER refund's
      amount (excluding the one being edited). If the new amount and that sum are not opposite-sign,
      throw 'Scollega prima il rimborso' before any write.
    - Editing an anchor transaction: the "other side" sum = every linked refund's amount. Same
      opposite-sign check against the new anchor amount.
    - A reimbursement with zero linked refunds imposes no guard (nothing to protect — the empty
      case).
  </behavior>
  <files>lib/services/transaction-edit.ts, tests/transaction-edit.test.ts</files>
  <action>
    Replace the `transactionPair`-based lookup (querying transactionAId/transactionBId) in
    `updateTransaction` with a lookup against `reimbursement`/`reimbursement_refund`: determine
    whether `input.transactionId` is a refund (present in reimbursement_refund) or an anchor
    (matches a reimbursement's expenseId via the same earliest-transaction tie-break used
    elsewhere in this phase). Compute the "other side" sum via Decimal.js (toDecimal, summed with
    `.plus()`, never native `+`) over whichever set applies (all other refunds, or all refunds, per
    the behavior above). Keep the exact same opposite-sign check shape
    (`(newAmount.gt(0) && otherSum.lt(0)) || (newAmount.lt(0) && otherSum.gt(0))`) and the exact
    same Italian message `'Scollega prima il rimborso'` — this generalizes correctly for any N
    without a scope-reduced special case, since it is a straightforward SUM in place of a single
    lookup. Note in a code comment that RMB-09 (Phase 74) is about UX/reconciliation choices beyond
    this block (e.g. distinguishing WHICH refund broke the invariant), not about this correctness
    check, which already holds for any N.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/transaction-edit.test.ts</automated>
  </verify>
  <done>The 3 existing DET-03 pair-guard tests still pass after being repointed to
  reimbursement/reimbursement_refund fixtures instead of transactionPair fixtures; the guard fires
  identically for a migrated N=1 pair as it did pre-migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| transactionListSelect -> transaction-table UI | User-facing read path; a wrong/stale paired-* value is visible to the user directly, not just a background aggregate |
| updateTransaction guard -> stored amounts | The sole safety check preventing an amount edit from silently corrupting a reimbursement's net |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-73-09 | Repudiation / silent-corruption | transactions.ts paired-* fields left stale after migration | high | mitigate | Task 1 rewrites all 5 fields against the new tables in the same task the migration ships; regression coverage via tests/transactions-dal.test.ts fragment assertions |
| T-73-10 | Tampering | Amount-edit guard silently stops firing post-migration | high | mitigate | Task 2 repoints the guard in the same phase as the migration (not deferred to Phase 74), closing the exact gap the orchestrator flagged as undetectable by a totals-only gate |
| T-73-11 | Tampering | Ordering non-determinism in the pair popover (N>1, equal-amount refunds) | medium | mitigate | Explicit `ORDER BY created_at ASC, transaction_id ASC` tie-break, documented as a known single-counterpart display limitation pending Phase 75/76 |

</threat_model>

<verification>
- `./node_modules/.bin/vitest run tests/transactions-dal.test.ts` green — paired-* fields correct for anchor, refund, and unpaired cases.
- `./node_modules/.bin/vitest run tests/transaction-edit.test.ts` green — guard fires/does not fire identically to pre-migration behavior for N=0/1, and correctly for N>1 via the SUM generalization.
- Full suite green.
</verification>

<success_criteria>
- Gate surface #4 (transaction-list paired-* fields) and #5 (amount-edit guard) from
  73-VALIDATION.md are both closed by real code changes, not left reading a dormant table.
- No UI component's props/types changed — this is a backend repoint only.
</success_criteria>

<output>
Create `.planning/phases/73-reimbursement-schema-and-netting/73-03-SUMMARY.md` when done
</output>
