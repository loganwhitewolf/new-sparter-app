---
phase: 82-number-engine-and-regression-gate
plan: 03
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - lib/services/pace-and-projection.ts
  - tests/pace-and-projection.test.ts
autonomous: true
requirements: [PACE-02, PACE-04, PACE-05, PACE-06]

must_haves:
  truths:
    - "isPartialMonth classifies exactly the current calendar month as partial, regardless of which day of the month `today` is — a month whose data merely stopped earlier (e.g. no import since May, today is July) is never classified partial (D-03)."
    - "isPartialMonth is a pure function of (yearMonth, today) and never throws, including when the selected year has zero Covered Months at all."
    - "The composition that builds a year's displayed series evaluates isPartialMonth identically regardless of whether the underlying coveredMonths array is sorted ascending or descending — a stateless per-month predicate, not a stateful reducer."
    - "computeCurrentMonthHybrid returns exactly spentSoFar when it exceeds pace, exactly pace when pace exceeds spentSoFar, and exactly the shared value (not a distinguishable branch) at the exact tie — verified with fixtures at pace-1, pace, pace+1 cents (D-06, PACE-04 boundary)."
    - "Decimal.max() inside computeCurrentMonthHybrid operates on unrounded Decimal instances (never on a value already round-tripped through a JS number); the single result is rounded to cents via toDbDecimal only once, at the return boundary."
    - "The period total is computed as the literal sum of the already-built monthly series, never an independently derived formula — buildYearSeries's `total` field is the reduce-sum of its own `months` array, so any divergence between the two is structurally impossible, not merely tested for (D-07, PACE-05)."
    - "resolveComparisonJudgement returns 'neutral' when delta is exactly zero for every direction, 'worse' for a positive delta on 'out', 'better' for a positive delta on 'in'/'allocation' — a single shared function, never duplicated per widget (D-08, D-09)."
    - "resolveComparisonJudgement and computeComparison are pure and deterministic — identical inputs always yield identical outputs, with no dependency on Date.now(), Math.random(), or object key iteration order."
    - "computeComparison never throws for a zero-value previous period ('0.00'); the composition layer only invokes the previous-year total-difference comparison when canShowPreviousYearTotalDifference(previousYearCoveredMonthCount) is true."
    - "canShowPreviousYearTotalDifference gates only the total-difference figure at a threshold of 6 Covered Months in the previous year (PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS, exported so Phases 83/84 consume one constant) — the average-vs-average comparison always renders regardless of this gate (D-10)."
  artifacts:
    - "lib/services/pace-and-projection.ts (extended)"
    - "tests/pace-and-projection.test.ts (extended)"
  key_links:
    - "isPartialMonth(yearMonth, today) -> excludes the current month from the Covered Months array before it reaches computePaceAndProjection"
    - "computeCurrentMonthHybrid(spentSoFar, pace) -> the displayed value for the one month classified partial"
    - "buildYearSeries's months[] -> its own total (sum, never re-derived)"
    - "computeComparison + resolveComparisonJudgement -> the single shared sign/colour resolution point for Phases 83/84"
  prohibitions: []
---

<objective>
Complete the pace/projection engine's remaining decisions: Partial Month exclusion (D-03), the
hybrid current-month value (D-06), the total-equals-sum-of-series invariant (D-07), and the single
shared comparison/judgement function plus its previous-year coverage threshold (D-08/D-09/D-10).

Purpose: these are the last four locked decisions the engine must satisfy before Phase 83/84 can
consume it — all pure, Decimal.js-only computation, no DB, extending the module Plan 82-01 started.
Output: lib/services/pace-and-projection.ts carries the complete engine surface Phases 83/84 import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-number-engine-and-regression-gate/82-CONTEXT.md
@docs/adr/0020-categories-year-view-retires-deviation.md
@.planning/dashboard-categories-DECISIONS.md
@lib/services/pace-and-projection.ts
@lib/dal/overview.ts
@lib/utils/decimal.ts
</context>

## Artifacts this plan produces

- `lib/services/pace-and-projection.ts` (extended) — new exports: `isPartialMonth(yearMonth: string, today?: Date): boolean`; `computeCurrentMonthHybrid(spentSoFar: string, pace: string): string`; `buildYearSeries(input): { months: MonthlyValue[]; total: string }`; `computeComparison(current: string, previous: string): string`; `resolveComparisonJudgement(delta: string, direction: 'in' | 'out' | 'allocation'): 'better' | 'worse' | 'neutral'`; `PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS = 6`; `canShowPreviousYearTotalDifference(previousYearCoveredMonthCount: number): boolean`.
- `tests/pace-and-projection.test.ts` (extended) — new describe blocks for Partial Month, hybrid current month, total-sum invariant, and comparison/judgement.

## Flagged Assumptions (spec-less probe fallback)

- **PACE-05 (unclassified edge-probe row).** The edge-probe classifier returned `unclassified` for
  PACE-05 (the total-equals-sum-of-series invariant has no adjacency/boundary/empty/ordering shape
  to apply). Stays flagged per protocol rather than auto-resolved; substantive coverage is Task 2's
  `buildYearSeries` design (the total field is structurally the sum, not merely asserted to equal it)
  and its dedicated unit test. Flag for a human: if a future phase ever needs a *partial-window* total
  (e.g. Phase 84's 9/6/3-month window), re-confirm the same structural-sum property still holds for a
  window slice rather than only the full year — this plan only proves the full-year case.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partial Month classification + hybrid current-month value (D-03, D-06)</name>
  <files>lib/services/pace-and-projection.ts, tests/pace-and-projection.test.ts</files>
  <read_first>
    lib/services/pace-and-projection.ts (Plan 82-01's existing MonthlyValue/PaceResult/computePaceAndProjection/buildCoveredMonthSeries shapes to extend, not duplicate);
    lib/utils/decimal.ts (toDecimal/toDbDecimal; Decimal.max is the decimal.js library's static comparator, imported directly from the `decimal.js` package alongside these helpers);
    .planning/phases/82-number-engine-and-regression-gate/82-CONTEXT.md (D-03's exact operational definition and its two worked examples: "if today is 30 July and data ends on the 28th, July is partial"; "if data stops in May and today is July, May is not partial");
    CONTEXT.md's Example dialogue ("Siamo al 20 luglio, il Ritmo di Alimentari è €300 ma ha già speso €480... €480. Una stima non può stare sotto un fatto già accaduto").
  </read_first>
  <behavior>
    - isPartialMonth('2026-07', new Date(2026,6,30)) -> true (current calendar month, regardless of how late in the month `today` falls).
    - isPartialMonth('2026-05', new Date(2026,6,20)) -> false (data stopped in May, today is July — May is covered and concluded, not partial).
    - isPartialMonth('2026-06', new Date(2026,6,1)) -> false (June is the month BEFORE today's month, even on day 1 of July).
    - computeCurrentMonthHybrid('480.00', '300.00') -> '480.00' (spent exceeds pace).
    - computeCurrentMonthHybrid('200.00', '300.00') -> '300.00' (pace exceeds spent).
    - computeCurrentMonthHybrid('300.00', '300.00') -> '300.00' (exact tie — both branches agree, verified as the SAME value, not a distinguishable code path).
  </behavior>
  <action>
    Add to lib/services/pace-and-projection.ts: `export function isPartialMonth(yearMonth: string, today: Date = new Date()): boolean` — parse `yearMonth` ('YYYY-MM') into year/month numbers, compare against `today.getFullYear()`/`today.getMonth()+1` for exact equality; return true only on that exact match (D-03 — no other date arithmetic, no day-of-month comparison at all, since D-03 explicitly makes no presumption based on how much of the month has elapsed). Add `export function computeCurrentMonthHybrid(spentSoFar: string, pace: string): string` — `import Decimal from 'decimal.js'` alongside the existing `toDecimal`/`toDbDecimal` import, compute `Decimal.max(toDecimal(spentSoFar), toDecimal(pace))` on the two UNROUNDED Decimal instances, then return `toDbDecimal(...)` exactly once at the end (D-06/D-11 — never round each operand before comparing, and never round twice).

    Extend tests/pace-and-projection.test.ts with describe('Partial Month classification (PACE-02, D-03)') covering the three &lt;behavior&gt; cases above plus an ordering-independence case: build a small array of yearMonths in both ascending and descending order and assert `.filter(m =&gt; !isPartialMonth(m, today))` produces the same excluded-month result regardless of input order (closes the edge-probe's PACE-02 "ordering" probe — the predicate is stateless per-month, not order-sensitive). Add describe('Current month hybrid value (PACE-04, D-06)') covering the three computeCurrentMonthHybrid cases above, plus an assertion that the tie case ('300.00','300.00') returns the identical string both branches would independently produce (proving there is no silent divergent branch at the boundary).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/pace-and-projection.test.ts -t "Partial Month"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/pace-and-projection.ts` exports `isPartialMonth` and `computeCurrentMonthHybrid` with the signatures above.
    - `node_modules/.bin/vitest run tests/pace-and-projection.test.ts -t "Partial Month"` and `-t "hybrid"` both pass, including the exact-tie case.
    - `grep -c "toDbDecimal" lib/services/pace-and-projection.ts` shows `computeCurrentMonthHybrid`'s body calling it exactly once (single rounding point, not per-operand).
  </acceptance_criteria>
  <done>isPartialMonth correctly distinguishes "current month" from "stopped-importing month"; computeCurrentMonthHybrid never displays a value below an already-observed fact and rounds exactly once at the return boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Total = sum of series + shared comparison/judgement + previous-year threshold (D-07, D-08, D-09, D-10)</name>
  <files>lib/services/pace-and-projection.ts, tests/pace-and-projection.test.ts</files>
  <read_first>
    lib/dal/overview.ts (the existing `current − previous` convention already documented there — MonthOverMonthChange's `delta: string // signed Decimal string; negative = spent less (saved money)` — D-08 reuses this convention, it does not invent a new one);
    .planning/phases/82-number-engine-and-regression-gate/82-CONTEXT.md (D-07, D-08, D-09, D-10 — note D-10's proposed ≥6 Covered Months threshold and that it gates ONLY the total difference, never the average comparison);
    .planning/dashboard-categories-DECISIONS.md D13 (the exact "no sign in the UI, magnitude + word, colour per direction" rationale, useful context even though word/colour rendering itself is a Phase 83/84 UI concern, not built here).
  </read_first>
  <behavior>
    - buildYearSeries given 3 MonthlyValue entries ('100.00','200.00','150.50') -> { months: [...], total: '450.50' }, where `total` is literally `months.reduce((s,m)=&gt;s.plus(toDecimal(m.amount)), toDecimal('0'))`, never a separately computed figure.
    - computeComparison('380.00', '200.00') -> '180.00'; computeComparison('200.00', '380.00') -> '-180.00' (current − previous, D-08).
    - resolveComparisonJudgement('180.00', 'out') -> 'worse'; resolveComparisonJudgement('-180.00', 'out') -> 'better'; resolveComparisonJudgement('0.00', 'out') -> 'neutral'.
    - resolveComparisonJudgement('180.00', 'allocation') -> 'better'; resolveComparisonJudgement('180.00', 'in') -> 'better'.
    - canShowPreviousYearTotalDifference(5) -> false; canShowPreviousYearTotalDifference(6) -> true (the D-10 threshold and one step either side).
  </behavior>
  <action>
    Add to lib/services/pace-and-projection.ts: `export function buildYearSeries(months: MonthlyValue[]): { months: MonthlyValue[]; total: string }` — `total` computed as `months.reduce((sum, m) =&gt; sum.plus(toDecimal(m.amount)), toDecimal('0'))` then `toDbDecimal(...)` (D-07/D-11 — the ONLY place `total` is derived; no independent SQL SUM or second formula exists anywhere in this module). Add `export function computeComparison(current: string, previous: string): string` returning `toDbDecimal(toDecimal(current).minus(toDecimal(previous)))` (D-08, the exact convention already documented in lib/dal/overview.ts — reused, not reinvented). Add `export type ComparisonJudgement = 'better' | 'worse' | 'neutral'` and `export function resolveComparisonJudgement(delta: string, direction: 'in' | 'out' | 'allocation'): ComparisonJudgement` — if `toDecimal(delta).isZero()` return 'neutral'; else for `direction === 'out'`, positive delta -> 'worse', negative -> 'better'; for `direction === 'in' | 'allocation'`, positive delta -> 'better', negative -> 'worse' (D-09 — this single function is the ONLY place a sign-to-judgement mapping exists; no widget-local copy). Add `export const PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS = 6` and `export function canShowPreviousYearTotalDifference(previousYearCoveredMonthCount: number): boolean` returning `previousYearCoveredMonthCount >= PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS` (D-10, RESEARCH.md open question 1 — exported as a single named constant so Phases 83/84 both read the same value rather than each hardcoding 6).

    Extend tests/pace-and-projection.test.ts with describe('Total equals sum of series (PACE-05, D-07)') asserting `buildYearSeries(...).total` equals the hand-computed sum for a fixture series, and that mutating one month's amount and recomputing changes `total` by exactly that delta (proving the structural link, not a coincidental match). Add describe('Comparison sign convention and judgement (PACE-06, D-08/D-09/D-10)') covering computeComparison's sign convention, resolveComparisonJudgement's three-direction/zero-delta matrix (the &lt;behavior&gt; cases above), a determinism assertion (calling resolveComparisonJudgement twice with identical inputs yields `===`-equal strings, proving no hidden Date/Math.random dependency), a case asserting `computeComparison('0.00', '0.00')` never throws (previous-period-zero, PACE-06 "empty" edge), and the `canShowPreviousYearTotalDifference` boundary matrix (5 -> false, 6 -> true, 7 -> true).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/pace-and-projection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/pace-and-projection.ts` exports `buildYearSeries`, `computeComparison`, `resolveComparisonJudgement`, `ComparisonJudgement`, `PREVIOUS_YEAR_TOTAL_DIFFERENCE_MIN_COVERED_MONTHS`, `canShowPreviousYearTotalDifference`.
    - `grep -c "function resolveComparisonJudgement" lib/services/pace-and-projection.ts` returns 1 (exactly one definition — no per-widget duplicate anywhere in the module).
    - `node_modules/.bin/vitest run tests/pace-and-projection.test.ts` passes in full (every describe block from both Task 1 and Task 2, plus Plan 82-01's seasonal-category block).
  </acceptance_criteria>
  <done>buildYearSeries's total is structurally the sum of its own months array; computeComparison/resolveComparisonJudgement give Phases 83/84 one shared, deterministic sign-and-judgement function; canShowPreviousYearTotalDifference exports the D-10 threshold as a single constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None (pure computation) | Every function in this plan is synchronous, has no DB/network/filesystem access, and operates only on Decimal-safe strings already produced by Plan 82-01's DAL layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-82-05 | Tampering | lib/services/pace-and-projection.ts (isPartialMonth, computeCurrentMonthHybrid, buildYearSeries, computeComparison, resolveComparisonJudgement) | low | accept | No untrusted-input boundary exists inside this plan's scope — every input is a Decimal-safe string or a `Date`, produced and validated by the DAL layer built in Plan 82-01; there is no I/O for an attacker to reach. |

No package-manager installs in this plan; `T-82-SC` omitted.
</threat_model>

<verification>
- `node_modules/.bin/vitest run tests/pace-and-projection.test.ts` green in full (all describe blocks from Plan 82-01 and this plan).
- `node_modules/.bin/vitest run` (full suite) green — this is the "after" half of RETIRE-05's byte-identical proof: Plan 82-01's `tests/pace-engine-lens-regression.test.ts` baseline, captured before any Phase 82 code beyond its own tracer existed, stays green here after every other Phase 82 plan (82-01, 82-02, 82-03) has landed.
- `yarn build` succeeds.
</verification>

<success_criteria>
- The engine's full public surface (Covered Months, pace, hybrid current month, total-sum invariant, comparison/judgement, previous-year threshold) exists, is pure/Decimal.js-only, and is unit-tested end to end.
- The full test suite — including the RETIRE-05 regression baseline from Plan 82-01 — is green after every Phase 82 change has landed.
</success_criteria>

<output>
Create `.planning/phases/82-number-engine-and-regression-gate/82-03-SUMMARY.md` when done
</output>
