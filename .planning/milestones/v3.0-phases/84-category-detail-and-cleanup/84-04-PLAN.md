---
phase: 84-category-detail-and-cleanup
plan: 4
type: execute
wave: 4
depends_on: ["84-03"]
files_modified:
  - components/dashboard/dashboard-filters.tsx
  - components/dashboard/deviation-badge.tsx
  - components/dashboard/category-detail-summary.tsx
  - components/dashboard/category-detail-trend-chart.tsx
  - tests/dashboard-filters.test.ts
  - tests/deviation-badge.test.tsx
  - tests/dashboard-utils.test.ts
  - tests/category-detail-components.test.tsx
  - lib/utils/dashboard.ts
  - lib/utils/date.ts
  - lib/validations/dashboard.ts
  - lib/routes.ts
  - tests/dashboard-year-contract.test.ts
  - tests/dashboard.spec.ts
  - CONTEXT.md
autonomous: true
requirements: [RETIRE-01, RETIRE-02]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "components/dashboard/dashboard-filters.tsx, deviation-badge.tsx, category-detail-summary.tsx, category-detail-trend-chart.tsx and their dedicated test files no longer exist in the repository."
    - "lib/utils/dashboard.ts no longer exports computeDeviation/buildDeviationMap/DeviationResult/DeviationReferenceRow/DeviationBaselineRow, but still exports roundedPercent-backed computeSavingsRate/computeDeltaPercent/computeBreakdownPercentages unchanged, since lib/dal/dashboard.ts and components/dashboard/overview/overview-kpi-derive.ts still import them."
    - "lib/utils/date.ts no longer exports dashboardPresetToDateRange/DASHBOARD_PRESETS/DashboardPreset, but periodToDateRange/ExpensePeriod and every other export are unchanged."
    - "lib/validations/dashboard.ts no longer exports DashboardPresetSchema/DashboardFiltersSchema/DashboardSortSchema/DashboardTypeSchema/DashboardFilters/DashboardPreset/DashboardType/DashboardSort/parseDashboardFilters, but CategoryYearDirectionSchema/CategoryYearSortSchema/parseCategoryYearDirection/parseCategoryYearSort are unchanged."
    - "lib/routes.ts's buildDashboardCategoriesHref/buildDashboardCategoryDetailHref emit ONLY the year-mode href shape; the preset-mode branch and the preset/defaultPreset/defaultSort fields no longer exist."
    - "CONTEXT.md (repo root) defines Confronto and no longer flags the annual-comparison naming question as open; the identifier-scoped D-19 grep returns zero hits outside its documented allowlist; yarn typecheck, the full test suite, and yarn check:language are green; Phase 82's RETIRE-05 canary (tests/pace-engine-lens-regression.test.ts) still passes untouched (RETIRE-01, RETIRE-02)."
  artifacts:
    - "CONTEXT.md: new 'Confronto' glossary entry; the 'Flagged ambiguities' naming-question line replaced with a closure note"
  key_links:
    - "lib/dal/dashboard.ts (buildOverviewData, buildBreakdownData) -> lib/utils/dashboard.ts (computeSavingsRate, computeDeltaPercent, computeBreakdownPercentages) — the imports this plan must NOT break"
    - "components/dashboard/overview/overview-kpi-derive.ts -> lib/utils/dashboard.ts (computeDeltaPercent, computeSavingsRate) — a second live consumer this plan must NOT break"
---

<objective>
Retirement part 2 (D-17, final): hard-delete the Deviation/Baseline/Noise-Threshold/Preset files and symbols that Plan 84-03 already made caller-less at the DAL layer, close the CONTEXT.md glossary's one remaining open item (D-18), and run the phase's exit gate (D-19). This is the removals-only half of the retirement: every file this plan deletes had zero live callers by the end of Plan 84-02 (page/component layer) and Plan 84-03 (DAL/test layer) — this plan does not change any behavior, only removes dead code and closes documentation.

Purpose: satisfy RETIRE-01/RETIRE-02's "no dead references left" / "no regression on any surface that used its helpers" acceptance criteria, and close the milestone's last open naming item.
Output: zero retired-vocabulary identifiers remain in app/lib/components/tests outside the documented guard allowlist; the full suite is green.
</objective>

## Artifacts this phase produces

No new files, no new exported symbols, no new URL params — this is a pure removals-only plan (D-17). It deletes: 4 component files (`dashboard-filters.tsx`, `deviation-badge.tsx`, `category-detail-summary.tsx`, `category-detail-trend-chart.tsx`), 3 test files (`dashboard-filters.test.ts`, `deviation-badge.test.tsx`, `dashboard-utils.test.ts`), and the symbols `computeDeviation`/`buildDeviationMap`/`DeviationResult`/`DeviationReferenceRow`/`DeviationBaselineRow` (lib/utils/dashboard.ts), `dashboardPresetToDateRange`/`DASHBOARD_PRESETS`/`DashboardPreset` (lib/utils/date.ts), `DashboardPresetSchema`/`DashboardFiltersSchema`/`DashboardSortSchema`/`DashboardTypeSchema`/`DashboardFilters`/`DashboardPreset`/`DashboardType`/`DashboardSort`/`parseDashboardFilters` (lib/validations/dashboard.ts), and the preset-mode branch + `preset`/`defaultPreset`/`defaultSort` fields of `lib/routes.ts`'s href builders. One new prose artifact: the **Confronto** glossary entry in `CONTEXT.md`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-category-detail-and-cleanup/84-CONTEXT.md
@.planning/phases/84-category-detail-and-cleanup/84-RESEARCH.md
@lib/utils/dashboard.ts
@lib/utils/date.ts
@lib/validations/dashboard.ts
@lib/routes.ts
@CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead component files and their tests; surgically trim lib/utils/dashboard.ts</name>
  <files>components/dashboard/dashboard-filters.tsx, components/dashboard/deviation-badge.tsx, components/dashboard/category-detail-summary.tsx, components/dashboard/category-detail-trend-chart.tsx, tests/dashboard-filters.test.ts, tests/deviation-badge.test.tsx, tests/dashboard-utils.test.ts, tests/category-detail-components.test.tsx, lib/utils/dashboard.ts</files>
  <read_first>
    lib/utils/dashboard.ts (the file in full — confirm `roundedPercent`, `computeSavingsRate`, `computeDeltaPercent`, `computeBreakdownPercentages` are used by callers OUTSIDE this file before touching anything, per the grep in the action below — do NOT delete this file wholesale, only `computeDeviation`, `buildDeviationMap`, and the types `DeviationResult`, `DeviationReferenceRow`, `DeviationBaselineRow`);
    tests/dashboard-utils.test.ts (tests ONLY `computeDeviation`/`buildDeviationMap` — the whole file dies, confirm no other describe block exists before deleting);
    tests/category-detail-components.test.tsx (imports `CategoryDetailEmptyState`, `CategoryDetailSkeleton`, `CategoryDetailSummary`, `CategoryDetailTrendChart`, `CategorySubcategoryBreakdown`, `CategoryTopTransactions` — `CategoryDetailSummary`/`CategoryDetailTrendChart` die with the files this task deletes; `CategoryDetailEmptyState`/`CategoryDetailSkeleton`/`CategoryTopTransactions` SURVIVE (reshaped in Plan 84-02, unchanged import path) and `CategorySubcategoryBreakdown` also survives (reshaped) — this file needs a partial rewrite, not a deletion: drop the dead-component describe blocks/imports, keep the rest, and update any assertion still referencing the OLD `CategorySubcategoryBreakdown` props (`deviations`) to the new `contributions` prop shape Plan 84-02 introduced).
  </read_first>
  <action>
    Delete `components/dashboard/dashboard-filters.tsx`, `components/dashboard/deviation-badge.tsx`, `tests/dashboard-filters.test.ts`, `tests/deviation-badge.test.tsx`, `tests/dashboard-utils.test.ts` in their entirety. Before deleting `components/dashboard/category-detail-summary.tsx` and `components/dashboard/category-detail-trend-chart.tsx`, run `grep -rln "category-detail-summary\|category-detail-trend-chart\|CategoryDetailSummary\|CategoryDetailTrendChart" app lib components tests` and confirm the ONLY remaining hits are inside `tests/category-detail-components.test.tsx` (which this task also fixes) — if any OTHER live importer turns up, stop and do not delete until that importer is resolved (it would indicate Plan 84-02 missed a call site). Then delete both files.

    Rewrite `tests/category-detail-components.test.tsx`: remove the `CategoryDetailSummary`/`CategoryDetailTrendChart` imports and their describe blocks entirely; keep the `CategoryDetailEmptyState`/`CategoryDetailSkeleton`/`CategoryTopTransactions` coverage as-is; update the `CategorySubcategoryBreakdown` describe block's fixtures from the old `{ subcategories, type, deviations }` props to the new `{ contributions, type }` shape Plan 84-02's Task 2 introduced (mirror that plan's own component test fixtures rather than inventing a new shape).

    In `lib/utils/dashboard.ts`, delete `computeDeviation`, `buildDeviationMap`, and the types `DeviationResult`, `DeviationReferenceRow`, `DeviationBaselineRow` — keep `roundedPercent`, `computeSavingsRate`, `computeDeltaPercent`, `computeBreakdownPercentages` and the `Decimal`/`toDecimal` imports exactly as they are (verified live consumers: `lib/dal/dashboard.ts`'s `buildOverviewData`/`buildBreakdownData`/`buildCategoryRankingData`/`buildCategoryYearRankingData`/`buildCategoryDetailData`, and `components/dashboard/overview/overview-kpi-derive.ts`).
  </action>
  <reversibility rating="costly">Undoing this task means restoring 4 component files, 3 test files, and 5 symbols in `lib/utils/dashboard.ts` in lock-step with Task 1 of Plan 84-03's DAL deletions — a clean `git revert` of this task's own commit is the intended escape hatch (D-14), not a partial restoration.</reversibility>
  <verify>
    <automated>yarn test tests/category-detail-components.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/dashboard/dashboard-filters.tsx` and the same for `deviation-badge.tsx`, `category-detail-summary.tsx`, `category-detail-trend-chart.tsx` all report "No such file".
    - `grep -n "computeDeviation\|buildDeviationMap\|DeviationResult\|DeviationReferenceRow\|DeviationBaselineRow" lib/utils/dashboard.ts` returns zero matches; `grep -n "computeSavingsRate\|computeDeltaPercent\|computeBreakdownPercentages" lib/utils/dashboard.ts` returns 3+ matches (unchanged exports survive).
    - `yarn test tests/category-detail-components.test.tsx --run` passes.
  </acceptance_criteria>
  <done>The 4 dead component files and 3 dead test files no longer exist; lib/utils/dashboard.ts keeps only its live exports.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dashboardPresetToDateRange/DASHBOARD_PRESETS, the preset validations, and the preset href branch in lib/routes.ts</name>
  <files>lib/utils/date.ts, lib/validations/dashboard.ts, lib/routes.ts, tests/dashboard-year-contract.test.ts</files>
  <read_first>
    lib/utils/date.ts (confirm `dashboardPresetToDateRange`, `DASHBOARD_PRESETS`, `DashboardPreset` have zero remaining importers — Plan 84-03 already removed the production DAL's use of `dashboardPresetToDateRange`; `periodToDateRange`/`ExpensePeriod` are a DIFFERENT, unrelated preset-shaped concept used by `lib/dal/expenses.ts` and must NOT be touched — verify by `grep -rn "DASHBOARD_PRESETS\|DashboardPreset\b" app lib components tests` before deleting);
    lib/validations/dashboard.ts (`DashboardPresetSchema`, `DashboardTypeSchema`, `DashboardSortSchema`, `DashboardFiltersSchema`, `parseDashboardFilters` and their inferred types — `CategoryYearDirectionSchema`/`CategoryYearSortSchema`/`parseCategoryYearDirection`/`parseCategoryYearSort` below them are UNRELATED and stay);
    lib/routes.ts lines 1-181 (`buildDashboardCategoriesHref`/`buildDashboardCategoryDetailHref`'s TWO branches: the `filters.year !== undefined` early-return using `buildYearModeSearch` — KEEP this branch and `buildYearModeSearch` itself, including Plan 84-01's `months`/`from` additions — versus the preset-mode body below it using `filters.preset`/`defaultPreset`/`defaultSort` — DELETE that body and those `DashboardCategoryFilters` fields; `lib/routes.ts`'s own import of `DashboardPreset`/`DashboardSort` types from `@/lib/validations/dashboard` at the top must be updated to drop `DashboardPreset`/`DashboardSort`, keeping only what `CategoryYearSort` needs);
    tests/dashboard-year-contract.test.ts lines 32-33 (KEEP — this guards that the retired `'deviation'` sort value degrades gracefully, unrelated to what this task deletes) and lines 74-78 (`'no year key at all leaves the preset-based href completely unchanged'` — this test DIES, since it exercises exactly the preset-mode branch this task removes).
  </read_first>
  <behavior>
    - Every remaining call site of `buildDashboardCategoriesHref`/`buildDashboardCategoryDetailHref` in live app/component code already always passes `year` (verified: `app/(app)/dashboard/categories/[id]/page.tsx`, `components/dashboard/category-ranking-list.tsx`, `components/dashboard/category-list-controls.tsx`) — removing the preset-mode branch changes no live behavior, only removes an already-unreachable code path plus its one remaining test.
  </behavior>
  <action>
    Delete `dashboardPresetToDateRange`, `DASHBOARD_PRESETS`, and the `DashboardPreset` type from `lib/utils/date.ts`; leave `periodToDateRange`, `ExpensePeriod`, `DateRange`, `endOfMonth` (module-private, stays), `monthKey`, `monthLabel`, `monthsBetween`, `formatMonthShort`, `formatMonthRange`, `yearProgressPercent`, `formatDayMonthRange`, `formatDayMonthLong`, `formatOptionalDateRange` completely untouched.

    Delete `DashboardPresetSchema`, `DashboardTypeSchema`, `DashboardSortSchema`, `DashboardFiltersSchema`, `parseDashboardFilters`, and the types `DashboardPreset`, `DashboardType`, `DashboardFilters`, `DashboardSort` from `lib/validations/dashboard.ts`; leave `CategoryYearDirectionSchema`, `CategoryYearDirection`, `CategoryYearSortSchema`, `CategoryYearSort`, `parseCategoryYearDirection`, `parseCategoryYearSort` untouched; drop the now-unused `import { DASHBOARD_PRESETS } from '@/lib/utils/date'` line.

    In `lib/routes.ts`, delete the preset-mode body of both `buildDashboardCategoriesHref` and `buildDashboardCategoryDetailHref` (everything after the `if (filters.year !== undefined) { ... return ... }` early return in each), and remove `preset`, `defaultPreset`, `defaultSort` from the `DashboardCategoryFilters` type — keep `type`, `sort` (now typed only as `CategoryYearSort`, drop the `DashboardSort` half of its union), `year`, `months`, `from`, `lens`. Update the top-level type import to drop `DashboardPreset`/`DashboardSort` from `@/lib/validations/dashboard`, keeping `CategoryYearSort`.

    In `tests/dashboard-year-contract.test.ts`, delete the `'no year key at all leaves the preset-based href completely unchanged'` test (lines ~74-78) — keep every other test in the file, including the two `parseCategoryYearSort`/`parseCategoryYearDirection` guard tests at lines 32-33 (they assert the retirement, they do not exercise it).
  </action>
  <reversibility rating="costly">A caller anywhere still relying on the preset-mode href shape would break silently until the next build — the pre-deletion grep in `<read_first>` is the safeguard; reverting requires restoring the routes.ts branch, both validations files' schemas, and the deleted test together.</reversibility>
  <verify>
    <automated>yarn test tests/dashboard-year-contract.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DASHBOARD_PRESETS\|DashboardPreset\b" lib/utils/date.ts lib/routes.ts` returns zero matches; `grep -n "periodToDateRange\|ExpensePeriod" lib/utils/date.ts` still returns matches (untouched).
    - `grep -n "DashboardPresetSchema\|DashboardFiltersSchema\|DashboardSortSchema\|parseDashboardFilters" lib/validations/dashboard.ts` returns zero matches; `grep -n "parseCategoryYearDirection\|parseCategoryYearSort" lib/validations/dashboard.ts` still returns matches (untouched).
    - `buildDashboardCategoriesHref({ year: 2026 })` still returns `/dashboard/categories?year=2026`; calling it with `{ preset: 'last-3-months' }` (no `year`) now produces a TypeScript compile error (the field no longer exists on `DashboardCategoryFilters`) — confirmed via `yarn typecheck`, not a runtime check.
    - `yarn test tests/dashboard-year-contract.test.ts --run` passes.
  </acceptance_criteria>
  <done>lib/utils/date.ts, lib/validations/dashboard.ts, and lib/routes.ts carry no preset-mode code; every live caller (already year-mode-only) is unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: CONTEXT.md Confronto glossary entry, Playwright hygiene, D-19 exit gate</name>
  <files>CONTEXT.md, tests/dashboard.spec.ts</files>
  <read_first>
    CONTEXT.md lines 213-227 (the `Ritmo`/`Proiezione`/`Contributo alla differenza`/`Finestra` entries — already rewritten by an earlier phase, do NOT duplicate or restate them) and lines 291-295 (`## Flagged ambiguities` — the LAST unresolved line: "Il confronto 'stima annuale vs anno precedente chiuso' non ha ancora un nome... Da fissare in fase UI" — this is the ONLY remaining open item D-18 asks this phase to close; everything else D-18 lists — Mese Coperto, Mese Parziale, Ritmo, Proiezione, Finestra, the rewritten Example dialogue, the D-12 debt-extinguished note — is ALREADY present in this file from an earlier phase);
    CONTEXT.md lines 229-231 (the surviving **MonthOverMonthChange** entry — its own definition, `getMonthOverMonthCategoryChanges` query reference, UI copy and field list are correct and SURVIVE untouched; only its second sentence, "Distinto dalla Deviation (che confronta vs la Baseline su 3 mesi).", must be deleted — it defines a surviving term by contrast against two retired ones, which is exactly the kind of trace D-18/RETIRE-01 asks this phase to remove from the canonical glossary; do not replace the deleted sentence with any new contrast, including one against `Confronto` — MonthOverMonthChange and Confronto are different measures at different scopes and a new cross-reference would invite the same conflation this sentence was written to prevent);
    84-CONTEXT.md's D-12/D-13 (the canonical label "Rispetto al {anno-1}" and the canonical code/glossary term `Confronto`, distinct from "delta" (KPI period-over-period) and the retired "deviation");
    84-RESEARCH.md's "D-19 Exit Criterion (corrected)" section (the identifier-scoped grep command and its allowlist rationale — `MonthMultiPicker.tsx`'s unrelated "preset" sense, and the 5 guard references in `tests/dashboard-year-contract.test.ts`/`tests/category-direction-copy.test.ts`/`dashboard-tab-nav.tsx`/`categories/page.tsx`/`tags/page.tsx` that assert the retirement and must NOT be touched);
    tests/dashboard.spec.ts lines 50-181 (the `'DASH-02 tab links preserve preset and type URL filters'` Playwright test — asserts `?preset=` round-trips through `buildDashboardTabHref`, which already dropped `preset` propagation in Phase 82; this test's premise has been stale since Phase 82 and is now doubly so after Task 2's routes.ts deletion — not part of this phase's Vitest Nyquist gate, but left as-is it bit-rots the suite).
  </read_first>
  <action>
    In `CONTEXT.md`, add a new glossary entry named **Confronto** (placed near `Contributo alla differenza`/`Finestra`, same "### Dashboard e analisi" section) defining it as: the comparison between the selected window's figures and the homologous window of the previous year — computed as `current − previous` (never a delta glyph), rendered as magnitude + word with the label "Rispetto al {anno-1}", applied identically to the summary column's Totale/Media and to each subcategory's contribution; distinguish it explicitly from "delta" (reserved for KPI period-over-period) and from the retired "deviation". Then replace the "Il confronto ... non ha ancora un nome... Da fissare in fase UI" sentence in `## Flagged ambiguities` with a closure note stating the term is now **Confronto**, fixed in Phase 84.

    In the same file, edit the **MonthOverMonthChange** entry (currently lines 229-231): delete only its second sentence — "Distinto dalla Deviation (che confronta vs la Baseline su 3 mesi)." — in full, including the trailing space it leaves behind so the surrounding prose still reads cleanly. Leave the rest of the entry (its opening definition sentence, the `getMonthOverMonthCategoryChanges` query reference, the UI copy strings, the field list, and the `_Avoid_` line) untouched. Do not add any replacement clause, and specifically do not draw a new contrast against `Confronto` — the two measures operate at different scopes (month-over-previous-calendar-month vs. window-over-homologous-previous-year-window) and a cross-reference here would reintroduce the same conflation risk the deleted sentence existed to guard against; the entry stands on its own definition alone.

    In `tests/dashboard.spec.ts`, remove the `'DASH-02 tab links preserve preset and type URL filters'` test block (its full body, roughly lines 50-181) — its premise (the tab nav propagating `?preset=`) has been false since Phase 82's `buildDashboardTabHref` rewrite; this is hygiene, not a Nyquist-gated deletion (Playwright is not part of `yarn test --run`), and it is safe here specifically because none of its assertions were passing against current behavior already.

    Run the phase exit gate: `yarn typecheck`; `yarn test --run` (full suite, confirm `tests/pace-engine-lens-regression.test.ts` is green with zero edits made to it across this entire phase); `yarn check:language`; then the identifier-scoped D-19 grep from 84-RESEARCH.md (`grep -rIn -E 'DashboardPreset|DashboardFilters|DashboardSort|parseDashboardFilters|dashboardPresetToDateRange|getCategoryDeviations|getDeviationDateRanges|buildDeviationDataset|buildDeviationMap|computeDeviation|DEVIATION_NOISE_THRESHOLD|DeviationData|DeviationDateRanges|getOverviewComparisonRanges' app lib components tests | grep -vE '^(tests/dashboard-year-contract\.test\.ts|tests/category-direction-copy\.test\.ts):'`) — this MUST print nothing (piped through `|| echo "RETIRE-01 clean"` for a clean exit code). Do not touch `components/data-table/MonthMultiPicker.tsx` (unrelated "preset" sense) or the 5 guard references this command's allowlist already excludes.
  </action>
  <verify>
    <automated>grep -rIn -E 'DashboardPreset|DashboardFilters|DashboardSort|parseDashboardFilters|dashboardPresetToDateRange|getCategoryDeviations|getDeviationDateRanges|buildDeviationDataset|buildDeviationMap|computeDeviation|DEVIATION_NOISE_THRESHOLD|DeviationData|DeviationDateRanges|getOverviewComparisonRanges' app lib components tests | grep -vE '^(tests/dashboard-year-contract\.test\.ts|tests/category-direction-copy\.test\.ts):' ; echo "exit code above must correspond to zero non-allowlisted hits"</automated>
  </verify>
  <acceptance_criteria>
    - The identifier-scoped D-19 grep command in `<verify>` produces zero output lines.
    - `grep -c "Confronto" CONTEXT.md` is greater than the pre-task count (a real new entry was added, not just a passing mention).
    - `grep -n "non ha ancora un nome" CONTEXT.md` returns zero matches (the open item is closed).
    - `grep -n "Distinto dalla Deviation" CONTEXT.md` returns zero matches, while `grep -n "getMonthOverMonthCategoryChanges" CONTEXT.md` still returns a match (the clause is gone, the surviving MonthOverMonthChange entry and its query reference are intact).
    - `yarn typecheck && yarn test --run && yarn check:language` all exit 0.
  </acceptance_criteria>
  <done>CONTEXT.md's last open item is closed, the D-19 identifier-scoped grep is clean, and the phase exit gate (typecheck, full suite, check:language) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation-only surface | `CONTEXT.md` edits and dead-code removal carry no runtime trust boundary of their own — they inherit the boundaries already covered in Plans 84-01/84-02/84-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-10 | Repudiation | A live caller of a deleted symbol slipping through undetected | high | mitigate | Every deletion in this plan is preceded by an explicit pre-deletion grep in `<read_first>`/`<action>`, and the phase-wide identifier-scoped D-19 grep (Task 3) is the final, falsifiable backstop across the whole app/lib/components/tests tree |
| T-84-11 | Denial of Service | An accidental deletion of a still-shared helper (`computeSavingsRate`/`computeDeltaPercent`/`computeBreakdownPercentages`/`periodToDateRange`) breaking Overview/Expenses | high | mitigate | Task 1/2's acceptance criteria explicitly assert these SURVIVING exports still exist after the surgical trims, not just that the deletions succeeded |

</threat_model>

<verification>
- `yarn typecheck`
- `yarn test --run` (full suite)
- `yarn check:language`
- The identifier-scoped D-19 grep command (Task 3's `<verify>`), zero output required
</verification>

<success_criteria>
- Every RETIRE-01/RETIRE-02 file and symbol inventoried in 84-RESEARCH.md is gone, with the documented guard/allowlist references intentionally kept.
- `CONTEXT.md`'s last open item (the annual-comparison naming question) is closed with the term **Confronto**.
- Full suite green, typecheck clean, `yarn check:language` clean, Phase 82's RETIRE-05 canary untouched and passing.
</success_criteria>

<output>
Create `.planning/phases/84-category-detail-and-cleanup/84-04-SUMMARY.md` when done
</output>
