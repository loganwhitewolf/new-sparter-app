---
phase: 83-categories-list
plan: 05
type: execute
wave: 3
depends_on: ["83-01", "83-02", "83-03", "83-04"]
files_modified:
  - lib/dal/dashboard.ts
  - tests/categories-ranking-dal.test.ts
  - tests/category-allocation-negative-domain.test.tsx
  - components/dashboard/category-sparkline.tsx
  - tests/category-sparkline.test.tsx
  - lib/routes.ts
  - tests/dashboard-filters.test.ts
autonomous: true
requirements: [CLIST-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "getCategoryYearRanking's per-month amount for the allocation direction is the raw signed sum(...) — never abs()'d — while 'in'/'out' keep the pre-existing abs(sum(...)) completely untouched; proven against real Postgres: a fixture with deposits +200.00 in January and a net divestment of -450.00 in May yields a sparkline point with amount '-450.00' for May, '200.00' for January, and the year's Totale is the exact signed Decimal.js sum of the 12-point series ('-250.00' in that fixture, once a net-zero June is included) — never independently re-derived or re-abs'd (D-07 holds for a negative total too)."
    - "A month whose allocation transactions net to exactly zero renders with amount '0.00' and never triggers the sparkline's negative-domain marker — only a strictly-negative rawAmount (< 0, never <= 0) is marked, so an exact-zero month reads identically to a genuinely zero-spend covered month, never as a divestment."
    - { statement: "On the allocation direction, a net-divestment month (negative amount) renders visually distinct from a zero month with an explicit marker — the existing parseAmount Math.max(parsed, 0) clamp must not survive, and the DAL must not erase the sign via abs() before the value reaches the component", verification: backstop }
    - "Feeding CategorySparkline the real sparkline data returned by getCategoryYearRanking(year, 'allocation') for a fixture with one negative month renders exactly one 'border-top:2px solid var(--total-allocation)' marker, in a single test that goes real Postgres -> DAL -> rendered HTML, never bypassing the DAL with a synthetic prop (closing the exact gap the prior unit test could not: it fed a synthetic negative string directly to the component)."
    - "When a category's estimatedHeightHint (pace) is null — insufficient Covered-Month coverage — and every covered/current month's amount is also zero (a brand-new category with 1 Covered Month and no spend yet), estimated (future) months in the sparkline still render at a non-zero, normalized height, never a flat 0% bar: the reference falls back first to the series' own observed covered/current magnitude, and if that is also zero, to a fixed positive constant that always self-normalizes to 100% of itself."
    - "When estimatedHeightHint is a real, non-null pace value, estimated bars continue to normalize against that exact value exactly as before this fix — the fallback path activates ONLY when the hint is null, never overriding a real pace value."
    - "buildDashboardCategoriesHref and buildDashboardCategoryDetailHref's preset-mode branch never silently drops an explicit `type: 'allocation'` filter — it mirrors buildYearModeSearch's own `filters.type && filters.type !== 'out'` check, so a caller passing `{ preset, type: 'allocation' }` without `year` gets `?type=allocation` in the emitted href instead of a silent fallback to 'out'."
  artifacts:
    - "lib/dal/dashboard.ts (edited — getCategoryYearRanking's amount column branches on directionCode; getCategoryRanking's own body, at an earlier line span, is provably untouched)"
    - "tests/categories-ranking-dal.test.ts (edited — new negative-domain + exact-zero-boundary test case)"
    - tests/category-allocation-negative-domain.test.tsx
    - "components/dashboard/category-sparkline.tsx (edited — resolveEstimatedReference fallback, ESTIMATED_HEIGHT_FALLBACK constant)"
    - "tests/category-sparkline.test.tsx (edited — new WR-02 fallback test case)"
    - "lib/routes.ts (edited — preset-mode type filter mirrors year-mode's check in both href builders)"
    - "tests/dashboard-filters.test.ts (edited — new allocation preset-mode test cases)"
  key_links:
    - "getCategoryYearRanking's amountSql branch (directionCode === 'allocation' -> signed sum(...), else abs(sum(...))) -> CategoryYearSparklinePoint.amount -> CategorySparkline's isNegative marker (real Postgres -> DAL -> component, proven in one test, tests/category-allocation-negative-domain.test.tsx)"
    - "resolveEstimatedReference(points, pointStates, estimatedHeightHint) -> referenceMagnitudes -> heightPercent (never 0 for an 'estimated' bar even when pace is null and every covered/current amount is also 0)"
    - "buildYearModeSearch's `filters.type && filters.type !== 'out'` check (lib/routes.ts, already shipped) -> mirrored into the preset-mode branches of buildDashboardCategoriesHref/buildDashboardCategoryDetailHref"
  prohibitions:
    - statement: "Must not silently present a net-divestment month as positive spending or as a zero-spend month for the allocation direction."
      category: correctness
      verification: test
    - statement: "Must not render an estimated (future) month's bar at a flat 0% height that reads as 'no data available' when the category's pace is unavailable."
      category: correctness
      verification: test
---

<objective>
Close the two verification gaps from 83-VERIFICATION.md (CR-01 blocker, WR-02 warning) that
prevent Phase 83 from being considered complete, plus fold in the related WR-01 latent defect
found by the same code review. Phase 83's four plans (83-01..04) are already executed and
committed; this plan does NOT reopen, replan, or re-implement any of that work — it fixes exactly
the defects the verifier and reviewer found in it.

CR-01: `getCategoryYearRanking`'s SQL-level `abs(sum(...))` destroys the sign of a net-divestment
allocation month before it ever reaches the sparkline component that was built (and unit-tested
with a synthetic prop) specifically to render that sign as a visual marker (D-09, UI-SPEC E2
backstop). WR-02: the sparkline's 'estimated' (future) bars collapse to a flat 0%-height bar
whenever the category's pace is null (< 2 pace-eligible Covered Months), contradicting the
component's own documented "never a flat/zero-height bar" contract. WR-01: the same route
builders' preset-mode branch never learned about the widened `'allocation'` type value, silently
dropping it for any future caller that omits `year`.

Purpose: CLIST-04 ("switch between Uscite, Entrate and Accantonamenti") is marked PARTIAL in
83-VERIFICATION.md specifically because Accantonamenti is reachable but not fully functional — the
direction's one defining visual property (negative-domain rendering) never fires with real data.
Output: `getCategoryYearRanking` preserves sign for the allocation direction; `CategorySparkline`
never collapses an estimated bar to 0% height; both route-href builders treat `'allocation'`
consistently in preset mode as they already do in year mode.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-categories-list/83-CONTEXT.md
@.planning/phases/83-categories-list/83-UI-SPEC.md
@.planning/phases/83-categories-list/83-VERIFICATION.md
@.planning/phases/83-categories-list/83-REVIEW.md
@lib/dal/dashboard.ts
@components/dashboard/category-sparkline.tsx
@lib/routes.ts
@tests/categories-ranking-dal.test.ts
@tests/category-sparkline.test.tsx
@tests/dashboard-filters.test.ts
@tests/transaction-title-edit.test.tsx
</context>

## Artifacts this phase produces

- `lib/dal/dashboard.ts` — `getCategoryYearRanking`'s per-month `amount` column: **contract
  change** — `CategoryYearSparklinePoint.amount` may now be negative for the `'allocation'`
  direction (sign preserved via a `directionCode`-branched SQL expression, instead of an
  unconditional `abs()`); unchanged (still non-negative) for `'in'`/`'out'`.
  `CategoryYearRankingItem.amount` (the row's Totale) may now be negative for `'allocation'` when
  net divestment exceeds net deposits across the year — D-07 still holds: the total is the exact
  signed Decimal.js sum of the sparkline, never independently re-derived. No change to
  `CategoryYearRankingItem.percentage`'s contract (already non-negative, via
  `computeBreakdownPercentages`'s own independent `.abs()`).
- `components/dashboard/category-sparkline.tsx` — new internal (non-exported) helper
  `resolveEstimatedReference(points, pointStates, estimatedHeightHint)` and constant
  `ESTIMATED_HEIGHT_FALLBACK`. Behavior change: an `'estimated'` bar's rendered height no longer
  collapses to 0% when `estimatedHeightHint` is null.
- `lib/routes.ts` — `buildDashboardCategoriesHref`/`buildDashboardCategoryDetailHref`'s
  preset-mode branch: **contract change** — now emits `?type=allocation` when called with
  `{ type: 'allocation' }` without `year` (previously silently dropped it, resolving back to
  `'out'`).
- New test file: `tests/category-allocation-negative-domain.test.tsx` (real-Postgres + render,
  end-to-end tracer proof for CR-01).
- Every other Phase 83 export (`getCategoryRanking`, `buildCategoryRankingData`,
  `CategoryRankingItem`, `resolveCategoryDirectionCopy`, `CategoryCoverageNudge`, etc.) is
  untouched by this plan.

## Assumption-Delta Architecture Checkpoint

<assumption_delta_decision>
  <noun>The year-end projection field on a Categories row ("Each row also shows the year-end
  projection" — ROADMAP.md Phase 83 Success Criteria #2, CONTEXT.md D-05/D-06).</noun>
  <decision>no-change</decision>
  <rationale>The detected "also" is an ordinary prose conjunction describing a SECOND FIELD on an
  already-singular row (name + total + share + sparkline + projection — D-04's exhaustive five
  fields) — not a singular-to-plural identity transition (no second platform, tenant, auth method,
  or source of truth is being introduced by this signal). This gap-closure plan touches neither the
  projection field's existence nor its rendering; its two fixes (DAL sign-preservation, sparkline
  height fallback) are orthogonal to the projection the pluralization detector flagged. No
  architectural change follows; recorded per protocol as a false positive.</rationale>
</assumption_delta_decision>

## Flagged Assumptions (spec-less probe fallback — not reclaimed by this gap-closure plan)

- **CLIST-01/02/03 `adjacency`/`empty`/`ordering` probes** — already resolved by Plans 83-01/83-04's
  own `must_haves` (ranking descending-then-name-then-id order, empty-year `[]` handling, sort
  stability with a null-projection fallback). This plan does not re-claim ownership.
- **CLIST-04 `adjacency`/`empty`/`ordering` probes** — already covered by 83-01's D-09
  predicate-flip `must_haves` and 83-04's `DirectionFilter` wiring; unrelated to the two failing
  truths this plan closes.
- **CLIST-04 `boundary` probe** — resolved here: see the "exact-zero month never triggers the
  marker" truth above, proven by Task 1's new test case (Jan positive / May negative / June
  exact-zero).
- **CLIST-04 `precision` probe** — `{ statement: "The allocation direction's signed monthly sums
  round via the exact same Decimal.js ROUND_HALF_UP contract (toDbDecimal, applied once at
  buildYearSeries's/computePaceAndProjection's own return boundary) already used for 'in'/'out' —
  this gap-closure plan changes only whether SQL-level abs() is applied, never any
  rounding/precision logic downstream", verification: backstop }` — no new dedicated
  negative-decimal tie-break test was written (e.g. a value ending in exactly .005); the rounding
  path itself is untouched code, so this is recorded as a backstop assumption, not a "covered"
  truth.
- **CLIST-05 `boundary`/`precision` probes** — year-parameter propagation; unrelated to the two
  gaps this plan closes; already the responsibility of 83-02/83-04's own `must_haves`.
- **CLIST-06/07 `unclassified` probes** — carried over unresolved from 83-01-PLAN.md's own
  "Flagged Assumptions" section (the row-total/detail-total structural equality and the raw
  Covered-Month-count UI rule). Neither VERIFICATION gap this plan closes concerns CLIST-06/07;
  not attempted here.

## Explicitly Deferred (Info-level anti-patterns from 83-REVIEW.md — not verification gaps)

- **IN-01** (duplicate `Intl.NumberFormat`/`formatAmount` in
  `components/dashboard/category-ranking-list.tsx:34-42`, should reuse
  `lib/utils/format-amount.ts`). **Deferred.** This exact duplicate already exists, byte-for-byte,
  in `components/dashboard/tag-ranking-list.tsx` — pre-existing tech debt this phase propagated,
  not introduced. A correct fix touches both call sites together for consistency; `tag-ranking-list.tsx`
  is the Tags surface, explicitly out of Phase 83's scope fence ("Any change to Overview or Tags
  output — RETIRE-05 must stay byte-identical"). Fixing only `category-ranking-list.tsx` in
  isolation would leave an inconsistent pair, worse than the current state. Zero behavioral
  difference either way (both formatters produce identical output). Left for a future dedicated
  cleanup task touching both files together.
- **IN-02** (`?year=0`/negative year treated as valid in
  `app/(app)/dashboard/categories/[id]/page.tsx:159-161`). **Deferred.** This file is the category
  DETAIL page, which 83-CONTEXT.md's own Scope Fence marks out of Phase 83's scope beyond
  "receiving the year in its URL" ("Any change to the category detail page beyond receiving the
  year in its URL... a plan that touches them has drifted"). The detail page's full rewrite is
  Phase 84 (CDET-01…07); its year-parsing validation belongs there, alongside the rest of that
  page's contract. Harmless today (falls back to `resolveYear`'s own fallback on the list page, per
  83-REVIEW.md), not a blocker for CLIST-01…07.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Allocation direction preserves sign end-to-end — DAL fix, real-Postgres proof, component render proof (CR-01)</name>
  <reversibility rating="reversible">A branch on an existing, Phase-83-only function's SQL amount expression, additive-only (no schema/migration). getCategoryYearRanking has exactly one production caller (app/(app)/dashboard/categories/page.tsx) plus its own test file — confirmed via grep across the whole repo — so no other consumer depends on the current non-negative guarantee. Trivially revertable by removing the directionCode branch.</reversibility>
  <files>lib/dal/dashboard.ts, tests/categories-ranking-dal.test.ts, tests/category-allocation-negative-domain.test.tsx</files>
  <read_first>
    lib/dal/dashboard.ts lines 1326-1387 (getCategoryYearRanking — the function to fix; the amount column at line 1347) and lines 1040-1102/1301 (getCategoryRanking — the PROTECTED baseline; its own `coalesce(abs(sum(...)))` at line ~1269 and its `orderBy` at line 1301 must stay byte-identical) and lines 802-938 (buildCategoryYearRankingData — confirms buildYearSeries/computeBreakdownPercentages need NO changes);
    lib/utils/dashboard.ts lines 86-99 (computeBreakdownPercentages — already computes percentage from `toDecimal(row.amount).abs()` against a sum of abs() values, so an individual category's signed total cannot make percentage negative or break the zero-total guard);
    components/dashboard/category-sparkline.tsx lines 24-30 (parseAmount, already un-clamped) and lines 84-131 (the isNegative marker check, `state === 'covered' || state === 'current') && rawAmount < 0` — already correct, NOT modified by this task);
    tests/categories-ranking-dal.test.ts in full (the existing seedAllocationTaxonomy local helper to reuse, and the exact harness boilerplate: vi.mock('@/lib/dal/auth'), vi.mock('react') wholesale replacement, connectReimbursementTestDb, describeIfReachable, requireHarnessDb, vi.doMock('@/lib/db', ...) + vi.resetModules() + dynamic import pattern);
    tests/category-sparkline.test.tsx lines 76-88 (the existing marker assertion string to reuse verbatim: 'border-top:2px solid var(--total-allocation)' and 'height:100%');
    tests/transaction-title-edit.test.tsx lines 1-16 (the `vi.importActual` SPREAD mock pattern for 'react' — `{ ...actual, cache: ... }` — required because this task's new test ALSO calls renderToStaticMarkup and needs real React internals; tests/categories-ranking-dal.test.ts's own wholesale `vi.mock('react', () => ({ cache: ... }))` replacement would break rendering if reused here);
    tests/fixtures/reimbursement-seed.ts (seedUser, seedExpenseWithTransaction signatures — amount is stored as given, unmodified, on both expense.totalAmount and transaction.amount);
    .planning/phases/83-categories-list/83-UI-SPEC.md `## Allocation (Savings) Sparkline Color` and its E2 negative-domain backstop row in `## UI Considerations`; .planning/phases/83-categories-list/83-CONTEXT.md D-09 and the Risk Summary's "selling an ETF drags a month negative" note.
  </read_first>
  <behavior>
    - getCategoryYearRanking(2024, 'allocation') against a fixture with one allocation category carrying a +200.00 transaction in January, a -450.00 transaction in May, and two transactions in June (+300.00 and -300.00, netting to exactly zero) -> the May sparkline point's amount is exactly '-450.00' (sign preserved, not abs()'d), the January point is '200.00', the June point is '0.00', and item.amount (Totale) is exactly '-250.00' — the Decimal.js sum of the signed 12-point series (D-07 holds against a NEGATIVE total too).
    - The June (exact-zero) point does NOT trigger the sparkline's negative-domain marker when rendered — only `rawAmount < 0`, never `<= 0`, is marked.
    - getCategoryYearRanking(year, 'out') and (year, 'in') continue to return only non-negative amounts for every EXISTING fixture in this file (no code change to their branch; the pre-existing tests in this file are the regression proof — they must still pass unmodified).
    - Feeding the allocation item's real sparkline (as returned by getCategoryYearRanking against real Postgres) directly into `<CategorySparkline type="allocation" pointStates={...} estimatedHeightHint={item.pace} />` and rendering it via renderToStaticMarkup produces HTML containing exactly ONE occurrence of 'border-top:2px solid var(--total-allocation)' (May's bar) — never for January (positive) or June (exact zero).
  </behavior>
  <action>
    In lib/dal/dashboard.ts's getCategoryYearRanking (the function added in Plan 83-01 — do NOT touch getCategoryRanking, a different function at an earlier line span), add a `const amountSql` computed just above the existing `let rows: CategoryYearRankingAggregateRow[] = []` line: `directionCode === 'allocation' ? sql<string>`coalesce(sum(${ledgerRowSource.amount}), 0)::text` : sql<string>`coalesce(abs(sum(${ledgerRowSource.amount})), 0)::text``. Replace the inline `amount: sql<string>`coalesce(abs(sum(${ledgerRowSource.amount})), 0)::text`` field inside the `.select({...})` object (currently at line 1347) with `amount: amountSql`. No other line in getCategoryYearRanking, buildCategoryYearRankingData, buildYearSeries, or computeBreakdownPercentages needs to change: buildYearSeries already reduce-sums whatever sign the sparkline carries (D-07 holds automatically once the source data is signed), and computeBreakdownPercentages (lib/utils/dashboard.ts line 89) already computes percentage from `toDecimal(row.amount).abs()` against a sum of abs() values, so percentage stays a safe non-negative share regardless of one category's signed total.

    In tests/categories-ranking-dal.test.ts, add one new it() (inside or alongside the existing 'getCategoryYearRanking — year+direction predicate flip...' describe block): reuse the file's own seedAllocationTaxonomy helper, seed the Jan/May/June fixture from <behavior> above via four seedExpenseWithTransaction calls against the same allocation subCategoryId, call getCategoryYearRanking(2024, 'allocation'), and assert the exact Decimal equalities from <behavior> (Jan '200.00', May '-450.00', June '0.00', item.amount '-250.00') using toDecimal(...).equals(toDecimal(...)) exactly like the file's existing assertions.

    Create tests/category-allocation-negative-domain.test.tsx (new file) as the end-to-end tracer proof. Mock '@/lib/dal/auth' exactly like tests/categories-ranking-dal.test.ts (`vi.mock('@/lib/dal/auth', () => ({ verifySession: vi.fn() }))`). Mock 'react' using the SPREAD pattern from tests/transaction-title-edit.test.tsx: `vi.mock('react', async () => { const actual = await vi.importActual<typeof import('react')>('react'); return { ...actual, cache: <T extends (...args: never[]) => unknown>(fn: T) => fn } })` — NOT the wholesale replacement, because this file also calls renderToStaticMarkup. Import connectReimbursementTestDb/resetReimbursementFixtures from tests/helpers/reimbursement-test-db, seedUser/seedExpenseWithTransaction from tests/fixtures/reimbursement-seed, and createElement from 'react' + renderToStaticMarkup from 'react-dom/server' + CategorySparkline from '@/components/dashboard/category-sparkline'. Duplicate the same seedAllocationTaxonomy helper shape used in tests/categories-ranking-dal.test.ts locally in this file (each test file owns its own copy; nothing shared/exported). Seed the identical Jan/May/June fixture, call the real getCategoryYearRanking(2024, 'allocation') against real Postgres, then `createElement(CategorySparkline, { points: item.sparkline.map(p => ({ month: p.month, label: p.label, amount: p.amount })), type: 'allocation', pointStates: item.sparkline.map(p => p.state), estimatedHeightHint: item.pace })`, render it with renderToStaticMarkup, and assert the html contains exactly one occurrence of 'border-top:2px solid var(--total-allocation)' via `html.split('border-top:2px solid var(--total-allocation)').length - 1 === 1`.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/categories-ranking-dal.test.ts tests/category-allocation-negative-domain.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/categories-ranking-dal.test.ts tests/category-allocation-negative-domain.test.tsx` passes against real Postgres (`yarn db:up` run first).
    - The new categories-ranking-dal.test.ts case asserts `toDecimal(mayPoint.amount).equals(toDecimal('-450.00'))` directly against the return value of getCategoryYearRanking — not a synthetic prop fed to a component — this is the DAL-boundary proof 83-VERIFICATION.md found missing.
    - tests/category-allocation-negative-domain.test.tsx's rendered-HTML assertion counts EXACTLY 1 occurrence of 'border-top:2px solid var(--total-allocation)', using data that actually passed through real Postgres and getCategoryYearRanking — not a synthetic prop.
    - `git diff lib/dal/dashboard.ts` shows changed lines ONLY inside getCategoryYearRanking's function body (approx. lines 1326-1387) — zero diff lines before line 1310, confirming getCategoryRanking and every other pre-existing function is untouched.
    - `node_modules/.bin/vitest run` (full suite) reports 0 failures, confirming no regression to 'in'/'out' behavior or the protected v2.8/v2.9 baselines (tests/pace-engine-lens-regression.test.ts, tests/reimbursement-regression.test.ts).
  </acceptance_criteria>
  <done>getCategoryYearRanking preserves the signed sum for the allocation direction while leaving 'in'/'out' byte-identical; a real-Postgres test proves a net-divestment month is negative at the DAL boundary and an exact-zero month stays unmarked; a second real-Postgres-backed test proves CategorySparkline renders the border marker for that exact value end-to-end, closing the CR-01 gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estimated sparkline bars never collapse to 0% height when pace is unavailable (WR-02)</name>
  <reversibility rating="reversible">Presentational-only fallback constant inside an existing component, no persisted contract, purely additive helper function — trivially revertable.</reversibility>
  <files>components/dashboard/category-sparkline.tsx, tests/category-sparkline.test.tsx</files>
  <read_first>
    components/dashboard/category-sparkline.tsx in full (resolveBarFillStyle's doc comment at lines 61-64 stating the "estimated ... never render a flat/zero-height bar" contract; the reference-magnitude computation at lines 99-105 — the exact code to change; parseAmount at lines 27-30 — reused as-is, not modified);
    tests/category-sparkline.test.tsx lines 104-121 ('an estimated-state bar is striped and normalized against estimatedHeightHint, never a flat zero bar' — the existing hint-PRESENT test whose exact numbers (150.00 hint, 75.00 covered amount, 100% height) must not change after this fix);
    .planning/phases/83-categories-list/83-VERIFICATION.md WR-02 section and 83-REVIEW.md WR-02 section (the exact failure mechanism and a suggested — not mandatory — fallback shape).
  </read_first>
  <behavior>
    - With estimatedHeightHint = '150.00' (unchanged from before), the estimated bar's reference is still exactly Number('150.00') = 150 — the existing passing test's exact numbers do not change.
    - With estimatedHeightHint = null and every covered/current point's amount also '0.00' (a category with 1 Covered Month and zero spend so far, 11 future 'estimated' months), the estimated bars' reference magnitudes are non-zero: the rendered height for those 11 bars is 100% (the fallback constant becomes the sole positive magnitude among all 12 points, so it normalizes to 100% of itself); the one true-zero 'covered' bar still renders at 0% (that IS real, verified data, correctly represented).
    - With estimatedHeightHint = null but at least one covered/current point has a nonzero amount, the estimated bars' reference falls back to that series' own observed max magnitude among non-estimated points (not the fixed constant) — proportional, not an arbitrary flat value.
  </behavior>
  <action>
    In components/dashboard/category-sparkline.tsx, add a module-level constant `const ESTIMATED_HEIGHT_FALLBACK = 1` (its exact value is inconsequential: whenever this fallback fires, it is the ONLY positive reference magnitude among referenceMagnitudes, so it always normalizes to 100% of itself, regardless of which specific positive number is chosen — document this in a one-line comment above the constant, citing resolveBarFillStyle's own "never flat/zero-height" contract at lines 61-64 that this change satisfies for the null-hint case). Add a function `function resolveEstimatedReference(points: CategorySparklinePoint[], pointStates: PointState[], estimatedHeightHint?: string | null): number` placed above CategorySparkline: if `estimatedHeightHint != null`, `return Number(estimatedHeightHint)` (byte-identical to today's behavior for this branch). Otherwise compute `observedMagnitudes` as `Math.abs(parseAmount(point.amount))` for every point whose `pointStates[index] !== 'estimated'` (skip 'estimated' entries — the DAL sets their amount to '0.00' by construction, per buildCategoryYearRankingData, so they carry no real signal), take `observedMax` as the max of those (0 if the array is empty), and `return observedMax > 0 ? observedMax : ESTIMATED_HEIGHT_FALLBACK`.

    Inside the `useBarRendering` branch (currently lines 99-105), compute `const estimatedReference = resolveEstimatedReference(points, pointStates, estimatedHeightHint)` once, BEFORE the `referenceMagnitudes` map, then inside that map replace the inline `state === 'estimated' ? Number(estimatedHeightHint ?? '0') : parseAmount(point.amount)` with `state === 'estimated' ? estimatedReference : parseAmount(point.amount)`.

    In tests/category-sparkline.test.tsx, add one new test() inside 'Path B — 4-state bar rendering': build 12 points all '0.00', pointStates = an array of 11 'estimated' entries plus 1 'covered' entry at index 0, estimatedHeightHint omitted (or explicitly null). Render via renderToStaticMarkup and assert `html.split('height:100%').length - 1 === 11` (exactly the 11 estimated bars, never the one true-zero covered bar at index 0) and that the striped `repeating-linear-gradient(135deg` pattern is still present (proving the bars stay visually 'estimated', not silently reclassified).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/category-sparkline.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/category-sparkline.test.tsx` passes, including the new WR-02 case AND the pre-existing 'estimatedHeightHint' case unchanged (same expected numbers as before this task).
    - `grep -n "function resolveEstimatedReference" components/dashboard/category-sparkline.tsx` returns exactly 1 match.
    - The new test's `height:100%` occurrence count assertion (in the test source, not a manual grep) equals exactly 11.
    - `yarn build` succeeds (no type errors from the new helper/constant).
  </acceptance_criteria>
  <done>An estimated (future) month's bar is never a flat 0%-height placeholder, whether or not the category's pace is available — proven by a new deterministic component test; the pre-existing pace-hint-present behavior is byte-identical to before this fix.</done>
</task>

<task type="auto">
  <name>Task 3: Preset-mode route builders stop dropping the allocation type (WR-01)</name>
  <reversibility rating="reversible">One-line branch mirroring an already-shipped pattern (buildYearModeSearch's own check) in a function with no new inputs — 'allocation' was already a valid DashboardCategoryFilters.type value; only the previously-silent-drop path changes.</reversibility>
  <files>lib/routes.ts, tests/dashboard-filters.test.ts</files>
  <read_first>
    lib/routes.ts lines 54-71 (buildYearModeSearch — the CORRECT pattern, `filters.type && filters.type !== 'out'`, already shipped for year mode) and lines 73-101 (buildDashboardCategoriesHref — the preset-mode branch at lines 87-89 needing the identical fix) and lines 150-181 (buildDashboardCategoryDetailHref — the preset-mode branch at lines 167-169 needing the same fix);
    tests/dashboard-filters.test.ts lines 188-266 (existing 'dashboard category list routes'/'dashboard category detail routes' describe blocks and their exact test() conventions to mirror — plain describe/test/expect, no mocking);
    .planning/phases/83-categories-list/83-VERIFICATION.md GAP 3/WR-01 section and 83-REVIEW.md WR-01 section.
  </read_first>
  <behavior>
    - buildDashboardCategoriesHref({ preset: 'last-3-months', type: 'allocation' }) (no year) emits '/dashboard/categories?preset=last-3-months&type=allocation' — the type is no longer silently dropped.
    - buildDashboardCategoryDetailHref(42, { preset: 'last-3-months', type: 'allocation' }) emits '/dashboard/categories/42?preset=last-3-months&type=allocation'.
    - Existing preset-mode behavior for 'in'/'out' is byte-identical to before: buildDashboardCategoriesHref({ type: 'in' }) still emits type=in; buildDashboardCategoriesHref({ type: 'out' }) still omits type ('out' stays the default in preset mode too).
  </behavior>
  <action>
    In lib/routes.ts, in buildDashboardCategoriesHref (currently line 87) replace `if (filters.type === 'in') { params.set('type', filters.type) }` with `if (filters.type && filters.type !== 'out') { params.set('type', filters.type) }` — byte-identical to buildYearModeSearch's own line 58-60. Apply the SAME replacement in buildDashboardCategoryDetailHref (currently line 167). Do not touch buildYearModeSearch itself (already correct) or any other branch in either function (preset/sort/lens logic stays exactly as-is).

    In tests/dashboard-filters.test.ts, add one new test() inside 'dashboard category list routes' asserting `buildDashboardCategoriesHref({ preset: 'last-3-months', type: 'allocation' })` toBe `/dashboard/categories?preset=last-3-months&type=allocation`, and one new test() inside 'dashboard category detail routes' asserting `buildDashboardCategoryDetailHref(42, { preset: 'last-3-months', type: 'allocation' })` toBe `/dashboard/categories/42?preset=last-3-months&type=allocation`. Do not remove or alter any existing test in this file.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/dashboard-filters.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/dashboard-filters.test.ts` passes, including the two new allocation-preset-mode cases.
    - `grep -n "filters.type === 'in'" lib/routes.ts` returns 0 matches (both preset-mode branches now use the widened check, matching buildYearModeSearch).
    - `grep -c "filters.type && filters.type !== 'out'" lib/routes.ts` returns at least 3 (buildYearModeSearch's existing one plus the two newly-fixed branches).
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>buildDashboardCategoriesHref/buildDashboardCategoryDetailHref's preset-mode branch never silently drops an explicit 'allocation' type, matching year-mode's already-correct behavior; IN-01 and IN-02 are explicitly deferred with recorded rationale, not silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated server / DAL layer -> Postgres | Same boundary as 83-01 — `getCategoryYearRanking` executes parameterized Drizzle SQL scoped to `verifySession()`'s `userId`; this plan changes only which SQL expression computes one already-selected column's value, never the WHERE clause's tenant scoping. |
| Test harness -> local Postgres | Same four-guard harness (`sparter_test`-suffixed, localhost-only, `TEST_DATABASE_URL`-isolated) already used by every other real-Postgres test in this repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-08 | Tampering | lib/dal/dashboard.ts (getCategoryYearRanking's amountSql branch) | low | accept | This is a correctness fix to an already-shipped, already-scoped query — no new input surface, no new endpoint, no change to the WHERE clause's userId scoping or to any parameterization. The only behavior change is which of two already-parameterized SQL fragments computes one output column, gated on the function's own `directionCode` argument (a fixed 3-member union, never user-supplied SQL). No new threat surface; recorded for traceability only. |
| T-83-09 | Information Disclosure (accepted design tradeoff, not a security defect) | components/dashboard/category-ranking-list.tsx (Totale column, read-only consumer of this plan's DAL change) | low | accept | Once the DAL fix lands, a category's yearly Totale can display as a genuinely negative amount for the allocation direction (net divestment > net deposits across the year) — intentional per D-09/CONTEXT.md's Risk Summary ("selling an ETF drags a month negative"), not an unintended disclosure: the figure is the authenticated user's own data, already visible to them via the sparkline and their own transaction list; no new party gains visibility, and no existing masking is removed. |

No package-manager installs in this plan; `T-83-SC` omitted (no new dependency — Drizzle, Decimal.js, Vitest, react-dom/server already in `package.json`).
</threat_model>

<verification>
- `node_modules/.bin/vitest run tests/categories-ranking-dal.test.ts tests/category-allocation-negative-domain.test.tsx tests/category-sparkline.test.tsx tests/dashboard-filters.test.ts` green (real Postgres, `yarn db:up`).
- `node_modules/.bin/vitest run` (full suite) reports 0 failures — including `tests/pace-engine-lens-regression.test.ts` (RETIRE-05) and `tests/reimbursement-regression.test.ts` (v2.8/v2.9 baselines), both unedited.
- `yarn build` succeeds.
- `yarn check:language` clean (no Italian in developer-facing code/comments added by this plan).
</verification>

<success_criteria>
- CR-01 closed: `getCategoryYearRanking` preserves sign for the allocation direction, proven end-to-end (real Postgres -> DAL -> rendered component) by a new test that does not bypass the DAL with a synthetic prop.
- WR-02 closed: an estimated (future) sparkline bar is never a flat 0%-height placeholder, whether or not pace is available, proven by a new deterministic component test.
- WR-01 closed: both Categories href builders treat `'allocation'` identically in preset mode and year mode.
- IN-01 and IN-02 are explicitly deferred with recorded rationale, not silently dropped.
- Every pre-existing test — including the two protected regression baselines — stays green and unedited.
</success_criteria>

<output>
Create `.planning/phases/83-categories-list/83-05-SUMMARY.md` when done
</output>
