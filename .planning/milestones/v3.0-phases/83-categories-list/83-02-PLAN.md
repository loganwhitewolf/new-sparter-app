---
phase: 83-categories-list
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/validations/dashboard.ts
  - lib/routes.ts
  - components/dashboard/dashboard-tab-nav.tsx
  - "app/(app)/dashboard/categories/[id]/page.tsx"
  - tests/dashboard-filters.test.ts
  - tests/dashboard-year-contract.test.ts
autonomous: true
requirements: [CLIST-03, CLIST-05, CLIST-07]

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A brand-new, additive CategoryYearDirectionSchema (z.enum(['out','in','allocation']).default('out')) and CategoryYearSortSchema (z.enum(['amount','projection']).default('amount')) exist in lib/validations/dashboard.ts alongside — never replacing — the existing DashboardPresetSchema/DashboardTypeSchema/DashboardSortSchema/DashboardFiltersSchema/parseDashboardFilters, which stay byte-identical because the category DETAIL page (Phase 84 scope) and the v2.8/v2.9 regression harness's captureAggregationSnapshot({filters:{preset:'last-month',type:'all',sort:'amount'}}) both still depend on the old shape unchanged."
    - "buildDashboardCategoriesHref and buildDashboardCategoryDetailHref (lib/routes.ts) gain an additive, optional `year?: number` field on DashboardCategoryFilters: when `filters.year` is provided the emitted href carries `?year=N` (and the caller is expected never to also pass `preset` in that mode); when `year` is omitted, both functions behave EXACTLY as before (preset/defaultPreset/defaultSort logic untouched) — proven by the pre-existing 'dashboard category list routes'/'dashboard category detail routes'/'Categories forwards ?lens=' describe blocks in tests/dashboard-filters.test.ts passing with ZERO edits (D-12)."
    - "buildDashboardTabHref (components/dashboard/dashboard-tab-nav.tsx) no longer reads or propagates `preset` at all — it reads `year`, `type`, `sort`, `lens` — so navigating Overview <-> Categories <-> Tags carries the selected year forward via the URL, satisfying CLIST-05's 'moving between tabs preserves it'; `lens` propagation (Phase 82 D-13) is completely unchanged."
    - "app/(app)/dashboard/categories/[id]/page.tsx's searchParams type gains `year?: string | string[]`, parsed via a simple, total (never-throwing) helper into `number | undefined`; its backHref now calls buildDashboardCategoriesHref({ year, type: filters.type, lens }) instead of the old { preset, type, defaultPreset, lens } shape — so a user who arrived at the detail page via a year-carrying row link returns to the list on the SAME year, not the list's own independent default (D-13, CLIST-07's coherence test: clicking a row and returning must not silently change the selected period)."
    - "The detail page's own DAL calls (getCategoryDetail, getCategoryDeviations) and its preset-based DashboardFilters control are completely untouched by this plan — only the back-link's href construction and the searchParams type change, matching the explicit scope fence ('any change to the category detail page beyond receiving the year in its URL' is out of scope for Phase 83)."
    - "A year value round-trips through buildDashboardCategoriesHref -> URL string -> Number(...) with no precision loss for any 4-digit calendar year (CLIST-05 boundary/precision probe) — proven by a dedicated test asserting the emitted query string and its re-parsed Number(...) form are identical for a representative set of years including the current year and a first/last-available year."
  artifacts:
    - "lib/validations/dashboard.ts (edited — additive: CategoryYearDirectionSchema, CategoryYearSortSchema, parseCategoryYearDirection, parseCategoryYearSort)"
    - "lib/routes.ts (edited — additive: DashboardCategoryFilters.year, year-mode branch in buildDashboardCategoriesHref/buildDashboardCategoryDetailHref)"
    - "components/dashboard/dashboard-tab-nav.tsx (edited — buildDashboardTabHref drops preset, adds year)"
    - "app/(app)/dashboard/categories/[id]/page.tsx (edited — year searchParam + backHref year-mode)"
    - tests/dashboard-year-contract.test.ts
  key_links:
    - "buildDashboardTabHref's new `year` read/set line -> DashboardTabNav's Overview/Categories/Tags Link hrefs (CLIST-05)"
    - "buildDashboardCategoriesHref({year, ...}) -> Categories list page's own self-links (Plan 83-04) and the detail page's back link (this plan)"
    - "app/(app)/dashboard/categories/[id]/page.tsx's new `year` searchParam -> backHref -> the list page, same year preserved (D-13, CLIST-07)"
  prohibitions:
    - statement: "MUST NOT change DashboardPresetSchema, DashboardTypeSchema, DashboardSortSchema, DashboardFiltersSchema, or parseDashboardFilters's existing behavior in any way — the category detail page and the shared regression test harness (tests/helpers/reimbursement-test-db.ts's captureAggregationSnapshot) depend on their exact current shape."
      category: correctness
      verification: test
    - statement: "MUST NOT alter buildDashboardCategoriesHref/buildDashboardCategoryDetailHref's output when `filters.year` is omitted — every existing describe block in tests/dashboard-filters.test.ts that never passes `year` must continue to pass with zero edits."
      category: correctness
      verification: test
---

<objective>
Build the year-based URL contract (D-12): additive year/direction/sort parsers, an additive
`year` mode on the Categories href builders that never disturbs the existing preset-based
behavior, `buildDashboardTabHref` propagating `year` instead of `preset` across all three
dashboard tabs (CLIST-05), and the category detail page's minimal "receive year, propagate it
back" edit (D-13, CLIST-07).

Purpose: every other piece of this phase (the list page, the row's detail link) depends on this
contract existing first — and it must be additive so the still-preset-based detail page and the
protected v2.8/v2.9 regression harness never regress.
Output: new parsers in `lib/validations/dashboard.ts`; an additive `year` mode in `lib/routes.ts`;
`buildDashboardTabHref` propagating `year`; the detail page receiving and forwarding `year`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-categories-list/83-CONTEXT.md
@lib/validations/dashboard.ts
@lib/routes.ts
@components/dashboard/dashboard-tab-nav.tsx
@app/(app)/dashboard/categories/[id]/page.tsx
@components/dashboard/overview/resolve-year.ts
@lib/utils/search-params.ts
@tests/dashboard-filters.test.ts
</context>

## Artifacts this phase produces

- `lib/validations/dashboard.ts` — additive exports: `CategoryYearDirectionSchema`, `CategoryYearDirection`, `CategoryYearSortSchema`, `CategoryYearSort`, `parseCategoryYearDirection(value)`, `parseCategoryYearSort(value)`.
- `lib/routes.ts` — additive: `DashboardCategoryFilters.year?: number` field; year-mode branch inside `buildDashboardCategoriesHref`/`buildDashboardCategoryDetailHref`.
- `components/dashboard/dashboard-tab-nav.tsx` — `buildDashboardTabHref` behavior change (drops `preset`, reads/sets `year`).
- `app/(app)/dashboard/categories/[id]/page.tsx` — `year` searchParam + backHref update.
- `tests/dashboard-year-contract.test.ts` — new file.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive year/direction/sort parsers + year-mode href builders (D-12)</name>
  <files>lib/validations/dashboard.ts, lib/routes.ts, tests/dashboard-filters.test.ts, tests/dashboard-year-contract.test.ts</files>
  <read_first>
    lib/validations/dashboard.ts (the full current file — DashboardPresetSchema/DashboardTypeSchema/DashboardSortSchema/DashboardFiltersSchema/parseDashboardFilters — the exact shape that must stay byte-identical);
    lib/routes.ts (the full current file — DashboardCategoryFilters type lines 27-42, buildDashboardCategoriesHref lines 44-67, buildDashboardCategoryDetailHref lines 116-142 — the exact preset-based logic that must stay reachable when `year` is omitted);
    tests/dashboard-filters.test.ts lines 185-309 (the exact existing assertions this task's additive change must NOT break — read every expected string literal);
    lib/utils/search-params.ts (firstTrimmed pattern, extractLensPassthrough/LensPassthrough — the "total function, never throws" convention to follow for the two new parsers).
  </read_first>
  <behavior>
    - parseCategoryYearDirection('allocation') -> 'allocation'; parseCategoryYearDirection('bogus') -> 'out' (default); parseCategoryYearDirection(undefined) -> 'out'; parseCategoryYearDirection(['in','out']) -> 'in' (first-element semantics, matching firstTrimmed's convention).
    - parseCategoryYearSort('projection') -> 'projection'; parseCategoryYearSort('deviation') -> 'amount' (default — 'deviation' is retired vocabulary and must fall back, never throw); parseCategoryYearSort(undefined) -> 'amount'.
    - buildDashboardCategoriesHref({ year: 2026, type: 'allocation', sort: 'projection' }) -> '/dashboard/categories?year=2026&type=allocation&sort=projection'.
    - buildDashboardCategoriesHref({ year: 2026 }) -> '/dashboard/categories?year=2026' (type/sort omitted when they equal their own defaults, matching the existing "omit defaults" convention).
    - buildDashboardCategoriesHref({ preset: 'last-3-months', type: 'in' }) (no `year` key at all) -> UNCHANGED from today: '/dashboard/categories?preset=last-3-months&type=in' — every existing test in the 'dashboard category list routes'/'dashboard category detail routes'/'Categories forwards ?lens=' describe blocks must still pass verbatim.
    - buildDashboardCategoryDetailHref(42, { year: 2026, type: 'out', lens }) -> '/dashboard/categories/42?year=2026&lens=...' (type omitted when default 'out'; lens preserved per D-13).
  </behavior>
  <action>
    In lib/validations/dashboard.ts, append (do not touch any existing line): `export const CategoryYearDirectionSchema = z.enum(['out', 'in', 'allocation']).default('out')`, `export type CategoryYearDirection = z.infer<typeof CategoryYearDirectionSchema>`, `export const CategoryYearSortSchema = z.enum(['amount', 'projection']).default('amount')`, `export type CategoryYearSort = z.infer<typeof CategoryYearSortSchema>`. Add `export function parseCategoryYearDirection(value: string | string[] | undefined): CategoryYearDirection` — extract the first element if an array, `CategoryYearDirectionSchema.safeParse(raw).success ? raw as CategoryYearDirection : 'out'`. Add `export function parseCategoryYearSort(value: string | string[] | undefined): CategoryYearSort` — identical pattern, default `'amount'`.

    In lib/routes.ts, add `year?: number` to the existing `DashboardCategoryFilters` type (do not remove any existing field — this is additive). In `buildDashboardCategoriesHref`, ADD a branch at the top: `if (filters.year !== undefined) { const params = new URLSearchParams(); params.set('year', String(filters.year)); if (filters.type && filters.type !== 'out') params.set('type', filters.type); if (filters.sort && filters.sort !== 'amount') params.set('sort', filters.sort); if (filters.lens) params.set('lens', filters.lens); const search = params.toString(); return APP_ROUTES.dashboardCategories + (search ? \`?${search}\` : '') }` — then fall through to the EXISTING preset-based body completely unchanged for the `filters.year === undefined` case. Apply the identical additive branch to `buildDashboardCategoryDetailHref`, using `dashboardCategoryDetail(id)` as the base path instead of `APP_ROUTES.dashboardCategories`. Note: the year-mode branch's `type` union naturally includes `'allocation'` at the call-site type level once Plan 83-04 imports `CategoryYearDirection` for its own local filter type — this plan does not need to widen `DashboardCategoryFilters.type` itself, since the additive branch reads `filters.type` as a plain string comparison, not a narrowed union.

    In tests/dashboard-year-contract.test.ts (new file), add describe('parseCategoryYearDirection / parseCategoryYearSort (D-09, D-08)') covering the &lt;behavior&gt; cases above via `test`/`expect`. Add describe('buildDashboardCategoriesHref / buildDashboardCategoryDetailHref — year mode (D-12, CLIST-05)') covering the year-mode href cases above, PLUS a round-trip precision case: for years [2024, 2025, 2026], assert `Number(new URL('http://x' + buildDashboardCategoriesHref({ year })).searchParams.get('year'))` equals the original `year` exactly (CLIST-05 precision probe). Do NOT touch tests/dashboard-filters.test.ts in this task — verify by running it that it still passes unmodified (it exercises only the no-`year` code path).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/dashboard-year-contract.test.ts tests/dashboard-filters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/dashboard-year-contract.test.ts` passes.
    - `node_modules/.bin/vitest run tests/dashboard-filters.test.ts` passes with ZERO diff (`git diff --stat tests/dashboard-filters.test.ts` shows no changes after this task).
    - `grep -c "year?: number" lib/routes.ts` returns 1 on the `DashboardCategoryFilters` type.
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>Additive direction/sort parsers exist and are total (never throw); buildDashboardCategoriesHref/buildDashboardCategoryDetailHref support a year-mode without disturbing the preset-mode default; every pre-existing test passes unedited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildDashboardTabHref year propagation + detail page year receipt/back-link (CLIST-05, D-13, CLIST-07)</name>
  <files>components/dashboard/dashboard-tab-nav.tsx, "app/(app)/dashboard/categories/[id]/page.tsx", tests/dashboard-filters.test.ts</files>
  <read_first>
    components/dashboard/dashboard-tab-nav.tsx (the full current file — buildDashboardTabHref's exact preset/type/sort/lens read+set lines);
    tests/dashboard-filters.test.ts lines 103-183 (the existing 'buildDashboardTabHref' describe block — every assertion referencing `preset` propagation must be rewritten to assert its absence and `year`'s presence instead, matching the exact editing precedent set by Phase 82 Plan 82-02's tag-drop rewrite);
    app/(app)/dashboard/categories/[id]/page.tsx (the full current file — Props['searchParams'] type lines 34-42, parseCategoryDetailFilters lines 54-65, backHref construction lines 153-158 — the exact call site to update);
    components/dashboard/overview/resolve-year.ts (read only — NOT called from the detail page; referenced here purely so the executor understands how Overview already treats `year` as a raw string, for consistency of parsing style).
  </read_first>
  <behavior>
    - buildDashboardTabHref('/dashboard/categories', new URLSearchParams({ preset: 'last-3-months', type: 'in' })) no longer includes `preset=` in its result (preset is dead to the tab nav now).
    - buildDashboardTabHref('/dashboard/overview', new URLSearchParams({ year: '2026', type: 'in' })) -> '/dashboard/overview?year=2026&type=in'.
    - buildDashboardTabHref with both `year` and `lens` present in the current params returns a URL carrying both, unchanged relative to Phase 82's lens-propagation guarantee.
    - Visiting /dashboard/categories/42?year=2026&type=in produces a backHref of '/dashboard/categories?year=2026&type=in' (never `preset=`).
    - Visiting /dashboard/categories/42 with NO `year` in the URL produces a backHref with no `year` param — the list page resolves its own default year (Plan 83-04's job), never a stale/guessed one.
  </behavior>
  <action>
    In components/dashboard/dashboard-tab-nav.tsx, inside buildDashboardTabHref: remove the `const preset = searchParams.get('preset')` line and its `if (preset) { params.set('preset', preset) }` block entirely (D-12 — the tab nav must never propagate the retired preset param). Add `const year = searchParams.get('year')` and `if (year) { params.set('year', year) }`, positioned first (before type/sort/lens, matching the field's semantic priority as the shared cross-tab parameter). Leave `type`, `sort`, and `lens` read/set lines completely untouched.

    In app/(app)/dashboard/categories/[id]/page.tsx: add `year?: string | string[]` to `Props['searchParams']`'s type (alongside the existing `preset`/`period`/`type`/`lens` fields — do not remove those, since `parseCategoryDetailFilters`/`parseDashboardFilters` still need them for the page's OWN preset-based DAL calls, untouched by this plan). Add a small local helper (or inline expression) to resolve the raw year: `const rawYear = Array.isArray(query.year) ? query.year[0] : query.year; const requestedYear = rawYear ? Number(rawYear) : undefined; const year = Number.isFinite(requestedYear) ? requestedYear : undefined` — total, never throws on garbage input. Change the `backHref` construction (currently `buildDashboardCategoriesHref({ preset: filters.preset, type: filters.type, defaultPreset: CATEGORY_DETAIL_DEFAULT_PRESET, lens })`) to: `buildDashboardCategoriesHref({ year, type: filters.type, lens })` — this now uses lib/routes.ts's additive year-mode branch when `year` is defined, and its EXISTING preset-based fallback (still reading `filters.preset`... note: since this call site no longer passes `preset`/`defaultPreset` at all, when `year` is undefined the year-mode branch is skipped AND the preset-based branch receives an EMPTY `filters.preset`/`defaultPreset` — verify this still produces a sane bare '/dashboard/categories' href by re-reading lib/routes.ts's fallback logic; if `filters.preset`/`defaultPreset` end up undefined, the existing `if (filters.preset && filters.preset !== defaultPreset)` guard already handles the undefined case safely, producing no `preset=` param). Leave every other line of this page (its own `parseCategoryDetailFilters` call, `getCategoryDetail`/`getCategoryDeviations` calls, `DashboardFilters` rendering) completely untouched — Phase 84 scope.

    Update tests/dashboard-filters.test.ts's `describe('buildDashboardTabHref')` block (lines 103-183): rewrite the two tests asserting preset propagation ('preserves only dashboard filter params across tabs', line 104; 'preserves sort across the tab nav builder', line 125) to use `year` instead of `preset` in both the input URLSearchParams and the expected output string (e.g. `new URLSearchParams({ year: '2026', type: 'in', page: '2' })` -> expect `'/dashboard/categories?year=2026&type=in'`). Leave the tag-drop and lens-forwarding tests (lines 132-182) untouched — they are orthogonal to this change and must keep passing.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/dashboard-filters.test.ts -t "buildDashboardTabHref"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "searchParams.get('preset')" components/dashboard/dashboard-tab-nav.tsx` returns 0.
    - `grep -c "searchParams.get('year')" components/dashboard/dashboard-tab-nav.tsx` returns 1.
    - `grep -c "year?: string | string\[\]" "app/(app)/dashboard/categories/[id]/page.tsx"` returns 1.
    - `node_modules/.bin/vitest run tests/dashboard-filters.test.ts -t "buildDashboardTabHref"` passes with the updated year-based assertions.
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>buildDashboardTabHref propagates year (never preset) across all three tabs while lens stays unchanged; the detail page receives an optional year from its URL and forwards it into its own back link, never disturbing its own preset-based DAL calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-supplied URL search params (`?year=`, `?type=`) -> server page component -> href construction | Same-user, same-tenant navigation only; the concern is a malformed/garbage `year` value crashing a page render, not cross-user data leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-03 | Denial of Service | app/(app)/dashboard/categories/[id]/page.tsx (year parsing) | low | mitigate | The new year-parsing expression is a total function (`Number(rawYear)` guarded by `Number.isFinite`) that never throws on garbage input (e.g. `?year=abc` or `?year=<script>`) — it degrades to `undefined`, falling through to the existing safe preset-based backHref path. |
| T-83-04 | Tampering | lib/routes.ts (buildDashboardCategoriesHref/buildDashboardCategoryDetailHref year mode) | low | accept | A crafted `?year=` value only affects which year's data the SAME authenticated user's own list/detail pages request — no cross-tenant read is possible since `getCategoryYearRanking`/`getCategoryDetail` both independently re-scope every query to `verifySession()`'s userId regardless of the requested year. |

No package-manager installs in this plan; `T-83-SC` omitted.
</threat_model>

<verification>
- `node_modules/.bin/vitest run tests/dashboard-year-contract.test.ts tests/dashboard-filters.test.ts` green.
- `yarn build` succeeds.
- `yarn check:language` clean.
</verification>

<success_criteria>
- Additive year/direction/sort parsers exist, total, never throwing.
- buildDashboardCategoriesHref/buildDashboardCategoryDetailHref support a year mode without disturbing any pre-existing preset-based test.
- buildDashboardTabHref propagates year (never preset) across Overview/Categories/Tags.
- The detail page receives and forwards year in its back link without touching its own preset-based DAL calls.
</success_criteria>

<output>
Create `.planning/phases/83-categories-list/83-02-SUMMARY.md` when done
</output>
