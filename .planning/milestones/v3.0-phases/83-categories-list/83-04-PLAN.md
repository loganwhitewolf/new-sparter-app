---
phase: 83-categories-list
plan: 04
type: execute
wave: 2
depends_on: ["83-01", "83-02", "83-03"]
files_modified:
  - "app/(app)/dashboard/categories/page.tsx"
  - components/dashboard/category-year-select.tsx
  - components/dashboard/category-ranking-list.tsx
  - tests/category-ranking-list.test.tsx
  - tests/categories-list-component.test.tsx
  - tests/category-detail-link.test.ts
autonomous: true
requirements: [CLIST-01, CLIST-02, CLIST-03, CLIST-04, CLIST-05, CLIST-06, CLIST-07]

estimate:
  tokens: 95000
  raw_tokens: 47500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Categories list page reads year + direction + sort from the URL exclusively (no ?preset= is ever parsed, no fallback) — resolveYear(params.year, years) picks the year, parseCategoryYearDirection/parseCategoryYearSort (Plan 83-02) pick direction/sort — matching D-01/D-02/D-12's 'year is the container, list has no month selection' exactly."
    - "The direction filter offers exactly three options (Uscite/Entrate/Accantonamenti) in that fixed order, each an always-enabled link that swaps BOTH the ranked data and resolveCategoryDirectionCopy's copy/colour set together — a category never appears under the wrong direction's vocabulary (D-09, D-11, CLIST-04)."
    - "The sort toggle offers Totale (default) and Proiezione; Proiezione is disabled with a stated reason (title attribute) whenever the year's pace-eligible Covered Month count is below MIN_COVERED_MONTHS_FOR_PACE (2) — derived from getCoveredMonthsInYear(year) + isPartialMonth, imported directly from Phase 82's exported engine, never re-implemented — and is NEVER hidden (CLIST-03, D-08, D-15, UI-SPEC E5)."
    - "Each row renders exactly the five D-04 fields — name, year total, % of total, 12-month sparkline, year-end projection — with the projection visually subordinate (smaller weight, muted colour) and explicitly labelled 'A questo passo', never a second unlabelled total (CLIST-01, CLIST-02, D-06). No previous-year comparison column is ever rendered on the list — that comparison lives only in the Phase 84 detail page's summary column (D-03); the five fields above are exhaustive, not a subset a future task might extend on this page."
    - "When a category's projection is null (D-15), the row renders NO projection label+value pair at all — not an em-dash, not a placeholder — while the grid's 5th column stays reserved (empty) for layout stability against CategoryYearRankingSkeleton (CLIST-02, CLIST-06)."
    - "Selecting 'Proiezione' as the sort reorders rows by projection descending, falling back to amount for any row whose projection is null (never crashing, never silently reverting to amount-only order for the whole list) — ties broken identically to the DAL's own name-then-id rule (CLIST-03)."
    - "Each row's link to the category detail is built via buildDashboardCategoryDetailHref(category.id, { year, type: direction, lens }) — carrying the SAME year the row's own total was computed from — so the coherence test 'clicking a row must not change the numbers' holds by construction (D-02, D-13, CLIST-07)."
    - "buildDashboardTabHref propagates the resolved year when the user navigates from Categories to Overview or Tags, and Overview navigating to Categories with ?year=2025 lands the list on 2025 — proven by an integration-level test asserting the round trip through both buildDashboardTabHref and the page's own resolveYear call (CLIST-05)."
    - "With exactly 1 raw Covered Month in the selected year, the list renders the certain figures (total, share, a one-point sparkline circle) for every category plus the CategoryCoverageNudge panel below the list, appearing together with the resolved data (never during the skeleton) — no projection anywhere on the page for that year (CLIST-06, D-14, UI-SPEC E8)."
    - "A year with zero transactions in the account at all (resolveYear returns null because getYearsWithData('cassa') is empty) renders a single dedicated empty state with an import call-to-action, replacing the entire page including the direction/sort controls — distinct from the per-direction empty state, which only replaces the list when the selected YEAR has data but the selected DIRECTION does not."
  artifacts:
    - "app/(app)/dashboard/categories/page.tsx (rewritten)"
    - components/dashboard/category-year-select.tsx
    - "components/dashboard/category-ranking-list.tsx (rewritten)"
    - tests/category-ranking-list.test.tsx
    - tests/categories-list-component.test.tsx
    - tests/category-detail-link.test.ts
  key_links:
    - "getCoveredMonthsInYear(year) [Phase 82, re-used free via cache()] -> page-level sort-toggle disablement decision AND CategoryCoverageNudge's coveredMonthCount, computed once, threaded down, never re-queried"
    - "getCategoryYearRanking(year, direction) [Plan 83-01] -> CategoryYearRankingList's row rendering, sort-by-projection reorder, and href construction"
    - "resolveCategoryDirectionCopy(direction) [Plan 83-03] -> page subheading, row share label, empty-state copy — one call site, never duplicated"
    - "CategorySparkline's pointStates/estimatedHeightHint props [Plan 83-03] <- each row's sparkline + item.pace"
  prohibitions:
    - statement: "MUST NOT parse or read searchParams.preset anywhere in app/(app)/dashboard/categories/page.tsx — pin-by-construction, matching Phase 82's precedent for ?lens= on this same page (D-12)."
      category: transparency
      verification: test
    - statement: "MUST NOT render a numeric projection value, an em-dash, or any placeholder in a row when that category's projection is null — the absence must be structural (no DOM node for that pair), never a rendered '—' string."
      category: correctness
      verification: test
---

<objective>
Assemble the real Categories list page and its row component on the year+direction contract:
year/direction/sort resolution with no preset fallback, the three-way direction filter, the
Totale/Proiezione sort toggle with its D-15 disablement, the five-field row with subordinate
projection, sort-by-projection reordering, the single-Covered-Month nudge, and the whole-year
empty state — wired against Plan 83-01's DAL, Plan 83-02's href/nav contract, and Plan 83-03's
sparkline/skeleton/nudge/copy primitives.

Purpose: this is where all seven CLIST requirements become observable and testable together — the
integration point the rest of the phase was built to support.
Output: `app/(app)/dashboard/categories/page.tsx` and `components/dashboard/category-ranking-list.tsx`
fully rewritten; a new `category-year-select.tsx`; the full phase regression suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-categories-list/83-CONTEXT.md
@.planning/phases/83-categories-list/83-UI-SPEC.md
@.planning/phases/83-categories-list/83-01-SUMMARY.md
@.planning/phases/83-categories-list/83-02-SUMMARY.md
@.planning/phases/83-categories-list/83-03-SUMMARY.md
@app/(app)/dashboard/categories/page.tsx
@components/dashboard/category-ranking-list.tsx
@app/(app)/dashboard/overview/page.tsx
@components/dashboard/overview/overview-header.tsx
@components/dashboard/overview/resolve-year.ts
@lib/dal/overview.ts
@lib/dal/covered-months.ts
@lib/services/pace-and-projection.ts
@tests/category-ranking-list.test.tsx
</context>

## Artifacts this phase produces

- `app/(app)/dashboard/categories/page.tsx` — fully rewritten default export; local components `DirectionFilter`, `SortToggle`, `CategoryRankingContent`, `NoYearsEmptyState`.
- `components/dashboard/category-year-select.tsx` — new `'use client'` component, `{ year: number; years: string[] }` props, mirrors `OverviewHeader`'s `<Select>`/`router.replace` pattern.
- `components/dashboard/category-ranking-list.tsx` — fully rewritten; new `Props` shape: `{ data: CategoryYearRankingItem[]; year: number; direction: 'in' | 'out' | 'allocation'; sort: CategoryYearSort; lens?: LensPassthrough; copy: CategoryDirectionCopy }`.
- Three test files (one rewritten, two new).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Categories page — year/direction/sort resolution, direction filter, sort toggle, year select, empty state (D-01, D-12, CLIST-03, CLIST-04, CLIST-05)</name>
  <reversibility rating="costly">Replaces this page's entire searchParams contract (preset-based -> year-based). Reversible in code (no data/migration involved) but every bookmarked ?preset= URL to this page silently stops mattering the moment this task lands — the accepted, deliberate cost of D-12, already signalled in Plan 83-02.</reversibility>
  <files>"app/(app)/dashboard/categories/page.tsx", components/dashboard/category-year-select.tsx</files>
  <read_first>
    "app/(app)/dashboard/categories/page.tsx" (the full current file — every symbol this task replaces: CATEGORIES_DEFAULT_PRESET/CATEGORIES_DEFAULT_SORT/categoryTypeOptions lines 17-22, parseCategoryDashboardFilters lines 52-64, SortToggle lines 66-108, CategoryRankingContent lines 110-133, the default export lines 135-173);
    app/(app)/dashboard/overview/page.tsx in full (the exact eager-await-then-Suspense structure to mirror: verifySession -> searchParams -> getYearsWithData -> resolveYear -> null check -> Suspense-wrapped inner async component);
    components/dashboard/overview/overview-header.tsx (the exact `<Select>` + `router.replace` + sessionStorage-restore pattern for the year selector — adapt, do not import this file itself, since it also renders LensSwitch/nudge slots this page does not have);
    lib/dal/overview.ts (getYearsWithData(lens): Promise<string[]> — call with 'cassa' only, Categories has no lens);
    lib/dal/covered-months.ts (getCoveredMonthsInYear(year) — the free-via-cache() call this page makes directly, independent of getCategoryYearRanking's own internal call to the same function);
    lib/services/pace-and-projection.ts (isPartialMonth, MIN_COVERED_MONTHS_FOR_PACE — imported directly here to decide sort-toggle disablement WITHOUT waiting for the ranking query);
    lib/validations/dashboard.ts (parseCategoryYearDirection, parseCategoryYearSort — Plan 83-02's new exports);
    lib/routes.ts (buildDashboardCategoriesHref's year-mode branch — Plan 83-02's addition);
    lib/utils/search-params.ts (extractLensPassthrough — unchanged, still forwarded per D-13 even though Categories never resolves it to an aggregation).
  </read_first>
  <behavior>
    - Visiting /dashboard/categories with no query params on an account with data in 2025/2026 lands on the most recent year with data (or the current calendar year if it has data), direction 'out', sort 'amount' — matching resolveYear's existing documented fallback order.
    - Visiting /dashboard/categories?year=2025&type=allocation&sort=projection resolves year=2025, direction='allocation', sort='projection' exactly, with NO reference anywhere in the render to a `preset` value.
    - On an account with zero transactions ever (getYearsWithData('cassa') returns []), the page renders NoYearsEmptyState only — no direction filter, no sort toggle, no year select.
    - The sort toggle's Proiezione option carries `aria-disabled="true"` and a `title` attribute stating a second imported month is needed whenever the selected year's Covered Months, filtered to exclude the current/partial month, number fewer than 2 — and is a plain `<span>`, never a `<Link>`, in that state.
    - The direction filter always renders exactly 3 links in the fixed order Uscite/Entrate/Accantonamenti, each pointing at buildDashboardCategoriesHref({ year, type: option, sort, lens }).
  </behavior>
  <action>
    Rewrite app/(app)/dashboard/categories/page.tsx from scratch, keeping the file's existing top-level import style. Replace `Props['searchParams']`'s type with `{ year?: string | string[]; type?: string | string[]; sort?: string | string[]; lens?: string | string[] }` — no `preset`/`period` keys anywhere in this type or in any parsing call in this file (D-12 pin-by-construction). Delete `CATEGORIES_DEFAULT_PRESET`, `CATEGORIES_DEFAULT_SORT`, `categoryTypeOptions`, `parseCategoryDashboardFilters`, the old `SortToggle`, `CategoryDashboardFilters` type, and the `DashboardFilters` import/render entirely.

    In the default export: `await verifySession()`; `const params = await searchParams`; `const lens = extractLensPassthrough(params.lens)`; `const years = await getYearsWithData('cassa')`; `const year = resolveYear(Array.isArray(params.year) ? params.year[0] : params.year, years)`; `const direction = parseCategoryYearDirection(params.type)`; `const sort = parseCategoryYearSort(params.sort)`. If `year === null`, return a local `<NoYearsEmptyState />` component (heading "Nessuna transazione registrata", body "Importa un estratto conto per vedere le tue categorie qui.", a `<Link>` to `APP_ROUTES.import`) — the whole-page empty state, rendered instead of everything else.

    Otherwise: `const coveredMonths = await getCoveredMonthsInYear(year)`; `const paceEligibleCount = coveredMonths.filter((m) => !isPartialMonth(m.yearMonth)).length`; `const projectionSortAvailable = paceEligibleCount >= MIN_COVERED_MONTHS_FOR_PACE`. Render, in order: a header row with "Categorie" heading + subheading (from `resolveCategoryDirectionCopy(direction).pageSubheading`, with the literal `{year}` token replaced by `String(year)`) + `<CategoryYearSelect year={year} years={years} />`; a `DirectionFilter` local component rendering the 3 fixed-order links (Uscite='out', Entrate='in', Accantonamenti='allocation'), each via `buildDashboardCategoriesHref({ year, type: option.value, sort, lens })`, active state via `direction === option.value`; a `SortToggle` local component with exactly two options (`{ value: 'amount', label: 'Totale' }`, `{ value: 'projection', label: 'Proiezione' }`), rendering a `<Link>` for 'amount' always, and for 'projection' either a `<Link>` (when `projectionSortAvailable`) or a `<span aria-disabled="true" title="Serve un secondo mese importato per calcolare la proiezione.">Proiezione</span>` (when not) — hrefs via `buildDashboardCategoriesHref({ year, type: direction, sort: option.value, lens })`; then `<Suspense fallback={<CategoryYearRankingSkeleton />}><CategoryRankingContent year={year} direction={direction} sort={sort} lens={lens} coveredMonthCount={coveredMonths.length} /></Suspense>`.

    `CategoryRankingContent` (async, inside this same file): calls `getCategoryYearRanking(year, direction)`, renders `<CategoryYearRankingList data={data} year={year} direction={direction} sort={sort} lens={lens} copy={resolveCategoryDirectionCopy(direction)} />`, and — matching UI-SPEC E8's "appears with the resolved list, never during skeleton" — renders `{coveredMonthCount === 1 ? <CategoryCoverageNudge coveredMonthCount={1} year={year} /> : null}` immediately below the list, inside this same Suspense-resolved component (never above it, never outside the Suspense boundary).

    Create components/dashboard/category-year-select.tsx: `'use client'`; Props `{ year: number; years: string[] }`; body mirrors overview-header.tsx's `<Select value={String(year)} onValueChange={update}>` block exactly (`useSearchParams`/`useRouter`/`usePathname`, `params.set('year', next)`, `router.replace(..., { scroll: false })`) — omit the sessionStorage-restore `useEffect` (that is an Overview-specific persistence feature, not part of this phase's locked decisions; do not invent it here).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/categories-list-component.test.tsx -t "page"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "params.preset\|searchParams.preset\|CATEGORIES_DEFAULT_PRESET" "app/(app)/dashboard/categories/page.tsx"` returns 0.
    - `grep -c "resolveYear(" "app/(app)/dashboard/categories/page.tsx"` returns at least 1.
    - `grep -c "aria-disabled" "app/(app)/dashboard/categories/page.tsx"` returns at least 1 (the disabled Proiezione span).
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>The page resolves year/direction/sort from the URL with zero preset dependency, offers all three directions and both sort options (with the correct disablement), and renders the account-wide empty state when there is no data at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Category row — five D-04 fields, subordinate projection, sort-by-projection reorder, year-carrying href</name>
  <files>components/dashboard/category-ranking-list.tsx, tests/category-ranking-list.test.tsx</files>
  <read_first>
    components/dashboard/category-ranking-list.tsx (the full current file — this task replaces every line: Props lines 14-24, deviationSortKey/compareItems/getDeviationValue lines 26-57, the component body lines 73-174, the DeviationBadge render lines 156-161, the href construction lines 102-107);
    tests/category-ranking-list.test.tsx (the full current file — every test here asserts on the OLD DeviationBadge/preset contract and must be rewritten to the new contract, not merely patched);
    .planning/phases/83-categories-list/83-UI-SPEC.md `## Row Structure and Layout` (exact 5-column desktop / 3-column mobile grid, column widths, the Totale/Proiezione label+value block) and `## Typography` (projection hierarchy: both figures at 16px, projection weight 500 + muted-fg, total weight 600 + foreground, inner `<strong>` of the projection at weight 600 + foreground);
    lib/dal/dashboard.ts (CategoryYearRankingItem/CategoryYearSparklinePoint shapes — Plan 83-01's exact exported types, read them directly rather than assuming);
    components/dashboard/category-sparkline.tsx (the widened Props from Plan 83-03 — pointStates/estimatedHeightHint — the exact prop names this row must pass through).
  </read_first>
  <behavior>
    - A row with amount '1200.00', percentage 40, projection '1500.00' renders: the name, "40% del totale" (or the direction-scoped share label from `copy.shareLabel`), a 12-bar sparkline, "Totale" label + the formatted amount at weight 600, and "A questo passo" label + the formatted projection with its inner value at weight 600 — both money figures present, visually distinct weights.
    - A row with projection: null renders NO "A questo passo" label or value anywhere in its DOM — asserted by absence of both the label text and any adjacent em-dash/placeholder string.
    - The row's Link href for a row in year=2026, direction='allocation' equals buildDashboardCategoryDetailHref(category.id, { year: 2026, type: 'allocation', lens }) — asserted against the exact string lib/routes.ts's year-mode branch produces.
    - Sorting by 'projection' with data [{amount:'100', projection:'50'}, {amount:'50', projection:'200'}, {amount:'80', projection:null}] orders as [projection 200, projection 50, projection-null falls back to its own amount 80] — never crashes on the null entry, never silently reverts the WHOLE list to amount order.
    - With zero rows, the component renders the per-direction empty state (heading/body from `copy.emptyStateHeading`/`copy.emptyStateBody`), never a blank container.
  </behavior>
  <action>
    Rewrite components/dashboard/category-ranking-list.tsx. New `type Props = { data: CategoryYearRankingItem[]; year: number; direction: 'in' | 'out' | 'allocation'; sort: CategoryYearSort; lens?: LensPassthrough; copy: CategoryDirectionCopy }` (import `CategoryYearRankingItem`/`CategoryYearSparklinePoint` from `@/lib/dal/dashboard`, `CategoryYearSort` from `@/lib/validations/dashboard`, `CategoryDirectionCopy` from `@/lib/services/category-direction-copy`). Delete `deviationSortKey`/`compareItems`/`getDeviationValue` and the `DeviationBadge`/`DashboardPreset`/`DashboardSort`/`DeviationData`/`DeviationResult` imports entirely — none of this component's old sort/deviation machinery survives.

    Add a pure, exported `compareByProjection(a: CategoryYearRankingItem, b: CategoryYearRankingItem): number` — `const aValue = toDecimal(a.projection ?? a.amount); const bValue = toDecimal(b.projection ?? b.amount); return bValue.comparedTo(aValue)` (falls back to the row's own amount when its projection is null — never crashes, never drops the row out of the list). Compute `const sortedData = sort === 'projection' ? [...data].sort(compareByProjection) : data` (the 'amount' case trusts the DAL's own descending order, matching the established convention).

    If `sortedData.length === 0`, render the per-direction empty state using `copy.emptyStateHeading`/`copy.emptyStateBody` (structurally identical to the existing empty-state block, just direction-scoped copy instead of the hardcoded "Nessuna categoria nel periodo selezionato" string).

    Otherwise render an `<ol>` of rows, each: rank badge (unchanged from today); name Link via `buildDashboardCategoryDetailHref(category.id, { year, type: direction, lens })` (replacing the old `{preset, type, defaultPreset, lens}` call); metadata line `{movementLabel(category.count)} · {category.percentage}${copy.shareLabel's numeric-suffix form}` — read `copy.shareLabel` as the FULL suffix string (e.g. "% del totale") and interpolate the percentage before it, per UI-SPEC's Row Metadata table; the existing % share bar (colour now resolved 3-way: `direction === 'in' ? 'bg-[var(--total-in)]' : direction === 'allocation' ? 'bg-[var(--total-allocation)]' : 'bg-[var(--total-out)]'`); `<CategorySparkline points={category.sparkline.map(p => ({ month: p.month, label: p.label, amount: p.amount }))} type={direction} pointStates={category.sparkline.map(p => p.state)} estimatedHeightHint={category.pace} label={\`Andamento mensile ${category.name}\`} />`; a "Totale" label (12px/500/muted) + `formatAmount(category.amount)` (16px/600/foreground); THEN, only `{category.projection !== null ? (<div>...</div>) : null}` — when non-null, "A questo passo" label (12px/500/muted) + the formatted projection value where the outer text is weight 500/muted-fg and an inner `<strong>` wrapping the formatted number is weight 600/foreground, per UI-SPEC's exact typography split — when null, render NOTHING for this pair (not even an empty reserved div beyond the row's own natural grid column, which stays empty by the grid's own column definition).

    Rewrite tests/category-ranking-list.test.tsx from scratch (delete every existing test — they assert on retired DeviationBadge/preset behavior). Cover every &lt;behavior&gt; case above via `renderToStaticMarkup`, keeping the existing `vi.mock('next/link', ...)` boilerplate. Build a `baseCategory: CategoryYearRankingItem` fixture matching the new shape (12-entry sparkline with `state` on every point, `projection`/`pace` fields).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/category-ranking-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/category-ranking-list.test.tsx` passes.
    - `grep -c "DeviationBadge" components/dashboard/category-ranking-list.tsx` returns 0.
    - `grep -c "A questo passo" components/dashboard/category-ranking-list.tsx` returns at least 1.
    - `grep -c "buildDashboardCategoryDetailHref(category.id, { year" components/dashboard/category-ranking-list.tsx` returns at least 1 (allowing for exact whitespace variance — verify by reading the source, not a brittle grep alone).
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>Each row renders exactly the five D-04 fields with the projection visually subordinate and structurally absent (not placeholder'd) when null; sort-by-projection never crashes on a null projection; every row's href carries the same year the row's own total was computed from.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase regression — RETIRE-05, cross-plan integration tests, CLIST-05/07 coherence proof, language check</name>
  <files>tests/categories-list-component.test.tsx, tests/category-detail-link.test.ts</files>
  <read_first>
    tests/pace-engine-lens-regression.test.ts (re-run only, confirm zero diff);
    tests/dashboard-year-contract.test.ts, tests/dashboard-filters.test.ts (Plan 83-02's contract);
    tests/categories-ranking-dal.test.ts (Plan 83-01's DAL proof);
    "app/(app)/dashboard/categories/page.tsx" and components/dashboard/category-ranking-list.tsx (this plan's own Task 1/2 output — read the final committed state, not the design intent, before writing integration assertions against it);
    components/dashboard/dashboard-tab-nav.tsx (buildDashboardTabHref — the CLIST-05 round-trip this task proves at the integration level).
  </read_first>
  <behavior>
    - buildDashboardTabHref('/dashboard/categories', new URLSearchParams({ year: '2025' })) -> a URL that, when its `year` param is fed through resolveYear against a `years` list containing '2025', resolves back to 2025 — the full CLIST-05 round trip proven as one assertion chain, not two independently-passing halves that were never chained together.
    - buildDashboardCategoryDetailHref(7, { year: 2025, type: 'allocation' }) round-trips: the emitted href's `year` query param, re-parsed via `Number(...)`, equals 2025 exactly (CLIST-07's href half — the "same total" half is the D-07 structural guarantee already proven in Plan 83-01, cross-referenced here, not re-derived).
    - A source-inspection assertion confirms "app/(app)/dashboard/categories/page.tsx" contains no substring "DeviationBadge", "getCategoryDeviations", or "?preset=" literal — the list has fully stopped consuming the retired machinery for CLIST-01..07's own surfaces (RETIRE-01/02 deletion itself remains Phase 84's job; this only proves Phase 83's OWN new code never re-introduces a dependency on it).
  </behavior>
  <action>
    Run the full test suite (`node_modules/.bin/vitest run`) and fix any cross-file regression this plan's Task 1/2 introduced elsewhere (e.g. a stale import in a file this plan did not intend to touch) — per Rule 3/Rule 1 auto-fix precedent, scoped strictly to compile/test failures caused by this plan's own edits, never unrelated refactors.

    Create tests/categories-list-component.test.tsx: a `renderToStaticMarkup`-based test importing the real `DashboardCategoriesPage` default export is not attempted here (it is an async Server Component awaiting DAL calls — instead, test the exported LOCAL pieces this plan's Task 1 must expose for testability: if `DirectionFilter`/`SortToggle`/`NoYearsEmptyState` are not separately exported, add a minimal named export for each — e.g. `export function DirectionFilter(...)`) directly, asserting: DirectionFilter renders exactly 3 links in Uscite/Entrate/Accantonamenti order; SortToggle renders Proiezione as a disabled `<span>` with the exact title string when `projectionSortAvailable` is false, and as a `<Link>` when true; NoYearsEmptyState renders the import CTA link. This satisfies the VALIDATION.md Wave-0 requirement for a dedicated categories-list-component test.

    Create tests/category-detail-link.test.ts: cover the &lt;behavior&gt; round-trip assertions above directly against `buildDashboardTabHref`, `resolveYear`, and `buildDashboardCategoryDetailHref` (all pure, already-exported functions — no DB, no render).

    Re-run `node_modules/.bin/vitest run tests/pace-engine-lens-regression.test.ts` and confirm, via `git diff --stat`, it is byte-identical to its state after Plan 83-01 (RETIRE-05 final confirmation, D-10). Run `yarn check:language` and fix any Italian string that leaked into a developer-facing identifier/comment (Italian is expected and correct inside JSX copy strings/UI-SPEC-sourced literals — the checker's own allowlist already accounts for this; only fix a genuine violation, do not silence the checker).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run && node_modules/.bin/vitest run tests/pace-engine-lens-regression.test.ts && yarn check:language</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green: `node_modules/.bin/vitest run` exits 0.
    - `git diff --stat tests/pace-engine-lens-regression.test.ts` (relative to Plan 83-01's commit) shows no changes.
    - `node_modules/.bin/vitest run tests/categories-list-component.test.tsx tests/category-detail-link.test.ts` passes.
    - `grep -c "DeviationBadge\|getCategoryDeviations" "app/(app)/dashboard/categories/page.tsx" components/dashboard/category-ranking-list.tsx` returns 0 across both files.
    - `yarn check:language` exits 0.
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>The full phase's test suite is green, RETIRE-05 is confirmed byte-identical, the CLIST-05/CLIST-07 URL round trips are proven at the integration level, and no Phase-83 file references the retired Deviation machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-supplied URL search params (`?year=`, `?type=`, `?sort=`) -> server page component -> DAL aggregation calls | Same-user, same-tenant only; `getCategoryYearRanking`/`getCoveredMonthsInYear` both independently re-scope to `verifySession()`'s userId regardless of the requested year/direction, so a crafted param can only ever request a different SLICE of the same authenticated user's own data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-06 | Tampering | "app/(app)/dashboard/categories/page.tsx" (direction/sort param parsing) | low | mitigate | `parseCategoryYearDirection`/`parseCategoryYearSort` (Plan 83-02) are total, never-throwing functions defaulting to 'out'/'amount' on any malformed input — a crafted `?type=<script>` never reaches the DAL layer, it is coerced to the safe default before any query executes. |
| T-83-07 | Information Disclosure | components/dashboard/category-ranking-list.tsx (row href construction) | low | accept | The row's detail href only ever encodes this same user's own `category.id` (sourced from `getCategoryYearRanking`'s own userId-scoped result) plus the already-validated year/direction/lens — no new IDOR surface beyond what `getCategoryDetail`'s own ownership check (Phase 84, pre-existing) already gates. |

No package-manager installs in this plan; `T-83-SC` omitted.
</threat_model>

<verification>
- `node_modules/.bin/vitest run` (full suite) green.
- `node_modules/.bin/vitest run tests/pace-engine-lens-regression.test.ts` byte-identical/unedited and green.
- `yarn build` succeeds.
- `yarn check:language` clean.
</verification>

<success_criteria>
- All seven CLIST requirements are observable end-to-end on the real page: ranked list with share+sparkline+projection, sort toggle, three-way direction filter, year shared with Overview, single-Covered-Month state, and the row-to-detail year coherence.
- RETIRE-05 stays green and byte-identical after the full phase's changes.
- No Phase-83 file re-introduces a dependency on the Deviation/Preset machinery.
</success_criteria>

<output>
Create `.planning/phases/83-categories-list/83-04-SUMMARY.md` when done
</output>
