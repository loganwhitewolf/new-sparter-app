---
phase: 83-categories-list
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/category-sparkline.tsx
  - components/dashboard/category-year-ranking-skeleton.tsx
  - components/dashboard/category-coverage-nudge.tsx
  - lib/services/category-direction-copy.ts
  - tests/category-sparkline.test.tsx
  - tests/categories-nudge.test.tsx
  - tests/category-direction-copy.test.ts
autonomous: true
requirements: [CLIST-04, CLIST-06]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CategorySparkline's `type` prop widens from 'in' | 'out' to 'in' | 'out' | 'allocation', resolving a third stroke/fill colour from var(--total-allocation) — a value already defined in app/globals.css for both light and dark mode, requiring no new CSS token (D-09, CLIST-04)."
    - "parseAmount's Math.max(parsed, 0) clamp is removed — a negative amount (an allocation month with net divestment) is accepted and rendered with an explicit distinguishing visual marker, never silently flattened to zero, which would have hidden exactly the case D-09 introduces (UI-SPEC E2 negative-domain backstop)."
    - { statement: "When the new optional `pointStates` prop is provided, the component renders 12 individually-styled bars (covered/current/estimated/uncovered per UI-SPEC's `## Sparkline Visual States` table); when `pointStates` is omitted, the component renders its EXISTING single-polyline/single-circle output byte-for-byte unchanged, so no caller of this component anywhere in the codebase regresses from this additive change.", verification: backstop }
    - "The single-point case (points.length === 1) keeps rendering as one centered circle regardless of whether pointStates is provided — this is CLIST-06's one-Covered-Month series and must never fall through to the 12-bar layout."
    - "CategoryYearRankingSkeleton (new component) reserves a 5th grid column matching the row's projection slot, so the skeleton-to-real-row transition never shifts layout — whether or not the resolved data ends up showing a projection (UI-SPEC E6 backstop: layout stability)."
    - "The existing, shared CategoryRankingSkeleton (components/dashboard/category-ranking-skeleton.tsx) is left completely untouched — it is also used by components/dashboard/tag-ranking-list.tsx, which this phase does not touch."
    - "CategoryCoverageNudge (new component) follows OverviewNudge's exact localStorage dismissal/lastSeenCount pattern and copy placement (dashed border, muted background, bold prefix + body per UI-SPEC's `## Nudge (Single-Covered-Month State)` section), never introducing a server action or database write (D-14, CLIST-06)."
    - "resolveCategoryDirectionCopy(direction) is the single, centrally-resolved source of every direction-scoped string used by the Categories list — page subheading, share label, empty-state heading/body — mirroring Phase 82's resolveComparisonJudgement pattern (one function, never a per-widget local copy) so a future 4th direction cannot be added with only some of its copy filled in (D-11)."
    - "Every one of resolveCategoryDirectionCopy's three branches (out/in/allocation) is exercised by a direct unit test asserting the exact Italian strings from UI-SPEC.md's `## Copywriting Contract` table — no retired vocabulary (Deviazione, Baseline, Preset) appears in any returned string."
  artifacts:
    - "components/dashboard/category-sparkline.tsx (edited — additive props, negative-domain support, 4-state bar rendering)"
    - components/dashboard/category-year-ranking-skeleton.tsx
    - components/dashboard/category-coverage-nudge.tsx
    - lib/services/category-direction-copy.ts
    - tests/category-sparkline.test.tsx
    - tests/categories-nudge.test.tsx
    - tests/category-direction-copy.test.ts
  key_links:
    - "CategorySparklinePoint[] + pointStates[] (Plan 83-01's CategoryYearRankingItem.sparkline, mapped 1:1 by Plan 83-04) -> CategorySparkline's 4-state bar rendering"
    - "resolveCategoryDirectionCopy('allocation') -> Plan 83-04's page subheading, row share label, empty-state copy"
    - "CategoryCoverageNudge's shouldShowNudge pure helper -> Plan 83-04's wiring against getCoveredMonthsInYear(year).length === 1"
  prohibitions:
    - statement: "MUST NOT modify components/dashboard/category-ranking-skeleton.tsx — it is shared with components/dashboard/tag-ranking-list.tsx, which is out of this phase's scope."
      category: correctness
      verification: test
    - statement: "MUST NOT change CategorySparkline's rendered output for any existing call site that omits the new pointStates prop — the additive change must be provably invisible to a caller that does not opt in."
      category: correctness
      verification: test
---

<objective>
Build the phase's self-contained visual primitives — the 4-state sparkline, the categories-list
skeleton, the single-Covered-Month nudge, and the centrally-resolved per-direction copy set — each
independently testable and each additive/new, so this plan can land in the same wave as the DAL and
URL-contract plans with zero risk of a mid-phase type or behavior regression to any existing caller.

Purpose: D-09 (Accantonamenti reachable), D-11 (one copy set per direction, centrally resolved) and
D-14 (the degenerate single-Covered-Month state) each need a concrete, testable artifact before
Plan 83-04 can assemble the real page around them.
Output: `category-sparkline.tsx` gains 4-state bar rendering and a third direction colour; two new
components (`category-year-ranking-skeleton.tsx`, `category-coverage-nudge.tsx`); one new resolver
(`lib/services/category-direction-copy.ts`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-categories-list/83-CONTEXT.md
@.planning/phases/83-categories-list/83-UI-SPEC.md
@components/dashboard/category-sparkline.tsx
@components/dashboard/category-ranking-skeleton.tsx
@components/dashboard/overview/overview-nudge.tsx
@lib/services/pace-and-projection.ts
@app/globals.css
</context>

## Artifacts this phase produces

- `components/dashboard/category-sparkline.tsx` — additive props: `type: 'in' | 'out' | 'allocation'` (widened), `pointStates?: Array<'covered' | 'current' | 'estimated' | 'uncovered'>`, `estimatedHeightHint?: string | null`; `parseAmount`'s zero-clamp removed.
- `components/dashboard/category-year-ranking-skeleton.tsx` — new component, no props.
- `components/dashboard/category-coverage-nudge.tsx` — new component; exports pure helper `shouldShowCoverageNudge(coveredMonthCount: number, stored: { lastSeenCount: number } | null): boolean`.
- `lib/services/category-direction-copy.ts` — new file; exports `type CategoryDirectionCopy = { pageSubheading: string; shareLabel: string; emptyStateHeading: string; emptyStateBody: string; directionLabel: string }` and `resolveCategoryDirectionCopy(direction: 'in' | 'out' | 'allocation'): CategoryDirectionCopy`.
- Three new test files, each colocated with its own new artifact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sparkline — allocation colour, negative domain, 4-state bar rendering (additive, backward-compatible)</name>
  <reversibility rating="reversible">A purely presentational, additive component change with no persisted data and no external contract — trivially revertable by removing the new optional props.</reversibility>
  <files>components/dashboard/category-sparkline.tsx, tests/category-sparkline.test.tsx</files>
  <read_first>
    components/dashboard/category-sparkline.tsx (the full current file — Props type lines 5-9, parseAmount lines 20-23, buildPolylinePoints lines 25-45, the color line 49, the single-circle branch lines 81-83);
    .planning/phases/83-categories-list/83-UI-SPEC.md `## Sparkline Visual States` and `## Allocation (Savings) Sparkline Color` sections (the exact opacity/gradient/height values per state) and `## Row Structure and Layout`'s sparkline column spec (150px width, 32px bar-track height, 2px gap, 1px bar radius);
    app/globals.css (confirm `--total-allocation` is already defined for both `:root` and `.dark` — grep for the token, do not add it).
  </read_first>
  <behavior>
    - CategorySparkline({ points: [12 entries], type: 'allocation' }) with no pointStates renders EXACTLY the existing polyline output (byte-identical to today's render), just with the stroke colour resolved from var(--total-allocation) instead of falling through to the 'out' branch (the current ternary would incorrectly colour an allocation sparkline orange).
    - CategorySparkline({ points: [1 entry], type: 'out' }) renders the single centered circle regardless of pointStates being present or absent (CLIST-06).
    - CategorySparkline({ points: [12 entries incl. one negative amount], type: 'allocation', pointStates: [...12 'covered'/'current'/'estimated' values] }) renders 12 bar elements; the negative-amount bar carries a distinguishing marker (e.g. a top border or inverted fill) rather than a flat/zero-height bar.
    - CategorySparkline with pointStates containing 'uncovered' renders that bar at full (100%) height with a muted diagonal pattern, regardless of the underlying (always '0.00') amount for that point.
    - CategorySparkline with pointStates containing 'estimated' and estimatedHeightHint='150.00' renders that bar's height normalized against the OTHER bars' magnitudes using 150.00 as its reference value, with a colour-family striped pattern — never a flat zero bar.
    - parseAmount('-45.50') returns -45.5 (no longer clamped to 0).
  </behavior>
  <action>
    In components/dashboard/category-sparkline.tsx: widen `Props.type` to `'in' | 'out' | 'allocation'`; add `pointStates?: Array<'covered' | 'current' | 'estimated' | 'uncovered'>` and `estimatedHeightHint?: string | null` as new optional fields. Change `parseAmount` to `return Number.isFinite(parsed) ? parsed : 0` (delete the `Math.max(parsed, 0)` call entirely — this is safe for the existing 'in'/'out' callers since their query-level `abs(sum(...))` never produces a negative value; it only becomes meaningful once 'allocation' data with genuine divestment months arrives). Change the colour resolution line to a 3-way: `type === 'in' ? 'var(--total-in)' : type === 'allocation' ? 'var(--total-allocation)' : 'var(--total-out)'`.

    Split the render into two paths. Path A (points.length &lt;= 1 OR pointStates is undefined): KEEP the existing SVG polyline/circle rendering completely unchanged (this preserves every current caller's exact output). Path B (points.length &gt; 1 AND pointStates is provided, length matching points): render a flex row container (`display:flex; align-items:flex-end; height:32px; gap:2px`, matching UI-SPEC's exact track/gap values) of `points.length` bar `<div>` elements (`flex:1; border-radius:1px`), replacing the SVG entirely for this path. For each index i: compute a reference magnitude — `Math.abs(pointStates[i] === 'estimated' ? Number(estimatedHeightHint ?? '0') : parseAmount(points[i].amount))`; compute `max` as the greatest reference magnitude across all 12 indices (fallback 0 -&gt; every bar renders at minimum height, no division by zero); `heightPercent = max === 0 ? 0 : (referenceMagnitude / max) * 100`. Apply per state, following UI-SPEC's `## Sparkline Visual States` table exactly: 'covered' -&gt; `height: ${heightPercent}%`, `backgroundColor: color`, `opacity: 0.45`; 'current' -&gt; same but `opacity: 1`; 'estimated' -&gt; `height: ${heightPercent}%`, `backgroundImage: repeating-linear-gradient(135deg, ${color} 0 3px, transparent 3px 6px)`; 'uncovered' -&gt; `height: 100%`, `backgroundImage: repeating-linear-gradient(45deg, transparent 0 3px, rgba(120,120,120,0.35) 3px 6px)` (ignore that point's amount entirely for height, per UI-SPEC: "100% (full height)"). For any 'covered'/'current' bar whose underlying raw amount (before Math.abs) is negative, add a visually distinguishing marker on top of the normal fill (e.g. `borderTop: '2px solid ' + color` at full opacity, regardless of the bar's own state opacity) so a divestment month never reads identically to a same-height positive month.

    Create tests/category-sparkline.test.tsx using `renderToStaticMarkup` (matching tests/category-ranking-list.test.tsx's existing pattern — no `next/link` mock needed here, this component has none). Cover every &lt;behavior&gt; case above via HTML string assertions (`toContain`/`not.toContain` on class/style substrings, and the 'var(--total-allocation)' colour string).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/category-sparkline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/category-sparkline.test.tsx` passes.
    - `grep -c "Math.max(parsed, 0)" components/dashboard/category-sparkline.tsx` returns 0.
    - `grep -c "var(--total-allocation)" components/dashboard/category-sparkline.tsx` returns at least 1.
    - `yarn build` succeeds (no type error from the widened `type` union at the component's sole existing call site, `components/dashboard/category-ranking-list.tsx`, which still passes only `'in' | 'out'`).
  </acceptance_criteria>
  <done>CategorySparkline supports a third direction colour and 4 distinguishable per-month states when opted into via pointStates, accepts negative amounts, and renders byte-identical output for every existing caller that has not opted in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Categories-list skeleton, single-Covered-Month nudge, and centrally-resolved direction copy (new files)</name>
  <files>components/dashboard/category-year-ranking-skeleton.tsx, components/dashboard/category-coverage-nudge.tsx, lib/services/category-direction-copy.ts, tests/categories-nudge.test.tsx, tests/category-direction-copy.test.ts</files>
  <read_first>
    components/dashboard/category-ranking-skeleton.tsx (the existing shared skeleton — the placeholder-shape convention to follow, NOT to edit; note its 2-column grid has no projection slot);
    components/dashboard/overview/overview-nudge.tsx (the full file — shouldShowNudge pure helper, buildStorageKey/readStored localStorage pattern, the dashed-pill visual convention to adapt, NOT copy verbatim since UI-SPEC specifies a different panel shape for this nudge);
    lib/services/pace-and-projection.ts (resolveComparisonJudgement — the "single shared per-X resolver, no per-widget local copy" pattern this task's resolveCategoryDirectionCopy must follow identically);
    .planning/phases/83-categories-list/83-UI-SPEC.md `## Copywriting Contract` (exact strings for subheading/share label/empty-state per direction) and `## Nudge (Single-Covered-Month State)` (exact panel styling: dashed 1px border, 8px radius, 16px padding, muted background/text, bold prefix + body) and `## Loading and Skeleton States` (the 5-column desktop skeleton shape including the projection placeholder).
  </read_first>
  <behavior>
    - resolveCategoryDirectionCopy('out').pageSubheading === "Dove spendi di più nel {year}, e dove arrivi a questo ritmo." (the literal template string with the `{year}` placeholder intact — the caller interpolates it); resolveCategoryDirectionCopy('in')/('allocation') return the corresponding two other rows from UI-SPEC's table, each with distinct shareLabel/emptyStateHeading/emptyStateBody text (never a shared fallback string across all three).
    - shouldShowCoverageNudge(1, null) -> true (first time, exactly 1 raw Covered Month, D-14); shouldShowCoverageNudge(0, null) -> false (zero Covered Months is a DIFFERENT state, the whole-year-empty case, not this nudge's job); shouldShowCoverageNudge(2, null) -> false (2+ Covered Months means the nudge's job is done, regardless of pace-eligibility); shouldShowCoverageNudge(1, { lastSeenCount: 1 }) -> false (already dismissed at this exact count).
    - CategoryYearRankingSkeleton renders a 5-column desktop grid row (or the mobile-collapsed equivalent) whose 5th column placeholder is present unconditionally — never omitted based on any prop, since the skeleton never knows in advance whether the resolved data will show a projection.
  </behavior>
  <action>
    Create lib/services/category-direction-copy.ts. Export `type CategoryDirectionCopy = { pageSubheading: string; shareLabel: string; emptyStateHeading: string; emptyStateBody: string; directionLabel: string }`. Export `function resolveCategoryDirectionCopy(direction: 'in' | 'out' | 'allocation'): CategoryDirectionCopy` — a single `switch (direction)` with three cases, each returning the exact strings from UI-SPEC.md's `## Copywriting Contract` (Page Heading and Direction table, Row Metadata table's Share label row, and the per-direction Empty State heading/body strings) plus `directionLabel` from the `## Direction Filter Button Labels` table ('Uscite' | 'Entrate' | 'Accantonamenti'). No default/fallback case — the switch is exhaustive over the 3-member union, so TypeScript enforces every branch is filled.

    Create components/dashboard/category-coverage-nudge.tsx, modeled on overview-nudge.tsx's structure but NOT copied verbatim (different visual shape per UI-SPEC). Export the pure helper `export function shouldShowCoverageNudge(coveredMonthCount: number, stored: { lastSeenCount: number } | null): boolean` — `if (coveredMonthCount !== 1) return false` (the nudge is SPECIFICALLY the exactly-1-raw-Covered-Month state, per D-14's literal wording — 0 is the separate whole-year-empty state, 2+ means the nudge's job is done); `if (!stored) return true`; `return coveredMonthCount !== stored.lastSeenCount` (re-show if the dismissed count no longer matches — defensive re-arm, mirroring OverviewNudge's re-appearance semantics but simplified since this nudge only ever fires at exactly 1). Export `function CategoryCoverageNudge({ coveredMonthCount, year }: { coveredMonthCount: number; year: number })` — `'use client'` component; SSR-safe `useState(false)` + `useEffect` restore from `localStorage` under a key like `sparter-categories-nudge-${year}` (mirroring overview-nudge.tsx's buildStorageKey/readStored pattern exactly, adapted to this key); renders UI-SPEC's exact panel: dashed 1px border, 8px radius, 16px padding, muted background/text, bold prefix "Con un secondo mese importato" (fg-coloured, weight 600) + body "vedrai il ritmo mensile e la proiezione di fine anno. Serve almeno un mese concluso oltre a quello in corso." (muted-fg, 14px); no dismiss button is required unless UI-SPEC specifies one (it does not — treat this nudge as a persistent info panel below the list, not a dismissible pill like OverviewNudge, since it is informational about missing data rather than an actionable alert).

    Create components/dashboard/category-year-ranking-skeleton.tsx, a new sibling to (not a modification of) category-ranking-skeleton.tsx. Export `function CategoryYearRankingSkeleton()` — render 5-7 placeholder rows (UI-SPEC `## Loading and Skeleton States` — count and animate-pulse convention identical to the existing skeleton), each matching the LOCKED 5-column desktop row shape from UI-SPEC's `## Row Structure and Layout` (rank badge circle, name+metadata+bar block, 12-bar sparkline placeholder, Totale placeholder, Proiezione placeholder) — the Proiezione (5th) column placeholder is ALWAYS rendered, unconditionally (the backstop invariant: layout must not shift when real data resolves with the projection absent).

    Create tests/categories-nudge.test.tsx: unit-test `shouldShowCoverageNudge` directly (no render needed) for the &lt;behavior&gt; cases above. Create tests/category-direction-copy.test.ts: unit-test `resolveCategoryDirectionCopy` for all three directions, asserting exact string equality against UI-SPEC's table values, and asserting none of the three returned objects contain the substrings "Deviazione", "Baseline", "Preset" (retired-vocabulary guard).
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run tests/categories-nudge.test.tsx tests/category-direction-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/vitest run tests/categories-nudge.test.tsx tests/category-direction-copy.test.ts` passes.
    - `grep -c "export function resolveCategoryDirectionCopy" lib/services/category-direction-copy.ts` returns 1.
    - `grep -c "export function shouldShowCoverageNudge" components/dashboard/category-coverage-nudge.tsx` returns 1.
    - `grep -c "Deviazione\|Baseline\|Preset" lib/services/category-direction-copy.ts` returns 0.
    - `yarn build` succeeds.
  </acceptance_criteria>
  <done>A new skeleton, a new coverage nudge (with a directly-testable pure visibility rule), and a single centrally-resolved direction-copy function all exist, none touching the shared CategoryRankingSkeleton used by Tags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-side localStorage read/write (CategoryCoverageNudge) | Same-browser, same-user only; no server round-trip, no cross-user surface — mirrors OverviewNudge's existing, already-accepted risk posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-05 | Tampering | components/dashboard/category-coverage-nudge.tsx (localStorage) | low | accept | A user editing their own localStorage can only affect their own client-side dismissal state for their own nudge — no data or other user is reachable through this surface, identical to the pre-existing OverviewNudge pattern this component mirrors. |

No package-manager installs in this plan; `T-83-SC` omitted.
</threat_model>

<verification>
- `node_modules/.bin/vitest run tests/category-sparkline.test.tsx tests/categories-nudge.test.tsx tests/category-direction-copy.test.ts` green.
- `yarn build` succeeds.
- `yarn check:language` clean.
</verification>

<success_criteria>
- CategorySparkline supports the allocation colour and 4-state bar rendering additively, with zero regression to any existing caller.
- A new skeleton reserves the projection column unconditionally.
- The single-Covered-Month nudge's visibility rule is a directly-testable pure function.
- Every direction's copy is resolved from one function, never duplicated per widget.
</success_criteria>

<output>
Create `.planning/phases/83-categories-list/83-03-SUMMARY.md` when done
</output>
