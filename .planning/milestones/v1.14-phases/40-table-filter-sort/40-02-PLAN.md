---
phase: 40-table-filter-sort
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - components/data-table/DataTableToolbar.tsx
  - components/data-table/HeaderSortButton.tsx
  - components/data-table/ChipsRow.tsx
  - components/data-table/use-table-url.ts
  - tests/data-table-toolbar.test.tsx
autonomous: true
requirements: [D-02, D-13, D-14, D-15, D-16, D-17, D-18]
must_haves:
  truths:
    - "A shared DataTableToolbar renders an always-visible search input, a 'Filtri (n)' Popover trigger with the active-filter count, an active-chip row with 'Cancella tutto', and a mobile bottom-sheet variant"
    - "HeaderSortButton renders aria-sort on the column header and cycles a column ASC → DESC → off via the URL"
    - "All toolbar state lives in the URL via router.replace(scroll:false); the component holds no server state"
  artifacts:
    - path: "components/data-table/DataTableToolbar.tsx"
      provides: "Shared toolbar consuming a TableConfig and writing filters to the URL"
      contains: "Filtri"
    - path: "components/data-table/HeaderSortButton.tsx"
      provides: "aria-sort column header with ASC/DESC/off cycle"
      contains: "aria-sort"
    - path: "components/data-table/use-table-url.ts"
      provides: "updateParam / replaceWith URL-mutation hook"
      contains: "router.replace"
  key_links:
    - from: "components/data-table/DataTableToolbar.tsx"
      to: "lib/utils/table-config.ts"
      via: "import type TableConfig"
      pattern: "TableConfig"
    - from: "components/data-table/use-table-url.ts"
      to: "next/navigation"
      via: "useRouter + useSearchParams"
      pattern: "useSearchParams"
---

<objective>
Build the shared, table-agnostic toolbar UI (CONTEXT 5-wave roadmap item 2): search input, "Filtri (n)" panel, active-chip row, "Cancella tutto", desktop header-sort with `aria-sort`, and the mobile detached-sort + bottom-sheet scaffold. It renders against a mock `TableConfig` and is NOT wired to any real table yet (that is Wave 4).

Purpose: Centralize the Variant A layout (LOCKED, D-13–D-18) into one component so all three tables share identical behavior. Extract from the working prototype rather than rewriting (PATTERNS.md "extract, don't rewrite").
Output: `components/data-table/` directory with `DataTableToolbar`, `HeaderSortButton`, `ChipsRow`, and the `use-table-url` URL-mutation hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-table-filter-sort/40-CONTEXT.md
@.planning/phases/40-table-filter-sort/40-PATTERNS.md
@app/proto/table-toolbar/variant-a.tsx
@app/proto/table-toolbar/shared.tsx
@components/transactions/transaction-filters.tsx
</context>

<artifacts_produced>
## Artifacts this plan produces
- `components/data-table/use-table-url.ts` — hook `useTableUrl(route)` returning `{ updateParam, replaceWith, searchParams, isPending }`
- `components/data-table/HeaderSortButton.tsx` — `HeaderSortButton` component (aria-sort, ASC/DESC/off cycle)
- `components/data-table/ChipsRow.tsx` — `ChipsRow` component (removable chips + "Cancella tutto")
- `components/data-table/DataTableToolbar.tsx` — `DataTableToolbar` component consuming `TableConfig`
- `tests/data-table-toolbar.test.tsx` — render/interaction tests
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: URL-mutation hook + HeaderSortButton + ChipsRow</name>
  <files>components/data-table/use-table-url.ts, components/data-table/HeaderSortButton.tsx, components/data-table/ChipsRow.tsx</files>
  <read_first>
    - components/transactions/transaction-filters.tsx (lines 1–19 imports; lines 36–59 `replaceWith` + `updateParam` + `startTransition` + `router.replace(..., {scroll:false})` — the canonical URL mutation pattern to extract into the hook)
    - app/proto/table-toolbar/shared.tsx (lines 386–416 `SortableHead` for the aria-sort + ArrowUp/ArrowDown markup; lines 362–383 `ChipsRow` for the chip markup)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (sections "HeaderSortButton.tsx" and the ASC→DESC→off cycle logic at lines 310–317; "ChipsRow pattern")
    - lib/routes.ts (route constants — the hook receives the route as an argument so it is table-agnostic)
  </read_first>
  <action>
    Create `components/data-table/use-table-url.ts` (`'use client'`). Export `useTableUrl(route: string)` returning `{ searchParams, isPending, replaceWith, updateParam, updateParams }`. Internally use `useRouter()`, `useSearchParams()`, `useTransition()`. `replaceWith(params: URLSearchParams)` calls `startTransition(() => router.replace(query ? \`${route}?${query}\` : route, { scroll: false }))`. `updateParam(key, value: string | null)` clones `searchParams`, sets or deletes the key, calls `replaceWith`. `updateParams(entries: Record<string, string | null>)` applies several keys in one URLSearchParams clone then one `replaceWith` (used by "Cancella tutto").

    Create `components/data-table/HeaderSortButton.tsx` (`'use client'`). Props: `{ column: SortColumn; activeSort?: string; activeDir?: 'asc'|'desc'; align?: 'left'|'right'; onSort: (key: string) => void }`. Render a `<TableHead>` with `aria-sort={activeSort === column.key ? (activeDir === 'asc' ? 'ascending' : 'descending') : 'none'}` and an inner `<button>` showing the label plus `ArrowUp`/`ArrowDown`/neutral `↕` (markup from shared.tsx lines 402–415). Export a pure helper `nextSort(current: {sort?: string; dir?: 'asc'|'desc'}, key: string)` implementing the cycle: inactive column → `{ sort: key, dir: 'desc' }`; active DESC → `{ sort: key, dir: 'asc' }`; active ASC → `{ sort: undefined, dir: undefined }` (off = back to default per D-13).

    Create `components/data-table/ChipsRow.tsx` (`'use client'`). Props `{ chips: { key: string; label: string; onRemove: () => void }[]; onClear: () => void }`. Render nothing when `chips.length === 0`. Otherwise render the removable pill buttons with the `X` icon and a "Cancella tutto" ghost button calling `onClear` (markup from shared.tsx lines 362–383).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/data-table/(use-table-url|HeaderSortButton|ChipsRow)" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "router\.replace" components/data-table/use-table-url.ts` returns a match
    - `grep -nE "scroll: false" components/data-table/use-table-url.ts` returns a match
    - `grep -nE "aria-sort" components/data-table/HeaderSortButton.tsx` returns a match
    - `grep -nE "export function nextSort" components/data-table/HeaderSortButton.tsx` returns a match
    - `grep -nE "Cancella tutto" components/data-table/ChipsRow.tsx` returns a match
    - The verify command outputs `0` (no tsc errors in these three files)
  </acceptance_criteria>
  <done>The URL-mutation hook and the two sort/chip primitives compile and expose the locked Variant A behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DataTableToolbar assembling search + Filtri panel + chips + mobile sheet</name>
  <files>components/data-table/DataTableToolbar.tsx, tests/data-table-toolbar.test.tsx</files>
  <read_first>
    - app/proto/table-toolbar/variant-a.tsx (full file — the LOCKED Variant A layout: search inline, "Filtri (n)" Popover, panel field rendering; extract this layout)
    - components/transactions/transaction-filters.tsx (lines 83–88 debounced search input → 300ms → updateParam('q', ...))
    - components/categorization/subcategory-picker.tsx (lines 14–25 vaul `Sheet`/`SheetContent side="bottom"` usage — reuse for the mobile filter sheet and mobile "Ordina" list)
    - components/data-table/use-table-url.ts (the hook from Task 1 — consume it)
    - lib/utils/table-config.ts (TableConfig / FilterField types from Wave 1 — the component is generic over these)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (section "DataTableToolbar.tsx": Filtri button + count badge lines 172–185, mobile bottom sheet lines 209–219, props shape lines 594–607)
  </read_first>
  <behavior>
    - Given a config with search.key='q', renders an always-visible search input with the configured placeholder
    - Typing in search debounces 300ms then calls updateParam('q', trimmedValue)
    - The "Filtri" trigger label shows "Filtri (2)" when two non-search filter params are active in the URL, and "Filtri" when zero are active
    - Each active filter renders a removable chip via field.toChip(value); "Cancella tutto" clears all filter params (not the search, not sort) in one URL write
    - For each config.sortable column, a HeaderSortButton is exposed (desktop); a mobile "Ordina" trigger lists the same columns
  </behavior>
  <action>
    Create `components/data-table/DataTableToolbar.tsx` (`'use client'`). Props (from PATTERNS.md lines 594–607): `{ config: TableConfig; route: string; monthsWithData?: string[]; filterOptions?: Record<string, { value: string; label: string }[]> }`. The component derives ALL current values from `useTableUrl(route).searchParams` — it owns no server state.

    Layout (Variant A, LOCKED): (1) inline search `<Input>` bound to `config.search` when non-null, debounced 300ms → `updateParam('q', value.trim() || null)`. (2) A `<Popover>` whose trigger is a `<Button variant="outline">` showing `SlidersHorizontal` + `Filtri` + ` (${n})` where `n` = count of currently-set filter param keys (keys drawn from `config.filters`, excluding `q`, `sort`, `dir`). (3) `PopoverContent` renders one control per `config.filters[]` entry switched on `field.type`: for this wave render the existing primitive controls (`select` → a `<select>` of `filterOptions[field.key]`; `status` → a two-option select; `text` → `<Input>`). Leave `month-multi` and `amount-range` as labeled placeholders that read/write the right URL keys (`months`, `amountMin`/`amountMax`) — Wave 3 swaps in the real pickers. (4) Below, a `<ChipsRow>` built by mapping every active filter param to `{ label: field.toChip(value), onRemove: () => updateParam(field.key, null) }`, with `onClear` removing every filter key in one `updateParams` write. (5) Desktop sort = export the per-column `HeaderSortButton` wiring via an exported helper `useToolbarSort(route)` returning `{ activeSort, activeDir, onSort }` (uses `nextSort` then `updateParams({ sort, dir })`). (6) Mobile: a detached "Ordina" `<Button>` (hidden on `md:`) opening a vaul `Sheet side="bottom"` listing `config.sortable` columns; the "Filtri" panel ALSO renders in a bottom `Sheet` on mobile (CSS-gated, same field rendering as the popover).

    Do NOT inline implementation code in this action — follow the analog files. Write `tests/data-table-toolbar.test.tsx` covering the behavior block using `@testing-library/react` with a mocked `next/navigation` (mock `useRouter().replace`, `useSearchParams`). Build a mock `TableConfig` with a `select` filter and two sortable columns. Assert: search renders with placeholder; the Filtri trigger text reflects active count from the mocked searchParams; clicking a chip's remove calls `router.replace` without that param; "Cancella tutto" issues one replace dropping all filter keys.
  </action>
  <verify>
    <automated>yarn vitest run tests/data-table-toolbar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "'use client'" components/data-table/DataTableToolbar.tsx` returns a match on line 1
    - `grep -nE "Filtri" components/data-table/DataTableToolbar.tsx` returns a match
    - `grep -nE "SlidersHorizontal" components/data-table/DataTableToolbar.tsx` returns a match
    - `grep -nE "config\.filters" components/data-table/DataTableToolbar.tsx` returns a match (panel rendered from config, not hardcoded)
    - `grep -nE "side=\"bottom\"|SheetContent" components/data-table/DataTableToolbar.tsx` returns a match (mobile sheet scaffolded)
    - `yarn vitest run tests/data-table-toolbar.test.tsx` exits 0
  </acceptance_criteria>
  <done>A single config-driven toolbar renders search + Filtri panel + chips + mobile sheet against a mock config and writes all state to the URL, verified by interaction tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (search/filter fields) → URL | Client-entered values written to the query string and later re-read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Tampering | DataTableToolbar writing arbitrary text to URL | mitigate | Toolbar only writes; the Wave 1 DAL-side parsers (`parseMonths`/`parseAmount`/`parseStatus`) re-validate on read, so a hand-edited URL cannot inject unvalidated values into queries |
| T-40-05 | Information Disclosure | filter values visible in shareable URL | accept | Filters are intentionally ephemeral and shareable (D-02); no secrets in filter values |
| T-40-SC | Tampering | npm installs (vaul/sheet already present) | mitigate | No new package installs; reuses existing shadcn `Sheet`/`Popover`/vaul; slopcheck N/A |
</threat_model>

<verification>
- `yarn vitest run tests/data-table-toolbar.test.tsx` green
- `npx tsc --noEmit` reports no new errors in `components/data-table/`
- No real table wiring yet — component renders against a mock config only
</verification>

<success_criteria>
- One shared `DataTableToolbar` renders the full Variant A layout from a `TableConfig`
- `aria-sort` present on sortable headers; ASC→DESC→off cycle implemented
- All state round-trips through the URL with `scroll: false`
</success_criteria>

<output>
Create `.planning/phases/40-table-filter-sort/40-02-SUMMARY.md` when done
</output>
