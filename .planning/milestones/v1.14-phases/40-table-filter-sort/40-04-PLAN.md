---
phase: 40-table-filter-sort
plan: 04
type: execute
wave: 4
depends_on: ["40-03"]
files_modified:
  - app/(app)/transactions/transactions.table.ts
  - app/(app)/transactions/page.tsx
  - lib/dal/transactions.ts
  - lib/validations/transactions.ts
  - app/(app)/expenses/expenses.table.ts
  - app/(app)/expenses/page.tsx
  - lib/dal/expenses.ts
  - lib/validations/expense.ts
  - app/(app)/import/files.table.ts
  - app/(app)/import/page.tsx
  - lib/dal/imports.ts
  - lib/validations/import.ts
  - tests/expenses-dal.test.ts
  - tests/imports-dal.test.ts
autonomous: true
requirements: [D-03, D-05, D-19, D-20, D-21, D-22, D-23, D-24, D-25, O-01]
must_haves:
  truths:
    - "Transactions table renders DataTableToolbar via transactionsTableConfig; q/months/amountMin/amountMax/platform/category/status all filter server-side and every sortable column sorts"
    - "Expenses no longer defaults to this-month; renders DataTableToolbar with amount + category + platform + categorization status (NO temporal filter); status 4 maps to uncategorized"
    - "Files table renders DataTableToolbar with the 3 processing buckets (Importato / Da completare / In errore), coverage-month filter, platform, and amount"
    - "Each of the three tables filters and sorts end-to-end against the URL"
  artifacts:
    - path: "app/(app)/transactions/transactions.table.ts"
      provides: "transactionsTableConfig: TableConfig"
      contains: "transactionsTableConfig"
    - path: "app/(app)/expenses/expenses.table.ts"
      provides: "expensesTableConfig: TableConfig (no month-multi field)"
      contains: "expensesTableConfig"
    - path: "app/(app)/import/files.table.ts"
      provides: "filesTableConfig: TableConfig (3 processing buckets)"
      contains: "filesTableConfig"
  key_links:
    - from: "app/(app)/transactions/page.tsx"
      to: "components/data-table/DataTableToolbar.tsx"
      via: "config={transactionsTableConfig} + monthsWithData prop"
      pattern: "DataTableToolbar"
    - from: "lib/dal/expenses.ts"
      to: "expense.status"
      via: "uncategorized bucket includes 1 and 4"
      pattern: "expense\\.status"
    - from: "lib/dal/imports.ts"
      to: "file.status"
      via: "3-bucket processing filter"
      pattern: "inArray\\(file\\.status"
---

<objective>
Wire the real per-table `TableConfig` instances and the DAL filter conditions for all three tables (CONTEXT 5-wave roadmap item 4): Transactions, then Expenses, then Files. Each table swaps its bespoke filter component for `<DataTableToolbar config={...}>` and gains the full filter set declared in the LOCKED field inventory.

Purpose: This is where the shared system actually replaces the three divergent controls. Each table is a self-contained vertical slice (config + page wiring + DAL conditions + validation), so each task ends with one table working end-to-end.
Output: three `*.table.ts` config files, three rewired pages, three DAL filter-condition updates, three validation parsers extended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-table-filter-sort/40-CONTEXT.md
@.planning/phases/40-table-filter-sort/40-PATTERNS.md
@.planning/table-filter-sort-DECISIONS.md
</context>

<artifacts_produced>
## Artifacts this plan produces
- `app/(app)/transactions/transactions.table.ts` — `transactionsTableConfig`
- `app/(app)/expenses/expenses.table.ts` — `expensesTableConfig`
- `app/(app)/import/files.table.ts` — `filesTableConfig`
- `lib/dal/transactions.ts` — months/amount/status WHERE conditions added
- `lib/dal/expenses.ts` — this-month default removed; amount conditions added; status bucket includes 4
- `lib/dal/imports.ts` — platform/statusBucket(3)/coverage-months/amount conditions added
- `lib/validations/transactions.ts`, `lib/validations/expense.ts`, `lib/validations/import.ts` — parsers extended for the new keys
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transactions — config + DAL conditions + page wiring</name>
  <files>app/(app)/transactions/transactions.table.ts, lib/dal/transactions.ts, lib/validations/transactions.ts, app/(app)/transactions/page.tsx, tests/transactions-dal.test.ts</files>
  <read_first>
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (section "transactions.table.ts" lines 357–384 — the exact config object; section "lib/dal/transactions.ts — modifications" lines 408–434 — the WHERE conditions; section "transactions/page.tsx" lines 506–523 — the Promise.all + DataTableToolbar swap)
    - lib/dal/transactions.ts (lines 47–60 TransactionFilters — Wave 1 already added months/amountMin/amountMax; line 137 getTransactions; lines 150–180 existing conditions block; the `ilike` description filter for `q`)
    - lib/validations/transactions.ts (lines 56–175 — `firstTrimmed`, `parseTransactionFilters`, the spread-conditional return)
    - app/(app)/transactions/page.tsx (lines 16–32 `buildTransactionTableKey`; lines 41–46 Promise.all; the current `<TransactionFilters>` usage)
    - components/data-table/DataTableToolbar.tsx (the shared component props from Wave 2/3)
    - lib/utils/search-params.ts (parseMonths/parseAmount/parseStatus from Wave 1)
  </read_first>
  <behavior>
    - getTransactions({ months: ['2026-05'] }) adds a TO_CHAR(occurred_at,'YYYY-MM') = '2026-05' condition (OR across multiple months)
    - getTransactions({ amountMin: '10', amountMax: '200' }) adds ABS(amount) BETWEEN-style conditions
    - getTransactions({ status: 'uncategorized' }) filters to rows with null subcategory; 'categorized' to non-null
    - parseTransactionFilters maps q (was name), months, amountMin, amountMax, status from searchParams
  </behavior>
  <action>
    Create `app/(app)/transactions/transactions.table.ts` exporting `transactionsTableConfig: TableConfig` exactly as in PATTERNS.md lines 365–383: id `'transactions'`, search `{ key: 'q', placeholder: 'Nome o descrizione…' }`, filters `[months (month-multi), amount (amount-range), platform (select), category (select), status]`, sortable `[occurredAt 'Data', amount 'Importo', description 'Descrizione', category 'Categoria', platform 'Piattaforma']`, defaultSort `{ key: 'occurredAt', dir: 'desc' }`. Use the Italian status chip labels from PATTERNS.md line 373.

    In `lib/dal/transactions.ts`, add the WHERE conditions from PATTERNS.md lines 410–432 after the existing conditions block: months → `or(...months.map(ym => sql\`TO_CHAR(${transaction.occurredAt}, 'YYYY-MM') = ${ym}\`))`; amount → `ABS(${transaction.amount}::numeric) >= ${amountMin}::numeric` and `<= ${amountMax}::numeric` (D-20 absolute value); status → `isNull(...subCategoryId)` for uncategorized / `isNotNull(...)` for categorized (use the join already present for categorization). Add `status?: 'uncategorized' | 'categorized'` to `TransactionFilters`. Implement uncategorized-first on `asc` for the status sort column per D-25 if a `status` sortable is added (transactions sortable list does not include status — skip). Ensure `or`, `isNull`, `isNotNull`, `sql` are imported.

    In `lib/validations/transactions.ts`, extend `parseTransactionFilters` to read `q` (replacing/aliasing the old `name` key — accept `q` as canonical per D-19), `months` (via `parseMonths`), `amountMin`/`amountMax` (via `parseAmount`), `status` (via `parseStatus(value, ['categorized','uncategorized'])`). Add these to `ParsedTransactionFilters`. Keep `name`→`q` back-compat read so old links still parse (full URL migration is Wave 5).

    In `app/(app)/transactions/page.tsx`: add `getMonthsWithData('transactions')` to the Promise.all (PATTERNS.md lines 516–522); replace `<TransactionFilters>` with `<DataTableToolbar config={transactionsTableConfig} route={APP_ROUTES.transactions} monthsWithData={monthsWithData} filterOptions={{ platform: platformOptions, category: categoryOptions }} />`; update `buildTransactionTableKey` to include `months`, `amountMin`, `amountMax`, `status` so the table remounts on those changes (D-04).

    Update `tests/transactions-dal.test.ts` to cover the three behavior cases (months OR, amount ABS bounds, status null/non-null). RED then GREEN.
  </action>
  <verify>
    <automated>yarn vitest run tests/transactions-dal.test.ts && npx tsc --noEmit 2>&1 | grep -E "transactions" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export const transactionsTableConfig" app/(app)/transactions/transactions.table.ts` returns a match
    - `grep -nE "ABS\(.*transaction\.amount" lib/dal/transactions.ts` returns a match
    - `grep -nE "TO_CHAR\(.*occurredAt.*YYYY-MM" lib/dal/transactions.ts` returns a match
    - `grep -nE "status\?: 'uncategorized' \| 'categorized'" lib/dal/transactions.ts` returns a match
    - `grep -nE "DataTableToolbar" app/(app)/transactions/page.tsx` returns a match
    - `grep -nE "getMonthsWithData\('transactions'\)" app/(app)/transactions/page.tsx` returns a match
    - `grep -nE "parseMonths|parseAmount|parseStatus" lib/validations/transactions.ts` returns matches
    - `yarn vitest run tests/transactions-dal.test.ts` exits 0; the tsc grep outputs `0`
  </acceptance_criteria>
  <done>Transactions filters and sorts end-to-end through DataTableToolbar; old TransactionFilters component is no longer rendered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expenses — drop this-month default + config + DAL + page</name>
  <files>app/(app)/expenses/expenses.table.ts, lib/dal/expenses.ts, lib/validations/expense.ts, app/(app)/expenses/page.tsx, tests/expenses-dal.test.ts</files>
  <read_first>
    - lib/dal/expenses.ts (lines 16–24 ExpenseFilters + period union; lines 53–62 buildExpenseOrderBy — ALREADY correct, do not change; line 69 `periodToDateRange(filters.period ?? 'this-month')` — the default to REMOVE; lines 81–86 existing status mapping `status='1'` uncategorized / `'2','3'` categorized)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (section "lib/dal/expenses.ts — modifications" lines 438–469 — remove this-month default, conditional period, add amount conditions; "expenses.table.ts" note line 386 — no months field)
    - .planning/phases/40-table-filter-sort/40-CONTEXT.md (D-05 remove this-month; D-11 Expenses have NO temporal filter; O-01 status 4 → uncategorized bucket)
    - lib/validations/expense.ts (current expense filter parser — extend it)
    - app/(app)/expenses/page.tsx (lines 43–57 inline filter parsing to replace; buildExpenseTableKey; current filter component usage)
    - app/(app)/transactions/transactions.table.ts (the config written in Task 1 — mirror the shape minus months)
  </read_first>
  <behavior>
    - getExpenses({}) with NO period returns all-time rows (no this-month date clamp)
    - getExpenses({ status: 'uncategorized' }) matches expense.status IN ('1','4'); 'categorized' matches IN ('2','3')
    - getExpenses({ amountMin: '50' }) adds ABS(total_amount) >= 50
  </behavior>
  <action>
    In `lib/dal/expenses.ts`, remove the `this-month` default (line 69): change base `conditions` to only `eq(expense.userId, userId)` and apply the period date-range ONLY when `filters.period` is explicitly present (PATTERNS.md lines 452–460). Update the status mapping (lines 81–86) so the `uncategorized` bucket is `inArray(expense.status, ['1','4'])` and `categorized` is `inArray(expense.status, ['2','3'])` — this resolves O-01 (status 4 → uncategorized, conservative). Add absolute-amount conditions on `expense.totalAmount` (PATTERNS.md lines 465–468). Add `amountMin?: string`, `amountMax?: string` to `ExpenseFilters`. Do NOT add a `months` filter (D-11). `buildExpenseOrderBy` is already correct — leave it.

    Create `app/(app)/expenses/expenses.table.ts` exporting `expensesTableConfig: TableConfig`: id `'expenses'`, search `{ key: 'q', placeholder: 'Titolo…' }`, filters `[amount (amount-range), category (select), platform (select), status (status, label 'Categorizzazione')]` — NO month-multi field. sortable columns for every displayed expense column (title/totalAmount/category/platform/date-of-record as applicable — mirror what the expenses table actually renders). defaultSort newest-first on the existing default sort key.

    In `lib/validations/expense.ts`, extend the parser to read `q`, `amountMin`/`amountMax` (`parseAmount`), `status` (`parseStatus(value, ['categorized','uncategorized'])`); stop defaulting `period`.

    In `app/(app)/expenses/page.tsx`, replace the inline filter parsing (lines 43–57) with the shared parser call; replace the bespoke filter component with `<DataTableToolbar config={expensesTableConfig} route={APP_ROUTES.expenses} filterOptions={{ category, platform }} />` (no `monthsWithData` prop — expenses have no temporal filter); update `buildExpenseTableKey` to drop `period` and include `amountMin`/`amountMax`/`status`.

    Update `tests/expenses-dal.test.ts`: assert no this-month clamp when period absent; status 'uncategorized' → IN ('1','4'); amount ABS condition. RED then GREEN.
  </action>
  <verify>
    <automated>yarn vitest run tests/expenses-dal.test.ts && npx tsc --noEmit 2>&1 | grep -E "expenses" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export const expensesTableConfig" app/(app)/expenses/expenses.table.ts` returns a match
    - `grep -cE "month-multi" app/(app)/expenses/expenses.table.ts` outputs `0` (no temporal filter, D-11)
    - `grep -nE "inArray\(expense\.status, \['1', ?'4'\]\)" lib/dal/expenses.ts` returns a match (O-01 resolved)
    - `grep -cE "this-month" lib/dal/expenses.ts` outputs `0` (default removed, D-05)
    - `grep -nE "ABS\(.*expense\.totalAmount" lib/dal/expenses.ts` returns a match
    - `grep -nE "DataTableToolbar" app/(app)/expenses/page.tsx` returns a match
    - `yarn vitest run tests/expenses-dal.test.ts` exits 0; the tsc grep outputs `0`
  </acceptance_criteria>
  <done>Expenses defaults to all-time, exposes amount/category/platform/status (no temporal), status 4 counts as uncategorized, all via DataTableToolbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Files — 3 processing buckets + coverage months + config + page</name>
  <files>app/(app)/import/files.table.ts, lib/dal/imports.ts, lib/validations/import.ts, app/(app)/import/page.tsx, tests/imports-dal.test.ts</files>
  <read_first>
    - lib/dal/imports.ts (lines 7 ParsedImportFilters import; lines 97–137 getImports conditions + orderBy; line 137 orderBy already has file.id tiebreaker from Wave 1)
    - lib/db/schema.ts (line 30–38 fileStatusEnum: pending_upload, uploaded, analyzing, analyzed, importing, imported, failed; line 329 negativeTotal; lines 330–331 referenceStartedAt/referenceEndedAt)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (section "lib/dal/imports.ts — modifications" lines 473–502 — platform, statusBucket 3 buckets, coverage months, amount; "import/page.tsx" lines 535–539 — getFilterKey + Promise.all)
    - .planning/phases/40-table-filter-sort/40-CONTEXT.md (D-22 buckets: Importato=imported / Da completare=uploaded+analyzed+transient / In errore=failed; D-23 label 'Elaborazione'; O-02 import-date is sortable not a filter)
    - lib/validations/import.ts (lines 181–253 ParsedImportFilters + parseImportFilters — extend)
    - app/(app)/import/page.tsx (lines 16–24 getFilterKey; Promise.all; current files table component)
  </read_first>
  <behavior>
    - getImports({ statusBucket: 'imported' }) → file.status = 'imported'
    - getImports({ statusBucket: 'pending' }) → file.status IN ('uploaded','analyzing','analyzed','importing','pending_upload')
    - getImports({ statusBucket: 'failed' }) → file.status = 'failed'
    - getImports({ months: ['2026-05'] }) → TO_CHAR(reference_started_at,'YYYY-MM') = '2026-05'
    - getImports({ platform: 'revolut' }) → platform slug condition; amountMin → ABS(negative_total) >= min
  </behavior>
  <action>
    In `lib/dal/imports.ts`, add the conditions from PATTERNS.md lines 478–501: platform slug eq; the 3 processing buckets mapping `statusBucket` to `imported` / `inArray(file.status, ['uploaded','analyzed','analyzing','importing','pending_upload'])` / `failed` (D-22 — include all transient states in the "pending/Da completare" bucket; verify against the actual enum at schema.ts lines 30–38); coverage-months via `or(...months.map(ym => sql\`TO_CHAR(${file.referenceStartedAt}, 'YYYY-MM') = ${ym}\`))`; absolute amount on `file.negativeTotal`. Add `platform?: string`, `statusBucket?: 'imported'|'pending'|'failed'`, `months?: string[]`, `amountMin?: string`, `amountMax?: string` to the import filters type (extend `ParsedImportFilters` source in lib/validations/import.ts). Ensure `inArray`, `or`, `sql`, `eq` imported.

    Create `app/(app)/import/files.table.ts` exporting `filesTableConfig: TableConfig`: id `'files'`, search `{ key: 'q', placeholder: 'Nome file…' }`, filters `[months (month-multi, coverage), amount (amount-range), platform (select), statusBucket (status, label 'Elaborazione', options Importato/Da completare/In errore)]`. NO category filter (field inventory: Files have no category). sortable columns for every displayed file column including import date (O-02: import date sortable, not a filter). defaultSort newest-first.

    In `lib/validations/import.ts`, extend `parseImportFilters` to read `q`, `months` (`parseMonths`), `amountMin`/`amountMax` (`parseAmount`), `platform` (slug allowlist), `statusBucket` (`parseStatus(value, ['imported','pending','failed'])`).

    In `app/(app)/import/page.tsx`, add `getMonthsWithData('files')` to the Promise.all; replace the bespoke files-filter control with `<DataTableToolbar config={filesTableConfig} route={APP_ROUTES.import} monthsWithData={monthsWithData} filterOptions={{ platform }} />`; update `getFilterKey` (lines 16–24) to include `platform`, `statusBucket`, `months`, `amountMin`, `amountMax`.

    Update `tests/imports-dal.test.ts`: assert each statusBucket maps to the right status set; coverage-month TO_CHAR; amount ABS on negative_total. RED then GREEN.
  </action>
  <verify>
    <automated>yarn vitest run tests/imports-dal.test.ts && npx tsc --noEmit 2>&1 | grep -E "import" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export const filesTableConfig" app/(app)/import/files.table.ts` returns a match
    - `grep -nE "Elaborazione" app/(app)/import/files.table.ts` returns a match (D-23 label)
    - `grep -nE "inArray\(file\.status" lib/dal/imports.ts` returns a match (pending bucket)
    - `grep -nE "TO_CHAR\(.*referenceStartedAt.*YYYY-MM" lib/dal/imports.ts` returns a match (coverage months)
    - `grep -nE "ABS\(.*file\.negativeTotal" lib/dal/imports.ts` returns a match
    - `grep -nE "getMonthsWithData\('files'\)" app/(app)/import/page.tsx` returns a match
    - `grep -nE "DataTableToolbar" app/(app)/import/page.tsx` returns a match
    - `yarn vitest run tests/imports-dal.test.ts` exits 0; the tsc grep outputs `0`
  </acceptance_criteria>
  <done>Files table filters by 3 processing buckets, coverage months, platform, and amount via DataTableToolbar; import date remains sortable-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → three DAL filter builders | All three tables now translate user query strings into SQL conditions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Tampering | statusBucket / status / platform params | mitigate | Each parsed through `parseStatus`/slug allowlist before reaching the DAL; unknown buckets dropped, never matched as raw status |
| T-40-10 | Injection | amount / months SQL conditions | mitigate | Values bound via `sql` template parameters (`${...}`); months pre-validated to `YYYY-MM`, amounts to non-negative numeric (Wave 1 parsers) |
| T-40-11 | Information Disclosure | cross-user rows in any table | mitigate | Every DAL query retains its existing `eq(*.userId, userId)` scope from `verifySession`; new conditions are AND-ed, never replace the user scope |
| T-40-12 | Repudiation | removing expenses this-month default surfaces all-time data | accept | Intentional UX change (D-05); no security impact, data already user-scoped |
| T-40-SC | Tampering | npm installs | mitigate | No new package installs in this wave; slopcheck N/A |
</threat_model>

<verification>
- `yarn vitest run tests/transactions-dal.test.ts tests/expenses-dal.test.ts tests/imports-dal.test.ts` all green
- `npx tsc --noEmit` reports no new errors across the three pages, three DAL files, three validations
- Each table renders `<DataTableToolbar>` (the three bespoke filter components are no longer rendered)
- Expenses no longer clamps to this-month; status 4 counts as uncategorized
</verification>

<success_criteria>
- All three tables (Transactions, Expenses, Files) filter and sort end-to-end via the shared toolbar
- Field inventory honored per table (months on Transactions+Files, none on Expenses; category on T+E, none on Files)
- Status semantics correct: categorization (2 states) for T/E, processing (3 buckets) for Files; expense status 4 → uncategorized
</success_criteria>

<output>
Create `.planning/phases/40-table-filter-sort/40-04-SUMMARY.md` when done
</output>
