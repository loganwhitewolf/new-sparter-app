---
phase: 40-table-filter-sort
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - lib/dal/months-with-data.ts
  - components/data-table/MonthMultiPicker.tsx
  - components/data-table/AmountRangePicker.tsx
  - components/data-table/DataTableToolbar.tsx
  - tests/months-with-data-dal.test.ts
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11, D-12, D-20]
must_haves:
  truths:
    - "getMonthsWithData('transactions') and getMonthsWithData('files') return the distinct YYYY-MM strings (DESC) that actually contain data for the signed-in user"
    - "MonthMultiPicker renders a year switcher + 12-cell grid with no-data cells disabled, 'Tutto l'anno' toggle, relative presets, and writes months=YYYY-MM,YYYY-MM to the URL"
    - "AmountRangePicker writes amountMin/amountMax to the URL (absolute-value semantics)"
    - "DataTableToolbar renders the real MonthMultiPicker and AmountRangePicker for month-multi / amount-range fields instead of the Wave-2 placeholders"
  artifacts:
    - path: "lib/dal/months-with-data.ts"
      provides: "getMonthsWithData(table) per-table distinct-month query"
      contains: "export const getMonthsWithData"
    - path: "components/data-table/MonthMultiPicker.tsx"
      provides: "year-grid multi-select month picker"
      contains: "Tutto l'anno"
    - path: "components/data-table/AmountRangePicker.tsx"
      provides: "min/max absolute amount inputs"
      contains: "amountMin"
  key_links:
    - from: "components/data-table/DataTableToolbar.tsx"
      to: "components/data-table/MonthMultiPicker.tsx"
      via: "month-multi field type render branch"
      pattern: "MonthMultiPicker"
    - from: "lib/dal/months-with-data.ts"
      to: "verifySession"
      via: "user-scoped distinct-month query"
      pattern: "verifySession"
---

<objective>
Build the two new filter controls that have no equivalent in the current tables (CONTEXT 5-wave roadmap item 3): the `MonthMultiPicker` (year-grid, data-aware) backed by a new `getMonthsWithData` DAL query, and the `AmountRangePicker`. Then replace the Wave-2 placeholders in `DataTableToolbar` with these real controls.

Purpose: The month-multi temporal model (ADR 0009, D-07–D-12) and absolute-amount range (D-20) are the genuinely new UI. The picker must offer only months that contain data (D-09), which requires the per-table DAL query. Extract the near-complete prototype widget; the only delta is replacing mock month data with the `monthsWithData` prop (PATTERNS.md).
Output: `getMonthsWithData` DAL, `MonthMultiPicker`, `AmountRangePicker`, and toolbar render branches wired to them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-table-filter-sort/40-CONTEXT.md
@.planning/phases/40-table-filter-sort/40-PATTERNS.md
@app/proto/table-toolbar/shared.tsx
</context>

<artifacts_produced>
## Artifacts this plan produces
- `lib/dal/months-with-data.ts` — `getMonthsWithData(table: 'transactions' | 'files'): Promise<string[]>` (React-cached, session-scoped)
- `components/data-table/MonthMultiPicker.tsx` — `MonthMultiPicker` component + exported `monthLabel(ym)` helper
- `components/data-table/AmountRangePicker.tsx` — `AmountRangePicker` component
- `components/data-table/DataTableToolbar.tsx` — month-multi and amount-range render branches now use the real components
- `tests/months-with-data-dal.test.ts` — DAL query test
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getMonthsWithData DAL query (per-table, session-scoped)</name>
  <files>lib/dal/months-with-data.ts, tests/months-with-data-dal.test.ts</files>
  <read_first>
    - lib/dal/transactions.ts (lines 1–5 header `import 'server-only'` + `cache` + `db` + `verifySession`; line 474 area `db.execute(sql\`...\`)` raw-SQL pattern)
    - lib/db/schema.ts (transaction.occurredAt column; file.referenceStartedAt at line 330 — the coverage-start column for files)
    - tests/imports-dal.test.ts (the `db.execute` mock harness — `mocks.executeResult` shape; replicate for the new test)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (section "lib/dal/months-with-data.ts" lines 321–353 — exact query shape)
  </read_first>
  <behavior>
    - getMonthsWithData('transactions') runs verifySession, then a DISTINCT TO_CHAR(occurred_at,'YYYY-MM') query scoped to the user, ordered DESC, returning string[]
    - getMonthsWithData('files') runs the same shape against reference_started_at
    - When the user has no rows, returns []
  </behavior>
  <action>
    Create `lib/dal/months-with-data.ts` with the standard DAL header (`import 'server-only'`, `import { cache } from 'react'`, `import { db } from '@/lib/db'`, `import { sql } from 'drizzle-orm'`, `import { verifySession } from '@/lib/dal/auth'`). Export `getMonthsWithData = cache(async (table: 'transactions' | 'files'): Promise<string[]> => { ... })`. Call `verifySession()` to get `userId`. Branch on `table`: for `'transactions'` run `db.execute(sql\`SELECT DISTINCT TO_CHAR(occurred_at, 'YYYY-MM') AS ym FROM transaction WHERE user_id = ${userId} ORDER BY ym DESC\`)`; for `'files'` run the same against `FROM file ... TO_CHAR(reference_started_at, 'YYYY-MM')` with `reference_started_at IS NOT NULL` added to the WHERE. Map the result rows to `row.ym` strings and return the array. Use the project's existing `db.execute` result-shape convention (check `transactions.ts` line ~474 for whether rows are at `.rows` or the array directly, and match it).

    Write `tests/months-with-data-dal.test.ts` (RED first): mock `verifySession` to return a fixed `userId`, mock `db.execute` to return a fixed row set (`[{ ym: '2026-05' }, { ym: '2026-04' }]`), and assert both `'transactions'` and `'files'` calls return `['2026-05','2026-04']` and that `verifySession` was invoked. Assert the empty-result case returns `[]`.
  </action>
  <verify>
    <automated>yarn vitest run tests/months-with-data-dal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export const getMonthsWithData" lib/dal/months-with-data.ts` returns a match
    - `grep -nE "import 'server-only'" lib/dal/months-with-data.ts` returns a match
    - `grep -nE "verifySession" lib/dal/months-with-data.ts` returns a match
    - `grep -nE "reference_started_at|referenceStartedAt" lib/dal/months-with-data.ts` returns a match (files branch)
    - `yarn vitest run tests/months-with-data-dal.test.ts` exits 0
  </acceptance_criteria>
  <done>A session-scoped, per-table distinct-month query returns the only months a picker should offer, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: MonthMultiPicker + AmountRangePicker + toolbar wiring</name>
  <files>components/data-table/MonthMultiPicker.tsx, components/data-table/AmountRangePicker.tsx, components/data-table/DataTableToolbar.tsx</files>
  <read_first>
    - app/proto/table-toolbar/shared.tsx (lines 176–256 `MonthPicker` — year switcher, 12-cell grid, "Tutto l'anno", presets, disabled-cell pattern at 226–243; lines 38–42 `monthLabel`; lines 259–277 `AmountRange`)
    - components/data-table/DataTableToolbar.tsx (the Wave-2 component — locate the `month-multi` and `amount-range` placeholder branches to replace)
    - components/data-table/use-table-url.ts (updateParam — the pickers write through it)
    - .planning/phases/40-table-filter-sort/40-PATTERNS.md (sections "MonthMultiPicker.tsx" lines 223–257 — the only delta is replacing the mock `ALL_MONTHS` with the `monthsWithData` prop; "AmountRangePicker.tsx" lines 264–283)
    - .planning/phases/40-table-filter-sort/40-CONTEXT.md (D-12 year-grid; D-10 presets keep; "Month URL encoding: months=2026-04,2026-05")
  </read_first>
  <action>
    Create `components/data-table/MonthMultiPicker.tsx` (`'use client'`) by extracting the prototype `MonthPicker` (shared.tsx 176–256). Props: `{ value: string[]; monthsWithData: string[]; onChange: (months: string[]) => void }`. Delta from the prototype: replace the mock `ALL_MONTHS` source with the `monthsWithData` prop — a month cell is enabled iff its `YYYY-MM` is in `monthsWithData`; disabled cells use the dashed/30% style at lines 226–243. Keep the year switcher (`viewYear` `useState`), the 12-cell grid, `MONTH_ABBR`, the "Tutto l'anno" toggle (selects/clears all enabled months in the viewed year), and the relative presets ("Ultimi 3 mesi", "Quest'anno", "Anno scorso", D-10) resolving to concrete `YYYY-MM` chips. Export the `monthLabel(ym)` helper (lines 38–42) for chip labels. `onChange` emits the full selected `string[]`.

    Create `components/data-table/AmountRangePicker.tsx` (`'use client'`) by extracting `AmountRange` (shared.tsx 259–277). Props: `{ min: string; max: string; onMin: (v: string) => void; onMax: (v: string) => void }`. Two `<Input type="number" inputMode="decimal">` with "min €" / "max €" placeholders separated by an en-dash. (Absolute-value semantics are enforced DAL-side in Wave 4; the UI just collects two non-negative numbers.)

    In `components/data-table/DataTableToolbar.tsx`, replace the two Wave-2 placeholder branches: the `month-multi` field now renders `<MonthMultiPicker value={selectedMonths} monthsWithData={props.monthsWithData ?? []} onChange={(m) => updateParam('months', m.length ? m.join(',') : null)} />`, where `selectedMonths` is parsed from `searchParams.get('months')?.split(',') ?? []`. The `amount-range` field renders `<AmountRangePicker min={searchParams.get('amountMin') ?? ''} max={searchParams.get('amountMax') ?? ''} onMin={(v) => updateParam('amountMin', v || null)} onMax={(v) => updateParam('amountMax', v || null)} />`. Ensure the active-chip mapping uses `monthLabel` for month chips so chips read "Mag 2026" not "2026-05".
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/data-table/(MonthMultiPicker|AmountRangePicker|DataTableToolbar)" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "Tutto l'anno" components/data-table/MonthMultiPicker.tsx` returns a match
    - `grep -nE "monthsWithData" components/data-table/MonthMultiPicker.tsx` returns a match (prop, not mock)
    - `grep -nE "export function monthLabel|export const monthLabel" components/data-table/MonthMultiPicker.tsx` returns a match
    - `grep -nE "inputMode=\"decimal\"" components/data-table/AmountRangePicker.tsx` returns a match
    - `grep -nE "MonthMultiPicker" components/data-table/DataTableToolbar.tsx` returns a match
    - `grep -nE "AmountRangePicker" components/data-table/DataTableToolbar.tsx` returns a match
    - `grep -nE "months', m\.length|join\(','\)" components/data-table/DataTableToolbar.tsx` returns a match (comma-encoded URL write)
    - The verify command outputs `0` (no tsc errors in these three files)
  </acceptance_criteria>
  <done>The toolbar renders a data-aware year-grid month picker and an amount-range control wired to the months / amountMin / amountMax URL keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL months/amount params → DAL | User-supplied month tokens and amount bounds re-read on the server |
| verifySession → DB | Distinct-month query must be scoped to the authenticated user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Information Disclosure | getMonthsWithData cross-user leakage | mitigate | `WHERE user_id = ${userId}` from `verifySession()`; userId is parameterized, never interpolated from request input |
| T-40-07 | Tampering | month/amount values in URL | mitigate | Wave-1 parsers (`parseMonths`/`parseAmount`) validate on read before reaching SQL; picker only emits validated `YYYY-MM` and numeric strings |
| T-40-08 | Injection | TO_CHAR / DISTINCT raw SQL | mitigate | `sql` tagged template with parameterized `userId` only; column/format literals are static, not user-derived |
| T-40-SC | Tampering | npm installs | mitigate | No new package installs; vaul/shadcn already present; slopcheck N/A |
</threat_model>

<verification>
- `yarn vitest run tests/months-with-data-dal.test.ts` green
- `npx tsc --noEmit` reports no new errors in `components/data-table/` or `lib/dal/months-with-data.ts`
- Month chips display "Mag 2026" form via `monthLabel`
</verification>

<success_criteria>
- `getMonthsWithData` returns user-scoped distinct months per table, DESC
- `MonthMultiPicker` disables no-data cells and writes comma-encoded `months`
- `AmountRangePicker` writes `amountMin`/`amountMax`
- Toolbar uses the real controls (no remaining placeholders)
</success_criteria>

<output>
Create `.planning/phases/40-table-filter-sort/40-03-SUMMARY.md` when done
</output>
