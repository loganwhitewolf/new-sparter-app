---
phase: 40-table-filter-sort
plan: 05
type: execute
wave: 5
depends_on: ["40-04"]
files_modified:
  - components/data-table/DataTableToolbar.tsx
  - components/data-table/EmptyState.tsx
  - lib/validations/transactions.ts
  - lib/validations/expense.ts
  - lib/validations/import.ts
  - app/(app)/transactions/page.tsx
  - app/(app)/expenses/page.tsx
  - app/(app)/import/page.tsx
autonomous: true
requirements: [D-04, D-05, D-13, D-17, D-19]
must_haves:
  truths:
    - "An empty table distinguishes 'Nessun dato' (no rows at all) from 'Nessun risultato' (rows exist but filters exclude all)"
    - "Sortable headers expose aria-sort and the load-more region announces via aria-live; the mobile sort control is screen-reader labeled"
    - "Legacy URL params migrate: name→q, from/to→months are dropped/translated, period removed; old links still resolve without error"
    - "The prototype route app/proto/table-toolbar is deleted and yarn build is green"
  artifacts:
    - path: "components/data-table/EmptyState.tsx"
      provides: "EmptyState component with no-data vs no-result variants"
      contains: "Nessun risultato"
  key_links:
    - from: "lib/validations/transactions.ts"
      to: "q param"
      via: "name→q migration read"
      pattern: "name|q"
---

<objective>
Final polish (CONTEXT 5-wave roadmap item 5): empty-state distinction, accessibility pass, legacy URL migration, prototype deletion, and a green production build.

Purpose: Close out the phase so the unified system is production-ready and the old prototype/legacy URL surface is gone. This wave touches no new behavior — it hardens what Waves 1–4 shipped.
Output: `EmptyState` component, a11y attributes on the toolbar/tables, URL migration in the three validation parsers, deleted `app/proto/table-toolbar/`, `yarn build` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-table-filter-sort/40-CONTEXT.md
@.planning/phases/40-table-filter-sort/40-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this plan produces
- `components/data-table/EmptyState.tsx` — `EmptyState` with `variant: 'no-data' | 'no-result'`
- a11y attributes: `aria-sort` (already from Wave 2, verified), `aria-live` on load-more region, screen-reader label on mobile "Ordina"
- URL migration logic in the three validation parsers (name→q, drop from/to, drop period)
- `app/proto/table-toolbar/` deleted
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Empty-state distinction + a11y pass</name>
  <files>components/data-table/EmptyState.tsx, components/data-table/DataTableToolbar.tsx, app/(app)/transactions/page.tsx, app/(app)/expenses/page.tsx, app/(app)/import/page.tsx</files>
  <read_first>
    - components/data-table/DataTableToolbar.tsx (Wave 2/3 component — confirm `aria-sort` present on HeaderSortButton; locate the mobile "Ordina" control to add an aria-label)
    - app/(app)/transactions/page.tsx, app/(app)/expenses/page.tsx, app/(app)/import/page.tsx (how each renders its table body and where the "no rows" branch lives; the load-more / infinite-scroll region with the existing aria-live, if any)
    - .planning/phases/40-table-filter-sort/40-CONTEXT.md (D-04 aria-live on load-more end state; Wave 5 "Nessun dato vs nessun risultato" + a11y pass)
    - components/data-table/HeaderSortButton.tsx (confirm aria-sort markup from Wave 2)
  </read_first>
  <action>
    Create `components/data-table/EmptyState.tsx` exporting `EmptyState({ variant }: { variant: 'no-data' | 'no-result' })`. `no-data` renders the "Nessun dato" message (the table has zero rows regardless of filters — e.g. fresh account); `no-result` renders "Nessun risultato" with a hint to relax filters (rows exist in the table but the active filters exclude them all). Use existing empty-state styling already present in the table pages for visual consistency.

    In each of the three pages, replace the single generic empty message with `<EmptyState variant={...} />`. Compute the variant: it is `'no-result'` when any filter param is active in `searchParams` (q/months/amount/category/platform/status/statusBucket) AND the result set is empty; otherwise `'no-data'`. The "any filter active" check can reuse the same active-filter-key derivation the toolbar uses (extract it to a shared helper if convenient, or inline a small check against the known filter keys).

    A11y pass on `DataTableToolbar.tsx`: confirm `aria-sort` is emitted by `HeaderSortButton` (from Wave 2 — verify, do not duplicate); add `aria-label="Ordina"` to the mobile detached sort trigger and `role="region" aria-live="polite"` to the load-more status area if it is rendered inside the toolbar (otherwise add `aria-live="polite"` to the load-more region in the pages). Ensure each filter control in the panel has an associated `<label>` or `aria-label`.
  </action>
  <verify>
    <automated>grep -rEl "Nessun risultato" components/data-table/EmptyState.tsx && grep -rEl "EmptyState" app/(app)/transactions/page.tsx app/(app)/expenses/page.tsx app/(app)/import/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "Nessun dato" components/data-table/EmptyState.tsx` returns a match
    - `grep -nE "Nessun risultato" components/data-table/EmptyState.tsx` returns a match
    - `grep -nE "variant: 'no-data' \| 'no-result'|'no-data' \| 'no-result'" components/data-table/EmptyState.tsx` returns a match
    - `grep -lE "EmptyState" app/(app)/transactions/page.tsx app/(app)/expenses/page.tsx app/(app)/import/page.tsx` lists all three files
    - `grep -nE "aria-live" components/data-table/DataTableToolbar.tsx app/(app)/transactions/page.tsx` returns at least one match
    - `grep -nE "aria-label=\"Ordina\"" components/data-table/DataTableToolbar.tsx` returns a match
  </acceptance_criteria>
  <done>Empty tables distinguish no-data from no-result; sortable headers, load-more region, and mobile sort control are screen-reader accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Legacy URL migration + prototype deletion + build green</name>
  <files>lib/validations/transactions.ts, lib/validations/expense.ts, lib/validations/import.ts</files>
  <read_first>
    - lib/validations/transactions.ts (the parser extended in Wave 4 — confirm `name`→`q` back-compat read is present; finalize so `q` is canonical and `from`/`to` are no longer parsed into active filters)
    - lib/validations/expense.ts (confirm `period` is no longer defaulted after Wave 4)
    - lib/validations/import.ts (confirm `from`/`to` legacy date params are dropped in favor of `months`)
    - .planning/phases/40-table-filter-sort/40-CONTEXT.md (Wave 5: "URL migration name→q, from/to→months, remove period")
    - app/proto/table-toolbar/NOTES.md (prototype verdict — confirms the prototype's purpose is fulfilled and it can be deleted; mirrors Phase 38/39 prototype-deletion-on-final-plan pattern)
  </read_first>
  <action>
    Finalize URL migration in the three validation parsers: in `lib/validations/transactions.ts`, make `q` the canonical search key and treat a legacy `name` param as an alias on read (read `q ?? name`) so old shared links still resolve, but never emit `name`; stop parsing `from`/`to` into active filters (the month-multi `months` param supersedes them — old `from`/`to` links simply yield no temporal filter rather than erroring). In `lib/validations/expense.ts`, ensure no `period` default remains and a stray `period` param is ignored. In `lib/validations/import.ts`, drop legacy date params in favor of `months`. None of these should throw on an unrecognized legacy param — unknown params are ignored (total parsing).

    Delete the prototype route directory `app/proto/table-toolbar/` entirely (`variant-a.tsx`, `shared.tsx`, `variant-b.tsx`/`variant-c.tsx` if present, `NOTES.md`, `page.tsx`) — its patterns are now extracted into `components/data-table/`. This mirrors the Phase 38/39 convention of deleting prototype routes on the final plan.

    Run `yarn build` and resolve any type or build errors surfaced by the deletion or the migration (e.g. dangling imports of prototype modules). The build must complete green. Also run `yarn check:language` and fix any developer-facing Italian leakage introduced (Italian is allowed only on product surfaces per CLAUDE.md).
  </action>
  <verify>
    <automated>test ! -d app/proto/table-toolbar && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d app/proto/table-toolbar` exits 0 (prototype directory deleted)
    - `grep -rE "proto/table-toolbar" app components lib` returns no matches (no dangling references)
    - `grep -nE "q ?? |q \?\? input\.name|input\.name" lib/validations/transactions.ts` returns a match (name→q alias read)
    - `grep -cE "period.*\?\?.*'this-month'|'this-month'" lib/validations/expense.ts` outputs `0`
    - `yarn build` exits 0 (production build green)
    - `yarn check:language` exits 0
  </acceptance_criteria>
  <done>Legacy links migrate cleanly, the prototype route is gone, and the production build plus language check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy/hand-crafted URLs → validation parsers | Old or malformed query strings re-enter the parsers post-migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-13 | Denial of Service | malformed legacy params (name/from/to/period) | mitigate | Parsers remain total — unknown/legacy params are ignored, never thrown on; a stale shared link degrades to the default view rather than erroring |
| T-40-14 | Tampering | EmptyState variant inferred from searchParams | accept | Variant only chooses a message string; no query or auth decision depends on it; no security impact |
| T-40-SC | Tampering | npm installs | mitigate | No new package installs; deletion-only + parser edits; slopcheck N/A |
</threat_model>

<verification>
- `app/proto/table-toolbar` no longer exists and nothing imports it
- `yarn build` green; `yarn check:language` green
- Empty states render the correct variant; a11y attributes present
- Legacy `?name=` links resolve to the search box; legacy `?period=`/`?from=&to=` links degrade to default view without error
</verification>

<success_criteria>
- No-data vs no-result empty states distinct
- a11y: aria-sort, aria-live, labeled mobile sort + filter controls
- URL migration complete (name→q, from/to→months, period removed) with total parsing
- Prototype route deleted; `yarn build` green
</success_criteria>

<output>
Create `.planning/phases/40-table-filter-sort/40-05-SUMMARY.md` when done
</output>
