---
phase: "03"
plan: "01"
type: "implementation"
---

<objective>
03-expense-management 00
</objective>

<context>
Phase: 03 (expense-management) — Milestone: M001: Migration

Wave 0 prerequisites for Phase 3: test stubs, shadcn Table install, Sonner Toaster wiring, and decimal utility creation.

Purpose: Establishes the test contract before any implementation, installs the missing Table component, enables toast feedback globally, and clears the STATE.md blocker on monetary arithmetic utils.
Output: tests/expenses.spec.ts with 8 fixme stubs, app/layout.tsx with Toaster, components/ui/table.tsx installed, lib/utils/decimal.ts created.
</context>

<must_haves>
- "Test stubs exist for all 8 EXP-01/02/03 behaviors (fixme — not yet passing)"
- "Sonner Toaster is mounted in root layout so toast() calls are visible in the DOM"
- "shadcn Table component is installed at components/ui/table.tsx"
- "toDecimal() and toDbDecimal() utils exist and are correctly typed"
</must_haves>
