---
phase: "19"
plan: "02"
type: "implementation"
---

<objective>
Build `/settings/categories` page with CRUD dialogs and embedded pattern panel
</objective>

<context>
Phase: 19 (s03) — Milestone: Category Management & UX Polish
</context>

<must_haves>
- `/settings/categories` is the main user-facing category management route.
- Interactive CRUD controls are isolated in client components and call the T01 actions via `useActionState`.
- The page includes pattern assignment/management from the same route using existing pattern actions.
- User-owned rows visibly show `Personale`; system subcategory overrides show the personalized name without losing original-name context.
- Existing `/settings/patterns` still renders and behaves as before.
</must_haves>
