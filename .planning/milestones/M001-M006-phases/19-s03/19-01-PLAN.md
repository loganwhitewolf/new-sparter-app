---
phase: "19"
plan: "01"
type: "implementation"
---

<objective>
Implement authenticated category CRUD actions and guards
</objective>

<context>
Phase: 19 (s03) — Milestone: Category Management & UX Polish
</context>

<must_haves>
- Category actions are authenticated Server Actions and never trust a submitted `userId`.
- User-owned category/subcategory creation and rename persist owner metadata compatible with S02 merged-tree reads.
- System subcategory rename writes/updates `user_subcategory_override` and preserves the original system name.
- Delete guards block linked current-user expenses with an Italian count-bearing message.
- Successful mutations revalidate via `revalidateCategorizationSurfaces()`; validation, auth, not-owned, duplicate, guard, and unexpected failures do not.
</must_haves>
