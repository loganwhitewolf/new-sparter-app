---
phase: "02"
plan: "05"
type: "implementation"
---

<objective>
02-authentication 04
</objective>

<context>
Phase: 02 (authentication) — Milestone: M001: Migration

Wire all UI and route protection into a complete authentication flow: update proxy.ts with real session checks and staging bypass, wire the login form to signInAction, create the register page with signUpAction, and update the topbar with live session data and logout.

Purpose: This is the final plan — after it completes, all four phase success criteria are testable: signup → /dashboard, login → session persists, /dashboard without session → /login, x-staging-key header → bypass.

Output: proxy.ts (route protection), app/(auth)/login/page.tsx (wired), app/(auth)/register/page.tsx (new), components/layout/topbar.tsx (session wired).

Note: Alert shadcn component may need install (`npx shadcn@latest add alert`) — check components/ui/alert.tsx exists first.
</context>

<must_haves>
- "proxy.ts checks x-staging-key header FIRST, then Better Auth session, redirects unauthenticated to /login"
- "login page wired to signInAction via useActionState — error banner renders on failure"
- "register page created with signUpAction — same structure as login, Italian copy"
- "topbar shows real session email (first char as avatar fallback), logout calls signOutAction"
</must_haves>
