---
phase: "15"
plan: "03"
type: "implementation"
---

<objective>
Render the state-aware import row action matrix
</objective>

<context>
Phase: 15 (s05) — Milestone: Import Management
</context>

<must_haves>
- Each status in the fixed `file_status` enum has an explicit tested action outcome.
- Imported rows link to exact transactions via `/transactions?importId=<fileId>` and still expose S03 delete plus S02 rename.
- Failed unknown-format rows link to S04 configure and retry paths; non-unknown failures do not show configure.
- In-progress rows show disabled Italian pending copy and no active duplicate analyze/import/delete actions.
</must_haves>
