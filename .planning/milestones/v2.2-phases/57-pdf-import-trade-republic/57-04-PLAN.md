---
phase: 57-pdf-import-trade-republic
plan: 04
type: execute
wave: 3
depends_on: ["57-02", "57-03"]
files_modified:
  - lib/services/import-parsers.ts
  - tests/import-detector.test.ts
  - tests/trade-republic-pdf-parser.test.ts
autonomous: false
requirements: [PDF-04]
user_setup: []

must_haves:
  truths:
    - "parseImportFile dispatches a .pdf file to parseTradeRepublicPdf and returns its ParsedImportFile (per D-02, PDF-04)"
    - "The detector (scoreCandidate / detectImportFormat) matches the TR synthetic headers against the seeded Trade Republic import_format_version with confidence at or above 0.8 (per D-02, PDF-04)"
    - "normalizeTransactionRow produces a valid signed amount and parsed date for TR rows via amountType separate (importo_entrata positive, importo_uscita negative) (per D-07, PDF-04)"
    - "An end-to-end test takes the Wave 0 fixture through parseImportFile -> detector -> normalizeTransactionRow and yields correctly-signed, dated, dedupable rows (per PDF-04)"
    - "CSV and XLSX parsing through parseImportFile is unchanged (no regression)"
  artifacts:
    - lib/services/import-parsers.ts
    - tests/import-detector.test.ts
    - tests/trade-republic-pdf-parser.test.ts
  key_links:
    - "parseImportFile branch order: .xlsx/.xls first, then a new .pdf branch BEFORE the CSV fallback (a PDF must never reach the CSV decode path)"
    - "scoreCandidate gives delimiterScore 1.0 when parsed.delimiter is null; headerScore 1.0 and signatureScore 1.0 when synthetic headers equal the seeded columns -> confidence >= 0.9"
    - "No changes to lib/services/import.ts (analyzeFile/importFile already call parseImportFile) — PDF support is transparent (research Q2)"
---

<objective>
Wire the Trade Republic PDF parser into the import pipeline by adding a .pdf dispatch branch to parseImportFile, then prove end-to-end that a TR fixture flows through detector -> normalizeTransactionRow -> dedup -> preview unchanged using synthetic headers.

Purpose: PDF-04 — PDF-extracted rows pass through the detector, normalizeTransactionRow, dedup, and preview unchanged. This is the integration seam: the single correct place to attach the parser (research Q2 — attaching in import.ts was explicitly rejected in ADR 0014 as it duplicates the pipeline).
Output: parseImportFile understands .pdf; a TR fixture detects as Trade Republic and normalizes into correctly-signed transactions; no changes to import.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pdf-import-trade-republic/57-CONTEXT.md
@.planning/phases/57-pdf-import-trade-republic/57-RESEARCH.md
@lib/services/import-parsers.ts
@.planning/phases/57-pdf-import-trade-republic/57-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the .pdf dispatch branch to parseImportFile</name>
  <files>lib/services/import-parsers.ts, tests/trade-republic-pdf-parser.test.ts</files>
  <behavior>
    - parseImportFile(bytes, { fileName: "statement.pdf" }) returns the result of parseTradeRepublicPdf (delimiter null, TR synthetic headers, rows)
    - parseImportFile still routes .xlsx and .xls to parseXlsx and .csv to the CSV path (no regression)
    - A .pdf file never reaches the CSV decode/chooseDelimiter path
    - The size cap and empty-file guards in parseImportFile still apply to .pdf inputs before dispatch
  </behavior>
  <read_first>
    - lib/services/import-parsers.ts (parseImportFile lines 164-233: the size guard 173-177, empty guard 179-181, lowerName computation 183, the xlsx branch 186-196, the CSV fallback 198-227)
    - lib/services/trade-republic-pdf-parser.ts (parseTradeRepublicPdf signature and return type from Wave 2)
    - .planning/phases/57-pdf-import-trade-republic/57-RESEARCH.md (Q2 integration point — extend import-parsers.ts not import.ts)
  </read_first>
  <action>
    In lib/services/import-parsers.ts, import parseTradeRepublicPdf from lib/services/trade-republic-pdf-parser. Inside parseImportFile, add a branch after the .xlsx/.xls branch and BEFORE the CSV decode block: if lowerName.endsWith('.pdf'), return await parseTradeRepublicPdf(bytes, options). The existing size cap (line 173) and empty-file guard (line 179) run before this branch and continue to apply to PDFs. The branch ordering matters — a .pdf must never fall through to chooseDelimiter / parseCsv (that path expects text and would fail).
    Add a test in tests/trade-republic-pdf-parser.test.ts asserting parseImportFile(bytes, { fileName: 'trade-republic-sample.pdf' }) returns the same ParsedImportFile shape as calling parseTradeRepublicPdf directly (delimiter null, TR synthetic headers, expected rowCount). Do NOT modify lib/services/import.ts — analyzeFile and importFile already call parseImportFile and receive PDF support transparently (research Q2). Do NOT duplicate the parser call anywhere else.
  </action>
  <verify>
    <automated>yarn test tests/trade-republic-pdf-parser.test.ts -t "parseImportFile|dispatch|pdf branch" &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('lib/services/import-parsers.ts','utf8');if(!/endsWith\('\.pdf'\)/.test(s))throw new Error('.pdf branch missing');if(!/parseTradeRepublicPdf/.test(s))throw new Error('parser not imported');console.log('pdf dispatch wired')"</automated>
  </verify>
  <done>parseImportFile routes .pdf to parseTradeRepublicPdf before the CSV path; .xlsx/.csv routing is unchanged; the dispatch test passes; import.ts is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end detector + normalize test for the TR fixture</name>
  <files>tests/import-detector.test.ts</files>
  <behavior>
    - Feeding the TR synthetic ParsedImportFile (headers data/descrizione/importo_entrata/importo_uscita, delimiter null) to the detector matches the seeded Trade Republic import_format_version with confidence >= 0.8
    - normalizeTransactionRow with the TR platform config maps importo_entrata to a positive amount and importo_uscita to a negative amount, and parses the data column to a valid date
    - A credit row and a debit row from the fixture produce opposite-signed amounts
    - The detector still resolves the existing CSV/XLSX formats correctly (no regression)
  </behavior>
  <read_first>
    - tests/import-detector.test.ts (existing detector test structure, how it builds ParsedImportFile inputs and platform configs, how it invokes scoreCandidate/detectImportFormat)
    - lib/services/import-format-detector.ts (scoreCandidate weights: headerScore 45%, signatureScore 5%, delimiterScore 5%; how synthetic headers and headerSignature are compared)
    - lib/utils/import.ts (normalizeTransactionRow amountType separate path; parseItalianAmount; parseBankDate)
    - scripts/seed-data.ts (the seeded TR importFormatVersions entry from Wave 0 — the platform config to mirror in the test)
  </read_first>
  <action>
    Extend tests/import-detector.test.ts with a Trade Republic case. Construct a ParsedImportFile matching the parser's output (headers [data, descrizione, importo_entrata, importo_uscita], delimiter null, a credit row and a debit row drawn from the fixture's actual values). Build the platform/format config from the seeded TR import_format_version values (amountType separate, positiveAmountColumn importo_entrata, negativeAmountColumn importo_uscita, timestampColumn data, descriptionColumn descrizione, delimiter ","). Assert the detector matches Trade Republic with confidence >= 0.8 (research Q3 predicts >= 0.9: headerScore 1.0, signatureScore 1.0 since headers.join(',') equals the seeded headerSignature, delimiterScore 1.0 because parsed.delimiter is null — Pitfall 5).
    Assert normalizeTransactionRow on a credit row yields a positive amount, on a debit row yields a negative amount (abs * -1 path, research Q6), and that the data column parses to a valid date via parseBankDate (research A2 / D-07). Assert the existing CSV/XLSX detector cases still pass (run the file, not just the new test). Use the project decimal helpers when asserting amounts; do not hand-roll monetary comparisons.
  </action>
  <verify>
    <automated>yarn test tests/import-detector.test.ts</automated>
  </verify>
  <done>The detector matches the TR synthetic headers to the seeded Trade Republic format with confidence >= 0.8, normalizeTransactionRow signs credit/debit correctly and parses TR dates, and existing detector cases remain green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end PDF import verification through the running app</name>
  <action>Human runs the app and verifies the full PDF import path end to end (see how-to-verify). This is interactive functional verification of upload, detection, preview signs, and savings-plan aggregation that automated tests cannot fully cover.</action>
  <what-built>The full PDF import path: upload a Trade Republic PDF, have it detected as Trade Republic, preview the extracted transactions with correct signs, and confirm dedup/aggregation works.</what-built>
  <how-to-verify>
    1. Ensure the dev database has the TR seed (yarn db:seed from Wave 0) and the app is running (yarn dev).
    2. Go to the import page and upload the Trade Republic PDF (the Wave 0 fixture or a real TR statement).
    3. Confirm the file is accepted (no MIME/extension/size error) and the detector identifies it as Trade Republic.
    4. In the preview, confirm: credit movements show as positive, debit movements as negative; dates are correct; the running totals look right; a recurring savings-plan row (the quantity: row) aggregates into a single Expense rather than splitting per quantity.
    5. Confirm an oversized (>5 MB) or tampered-balance PDF is rejected with an explicit error and imports nothing.
  </how-to-verify>
  <resume-signal>Type "approved" if the TR PDF imports with correct signs and aggregation, or describe what was wrong (e.g. signs flipped, rows doubled = PANORAMICA not discarded, savings plan split).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parseImportFile dispatch | A misrouted .pdf reaching the CSV path would silently produce garbage rows; branch ordering is the control |
| detector confidence | Low confidence could misattribute a TR PDF to another format; synthetic-header exact match guards this |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-04-01 | Tampering | parseImportFile branch order | medium | mitigate | .pdf branch placed before the CSV fallback and tested so a PDF never reaches chooseDelimiter/parseCsv |
| T-57-04-02 | Spoofing | detector format match | low | mitigate | Synthetic headers must exactly equal the seeded import_format_version columns; detector confidence threshold >= 0.8 asserted in test |
| T-57-04-03 | Elevation of Privilege | R2 read in import flow | low | accept | getFileForUser ownership check in import.ts unchanged; this plan adds no new data access path |
</threat_model>

<verification>
- parseImportFile routes .pdf to parseTradeRepublicPdf before CSV; xlsx/csv unchanged
- import.ts untouched
- detector matches TR synthetic headers with confidence >= 0.8
- normalizeTransactionRow signs credit/debit correctly and parses TR dates
- existing detector tests green
- human-verified end-to-end import with correct signs and savings-plan aggregation
</verification>

<success_criteria>
A Trade Republic PDF uploaded through the app is detected as Trade Republic, its transactions preview with correct signs and dates, recurring savings-plan rows aggregate into one Expense, and oversized/tampered PDFs are rejected — all through the unchanged detector/normalize/dedup/preview pipeline.
</success_criteria>

<output>
Create `.planning/phases/57-pdf-import-trade-republic/57-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `lib/services/import-parsers.ts` (MODIFIED) — `.pdf` dispatch branch in `parseImportFile` routing to `parseTradeRepublicPdf` before the CSV fallback
- `tests/import-detector.test.ts` (MODIFIED) — Trade Republic detection + `normalizeTransactionRow` sign/date case
- `tests/trade-republic-pdf-parser.test.ts` (MODIFIED) — `parseImportFile` dispatch test
- No change to `lib/services/import.ts` — PDF support is transparent through the existing `parseImportFile` call sites
