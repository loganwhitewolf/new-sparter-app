---
phase: 56-import-format-refactor
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - lib/db/schema.ts
  - drizzle/migrations
autonomous: true
requirements: [IFMT-01]
must_haves:
  truths:
    - "import_format_version carries the full parsing contract columns (delimiter, descriptionColumn, amountType, amountColumn, positiveAmountColumn, negativeAmountColumn, timestampColumn, dateFormat, dateReplace, decimalReplace, multiplyBy, descriptionStripPattern), added as NULLABLE in this transition migration so existing rows are valid before backfill."
    - "platform still carries the same contract columns at the end of this plan (they are dropped in Plan 03 AFTER the seed-extras data copy) — this plan only ADDS to import_format_version, it does not drop from platform."
    - "A generated SQL migration file exists under drizzle/migrations and was produced by drizzle-kit generate, not drizzle-kit push."
  artifacts:
    - "lib/db/schema.ts (importFormatVersion gains contract columns)"
    - "drizzle/migrations (new generated ADD COLUMN migration)"
  key_links:
    - "the new import_format_version columns mirror the exact types of the platform columns (varchar lengths, amountTypeEnum, boolean defaults, integer multiplyBy default 1) so the Plan 03 copy is type-identical and behavior-preserving"
---

<objective>
Move the parsing contract onto `import_format_version` at the schema layer, as the FIRST of a two-step migration (add-then-drop). Add the twelve contract columns to `importFormatVersion` in lib/db/schema.ts as NULLABLE, generate the SQL via `drizzle-kit generate`, and DO NOT drop the platform columns yet (D: ADR 0013 contract ownership; IFMT-01 schema half). The drop happens in Plan 03 only after the seed-extras data copy runs, so production rows are never left without a contract (Claude's-Discretion item in CONTEXT.md: "keep the platform contract columns nullable through a transition migration vs drop in one step — as long as the seed-extras data copy runs before any drop").

Purpose: Give `import_format_version` the columns it must own so versioning becomes real (one contract per version, not per platform).
Output: schema change + generated migration that ADDS the contract to import_format_version.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-import-format-refactor/56-CONTEXT.md
@docs/adr/0013-import-format-owns-parsing-contract.md
@CLAUDE.md
@lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the parsing contract columns to importFormatVersion (nullable)</name>
  <files>lib/db/schema.ts</files>
  <read_first>
    - lib/db/schema.ts `platform` table (≈ line 253) — the authoritative column definitions to mirror: `delimiter` varchar(4) notNull, `descriptionColumn` varchar(120) notNull, `amountType` amountTypeEnum notNull, `amountColumn`/`positiveAmountColumn`/`negativeAmountColumn` varchar(120) nullable, `timestampColumn` varchar(120) notNull, `dateFormat` varchar(60) nullable, `dateReplace` boolean default(false) notNull, `decimalReplace` boolean default(false) notNull, `multiplyBy` integer default(1) notNull, `descriptionStripPattern` text nullable.
    - lib/db/schema.ts `importFormatVersion` table (≈ line 289) — where the new columns are added; it already has the `unique(platformId, version)` constraint that this refactor makes meaningful.
  </read_first>
  <action>In lib/db/schema.ts, add the twelve parsing-contract columns to the `importFormatVersion` pgTable definition, using the SAME Drizzle column builders and lengths as the matching `platform` columns, but with one critical difference: declare every added column as NULLABLE (omit `.notNull()`) in this transition migration so the ALTER TABLE on the existing populated import_format_version rows succeeds without a default-backfill conflict. Add: `delimiter` varchar(4); `descriptionColumn` varchar(120); `amountType` amountTypeEnum; `amountColumn` varchar(120); `positiveAmountColumn` varchar(120); `negativeAmountColumn` varchar(120); `timestampColumn` varchar(120); `dateFormat` varchar(60); `dateReplace` boolean (no default in transition, nullable); `decimalReplace` boolean (nullable); `multiplyBy` integer (nullable); `descriptionStripPattern` text. Leave the `platform` table columns untouched in this plan. Use snake_case DB column names identical to the platform table (e.g. description_column, amount_type, positive_amount_column, timestamp_column, date_format, date_replace, decimal_replace, multiply_by, description_strip_pattern).</action>
  <verify>
    <automated>yarn lint lib/db/schema.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - importFormatVersion in schema.ts declares all twelve contract columns, all nullable, with DB column names matching the platform table.
    - The platform table definition is unchanged (no columns removed in this plan).
    - `yarn lint lib/db/schema.ts` passes.
  </acceptance_criteria>
  <done>importFormatVersion owns the contract columns at the type level; platform still has them (dropped in Plan 03). IFMT-01 schema-add half complete.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the ADD-COLUMN SQL migration with drizzle-kit generate</name>
  <files>drizzle/migrations</files>
  <read_first>
    - CLAUDE.md "Drizzle migrations" rule — NEVER `drizzle-kit push` in production; always `drizzle-kit generate` then `scripts/migrate.ts`. The package script is `yarn db:generate` (= `drizzle-kit generate`).
    - package.json scripts: `db:generate`, `db:migrate`.
  </read_first>
  <action>Run `yarn db:generate` to emit a new timestamped SQL migration under drizzle/migrations that ALTER TABLE import_format_version ADD COLUMN for the twelve nullable contract columns. Inspect the generated SQL and confirm it ONLY adds columns to import_format_version and does NOT drop or alter any platform column. Do NOT run `drizzle-kit push`. Applying the migration against a real DB (yarn db:migrate) is an operator step done at deploy time and is NOT part of this autonomous plan — the deliverable here is the committed generated SQL file and the matching journal entry.</action>
  <verify>
    <automated>git status --porcelain drizzle/migrations | grep -v '^#' | grep -c '\.sql$'</automated>
  </verify>
  <acceptance_criteria>
    - A new `.sql` migration file appears under drizzle/migrations containing only ADD COLUMN statements on import_format_version.
    - drizzle/migrations/meta journal updated by the generator.
    - No `drizzle-kit push` was used; no platform column is dropped or altered by this migration.
  </acceptance_criteria>
  <done>A generated transition migration adds the nullable contract columns to import_format_version, ready for the operator migrate step at deploy and for the Plan 03 data copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production DB | A generated DDL migration will later be applied to the production database; an incorrect or destructive migration corrupts the import contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-02 | Tampering | generated SQL migration on import_format_version | high | mitigate | Add-only nullable columns in this step; explicit inspection that the generated SQL touches only import_format_version and drops nothing; the platform-column DROP is deferred to Plan 03 and gated behind the seed-extras data copy. |
| T-56-03 | Denial of Service | applying DDL without generate (push) | high | mitigate | Migration produced via `drizzle-kit generate` (`yarn db:generate`) only; `drizzle-kit push` is forbidden per CLAUDE.md; apply runs via scripts/migrate.ts at operator time. |
</threat_model>

<verification>
- New generated `.sql` migration present under drizzle/migrations, ADD COLUMN only, import_format_version only.
- `yarn lint lib/db/schema.ts` clean.
- `yarn test tests/import-hash-contract.test.ts` still GREEN (schema add alone changes no runtime parsing path).
</verification>

<success_criteria>
import_format_version owns the parsing-contract columns at the schema level (nullable, transition state); a generated add-only migration exists; platform columns remain intact for Plan 03 to copy then drop.
</success_criteria>

<output>
Create `.planning/phases/56-import-format-refactor/56-02-SUMMARY.md` when done. Record the generated migration filename and confirm it is ADD-COLUMN-only on import_format_version.
</output>
