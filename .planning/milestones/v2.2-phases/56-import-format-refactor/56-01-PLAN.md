---
phase: 56-import-format-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/import-hash-contract.test.ts
autonomous: true
requirements: [IFMT-02]
must_haves:
  truths:
    - "Running the new regression test against the CURRENT (pre-refactor) code passes GREEN — it pins the transactionHash output for every existing CSV fixture."
    - "Each pinned transactionHash is a hard-coded 64-char hex literal in the test, so any future drift in normalization, multiplier, sign, date parsing, or descriptionStripPattern fails the test."
  artifacts:
    - "tests/import-hash-contract.test.ts"
  key_links:
    - "test imports normalizeTransactionRow from lib/utils/import.ts and the platform seed shapes from scripts/seed-data.ts — the same source the detector/import path consume, so the pin tracks production behavior"
---

<objective>
Establish the behavior-preserving safety net for the whole phase: a regression test that pins the exact `transactionHash` each existing CSV fixture produces under the CURRENT code, BEFORE any column is moved (D: ADR 0013 — "A regression test over real fixtures is the safety net — recommended to write FIRST, before moving columns"; IFMT-02).

This test is the contract baseline. It must be committed GREEN against unmodified code. Plans 02–04 move the parsing contract from `platform` to `import_format_version` and re-point every consumer; this test re-running GREEN after those changes is the proof of behavior preservation (IFMT-02). Do NOT change any production source in this plan.

Purpose: Make the refactor verifiable. Without a pinned baseline first, "behavior-preserving" is unprovable.
Output: tests/import-hash-contract.test.ts, GREEN against current code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-import-format-refactor/56-CONTEXT.md
@docs/adr/0013-import-format-owns-parsing-contract.md
@CLAUDE.md

# Source the test pins against (read these to build the pin):
@lib/utils/import.ts
@scripts/seed-data.ts
@tests/import-detector.test.ts
@tests/fixtures/import/general.csv
@tests/fixtures/import/satispay.csv
@tests/fixtures/import/intesa-sp.csv
@tests/fixtures/import/intesa-sp-carta-credito.csv
@tests/fixtures/import/revolut.csv
@tests/fixtures/import/fineco.csv
@tests/fixtures/import/crypto-com.csv
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin transactionHash for every CSV fixture against current code</name>
  <files>tests/import-hash-contract.test.ts</files>
  <read_first>
    - lib/utils/import.ts — `normalizeTransactionRow(row, platform, context)` returns `{ transactionHash, amount, occurredAt, description, descriptionHash, valid }`; `computeTransactionHash` joins `userId | occurredAt.toISOString() | amount | normalizeDescription(description)` then sha256. The `ImportPlatformConfig` fields consumed: `timestampColumn`, `descriptionColumn`, `descriptionStripPattern`, `amountType`, `amountColumn`, `positiveAmountColumn`, `negativeAmountColumn`, `multiplyBy`.
    - scripts/seed-data.ts — `platforms` array (id 1 General .. id 7 Fineco). Each shape carries the contract columns. NOTE: `descriptionStripPattern` is NOT on the seed-data shapes (it is applied to Fineco only via a seed-extras STEP `set-fineco-description-strip-pattern` = `\\s+Carta N\\..*$`). To faithfully pin Fineco, set `descriptionStripPattern: '\\s+Carta N\\..*$'` on the Fineco config in this test and `null` for the others — mirror exactly what production rows hold today.
    - tests/import-detector.test.ts — shows how to read fixtures via `parseImportFile(readFileSync(fixturePath(name)), { fileName })` and how the `formats`/`platform` config is assembled from `seedPlatforms`.
  </read_first>
  <behavior>
    - For each CSV fixture present under tests/fixtures/import (general, crypto-com, satispay, intesa-sp, intesa-sp-carta-credito, revolut, fineco), parse it with `parseImportFile`, then run `normalizeTransactionRow` for EACH data row using that platform's contract config (built from `scripts/seed-data.ts` plus the Fineco descriptionStripPattern noted above), userId = a fixed literal e.g. 'hash-contract-user'.
    - Collect the resulting `transactionHash` for every row (skip rows where the hash is null, but assert at least one non-null hash per fixture so an accidental all-null does not pass vacuously).
    - Assert the collected hashes for each fixture EQUAL a hard-coded expected array of 64-char hex literals captured from the current run. (First write the test computing+logging the hashes, run once to capture, then paste the literals as the expectation so the pin is a static literal, not self-referential.)
    - Also pin, per fixture, the `amount` and `occurredAt.toISOString()` for the first row as secondary literals — these catch sign/multiplier/date drift with a readable failure even if the hash literal is right by coincidence.
  </behavior>
  <action>Create tests/import-hash-contract.test.ts as a pure unit test (no DB, no R2, no network) using vitest. Import `parseImportFile` from lib/services/import-parsers, `normalizeTransactionRow` from lib/utils/import, and `platforms` from scripts/seed-data. Map each fixture file name to its seed platform by slug. Build each platform's `ImportPlatformConfig` from the seed shape, overriding `descriptionStripPattern` to `\\s+Carta N\\..*$` for the fineco slug and `null` otherwise (this reproduces today's production state where the strip pattern lives on the Fineco platform row). Use a fixed `userId` literal so the hash is deterministic. For every data row produce the hash via `normalizeTransactionRow` and assert the per-fixture array of hashes equals a hard-coded literal array of hex strings. Capture those literals by running the test once with a temporary log, then bake them in as the expectation. Add per-fixture first-row `amount` and `occurredAt` ISO literal assertions. Do NOT modify any file under lib/, scripts/, or app/ in this plan — the test must pass against UNCHANGED production code.</action>
  <verify>
    <automated>yarn test tests/import-hash-contract.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test tests/import-hash-contract.test.ts` is GREEN against the current, unmodified codebase.
    - The expected hashes are hard-coded hex literals (not derived at runtime from the same call under test).
    - At least one non-null hash is asserted per fixture (no vacuous pass).
    - No file outside tests/ was modified.
  </acceptance_criteria>
  <done>tests/import-hash-contract.test.ts exists, contains static hash literals for every CSV fixture, and passes GREEN against the pre-refactor code, giving Plans 02–04 a verifiable behavior-preservation gate (IFMT-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test only; no new runtime trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | regression test baseline correctness | medium | mitigate | Hashes are hard-coded literals captured from current code; a non-vacuous assertion (≥1 non-null hash per fixture) prevents a falsely-green baseline that would mask drift in Plans 02–04. |
</threat_model>

<verification>
- `yarn test tests/import-hash-contract.test.ts` GREEN on current HEAD before any source change.
- `yarn check:language` clean (the test file is developer-facing English).
</verification>

<success_criteria>
A committed regression test pins the transactionHash output of all CSV fixtures against current behavior, GREEN, ready to gate the refactor.
</success_criteria>

<output>
Create `.planning/phases/56-import-format-refactor/56-01-SUMMARY.md` when done. In it, list the exact pinned hash literals captured (or the fixtures covered) so Plan 04 can confirm the same test stays GREEN post-refactor.
</output>
