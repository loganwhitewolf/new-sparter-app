---
phase: 37-flow-nature-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/nature-labels.ts
  - tests/nature-labels.test.ts
  - tests/dashboard-charts.test.tsx
  - tests/dashboard-dal.test.ts
  - tests/category-actions.test.ts
  - tests/category-settings-seed.ts
autonomous: true
requirements:
  - R-FN-06
  - R-FN-07
must_haves:
  truths:
    - "lib/utils/nature-labels.ts exists and exports NATURE_LABELS, NATURE_ORDER, NATURE_COLORS, FlowNature"
    - "tests/dashboard-charts.test.tsx no longer asserts presence of 'Entrate'/'Uscite' raw labels and asserts at least one Italian nature label"
    - "Wave 0 RED scaffolds exist for R-FN-03, R-FN-04, R-FN-07, R-FN-08, R-FN-09 and fail with informative messages (not 'cannot find module')"
  artifacts:
    - path: lib/utils/nature-labels.ts
      provides: "Shared nature enum union, label map, ordered array, color map"
      exports: ["FlowNature", "NATURE_LABELS", "NATURE_ORDER", "NATURE_COLORS"]
    - path: tests/nature-labels.test.ts
      provides: "Unit coverage for label utility (R-FN-07)"
    - path: tests/dashboard-charts.test.tsx
      provides: "Chart test suite updated for nature-segmented assertions (R-FN-06)"
  key_links:
    - from: tests/dashboard-charts.test.tsx
      to: lib/utils/nature-labels.ts
      via: "imported NATURE_LABELS used in assertions"
      pattern: "from '@/lib/utils/nature-labels'"
---

<objective>
Establish the shared `nature-labels` utility (single source of truth per D-04, D-05, D-06) and update Wave 0 tests so the implementation waves never run against stale assertions. This plan also lands the RED scaffolds for the new DAL function, the chart rewrite, and the actions, so subsequent waves operate against failing tests they must turn GREEN.

Purpose: Without this plan, Wave 2/3 work would either be blocked by `dashboard-charts.test.tsx` asserting `Entrate`/`Uscite` (per RESEARCH key finding #4) or proceed with no test coverage on the new behaviour. Establishing the label utility first also unblocks Wave 3 (chart) and Plan 37-05 (settings select) from divergent label maps.

Output:
- `lib/utils/nature-labels.ts` (new shared module)
- `tests/nature-labels.test.ts` (new)
- Updates to `tests/dashboard-charts.test.tsx`, `tests/dashboard-dal.test.ts`, `tests/category-actions.test.ts`, `tests/category-settings-seed.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-flow-nature-chart/37-CONTEXT.md
@.planning/phases/37-flow-nature-chart/37-RESEARCH.md
@docs/adr/0003-flownature-at-subcategory-level.md
@docs/adr/0004-nature-segments-algebraic-sum.md
@tests/dashboard-charts.test.tsx
@tests/dashboard-dal.test.ts
@tests/category-actions.test.ts
@tests/category-settings-seed.ts
@components/ui/chart.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/utils/nature-labels.ts shared module</name>
  <files>lib/utils/nature-labels.ts, tests/nature-labels.test.ts</files>
  <read_first>
    - .planning/phases/37-flow-nature-chart/37-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/37-flow-nature-chart/37-RESEARCH.md (Pattern 5)
    - lib/utils/decimal.ts (project utility import convention)
  </read_first>
  <behavior>
    - NATURE_LABELS maps each enum value to its exact Italian label (D-04) and the sentinel 'unclassified' to 'Non classificato' (D-05).
    - NATURE_ORDER is an ordered array of FlowNature values followed by a trailing `null` (the 'unclassified' slot).
    - NATURE_COLORS provides a stable color for each nature plus the 'unclassified' key; values are non-empty strings.
    - FlowNature is exported as a TypeScript union of the six string literals matching the planned pgEnum.
  </behavior>
  <action>
    Create `lib/utils/nature-labels.ts` exporting:
    (a) `FlowNature` TypeScript union of literals: 'essential' | 'discretionary' | 'operational' | 'financial' | 'debt' | 'extraordinary' (must match the pgEnum to be added in Plan 37-02 per R-FN-01).
    (b) `NATURE_LABELS: Record<FlowNature | 'unclassified', string>` with the exact D-04 / D-05 mappings: essential→'Essenziale', discretionary→'Discrezionale', operational→'Operativo', financial→'Finanziario', debt→'Debiti', extraordinary→'Straordinario', unclassified→'Non classificato'.
    (c) `NATURE_ORDER: ReadonlyArray<FlowNature | null>` in this exact sequence: ['essential', 'discretionary', 'operational', 'financial', 'debt', 'extraordinary', null].
    (d) `NATURE_COLORS: Record<FlowNature | 'unclassified', string>` with distinct CSS-var-based or hex colors; the 'unclassified' entry must be a neutral gray (suggested `#a1a1aa`).
    Then create `tests/nature-labels.test.ts` with cases that import from `@/lib/utils/nature-labels` and assert: NATURE_LABELS has all 7 expected keys with exact Italian strings; NATURE_ORDER has length 7 with last element `null`; NATURE_COLORS has all 7 keys with non-empty string values; FlowNature type compile-time enforced by `satisfies` check or const literal assertion.
    Comments and identifiers in English; only label values are Italian (per project Language Convention).
  </action>
  <verify>
    <automated>yarn test --run tests/nature-labels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/utils/nature-labels.ts` exists and `grep -c "export" lib/utils/nature-labels.ts` is at least 4 (FlowNature, NATURE_LABELS, NATURE_ORDER, NATURE_COLORS).
    - `grep -E "'Essenziale'|'Discrezionale'|'Operativo'|'Finanziario'|'Debiti'|'Straordinario'|'Non classificato'" lib/utils/nature-labels.ts` returns 7 matching lines.
    - `grep -c "null" lib/utils/nature-labels.ts` shows null appears in NATURE_ORDER (at least 1 match).
    - `yarn test --run tests/nature-labels.test.ts` exits 0 with all tests passing.
    - `yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Shared nature label utility exists, is type-safe, and has GREEN unit tests covering all label entries and ordering invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/dashboard-charts.test.tsx for nature-segmented chart (R-FN-06)</name>
  <files>tests/dashboard-charts.test.tsx</files>
  <read_first>
    - tests/dashboard-charts.test.tsx (full current content)
    - lib/utils/nature-labels.ts (just created in Task 1)
    - components/dashboard/entrate-uscite-chart.tsx (current shape — used as mock data reference)
    - .planning/phases/37-flow-nature-chart/37-RESEARCH.md (Pattern 3 chart structure)
  </read_first>
  <action>
    Rewrite the `describe('EntrateUsciteChart (D-10, D-11)', ...)` block to reflect the nature-segmented chart contract Plan 37-04 will deliver. Concretely:
    (1) Replace the `data` fixture used by `EntrateUsciteChart` with one shaped for `MonthlyNatureTrendPoint` (which will be defined in Plan 37-03): each point has `{ month: 'YYYY-MM', label: string, segments: Record<string, string>, totalNc: number, totalIgn: number }` where `segments` keys include at least 'essential', 'discretionary' and 'unclassified' with decimal string values.
    (2) Remove the assertions `expect(html).toContain('Entrate')` and `expect(html).toContain('Uscite')`.
    (3) Add assertions: `expect(html).toContain('Essenziale')` and `expect(html).toContain('Non classificato')` (rendered via custom legend per Pattern 3 fallback).
    (4) Keep the existing negative assertions for 'Non categorizzato', 'Ignorato', 'Bilancio'.
    (5) Update the `describe` title to `'EntrateUsciteChart (R-FN-04, R-FN-06)'` (replace stale D-10/D-11 reference).
    (6) Add a new `it('renders one segment per nature in NATURE_ORDER')` case that imports `NATURE_ORDER` from `@/lib/utils/nature-labels` and asserts the HTML contains the Italian label for each non-null nature (using `NATURE_LABELS[key]`).
    (7) Wrap chart render in a `Suspense` boundary if useSearchParams is invoked at module load — Plan 37-04 uses `useSearchParams` so the test must mount inside a `<Suspense fallback={null}>` or mock `next/navigation` with stubs returning `new URLSearchParams()`, `push: vi.fn()`, `replace: vi.fn()`, `usePathname: () => '/dashboard/overview'`.
    Leave the `BilancioBarsChart` describe block untouched.
    The test must be in RED state after this task (chart is still the old shape) — this is intentional and turns GREEN in Plan 37-04.
  </action>
  <verify>
    <automated>yarn test --run tests/dashboard-charts.test.tsx 2>&1 | grep -E "Essenziale|Non classificato|FAIL|expected" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'Entrate'" tests/dashboard-charts.test.tsx` returns 0.
    - `grep -c "'Uscite'" tests/dashboard-charts.test.tsx` returns 0.
    - `grep -c "'Essenziale'" tests/dashboard-charts.test.tsx` returns at least 1.
    - `grep -c "'Non classificato'" tests/dashboard-charts.test.tsx` returns at least 1.
    - `grep -c "next/navigation" tests/dashboard-charts.test.tsx` returns at least 1 (mock or import for URL hooks).
    - `grep -c "MonthlyNatureTrendPoint\|segments:" tests/dashboard-charts.test.tsx` returns at least 1.
    - `yarn test --run tests/dashboard-charts.test.tsx` ends in FAIL (RED) with failures referencing missing 'Essenziale' or chart shape — NOT TypeScript compile errors.
  </acceptance_criteria>
  <done>Chart tests assert the new nature-segmented contract (Italian labels via NATURE_LABELS), retain BilancioBarsChart coverage, and are intentionally RED awaiting Plan 37-04.</done>
</task>

<task type="auto">
  <name>Task 3: Add RED scaffolds for DAL nature trend, seed nature, and category actions</name>
  <files>tests/dashboard-dal.test.ts, tests/category-settings-seed.ts, tests/category-actions.test.ts</files>
  <read_first>
    - tests/dashboard-dal.test.ts (read existing describe blocks for getMonthlyTrend / buildMonthlyTrendData)
    - tests/category-settings-seed.ts (read existing seed shape assertions)
    - tests/category-actions.test.ts (read existing createSubcategoryAction tests)
    - .planning/phases/37-flow-nature-chart/37-RESEARCH.md (DAL section, Seed section, Action section)
    - lib/utils/nature-labels.ts
  </read_first>
  <action>
    Add focused RED test cases (one new `describe` block per file) that pin the contracts Wave 2 + Plan 37-05 must deliver. Do NOT modify existing GREEN tests.

    In `tests/dashboard-dal.test.ts`, add a new `describe('getMonthlyTrendByNature (R-FN-04, R-FN-08, R-FN-09)', ...)` block with cases that:
    - import (lazy) `getMonthlyTrendByNature` from `@/lib/dal/dashboard` and assert it is a function (will fail with 'is not a function' until Plan 37-03 ships).
    - import `buildMonthlyNatureTrendData` if it is exposed, OR mark a placeholder `it.todo` for it.
    - assert the exported type token `MonthlyNatureTrendPoint` is referenced (compile-time check; use `expectTypeOf` if available, else a sentinel runtime check that the function name exists).
    - include an `it('excludes subcategories with excludeFromTotals=true (R-FN-09)')` placeholder that lays out the fixture shape (transactions in a `trasferimenti` subcategory with `excludeFromTotals=true` must NOT appear in any nature segment).

    In `tests/category-settings-seed.ts`, add a new `describe('seed nature assignment (R-FN-03)')` block that:
    - reads `subCategoriesSeed` (or whatever export `scripts/seed-data.ts` provides; if the file does not export the array, mark the assertion as `it.todo` with a comment referencing R-FN-03 — Plan 37-02 will make it exportable).
    - asserts at least 1 system subcategory has `nature: 'essential'`, at least 1 has `nature: 'discretionary'`, and the `ignore` category subcategories have `nature: null` or undefined.
    - asserts FlowNature import from `@/lib/utils/nature-labels` succeeds (regression guard).

    In `tests/category-actions.test.ts`, add a new `describe('createSubcategoryAction nature requirement (R-FN-09 action layer)')` block that:
    - asserts that calling `createSubcategoryAction` (existing import) with a payload missing `nature` returns a Zod validation error or rejected state — this will FAIL until Plan 37-05 extends the schema.
    - adds a placeholder `it('setSubcategoryNatureAction persists override (R-FN-07)')` that imports `setSubcategoryNatureAction` (does not exist yet — will throw ReferenceError until 37-05); wrap in try/catch and assert the action exists.

    Every new test must reference its R-FN-* requirement ID in the `it()` description string.
  </action>
  <verify>
    <automated>yarn test --run tests/dashboard-dal.test.ts tests/category-settings-seed.ts tests/category-actions.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getMonthlyTrendByNature" tests/dashboard-dal.test.ts` returns at least 2.
    - `grep -c "R-FN-04\|R-FN-08\|R-FN-09" tests/dashboard-dal.test.ts` returns at least 2.
    - `grep -c "R-FN-03" tests/category-settings-seed.ts` returns at least 1.
    - `grep -c "setSubcategoryNatureAction\|nature" tests/category-actions.test.ts | grep -v 'nature\b'` — `grep -c "setSubcategoryNatureAction" tests/category-actions.test.ts` returns at least 1.
    - `yarn test --run tests/dashboard-dal.test.ts tests/category-settings-seed.ts tests/category-actions.test.ts` produces FAIL output mentioning `getMonthlyTrendByNature` or `setSubcategoryNatureAction` (intentionally RED).
    - All EXISTING tests in those files still PASS (no regression in green count): inspect summary line shows only the new tests failing.
  </acceptance_criteria>
  <done>Three test files now contain RED scaffolds for the DAL, seed, and action contracts; existing assertions remain GREEN; planner-controlled requirement IDs appear in `it()` descriptions for traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test scope only | This plan does not introduce any runtime trust boundary; modifications are limited to `lib/utils/nature-labels.ts` (pure data) and `tests/*` (test scope). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | `lib/utils/nature-labels.ts` | accept | Pure constant module; no user input; reviewed in code review for label correctness against D-04/D-05. |
| T-37-02 | Information Disclosure | tests/* | accept | Tests run only in dev / CI; no production data exposed. |
| T-37-SC | Tampering | npm installs | mitigate | No new npm/pip/cargo installs introduced by this plan; legitimacy gate not triggered. |
</threat_model>

<verification>
- `yarn test --run tests/nature-labels.test.ts` exits 0.
- `yarn test --run tests/dashboard-charts.test.tsx tests/dashboard-dal.test.ts tests/category-actions.test.ts tests/category-settings-seed.ts` shows new RED tests failing on missing implementations (NOT TS compile errors).
- `yarn tsc --noEmit` exits 0.
- `yarn check:language` exits 0 (Italian strings limited to label values, comments/identifiers in English).
</verification>

<success_criteria>
- `lib/utils/nature-labels.ts` exported and GREEN tested.
- `tests/dashboard-charts.test.tsx` no longer references `'Entrate'`/`'Uscite'` and references at least one Italian nature label.
- RED scaffolds for `getMonthlyTrendByNature`, `setSubcategoryNatureAction`, seed nature assignment exist and reference their requirement IDs.
- No regressions in tests outside the scope of this phase (full `yarn test` not required here; spot-check at task verify level).
</success_criteria>

<output>
Create `.planning/phases/37-flow-nature-chart/37-01-SUMMARY.md` when done, recording: the exact NATURE_COLORS palette chosen, any deviations from Pattern 5, and the failing test count entering Plan 37-02/03.
</output>
