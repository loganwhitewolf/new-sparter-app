---
phase: 37-flow-nature-chart
plan: 04
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-03
files_modified:
  - components/dashboard/entrate-uscite-chart.tsx
  - app/(app)/dashboard/overview/page.tsx
  - tests/dashboard-charts.test.tsx
autonomous: true
requirements:
  - R-FN-04
  - R-FN-05
  - R-FN-06
must_haves:
  truths:
    - "EntrateUsciteChart renders a stacked BarChart with one Bar per FlowNature plus an 'unclassified' Bar (R-FN-04)"
    - "Clicking a legend item updates the URL `?hidden=` param via router.replace + startTransition (R-FN-05)"
    - "When all natures are visible, `?hidden=` is absent from the URL; when hidden set is empty after a toggle, the param is removed (D-03)"
    - "Bars with their key in the hidden set are not rendered (Recharts `hide` prop)"
    - "The null-nature segment renders under the Italian label 'Non classificato' and is visible only when at least one month has a non-zero unclassified amount (Claude's discretion + D-05)"
    - "BilancioBarsChart and its data source `getAggregatedTransactionsData` are unmodified — the overview page now calls both DAL functions in parallel"
  artifacts:
    - path: components/dashboard/entrate-uscite-chart.tsx
      provides: "Rewritten chart: stacked nature bars, URL-persisted legend toggle, custom ChartLegendContent wrapper with onClick"
      min_lines: 80
    - path: app/(app)/dashboard/overview/page.tsx
      provides: "Parallel Promise.all of getAggregatedTransactionsData + getMonthlyTrendByNature; both charts wired"
  key_links:
    - from: components/dashboard/entrate-uscite-chart.tsx
      to: lib/utils/nature-labels.ts
      via: "imports NATURE_ORDER, NATURE_LABELS, NATURE_COLORS"
      pattern: "from '@/lib/utils/nature-labels'"
    - from: components/dashboard/entrate-uscite-chart.tsx
      to: lib/dal/dashboard.ts
      via: "imports MonthlyNatureTrendPoint type"
      pattern: "MonthlyNatureTrendPoint"
    - from: components/dashboard/entrate-uscite-chart.tsx
      to: "URL ?hidden= param"
      via: "useSearchParams + router.replace pattern"
      pattern: "router\\.replace.*hidden"
    - from: app/(app)/dashboard/overview/page.tsx
      to: lib/dal/dashboard.ts
      via: "calls getMonthlyTrendByNature in parallel with getAggregatedTransactionsData"
      pattern: "getMonthlyTrendByNature"
---

<objective>
Rewrite `EntrateUsciteChart` as a stacked nature-segmented chart with URL-persisted legend toggles, and wire it into the overview page alongside the unchanged `BilancioBarsChart`. This turns the Plan 37-01 chart RED tests GREEN and delivers R-FN-04, R-FN-05, R-FN-06.

Purpose: The current `EntrateUsciteChart` is a two-bar In/Out view that no longer matches the milestone vision. Per ADR-0004 we render N stacked nature segments with algebraic sum; per D-01/D-02/D-03 the legend itself is the toggle, synced to `?hidden=`. The overview page must keep `BilancioBarsChart` running on the original DAL (Pitfall 2).

Output:
- Rewritten `components/dashboard/entrate-uscite-chart.tsx`.
- Updated `app/(app)/dashboard/overview/page.tsx` calling both DAL functions in parallel.
- Plan 37-01 `tests/dashboard-charts.test.tsx` RED cases turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-flow-nature-chart/37-CONTEXT.md
@.planning/phases/37-flow-nature-chart/37-RESEARCH.md
@.planning/phases/37-flow-nature-chart/37-01-SUMMARY.md
@.planning/phases/37-flow-nature-chart/37-03-SUMMARY.md
@docs/adr/0003-flownature-at-subcategory-level.md
@docs/adr/0004-nature-segments-algebraic-sum.md
@components/dashboard/entrate-uscite-chart.tsx
@components/dashboard/overview-filters.tsx
@components/ui/chart.tsx
@app/(app)/dashboard/overview/page.tsx
@lib/utils/nature-labels.ts
@lib/dal/dashboard.ts
@tests/dashboard-charts.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite EntrateUsciteChart as stacked nature chart with custom legend onClick</name>
  <files>components/dashboard/entrate-uscite-chart.tsx</files>
  <read_first>
    - components/dashboard/entrate-uscite-chart.tsx (current implementation — two Bar series)
    - components/dashboard/overview-filters.tsx (URL sync pattern: useSearchParams + new URLSearchParams + router.replace + startTransition + scroll:false)
    - components/ui/chart.tsx (verified: ChartLegendContent does NOT forward onClick; must be wrapped or replaced — use the fallback strategy per RESEARCH Pattern 3 note)
    - lib/utils/nature-labels.ts
    - lib/dal/dashboard.ts (MonthlyNatureTrendPoint type signature)
    - .planning/phases/37-flow-nature-chart/37-RESEARCH.md (Pattern 3, Pitfall 1, Pitfall 4)
  </read_first>
  <behavior>
    - Component is a `'use client'` component accepting `data: MonthlyNatureTrendPoint[]`.
    - Parses `?hidden=` searchParams into `Set<string>` (keys: 'essential', 'discretionary', 'operational', 'financial', 'debt', 'extraordinary', 'unclassified').
    - Renders one `<Bar stackId="a" dataKey={key} fill={NATURE_COLORS[key]} hide={hidden.has(key)} />` per entry in `NATURE_ORDER` (mapping `null` to `'unclassified'`).
    - The `'unclassified'` Bar is rendered only when at least one data point has `segments['unclassified']` numerically non-zero across the visible range.
    - Custom legend (either via `ChartLegendContent` wrapper or a fully custom legend function passed to `<ChartLegend content={...} />`) renders one item per nature in `NATURE_ORDER` filtered for visibility; clicking an item calls `toggleNature(key)`.
    - `toggleNature(key)` reads current `?hidden=` set, flips the key, writes back via `router.replace(pathname + (search ? '?' + search : ''), { scroll: false })` wrapped in `startTransition`. When the resulting set is empty, deletes the `hidden` param entirely (D-03).
    - Tooltip continues to use `ChartTooltipContent` and displays Italian nature labels via `NATURE_LABELS`.
    - chartData pre-populates ALL nature keys per month (using `parseFloat(point.segments[key] ?? '0')`) to avoid Recharts ordering glitches (Pitfall 4).
  </behavior>
  <action>
    Rewrite `components/dashboard/entrate-uscite-chart.tsx` (full replace allowed — old two-Bar version is obsolete):
    (1) Keep the `'use client'` directive at the top.
    (2) Imports: `useSearchParams`, `useRouter`, `usePathname` from `next/navigation`; `useTransition`, `useMemo` from `react`; `Bar`, `BarChart`, `XAxis`, `YAxis`, `CartesianGrid` (optional) from `recharts`; `ChartContainer`, `ChartLegend`, `ChartTooltip`, `ChartTooltipContent` from `@/components/ui/chart`; `NATURE_ORDER`, `NATURE_LABELS`, `NATURE_COLORS`, type `FlowNature` from `@/lib/utils/nature-labels`; type `MonthlyNatureTrendPoint` from `@/lib/dal/dashboard`.
    (3) Define `type SegmentKey = FlowNature | 'unclassified'`. Define `const SEGMENT_KEYS: ReadonlyArray<SegmentKey>` derived from NATURE_ORDER by mapping `null → 'unclassified'`.
    (4) Define `chartConfig` (the shadcn ChartConfig object) mapping each SegmentKey to `{ label: NATURE_LABELS[key], color: NATURE_COLORS[key] }` so the tooltip and ChartContainer CSS vars resolve correctly.
    (5) Build `chartData = useMemo(() => data.map(point => ({ month: point.month, label: point.label, ...Object.fromEntries(SEGMENT_KEYS.map(k => [k, parseFloat(point.segments[k] ?? '0')])) })), [data])`.
    (6) Compute `unclassifiedHasData = useMemo(() => data.some(p => Number(p.segments['unclassified'] ?? 0) !== 0), [data])`.
    (7) `const visibleKeys = unclassifiedHasData ? SEGMENT_KEYS : SEGMENT_KEYS.filter(k => k !== 'unclassified')`.
    (8) Parse `hidden` from `searchParams.get('hidden')` into a `Set<SegmentKey>`.
    (9) Implement `toggleNature(key: SegmentKey)` per RESEARCH Pattern 3 (use `new URLSearchParams(searchParams.toString())`, `params.set/delete`, `startTransition(() => router.replace(...))`, `{ scroll: false }`).
    (10) Render layout: `<ChartContainer config={chartConfig} className="min-h-[260px] w-full"><BarChart data={chartData} barGap={2} barCategoryGap="20%"><XAxis dataKey="label" .../><YAxis .../><ChartTooltip content={<ChartTooltipContent />} /><ChartLegend content={<CustomNatureLegend visibleKeys={visibleKeys} hidden={hidden} onToggle={toggleNature} />} />{visibleKeys.map(key => <Bar key={key} dataKey={key} stackId="a" fill={`var(--color-${key})`} hide={hidden.has(key)} />)}</BarChart></ChartContainer>`.
    (11) Define `CustomNatureLegend` as an internal component inside the file: renders a flex row of buttons (`<button type="button" onClick={() => onToggle(key)} className={cn('flex items-center gap-1.5 cursor-pointer', hidden.has(key) && 'opacity-40')} aria-pressed={!hidden.has(key)}>...</button>`), color swatch from `NATURE_COLORS[key]`, label from `NATURE_LABELS[key]`. This bypasses the `ChartLegendContent` onClick limitation (verified by reading components/ui/chart.tsx in Plan 37-01 Wave 0 work).
    (12) Do NOT add any sign-split, ABS, or filtering — the DAL hands algebraic sums; the chart just renders them.
    (13) Make sure the component never imports `MonthlyTrendPoint` (the old type) — it is the wrong shape.
  </action>
  <verify>
    <automated>yarn test --run tests/dashboard-charts.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use client" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "MonthlyNatureTrendPoint" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "MonthlyTrendPoint\b" components/dashboard/entrate-uscite-chart.tsx` returns 0 (old type must be gone).
    - `grep -c "NATURE_ORDER\|NATURE_LABELS\|NATURE_COLORS" components/dashboard/entrate-uscite-chart.tsx` returns at least 3.
    - `grep -c "router.replace" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "startTransition" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "scroll: false" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "hide=" components/dashboard/entrate-uscite-chart.tsx` returns at least 1 (Recharts `hide` prop on Bar).
    - `grep -c "stackId=\"a\"" components/dashboard/entrate-uscite-chart.tsx` returns at least 1.
    - `grep -c "ABS(" components/dashboard/entrate-uscite-chart.tsx` returns 0.
    - `yarn test --run tests/dashboard-charts.test.tsx` exits 0 (RED scaffolds turn GREEN).
    - `yarn tsc --noEmit` exits 0.
    - `yarn check:language` exits 0 (no new Italian strings outside NATURE_LABELS consumption).
  </acceptance_criteria>
  <done>EntrateUsciteChart renders stacked nature segments, legend-as-toggle updates URL, unclassified segment hidden when zero, chart tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire overview page to call getMonthlyTrendByNature alongside existing DAL</name>
  <files>app/(app)/dashboard/overview/page.tsx</files>
  <read_first>
    - app/(app)/dashboard/overview/page.tsx (current data fetching pattern; identify how `getAggregatedTransactionsData` is called)
    - lib/dal/dashboard.ts (verify exports `getMonthlyTrendByNature`, `MonthlyNatureTrendPoint`)
    - components/dashboard/entrate-uscite-chart.tsx (after Task 1 — confirm prop type is `MonthlyNatureTrendPoint[]`)
  </read_first>
  <action>
    Modify `app/(app)/dashboard/overview/page.tsx`:
    (1) Import `getMonthlyTrendByNature` (new) alongside the existing `getAggregatedTransactionsData` import.
    (2) Replace the existing single `await getAggregatedTransactionsData(preset)` with a `Promise.all([getAggregatedTransactionsData(preset), getMonthlyTrendByNature(preset)])` call to keep page TTFB stable.
    (3) Destructure the result: `const [aggregated, natureTrend] = await Promise.all([...])`.
    (4) Pass `aggregated` to `BilancioBarsChart` exactly as before (do NOT change its props).
    (5) Pass `natureTrend` (typed `MonthlyNatureTrendPoint[]`) to `EntrateUsciteChart` via `data={natureTrend}`.
    (6) Wrap `EntrateUsciteChart` in `<Suspense fallback={...}>` if not already wrapped — required because the client component reads `useSearchParams` (Next.js 16 hydration requirement).
    (7) Keep all other parts of the page (filters, KPI cards, deviation widgets) unchanged.
  </action>
  <verify>
    <automated>yarn tsc --noEmit &amp;&amp; yarn test --run tests/dashboard-charts.test.tsx tests/dashboard-dal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getMonthlyTrendByNature" "app/(app)/dashboard/overview/page.tsx"` returns at least 1.
    - `grep -c "getAggregatedTransactionsData" "app/(app)/dashboard/overview/page.tsx"` returns at least 1 (still present).
    - `grep -c "Promise.all" "app/(app)/dashboard/overview/page.tsx"` returns at least 1.
    - `grep -c "Suspense" "app/(app)/dashboard/overview/page.tsx"` returns at least 1.
    - `yarn tsc --noEmit` exits 0.
    - `yarn test --run tests/dashboard-charts.test.tsx tests/dashboard-dal.test.ts` exits 0.
    - Manual smoke: navigating to `/dashboard/overview` renders the stacked chart; clicking a legend item appends `?hidden=<key>` to the URL; refreshing preserves the hidden state; clicking again removes it.
  </acceptance_criteria>
  <done>Overview page calls both DAL functions in parallel; BilancioBarsChart untouched; new chart receives nature data and toggles URL on legend click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ↔ client | `?hidden=` query string is untrusted client input; chart uses it only as a visibility filter, never as a DB query input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Tampering | `?hidden=` query string | accept | Unknown values in the set simply match no Bar and are ignored; no security impact (visibility-only). ASVS L1 V5 satisfied via implicit allowlist (`Set.has(key)` against SEGMENT_KEYS). |
| T-37-04-02 | Denial of Service | Recharts re-render on toggle | accept | `startTransition` wraps the URL change; client-side render only; no server resource consumption per toggle. |
| T-37-04-03 | Information Disclosure | Chart data per user | mitigate | DAL layer already gates on `verifySession()`; this plan touches only the rendering layer. |
| T-37-04-SC | Tampering | npm installs | mitigate | No new package installs (recharts and shadcn already present). |
</threat_model>

<verification>
- `yarn test --run tests/dashboard-charts.test.tsx` exits 0.
- `yarn tsc --noEmit` exits 0.
- Manual smoke (dev server):
  1. `/dashboard/overview` shows the stacked nature chart and `BilancioBarsChart` side by side.
  2. Clicking the 'Essenziale' legend item hides those segments and sets `?hidden=essential` in the URL.
  3. Clicking again removes `?hidden=` from the URL (D-03).
  4. Loading `/dashboard/overview?hidden=debt,discretionary` on first paint hides both segments without flicker.
  5. The 'Non classificato' segment appears only if there are any expenses tied to subcategories with null effectiveNature; otherwise the legend item is absent.
- `yarn check:language` exits 0.
</verification>

<success_criteria>
- R-FN-04 satisfied: stacked nature bars replace In/Out two-bar chart, algebraic sums rendered.
- R-FN-05 satisfied: `?hidden=` URL param persists toggle state.
- R-FN-06 satisfied: null nature renders as 'Non classificato' segment (conditional on data).
- `BilancioBarsChart` still works (unchanged surface).
- Tests GREEN; TS compiles; language check passes.
</success_criteria>

<output>
Create `.planning/phases/37-flow-nature-chart/37-04-SUMMARY.md` when done, recording: whether the custom legend approach worked or `ChartLegendContent` was extended, the final unclassified visibility rule chosen, and any visual notes for design review.
</output>
