---
phase: 38-first-import-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - app/(app)/onboarding/layout.tsx
  - app/(app)/onboarding/page.tsx
  - app/(app)/onboarding/_components/onboarding-shell.tsx
  - app/(app)/onboarding/_components/progress-dots.tsx
  - app/(app)/onboarding/_components/step-1-upload.tsx
  - app/(app)/onboarding/_components/step-2-overview.tsx
  - app/(app)/onboarding/_components/step-3-education.tsx
  - app/(app)/onboarding/_components/sticky-cta.tsx
  - lib/validations/onboarding.ts
  - tests/onboarding-page.test.tsx
  - tests/onboarding-step-validation.test.ts
autonomous: true
requirements:
  - R-OB-03
  - R-OB-04
  - R-OB-05
  - R-OB-06
  - R-OB-09
  - R-OB-10
must_haves:
  truths:
    - "GET /onboarding renders the upload screen (Step 1) — drag-and-drop hint, file accept .csv/.xlsx, max 10 MB copy"
    - "GET /onboarding?step=2 renders the overview screen showing N transactions, total income, total expenses, months covered label, and % auto-categorized"
    - "GET /onboarding?step=3 renders the education screen with the transfers/giroconto tip text"
    - "Invalid step values (?step=99, ?step=abc) coerce to step=1 server-side via Zod validation"
    - "Steps 1–3 render with full-screen dark hero background (D-09) and reuse the design-system tokens for text color and surface — no hardcoded slate-800/slate-900 strings"
    - "Step 2 reads real data from getFileCoveredMonths and the latest file for the user (not mock data)"
    - "Onboarding pages bypass the standard (app) shell — no Sidebar, no Topbar, no BottomNav rendered"
  artifacts:
    - path: app/(app)/onboarding/layout.tsx
      provides: "Onboarding-specific layout that bypasses the standard app shell"
      contains: "return <>{children}</>"
    - path: app/(app)/onboarding/page.tsx
      provides: "RSC entry point — parses ?step searchParam, renders step components"
      exports: ["default"]
    - path: app/(app)/onboarding/_components/onboarding-shell.tsx
      provides: "Full-screen hero shell with progress dots header (R-OB-09)"
      exports: ["OnboardingShell"]
    - path: app/(app)/onboarding/_components/progress-dots.tsx
      provides: "5-dot progress indicator with current-step label"
      exports: ["ProgressDots"]
    - path: app/(app)/onboarding/_components/step-1-upload.tsx
      provides: "Step 1 — single-file upload drop-zone that reuses /api/files/initiate + R2 PUT + /api/files/confirm"
      exports: ["Step1Upload"]
    - path: app/(app)/onboarding/_components/step-2-overview.tsx
      provides: "Step 2 — RSC overview showing real data from latest user file"
      exports: ["Step2Overview"]
    - path: app/(app)/onboarding/_components/step-3-education.tsx
      provides: "Step 3 — transfers/giroconto education tip"
      exports: ["Step3Education"]
    - path: lib/validations/onboarding.ts
      provides: "Zod schema for ?step searchParam (1..5, default 1)"
      exports: ["OnboardingStepSchema", "parseOnboardingStep"]
  key_links:
    - from: app/(app)/onboarding/_components/step-1-upload.tsx
      to: /api/files/initiate
      via: "fetch POST in handleUpload"
      pattern: "/api/files/initiate"
    - from: app/(app)/onboarding/_components/step-1-upload.tsx
      to: /api/files/confirm
      via: "fetch POST after R2 PUT"
      pattern: "/api/files/confirm"
    - from: app/(app)/onboarding/_components/step-1-upload.tsx
      to: lib/actions/import.ts (analyzeImportAction + confirmImportAction)
      via: "server action call to run analyze and confirm import inside db.transaction"
      pattern: "analyzeImportAction|confirmImportAction"
    - from: app/(app)/onboarding/_components/step-2-overview.tsx
      to: lib/dal/imports.ts (getFileCoveredMonths)
      via: "RSC call"
      pattern: "getFileCoveredMonths"
    - from: app/(app)/onboarding/_components/step-2-overview.tsx
      to: lib/utils/date.ts (formatMonthRange)
      via: "RSC display formatting"
      pattern: "formatMonthRange"
---

<objective>
Build the onboarding route group, the Zod step-parameter validation, and the first three steps of the flow: upload (Step 1), overview (Step 2), and education (Step 3). The onboarding shell uses the winning Variant B design (full-screen hero, progress dots) but maps the prototype's hardcoded slate-* classes to design-system tokens per D-09. Upload reuses the existing R2 presigned PUT pipeline (D-04) and the existing analyzeImportAction + confirmImportAction so the import still runs inside `db.transaction` per project rules.

Purpose: After Wave 1, the layout guard redirects new users to `/onboarding` — but that route does not yet exist. This plan creates the route and the first three screens, satisfying R-OB-03 through R-OB-06 and the visual contract in R-OB-09. Step 4 (categorization wizard) and Step 5 (outro) plus prototype cleanup land in Plan 38-03.

Output:
- `app/(app)/onboarding/layout.tsx` (route-group layout that bypasses the standard app shell)
- `app/(app)/onboarding/page.tsx` (server component that selects which step to render)
- `app/(app)/onboarding/_components/*` (5 components: shell, progress dots, three step bodies, sticky CTA)
- `lib/validations/onboarding.ts` (step Zod validation)
- `tests/onboarding-page.test.tsx`, `tests/onboarding-step-validation.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-first-import-onboarding/38-CONTEXT.md
@.planning/phases/38-first-import-onboarding/38-01-PLAN.md
@docs/adr/0005-first-import-onboarding-gate.md
@docs/adr/0006-file-period-model.md
@app/(app)/prototype/onboarding/_variants/variant-b.tsx
@app/(app)/prototype/onboarding/NOTES.md
@components/import/import-uploader.tsx
@lib/actions/import.ts
@lib/dal/imports.ts
@lib/dal/transactions.ts
@lib/dal/auth.ts
@lib/utils/date.ts
@lib/routes.ts
@components/ui/button.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Onboarding route group layout + step Zod schema + ProgressDots + OnboardingShell (R-OB-03, R-OB-09)</name>
  <files>app/(app)/onboarding/layout.tsx, app/(app)/onboarding/_components/onboarding-shell.tsx, app/(app)/onboarding/_components/progress-dots.tsx, lib/validations/onboarding.ts, tests/onboarding-step-validation.test.ts</files>
  <read_first>
    - app/(app)/layout.tsx (Wave 1 result — async layout that includes the guard; the onboarding layout must NOT re-run that guard for /onboarding itself; exemption logic already handled in Wave 1 via pathname check)
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx (full file — STEP_NAMES, Dots component, bg gradient logic, header layout — this is the visual reference)
    - app/(app)/prototype/onboarding/NOTES.md (D-09 mapping rules: hardcoded Tailwind → design-system tokens)
    - app/globals.css (design-system CSS variables — `--background`, `--foreground`, `--muted`, `--primary` — confirm available token names)
    - lib/validations/import.ts (existing Zod patterns — `safeParse` usage and parse helpers)
    - components/ui/button.tsx (canonical Button component to use)
    - tests/import-format-wizard-actions.test.ts (Zod schema testing pattern)
  </read_first>
  <behavior>
    - `app/(app)/onboarding/layout.tsx` is the route-group layout for `/onboarding`. It returns its `children` wrapped in a single full-viewport div with `data-onboarding-root` so the standard Sidebar/Topbar/BottomNav from the parent `(app)/layout.tsx` are SHADOWED — concretely, since Next.js nests layouts, the onboarding layout still renders inside the parent. We MUST therefore make the parent's exemption (already in Wave 1) ALSO skip rendering the chrome for `/onboarding`. SOLUTION: parent `(app)/layout.tsx` (Wave 1) renders chrome unconditionally. To bypass chrome, this task ALSO updates `app/(app)/layout.tsx` to render `<>{children}</>` (no chrome) when pathname starts with `/onboarding` — the existing Wave 1 redirect logic already reads the pathname. The onboarding layout itself just renders `<main className="min-h-screen bg-background text-foreground">{children}</main>`.
    - `OnboardingStepSchema`: Zod `z.coerce.number().int().min(1).max(5).catch(1)` (default 1, clamp invalid).
    - `parseOnboardingStep(value: string | string[] | undefined): 1|2|3|4|5` — handles array form, returns the parsed number.
    - `ProgressDots({ current: 1..5, theme: 'dark'|'light' })` renders five dots; the current dot is larger/brighter; trailing label text is the Italian step name from a STEP_NAMES constant: 1→'Carica il file', 2→'Riepilogo', 3→'Come funziona', 4→'Categorizzazione', 5→'Completato'. Dot color classes use `text-foreground`/`text-muted-foreground` instead of `text-white/text-white-50`.
    - `OnboardingShell({ step, theme, children, footer? })` renders header with 'Sparter' wordmark + ProgressDots, the children area, and an optional sticky bottom CTA bar (`footer` slot). Theme 'dark' applies `bg-background-onboarding-dark` (a new utility class) OR uses design tokens like `bg-slate-900 dark:bg-slate-900` — choose ONE token-based approach: use `data-theme="onboarding-dark"` attribute and add corresponding CSS variable overrides in `app/globals.css` mapped to existing tokens. If `app/globals.css` already supports dark mode, prefer adding a `[data-theme="onboarding-dark"]` block that sets `--background` and `--foreground` so existing components inherit.
  </behavior>
  <action>
    Step A — Create `lib/validations/onboarding.ts`:
    Export `OnboardingStepSchema = z.coerce.number().int().min(1).max(5).catch(1)`.
    Export `parseOnboardingStep(value: string | string[] | undefined): 1|2|3|4|5` that handles undefined → 1, array → first element, otherwise passes string to `OnboardingStepSchema.parse()`. Return type is the literal union `1 | 2 | 3 | 4 | 5` (assert via `as`).
    Export `STEP_NAMES: Record<1|2|3|4|5, string>` with the Italian labels: 1→'Carica il file', 2→'Riepilogo', 3→'Come funziona', 4→'Categorizzazione', 5→'Completato'.

    Step B — Modify `app/(app)/layout.tsx` (built in Wave 1) to bypass chrome on onboarding:
    After the existing pathname read (already added in Wave 1 via `headers().get('x-pathname')`), add: `const isOnboarding = pathname.startsWith(APP_ROUTES.onboarding); if (isOnboarding) return <>{children}</>;`. Place this check AFTER the redirect guard (the guard already exempts /onboarding so it will fall through to this branch when the user is on /onboarding).

    Step C — Create `app/(app)/onboarding/layout.tsx`:
    Default-export a React component returning `<main className="min-h-screen bg-background text-foreground">{children}</main>`. No `'use client'` — keep it a server component. Mark the file with a header comment: `// Onboarding route group — bypasses app shell per D-09 + D-11.`

    Step D — Create `app/(app)/onboarding/_components/progress-dots.tsx` (client component, `'use client'`):
    Props: `{ current: 1|2|3|4|5, theme?: 'dark'|'light' }`. Default theme 'dark'.
    Render `<div className="flex items-center gap-3"><div className="flex gap-1.5 items-center">{[1,2,3,4,5].map(...)}</div><span ...>{STEP_NAMES[current]}</span></div>`. Each dot is a div with classes derived from current/index/theme. Use design-system tokens: in 'dark' theme use `bg-white` / `bg-white/50` / `bg-white/20` (these are still Tailwind opacity, not raw hex — acceptable per shadcn convention) OR use `bg-foreground` / `bg-foreground/50` / `bg-foreground/20` and set `--foreground` via the `data-theme` attribute on OnboardingShell. Pick the latter for portability.

    Step E — Create `app/(app)/onboarding/_components/onboarding-shell.tsx` (server component):
    Props: `{ step, theme: 'dark'|'light', children, footer? }`. Render an outer div with `data-theme={theme === 'dark' ? 'onboarding-dark' : 'onboarding-light'}` and `className="fixed inset-0 z-50 overflow-y-auto bg-background text-foreground"`. Inside: header bar with 'Sparter' wordmark and `<ProgressDots current={step} theme={theme} />`. Then `{children}` slot. Then conditional `{footer}` rendered in a fixed bottom bar with `border-t border-border bg-background`. Do not hardcode hex/slate colors anywhere — only token classes.

    Step F — Add `[data-theme="onboarding-dark"]` and `[data-theme="onboarding-light"]` blocks in `app/globals.css` that remap `--background` / `--foreground` / `--border` to the Variant B palette equivalents (dark: slate-900 / white / white-10; light: white / slate-900 / slate-200). The hex/oklch values go here ONCE in the design-token layer; components reference only `bg-background` / `text-foreground` etc.

    Step G — Create `tests/onboarding-step-validation.test.ts` (vitest):
    Cases:
    - "parseOnboardingStep(undefined) returns 1" (R-OB-03)
    - "parseOnboardingStep('1' through '5') returns the numeric literal" (R-OB-03)
    - "parseOnboardingStep('99') clamps to 1" (R-OB-03)
    - "parseOnboardingStep('abc') falls back to 1" (R-OB-03)
    - "parseOnboardingStep(['2', '3']) returns 2 (uses first element)" (R-OB-03)
    - "STEP_NAMES has Italian labels for all five steps" (R-OB-09)

    English comments and identifiers; Italian only inside STEP_NAMES values.
  </action>
  <verify>
    <automated>yarn test --run tests/onboarding-step-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const OnboardingStepSchema\\|export function parseOnboardingStep" lib/validations/onboarding.ts` returns at least 2.
    - `grep -c "STEP_NAMES" lib/validations/onboarding.ts` returns at least 1 with all five labels present (`grep -c "Carica il file" lib/validations/onboarding.ts` ≥ 1).
    - `test -f app/(app)/onboarding/layout.tsx` succeeds.
    - `grep -c "export function ProgressDots\\|export const ProgressDots" "app/(app)/onboarding/_components/progress-dots.tsx"` returns at least 1.
    - `grep -c "export function OnboardingShell\\|export const OnboardingShell" "app/(app)/onboarding/_components/onboarding-shell.tsx"` returns at least 1.
    - `grep -cE "(bg-slate-|text-slate-|from-slate-|to-slate-|#[0-9a-fA-F]{6})" "app/(app)/onboarding/_components/progress-dots.tsx" "app/(app)/onboarding/_components/onboarding-shell.tsx"` returns 0 (D-09: no hardcoded slate-* classes or hex in components).
    - `grep -c "data-theme=\"onboarding-dark\"\\|data-theme=\"onboarding-light\"" app/globals.css` returns at least 2.
    - `grep -c "return <>{children}</>" "app/(app)/layout.tsx"` returns at least 1 (chrome bypass for onboarding).
    - `yarn test --run tests/onboarding-step-validation.test.ts` exits 0 with all cases passing.
    - `yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Onboarding route group exists with token-based shell + progress dots, parent layout bypasses chrome on /onboarding, Zod step parser validated, design tokens declared in globals.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Step 1 upload + Step 3 education + onboarding page.tsx router (R-OB-04, R-OB-06, R-OB-09)</name>
  <files>app/(app)/onboarding/page.tsx, app/(app)/onboarding/_components/step-1-upload.tsx, app/(app)/onboarding/_components/step-3-education.tsx, app/(app)/onboarding/_components/sticky-cta.tsx, tests/onboarding-page.test.tsx</files>
  <read_first>
    - components/import/import-uploader.tsx (full file — full R2 upload sequence: hash → /api/files/initiate → presigned PUT via uploadFileToPresignedUrl → /api/files/confirm; this is the canonical upload flow to replicate)
    - components/import/upload-put.ts (uploadFileToPresignedUrl + UploadPutError export)
    - lib/actions/import.ts (analyzeImportAction + confirmImportAction signatures and how they consume FormData)
    - lib/services/import-format-detector.ts (existing platform auto-detection — invoked by analyzeFile; no changes needed but read to confirm the dispatch path)
    - lib/services/import-format-wizard.ts (existing private-platform creation — used when format not detected; the onboarding upload redirects to /import/[fileId]/configure when this is needed)
    - app/(app)/import/[fileId]/analyze/page.tsx (existing import analyze flow — onboarding upload mirrors this but auto-advances ?step=2 after success)
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx lines 65–82 (Step 1 upload layout)
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx lines 117–134 (Step 3 education layout)
    - lib/validations/import.ts (MAX_IMPORT_FILE_SIZE_BYTES, IMPORT_CONTENT_TYPES)
    - lib/validations/onboarding.ts (just created — parseOnboardingStep, STEP_NAMES)
    - tests/import-actions.test.ts (testing convention for actions + analyze flow)
  </read_first>
  <behavior>
    - `page.tsx` is an async RSC. It reads `searchParams.step` via `parseOnboardingStep`, awaits `verifySession`, and renders one of `<Step1Upload />`, `<Step2Overview />`, `<Step3Education />`, plus a placeholder note for Step 4 and Step 5 ("rendered by Plan 38-03"). Step 2 and Step 4-aware data fetching are delegated to their own components for now.
    - `Step1Upload` (client component) mirrors `ImportUploader` but: (a) on successful `/api/files/confirm` it does NOT redirect to `/import/[fileId]/analyze` — instead it calls `analyzeImportAction(formData)` then `confirmImportAction(formData)` server actions in sequence, then `router.push('/onboarding?step=2')`. (b) If `analyzeImportAction` returns an unknown-format result (formatVersionId === null and the wizard is needed), it redirects to `/import/${fileId}/configure` so the existing wizard handles the private-platform path per D-04, with a small banner explaining the user will return to onboarding after configuring. (c) Italian copy: "Il tuo primo estratto conto", "Carica il file della tua banca e vedremo insieme dove vanno i tuoi soldi.", drop-zone hint "Trascina qui il tuo file" + "oppure clicca per sfogliare", "CSV, XLS, XLSX · max 10 MB". (d) Upload runs through R2 presigned PUT — no proxying bytes through the server, per project hard rule.
    - `Step3Education` (server component) shows the uncategorized count from the user's most recent file (latest file by `referenceEndedAt` for the user), the auto-categorized count, and the tip box with the exact text: "I trasferimenti tra conti e i giroconti vengono esclusi dai totali in dashboard — è normale se i numeri sembrano diversi da quelli che ti aspetti." (R-OB-06 / D-06). Numbers are read from the latest `file` row (positiveTotal / negativeTotal / importedCount / `expense.subCategoryId IS NOT NULL` count vs IS NULL count for that file).
    - `StickyCta` (client component) renders the bottom CTA bar visible on Steps 2 and 3 with a single "Continua" button (Step 2) or "Inizia la categorizzazione" (Step 3) that navigates to `?step=N+1`. Step 1 has no CTA (advance is automatic after upload). Step 5 has no CTA (handled in Plan 38-03).
  </behavior>
  <action>
    Step A — Create `app/(app)/onboarding/page.tsx` (async RSC):
    Imports: `verifySession`, `parseOnboardingStep`, `OnboardingShell`, the four step components, `redirect`. Component signature: `export default async function OnboardingPage({ searchParams }: { searchParams: Promise<{ step?: string | string[] }> })`. Resolve searchParams, call `parseOnboardingStep(params.step)` → `step`. Call `verifySession()` (the layout already enforces it but this gives us userId for steps 2/3). Choose theme: `step === 4 ? 'light' : 'dark'`. Render `<OnboardingShell step={step} theme={theme} footer={step >= 2 && step <= 3 ? <StickyCta step={step} /> : undefined}>` and inside switch on step: 1→`<Step1Upload />`, 2→`<Step2Overview userId={userId} />` (Plan 38-03 will fill if missing — for this plan, render placeholder if Step2Overview not present yet, but Task 3 of this plan creates Step2Overview so it WILL exist), 3→`<Step3Education userId={userId} />`, 4/5 → render `<p data-testid="step-placeholder">Step {step} arriva con Plan 38-03</p>` placeholder.

    Step B — Create `app/(app)/onboarding/_components/step-1-upload.tsx` (client component):
    Copy the upload state machine from `components/import/import-uploader.tsx` (hashing → initiating → uploading → confirming) but REPLACE the final `router.push('/import/${fileId}/analyze')` with: call `analyzeImportAction(fd)` (where `fd` is a FormData with `fileId`), inspect the returned `data.formatVersionId`; if it is null → `router.push('/import/${fileId}/configure?from=onboarding')`; else call `confirmImportAction(fd)` → on success `router.push('/onboarding?step=2')`. Wrap UI in the Variant B layout: centered `CloudUpload` icon, h1 "Il tuo primo estratto conto", paragraph, dashed-border drop-zone (use `border-2 border-dashed border-foreground/20` — token-based, NO white hex), file input (hidden), small footer text "CSV, XLS, XLSX · max 10 MB". Token-only colors. Error display uses the existing `Alert` component.

    Step C — Create `app/(app)/onboarding/_components/step-3-education.tsx` (server component):
    Props: `{ userId: string }`. Read the latest file by userId via a new helper `getLatestImportSummaryForUser(userId)` (add to `lib/dal/imports.ts`) that returns `{ fileId: string, importedCount: number, autoCategorizedCount: number, uncategorizedCount: number, positiveTotal: string, negativeTotal: string, firstMonth: Date|null, lastMonth: Date|null } | null`. Implementation uses the `file` row plus a left join to `expense` to count auto-categorized vs uncategorized expenses for that file. Render: a large `{uncategorizedCount}` numeral, label "transazioni da categorizzare", subtext "Le altre {autoCategorizedCount} erano già note. Ci vogliono solo pochi tocchi.", and a card with the giroconto tip text (exact wording in behavior block). If no file exists for the user, render a fallback "Nessun file caricato" with a CTA back to `?step=1`. NOTE: the user can only reach step=3 after step=1 succeeded, so the null branch is defensive only.

    Step D — Create `app/(app)/onboarding/_components/sticky-cta.tsx` (client component):
    Props: `{ step: 1|2|3|4|5 }`. Renders `<Link>` (next/link) styled as Button to `?step=${step + 1}`. Button text per step: 2→"Continua a categorizzare", 3→"Inizia la categorizzazione". Step 4 CTA is handled by Plan 38-03 (this component returns null for steps 1, 4, 5). Step 2 also shows a secondary file-name + check-icon row above the dots — but that lives in Step2Overview not here.

    Step E — Create `tests/onboarding-page.test.tsx` (vitest + React Testing Library):
    Mock `next/navigation` (`useRouter`, `useSearchParams`, `redirect`). Mock `@/lib/dal/auth` and `@/lib/dal/imports`. Cases (all reference R-OB-* in `it()` description):
    - "renders Step1Upload when ?step is missing" (R-OB-03)
    - "renders Step1Upload when ?step=1" (R-OB-04)
    - "renders Step3Education with the transfers/giroconto tip text when ?step=3" (R-OB-06) — assert the exact tip sentence appears in rendered HTML.
    - "renders step placeholder for ?step=4 and ?step=5" (Plan 38-03 will fill).
    - "uses 'light' theme on step 4, 'dark' otherwise" (R-OB-09) — assert `data-theme` attribute on OnboardingShell.
    - "Step1Upload shows file accept hint 'CSV, XLS, XLSX · max 10 MB'" (R-OB-04).
    - "Step3Education contains the exact Italian text 'I trasferimenti tra conti e i giroconti vengono esclusi dai totali in dashboard'" (R-OB-06).
  </action>
  <verify>
    <automated>yarn test --run tests/onboarding-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "app/(app)/onboarding/page.tsx"` and `grep -c "parseOnboardingStep" "app/(app)/onboarding/page.tsx"` returns at least 1.
    - `grep -c "export function Step1Upload\\|export const Step1Upload" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "'use client'" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "/api/files/initiate" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "/api/files/confirm" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "uploadFileToPresignedUrl" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "analyzeImportAction\\|confirmImportAction" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 2.
    - `grep -c "/onboarding?step=2" "app/(app)/onboarding/_components/step-1-upload.tsx"` returns at least 1.
    - `grep -c "I trasferimenti tra conti e i giroconti" "app/(app)/onboarding/_components/step-3-education.tsx"` returns at least 1.
    - `grep -c "export function Step3Education\\|export const Step3Education" "app/(app)/onboarding/_components/step-3-education.tsx"` returns at least 1.
    - `grep -cE "(bg-slate-|text-slate-|from-slate-|to-slate-|#[0-9a-fA-F]{6})" "app/(app)/onboarding/_components/step-1-upload.tsx" "app/(app)/onboarding/_components/step-3-education.tsx" "app/(app)/onboarding/page.tsx"` returns 0 (D-09 token-only).
    - `grep -c "getLatestImportSummaryForUser" "lib/dal/imports.ts"` returns at least 1.
    - `yarn test --run tests/onboarding-page.test.tsx` exits 0 with all cases passing.
    - `yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The onboarding page selects the right step component based on `?step`, Step 1 reuses the R2 presigned PUT pipeline + analyze/confirm actions, Step 3 displays real auto/uncategorized counts and the exact giroconto tip text, all without any hardcoded slate-* or hex colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Step 2 Overview with real data + formatMonthRange display (R-OB-05, R-OB-10)</name>
  <files>app/(app)/onboarding/_components/step-2-overview.tsx, lib/dal/imports.ts, tests/step-2-overview.test.tsx</files>
  <read_first>
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx lines 84–115 (Step 2 layout — file name + check icon, large transaction count, two stat cards income/expenses, auto-categorized progress bar, months covered footer)
    - lib/dal/imports.ts (updated in Task 2 with getLatestImportSummaryForUser — extend with month range data here too)
    - lib/utils/date.ts (formatMonthRange from Wave 1)
    - lib/utils/decimal.ts (toDecimal, toDbDecimal — for displaying monetary aggregates)
    - lib/db/schema.ts lines 300–349 (file table — positiveTotal, negativeTotal, importedCount, referenceStartedAt, referenceEndedAt)
    - lib/db/schema.ts lines 351–386 (expense table — for auto-categorized count via leftJoin)
    - tests/dashboard-utils.test.ts (vitest + RTL conventions; if absent fall back to tests/onboarding-page.test.tsx — created in Task 2)
  </read_first>
  <behavior>
    - `Step2Overview` is a server component. Props: `{ userId: string }`. It calls `getLatestImportSummaryForUser(userId)` (extended in this task to include the month range fields). Renders:
      - A row with `CheckCircle` icon (lucide) + the file display name (truncated)
      - A large numeral `{importedCount}` with subtitle "transazioni importate"
      - Two stat cards: income (`+€{formattedPositiveTotal}` in green via `text-success` token) and expenses (`−€{formattedNegativeTotal}` in red via `text-destructive` token). Amounts formatted via `Intl.NumberFormat('it-IT', { minimumFractionDigits: 0 })` for the prototype look (no decimal places when integer).
      - A progress bar: `{autoCategorizedCount} di {importedCount} già categorizzate` + percentage. Percentage = Math.round(autoCategorizedCount / importedCount * 100); guard divide-by-zero by rendering 0%.
      - Months-covered label rendered via `formatMonthRange(firstMonth, lastMonth)` from `lib/utils/date.ts` when both dates are present; "Periodo non disponibile" fallback when null.
    - `getLatestImportSummaryForUser(userId)` returns an extended shape including `firstMonth: Date | null` and `lastMonth: Date | null` — computed by joining a sub-select on `transaction` for that file (DATE_TRUNC('month', MIN/MAX(occurredAt))). Avoid native arithmetic on totalAmount; pass strings to Decimal helpers if any computation is needed.
    - Decimal arithmetic rule: never use `+` / `-` / `*` / `/` on monetary strings; use `toDecimal()` for any computation. Percentage uses integer division on integer counts only (transactions and expenses are integer counts, not money) so plain JS arithmetic is acceptable for percentage.
  </behavior>
  <action>
    Step A — Extend `lib/dal/imports.ts`:
    Make `getLatestImportSummaryForUser(userId: string): Promise<LatestImportSummary | null>` return type `LatestImportSummary = { fileId: string; fileName: string; importedCount: number; autoCategorizedCount: number; uncategorizedCount: number; positiveTotal: string; negativeTotal: string; firstMonth: Date | null; lastMonth: Date | null }`. Query: `db.select({ ... }).from(file).leftJoin(expense, eq(expense.importedFromFileId, file.id)).where(eq(file.userId, userId)).orderBy(desc(file.importedAt)).limit(1)` then a second query for first/last month via the Wave-1 `getFileCoveredMonths(fileId, userId)`. Counts use `count(*)` and `count(*) FILTER (WHERE expense.sub_category_id IS NOT NULL)` aggregations.

    Step B — Create `app/(app)/onboarding/_components/step-2-overview.tsx` (server component):
    Imports: `CheckCircle` from lucide-react, `getLatestImportSummaryForUser`, `formatMonthRange`. Render a `flex flex-col min-h-screen px-6 pt-4 pb-24 text-foreground` outer div (token-based). Use a `Intl.NumberFormat('it-IT', { minimumFractionDigits: 0, maximumFractionDigits: 2 })` for the income/expenses. Build the percentage display defensively when importedCount is 0. Apply rounded-2xl card surfaces using `bg-foreground/10` (theme-driven). Use the months label or 'Periodo non disponibile'. Apply the tokens `text-success` and `text-destructive` for the income/expenses tints (declare these in `app/globals.css` if not already present — link to existing `--destructive` token; success is `--chart-1` or new `--success` if needed).

    Step C — Create `tests/step-2-overview.test.tsx` (vitest + RTL):
    Mock `getLatestImportSummaryForUser` to return:
    - Case 1 (R-OB-05): `{ fileId: 'f1', fileName: 'estratto-aprile.csv', importedCount: 123, autoCategorizedCount: 80, uncategorizedCount: 43, positiveTotal: '4500.00', negativeTotal: '3200.50', firstMonth: new Date(2026, 3, 1), lastMonth: new Date(2026, 3, 30) }` — assert rendered HTML contains '123', 'transazioni importate', 'Apr 2026', '+€4.500' (or the Intl-formatted variant), '−€3.200,50', '65%' (rounded 80/123).
    - Case 2 (R-OB-10 multi-month): same shape but lastMonth = `new Date(2026, 4, 30)` — assert label is 'Apr–Mag 2026'.
    - Case 3 (R-OB-05 boundary): importedCount: 0, autoCategorizedCount: 0 — assert '0%' is rendered, no NaN string.
    - Case 4 (defensive): summary is null — assert fallback markup contains 'Nessun file caricato'.
    Use `import { render } from '@testing-library/react'`; component is async so wrap in `await render(await Step2Overview({ userId: 'u1' }))` if needed, or use React 19's experimental async-RSC render pattern (consult tests/dashboard-charts.test.tsx for the project's chosen RSC test pattern). If async RSC rendering is unsupported in vitest, factor a pure-data formatter helper `buildStep2ViewModel(summary): Step2ViewModel` and test the formatter directly (no React).
  </action>
  <verify>
    <automated>yarn test --run tests/step-2-overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function Step2Overview\\|export const Step2Overview\\|export default" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns at least 1.
    - `grep -c "formatMonthRange" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns at least 1.
    - `grep -c "getLatestImportSummaryForUser" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns at least 1.
    - `grep -c "Periodo non disponibile" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns at least 1.
    - `grep -cE "(bg-slate-|text-slate-|from-slate-|to-slate-|#[0-9a-fA-F]{6}|text-green-|text-red-)" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns 0 (D-09: no hardcoded color classes; use `text-success`/`text-destructive`).
    - `grep -cE "(totalAmount\\.toFixed|positiveTotal\\.[a-z]+\\()" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns 0 OR the file uses `toDecimal()` (`grep -c "toDecimal" "app/(app)/onboarding/_components/step-2-overview.tsx"` returns at least 1 if any monetary computation is performed).
    - `grep -c "LatestImportSummary" "lib/dal/imports.ts"` returns at least 1.
    - `yarn test --run tests/step-2-overview.test.tsx` exits 0 with all 4 cases passing.
    - `yarn tsc --noEmit` exits 0.
    - `yarn check:language` exits 0.
  </acceptance_criteria>
  <done>Step 2 overview renders real per-file aggregates (counts, totals, percentage, months covered label) from the latest user file, with token-based colors only and Italian formatting; null state has a defensive fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/files/initiate | Existing upload boundary (not introduced by this plan); reused as-is. |
| Browser → R2 presigned PUT | Existing boundary; reused as-is — never proxies bytes through the server (project hard rule). |
| Browser → onboarding Server Components | RSC pages receive only `searchParams` from the URL (step) which is Zod-validated before use. |
| URL `?step` query parameter | Untrusted; clamped to 1..5 via `parseOnboardingStep`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Tampering | `?step` URL query param | mitigate | Zod schema `z.coerce.number().int().min(1).max(5).catch(1)` rejects out-of-range, non-numeric, and array-shaped inputs; default fallback to step=1. |
| T-38-07 | Spoofing | `getLatestImportSummaryForUser(userId)` | mitigate | All queries filter `WHERE userId = $1` with userId from `verifySession()`; expense join also constrained to same userId; no cross-user data leakage. |
| T-38-08 | Information Disclosure | Step 3 education panel | mitigate | Counts shown are scoped to user's latest file only; no global aggregates exposed. |
| T-38-09 | Tampering | Upload flow `analyzeImportAction` + `confirmImportAction` | mitigate | Both server actions already call `verifySession()` (existing code; no changes to action layer); onboarding form does not bypass this. |
| T-38-10 | Repudiation | Onboarding upload not distinguished from regular import | accept | Onboarding uploads are normal imports in the `file` table — no special source flag. Acceptable: the user can repeat any upload from /import. |
| T-38-SC | Tampering | npm/pip installs | accept | No new packages introduced (cmdk and radix-ui already installed). Legitimacy gate not triggered. |
</threat_model>

<verification>
- `yarn test --run tests/onboarding-step-validation.test.ts tests/onboarding-page.test.tsx tests/step-2-overview.test.tsx tests/imports-dal.test.ts` exits 0.
- `yarn tsc --noEmit` exits 0.
- `yarn lint` exits 0.
- `yarn check:language` exits 0 — only Italian appears in displayed user copy and STEP_NAMES values; identifiers and comments in English.
- Manual smoke (post-task): visiting `/onboarding` while authenticated returns 200 and renders the upload screen; visiting `/onboarding?step=3` renders the education panel with the giroconto tip text.
</verification>

<success_criteria>
- `/onboarding` route exists with correct step-based content (1=upload, 2=overview, 3=education).
- Step 1 uses the existing R2 presigned PUT + analyze/confirm pipeline (no upload byte proxying).
- Step 2 displays real data from the latest user file with Italian month-range label.
- Step 3 contains the exact giroconto tip text and dynamic auto/uncategorized counts.
- Visual design uses ONLY design-system tokens (no slate-*, no hex, no green-/red- Tailwind classes in onboarding components).
- The parent (app) layout bypasses Sidebar/Topbar/BottomNav for `/onboarding`.
</success_criteria>

<output>
Create `.planning/phases/38-first-import-onboarding/38-02-SUMMARY.md` when done, recording: which token strategy was used (data-theme attribute remap vs Tailwind opacity utilities), how async RSC was tested in Step 2 (RTL async render or view-model extraction), and any deviations from variant-b.tsx layout.
</output>
