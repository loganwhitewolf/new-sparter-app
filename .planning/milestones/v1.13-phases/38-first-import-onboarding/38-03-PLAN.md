---
phase: 38-first-import-onboarding
plan: 03
type: execute
wave: 3
depends_on:
  - 38-02
files_modified:
  - app/(app)/onboarding/page.tsx
  - app/(app)/onboarding/_components/step-4-categorize.tsx
  - app/(app)/onboarding/_components/subcategory-combobox.tsx
  - app/(app)/onboarding/_components/step-5-outro.tsx
  - app/(app)/onboarding/_components/sticky-cta.tsx
  - lib/actions/onboarding.ts
  - lib/validations/onboarding.ts
  - tests/onboarding-categorize-action.test.ts
  - tests/subcategory-combobox.test.tsx
  - tests/step-5-outro.test.tsx
autonomous: true
requirements:
  - R-OB-07
  - R-OB-08
  - R-OB-09
  - R-OB-11
must_haves:
  truths:
    - "GET /onboarding?step=4 renders up to 15 uncategorized expenses ordered by |totalAmount| DESC deduplicated by descriptionHash"
    - "Each expense row has a shadcn Command + Popover combobox listing all subcategories visible to the user with the FlowNature label rendered as a badge next to the subcategory name"
    - "Selecting a subcategory submits the onboardingCategorizeExpense server action which updates expense.subCategoryId and expense.status='3' scoped to the current userId"
    - "Step 4 background uses the light onboarding theme (data-theme='onboarding-light') per D-09"
    - "A 'Categorizza il resto dopo' skip CTA is always visible alongside the primary 'Continua' CTA at the bottom"
    - "GET /onboarding?step=5 renders the outro with two CTAs: 'Vai alla dashboard' linking to /dashboard and 'Personalizza le categorie' linking to /settings/categories"
    - "Prototype directory app/(app)/prototype/onboarding and components/ui/prototype-switcher.tsx are deleted; no imports of PrototypeSwitcher remain in the codebase"
    - "yarn build exits 0 (full E2E gate)"
  artifacts:
    - path: app/(app)/onboarding/_components/step-4-categorize.tsx
      provides: "Step 4 manual categorization wizard"
      exports: ["Step4Categorize"]
    - path: app/(app)/onboarding/_components/subcategory-combobox.tsx
      provides: "shadcn Command + Popover combobox with FlowNature badge per subcategory"
      exports: ["SubcategoryCombobox"]
    - path: app/(app)/onboarding/_components/step-5-outro.tsx
      provides: "Step 5 outro screen with two dashboard/settings CTAs"
      exports: ["Step5Outro"]
    - path: lib/actions/onboarding.ts
      provides: "onboardingCategorizeExpense server action — thin wrapper around the existing categorizeExpense"
      exports: ["onboardingCategorizeExpense"]
  key_links:
    - from: app/(app)/onboarding/_components/step-4-categorize.tsx
      to: lib/dal/transactions.ts (getTopUncategorizedExpenses)
      via: "RSC fetch top 15"
      pattern: "getTopUncategorizedExpenses"
    - from: app/(app)/onboarding/_components/subcategory-combobox.tsx
      to: lib/utils/nature-labels.ts (NATURE_LABELS)
      via: "import for FlowNature badge text"
      pattern: "NATURE_LABELS"
    - from: app/(app)/onboarding/_components/subcategory-combobox.tsx
      to: lib/actions/onboarding.ts (onboardingCategorizeExpense)
      via: "Server Action invocation on select"
      pattern: "onboardingCategorizeExpense"
    - from: app/(app)/onboarding/_components/step-5-outro.tsx
      to: APP_ROUTES.dashboard + APP_ROUTES.categorySettings
      via: "Link href"
      pattern: "APP_ROUTES\\.(dashboard|categorySettings)"
---

<objective>
Land the final onboarding screens — Step 4 manual categorization (top 15 uncategorized expenses with shadcn Combobox + FlowNature badges, scoped server action) and Step 5 outro (two CTAs to /dashboard and /settings/categories) — and delete the prototype directory in the same commit as the real implementation lands (D-12). Update the SticktyCta from Plan 38-02 to handle Step 4's two-button bar and Step 5 (no CTA).

Purpose: This plan delivers the parts of the onboarding flow that drive user value (manual categorization is the only meaningful action remaining after upload + overview) and closes the prototype gate so the codebase has a single source of truth for the onboarding UI. After this plan, all R-OB-* requirements are GREEN.

Output:
- `app/(app)/onboarding/_components/step-4-categorize.tsx` (Step 4 wizard)
- `app/(app)/onboarding/_components/subcategory-combobox.tsx` (Command + Popover combobox with FlowNature badge)
- `app/(app)/onboarding/_components/step-5-outro.tsx` (outro)
- Updated `app/(app)/onboarding/_components/sticky-cta.tsx` (Step 4 dual-CTA + Step 5 none)
- `lib/actions/onboarding.ts` (onboardingCategorizeExpense)
- `app/(app)/onboarding/page.tsx` (Step 4 + Step 5 wiring; placeholders removed)
- Deleted: `app/(app)/prototype/onboarding/` (entire directory) and `components/ui/prototype-switcher.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-first-import-onboarding/38-CONTEXT.md
@.planning/phases/38-first-import-onboarding/38-01-PLAN.md
@.planning/phases/38-first-import-onboarding/38-02-PLAN.md
@docs/adr/0005-first-import-onboarding-gate.md
@app/(app)/prototype/onboarding/_variants/variant-b.tsx
@lib/actions/expenses.ts
@lib/dal/categories.ts
@lib/dal/transactions.ts
@lib/utils/nature-labels.ts
@lib/routes.ts
@components/ui/command.tsx
@components/ui/popover.tsx
@components/ui/button.tsx
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onboardingCategorizeExpense server action + SubcategoryCombobox (R-OB-07)</name>
  <files>lib/actions/onboarding.ts, lib/validations/onboarding.ts, app/(app)/onboarding/_components/subcategory-combobox.tsx, tests/onboarding-categorize-action.test.ts, tests/subcategory-combobox.test.tsx</files>
  <read_first>
    - lib/actions/expenses.ts (full file — `categorizeExpense` server action: verifySession, isSubCategoryVisibleToUser, db.transaction with classification history write, revalidateCategorizationSurfaces)
    - lib/validations/expense.ts (SingleCategorizeSchema definition — shape: { id, subCategoryId })
    - lib/dal/categories.ts (getCategories — returns CategoryWithSubCategories[] with effectiveNature per subcategory)
    - lib/utils/nature-labels.ts (NATURE_LABELS export — keys include 'unclassified' for null nature)
    - components/ui/command.tsx (Command, CommandInput, CommandList, CommandItem, CommandGroup, CommandEmpty exports — confirm the API)
    - components/ui/popover.tsx (Popover, PopoverTrigger, PopoverContent)
    - components/ui/badge.tsx (Badge component)
    - components/ui/button.tsx
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx lines 136–168 (Step 4 prototype rendering — note: prototype uses native select; this plan replaces with Combobox)
    - tests/expense-actions.test.ts (testing pattern for server actions)
    - tests/category-settings-ui.test.tsx (RTL + cmdk testing pattern, if present)
  </read_first>
  <behavior>
    - `lib/actions/onboarding.ts` exports `onboardingCategorizeExpense(prevState, formData)` — a thin `"use server"` wrapper that:
      (1) Parses `{ id: string, subCategoryId: number }` via the existing `SingleCategorizeSchema` (no new schema needed; reuse).
      (2) Calls `verifySession()` for userId.
      (3) Calls `isSubCategoryVisibleToUser(subCategoryId, userId)` — returns error if not visible.
      (4) Runs the SAME transaction body as `categorizeExpense` in `lib/actions/expenses.ts` (update expense, write classification history with source='manual') BUT inside a wrapper that adds `revalidatePath(APP_ROUTES.onboarding)` (so step=4 reflects the removal) in addition to the existing `revalidateCategorizationSurfaces()` call.
      (5) Returns `{ error: null }` on success, `{ error: string }` otherwise — same `ActionState` shape as expense actions.
      Per Claude's Discretion in CONTEXT.md, we create a new action (rather than reuse categorizeExpense) so we can add the onboarding-specific revalidate without polluting the generic expense path.
    - Extend `lib/validations/onboarding.ts` with `OnboardingCategorizeExpenseSchema` — just re-export `SingleCategorizeSchema` under a new name if the project prefers domain-named schemas; otherwise import directly in the action and skip the alias. Choose: import directly to avoid indirection.
    - `SubcategoryCombobox` is a client component. Props: `{ expenseId: string, expenseTitle: string, expenseAmount: string, categories: CategoryWithSubCategories[] }`. Renders a Popover-wrapped Command listing subcategories grouped by category. Each subcategory row shows the name + a Badge with the Italian nature label (NATURE_LABELS[effectiveNature ?? 'unclassified']). Selecting an item invokes `onboardingCategorizeExpense` via `useActionState` from react. On success the row is removed from view (optimistic + after revalidate). Loading state during pending action. Italian copy: trigger button text "Seleziona categoria...", search placeholder "Cerca categoria...".
    - Use `data-state` and the existing shadcn Command/Popover idioms; no hardcoded colors — use `bg-card`, `text-foreground`, `border`.
  </behavior>
  <action>
    Step A — Create `lib/actions/onboarding.ts`:
    `'use server'` directive. Imports: `verifySession`, `SingleCategorizeSchema`, `isSubCategoryVisibleToUser`, `db`, `expense`, `and`, `eq`, `writeClassificationHistory`, `revalidatePath`, `APP_ROUTES`, `revalidateCategorizationSurfaces`, `ActionState`. Export `async function onboardingCategorizeExpense(prevState: ActionState, formData: FormData): Promise<ActionState>` implementing the behavior described above. Mirror the exact transaction body from `categorizeExpense` (read before-row, update expense.subCategoryId + status='3', write classification history with `source: 'manual'`, ignore history write failure). After the transaction, call `revalidateCategorizationSurfaces()` then `revalidatePath(APP_ROUTES.onboarding)`. Same error mapping as the original action.

    Step B — Create `app/(app)/onboarding/_components/subcategory-combobox.tsx`:
    `'use client'`. Use `useActionState(onboardingCategorizeExpense, { error: null })` from `react`. Render:
    (1) Outer card div with rounded-2xl bg-card border border-border p-4.
    (2) Header row: `expenseTitle` (font-semibold) + `expenseAmount` formatted (green when amount >= 0, red when negative — use `text-success` / `text-destructive` tokens; NOT `text-green-600` / `text-red-600`).
    (3) `<Popover>` with `<PopoverTrigger asChild><Button variant="outline" className="w-full justify-between">{selectedLabel ?? 'Seleziona categoria...'}</Button></PopoverTrigger>`.
    (4) `<PopoverContent className="w-[--radix-popover-trigger-width] p-0"><Command><CommandInput placeholder="Cerca categoria..." /><CommandList><CommandEmpty>Nessuna categoria trovata</CommandEmpty>{categories.map(cat => (<CommandGroup heading={cat.name}>{cat.subCategories.map(sub => (<CommandItem key={sub.id} value={`${cat.name} ${sub.name}`} onSelect={() => handleSelect(sub.id)}><span>{sub.name}</span><Badge variant="secondary" className="ml-auto">{NATURE_LABELS[sub.effectiveNature ?? 'unclassified']}</Badge></CommandItem>))}</CommandGroup>))}</CommandList></Command></PopoverContent>`.
    (5) `handleSelect(subCategoryId: number)` builds a FormData with `id: expenseId` and `subCategoryId`, calls the action via the form transition; while pending shows a Loader2 icon on the trigger.
    Identifiers in English; only the listed Italian strings are visible labels.

    Step C — Create `tests/onboarding-categorize-action.test.ts` (vitest, model after tests/expense-actions.test.ts):
    Mock `verifySession`, `isSubCategoryVisibleToUser`, `db.transaction`, `writeClassificationHistory`, `revalidatePath`, `revalidateCategorizationSurfaces`. Cases (each references R-OB-07 in `it()` description):
    - "rejects when SingleCategorizeSchema fails (missing id)" — assert `{ error: <message> }`.
    - "rejects when isSubCategoryVisibleToUser returns false" — assert error 'Sottocategoria non valida.'.
    - "scopes the UPDATE to (id, userId) — IDOR guard" — assert the update was issued with `eq(expense.userId, userId)` predicate (capture via mock tx.update().set().where chain).
    - "writes classification history with source='manual'" — assert writeClassificationHistory called with `{ source: 'manual', ... }`.
    - "calls revalidatePath(APP_ROUTES.onboarding) after success in addition to revalidateCategorizationSurfaces" — assert both mocks invoked.
    - "returns { error: null } on happy path".

    Step D — Create `tests/subcategory-combobox.test.tsx` (vitest + RTL):
    Mock the action via vi.mock('@/lib/actions/onboarding', () => ({ onboardingCategorizeExpense: vi.fn().mockResolvedValue({ error: null }) })). Fixture categories: two categories each with two subcategories; one subcategory has effectiveNature='essential', another has effectiveNature=null. Cases:
    - "renders the trigger with 'Seleziona categoria...' before any selection" (R-OB-07).
    - "opens the Popover and lists subcategories grouped by category on click" (R-OB-07).
    - "renders the FlowNature Italian label as a badge next to each subcategory" (R-OB-07) — assert 'Essenziale' appears in the same row as the essential subcategory; 'Non classificato' appears in the row with null nature.
    - "calls onboardingCategorizeExpense with the expense id and selected subCategoryId on select" (R-OB-07) — fire a click and assert the action mock was called with a FormData containing both fields.
    - "trigger uses ONLY design-system color tokens for the amount tint" — query the amount span and assert its class list includes `text-success` or `text-destructive`, NOT `text-green-600`/`text-red-600`.

    Identifiers in English; visible labels Italian.
  </action>
  <verify>
    <automated>yarn test --run tests/onboarding-categorize-action.test.ts tests/subcategory-combobox.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use server'" lib/actions/onboarding.ts` returns at least 1.
    - `grep -c "export async function onboardingCategorizeExpense" lib/actions/onboarding.ts` returns exactly 1.
    - `grep -c "revalidatePath(APP_ROUTES.onboarding)" lib/actions/onboarding.ts` returns at least 1.
    - `grep -c "writeClassificationHistory" lib/actions/onboarding.ts` returns at least 1.
    - `grep -c "eq(expense.userId, userId)" lib/actions/onboarding.ts` returns at least 1 (IDOR guard preserved).
    - `grep -c "export function SubcategoryCombobox\\|export const SubcategoryCombobox" "app/(app)/onboarding/_components/subcategory-combobox.tsx"` returns at least 1.
    - `grep -c "NATURE_LABELS" "app/(app)/onboarding/_components/subcategory-combobox.tsx"` returns at least 1.
    - `grep -c "PopoverTrigger\\|PopoverContent" "app/(app)/onboarding/_components/subcategory-combobox.tsx"` returns at least 2.
    - `grep -c "CommandInput\\|CommandItem\\|CommandGroup" "app/(app)/onboarding/_components/subcategory-combobox.tsx"` returns at least 3.
    - `grep -cE "(text-green-|text-red-|bg-slate-|#[0-9a-fA-F]{6})" "app/(app)/onboarding/_components/subcategory-combobox.tsx"` returns 0.
    - `yarn test --run tests/onboarding-categorize-action.test.ts tests/subcategory-combobox.test.tsx` exits 0.
    - `yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Server action enforces IDOR scoping and revalidates both the standard categorization surfaces and /onboarding; combobox renders FlowNature badges via NATURE_LABELS and submits the action correctly; all assertions GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Step 4 wizard + Step 5 outro + sticky-cta update + page.tsx wiring (R-OB-07, R-OB-08, R-OB-09)</name>
  <files>app/(app)/onboarding/page.tsx, app/(app)/onboarding/_components/step-4-categorize.tsx, app/(app)/onboarding/_components/step-5-outro.tsx, app/(app)/onboarding/_components/sticky-cta.tsx, tests/step-5-outro.test.tsx</files>
  <read_first>
    - app/(app)/onboarding/page.tsx (built in Plan 38-02 — current step routing logic with placeholders for steps 4/5)
    - app/(app)/onboarding/_components/sticky-cta.tsx (built in Plan 38-02 — currently handles steps 2 and 3 only)
    - app/(app)/onboarding/_components/subcategory-combobox.tsx (created in Task 1 of this plan)
    - app/(app)/prototype/onboarding/_variants/variant-b.tsx lines 136–168 (Step 4 layout) and lines 170–189 (Step 5 outro)
    - lib/dal/transactions.ts (getTopUncategorizedExpenses from Plan 38-01)
    - lib/dal/categories.ts (getCategories — RSC entry returns CategoryWithSubCategories[])
    - lib/routes.ts (APP_ROUTES.dashboard, APP_ROUTES.categorySettings, APP_ROUTES.onboarding)
    - components/ui/button.tsx (variants: default, outline, ghost)
    - tests/onboarding-page.test.tsx (existing test patterns)
  </read_first>
  <behavior>
    - `Step4Categorize` is an async server component. Props: `{ userId: string }`. Calls in parallel `getTopUncategorizedExpenses(userId, 15)` and `getCategories()`. Renders:
      - Header: h1 "Categorizza le spese principali", subtitle "Le 15 con il valore più alto" + count remaining.
      - List of expense rows, each with `<SubcategoryCombobox expenseId={e.id} expenseTitle={e.title} expenseAmount={e.totalAmount} categories={categories} />`.
      - Background: rendered inside OnboardingShell with theme='light' (page.tsx selects theme based on step) — but the Variant B prototype shows the categorization screen as light (D-09). The shell handles the background; this component focuses on content.
      - When the list is empty (all 15 done), render a celebratory empty state: "Tutto categorizzato!" + an inline CTA to advance to step 5 via `<Link href="/onboarding?step=5">Continua</Link>`.
    - `Step5Outro` is a server component. Renders the celebratory "Benvenuto in Sparter!" outro with two Link buttons:
      - Primary: `<Link href={APP_ROUTES.dashboard}>Vai alla dashboard</Link>` styled as Button default.
      - Secondary: `<Link href={APP_ROUTES.categorySettings}>Personalizza le categorie</Link>` styled as Button outline.
      - Optional check-circle icon at the top (lucide `CheckCircle`).
    - `StickyCta` extended cases:
      - step 4: render two buttons in a flex row — left (ghost) "Categorizza il resto dopo" linking to `/onboarding?step=5`; right (default) "Continua" linking to `/onboarding?step=5` as well (both lead to outro; the difference is messaging — the left signals "I'll do this later", the right signals "I'm done for now").
      - step 5: render nothing (return null).
    - `page.tsx`: remove the placeholders, wire Step 4 and Step 5 into the switch, and pass `userId` to Step 4. Confirm theme='light' on step 4, theme='dark' otherwise (was set in Plan 38-02 but verify here).
    - Background gradient for step 5: prototype uses `from-green-600 to-green-700`. Token-based replacement: declare a `[data-theme="onboarding-success"]` block in `app/globals.css` and use it on the shell for step 5; shell selects `data-theme` based on a `theme` prop value extended to include `'success'`. Alternatively, reuse `'dark'` theme for step 5 to avoid introducing a third token set — per Claude's Discretion in CONTEXT.md, choose REUSE 'dark' to minimize new token surface. Document the choice in the SUMMARY.
  </behavior>
  <action>
    Step A — Update `app/(app)/onboarding/_components/sticky-cta.tsx`:
    Extend the prop type to handle step 4 (two buttons) and step 5 (null). Step 4 uses Button variant='ghost' for the secondary and variant='default' for the primary, both linking to `/onboarding?step=5`. Step 5 returns null. Use `Link` from `next/link`. Replace any prototype slate-* classes with token-based `border-border`, `bg-background`. Container retains `fixed bottom-0 left-0 right-0` + `p-4` + `border-t border-border bg-background`.

    Step B — Create `app/(app)/onboarding/_components/step-4-categorize.tsx` (async RSC):
    Import `getTopUncategorizedExpenses`, `getCategories`, `SubcategoryCombobox`. Render the layout in token-based tailwind (light theme is set by the shell). The empty-state branch renders a card with check-icon + "Tutto categorizzato!" + a Link to step=5 styled as Button.

    Step C — Create `app/(app)/onboarding/_components/step-5-outro.tsx` (server component):
    Imports: `Link` from next/link, `APP_ROUTES`, `CheckCircle` from lucide-react, `Button`. Render: circular icon container, h1 "Benvenuto in Sparter!", paragraph "Il tuo primo estratto è pronto. Cosa vuoi fare adesso?", two CTAs as Button-styled Links to `APP_ROUTES.dashboard` and `APP_ROUTES.categorySettings`. Token-only colors. Do not introduce a `?firstRun` query string (D-08 — no firstRun flag).

    Step D — Update `app/(app)/onboarding/page.tsx`:
    Remove the step-4/5 placeholders. In the switch: 4 → `<Step4Categorize userId={userId} />`, 5 → `<Step5Outro />`. Confirm the StickyCta is rendered in the footer slot for steps 2, 3, AND 4 (step 4 has its own dual-CTA bar) — for step 5 the footer slot is undefined. Confirm theme: step 4 → 'light', step 5 → 'dark' (REUSE decision per behavior block).

    Step E — Create `tests/step-5-outro.test.tsx` (vitest + RTL):
    Cases (each references R-OB-08 in `it()` description):
    - "renders the Italian welcome heading 'Benvenuto in Sparter!'" (R-OB-08).
    - "renders a link to /dashboard with text 'Vai alla dashboard'" (R-OB-08) — assert `getByRole('link', { name: /Vai alla dashboard/ })` href is `/dashboard`.
    - "renders a link to /settings/categories with text 'Personalizza le categorie'" (R-OB-08) — assert href is `/settings/categories`.
    - "does NOT append a firstRun query parameter to either CTA" (D-08) — assert neither href contains `firstRun`.
    - "uses ONLY design-system tokens for background and text colors" — assert no green-/red- classes appear in the rendered output.

    Step F — Extend `tests/onboarding-page.test.tsx` (Plan 38-02 file) with cases:
    - "?step=4 renders Step4Categorize with the user id from session" (R-OB-07) — mock getTopUncategorizedExpenses + getCategories, assert getTopUncategorizedExpenses was called with userId and limit=15.
    - "?step=5 renders Step5Outro" (R-OB-08).
    - "step=4 uses 'light' theme on OnboardingShell" (R-OB-09).
    - "step=5 sticky CTA bar is not rendered" — assert no element with `data-testid='sticky-cta'` exists OR the StickyCta returns null.
    - "step=4 renders the secondary 'Categorizza il resto dopo' button" (D-07) — assert text appears in DOM.
  </action>
  <verify>
    <automated>yarn test --run tests/step-5-outro.test.tsx tests/onboarding-page.test.tsx tests/subcategory-combobox.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function Step4Categorize\\|export const Step4Categorize\\|export default" "app/(app)/onboarding/_components/step-4-categorize.tsx"` returns at least 1.
    - `grep -c "getTopUncategorizedExpenses" "app/(app)/onboarding/_components/step-4-categorize.tsx"` returns at least 1.
    - `grep -c "SubcategoryCombobox" "app/(app)/onboarding/_components/step-4-categorize.tsx"` returns at least 1.
    - `grep -c "Tutto categorizzato" "app/(app)/onboarding/_components/step-4-categorize.tsx"` returns at least 1.
    - `grep -c "Categorizza le spese principali" "app/(app)/onboarding/_components/step-4-categorize.tsx"` returns at least 1.
    - `grep -c "Benvenuto in Sparter" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns at least 1.
    - `grep -c "Vai alla dashboard" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns at least 1.
    - `grep -c "Personalizza le categorie" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns at least 1.
    - `grep -c "APP_ROUTES.dashboard" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns at least 1.
    - `grep -c "APP_ROUTES.categorySettings" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns at least 1.
    - `grep -c "firstRun" "app/(app)/onboarding/_components/step-5-outro.tsx"` returns 0 (D-08 negative assertion).
    - `grep -c "Categorizza il resto dopo" "app/(app)/onboarding/_components/sticky-cta.tsx"` returns at least 1.
    - `grep -c "Step4Categorize\\|Step5Outro" "app/(app)/onboarding/page.tsx"` returns at least 2.
    - `grep -cE "(bg-slate-|text-slate-|from-slate-|from-green-|to-green-|to-slate-|#[0-9a-fA-F]{6})" "app/(app)/onboarding/_components/step-4-categorize.tsx" "app/(app)/onboarding/_components/step-5-outro.tsx" "app/(app)/onboarding/_components/sticky-cta.tsx"` returns 0.
    - `yarn test --run tests/step-5-outro.test.tsx tests/onboarding-page.test.tsx` exits 0 with all cases passing.
    - `yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Step 4 wizard renders top 15 deduplicated uncategorized expenses with Combobox + FlowNature badges and supports a "Categorize the rest later" skip CTA; Step 5 outro routes to dashboard and category settings without a firstRun flag; all theme tokens are design-system only.</done>
</task>

<task type="auto">
  <name>Task 3: Delete prototype + verify full E2E gate (R-OB-11, D-12)</name>
  <files>app/(app)/prototype/onboarding/, components/ui/prototype-switcher.tsx</files>
  <read_first>
    - app/(app)/prototype/onboarding/page.tsx (PrototypeSwitcher import — confirm no other consumer)
    - components/ui/prototype-switcher.tsx (the file to delete)
    - tests/onboarding-page.test.tsx (built in Plan 38-02 + extended in Task 2 of this plan — confirms onboarding flow tests are decoupled from prototype)
  </read_first>
  <action>
    Step A — Verify no production code references the prototype:
    Run `rg -n "prototype-switcher\\|PrototypeSwitcher\\|prototype/onboarding" --type ts --type tsx app components lib tests` (excluding the prototype directory itself). If any result appears OUTSIDE `app/(app)/prototype/`, STOP and surface as a blocking issue — do not delete until those references are removed. The expected output is zero references outside the prototype directory.

    Step B — Delete the prototype directory and the switcher:
    `rm -rf "app/(app)/prototype/onboarding/"` and `rm "components/ui/prototype-switcher.tsx"`. If the parent `app/(app)/prototype/` directory becomes empty, remove it as well (`rmdir "app/(app)/prototype/"`).

    Step C — Final E2E gate:
    Run, in this order:
    (1) `yarn tsc --noEmit` — must exit 0 (no dangling type references).
    (2) `yarn lint` — must exit 0.
    (3) `yarn test` — must exit 0 (full vitest suite).
    (4) `yarn check:language` — must exit 0.
    (5) `yarn build` — must exit 0 (full Next.js build with webpack flag).
    If any step fails, fix the failure (most likely a stale import) and re-run; do not move on with a failing gate.
  </action>
  <verify>
    <automated>test ! -d "app/(app)/prototype/onboarding" &amp;&amp; test ! -f "components/ui/prototype-switcher.tsx" &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `test -d "app/(app)/prototype/onboarding"` returns non-zero exit (directory does not exist).
    - `test -f "components/ui/prototype-switcher.tsx"` returns non-zero exit (file does not exist).
    - `rg -c "prototype-switcher|PrototypeSwitcher" app components lib tests --type ts --type tsx 2>/dev/null` returns 0 matches.
    - `rg -c "prototype/onboarding" app components lib tests --type ts --type tsx 2>/dev/null` returns 0 matches.
    - `yarn tsc --noEmit` exits 0.
    - `yarn lint` exits 0.
    - `yarn test` exits 0 (full suite, not just onboarding files).
    - `yarn check:language` exits 0.
    - `yarn build` exits 0 (full Next.js build succeeds — final E2E gate per the success_criteria of Phase 38).
  </acceptance_criteria>
  <done>The prototype directory and switcher are removed; the full repo gate (tsc, lint, test, language, build) passes; R-OB-11 / D-12 satisfied in the same wave that ships the real implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → onboardingCategorizeExpense | Server Action receives a FormData with `id` (string) and `subCategoryId` (number) from the browser; both validated via Zod (SingleCategorizeSchema) and authorization via verifySession + isSubCategoryVisibleToUser. |
| URL → /onboarding?step=4 | Step value already clamped via parseOnboardingStep (Plan 38-02); no new untrusted input introduced here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-11 | Spoofing | `onboardingCategorizeExpense` accepts `id` (expense id) from the form | mitigate | The UPDATE clause uses `and(eq(expense.id, parsed.id), eq(expense.userId, userId))` — a user submitting another user's expense id results in zero rows updated; classification history write only runs after the update returns a row. Same IDOR pattern as the existing categorizeExpense action. |
| T-38-12 | Tampering | `subCategoryId` from form | mitigate | `isSubCategoryVisibleToUser` enforces both system-and-user visibility before the UPDATE runs; arbitrary numeric IDs are rejected with 'Sottocategoria non valida.'. |
| T-38-13 | Information Disclosure | Step 4 reads `getCategories()` and `getTopUncategorizedExpenses(userId, 15)` | mitigate | Both calls scope to the current session userId; getCategories already filters by userId in the existing implementation; getTopUncategorizedExpenses parameterizes user_id via tagged template (Plan 38-01). |
| T-38-14 | Denial of Service | Combobox lists potentially hundreds of subcategories | accept | cmdk + Radix Popover handle thousands of items efficiently; user-owned subcategories are bounded in practice (<200). |
| T-38-15 | Repudiation | Categorization without audit trail | mitigate | Classification history rows written with `source='manual'` for each categorization (same as existing categorizeExpense action). |
| T-38-SC | Tampering | npm/pip installs | accept | No new packages introduced; cmdk and radix-ui already installed per package.json. |
</threat_model>

<verification>
- `yarn test --run tests/onboarding-categorize-action.test.ts tests/subcategory-combobox.test.tsx tests/step-5-outro.test.tsx tests/onboarding-page.test.tsx` exits 0.
- `yarn tsc --noEmit` exits 0.
- `yarn lint` exits 0.
- `yarn test` (full suite) exits 0.
- `yarn check:language` exits 0.
- `yarn build` exits 0 (final E2E gate).
- Manual smoke: `/onboarding?step=4` lists up to 15 uncategorized expenses with Combobox + FlowNature badges; clicking a subcategory categorizes the expense and removes it from the list. `/onboarding?step=5` shows the outro with two working CTAs to `/dashboard` and `/settings/categories`. `app/(app)/prototype/onboarding/` no longer exists.
</verification>

<success_criteria>
- All R-OB-* requirements satisfied across the three plans (every ID present in at least one plan's `requirements` field).
- Server action enforces IDOR + subcategory visibility and revalidates both standard categorization surfaces and the onboarding route.
- Combobox uses shadcn Command + Popover with FlowNature badges (D-07 / R-OB-07).
- Outro routes to /dashboard and /settings/categories without a firstRun flag (D-08 / R-OB-08).
- Prototype directory and switcher component deleted (D-12 / R-OB-11).
- Full Next.js build passes (yarn build) — final phase gate.
</success_criteria>

<output>
Create `.planning/phases/38-first-import-onboarding/38-03-SUMMARY.md` when done, recording: the final Step 5 theme choice (`'dark'` reuse vs new `'success'` token block), the cmdk Combobox testing approach, the build size delta from `yarn build` output, and confirmation that the prototype was deleted in the same commit as the real implementation (D-12 invariant).
</output>
