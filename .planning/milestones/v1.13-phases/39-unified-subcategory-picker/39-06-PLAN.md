---
phase: 39-unified-subcategory-picker
plan: 06
type: execute
wave: 5
depends_on: ["39-03", "39-04", "39-05"]
files_modified:
  - components/expenses/category-combobox.tsx
  - app/(app)/onboarding/_components/subcategory-combobox.tsx
  - app/(app)/prototype/subcategory-picker/page.tsx
  - app/(app)/prototype/subcategory-picker/_prototype.tsx
  - app/(app)/prototype/subcategory-picker/NOTES.md
requirements: [R-UP-09, R-UP-10]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "CategoryCombobox no longer exists and nothing imports it"
    - "The onboarding SubcategoryCombobox's old Popover+Command implementation is gone (the file either deleted or contains only the SubcategoryPicker-based control from 39-03)"
    - "No cascading Categoria/Sottocategoria Select pairs remain in any categorization or pattern surface — including components/patterns/pattern-actions.tsx (the edit-existing-pattern dialog, surface #8 per D-08/D-10)"
    - "No user-facing amountSign (Segno importo) or confidence (Confidenza) control survives in any pattern surface (create-pattern-dialog, pattern-actions, suggestion-promote-form): no SelectContent for sign and no name=amountSign / name=confidence inputs remain"
    - "The prototype route app/(app)/prototype/subcategory-picker/ is deleted"
    - "yarn build and yarn check:language both pass"
  artifacts:
    - path: ".planning/phases/39-unified-subcategory-picker/39-06-SUMMARY.md"
      provides: "Record of removals + green build/language gate"
  key_links:
    - from: "repository"
      to: "components/categorization/subcategory-picker.tsx"
      via: "the only remaining subcategory-selection UI"
      pattern: "SubcategoryPicker"
---

<objective>
Final cleanup per D-10/R-UP-09/R-UP-10: now that every surface uses `SubcategoryPicker` (39-03/39-04/39-05), delete the superseded selection UIs and the prototype route, then prove the codebase is clean with `yarn build` + `yarn check:language`.

Delete `CategoryCombobox` (`components/expenses/category-combobox.tsx`), remove the onboarding `SubcategoryCombobox`'s old Popover+Command implementation, confirm no cascading Categoria/Sottocategoria `Select` pairs remain in any categorization or pattern surface — explicitly including `components/patterns/pattern-actions.tsx` (the edit-existing-pattern dialog, adoption surface #8) — and that no user amountSign/confidence control survives in any pattern surface, then delete the prototype route `app/(app)/prototype/subcategory-picker/`.

Purpose: Leave exactly one subcategory-selection control in the codebase (`SubcategoryPicker`), no duplicate selection UX, no user sign/confidence control on any pattern surface, and no throwaway prototype.
Output: Old components/route deleted; the cleanup grep sweep covers all migrated surfaces including pattern-actions.tsx; green build + language gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-unified-subcategory-picker/39-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CategoryCombobox + verify the cascading Select pairs and user sign/confidence controls are gone (all surfaces, incl. pattern-actions)</name>
  <files>components/expenses/category-combobox.tsx, app/(app)/onboarding/_components/subcategory-combobox.tsx</files>
  <read_first>
    - components/expenses/category-combobox.tsx (the component about to be deleted; confirm buildCategoryOptions/filterCategoryOptions were already extracted to lib/categorization/subcategory-options.ts in 39-01 so nothing here is still needed)
    - app/(app)/onboarding/_components/subcategory-combobox.tsx (after 39-03 this should already be SubcategoryPicker-based; confirm no Popover/Command/NATURE_LABELS remnant)
    - .planning/phases/39-unified-subcategory-picker/39-03-SUMMARY.md and 39-04-SUMMARY.md and 39-05-SUMMARY.md (confirm all callers migrated before deleting; 39-05 must have already migrated create-pattern-dialog.tsx, pattern-actions.tsx, and suggestion-promote-form.tsx)
  </read_first>
  <action>
    First confirm no live imports remain: run `grep -rn "CategoryCombobox" --include=*.tsx --include=*.ts .` and `grep -rn "category-combobox" --include=*.tsx --include=*.ts .` — the only matches should be the file itself (and possibly 39-01's extracted-helpers note). If any real consumer still imports `CategoryCombobox`, STOP and report (an adoption plan missed a surface). Otherwise delete `components/expenses/category-combobox.tsx`.
    For the onboarding `subcategory-combobox.tsx`: after 39-03 it is already rebuilt on SubcategoryPicker. If any Popover/Command/`NATURE_LABELS` remnant remains here (or in any other categorization/pattern surface), remove it. If `buildOnboardingCategorizeFormData` is exported and still referenced by a test, keep that export. Do not delete the onboarding file outright (39-03 rebuilt it in place); only ensure no legacy combobox code lingers.
    Finally, sweep ALL migrated surfaces for leftover cascading Select pairs AND user sign/confidence controls. `grep -rn "SelectContent" components/expenses/expense-form-dialog.tsx components/transactions/transaction-form-dialog.tsx components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx components/import/suggestion-promote-form.tsx` must return nothing (note: pattern-actions.tsx is included — surface #8 per D-10). Additionally `grep -rn 'name="amountSign"' components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx` and `grep -rn 'name="confidence"' components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx` must return nothing (no user sign/confidence control survives). If any of these remain, the corresponding adoption plan (39-05) left a regression — report it. Keep dev-facing code/comments English.
  </action>
  <verify>
    <automated>test ! -f components/expenses/category-combobox.tsx; ! grep -rq "from '@/components/expenses/category-combobox'" components app lib; ! grep -rq "SelectContent" components/expenses/expense-form-dialog.tsx components/transactions/transaction-form-dialog.tsx components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx components/import/suggestion-promote-form.tsx; ! grep -rq 'name="amountSign"' components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx; ! grep -rq 'name="confidence"' components/patterns/create-pattern-dialog.tsx components/patterns/pattern-actions.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/expenses/category-combobox.tsx no longer exists.
    - No file imports CategoryCombobox / category-combobox.
    - No NATURE_LABELS / Popover-based combobox remains in the onboarding subcategory control.
    - No SelectContent (cascading pair) remains in the FIVE migrated form/pattern surfaces, including components/patterns/pattern-actions.tsx.
    - No `name="amountSign"` or `name="confidence"` input remains in create-pattern-dialog.tsx or pattern-actions.tsx (no user sign/confidence control survives in any pattern surface).
  </acceptance_criteria>
  <done>The old pickers are gone; no cascading Select or user amountSign/confidence control survives in any pattern surface; only SubcategoryPicker remains as the selection UI.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the prototype route and run the E2E build + language gate</name>
  <files>app/(app)/prototype/subcategory-picker/page.tsx, app/(app)/prototype/subcategory-picker/_prototype.tsx, app/(app)/prototype/subcategory-picker/NOTES.md</files>
  <read_first>
    - app/(app)/prototype/subcategory-picker/ (the three files to delete: page.tsx, _prototype.tsx, NOTES.md)
    - .planning/phases/38-first-import-onboarding/ (precedent: a prior phase deleted its prototype on merge — same pattern)
    - components/categorization/subcategory-picker.tsx (confirm the production picker does NOT import anything from the prototype directory before deleting it)
  </read_first>
  <action>
    Confirm nothing in production imports from `app/(app)/prototype/subcategory-picker/`: `grep -rn "prototype/subcategory-picker" --include=*.tsx --include=*.ts .` should only match the prototype's own files (and planning docs). The production `SubcategoryPicker` (39-02) was ported, not imported, from the prototype — verify no live import. Then delete the entire `app/(app)/prototype/subcategory-picker/` directory (page.tsx, _prototype.tsx, NOTES.md). Run the full gate: `yarn build` must exit 0 and `yarn check:language` must pass (per D-10/R-UP-10). If `check:language` flags any developer-facing Italian introduced by this phase, fix it (product/domain surfaces remain Italian by design). Record removals + gate results in the SUMMARY.
  </action>
  <verify>
    <automated>test ! -d "app/(app)/prototype/subcategory-picker"; yarn build 2>&1 | tail -5; yarn check:language 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The directory app/(app)/prototype/subcategory-picker/ does not exist.
    - No production file imports from prototype/subcategory-picker.
    - yarn build exits 0.
    - yarn check:language passes.
  </acceptance_criteria>
  <done>Prototype route deleted; the phase ends with a green build and language gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (deletion-only) | this plan removes code/routes; it introduces no new runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-12 | Denial of service | deleting an in-use component | mitigate | Pre-deletion grep gates confirm zero live imports of CategoryCombobox and the prototype route before removal; yarn build is the backstop that fails on any dangling import. |
| T-39-13 | Information disclosure | prototype route exposed in prod | mitigate | Deleting app/(app)/prototype/subcategory-picker/ removes the prototype surface entirely (R-UP-10). |
</threat_model>

<verification>
- `test ! -f components/expenses/category-combobox.tsx` passes.
- `grep SelectContent` over the five migrated surfaces (expense-form-dialog, transaction-form-dialog, create-pattern-dialog, pattern-actions, suggestion-promote-form) matches NOTHING.
- `grep name="amountSign"` / `grep name="confidence"` over create-pattern-dialog.tsx + pattern-actions.tsx matches NOTHING.
- `test ! -d "app/(app)/prototype/subcategory-picker"` passes.
- `yarn build` exits 0.
- `yarn check:language` passes.
- `grep -r SubcategoryPicker` is the only subcategory-selection control referenced across surfaces.
</verification>

<success_criteria>
CategoryCombobox, the old onboarding combobox implementation, and the cascading Select pairs (including the one in pattern-actions.tsx) are removed; no user amountSign/confidence control survives in any pattern surface; the prototype route is deleted; `yarn build` and `yarn check:language` both pass — leaving exactly one selection UI (SubcategoryPicker).
</success_criteria>

<output>
Create `.planning/phases/39-unified-subcategory-picker/39-06-SUMMARY.md` when done.
</output>
