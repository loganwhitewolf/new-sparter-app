---
phase: 68-tags-dashboard-and-navigation
plan: 08
type: execute
wave: 3
depends_on: ["68-01", "68-04", "68-05"]
files_modified:
  - app/(app)/dashboard/tags/page.tsx
  - components/dashboard/tag-ranking-list.tsx
  - components/dashboard/tag-ranking-skeleton.tsx
autonomous: true
requirements: [TAG-05]

must_haves:
  truths:
    - "empty | Tag section (list-collection) — zero tags exist: dashed empty-state box with 'Nessun tag creato' + a link to /settings/tags"
    - "loading | Tag section list — a skeleton card grid renders under Suspense during the initial fetch, mirroring CategoryRankingSkeleton's shape"
    - "populated | Tag section at typical volume (5-15 tags) — grid gap-3 card list, sorted by absolute total descending, no pagination/cap"
    - "partial | a tag with a date range but zero matching transactions yet renders identically to the zero-total case — never partially rendered (name/badge/total/caption/archive action always present)"
    - "zero-one-many | Tag section (0/1/many tags) — 0 -> empty-state box; 1 -> single card, no layout awkwardness; many -> same grid, no cap"
    - "overflow | Tag-section card title (long tag name) — truncate + title= attribute, identical to CategoryRankingList's contract"
    - {statement: "Tag-section per-tag caption with a very wide date range (spans years) fits on one line without an explicit line-clamp", verification: backstop}
    - {statement: "getTags(userId)/getTagTotals(userId) fetch failure surfaces the 'Non è stato possibile caricare i tag. Ricarica la pagina.' copy in the same dashed-box shape as the empty state", verification: backstop}
    - "Each Tag-section card is clickable and navigates to /transactions?tag={tagId} (LOCKED — in scope for this phase, not deferred)"
    - "The Archivia action on each card reuses ArchiveTagDialog verbatim (Phase 67) — no new archive dialog/service code"
  artifacts:
    - app/(app)/dashboard/tags/page.tsx
    - components/dashboard/tag-ranking-list.tsx
    - components/dashboard/tag-ranking-skeleton.tsx
  key_links:
    - "app/(app)/dashboard/tags/page.tsx -> getTagTotals(userId) (Plan 68-04) -> TagRankingList"
    - "TagRankingList card -> Link href=/transactions?tag={tagId} -> tagScopedTransactions (Plan 68-01)"
    - "TagRankingList card action area -> ArchiveTagDialog (Phase 67, unchanged) -> archiveTagAction (Plan 68-04's second revalidatePath)"
---

<objective>
Build the TAG-05 Tag section: a new `/dashboard/tags` RSC page rendering every one of the user's
tags with its own independent, all-time, dashboard-exclusion-aware total (from Plan 68-04's
`getTagTotals`), an "Archiviato" badge for archived tags, and a click-through to
`/transactions?tag={tagId}` — reusing `ArchiveTagDialog` from Phase 67 verbatim.

Purpose: This is the entire TAG-05 requirement. The Tag section deliberately does NOT subscribe
to the dashboard's `preset`/`year`/`?tag=` filter (LOCKED DECISION 1 — its per-tag total is
independent and all-time), so this page needs none of the DAL threading from Plans 68-02/03.

Output: A working `/dashboard/tags` page reachable from the 3-tab nav (Plan 68-05), rendering the
full 0/1/many-tags, empty/loading/error, and long-tag-name states per the UI-SPEC.
</objective>

## Artifacts This Phase Produces

- `TagRankingList` — NEW component, `components/dashboard/tag-ranking-list.tsx` — `{ items: TagTotalItem[] }`, card grid + empty state + click-through + Archivia action.
- `TagRankingSkeleton` — NEW component, `components/dashboard/tag-ranking-skeleton.tsx` — loading-state card grid.
- `/dashboard/tags` page — NEW, `app/(app)/dashboard/tags/page.tsx` — the TAG-05 Tag section route.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-tags-dashboard-and-navigation/68-RESEARCH.md
@.planning/phases/68-tags-dashboard-and-navigation/68-PATTERNS.md
@.planning/phases/68-tags-dashboard-and-navigation/68-UI-SPEC.md
@lib/dal/tags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TagRankingList + TagRankingSkeleton</name>
  <files>components/dashboard/tag-ranking-list.tsx (NEW), components/dashboard/tag-ranking-skeleton.tsx (NEW)</files>
  <read_first>
    components/dashboard/category-ranking-list.tsx (full file, 168 lines) — the card shell (lines
    106-109), title-link pattern (lines 117-124, truncate + title= + focus-visible ring), the
    amount formatter (lines 55-63), list container (`<ol className="grid gap-3">`, line 95),
    empty-state shape (lines 81-92, adapted per UI-SPEC to the dashed-box + "Nessun tag creato"
    copy, not this file's own `min-h-[260px]` copy).
    components/dashboard/category-ranking-skeleton.tsx (full file, 31 lines) — the skeleton card
    shape to mirror for `TagRankingSkeleton` (drop the percentage-bar skeleton row and the
    sparkline-sized box, since TAG-05 cards have neither).
    components/tags/tag-mutation-dialogs.tsx lines 304-342 (`ArchiveTagDialog({ tag: TagRow })` —
    reuse verbatim, do not rebuild).
    components/dashboard/overview/overview-movers-panel.tsx line 158-ish (the sign-based tone
    rule: `Number(m.delta) >= 0 ? 'text-[var(--total-in)]' : 'text-[var(--total-out)]'` — the
    correct amount-color rule for a tag total, since a tag has no fixed direction, unlike
    `category-ranking-list.tsx`'s fixed `type === 'in' ? ... : ...` bar-color rule).
    lib/dal/tags.ts `TagTotalItem` (Plan 68-04) — `{ tagId, name, archived, count, minDate,
    maxDate, total }`.
    68-UI-SPEC.md "Design Contract Details > 2. Tag section (TAG-05)" — full card content order
    and the locked caption format `"{count} movimenti · {minDate}–{maxDate}"`.
  </read_first>
  <action>
    Create `components/dashboard/tag-ranking-list.tsx` as a `'use client'` component (needed for
    the `Link` + `ArchiveTagDialog`'s client-side dialog state) accepting `{ items: TagTotalItem[]
    }`. If `items.length === 0`, render the dashed empty-state box
    (`rounded-xl border border-dashed py-12`, matching `CategoryDetailEmptyState`'s shape, not
    `CategoryRankingList`'s `min-h-[260px]` variant) with heading "Nessun tag creato" and body
    "Crea un tag dalle impostazioni per raggruppare le transazioni di un viaggio, evento o
    progetto." plus an inline `<Link href="/settings/tags">Vai a Impostazioni → Tag</Link>".
    Otherwise render `<ol className="grid gap-3" aria-label="Classifica tag">`, one `<li
    className="group rounded-xl border bg-card p-4 shadow-sm transition-colors
    hover:border-primary/50">` per item, sorted as already sorted by `getTagTotals` (no re-sort
    needed — do not re-derive order here). Inside each card: (1) a `<Link href={\`/transactions?tag=${item.tagId}\`}
    className="block truncate text-sm font-semibold ... hover:underline focus-visible:ring-2 ..."
    title={item.name} aria-label={\`${item.name}: apri transazioni\`}>{item.name}</Link>` — the
    click-through target (LOCKED, in scope); (2) inline `Badge variant="secondary"
    className="text-[10px]"` "Archiviato" immediately after the name, only if `item.archived`; (3)
    the total, `font-mono text-sm font-semibold tabular-nums`, colored via the sign-based tone rule
    (never the fixed in/out bar-color rule); (4) the caption `text-xs text-muted-foreground`
    formatted as `"{count} movimenti · {minDate}–{maxDate}"` (singular "movimento" for count === 1,
    matching `movementLabel`'s existing pluralization idiom; when `minDate`/`maxDate` are both
    null, render just the count with no date range, e.g. "0 movimenti"); (5) an
    `<ArchiveTagDialog tag={...} />` in the card's action area — this needs a `TagRow`-shaped
    object, so pass `{ id: item.tagId, name: item.name, archived: item.archived, ... }` — check
    `ArchiveTagDialog`'s actual prop usage (it only reads `tag.id` and `tag.name` internally) and
    construct the minimal shape it needs rather than fetching a second, separate `TagRow[]` just
    for this. Format `total` via the existing `Intl.NumberFormat('it-IT', { style: 'currency',
    currency: 'EUR' })` idiom (copy `formatAmount` from `category-ranking-list.tsx` verbatim).
    Create `components/dashboard/tag-ranking-skeleton.tsx` mirroring
    `CategoryRankingSkeleton`'s pulse-box card shape, minus the percentage-bar skeleton row.
  </action>
  <acceptance_criteria>
    Zero items renders the dashed empty-state box with the exact locked copy and a working
    `/settings/tags` link. One item renders a single card with no layout awkwardness. A card with
    a long tag name truncates with `title=` set to the full name. An archived tag's card shows the
    "Archiviato" badge and its Archivia button is still present/enabled (never hidden/disabled).
    Total color follows the sign of the value, not a fixed direction. Clicking the tag-name Link
    navigates to `/transactions?tag={tagId}`.
  </acceptance_criteria>
  <verify>
    <automated>yarn vitest run tests/tag-ranking-list.test.tsx</automated>
  </verify>
  <done>Both new components render every UI-SPEC-required state (empty/populated/archived/long-name/zero-total) without hiding or disabling any element.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the /dashboard/tags page</name>
  <files>app/(app)/dashboard/tags/page.tsx (NEW)</files>
  <read_first>
    app/(app)/dashboard/categories/page.tsx (full file, 145 lines) — the closest role-match analog:
    `Suspense` + async content component split, page-level `<h1>`/description block shape (lines
    120-126).
    app/(app)/settings/tags/page.tsx (full file, 23 lines) — the `verifySession()` + `getTags(userId)`
    call-site precedent for a tags-adjacent RSC page.
    lib/dal/tags.ts — `getTagTotals(userId)` (Plan 68-04).
  </read_first>
  <action>
    Create `app/(app)/dashboard/tags/page.tsx` as an async Server Component. Call
    `verifySession()` to get `userId`, then `getTagTotals(userId)` inside an async content
    component wrapped in `<Suspense fallback={<TagRankingSkeleton />}>`, mirroring
    `DashboardCategoriesPage`'s `CategoryRankingContent`/`Suspense` split. Render a page header
    (`<h1 className="text-xl font-semibold">Tag</h1>` + a one-line description, matching the
    Categorie page's header block shape) followed by the Suspense-wrapped `<TagRankingList items=
    {...} />`. This page intentionally has NO `DashboardFilters`/`TagFilterSelect` and reads no
    `searchParams` — TAG-05's per-tag total is all-time and does not subscribe to
    `preset`/`year`/`?tag=` (LOCKED DECISION 1). If `getTagTotals` throws, the async content
    component's error boundary (or a try/catch returning the error-state markup) surfaces "Non è
    stato possibile caricare i tag. Ricarica la pagina." in the same dashed-box shape as the empty
    state, distinguished only by heading text (backstop item — no dedicated automated
    fetch-failure test exists in this repo per RESEARCH.md; this is a verify-time-only check).
  </action>
  <acceptance_criteria>
    Navigating to `/dashboard/tags` renders the page with the current user's tags, each showing
    its all-time total. The page does not read or forward `preset`, `year`, or `tag` search
    params. The 3-tab nav (Plan 68-05) correctly highlights "Tag" as the active tab on this route.
  </acceptance_criteria>
  <verify>
    <automated>yarn vitest run tests/tag-ranking-list.test.tsx</automated>
  </verify>
  <done>/dashboard/tags is a working, reachable page rendering TAG-05's independent per-tag totals with archive action, verified against real getTagTotals data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `/dashboard/tags` page | No user-controlled tag-identifying URL param — renders only the authenticated user's own tags via `getTagTotals(userId)` |
| Card click-through `/transactions?tag={tagId}` | `tagId` here is a value THIS page itself rendered (the user's own tag), not attacker-controlled input at the point of construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Information Disclosure | `/dashboard/tags` page | low | accept | No user-controlled tag-identifying param exists on this route — `getTagTotals(userId)` (Plan 68-04) is scoped by the authenticated session's own `userId` only, identical in shape to the pre-existing `/settings/tags` page. The card's outbound `/transactions?tag={tagId}` link is still defended on the RECEIVING end (Plan 68-01's `tagScopedTransactions` EXISTS predicate, scoped by `transaction.userId`), even though the tagId here always legitimately belongs to the viewing user. |

</threat_model>

<verification>
`yarn vitest run tests/tag-ranking-list.test.tsx` passes, covering the new components and page-level rendering across empty/populated/archived states.
</verification>

<success_criteria>
`/dashboard/tags` renders every one of the user's tags (active and archived) with an independent all-time total, archive action, and click-through to the filtered transactions list — matching every UI-SPEC state requirement.
</success_criteria>

<output>
Create `.planning/phases/68-tags-dashboard-and-navigation/68-08-SUMMARY.md` when done
</output>
