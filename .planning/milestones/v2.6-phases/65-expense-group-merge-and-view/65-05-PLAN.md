---
phase: 65
plan: 5
type: execute
wave: 3
depends_on: ["65-02", "65-03"]
files_modified:
  - app/(app)/expenses/groups/[groupId]/page.tsx
  - components/expenses/group-detail-client.tsx
  - components/expenses/group-title-edit.tsx
  - tests/group-detail-page.test.tsx
autonomous: true
requirements: [GRP-04]

must_haves:
  truths:
    - "A groupId that does not exist, or exists but is owned by a different user, resolves to notFound() — getExpenseGroupForDetail's undefined result is the sole ownership signal the page trusts (mirrors DET-06/DET-07's precedent on /expenses/[id])."
    - "The group detail page shows the shared subcategory as read-only display, with no 'cambia categoria' control — member-level or group-level recategorization is out of scope for this phase (D-03; GRP-05 group-recategorize lands in Phase 66)."
    - "Each member expense is listed with its OWN original title and OWN total (not the group's composed total), and links to that member's own /expenses/[id] page (GRP-04)."
    - "A member with zero linked transactions renders normally in the member list with a '0,00 €' total — no crash, no special-casing (GRP-04 empty edge)."
    - "The combined transaction list spans every member's transactions, sorted occurredAt DESC, independent of which member each transaction belongs to (GRP-04 ordering edge)."
  artifacts:
    - app/(app)/expenses/groups/[groupId]/page.tsx
    - components/expenses/group-detail-client.tsx
    - components/expenses/group-title-edit.tsx
  key_links:
    - "page.tsx -> getExpenseGroupForDetail({ userId, groupId }) -> notFound() on undefined is the entire ownership boundary for this route; group-detail-client.tsx never re-checks ownership."
  prohibitions: []
---

<objective>
Build the group detail page: `/expenses/groups/[groupId]`, the single place GRP-04 requires for
viewing a group's shared subcategory, its member expenses with their own titles/totals, the full
combined transaction list, and the rename control.

Purpose: this is the canonical landing page every "Unita" row/badge and cross-reference link in
this phase points to. It consumes `getExpenseGroupForDetail` (Plan 65-03) and
`renameExpenseGroupAction` (Plan 65-02).
Output: a new RSC page, its client component, and a small inline rename control.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-expense-group-merge-and-view/65-CONTEXT.md
@app/(app)/expenses/[id]/page.tsx
@components/expenses/expense-detail-client.tsx
@components/expenses/expense-title-edit.tsx
@components/detail-pages/detail-page-shell.tsx
@lib/routes.ts
@lib/dal/expenses.ts
@lib/actions/expenses.ts
@tests/expense-detail-page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the /expenses/groups/[groupId] RSC page</name>
  <files>app/(app)/expenses/groups/[groupId]/page.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    app/(app)/expenses/[id]/page.tsx — the exact RSC page shape to mirror: await params,
    verifySession, DAL fetch, notFound() on missing.
    lib/dal/expenses.ts — `getExpenseGroupForDetail({ userId, groupId })` signature (Plan 65-03,
    `groupId: number`).
    tests/expense-detail-page.test.tsx — the `pageMocks`/`vi.mock('@/lib/dal/...')`/`renderExpensePage`
    helper pattern for RSC page tests (mock `next/navigation`'s `notFound`, mock the DAL call,
    assert `notFound()` throws and is caught by the test's `rejects.toThrow`).
  </read_first>
  <action>
    Create `app/(app)/expenses/groups/[groupId]/page.tsx` as an async RSC page with `params:
    Promise&lt;{ groupId: string }&gt;`. Parse `groupId` with `Number(rawGroupId)`; if not a
    positive safe integer, call `notFound()` immediately without querying the DAL (mirrors the
    existing `parseCategoryId` guard pattern in `app/(app)/dashboard/categories/[id]/page.tsx`).
    Otherwise call `verifySession()` then `getExpenseGroupForDetail({ userId, groupId })`; call
    `notFound()` if the result is `undefined`; otherwise render
    `&lt;GroupDetailClient group={group} /&gt;` (component built in Task 2).
  </action>
  <acceptance_criteria>
    app/(app)/expenses/groups/[groupId]/page.tsx exists and default-exports an async function
    taking `{ params: Promise&lt;{ groupId: string }&gt; }`.
    A non-numeric `groupId` route param calls `notFound()` without calling
    `getExpenseGroupForDetail`.
    `yarn test -- tests/group-detail-page.test.tsx` exits 0, including: happy-path render, a
    missing groupId, a non-owned groupId (DAL returns undefined), and a malformed (non-numeric)
    groupId — all three resolving to `notFound()`.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/group-detail-page.test.tsx</automated>
  </verify>
  <done>The route exists, is ownership-scoped entirely through getExpenseGroupForDetail's
  undefined result, and rejects malformed ids before touching the database.</done>
</task>

<task type="auto">
  <name>Task 2: Build GroupDetailClient (subcategory, members, combined transactions)</name>
  <files>components/expenses/group-detail-client.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    components/expenses/expense-detail-client.tsx — full file: `DetailPageShell` slot usage
    (`datiCard`, `categoriaCard`, `collegamentiCard`, `riepilogoCard`, `transactionsCard`,
    `bottomCardsSideBySide`), the transaction-list-preview rendering (`LINKED_TRANSACTIONS_PREVIEW_LIMIT`,
    `formatDate`, `formatTransactionAmount`), and `amountToneClass` usage.
    components/detail-pages/detail-page-shell.tsx — available card slots; this task deliberately
    repurposes the `collegamentiCard` slot for the member list (each member IS a cross-reference
    to its own expense detail page), and passes no `azioniCard` (no delete/dissolve action exists
    in this phase).
    lib/routes.ts — `expenseDetailHref`, `transactionDetailHref`.
  </read_first>
  <action>
    Create `components/expenses/group-detail-client.tsx` accepting `{ group: ExpenseGroupDetailRow
    }` (type from `@/lib/dal/expenses`). Render `DetailPageShell` with `backHref={APP_ROUTES.expenses}`,
    `title={group.title}`, `amount={formatSignedAmount(group.totalAmount)}` `amountInline`,
    `layout="two-column"`, `bottomCardsSideBySide`. `datiCard`: a "Titolo" field using
    `GroupTitleEdit` (Task 3) plus a read-only "Categoria" block showing
    `group.subCategoryName`/`group.categoryName` with NO "cambia categoria" button (D-03 — group
    recategorization is Phase 66 scope; this page only displays the shared category). `categoriaCard`:
    omit or fold into datiCard — keep the category display in `datiCard` since there is no
    interactive picker to warrant a separate card this phase. `collegamentiCard` (repurposed as
    "Membri"): one row per `group.members[]` entry showing the member's own `title` and own
    `totalAmount` (via `toDecimal`/formatting, matching `formatSignedAmount`'s pattern), each
    linking to `expenseDetailHref(member.id)`; a member with `transactionCount: 0` still renders
    its `'0,00 €'` total normally. `riepilogoCard`: `group.totalAmount`, `group.transactionCount`,
    `group.createdAt`. `transactionsCard`: `group.transactions` (already sorted occurredAt DESC by
    the DAL), previewed/linked exactly like `expense-detail-client.tsx`'s transactions table
    (`transactionDetailHref(tx.id)`, `formatTransactionAmount`, `formatDate`).
  </action>
  <acceptance_criteria>
    components/expenses/group-detail-client.tsx exports `GroupDetailClient`.
    The rendered `datiCard`/category display contains no button or control labeled "Cambia
    categoria" anywhere on this page.
    Each entry in `group.members` renders its own title and own total (not `group.totalAmount`)
    and links to `expenseDetailHref(member.id)`.
    `yarn test -- tests/group-detail-page.test.tsx` exits 0, including: member list shows each
    member's own title/total and correct hrefs; a zero-transaction member renders without
    crashing; combined transactions render in the DAL-provided (occurredAt DESC) order; no
    "Cambia categoria" text anywhere in the output.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/group-detail-page.test.tsx</automated>
  </verify>
  <done>GroupDetailClient renders the shared category read-only, each member with its own
  title/total linking to its own detail page, and the full combined transaction list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create GroupTitleEdit inline rename control</name>
  <files>components/expenses/group-title-edit.tsx, tests/group-detail-page.test.tsx</files>
  <read_first>
    components/expenses/expense-title-edit.tsx — the exact pencil-inline-edit pattern to mirror:
    `useActionState`, edit/view toggle, submit/cancel buttons, min-length gate.
    lib/actions/expenses.ts — `renameExpenseGroupAction` signature (Plan 65-02).
  </read_first>
  <behavior>
    - Not editing: renders `title` as plain text (no outer `Link` — this control lives ON the
      group's own detail page, so no self-link is needed, unlike `ExpenseTitleEdit`) plus a pencil
      button that enters edit mode.
    - Editing: renders a text input pre-filled with `title`, a hidden `groupId` field, Salva/Annulla
      buttons; Salva is disabled below 2 trimmed characters or while pending.
    - On successful submit (`state.error === null` after a submission), calls `onSuccess(newTitle)`
      and exits edit mode; on error, shows `state.error` and stays in edit mode.
  </behavior>
  <action>
    Create `components/expenses/group-title-edit.tsx` as a `'use client'` component with props
    `{ groupId: number; title: string; onSuccess?: (newTitle: string) => void }`, using
    `useActionState(renameExpenseGroupAction, { error: null })` and the same
    submittedRef/pendingValueRef pattern as `ExpenseTitleEdit`, with a hidden `<input type="hidden"
    name="groupId" value={groupId} />` instead of an `id` field. Wire it into
    `group-detail-client.tsx`'s `datiCard` "Titolo" field from Task 2.
  </action>
  <acceptance_criteria>
    components/expenses/group-title-edit.tsx exports `GroupTitleEdit`.
    The rendered edit form contains `name="groupId"` and `name="title"` hidden/visible inputs, and
    no `name="id"` field.
    `yarn test -- tests/group-detail-page.test.tsx` exits 0, including a case asserting the Salva
    button is disabled for a value shorter than 2 trimmed characters.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/group-detail-page.test.tsx</automated>
  </verify>
  <done>GroupTitleEdit lets the user rename the group inline via renameExpenseGroupAction,
  matching ExpenseTitleEdit's UX exactly minus the self-link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `/expenses/groups/[groupId]` route param → getExpenseGroupForDetail | Untrusted route param; the page must never render group data without the DAL's ownership-scoped confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-65-10 | Information Disclosure | app/(app)/expenses/groups/[groupId]/page.tsx | high | mitigate | `notFound()` fires for any `groupId` that `getExpenseGroupForDetail` cannot resolve for the current `userId` — malformed, missing, and non-owned ids are indistinguishable to the requester. |
| T-65-11 | Tampering | GroupTitleEdit → renameExpenseGroupAction | low | mitigate | The action re-verifies ownership server-side (Plan 65-02); this page never trusts client state for the write, only for the optimistic UI toggle. |
</threat_model>

<verification>
`yarn test -- tests/group-detail-page.test.tsx` passes in full.
</verification>

<success_criteria>
- `/expenses/groups/[groupId]` exists, ownership-scoped via notFound(), rejects malformed ids.
- GroupDetailClient shows read-only shared category, per-member own title/total with links, and
  the combined transaction list.
- GroupTitleEdit renames the group inline via renameExpenseGroupAction.
</success_criteria>

<output>
Create `.planning/phases/65-expense-group-merge-and-view/65-05-SUMMARY.md` when done
</output>
