---
phase: 65
plan: 6
type: execute
wave: 3
depends_on: ["65-02", "65-03"]
files_modified:
  - components/transactions/transaction-table.tsx
  - components/transactions/transaction-detail-client.tsx
  - components/expenses/expense-detail-client.tsx
  - tests/transaction-table-menu.test.tsx
  - tests/transaction-detail-page.test.tsx
  - tests/expense-detail-page.test.tsx
autonomous: true
requirements: [GRP-08]

must_haves:
  truths:
    - "A transaction whose linked expense is grouped displays the group's title in place of the expense's own title, at every precedence position: customTitle wins if set, then groupTitle, then expenseTitle, then the raw bank description (GRP-08 adjacency edge — customTitle and groupTitle can both be present)."
    - "A transaction whose linked expense is NOT grouped renders through the exact same customTitle -> expenseTitle -> description chain as before this phase — the groupTitle branch is a no-op for the non-grouped majority (GRP-08 empty edge)."
    - "A grouped member expense's own detail page (/expenses/[id]) shows a 'Parte di: {groupTitle}' cross-reference linking to the group detail page, and hides its own 'Cambia categoria'/'Assegna categoria' control (D-03 — recategorization while grouped is not offered here)."
  artifacts:
    - components/transactions/transaction-table.tsx (title precedence updated)
    - components/transactions/transaction-detail-client.tsx (title precedence + cross-ref label updated)
    - components/expenses/expense-detail-client.tsx (group cross-ref + hidden category control)
  key_links:
    - "This plan reads groupId/groupTitle fields added in Plan 65-03 (transactionListSelect, getTransactionForDetail, getExpenseForDetail) — it changes zero DAL code, display only."
  prohibitions:
    - statement: "Member-level recategorization must never be offered while an expense is grouped — the 'Cambia categoria'/'Assegna categoria' control must not render on a grouped expense's own detail page"
      status: unverified
      flagged: true
---

<objective>
Finish GRP-08: transaction rows (list + detail) display a grouped member's group title ahead of
its own expense title, and a grouped member's own expense detail page declares its group
membership while hiding the recategorize control that D-03 says should not be offered there.

Purpose: this is the last cross-reference surface the phase touches — purely presentational,
reading the `groupId`/`groupTitle` fields Plan 65-03 already added to the DAL layer. No DAL code
changes in this plan.
Output: title-precedence updates in two transaction display components, and a cross-reference +
hidden-control update in the expense detail client.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-expense-group-merge-and-view/65-CONTEXT.md
@components/transactions/transaction-table.tsx
@components/transactions/transaction-detail-client.tsx
@components/transactions/transaction-title-edit.tsx
@components/expenses/expense-detail-client.tsx
@lib/dal/transactions.ts
@lib/dal/expenses.ts
@lib/routes.ts
@tests/transaction-table-menu.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transaction list title precedence includes groupTitle</name>
  <files>components/transactions/transaction-table.tsx, tests/transaction-table-menu.test.tsx</files>
  <read_first>
    components/transactions/transaction-table.tsx — `transactionRowLabel()` (customTitle ->
    expenseTitle -> description), the `TransactionTitleEdit` call site passing
    `fallbackTitle={transaction.expenseTitle}`.
    components/transactions/transaction-title-edit.tsx — confirms `fallbackTitle` is the single
    prop this component uses between `customTitle` and `description` (no internal change needed
    there — only the value the caller passes changes).
    tests/transaction-table-menu.test.tsx — `makeTransaction()` factory; it will need the new
    `TransactionListRow` fields (`groupId`, `groupTitle`, added in Plan 65-03) defaulted to `null`.
  </read_first>
  <behavior>
    - `transactionRowLabel(transaction)` returns, in order: `customTitle` (trimmed) if non-empty,
      else `groupTitle` (trimmed) if non-empty, else `expenseTitle` (trimmed) if non-empty, else
      `description`.
    - The `fallbackTitle` prop passed to `TransactionTitleEdit` becomes `transaction.groupTitle ??
      transaction.expenseTitle` (was `transaction.expenseTitle` alone).
    - A transaction with `groupTitle: null` (the non-grouped majority) produces byte-identical
      output to before this task.
  </behavior>
  <action>
    In components/transactions/transaction-table.tsx, change `transactionRowLabel` to check
    `transaction.groupTitle?.trim()` between the existing `customTitle` and `expenseTitle` checks.
    Change the `fallbackTitle={transaction.expenseTitle}` prop passed to `TransactionTitleEdit` to
    `fallbackTitle={transaction.groupTitle ?? transaction.expenseTitle}`. Leave every other
    `transaction.expenseTitle` reference in this file untouched — the categorize-dialog-prefill
    call sites (`setCategorizeTarget({ ... title: transaction.expenseTitle ?? rowLabel })`) only
    fire for uncategorized expenses, which can never be grouped (a group requires a shared
    non-null subCategoryId), so they are correctly out of this task's scope. Update
    `tests/transaction-table-menu.test.tsx`'s `makeTransaction()` factory defaults to include
    `groupId: null, groupTitle: null`, and add a new test asserting a transaction with
    `groupTitle: 'Cherasco'` and `expenseTitle: 'Cherasco 57 SRL'` renders `'Cherasco'` (group wins
    over expense title) while a transaction with `customTitle` set still shows the customTitle
    over both.
  </action>
  <acceptance_criteria>
    `transactionRowLabel` in components/transactions/transaction-table.tsx checks `groupTitle`
    between `customTitle` and `expenseTitle`.
    The `TransactionTitleEdit` call site's `fallbackTitle` prop reads
    `transaction.groupTitle ?? transaction.expenseTitle`.
    `yarn test -- tests/transaction-table-menu.test.tsx` exits 0, including all pre-existing
    (unmodified) assertions plus the new precedence cases.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/transaction-table-menu.test.tsx</automated>
  </verify>
  <done>Grouped transactions display the group title ahead of the expense title everywhere in the
  transactions table; ungrouped transactions are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction detail page title precedence + "Spesa collegata" label</name>
  <files>components/transactions/transaction-detail-client.tsx, tests/transaction-detail-page.test.tsx</files>
  <read_first>
    components/transactions/transaction-detail-client.tsx — the `displayTitle` computation
    (`customTitle ?? expenseTitle ?? description`), the `fallbackTitle` prop passed to
    `TransactionTitleEdit`, and the "Spesa collegata" cross-reference block
    (`transaction.expenseTitle ?? 'Vedi spesa'`, linking to `expenseDetailHref(transaction.expenseId)`).
  </read_first>
  <action>
    Update `displayTitle` to `transaction.customTitle ?? transaction.groupTitle ??
    transaction.expenseTitle ?? transaction.description`. Update the `fallbackTitle` prop passed
    to `TransactionTitleEdit` to `transaction.groupTitle ?? transaction.expenseTitle`. Update the
    "Spesa collegata" label to `transaction.groupTitle ?? transaction.expenseTitle ?? 'Vedi
    spesa'` — the link target stays `expenseDetailHref(transaction.expenseId)` unchanged (the
    cross-reference chain is transaction -> member expense's own detail page -> that page's own
    "Parte di" link to the group, built in Task 3; this page never links directly to the group
    route). Extend tests/transaction-detail-page.test.tsx's transaction-detail-row factory with
    `groupId: null, groupTitle: null` defaults and add a case asserting a grouped transaction's
    detail page shows the group title in both the header and the "Spesa collegata" block while
    still linking to the member expense's own detail href.
  </action>
  <acceptance_criteria>
    `displayTitle` and the `fallbackTitle` prop in transaction-detail-client.tsx both prefer
    `groupTitle` over `expenseTitle`.
    The "Spesa collegata" label prefers `groupTitle` over `expenseTitle`; its link target is
    unchanged (`expenseDetailHref`, not a group route).
    `yarn test -- tests/transaction-detail-page.test.tsx` exits 0, including all pre-existing
    assertions plus the new grouped-transaction case.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/transaction-detail-page.test.tsx</automated>
  </verify>
  <done>The transaction detail page shows the group title ahead of the expense title everywhere,
  while its "Spesa collegata" link still points at the member's own expense detail page.</done>
</task>

<task type="auto">
  <name>Task 3: Expense detail page — "Parte di" cross-ref + hidden category control when grouped</name>
  <files>components/expenses/expense-detail-client.tsx, tests/expense-detail-page.test.tsx</files>
  <read_first>
    components/expenses/expense-detail-client.tsx — `collegamentiCard` (sourceFile/platformName
    cross-refs) and `categoriaSection` (the "Cambia categoria"/"Assegna categoria" button).
    lib/dal/expenses.ts — `ExpenseDetailRow`'s `groupId`/`groupTitle` fields (Plan 65-03).
    lib/routes.ts — `expenseGroupDetailHref` (Plan 65-03).
    tests/expense-detail-page.test.tsx — `makeExpenseDetailRow()` factory and the existing
    "Cambia categoria"/"Assegna categoria" assertions this task must not break for an ungrouped
    expense.
  </read_first>
  <action>
    In `collegamentiCard`, when `expense.groupId` is non-null, add a "Parte di" row (matching the
    existing sourceFile/platformName row style) showing `expense.groupTitle` as a `Link` to
    `expenseGroupDetailHref(expense.groupId)`. In `categoriaSection`, when `expense.groupId` is
    non-null, omit the "Cambia categoria"/"Assegna categoria" `Button` entirely (the category name
    display itself stays — only the edit control is hidden), per D-03. Update
    `tests/expense-detail-page.test.tsx`'s `makeExpenseDetailRow()` factory defaults to include
    `groupId: null, groupTitle: null`, and add a new case with `groupId`/`groupTitle` set asserting
    the page shows a "Parte di" link to the group route and does NOT show "Cambia categoria".
  </action>
  <acceptance_criteria>
    components/expenses/expense-detail-client.tsx renders a "Parte di" cross-reference linking to
    `expenseGroupDetailHref(expense.groupId)` when `expense.groupId` is set.
    "Cambia categoria"/"Assegna categoria" never renders when `expense.groupId` is set; both still
    render for an ungrouped expense (pre-existing assertions in tests/expense-detail-page.test.tsx
    keep passing unmodified).
    `yarn test -- tests/expense-detail-page.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>yarn test -- tests/expense-detail-page.test.tsx</automated>
  </verify>
  <done>A grouped member's own detail page declares its group membership and hides its
  recategorize control; an ungrouped expense's page is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is presentational only — it reads fields already ownership-scoped by the DAL functions built in Plan 65-03 and introduces no new query or write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-65-12 | Tampering | expense-detail-client.tsx (hidden category control) | low | accept | Hiding the "Cambia categoria" button is a UI affordance only; the actual server-side block against recategorizing a grouped member was already added to `categorizeExpense` in Plan 65-02 — this plan adds no new authorization logic, just matching UI. |
</threat_model>

<verification>
`yarn test -- tests/transaction-table-menu.test.tsx tests/transaction-detail-page.test.tsx tests/expense-detail-page.test.tsx` passes in full, including every pre-existing assertion.
</verification>

<success_criteria>
- Transaction list and detail pages show groupTitle ahead of expenseTitle for grouped members;
  zero change for ungrouped transactions.
- A grouped member's own expense detail page shows "Parte di: {groupTitle}" and hides its
  recategorize control.
</success_criteria>

<output>
Create `.planning/phases/65-expense-group-merge-and-view/65-06-SUMMARY.md` when done
</output>
