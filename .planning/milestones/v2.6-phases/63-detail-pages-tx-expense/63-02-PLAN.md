---
phase: 63-detail-pages-tx-expense
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - app/(app)/transactions/[id]/page.tsx
  - components/transactions/transaction-detail-client.tsx
  - components/transactions/transaction-amount-edit.tsx
  - components/transactions/transaction-date-edit.tsx
  - tests/transaction-detail-page.test.tsx
autonomous: true
requirements: [DET-05]

must_haves:
  truths:
    - "Visiting /transactions/[id] for an owned transaction shows amount, date, title, description (locked), category, and cross-refs (linked expense, source file or Manuale)"
    - "Visiting /transactions/[id] for a non-owned or non-existent id returns a 404 (notFound())"
    - "Editing amount on a paired transaction that would break the opposite-sign invariant shows 'Scollega prima il rimborso' inline and the field stays in edit mode with the attempted value"
    - "Editing amount or date successfully triggers router.refresh() with no toast/notice"
    - "The description field and any hash values are rendered as readonly text — no input element exists for them"
  artifacts:
    - app/(app)/transactions/[id]/page.tsx
    - components/transactions/transaction-detail-client.tsx
    - components/transactions/transaction-amount-edit.tsx
    - components/transactions/transaction-date-edit.tsx
  key_links:
    - "TransactionDetailClient invokes updateTransactionAction (lib/actions/transaction-edit.ts, Phase 62) for amount/date/title edits — no new write path is introduced"
    - "Category editing goes through updateExpense (lib/actions/expenses.ts) against transaction.expenseId, not a transaction-specific category action — per D-12"
    - "CounterpartPickerDialog, DetachExpenseDialog, and the existing DeleteTransactionMenuItem-equivalent delete confirmation are invoked unmodified from the client wrapper — per D-10"
---

<objective>
Build `/transactions/[id]` — the single place to view and edit everything editable
about a transaction, wired against Phase 62's `updateTransaction`/`updateExpense`
actions and reusing every existing dialog unchanged (D-10).

Purpose: deliver DET-05 exactly as locked in CONTEXT.md — pencil-inline edit for
amount/date/title, category via `SubcategoryPicker` routed through the linked expense,
immutable fields visibly readonly, and the full action set (cerca su internet,
collega/scollega rimborso, spesa a sé, elimina) plus cross-refs.

Output: a working authenticated route page, a client wrapper orchestrating the
existing dialogs, and two new inline-edit components for amount and date.
</objective>

## Artifacts this phase produces

- `app/(app)/transactions/[id]/page.tsx` — new file, `export default async function TransactionDetailPage(...)`
- `components/transactions/transaction-detail-client.tsx` — new file, `export function TransactionDetailClient(...)`
- `components/transactions/transaction-amount-edit.tsx` — new file, `export function TransactionAmountEdit(...)`
- `components/transactions/transaction-date-edit.tsx` — new file, `export function TransactionDateEdit(...)`
- `tests/transaction-detail-page.test.tsx` — new test file
- Consumes (not produced here): `getTransactionForDetail`, `DetailPageShell` (63-01); `updateTransactionAction` (Phase 62)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-detail-pages-tx-expense/63-CONTEXT.md
@.planning/phases/62-transaction-edit-core/62-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Amount and date inline-edit components</name>
  <files>components/transactions/transaction-amount-edit.tsx, components/transactions/transaction-date-edit.tsx, tests/transaction-detail-page.test.tsx</files>
  <read_first>
    components/transactions/transaction-title-edit.tsx (the exact pencil/Enter/Esc/useActionState pattern to replicate — D-05)
    lib/actions/transaction-edit.ts (updateTransactionAction signature: reads `id`, `amount`, `occurredAt`, `customTitle` from FormData; returns `{ error: string | null }`; surfaces the service's Italian messages verbatim)
    lib/validations/transaction-edit.ts (UpdateTransactionSchema — amount accepts comma or dot decimal separator, occurredAt is an ISO date string)
  </read_first>
  <behavior>
    - `TransactionAmountEdit` displays the formatted signed amount (not absolute value —
      D-06 says amount edit is a free signed input, and the transaction page shows the
      real sign, unlike the absolute-value table columns) with a pencil icon; clicking
      enters edit mode with a text input pre-filled with the Italian-formatted value
      (comma decimal separator, sign included, e.g. "-12,99")
    - Submitting a valid new amount calls `updateTransactionAction` with `id` and
      `amount` set, and on success (`state.error === null`) exits edit mode and calls
      the `onSuccess` prop (which the page uses to trigger `router.refresh()`)
    - Submitting an amount that breaks a pair invariant leaves edit mode open, shows
      "Scollega prima il rimborso" in red text below the input, and preserves the
      user's typed value in the input (D-07)
    - Escape key exits edit mode without submitting, reverting to the last-saved value
    - `TransactionDateEdit` follows the identical open/save/cancel/error shape but uses
      `<input type="date">` (native date input — Claude's Discretion, RESEARCH.md
      Open Question 1) and submits `occurredAt` as an ISO `YYYY-MM-DD` string
  </behavior>
  <action>
    Create `components/transactions/transaction-amount-edit.tsx` (`'use client'`)
    exporting `TransactionAmountEdit({ id, amount, currency, onSuccess }: { id: string;
    amount: string; currency: string; onSuccess?: () => void })`. Copy the
    `useActionState(updateTransactionAction, { error: null })` + `submittedRef` +
    `pendingValueRef` + `useEffect` structure from `transaction-title-edit.tsx`
    verbatim in shape, swapping the field name to `amount` and the display formatter
    to a signed Italian amount (`new Intl.NumberFormat('it-IT', { style: 'currency',
    currency }).format(Number(toDecimal(amount)))` — import `toDecimal` from
    `@/lib/utils/decimal`; do not strip the sign). On submit, normalize the input
    value's comma to a dot before setting the hidden `amount` field the same way
    `updateTransactionAction` itself does (`replace(',', '.')` happens server-side in
    the action already — the component just passes the raw comma-formatted string
    through, matching how `UpdateTransactionSchema` validates it).

    Create `components/transactions/transaction-date-edit.tsx` (`'use client'`)
    exporting `TransactionDateEdit({ id, occurredAt, onSuccess }: { id: string;
    occurredAt: Date; onSuccess?: () => void })` with the same
    `useActionState(updateTransactionAction, ...)` wiring, field name `occurredAt`,
    input type `date`, value formatted as `occurredAt.toISOString().slice(0, 10)` for
    the input's initial value and display value formatted with the existing
    `it-IT` `Intl.DateTimeFormat` (day/month short/year) pattern used elsewhere in
    `transaction-table.tsx` when not in edit mode.

    Both components render the pair-guard/validation error text with
    `className="text-xs text-destructive"` exactly as `transaction-title-edit.tsx` does.
  </action>
  <verify>
    <automated>yarn vitest run tests/transaction-detail-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function TransactionAmountEdit" components/transactions/transaction-amount-edit.tsx` matches
    - `grep -n "export function TransactionDateEdit" components/transactions/transaction-date-edit.tsx` matches
    - `grep -n "updateTransactionAction" components/transactions/transaction-amount-edit.tsx components/transactions/transaction-date-edit.tsx` — both files import and call it (no new server action created)
    - `yarn vitest run tests/transaction-detail-page.test.tsx` includes and passes a case
      asserting that when the action returns `{ error: 'Scollega prima il rimborso' }`,
      the rendered output contains that exact string and the input remains present
      (still in edit mode)
    - `yarn tsc --noEmit` introduces no new errors attributable to these two files
  </acceptance_criteria>
  <done>Both inline-edit components exist, call the existing updateTransactionAction, and correctly surface the pair-guard error inline without exiting edit mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /transactions/[id] RSC page + client wrapper</name>
  <files>app/(app)/transactions/[id]/page.tsx, components/transactions/transaction-detail-client.tsx, tests/transaction-detail-page.test.tsx</files>
  <read_first>
    app/(app)/import/[fileId]/suggestions/page.tsx (ownership-gate RSC page pattern: await params, verifySession, DAL fetch, notFound() on undefined)
    components/transactions/transaction-table.tsx (lines 460-655: menu action wiring for Cerca su Google, Ricategorizza/Categorizza spesa, Collega/Scollega rimborso, Spesa a sé, DeleteTransactionMenuItem — the exact dialogs and handlers to lift unchanged)
    components/transactions/counterpart-picker-dialog.tsx (props: open, onOpenChange, transactionId, transactionAmount, transactionOccurredAt, onPaired)
    components/transactions/detach-expense-dialog.tsx (props: open, onOpenChange, transactionId, defaultTitle, categories, mostUsed, onSuccess)
    components/expenses/expense-categorize-dialog.tsx (props: open, onOpenChange, expense: {id, title}, categories, mostUsed, onSuccess)
    components/transactions/transaction-pair-popover.tsx (pairedWithId/netAmount/pairedDescription/pairedAmount/pairedOccurredAt display pattern for the paired-transaction cross-ref)
    lib/dal/transactions.ts (getTransactionForDetail from Plan 01 — TransactionDetailRow field list)
    lib/dal/categories.ts, lib/dal/subcategory-usage.ts (getCategories(), getMostUsedSubcategories(allowedDirections) — needed to open SubcategoryPicker/DetachExpenseDialog/CounterpartPickerDialog from this page)
    lib/routes.ts (transactionDetailHref, expenseDetailHref from Plan 01; APP_ROUTES.transactions for the delete-redirect and back link)
    components/detail-pages/detail-page-shell.tsx (Plan 01 — props contract)
  </read_first>
  <behavior>
    - GET `/transactions/{ownedId}` renders 200 with description (locked per D-03: lock
      icon + tooltip "chiave di riconciliazione bancaria — non modificabile", hashes
      never shown), amount, date,
      title, category (or amber "Categorizza" state text if the linked expense has no
      subCategoryId), cross-refs section showing the linked expense as a link to
      `/expenses/[expenseId]` and either the source file link (existing
      `${APP_ROUTES.import}?fileId=...` pattern from transaction-table.tsx) or a
      "Manuale" badge when `fileId` is null
    - GET `/transactions/{nonOwnedId}` and `/transactions/{missingId}` both call
      `notFound()` (Next.js 404), verified via the DAL returning `undefined`
    - Header shows "Cerca su internet" as a primary button + overflow menu for the rest
      (D-09): opens a Google search in a
      new tab using `customTitle ?? description`, same construction as the existing
      `Cerca su Google` menu item) and an overflow menu with: Collega rimborso /
      Scollega rimborso (toggled by `pairedWithId` presence, mirrors
      `transaction-pair-popover` semantics), Spesa a sé (only when `expenseId` is
      present, opens `DetachExpenseDialog`), and Elimina (destructive, opens the same
      confirmation-dialog pattern as `DeleteTransactionMenuItem` in
      `transaction-table.tsx`, redirecting to `APP_ROUTES.transactions` with a success
      toast on completion per D-11 — not just closing a dropdown)
    - Category card taps open `SubcategoryPicker`; selecting a subcategory calls
      `updateExpense` (via a form action, same call shape as
      `ExpenseCategorizeDialog`/`categorizeExpense`) against the transaction's
      `expenseId`, then `router.refresh()`; card includes the subtitle "La categoria è
      assegnata alla spesa aggregata. Modificarla qui modifica la spesa per tutte le
      transazioni collegate." when the transaction's expense holds more than 1
      transaction (`expenseTransactionCount > 1`)
    - If `pairedWithId` is present, the Collegamenti card shows the pair details
      (counterpart description/amount/date/net) using the same formatting as
      `TransactionPairPopover`, with a link to `/transactions/{pairedWithId}`
  </behavior>
  <action>
    Create `app/(app)/transactions/[id]/page.tsx` as an async RSC page:
    `export default async function TransactionDetailPage({ params }: { params: Promise<{ id: string }> })`.
    `const { id } = await params`, `const { userId } = await verifySession()` (from
    `@/lib/dal/auth`), `const tx = await getTransactionForDetail({ userId, id })`, `if
    (!tx) notFound()`. Fetch `categories` (`getCategories()`) and `mostUsed`
    (`getMostUsedSubcategories(['in','out','transfer','allocation'])`) in parallel via
    `Promise.all` alongside the transaction fetch — same three data dependencies
    `transactions/page.tsx` already loads for its dialogs. Render
    `<TransactionDetailClient transaction={tx} categories={categories} mostUsed={mostUsed} />`.

    Create `components/transactions/transaction-detail-client.tsx` (`'use client'`)
    exporting `TransactionDetailClient({ transaction, categories, mostUsed })`. Use
    `useRouter()` from `next/navigation` for `router.refresh()` (silent, D-08) and
    `router.push(APP_ROUTES.transactions)` after delete. Hold local `useState` booleans
    for each dialog (`pairPickerOpen`, `detachOpen`, `categoryPickerOpen`, `deleteOpen`)
    exactly like the pattern already used in `transaction-table.tsx`'s row-level state
    (`pairTarget`, `detachTarget`, `categorizeTarget`). Wire:
    - `CounterpartPickerDialog` with `transactionId={transaction.id}`,
      `transactionAmount={transaction.amount}`, `transactionOccurredAt={transaction.occurredAt}`,
      `onPaired={() => { setPairPickerOpen(false); router.refresh() }}`
    - Unpair: reuse `deleteTransactionPairAction` exactly as `transaction-table.tsx`'s
      `handleUnpair` does (same import, same call shape), on success call
      `router.refresh()`
    - `DetachExpenseDialog` with `transactionId`, `defaultTitle` derived the same way
      as `transaction-table.tsx` (`(customTitle?.trim() || description).slice(0, 120)`),
      `categories`, `mostUsed`, `onSuccess={() => { setDetachOpen(false); router.refresh() }}`
    - `SubcategoryPicker` (via a thin categorize handler calling the existing
      `updateExpense` server action from `@/lib/actions/expenses` with `id:
      transaction.expenseId`, `title: transaction.expenseTitle`, `subCategoryId`) —
      pass `allowedCategoryTypes={['in','out','transfer','allocation']}` and
      `defaultType={null}` matching `ExpenseCategorizeDialog`'s call shape
    - Delete: same two-state (`deleteLinkedExpenses` checkbox) confirmation dialog
      body as `DeleteTransactionMenuItem` in `transaction-table.tsx`, calling
      `deleteTransaction` from `@/lib/actions/transactions`; on success,
      `toast.success('Transazione eliminata.')` then `router.push(APP_ROUTES.transactions)`
      (D-11 — redirect to origin table, not a local list update)
    - Render `<TransactionAmountEdit>`/`<TransactionDateEdit>`/`<TransactionTitleEdit>`
      (existing component, reused unmodified) each with `onSuccess={() =>
      router.refresh()}`
    - Description card: plain readonly text + `Lock` icon (lucide-react) + a tooltip
      (reuse whatever tooltip primitive the codebase already has under
      `@/components/ui/tooltip`; if none exists, use the native `title` attribute on
      the lock icon exactly as `expense-transactions-dialog.tsx` uses `title=` on
      other elements — do not add a new tooltip dependency) with text "chiave di
      riconciliazione bancaria — non modificabile"
    - Never render `transactionHash`/`descriptionHash` anywhere in this component
  </action>
  <verify>
    <automated>yarn vitest run tests/transaction-detail-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notFound()" "app/(app)/transactions/[id]/page.tsx"` matches
    - `grep -n "getTransactionForDetail" "app/(app)/transactions/[id]/page.tsx"` matches
    - `grep -n "transactionHash\|descriptionHash" components/transactions/transaction-detail-client.tsx` — zero matches (immutable fields never rendered)
    - `grep -c "input" components/transactions/transaction-detail-client.tsx` — the only `<input>` elements present are inside the reused edit/dialog components (no raw description input added directly in this file; verify by inspecting the description card block manually, not just grep count, since amount/date/title edit inputs live in imported components)
    - `yarn vitest run tests/transaction-detail-page.test.tsx` passes, covering: 200 render with all fields, 404 for a mismatched-owner fixture, description rendered without an editable control, category subtitle appears only when `expenseTransactionCount > 1`
    - `yarn tsc --noEmit` introduces no new errors attributable to these two files
  </acceptance_criteria>
  <done>/transactions/[id] renders all required fields and actions, 404s for non-owned ids, and every write path reuses an existing Phase 62 action or dialog with zero new server actions created in this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → RSC page | `id` route param is attacker-controlled |
| Client wrapper → server actions | Dialog forms submit `transactionId`/`expenseId` values sourced from the already-ownership-checked page props, but actions re-verify ownership server-side regardless |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-04 | Spoofing/Elevation (IDOR) | `app/(app)/transactions/[id]/page.tsx` | high | mitigate | `getTransactionForDetail` ownership-scoped query (Plan 01) + `notFound()` on undefined; no raw id-only lookup anywhere in this page |
| T-63-05 | Tampering | `TransactionAmountEdit` | high | mitigate | All writes route through `updateTransactionAction`, which re-validates ownership and the pair-guard server-side (Phase 62) — the client component cannot bypass either check |
| T-63-06 | Information Disclosure | Description/hash display | medium | mitigate | `transactionHash`/`descriptionHash` are structurally never read into the rendered client component (grep-gated in acceptance criteria) |
| T-63-07 | Repudiation | Delete action | low | accept | Existing confirmation dialog + toast pattern already accepted at table level; unchanged here |
</threat_model>

<verification>
- `yarn vitest run tests/transaction-detail-page.test.tsx`
- `yarn tsc --noEmit`
- Manual: navigate to `/transactions/{id}`, edit amount on an unpaired transaction, confirm `router.refresh()` updates the header with no toast; attempt the same edit on a paired transaction with an amount that flips relative sign parity, confirm the Italian error appears inline
</verification>

<success_criteria>
- DET-05 fully satisfied: all fields shown, pencil-inline edit for amount/date/title, category via SubcategoryPicker, immutable fields visibly readonly, all five actions present, cross-refs to linked expense and source file/Manuale.
- Zero new server actions or dialogs created — everything routes through Phase 62 actions and existing table dialogs.
</success_criteria>

<output>
Create `.planning/phases/63-detail-pages-tx-expense/63-02-SUMMARY.md` when done
</output>
