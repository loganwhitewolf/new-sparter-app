---
phase: 63-detail-pages-tx-expense
plan: 04
type: execute
wave: 3
depends_on: ["63-02", "63-03"]
files_modified:
  - components/transactions/transaction-table.tsx
  - components/expenses/expense-table.tsx
  - tests/transaction-table-menu.test.tsx
  - tests/expense-table-menu.test.tsx
autonomous: true
requirements: [DET-07]

must_haves:
  truths:
    - "Transaction table row menu has a 'Dettagli' entry linking to /transactions/[id]"
    - "Expense table row menu has a single 'Dettagli' entry linking to /expenses/[id]; the 'Modifica' entry no longer exists"
    - "expense-form-dialog's edit mode is no longer invoked from expense-table.tsx (create mode is untouched)"
    - "expense-transactions-dialog.tsx is no longer imported or rendered by expense-table.tsx"
  artifacts:
    - components/transactions/transaction-table.tsx (Dettagli menu entry added)
    - components/expenses/expense-table.tsx (Dettagli entry now navigates; Modifica entry removed; ExpenseTransactionsDialog usage removed)
  key_links:
    - "Both Dettagli entries use transactionDetailHref/expenseDetailHref from lib/routes.ts (Plan 01) — no hand-rolled path strings"
    - "expense-transactions-dialog.tsx component file itself is left in place (deletion is explicitly Phase 64 scope per D-13/D-14) — only its call site in expense-table.tsx is removed"
---

<objective>
Wire the two existing tables to the new detail pages (DET-07): add a "Dettagli" entry
to the transaction table's row menu, and collapse the expense table's "Dettagli" +
"Modifica" pair into a single "Dettagli" entry that navigates to `/expenses/[id]`.

Purpose: this is the last piece connecting Phase 63's new pages to the app's existing
navigation surface, and the step that actually retires the old expense dialog
workflow from the table (though the dialog component itself survives until Phase 64
per D-13/D-14 — only its table call site is removed here).

Output: two modified table components with no dead dialogs mounted for the removed
edit-mode/dettagli-dialog flow.
</objective>

## Artifacts this phase produces

- No new source files. Modifies `components/transactions/transaction-table.tsx` (adds a Dettagli `DropdownMenuItem`) and `components/expenses/expense-table.tsx` (collapses Dettagli+Modifica, removes `ExpenseTransactionsDialog`/`ExpenseFormDialog mode="edit"` call sites).
- `tests/transaction-table-menu.test.tsx`, `tests/expense-table-menu.test.tsx` — new test files
- Consumes (not produced here): `transactionDetailHref`, `expenseDetailHref` (63-01)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-detail-pages-tx-expense/63-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transaction table — add Dettagli menu entry</name>
  <files>components/transactions/transaction-table.tsx, tests/transaction-table-menu.test.tsx</files>
  <read_first>
    components/transactions/transaction-table.tsx (full DropdownMenuContent block, lines 530-655 — exact position and sibling items: Ricategorizza/Cerca su Google/Categorizza spesa, Scollega/Collega rimborso, Spesa a sé, DeleteTransactionMenuItem separator)
    lib/routes.ts (transactionDetailHref from Plan 01)
  </read_first>
  <behavior>
    - Every transaction row's "⋯" menu shows a "Dettagli" entry as the first item,
      above the existing Ricategorizza/Cerca su Google branch
    - Clicking "Dettagli" navigates to `/transactions/{transaction.id}` (a real `Link`,
      not a client-side state change)
    - No existing menu item (Ricategorizza, Cerca su Google, Categorizza spesa,
      Scollega, Collega rimborso, Spesa a sé, Elimina) is removed, reordered relative
      to each other, or has its behavior changed — table quick flows all stay per D-15;
      DET-07 removes only the expense "dettagli"+"modifica" dialogs (Task 2)
  </behavior>
  <action>
    In `components/transactions/transaction-table.tsx`, import `transactionDetailHref`
    from `@/lib/routes` (the `APP_ROUTES` import already present stays for other uses).
    Inside the `<DropdownMenuContent align="end">` block (around line 545), add a new
    `<DropdownMenuItem asChild>` wrapping a `<Link href={transactionDetailHref(transaction.id)}>Dettagli</Link>`
    as the very first child, immediately after the opening `<DropdownMenuContent
    align="end">` tag and before the `{isCategorized ? ... : ...}` branch. `Link` from
    `next/link` is already imported in this file (used for the file cross-ref) — reuse
    that import, do not add a duplicate.
  </action>
  <verify>
    <automated>yarn vitest run tests/transaction-table-menu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "transactionDetailHref" components/transactions/transaction-table.tsx` matches
    - `grep -n "Dettagli" components/transactions/transaction-table.tsx` matches
    - `yarn vitest run tests/transaction-table-menu.test.tsx` passes, asserting the
      rendered menu contains an `<a href="/transactions/{id}">Dettagli</a>` and that
      the existing "Cerca su Google" / "Elimina" items are still present and unchanged
    - `yarn tsc --noEmit` introduces no new errors attributable to this file
  </acceptance_criteria>
  <done>Transaction table row menu has a working Dettagli entry as its first item; no other menu item changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expense table — collapse Dettagli+Modifica into a single Dettagli link</name>
  <files>components/expenses/expense-table.tsx, tests/expense-table-menu.test.tsx</files>
  <read_first>
    components/expenses/expense-table.tsx (full file — specifically: imports lines 1-47, `transactionsDialogExpense` state line 85-88, DropdownMenuContent block lines 344-383, `ExpenseTransactionsDialog` render lines 457-463, `ExpenseFormDialog` mode="edit" usage lines 353-371)
    lib/routes.ts (expenseDetailHref from Plan 01)
  </read_first>
  <behavior>
    - Every expense row's "⋯" menu shows exactly one "Dettagli" entry (no "Modifica"
      entry anywhere in the menu) that navigates to `/expenses/{exp.id}` as a real link
    - `expense-transactions-dialog.tsx` is no longer imported or rendered by this file
      (its dialog-opening state `transactionsDialogExpense` and its JSX render block
      are both removed)
    - `ExpenseFormDialog` with `mode="edit"` is no longer rendered by this file (the
      component itself is untouched — `mode="create"` usage elsewhere in the app, e.g.
      the "Nuova spesa" button on `expenses/page.tsx`, is out of scope for this plan
      and must not be touched)
    - `IgnoreExpenseMenuItem` and `DeleteExpenseMenuItem` remain exactly as before,
      unchanged in position or behavior
  </behavior>
  <action>
    In `components/expenses/expense-table.tsx`:
    1. Add `import Link from 'next/link'` and `import { expenseDetailHref } from
       '@/lib/routes'`.
    2. Remove the `import { ExpenseTransactionsDialog } from
       './expense-transactions-dialog'` line and the `transactionsDialogExpense`
       state declaration (lines ~85-88) and its setter calls.
    3. In the `DropdownMenuContent` block (~line 344), replace the current
       `<DropdownMenuItem onSelect={() => { setTransactionsDialogExpense(...); ...
       }}>Dettagli</DropdownMenuItem>` with `<DropdownMenuItem asChild><Link
       href={expenseDetailHref(exp.id)}>Dettagli</Link></DropdownMenuItem>`.
    4. Delete the entire `<ExpenseFormDialog mode="edit" ...>` block (~lines 353-371)
       that currently renders the "Modifica" `DropdownMenuItem` via its `trigger` prop
       — this removes both the menu entry and the dialog invocation in one deletion
       since `trigger` is how this dialog injects its own menu item.
    5. Remove the `<ExpenseTransactionsDialog ... />` render block (~lines 457-463)
       entirely.
    6. Leave `ExpenseFormDialog`'s `mode="create"` usage elsewhere in the codebase
       (e.g. on the expenses list page) completely untouched — this plan only removes
       the `mode="edit"` call site inside this table file.
  </action>
  <verify>
    <automated>yarn vitest run tests/expense-table-menu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ExpenseTransactionsDialog" components/expenses/expense-table.tsx` — zero matches
    - `grep -n 'mode="edit"' components/expenses/expense-table.tsx` — zero matches
    - `grep -c "Dettagli" components/expenses/expense-table.tsx` — exactly 1 match (the single collapsed entry)
    - `grep -n "Modifica" components/expenses/expense-table.tsx` — zero matches
    - `grep -n "expenseDetailHref" components/expenses/expense-table.tsx` matches
    - `grep -n "export function ExpenseTransactionsDialog" components/expenses/expense-transactions-dialog.tsx` — file itself still exists and exports unchanged (deletion is Phase 64 scope, not this plan's)
    - `yarn vitest run tests/expense-table-menu.test.tsx` passes, asserting: the menu
      contains exactly one `<a href="/expenses/{id}">Dettagli</a>`, no "Modifica" text
      anywhere in the rendered menu, and `IgnoreExpenseMenuItem`/`DeleteExpenseMenuItem`
      behavior (ignore + delete confirmation) is unaffected
    - `yarn tsc --noEmit` introduces no new errors attributable to this file
  </acceptance_criteria>
  <done>Expense table row menu has a single Dettagli link to /expenses/[id]; Modifica entry and expense-transactions-dialog/edit-mode form-dialog call sites are gone from this file; the dialog component file itself is untouched pending Phase 64 deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Table row → detail page link | `exp.id`/`transaction.id` values are already scoped to the current user's own table query results — the link itself introduces no new trust boundary, but the destination page (Plans 02/03) must independently re-verify ownership, which it does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-12 | Tampering (dead code / stale UI) | `expense-table.tsx` | low | mitigate | Acceptance criteria grep-gate the complete removal of the old `Modifica`/`ExpenseTransactionsDialog` call sites so no orphaned dialog state or unreachable menu path remains |
</threat_model>

<verification>
- `yarn vitest run tests/transaction-table-menu.test.tsx tests/expense-table-menu.test.tsx`
- `yarn tsc --noEmit`
- `yarn check:language` (route/menu-label strings touched in this plan)
- Manual: open Transazioni table, click a row's "⋯" → Dettagli → confirm landing on `/transactions/[id]`; open Spese table, confirm only one "Dettagli" entry exists and it navigates to `/expenses/[id]`
</verification>

<success_criteria>
- DET-07 fully satisfied: old expense edit/details dialogs are no longer reachable from the expense table; no dead menu entries; both tables link to the new detail pages.
</success_criteria>

<output>
Create `.planning/phases/63-detail-pages-tx-expense/63-04-SUMMARY.md` when done
</output>
