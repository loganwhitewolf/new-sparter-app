---
phase: 62-transaction-edit-core
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/dal/expenses.ts
  - lib/validations/expense.ts
  - lib/actions/expenses.ts
  - tests/expense-edit.test.ts
autonomous: true
requirements: [DET-04]

must_haves:
  truths:
    - "Editing an expense's title, notes, or subCategoryId via updateExpense persists exactly those fields and never writes totalAmount, transactionCount, firstTransactionAt, or lastTransactionAt."
    - "Assigning a subCategoryId through updateExpense transitions status to '3' (manually categorized) and writes a classification-history row with source 'manual', matching the status/history behavior of the existing categorizeExpense action."
    - "Clearing subCategoryId (setting it to undefined/null) through updateExpense transitions status to '1' (uncategorized) without writing a classification-history row for the clear."
    - "updateExpense only mutates rows owned by the caller's userId; attempting to edit another user's expense affects zero rows and is reported as a failure to the caller."
  artifacts:
    - lib/dal/expenses.ts (updateExpense extended)
    - lib/validations/expense.ts (UpdateExpenseSchema reused/extended)
    - lib/actions/expenses.ts (updateExpense action extended)
    - tests/expense-edit.test.ts
  key_links:
    - "updateExpense DAL function wraps its SELECT (for fromSubCategoryId/fromStatus) + UPDATE + writeClassificationHistory in db.transaction(async (tx) => ...), mirroring categorizeExpense in lib/actions/expenses.ts lines 304-333."
    - "updateExpense action (lib/actions/expenses.ts) continues to use UpdateExpenseSchema from lib/validations/expense.ts; no new schema file needed since the existing schema already models {id, title, subCategoryId?, notes?}."
---

<objective>
Extend the existing `updateExpense` DAL function (`lib/dal/expenses.ts`) so it matches the categorize flow's status/history semantics required by DET-04: wrap the update in `db.transaction`, write `expenseClassificationHistory` when `subCategoryId` changes (source `'manual'`), and keep derived fields (`totalAmount`, `transactionCount`, `firstTransactionAt`, `lastTransactionAt`) permanently unwritable through this path. No new route or schema files — extend what already exists.

Purpose: DET-04 closes the last backend requirement of Phase 62; Phase 63's expense detail page will call this same DAL function for pencil-inline title/notes/category edits.
Output: `lib/dal/expenses.ts` (`updateExpense` rewritten to be atomic + history-aware), `tests/expense-edit.test.ts` (RED→GREEN coverage for DET-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-transaction-edit-core/62-RESEARCH.md
@.planning/phases/62-transaction-edit-core/62-PATTERNS.md
@lib/dal/expenses.ts
@lib/actions/expenses.ts
@lib/validations/expense.ts
@lib/dal/classification-history.ts
@lib/db/schema.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing tests for updateExpense atomicity, status transitions, and history</name>
  <files>tests/expense-edit.test.ts</files>
  <read_first>
    tests/expense-deletion-service.test.ts, lib/dal/expenses.ts (current updateExpense, lines 344-361), lib/actions/expenses.ts (categorizeExpense, lines 287-339), lib/dal/classification-history.ts, lib/db/schema.ts (expense table)
  </read_first>
  <behavior>
    Follow `tests/expense-deletion-service.test.ts`'s mocking style: hoisted `mocks = { transaction, select, update, insert }`; `vi.mock('server-only', () => ({}))`; `vi.mock('@/lib/db', () => ({ db: { transaction: mocks.transaction } }))`; `vi.mock('@/lib/db/schema', () => ({ expense: { id: 'expense.id', userId: 'expense.userId', subCategoryId: 'expense.subCategoryId', status: 'expense.status' }, expenseClassificationHistory: { } }))`; `vi.mock('drizzle-orm', () => ({ and: vi.fn((...args) => ({ kind: 'and', args })), eq: vi.fn((a, b) => ({ kind: 'eq', a, b })) }))`. Mock `@/lib/dal/classification-history` with `vi.fn()` for `writeClassificationHistory` so tests can assert on call args without touching the real insert chain. `beforeEach`: `vi.clearAllMocks()`; `mocks.transaction.mockImplementation(async (cb) => cb({ select: mocks.select, update: mocks.update }))`.
    Import `updateExpense` from `@/lib/dal/expenses` via top-level `await import(...)` after mocks (RED until Task 2).
    Cases (`describe('updateExpense', ...)`):
    - Test 1 (DET-04 atomicity): call `updateExpense({ id: 'exp-1', userId: 'user-1', title: 'Spesa X', subCategoryId: 7 })` with select returning `[{ subCategoryId: null, status: '1' }]` and update chain resolving normally. Assert `mocks.transaction` was called exactly once (the whole read+write+history sequence is atomic).
    - Test 2 (DET-04 categorize transition): same call as Test 1; assert `tx.update(expense).set(...)` payload includes `subCategoryId: 7`, `status: '3'`, and does NOT include `totalAmount`, `transactionCount`, `firstTransactionAt`, or `lastTransactionAt` keys. Assert `writeClassificationHistory` was called once with `{ userId: 'user-1', expenseId: 'exp-1', fromSubCategoryId: null, toSubCategoryId: 7, fromStatus: '1', toStatus: '3', source: 'manual' }`.
    - Test 3 (DET-04 uncategorize transition): call `updateExpense({ id: 'exp-1', userId: 'user-1', title: 'Spesa X', subCategoryId: undefined })` with select returning `[{ subCategoryId: 7, status: '3' }]`. Assert the update `.set(...)` includes `subCategoryId: null`, `status: '1'`. Assert `writeClassificationHistory` was NOT called (only actual categorization events are recorded, matching `categorizeExpense`'s scope — clearing is not a categorize action).
    - Test 4 (DET-04 no-op on unchanged category): call `updateExpense({ id: 'exp-1', userId: 'user-1', title: 'New title only' })` where `subCategoryId` argument is omitted entirely and the loaded row already has `subCategoryId: 7, status: '3'` — distinguish "omitted" (leave category untouched, keep status '3') from "explicitly cleared" (Test 3). Use a sentinel: the DAL input type must let title-only edits pass `subCategoryId: undefined` meaning "don't touch," while an explicit clear is a separate signal — document this distinction inline in the action wrapper's call site expectations, and assert in this test that `.set(...)` does NOT include a `subCategoryId` or `status` key at all when only `title` is supplied. Assert `writeClassificationHistory` was NOT called.
    - Test 5 (derived-field immutability): for every one of Tests 1-4, assert the `.set(...)` payload object has no own property named `totalAmount`, `transactionCount`, `firstTransactionAt`, or `lastTransactionAt` (use `expect(Object.keys(setPayload)).not.toEqual(expect.arrayContaining([...]))` or explicit `.toHaveProperty` negations).
    - Test 6 (ownership scoping): assert the `.where(...)` argument passed to `tx.update(expense).set(...).where(...)` is built from `and(eq(expense.id, 'exp-1'), eq(expense.userId, 'user-1'))` — inspect the mocked `and`/`eq` call args to confirm both conditions are present (IDOR regression guard).
    Each test independent; no cross-test mutable state beyond `beforeEach`.
  </behavior>
  <action>
    Write the test file per the behavior block. Run it once to confirm the failures are all "current updateExpense doesn't call db.transaction / writeClassificationHistory" assertion mismatches (the module resolves and exports today, so this RED state is behavioral, not structural — call this out explicitly in the task commit message). Commit with message `test(62-02): add failing tests for updateExpense atomicity and history`.
  </action>
  <verify>
    <automated>yarn vitest run tests/expense-edit.test.ts 2>&1 | grep -E "FAIL|failed"</automated>
  </verify>
  <done>tests/expense-edit.test.ts exists with the 6 cases above; running it against the current (pre-Task-2) lib/dal/expenses.ts fails on the atomicity/history/status assertions.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — rewrite updateExpense DAL to be atomic and history-aware</name>
  <files>lib/dal/expenses.ts</files>
  <read_first>
    tests/expense-edit.test.ts (just written), lib/dal/expenses.ts (current updateExpense at line 344), lib/actions/expenses.ts (categorizeExpense at line 287, bulkCategorize at line 207), lib/dal/classification-history.ts, lib/db/schema.ts (expenseStatusEnum, expense table)
  </read_first>
  <action>
    Replace the current `updateExpense` function body (lib/dal/expenses.ts, currently lines 344-361: a bare `db.update(expense).set({...}).where(...)` with no transaction and no history write) with an atomic, history-aware implementation. Keep the exported function name `updateExpense` and its call sites in `lib/actions/expenses.ts` (`updateExpenseDAL`, imported as `updateExpense as updateExpenseDAL`) unchanged — only the internal implementation and the input type change.

    New input type: `{ id: string; userId: string; title: string; subCategoryId?: number | null; notes?: string }`. Distinguish three states for `subCategoryId` at the call boundary: `undefined` = "field not present in this edit, leave category/status untouched"; explicit `null` = "clear the category" (status → `'1'`); a positive number = "assign category" (status → `'3'`, write history when it differs from the current `subCategoryId`).

    Import `writeClassificationHistory` from `@/lib/dal/classification-history` and `db` from `@/lib/db` (already imported) into `lib/dal/expenses.ts`. Wrap the whole operation in `db.transaction(async (tx) => { ... })`:
    1. Select `{ subCategoryId: expense.subCategoryId, status: expense.status }` from `expense` where `and(eq(expense.id, data.id), eq(expense.userId, data.userId))` limit 1 — capture as `before`.
    2. Build `updateSet: Record<string, unknown> = { title: data.title, updatedAt: new Date() }`. Add `notes: data.notes ?? null` only when `data.notes !== undefined` — actually always set notes to `data.notes ?? null` to preserve existing "clear notes by omitting" behavior from the current implementation (check the existing behavior before changing it: current code always sets `notes: data.notes ?? null`; keep that exact semantic, do not change notes handling — only rework subCategoryId/status/history logic).
    3. If `data.subCategoryId === undefined`: do NOT add `subCategoryId` or `status` keys to `updateSet` (Test 4 behavior — omitted field is untouched).
    4. Else if `data.subCategoryId === null`: add `subCategoryId: null, status: '1'` to `updateSet`. Do not call `writeClassificationHistory`.
    5. Else (positive number): add `subCategoryId: data.subCategoryId, status: '3'` to `updateSet`. After the update succeeds, call `writeClassificationHistory(tx, { userId: data.userId, expenseId: data.id, fromSubCategoryId: before[0]?.subCategoryId ?? null, toSubCategoryId: data.subCategoryId, fromStatus: before[0]?.status ?? null, toStatus: '3', source: 'manual' })` wrapped in try/catch with the history write treated as non-fatal (matching `categorizeExpense`'s `// history write failure is non-fatal` pattern) — do not let a history-write failure roll back the category update.
    6. Run `tx.update(expense).set(updateSet).where(and(eq(expense.id, data.id), eq(expense.userId, data.userId)))`.
    7. Never add `totalAmount`, `transactionCount`, `firstTransactionAt`, or `lastTransactionAt` to `updateSet` under any branch — these keys must not appear anywhere in this function.

    Do not touch `updateExpenseTitle`, `deleteExpense`, or `deleteExpenses` in this same file — leave them exactly as-is.
  </action>
  <verify>
    <automated>yarn vitest run tests/expense-edit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests in tests/expense-edit.test.ts pass.
    - `grep -n "totalAmount\|transactionCount\|firstTransactionAt\|lastTransactionAt" lib/dal/expenses.ts` shows zero matches inside the `updateExpense` function body (search the function's line range specifically, since other DAL functions in the same file legitimately reference these columns for reads).
    - `grep -n "db.transaction" lib/dal/expenses.ts` shows the `updateExpense` function wraps its select+update+history calls in one `db.transaction(async (tx) => ...)` block.
    - `grep -n "writeClassificationHistory" lib/dal/expenses.ts` confirms the import and the call inside the categorize branch.
    - `yarn tsc --noEmit` passes with no new errors attributable to `lib/dal/expenses.ts` or its callers in `lib/actions/expenses.ts`.
  </acceptance_criteria>
  <done>updateExpense DAL function is atomic (single db.transaction), writes classification history on category assignment with source 'manual', transitions status consistently with categorizeExpense ('3' on assign, '1' on explicit clear, untouched on omit), and never writes derived aggregate fields.</done>
</task>

</tasks>

## Artifacts this phase produces

- `lib/dal/expenses.ts` — `updateExpense` function signature changes to accept `subCategoryId?: number | null` (previously `subCategoryId?: number`), enabling the three-state omit/clear/assign distinction. No new exports; existing import `updateExpense as updateExpenseDAL` in `lib/actions/expenses.ts` is unaffected.
- `tests/expense-edit.test.ts` — new test file, no exports consumed elsewhere.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (form/action call) → `updateExpense` action | Untrusted `FormData` (id, title, subCategoryId, notes) crosses into the server action in `lib/actions/expenses.ts`. |
| `updateExpense` action → `updateExpense` DAL | `userId` is derived server-side via `verifySession()`, never taken from the client payload. |
| `updateExpense` DAL → DB | Ownership-gated read (for `before` state) and write on `expense`; classification-history insert scoped to the same `userId`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-07 | Tampering | `updateExpense` DAL — IDOR on expense id | high | mitigate | Both the read and the write are scoped with `and(eq(expense.id, data.id), eq(expense.userId, data.userId))`; Task 1 Test 6 asserts both conditions are present in the `.where(...)` call. |
| T-62-08 | Tampering | `updateExpense` DAL — derived-aggregate injection | high | mitigate | `updateSet` is built key-by-key from an explicit allowlist (title/notes/subCategoryId/status/updatedAt); `totalAmount`/`transactionCount`/`firstTransactionAt`/`lastTransactionAt` never appear in the function body under any branch — Task 1 Test 5 asserts this across all four call shapes. |
| T-62-09 | Tampering | `updateExpense` DAL — read/write split allows a race between the ownership read and the update | medium | mitigate | The select (`before`), the update, and the history write all run inside one `db.transaction(async (tx) => ...)`, closing the same class of race documented for `transaction-pairs.ts` (CR-02). |
| T-62-10 | Repudiation | Classification history write failure is silently swallowed | low | accept | Matches the existing, already-shipped `categorizeExpense`/`bulkCategorize` behavior (non-fatal history write) — consistency with the categorize flow is the DET-04 requirement itself; changing this here would create two divergent behaviors for the same status transition. |

</threat_model>

<verification>
- `yarn vitest run tests/expense-edit.test.ts` — all 6 cases green.
- `yarn tsc --noEmit` — no new type errors in `lib/dal/expenses.ts`.
- Manual grep: `updateExpense` function body contains no reference to `totalAmount`, `transactionCount`, `firstTransactionAt`, or `lastTransactionAt`.
</verification>

<success_criteria>
- DET-04: `updateExpense` covers `title`, `notes`, `subCategoryId` with status transitions consistent with the categorize flow (assign → '3' + history; clear → '1', no history; omit → untouched); derived expense fields are never writable through it.
</success_criteria>

<output>
Create `.planning/phases/62-transaction-edit-core/62-02-SUMMARY.md` when done
</output>
