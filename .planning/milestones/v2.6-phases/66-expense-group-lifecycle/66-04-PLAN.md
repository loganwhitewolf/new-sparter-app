---
phase: 66-expense-group-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["66-02"]
files_modified:
  - components/expenses/merge-expenses-dialog.tsx
  - components/expenses/expense-table.tsx
  - components/expenses/group-categorize-dialog.tsx
  - tests/merge-expenses-dialog.test.tsx
  - tests/expense-table-menu.test.tsx
autonomous: true
requirements: [GRP-05, GRP-06]

must_haves:
  truths:
    - "A composed group row in the expenses table (id `group:${groupId}`) is selectable in the bulk-selection checkbox — this is the entry point for GRP-06's add-to-group flow (D-04)."
    - "Selecting exactly one group row + N ungrouped rows enables 'Unisci' and routes MergeExpensesDialog into add-to-group mode (D-04); selecting 2+ group rows disables 'Unisci' (D-06); selecting 2+ ungrouped rows with zero group rows behaves exactly as before (Phase 65 create-group flow, unchanged)."
    - "Categorizza/Elimina bulk actions are disabled (no-op with an explanatory toast) whenever the current selection includes any group row — bulk categorize/delete are not meaningful operations on a group's synthetic id."
    - "The expenses table's group row exposes a 'Cambia categoria' action (dropdown item) that opens a SubcategoryPicker and calls categorizeExpenseGroup — the group-row surface for GRP-05/D-01."
  artifacts:
    - "components/expenses/group-categorize-dialog.tsx exports GroupCategorizeDialog"
    - "components/expenses/merge-expenses-dialog.tsx's MergeExpensesDialog gains an optional targetGroup prop and exports runAddToGroupStep"
  key_links:
    - "expense-table.tsx computes targetGroup from the selection (the single selected group row's {id, title, subCategoryId}) and passes it to MergeExpensesDialog — the two files must agree on this exact shape since they execute in the same plan (Task 1 defines the prop, Task 2 consumes it)."
    - "GroupCategorizeDialog and runAddToGroupStep both ultimately call the Plan 66-02 server actions (categorizeExpenseGroup, addExpensesToGroupAction) — no new backend logic is added in this plan."
  prohibitions:
    - statement: "Group-to-group merge is rejected (D-06) — selecting two group rows is not a supported 'Unisci'."
      status: enforced
      verification: "test: tests/expense-table-menu.test.tsx asserts the Unisci button is absent/disabled when 2+ group rows are selected"
---

<objective>
Wire GRP-05 (group-row recategorize) and GRP-06 (add-to-group via "Unisci") into the expenses
table and the merge dialog. Task order matters: Task 1 defines `MergeExpensesDialog`'s new
`targetGroup` prop and add-to-group flow FIRST, so Task 2 (the table's selection logic) has a
stable contract to wire against within the same plan, and Task 3 adds the group row's own
"Cambia categoria" control via a new small dedicated dialog component.

Purpose: complete the two user-facing entry points for Phase 66's lifecycle mutations that live
on the expenses table — the table is the primary post-import categorization surface (D-01) and
the only entry point for add-to-group this phase (D-04).

Output: `MergeExpensesDialog` supports add-to-group mode; `ExpenseTable` makes group rows
selectable and gates "Unisci"/Categorizza/Elimina correctly; a new `GroupCategorizeDialog`
component wires the group row's own recategorize action.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-expense-group-lifecycle/66-CONTEXT.md
@.planning/phases/66-expense-group-lifecycle/66-RESEARCH.md
@.planning/phases/66-expense-group-lifecycle/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MergeExpensesDialog add-to-group mode (GRP-06, D-04/D-05)</name>
  <files>components/expenses/merge-expenses-dialog.tsx, tests/merge-expenses-dialog.test.tsx</files>
  <read_first>
    components/expenses/merge-expenses-dialog.tsx (full file in context) — the existing
    'title' -> 'categorize' -> 'confirm' Step machine (L21, L112-168) and its four exported pure
    functions (isGroupTitleValid, getUncategorizedIds, nextStepAfterTitle,
    getSharedSubCategoryId L33-68) plus runCategorizeStep/runMergeStep (L74-102) are the exact
    precedent to extend, NOT replace — all four existing exports and the create-group flow must
    keep working byte-identically when `targetGroup` is absent/null (backward compatible with
    Plan 65-04's usage).
    tests/merge-expenses-dialog.test.tsx (full file in context) — the render-smoke technique
    (Dialog/DialogContent/SubcategoryPicker mocked as passthrough divs, L32-50) and the
    pure-function unit-test style (L70-180) are what the new tests must follow — this repo has
    no jsdom, so interaction cannot be simulated; only pure functions and static render structure
    are testable.
  </read_first>
  <behavior>
    - When `targetGroup` is null/omitted, MergeExpensesDialog behaves EXACTLY as before
      (create-group flow, 'title' -> 'categorize'?/'confirm').
    - When `targetGroup` is provided, the dialog opens directly at a 'confirm' step (no 'title'
      step — the group already has a title) with descriptive text naming the group and its
      subcategory, and a note that uncategorized selections will be auto-assigned to the group's
      subcategory.
    - `runAddToGroupStep({ selectedExpenses, targetGroupId, targetSubCategoryId })`: if any
      selectedExpenses has `subCategoryId === null`, first calls `bulkCategorize` scoped to just
      those ids with `subCategoryId: String(targetSubCategoryId)` (reusing `getUncategorizedIds`
      unchanged); on error, return that error without calling the add action. Otherwise (or after
      the categorize call succeeds), calls the new `addExpensesToGroupAction` with the FULL
      original `selectedExpenses` id set and `targetGroupId` (mirrors `runMergeStep`'s "always
      full original set" rule).
  </behavior>
  <action>
    Import `addExpensesToGroupAction` alongside the existing `bulkCategorize, mergeExpenses`
    import from `@/lib/actions/expenses` (L15).
    Add a new prop to `Props` (after `mostUsed`, before `onSuccess`): `targetGroup?: { id: number;
    title: string; subCategoryId: number } | null`.
    Add exported function `runAddToGroupStep({ selectedExpenses, targetGroupId,
    targetSubCategoryId }: { selectedExpenses: MergeSelectedExpense[]; targetGroupId: number;
    targetSubCategoryId: number }): Promise&lt;{ error: string | null }&gt;` near
    `runMergeStep` (after L102): if `getUncategorizedIds(selectedExpenses).length > 0`, build a
    FormData with `ids` = JSON.stringify(getUncategorizedIds(selectedExpenses)) and
    `subCategoryId` = String(targetSubCategoryId), call `bulkCategorize({error: null}, fd)`; if
    that result has an error, return it immediately (do not proceed). Then build a second
    FormData with `groupId` = String(targetGroupId) and `expenseIds` =
    JSON.stringify(selectedExpenses.map((e) => e.id)), call
    `addExpensesToGroupAction({error: null}, fd)`, and return its result.
    In the `MergeExpensesDialog` component body: initialize `step` state to
    `targetGroup ? 'confirm' : 'title'` (the initializer function form of `useState` — read
    `targetGroup` once at mount, this dialog is remounted per open per the existing pattern
    since `open`/`onOpenChange` fully control visibility upstream). In `handleOpenChange`'s close
    branch (currently resets to `'title'`), reset to `targetGroup ? 'confirm' : 'title'` instead.
    Replace the DialogDescription/confirm-step body conditionally: when `targetGroup` is set,
    render `Stai per aggiungere {selectedExpenses.length} spese al gruppo "{targetGroup.title}".`
    plus, only when `getUncategorizedIds(selectedExpenses).length > 0`, an additional line: `Le
    spese non categorizzate verranno assegnate automaticamente alla categoria del gruppo.` The
    confirm button's label becomes `Aggiungi` (instead of `Unisci`) and its handler
    (`handleConfirmAddToGroup`) calls `runAddToGroupStep({ selectedExpenses, targetGroupId:
    targetGroup.id, targetSubCategoryId: targetGroup.subCategoryId })` inside `startTransition`,
    with the same success/error toast + `onSuccess()` + `resetAndClose()` shape as
    `handleConfirmMerge`. The dialog must NEVER render the 'title' Input or the 'categorize'
    SubcategoryPicker sheet when `targetGroup` is set — add-to-group has no picker step (D-05:
    the target subcategory is fixed, not user-chosen).
    In tests/merge-expenses-dialog.test.tsx, mock `addExpensesToGroupAction` alongside the
    existing `bulkCategorize`/`mergeExpenses` mocks (L23-26). Add:
    `describe('runAddToGroupStep (add-to-group, targetGroup mode)', ...)` with cases: calls
    bulkCategorize scoped to only uncategorized ids THEN addExpensesToGroupAction with the FULL
    id set, when the selection has an uncategorized member; calls addExpensesToGroupAction
    directly (bulkCategorize NOT called) when every selected expense is already categorized;
    propagates a bulkCategorize error without calling addExpensesToGroupAction; propagates an
    addExpensesToGroupAction error. Add a render-smoke case in the existing
    `describe('MergeExpensesDialog (render smoke test — title step)', ...)` block: passing
    `targetGroup={{ id: 7, title: 'Amazon condiviso', subCategoryId: 42 }}` renders the confirm
    step immediately (contains `'Amazon condiviso'`, contains the `Aggiungi` button text, does
    NOT contain the title-step `Input`'s `placeholder="Titolo del gruppo"`).
  </action>
  <acceptance_criteria>
    - `vitest tests/merge-expenses-dialog.test.tsx --run` passes, including all new/updated cases.
    - Every pre-existing exported function (isGroupTitleValid, getUncategorizedIds, nextStepAfterTitle, getSharedSubCategoryId, runCategorizeStep, runMergeStep) is unchanged in signature and behavior — the existing test cases for them still pass unmodified.
    - When `targetGroup` is omitted, rendering MergeExpensesDialog produces byte-identical output to before this task (create-group flow untouched).
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/merge-expenses-dialog.test.tsx --run</automated>
  </verify>
  <done>MergeExpensesDialog supports both create-group (unchanged) and add-to-group (new, confirm-only, no picker) modes; runAddToGroupStep exists and is exported; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExpenseTable selection/eligibility logic for group rows (GRP-06, D-04/D-06)</name>
  <files>components/expenses/expense-table.tsx, tests/expense-table-menu.test.tsx</files>
  <read_first>
    components/expenses/expense-table.tsx (full file in context) — the checkbox
    `disabled={isGrouped}` at L284 is the exact line that currently makes group rows
    unselectable and must change; the existing `mergeEligible` computation (L97-103) and the
    `<MergeExpensesDialog>` invocation (L480-493) are what this task extends.
    tests/expense-table-menu.test.tsx (full file in context) — the
    `'renders the grouped row checkbox as disabled'` test (L148-154) currently asserts the OLD
    behavior and must be updated to assert the OPPOSITE (checkbox now enabled) as part of this
    task — this is an intentional, expected behavior change for GRP-06, not a regression.
  </read_first>
  <behavior>
    - A group row's checkbox is selectable (not disabled).
    - Selection of 2+ ungrouped rows only (zero group rows) behaves exactly as before:
      `mergeEligible` requires the categorized subset to share one subcategory.
    - Selection of exactly 1 group row + 1+ ungrouped rows enables "Unisci" UNLESS any selected
      ungrouped row has a non-null subCategoryId that differs from the group row's subCategoryId
      (client-side mirror of the server-side D-05 gate — avoids a guaranteed-to-fail round trip).
    - Selection of 2+ group rows disables "Unisci" regardless of any other selected rows (D-06).
    - Whenever the selection includes 1+ group rows, the Categorizza/Elimina bulk actions become
      no-ops that show an Italian error toast instead of opening their dialogs.
  </behavior>
  <action>
    Remove the `disabled={isGrouped}` prop from the row checkbox `<input type="checkbox">` at
    L281-289 (the `checked={isGrouped ? false : isSelected}` ternary must ALSO become just
    `checked={isSelected}` — a group row can now be genuinely selected/reflected in
    `selectedIds`).
    Replace the `selectedRows`/`categorizedSubCatIds`/`mergeEligible` block (L99-103) with:
    `const selectedGroupRows = selectedRows.filter((e) => e.groupId !== null)`; `const
    selectedUngroupedRows = selectedRows.filter((e) => e.groupId === null)`; `const
    targetGroupRow = selectedGroupRows.length === 1 ? selectedGroupRows[0] : null`; `const
    conflictingSubCategory = targetGroupRow ? selectedUngroupedRows.some((e) => e.subCategoryId
    !== null && e.subCategoryId !== targetGroupRow.subCategoryId) : false`; `const
    createGroupEligible = selectedGroupRows.length === 0 && selectedUngroupedRows.length >= 2 &&
    new Set(selectedUngroupedRows.filter((e) => e.subCategoryId !== null).map((e) =>
    e.subCategoryId)).size &lt;= 1`; `const addToGroupEligible = targetGroupRow !== null &&
    selectedUngroupedRows.length >= 1 && !conflictingSubCategory`; `const mergeEligible =
    createGroupEligible || addToGroupEligible`. Compute `const targetGroup = targetGroupRow &&
    addToGroupEligible ? { id: targetGroupRow.groupId as number, title: targetGroupRow.title,
    subCategoryId: targetGroupRow.subCategoryId as number } : null` for the dialog prop.
    Update the `<MergeExpensesDialog>` invocation (L480-493): pass `targetGroup={targetGroup}`;
    when `targetGroup` is set, `selectedExpenses` must be built from `selectedUngroupedRows`
    ONLY (never include the group row's own synthetic `group:${id}` id — it is not a real
    expense id and would fail `AddExpensesToGroupSchema`'s `z.string().uuid()` validation);
    otherwise (create-group mode) keep the existing `selectedRows.map(...)` behavior unchanged.
    The `onSuccess` callback's `mergedIds` computation must also branch: in add-to-group mode,
    clear only the ungrouped ids that were added (they disappear from the table as standalone
    rows and get folded into the existing group's composed row on `router.refresh()`), NOT the
    group row's own id.
    Gate the bulk bar's Categorizza/Elimina handlers: wrap the existing
    `onBulkCategorize={() => setBulkDialogOpen(true)}` and `onBulkDelete={() =>
    setBulkDeleteOpen(true)}` (L437-438) so that when `selectedGroupRows.length > 0`, clicking
    either instead calls `toast.error('Apri il gruppo per categorizzare o eliminare le spese che
    ne fanno parte.')` and does nothing else (dialogs never open).
    In tests/expense-table-menu.test.tsx, update `'renders the grouped row checkbox as
    disabled'` (L148-154) to assert the checkbox is NOT disabled (rename the `it` description to
    reflect the new behavior, e.g. `'renders the grouped row checkbox as selectable (GRP-06
    add-to-group entry point)'`). Add a new `describe('ExpenseTable — add-to-group selection
    eligibility (GRP-06/D-06)', ...)` block — since this repo has no jsdom/interaction
    simulation, test the STATIC render output for each fixed `selectedIds`-equivalent scenario by
    rendering with different `expenses` arrays is not sufficient (selection is internal
    component state, not a prop) — instead extract the eligibility computation above into a
    small exported pure helper (e.g. `export function computeMergeEligibility(selectedRows:
    Pick&lt;ExpenseRow, 'groupId' | 'subCategoryId' | 'title'&gt;[]): { mergeEligible: boolean;
    targetGroup: {...} | null }` placed near the top of expense-table.tsx) so it is directly unit
    -testable without rendering, following the exact precedent of merge-expenses-dialog.tsx's
    exported pure functions. Test cases: 2 ungrouped same-subcategory -> createGroupEligible;
    2 ungrouped different-subcategory -> not eligible; 1 group + 1 ungrouped matching subcategory
    -> addToGroupEligible with correct targetGroup; 1 group + 1 ungrouped CONFLICTING subcategory
    -> not eligible; 2 group rows -> not eligible (D-06); 1 group + 1 uncategorized ungrouped ->
    addToGroupEligible (uncategorized never conflicts).
  </action>
  <acceptance_criteria>
    - `vitest tests/expense-table-menu.test.tsx --run` passes, including the updated checkbox test and the new computeMergeEligibility unit tests.
    - Selecting 2 group rows never enables "Unisci" (D-06) — covered by an explicit test case.
    - The Categorizza/Elimina bulk buttons never open their dialogs when any group row is selected — covered by an explicit test case (spy/mock the toast call).
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/expense-table-menu.test.tsx --run</automated>
  </verify>
  <done>Group rows are selectable; Unisci correctly routes create-group vs add-to-group vs disabled (2+ groups); Categorizza/Elimina are gated off when a group is selected; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Group-row recategorize control (GRP-05, D-01b)</name>
  <files>components/expenses/group-categorize-dialog.tsx, components/expenses/expense-table.tsx, tests/expense-table-menu.test.tsx</files>
  <read_first>
    components/expenses/expense-categorize-dialog.tsx (full file in context) — the EXACT
    analog to mirror: a thin wrapper around `SubcategoryPicker` that builds FormData and calls a
    server action, toasts, and calls `onSuccess`.
    components/expenses/expense-table.tsx — the existing `categorizeDialogExpense` state
    (L87-90), its "Da categorizzare" trigger button (L344-356), and the
    `<ExpenseCategorizeDialog>` render at the bottom (L469-478) are the pattern to mirror for the
    NEW group equivalent; the row dropdown's `{!isGrouped ? (...) : null}` branch (L390-404) is
    where the new "Cambia categoria" item is added, INSIDE the grouped branch (currently empty
    besides the always-present "Dettagli" item).
  </read_first>
  <behavior>
    - `GroupCategorizeDialog({ open, onOpenChange, group: {id, title, subCategoryId},
      categories, mostUsed, onSuccess })` renders a `SubcategoryPicker`; on pick, calls
      `categorizeExpenseGroup` with FormData `{groupId: String(id), subCategoryId}`; on success,
      toasts and calls `onSuccess`, on error toasts the message and stays open.
    - A grouped row's dropdown menu (opened via the row's "..." button) shows a "Cambia
      categoria" item that opens `GroupCategorizeDialog` scoped to that row's group.
  </behavior>
  <action>
    Create `components/expenses/group-categorize-dialog.tsx`, mirroring
    `expense-categorize-dialog.tsx` exactly: `'use client'`; import `useTransition` from react,
    `toast` from sonner, `SubcategoryPicker` from `@/components/categorization/subcategory-picker`,
    `categorizeExpenseGroup` from `@/lib/actions/expenses`, `CategoryWithSubCategories` and
    `MostUsedSubcategory` types. `Props = { open: boolean; onOpenChange: (open: boolean) => void;
    group: { id: number; title: string }; categories: CategoryWithSubCategories[]; mostUsed:
    MostUsedSubcategory[]; onSuccess: (subCategoryId?: string) => void }`. `handleChange` builds
    FormData with `groupId` = String(group.id) and `subCategoryId`, calls
    `categorizeExpenseGroup({error: null}, fd)` inside `startTransition`; on error, `toast.error`;
    on success, `toast.success('Gruppo categorizzato.')`, `onSuccess(subCategoryId)`,
    `onOpenChange(false)`. Render `<SubcategoryPicker open onOpenChange categories mostUsed
    allowedCategoryTypes={['in','out','transfer','allocation']} defaultType={null}
    onChange={handleChange} pending={isPending} />` — same allowedCategoryTypes/defaultType as
    ExpenseCategorizeDialog.
    In `expense-table.tsx`: add state `const [categorizeGroupTarget, setCategorizeGroupTarget] =
    useState&lt;{ id: number; title: string } | null&gt;(null)` near `categorizeDialogExpense`
    (L87-90). Import `GroupCategorizeDialog`. Inside the `isGrouped` dropdown branch (currently
    `null` at L404), add a `DropdownMenuItem` labeled `Cambia categoria` with `onSelect={(e) => {
    e.preventDefault(); setCategorizeGroupTarget({ id: exp.groupId as number, title: exp.title });
    setOpenDropdownId(null) }}`. Render `<GroupCategorizeDialog open={categorizeGroupTarget !==
    null} onOpenChange={(o) => { if (!o) setCategorizeGroupTarget(null) }} group={
    categorizeGroupTarget ?? { id: 0, title: '' }} categories={categories} mostUsed={mostUsed}
    onSuccess={() => { setCategorizeGroupTarget(null); router.refresh() }} />` near the existing
    `<ExpenseCategorizeDialog>` render (after L478) — `router.refresh()` is required (unlike the
    individual-expense flow) because the group row's displayed category comes from a server-side
    composed read (`composeExpenseRows`), not local state the table already tracks per-row.
    In tests/expense-table-menu.test.tsx, extend the `'ExpenseTable — grouped row rendering
    (GRP-03)'` describe block (or add a new one) with a case: a grouped row's dropdown contains
    a `Cambia categoria` entry (assert via the mocked dropdown-menu render, matching the file's
    existing DropdownMenu mock technique).
  </action>
  <acceptance_criteria>
    - `vitest tests/expense-table-menu.test.tsx --run` passes, including the new "Cambia categoria" assertion.
    - GroupCategorizeDialog never imports or references `expense.id` / `bulkCategorize` — it only ever targets a groupId via `categorizeExpenseGroup`.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/expense-table-menu.test.tsx --run</automated>
  </verify>
  <done>components/expenses/group-categorize-dialog.tsx exists and is exported; the group row's dropdown offers "Cambia categoria"; wiring calls categorizeExpenseGroup and refreshes on success; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ExpenseTable/MergeExpensesDialog (client) → server actions | groupId/expenseIds the client computes from row state are re-validated server-side (Plan 66-02) — client-side eligibility gating here is UX-only, never a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-10 | Tampering | client-side eligibility computation (computeMergeEligibility) | low | accept | This is a UX affordance only — a manipulated client bundle that bypasses the eligibility gate and calls addExpensesToGroupAction/categorizeExpenseGroup directly still hits the full IDOR + subcategory + ignored-status validation added in Plan 66-01/66-02. No new server trust is placed in this client computation. |
| T-66-11 | Information Disclosure | group row's `groupId`/`subCategoryId` exposed in table row state | low | accept | Already exposed since Phase 65 (composeExpenseRows/ExpenseRow) for the row's own owner; not new surface area introduced by this plan. |
</threat_model>

<verification>
Run `vitest tests/merge-expenses-dialog.test.tsx --run` and `vitest tests/expense-table-menu.test.tsx --run`
— both green, including all new/updated cases described above.
</verification>

<success_criteria>
- Group rows are selectable in the expenses table checkbox.
- "Unisci" correctly routes to create-group, add-to-group, or disabled (2+ group rows, D-06) based on selection composition.
- Categorizza/Elimina bulk actions no-op with a toast when a group row is selected.
- A grouped row's dropdown exposes "Cambia categoria", wired to categorizeExpenseGroup via the new GroupCategorizeDialog.
- MergeExpensesDialog's create-group flow (Phase 65) is behaviorally unchanged when targetGroup is absent.
</success_criteria>

## Artifacts this phase produces (this plan)

- `components/expenses/group-categorize-dialog.tsx`: `GroupCategorizeDialog({ open, onOpenChange, group, categories, mostUsed, onSuccess })`.
- `components/expenses/merge-expenses-dialog.tsx`: new `targetGroup?` prop on `MergeExpensesDialog`; new exported `runAddToGroupStep({ selectedExpenses, targetGroupId, targetSubCategoryId })`.
- `components/expenses/expense-table.tsx`: new exported `computeMergeEligibility(selectedRows)` pure helper; new local state `categorizeGroupTarget`.

<output>
Create `.planning/phases/66-expense-group-lifecycle/66-04-SUMMARY.md` when done
</output>
