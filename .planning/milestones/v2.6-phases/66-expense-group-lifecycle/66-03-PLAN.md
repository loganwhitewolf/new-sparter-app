---
phase: 66-expense-group-lifecycle
plan: 03
type: execute
wave: 3
depends_on: ["66-02"]
files_modified:
  - tests/expense-group-invariance.test.ts
autonomous: true
requirements: [GRP-09]

must_haves:
  truths:
    - "Group recategorization (GRP-05) writes, for every member in ONE db.transaction: expense.subCategoryId, status='3', a per-member Tier-2 classification-history row, AND expense_group.subCategoryId (dual sources in lockstep — D-02/D-09)."
    - "Add-to-group (GRP-06) enforces the same shared-subcategory gate as create; uncategorized adds get the Phase-65 softening (categorize-to-group-subcategory first, Tier-2 visible), differently-categorized adds are rejected."
    - "Remove-member (GRP-07) deletes only that membership row -> member becomes ungrouped standalone; when removal leaves exactly one member the group auto-dissolves (delete last membership + group row); dissolve deletes all membership rows + group row; all in one db.transaction scoped to userId+ownership."
    - "GRP-09 automated integration test (tests/expense-group-invariance.test.ts, node-only Vitest) snapshots dashboard aggregates (effectiveAmount sums by direction/nature/category from lib/dal/dashboard.ts's buildBreakdownData) and asserts: (A) merge leaves aggregates byte-identical; (B) dissolve leaves aggregates byte-identical to the immediately-prior state; (C) recategorize is the ONLY step that moves a total, and its delta equals recategorizing the same members individually."
  artifacts:
    - "tests/expense-group-invariance.test.ts exists and is included in the vitest.config.ts default include glob (tests/**/*.test.ts) — no config change needed."
  key_links:
    - "The test drives the REAL mergeExpenses / categorizeExpenseGroup / dissolveExpenseGroupAction exports from lib/actions/expenses.ts (Plan 66-02) and the REAL createExpenseGroup / dissolveExpenseGroup services (Plan 66-01 + Phase 65) against a stateful in-memory fake db — only the Postgres boundary is faked, the actual action/service code under test runs for real."
    - "The aggregate snapshot function calls the REAL buildBreakdownData export from lib/dal/dashboard.ts (not re-implemented) — only the SQL GROUP BY step (which requires a live Postgres to run for real) is hand-computed from the in-memory fixture, mirroring getCategoriesBreakdown's grouping semantics."
  prohibitions:
    - statement: "No grouping operation (merge / add / remove / dissolve) ever moves a transaction row."
      status: enforced
      verification: "test: tests/expense-group-invariance.test.ts asserts the transaction fixture array is reference-stable (same objects, same values) across every lifecycle step except the recategorize step's expense.subCategoryId field"
    - statement: "No grouping operation ever moves a subcategory assignment EXCEPT the explicit recategorize act (GRP-05)."
      status: enforced
      verification: "test: Assertion A (merge) and Assertion C (dissolve) below"
    - statement: "No grouping operation ever moves a dashboard total EXCEPT recategorize (whose delta == individual recategorization)."
      status: enforced
      verification: "test: Assertions A, B, C below"
    - statement: "Group-to-group merge is rejected (D-06)."
      status: verified-elsewhere
      verification: "test: covered by Plan 66-04's expense-table eligibility test, not repeated here — this plan's scope is the aggregate-invariance proof, not UI eligibility"
    - statement: "Dissolution never stores or restores a per-member pre-merge subcategory (D-09)."
      status: enforced
      verification: "test: after dissolveExpenseGroupAction, the fixture's freed member expenses still carry the RECATEGORIZED subCategoryId (not the original pre-merge one) — asserted explicitly, not just via aggregate equality"
---

<objective>
Write `tests/expense-group-invariance.test.ts` — the GRP-09 acceptance gate for Phase 66. This
single test drives the REAL `mergeExpenses`, `categorizeExpenseGroup`, `dissolveExpenseGroupAction`
(and, for the comparison scenario, `categorizeExpense`) exports against a stateful in-memory fake
`db`, then snapshots dashboard category-breakdown aggregates before/after each lifecycle step
using the REAL `buildBreakdownData` export from `lib/dal/dashboard.ts`.

Purpose: prove, by exercising the ACTUAL production code (not a re-derivation of its logic),
that merge and dissolve never move a dashboard total, and that group-recategorization's total
movement is identical to recategorizing the same expenses individually — the phase's central
safety guarantee (ADR 0017, D-09, D-10).

Output: one new test file. No production code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-expense-group-lifecycle/66-CONTEXT.md
@.planning/phases/66-expense-group-lifecycle/66-RESEARCH.md
@.planning/phases/66-expense-group-lifecycle/66-01-SUMMARY.md
@.planning/phases/66-expense-group-lifecycle/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GRP-09 invariance test — merge / recategorize / dissolve leave dashboard aggregates provably correct</name>
  <files>tests/expense-group-invariance.test.ts</files>
  <read_first>
    tests/expense-group-service.test.ts (full file, already read in Plan 66-01/66-02 context) —
    `makeDbOrTx` L41-84 is the STATIC (canned-return) precedent for table-identity dispatch; this
    task needs a STATEFUL variant (mutations from one call must be visible on the next call).
    tests/expense-actions.test.ts (full file) — per-describe-block `mocks.dbTransaction.mockImplementation`
    pattern (e.g. L246-265) confirms `db.transaction(fn)` is mocked by directly invoking `fn(tx)`
    — no real transactional semantics, just a pass-through, which this task's fake db must also
    provide.
    lib/dal/dashboard.ts — `buildBreakdownData(rows: BreakdownAggregateRow[])` (L584-641, pure,
    no db access) and `DASHBOARD_TOTAL_EXPENSE_STATUSES = ['1','2','3']` (L273) and
    `getCategoriesBreakdown`'s live query shape (L970-1029) — the exact GROUP BY this task's
    snapshot helper must mirror by hand: group by (categoryId, subCategoryId), amount =
    abs(sum(transaction.amount)) for transactions whose expense.status is in
    DASHBOARD_TOTAL_EXPENSE_STATUSES and expense.subCategoryId is not null, count = count
    distinct expense.id.
    lib/actions/expenses.ts — mergeExpenses (L438-506), categorizeExpense (L303-376),
    categorizeExpenseGroup and dissolveExpenseGroupAction (Plan 66-02, read from
    66-02-SUMMARY.md for final signatures/behavior if it differs from the plan text).
    lib/services/expense-group.ts — createExpenseGroup (L50-101) and dissolveExpenseGroup
    (Plan 66-01, read from 66-01-SUMMARY.md for final signature).
    lib/dal/categories.ts isSubCategoryVisibleToUser (L350-365, signature only — this test mocks
    the whole `@/lib/dal/categories` module to always resolve true).
  </read_first>
  <action>
    Build the test in these parts, in order:

    1. Mocks (module scope, before any import): `vi.mock('server-only', () => ({}))`;
       `vi.mock('@/lib/dal/auth', () => ({ verifySession: vi.fn() }))` (resolves `{ userId:
       FIXTURE_USER_ID }` in beforeEach); `vi.mock('@/lib/dal/categories', () => ({
       isSubCategoryVisibleToUser: vi.fn().mockResolvedValue(true) }))`;
       `vi.mock('@/lib/dal/classification-history', () => ({ writeClassificationHistory:
       vi.fn().mockResolvedValue(undefined) }))`; `vi.mock('@/lib/actions/revalidation', () => ({
       revalidateCategorizationSurfaces: vi.fn() }))`; `vi.mock('drizzle-orm', ...)` reusing the
       EXACT operator-mock shape from tests/expense-group-dal.test.ts (eq/and/or/inArray/isNull/
       sql all returning plain `{op, ...}` descriptor objects — this is what makes the fake db's
       generic WHERE-interpreter possible); `vi.mock('@/lib/db/schema', () => ({...}))` with
       string-keyed columns (`'expense.id'`, `'expense.userId'`, `'expense.subCategoryId'`,
       `'expense.status'`, `'expense.updatedAt'`, `'expenseGroup.id'`, `'expenseGroup.userId'`,
       `'expenseGroup.subCategoryId'`, `'expenseGroup.updatedAt'`,
       `'expenseGroupMembership.id'`, `'.groupId'`, `'.expenseId'`) for every table this test's
       action calls touch — reuse the identical string convention already used in
       tests/expense-group-dal.test.ts and tests/expense-actions.test.ts.

    2. Build `createFixtureDb()` — a factory (NOT a module-level singleton, so the "grouped
       path" and "individual comparison path" scenarios get fully independent state) that holds
       closured mutable arrays `expenses: Array<{id, userId, subCategoryId, status}>`,
       `groups: Array<{id, userId, subCategoryId}>`, `memberships: Array<{groupId, expenseId}>`,
       and returns a `db`-shaped object: `{ transaction: (fn) => fn(txHandle), select, update,
       insert, delete }` where `txHandle` exposes the same four methods bound to the SAME
       closured arrays (so nested tx calls see prior tx writes). Implement:
       - `select(cols?)` returns a chainable object supporting `.from(table)`, `.innerJoin(table,
         cond)`, `.where(cond)`, `.limit(n)`, and is `.then()`-able (thenable, matching
         tests/expense-group-dal.test.ts's `makeSelectChain`) — resolving to the filtered
         (and, for `.innerJoin(expense, eq(expense.id, expenseGroupMembership.expenseId))`
         specifically, joined) rows from the array identified by `table` (identity comparison
         against the imported `expense`/`expenseGroup`/`expenseGroupMembership` mocks, exactly
         like `tests/expense-group-service.test.ts`'s `makeDbOrTx`). A small `evalCond(row,
         cond)` recursive evaluator interprets the mocked drizzle-orm descriptor tree
         (`and`/`or`/`eq`/`inArray`/`isNull`), resolving a column-ref string like
         `'expense.userId'` to `row.userId` by stripping the table prefix.
       - `update(table)` returns `{ set: (patch) => ({ where: (cond) => ({ returning: (cols) =>
         { mutate matching rows in place, in the array for `table`, with `patch`; resolve the
         matched rows' `id`s } }) }) }`.
       - `insert(table)` returns `{ values: (rowsOrRow) => ({ returning: (cols) => { push new
         row(s) into the array for `table`, resolving the inserted row(s); for
         expenseGroupMembership, throw a `23505`-caused Error (matching errorCauseCode's
         expected `{ cause: { code: '23505' } }` shape) when a pushed row's expenseId already
         exists in the array — this is what lets the REAL createExpenseGroup/addExpensesToGroup
         race-translation code path be exercised for real if ever hit } })}` — return value
         from `.values()` without `.returning()` must ALSO work (a bare `await
         .insert(...).values(...)` with no `.returning()` chained, matching
         createExpenseGroup's membership insert, which does not call `.returning()`).
       - `delete(table)` returns `{ where: (cond) => { splice matching rows out of the array for
         `table` } }`.
       Expose `db.expenses`/`db.groups`/`db.memberships` (or a returned `{ db, expenses, groups,
       memberships }` tuple) so the test can seed fixture rows directly and read them back for
       assertions, without going through the query interpreter.

    3. `vi.mock('@/lib/db', ...)` cannot be table-scoped per-test with a factory this way since
       `vi.mock` is hoisted and module-scoped — resolve this by having the mock return a mutable
       `let currentDb: ReturnType&lt;typeof createFixtureDb&gt;` object whose methods PROXY to
       `currentDb.<method>` at call time (so each test/`beforeEach` can reassign `currentDb =
       createFixtureDb()` to get a fresh, independent fixture) — same technique as
       `vi.hoisted(() => ({ ... }))` used throughout this repo's existing DAL tests.

    4. Local `snapshotBreakdown(fixtureExpenses, fixtureTransactions, subCategoryLookup)` helper:
       for each transaction whose owning expense has `status` in `['1','2','3']` and non-null
       `subCategoryId`, resolve `(categoryId, categoryName, categorySlug, categoryType,
       subCategoryId, subCategoryName, subCategorySlug)` via `subCategoryLookup` (a small fixture
       map you define — 3 subcategories across 2 categories is enough: e.g. `CAT_A_SUB` under
       "Casa"/out, `CAT_B_SUB` under "Svago"/out (a stable control category never touched),
       `CAT_C_SUB` under "Casa"/out — the recategorize TARGET), accumulate `amount` (sum of
       `Math.abs(Number(tx.amount))` per bucket, then format via `toDbDecimal`/`toDecimal` from
       `@/lib/utils/decimal` for parity with the real SQL's `::text` cast) and `count` (count of
       distinct expense ids per bucket) into a `BreakdownAggregateRow[]`, then return
       `buildBreakdownData(rows)` (the REAL import from `@/lib/dal/dashboard`, NOT mocked).
       Use `JSON.stringify(...)` on the returned array for all equality assertions (D-10's
       "byte-identical" requirement).

    5. Import the REAL modules under test (no mocking): `mergeExpenses`, `categorizeExpense`,
       `categorizeExpenseGroup`, `dissolveExpenseGroupAction` from `@/lib/actions/expenses`;
       `buildBreakdownData` from `@/lib/dal/dashboard`.

    6. Scenario A — "grouped path", ONE `it(...)`:
       - Seed a fresh fixture: 3 expenses (`exp-1`/`exp-2`/`exp-3`, all `userId: FIXTURE_USER_ID`,
         `subCategoryId: CAT_A_SUB`, `status: '3'`) each with one transaction of amount
         `-30.00`/`-20.00`/`-10.00`; plus 1 control expense (`exp-control`, `subCategoryId:
         CAT_B_SUB`, `status: '3'`) with its own `-15.00` transaction — proves the invariance
         doesn't accidentally touch an unrelated category.
       - snapshotT0 = snapshotBreakdown(...). Assert CAT_A total is `60.00` and CAT_B total is
         `15.00` (fixture sanity check).
       - Call the REAL `mergeExpenses({error: null}, formDataWith({selectedExpenseIds:
         JSON.stringify(['exp-1','exp-2','exp-3']), groupTitle: 'Amazon condiviso'}))`. Assert
         `result.error === null`. Capture the inserted groupId from the fixture's `groups` array.
       - snapshotAfterMerge = snapshotBreakdown(...). **Assertion A:**
         `JSON.stringify(snapshotAfterMerge) === JSON.stringify(snapshotT0)` — merge is
         byte-identical to pre-merge.
       - Call the REAL `categorizeExpenseGroup({error: null}, formDataWith({groupId: String(
         insertedGroupId), subCategoryId: String(CAT_C_SUB)}))`. Assert `result.error === null`.
       - snapshotT1 = snapshotBreakdown(...). **Assertion B (part 1):**
         `JSON.stringify(snapshotT1) !== JSON.stringify(snapshotT0)` — recategorize DOES move a
         total (CAT_A drops to `0.00`, CAT_C gains `60.00`, CAT_B unchanged at `15.00`).
       - Call the REAL `dissolveExpenseGroupAction({error: null}, formDataWith({groupId: String(
         insertedGroupId)}))`. Assert `result.error === null`.
       - snapshotT2 = snapshotBreakdown(...). **Assertion C:**
         `JSON.stringify(snapshotT2) === JSON.stringify(snapshotT1)` — dissolve is byte-identical
         to the immediately-prior (post-recategorize) state.
       - **D-09 structural assertion:** read `exp-1`/`exp-2`/`exp-3` back from the fixture's
         `expenses` array directly — assert `subCategoryId === CAT_C_SUB` for all three (freed
         members keep the RECATEGORIZED category, nothing reverted) and assert the fixture's
         `memberships`/`groups` arrays contain zero rows for the dissolved groupId.

    7. Scenario B — "individual comparison path", a SECOND independent `it(...)` using a FRESH
       `createFixtureDb()` (via a fresh `beforeEach`-style reset or a locally-scoped fixture
       inside this test) seeded IDENTICALLY to Scenario A's starting state (3 expenses at
       CAT_A_SUB + 1 control at CAT_B_SUB, never grouped): call the REAL `categorizeExpense`
       three times, once per exp-1/exp-2/exp-3, each with `subCategoryId: String(CAT_C_SUB)` —
       assert every call resolves `{ error: null }`. snapshotIndividual =
       snapshotBreakdown(...). **Assertion B (part 2, the actual GRP-09 "no hidden movement"
       proof):** `JSON.stringify(snapshotIndividual) === JSON.stringify(snapshotT1)` (snapshotT1
       captured from Scenario A) — the grouped recategorization's aggregate effect is IDENTICAL
       to recategorizing the same three expenses one at a time.

    Name the two scenarios clearly, e.g. `describe('GRP-09: merge -> recategorize -> dissolve
    invariance', ...)` with the two `it` blocks inside, so a failing assertion's test name
    self-documents which of A/B/C broke.
  </action>
  <acceptance_criteria>
    - `vitest tests/expense-group-invariance.test.ts --run` exits 0 with both `it` blocks passing (Assertions A, B, C, and the D-09 structural assertion all hold).
    - The test imports and calls the REAL `mergeExpenses`, `categorizeExpenseGroup`, `dissolveExpenseGroupAction`, `categorizeExpense` from `@/lib/actions/expenses` and the REAL `buildBreakdownData` from `@/lib/dal/dashboard` — none of these four are mocked.
    - Deliberately breaking the invariant locally (e.g. temporarily commenting out `dissolveExpenseGroup`'s membership delete in a scratch run) causes Assertion C to fail — confirms the test is not vacuously true. (Verify this manually during development; do not leave the production code altered.)
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/vitest tests/expense-group-invariance.test.ts --run</automated>
  </verify>
  <done>tests/expense-group-invariance.test.ts exists, passes, and exercises the real merge/recategorize/dissolve/individual-recategorize action code against a stateful in-memory db, proving GRP-09's three invariance assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (test-only file) | This plan adds no production code and introduces no new runtime trust boundary — it exercises Plan 66-01/66-02's already-threat-modeled action/service code against a fixture db. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-66-09 | N/A | tests/expense-group-invariance.test.ts | low | accept | Test-only code, never shipped/executed in production; no new attack surface. Its value is verification of Plan 66-01/66-02's production mitigations, not a mitigation itself. |
</threat_model>

<verification>
`vitest tests/expense-group-invariance.test.ts --run` exits 0. This is the phase's stated
acceptance gate — Phase 66 is not complete until this file is green.
</verification>

<success_criteria>
- tests/expense-group-invariance.test.ts exists, is picked up by vitest.config.ts's default include glob, and passes.
- Assertions A (merge no-op), B (recategorize moves totals AND matches individual recategorization), and C (dissolve restores pre-dissolve state) all hold against the real action/service code.
- D-09's "no pre-merge subcategory stored/restored" claim is asserted directly on fixture row state, not just inferred from aggregate equality.
</success_criteria>

## Artifacts this phase produces (this plan)

- `tests/expense-group-invariance.test.ts` — the GRP-09 acceptance test; internal (non-exported) helpers `createFixtureDb()` and `snapshotBreakdown(...)` live only in this file.

<output>
Create `.planning/phases/66-expense-group-lifecycle/66-03-SUMMARY.md` when done
</output>
