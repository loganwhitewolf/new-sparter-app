---
phase: 70-dashboard-tag-filter-removal
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - lib/dal/overview.ts
  - lib/dal/dashboard.ts
  - tests/overview-dal.test.ts
  - tests/dashboard-dal.test.ts
  - components/dashboard/tag-filter-select.tsx
  - tests/tag-filter-select.test.tsx
  - lib/validations/dashboard.ts
  - tests/dashboard-filters.test.ts
  - lib/dal/tags.ts
autonomous: false
requirements: [TAG-13]
must_haves:
  truths:
    - "getOverview, getOverviewChart and getMonthOverMonthCategoryChanges in lib/dal/overview.ts expose no tag parameter and the module no longer imports the tag-scoping predicate (TAG-13)."
    - "getUncategorizedCount, getOverviewAmountTotals, getCategoryRanking, getCategoryDetail and the deviations input type in lib/dal/dashboard.ts expose no tag parameter and the module no longer imports the tag-scoping predicate (TAG-13)."
    - "The tag-select component, its test, parseTagIdParam and its test block no longer exist anywhere; grep across app/lib/components/tests returns zero hits for TagFilterSelect, parseTagIdParam and no-data-for-tag."
    - "REGRESSION FENCE HOLDS: lib/dal/transaction-tags-sql.ts is byte-unchanged and tagScopedTransactions survives in exactly three lib/ files — transaction-tags-sql.ts (definition), transactions.ts (import + call), tags.ts (one comment). /transactions?tag= and the Phase 71 toolbar tag filter (TAG-14) still filter."
    - "resolveOwnedTagId still exists in lib/dal/tags.ts and is still called by app/(app)/transactions/page.tsx; ONLY its docstring framing changes (from the dashboard filter to the transactions filter)."
    - "Deleted tests are deleted, not skipped (D4); no surviving dashboard assertion was edited, because every removed parameter was undefined on the default path (D3)."
  artifacts:
    - "lib/dal/overview.ts — three tag-free exported signatures, five predicate call sites removed, import removed."
    - "lib/dal/dashboard.ts — four tag-free exported signatures plus the deviations input type, eight predicate call sites removed, import removed."
    - "tests/overview-dal.test.ts and tests/dashboard-dal.test.ts — the six tag-threading describes and their now-orphaned harness removed; every other describe untouched."
    - "components/dashboard/tag-filter-select.tsx and tests/tag-filter-select.test.tsx — deleted from disk."
    - "lib/validations/dashboard.ts — parseTagIdParam and its docstring removed; parseDashboardFilters and the zod schemas untouched."
    - "lib/dal/tags.ts — resolveOwnedTagId intact, docstring reframed around the transactions ?tag= filter."
  key_links:
    - "lib/dal/overview.ts + lib/dal/dashboard.ts -> lib/dal/transaction-tags-sql.ts: the two dashboard imports are cut; the transactions.ts import of the same module is the one that must survive."
    - "lib/dal/tags.ts resolveOwnedTagId -> app/(app)/transactions/page.tsx: the surviving consumer that makes the function (and its guard) still load-bearing."
    - "tests/overview-dal.test.ts vi.mock of the tag predicate -> lib/dal/overview.ts import: once the import is gone the module mock is dead and must be removed with it."
---

<objective>
Finish TAG-13: strip the now-unreachable `tagId` parameters and tag-scoping predicate calls out of
the two dashboard DAL modules, delete the orphaned tag-select component and `parseTagIdParam`
validator together with their tests, reframe the one surviving docstring, and then prove — by grep,
by the full suite, and by a human regression check — that the transactions tag filter is still alive.

**REGRESSION FENCE (LOCKED — the single most important constraint in this phase):**
`lib/dal/transaction-tags-sql.ts` and its `tagScopedTransactions` export MUST SURVIVE UNCHANGED.
That helper still powers `/transactions?tag=` (`lib/dal/transactions.ts`, the
`conditions.push(tagScopedTransactions(filters.tagId))` line) and the Phase 71 transactions toolbar
tag filter (TAG-14) that shipped days ago. Only the DASHBOARD callers lose their parameters and
calls. Likewise `resolveOwnedTagId` in `lib/dal/tags.ts` MUST SURVIVE — the transactions page calls
it — and only its docstring framing changes. **A change that deletes the helper, the file, or the
function is WRONG and must be reverted, not patched forward.**

Purpose: leave no dead optional parameter that would let the period-scoped tag filter be re-wired
by accident, while keeping the tag NAVIGATION filter on the transactions list fully intact.
Output: tag-free dashboard DAL, orphans deleted, regression proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-dashboard-tag-filter-removal/CONTEXT.md
@.planning/phases/70-dashboard-tag-filter-removal/70-01-SUMMARY.md
@CLAUDE.md

# Files this plan edits (READ before editing):
@lib/dal/overview.ts
@lib/dal/dashboard.ts
@lib/validations/dashboard.ts
@lib/dal/tags.ts

# KEEP — read to confirm the fence, do NOT modify:
#   lib/dal/transaction-tags-sql.ts  -> the tagScopedTransactions definition (regression-critical)
#   lib/dal/transactions.ts          -> the surviving import + call site
#   app/(app)/transactions/page.tsx  -> the surviving resolveOwnedTagId caller
#   tests/transaction-tags-sql.test.ts, tests/transactions-dal.test.ts -> the surviving coverage
</context>

<!-- planner-discipline-allow: TagFilterSelect -->
<!-- planner-discipline-allow: parseTagIdParam -->
<!-- planner-discipline-allow: no-data-for-tag -->
<!-- planner-discipline-allow: tagScopedTransactions -->

<tasks>

<task type="auto">
  <name>Task 1: de-thread tagId from the two dashboard DAL modules (REGRESSION-CRITICAL)</name>
  <files>lib/dal/overview.ts, lib/dal/dashboard.ts, tests/overview-dal.test.ts, tests/dashboard-dal.test.ts</files>
  <action>
    STEP 0 (mandatory, before editing): re-verify the call-site map with
    `/usr/bin/grep -rn "tagScopedTransactions" lib/` and `/usr/bin/grep -n "tagId" lib/dal/overview.ts lib/dal/dashboard.ts`.
    Use `/usr/bin/grep`, never bare `grep` — the repo's RTK shell hook rewrites `grep` and returns a
    summarised, falsified line set that is unsafe to plan edits against. Confirm before you start
    that `lib/dal/transaction-tags-sql.ts`, `lib/dal/transactions.ts` and `lib/dal/tags.ts` are in the
    hit set: those three are the SURVIVORS and are not to be edited in this task.

    (a) lib/dal/overview.ts — remove the tag threading only:
      - Delete the `import { tagScopedTransactions } from '@/lib/dal/transaction-tags-sql'` line
        (this file's import, not the one in transactions.ts).
      - `getOverview`: drop the trailing optional tag parameter, and drop the trailing argument from
        the two `getOverviewAmountTotals(...)` and two `getUncategorizedCount(...)` calls.
      - `getMonthOverMonthCategoryChanges`: drop the trailing optional tag parameter (the
        `limit = 10` default parameter STAYS and becomes the last one), and delete the predicate call
        from all four `and(...)` blocks — two in the allocation-grain branch, two in the in/out-grain
        branch. Fix the trailing commas left behind in each `and(...)`.
      - `getOverviewChart`: drop the trailing optional tag parameter and delete the predicate call
        from its single `and(...)` block.
      - Trim the v2.6/68-0x sentences in the surrounding comments that describe the tag narrowing.
        Keep every other explanatory comment (D-06 year-crossing, D-07 noise floor, D-08 isNew,
        WR-06 de-dup keying, T-42-05 session scoping) verbatim.
      - Do NOT touch `effectiveAmount`, `isNotSecondary`, `dateScopedTransactions` or any other
        predicate — only the tag one goes.

    (b) lib/dal/dashboard.ts — same treatment:
      - Delete this module's import of the tag-scoping predicate.
      - `getUncategorizedCount(userId, from, to)` and `getOverviewAmountTotals(userId, from, to)`:
        drop the trailing optional tag parameter and the predicate call in each `and(...)`.
      - `getCategoryRanking(filters)`: drop the trailing optional tag parameter and its predicate call.
      - The deviations input object type: delete the optional tag field from the type, and delete the
        two predicate calls that read it (the reference query and the baseline query). Keep the
        `type` and `categoryId` fields.
      - `getCategoryDetail(categoryId, filters)`: drop the trailing optional tag parameter and delete
        the predicate call from all three data queries (trend, subcategory breakdown, top
        transactions). The category-metadata lookup never carried one.
      - Fix trailing commas in every touched `and(...)`.

    (c) tests/overview-dal.test.ts — delete the three tag-threading describes (the ones titled for
      getOverview, getMonthOverMonthCategoryChanges and getOverviewChart, all tagged 68-03/TAG-04)
      and, because the module import is gone, delete the `vi.mock('@/lib/dal/transaction-tags-sql', ...)`
      block and the corresponding spy field + its comment from the hoisted mocks object. KEEP the
      categorySlug describes and the `rowsQueue` harness they depend on. Then re-grep `whereArgs` in
      this file: if its only remaining occurrences are the harness itself (declaration, the recorder
      push, the beforeEach reset, the header comment), delete those too; if any surviving assertion
      still reads it, leave the harness in place.

    (d) tests/dashboard-dal.test.ts — delete the three tag-threading describes (the ones for
      getUncategorizedCount/getOverviewAmountTotals, getCategoryRanking/getCategoryDeviations, and
      getCategoryDetail) and the two module-level helper functions that locate the EXISTS fragment,
      which are used by nothing else (verified). Then apply the same `whereArgs` re-grep rule as in
      (c). KEEP `rowsQueue`: it is wired into the shared `select` mock chain used by every test in the
      file, not just the deleted ones. Trim the tag mention from the file's header comment.

    (e) HARD KEEP — assert, do not edit: `lib/dal/transaction-tags-sql.ts`,
      `lib/dal/transactions.ts`, `tests/transaction-tags-sql.test.ts`, `tests/transactions-dal.test.ts`
      and everything under `app/(app)/transactions/`. If a type error appears to originate in any of
      them, you have removed too much — revert and narrow the edit.

    Do NOT leave tombstone comments naming the removed symbols.
  </action>
  <verify>
    <automated>cd /Users/andreabernardini/ai-projects/new-sparter-app && ! /usr/bin/grep -rn "tagScopedTransactions" lib/dal/overview.ts lib/dal/dashboard.ts && ! /usr/bin/grep -rn "tagId" lib/dal/overview.ts lib/dal/dashboard.ts && /usr/bin/grep -q "export function tagScopedTransactions" lib/dal/transaction-tags-sql.ts && /usr/bin/grep -q "tagScopedTransactions(filters.tagId)" lib/dal/transactions.ts && [ "$(/usr/bin/grep -rl 'tagScopedTransactions' lib/ | LC_ALL=C sort | tr '\n' ',')" = "lib/dal/tags.ts,lib/dal/transaction-tags-sql.ts,lib/dal/transactions.ts," ] && [ -z "$(git diff --name-only c26b48c26b8feb483bb5b8f36c65a3593aa0083c -- lib/dal/transaction-tags-sql.ts lib/dal/transactions.ts)" ] && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run && ./node_modules/.bin/eslint lib/dal/overview.ts lib/dal/dashboard.ts tests/overview-dal.test.ts tests/dashboard-dal.test.ts && yarn check:language</automated>
  </verify>
  <done>
    Neither dashboard DAL module mentions a tag id or imports the tag-scoping predicate. The
    predicate survives in exactly three lib/ files — transaction-tags-sql.ts (definition),
    transactions.ts (import + call), tags.ts (one comment) — and `git diff` proves
    transaction-tags-sql.ts and transactions.ts are byte-unchanged. `tsc --noEmit` clean, full vitest
    suite green with no surviving assertion edited (D3), ESLint clean on the four touched files,
    `yarn check:language` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: delete the orphaned control + validator with their tests; reframe the surviving docstring</name>
  <files>components/dashboard/tag-filter-select.tsx, tests/tag-filter-select.test.tsx, lib/validations/dashboard.ts, tests/dashboard-filters.test.ts, lib/dal/tags.ts</files>
  <action>
    Everything here is orphaned by plan 70-01 and task 1 above. Re-verify first with
    `/usr/bin/grep -rn "TagFilterSelect|buildTagFilterSearch|parseTagIdParam|no-data-for-tag" app lib components tests -E`
    (`/usr/bin/grep`, not the RTK-hooked `grep`) and confirm the only remaining hits are the files
    listed below plus the one docstring reference in `lib/validations/dashboard.ts`.

    (a) Delete `components/dashboard/tag-filter-select.tsx` from disk. It exports the tag-select
      component and a search-string helper; after plan 70-01 nothing imports either (the helper is
      referenced only by the component itself and by the test being deleted next).

    (b) Delete `tests/tag-filter-select.test.tsx` from disk (D4 — deleted, not skipped).

    (c) lib/validations/dashboard.ts — delete `parseTagIdParam` together with its whole JSDoc block.
      Leave `DashboardPresetSchema`, `DashboardTypeSchema`, `DashboardSortSchema`,
      `DashboardFiltersSchema`, the exported types and `parseDashboardFilters` exactly as they are.

    (d) tests/dashboard-filters.test.ts — delete the `describe` block covering that parser and remove
      the symbol from the import statement at the top, keeping `parseDashboardFilters`. Every other
      describe in the file stays untouched.

    (e) lib/dal/tags.ts — KEEP `resolveOwnedTagId`: the function body, signature, fail-closed
      behaviour and its call from `app/(app)/transactions/page.tsx` are all unchanged. ONLY rewrite
      its JSDoc so the guard is framed around the transactions `?tag=` filter (TAG-14) instead of the
      dashboard filter: keep the structural-ownership explanation (the predicate only narrows rows
      already scoped by the session user, so a foreign id matches zero rows) and the fail-closed
      note, and delete the sentence about "every RSC page reading ?tag= in Wave 3/4 of this phase",
      which describes a phase that no longer exists. State plainly that the transactions page is the
      caller. Do NOT touch `getTag`, `getTags`, `updateTagRow`, `archiveTagRow` or the surviving
      comment mentioning the tag-scoping predicate on that same page.
  </action>
  <verify>
    <automated>cd /Users/andreabernardini/ai-projects/new-sparter-app && [ ! -f components/dashboard/tag-filter-select.tsx ] && [ ! -f tests/tag-filter-select.test.tsx ] && ! /usr/bin/grep -rnE "TagFilterSelect|buildTagFilterSearch|parseTagIdParam|no-data-for-tag" app lib components tests && /usr/bin/grep -q "export async function resolveOwnedTagId" lib/dal/tags.ts && /usr/bin/grep -q "resolveOwnedTagId" "app/(app)/transactions/page.tsx" && [ "$(/usr/bin/grep -rl 'tagScopedTransactions' lib/ | LC_ALL=C sort | tr '\n' ',')" = "lib/dal/tags.ts,lib/dal/transaction-tags-sql.ts,lib/dal/transactions.ts," ] && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run && ./node_modules/.bin/eslint lib/validations/dashboard.ts tests/dashboard-filters.test.ts lib/dal/tags.ts && yarn check:language</automated>
  </verify>
  <done>
    Both orphan files are gone from disk; grep across app/lib/components/tests returns ZERO hits for
    the tag-select component, its search helper, the candidate-id parser and the tag empty-state
    variant. `resolveOwnedTagId` still exists and is still called by the transactions page; the tag
    predicate still lives in exactly the three expected lib/ files. `tsc --noEmit` clean, full vitest
    suite green, ESLint clean on the touched files, `yarn check:language` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: human-verify the removal and the transactions-filter regression</name>
  <what-built>The period-scoped dashboard tag filter is fully removed: no control on /dashboard/overview or /dashboard/categories, no ?tag= read on any of the three dashboard pages (including the category drill-down), no tag threading through the dashboard DAL, and the orphaned component/validator and their tests deleted. The transactions tag filter (TAG-14) and /tags/[id] were deliberately left untouched.</what-built>
  <how-to-verify>
    Run the dev server, then:

    REMOVAL (TAG-13)
    1. Open /dashboard/overview. Confirm there is no tag control next to the year selector, and the
       KPI cards / chart / movers show the same numbers as before this phase (compare against a
       screenshot or a pre-phase deploy if you have one).
    2. Open /dashboard/categories and /dashboard/categories/{any category}. Confirm neither shows a
       tag control and both render their normal rankings/details.
    3. LEGACY URL, D1 — visit each of these three URLs with a real tag id appended:
       /dashboard/overview?tag=<id>, /dashboard/categories?tag=<id>,
       /dashboard/categories/<categoryId>?tag=<id>.
       Each must render the NORMAL, UNFILTERED page: no redirect, no error, no "no transactions with
       this tag" empty state, and the numbers must equal the same URL without ?tag=.
    4. Pick a year with no data and open /dashboard/overview?year=<that year>&tag=<id>. Confirm the
       empty state is the ordinary "no data for this year" message, not a tag-specific one.

    REGRESSION — THIS IS THE KEY CHECK
    5. Open /transactions. Open the "Filtri" popover/sheet and confirm the "Tag" select is still
       there. Select a tag: the URL gains ?tag=<id>, the table narrows to that tag's transactions,
       and an active chip shows the tag NAME. Clear it via the chip X and via "Cancella tutto".
    6. Visit /transactions?tag=<id> directly. Confirm it still filters.
    7. Open /dashboard/tags and click through a tag to /tags/[id]. Confirm the dedicated all-time page
       still renders its totals, per-category breakdown and transaction list (Phase 69, untouched).

    If any of steps 5-7 fails, the regression fence was crossed — the shared tag-scoping predicate or
    resolveOwnedTagId was removed. Report it rather than approving.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what broke (e.g. transactions tag filter gone, a dashboard page 500s on ?tag=, numbers moved).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> URL `?tag=` -> transactions RSC -> DAL | The SURVIVING crossing. A client-supplied tag id still reaches `getTransactions` via `resolveOwnedTagId` + the tag-scoping predicate. This plan must not weaken it. |
| dashboard DAL -> SQL WHERE clause | The removed crossing. After this plan no dashboard query can be narrowed by a client-supplied tag id, because the parameters no longer exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-04 | Tampering (regression) | `lib/dal/transaction-tags-sql.ts` -> `/transactions?tag=` | high | mitigate | Deleting the shared predicate (or its file) while removing the dashboard callers would silently un-narrow the transactions list and the TAG-14 toolbar filter, returning the user's whole transaction set where a tagged subset was requested. Mitigated by: the explicit HARD KEEP in task 1(e); an exact-survivor grep gate asserting the predicate appears in exactly `lib/dal/tags.ts`, `lib/dal/transaction-tags-sql.ts`, `lib/dal/transactions.ts`; a `git diff --name-only` gate asserting the two survivor source files are byte-unchanged; the surviving unit tests (`tests/transaction-tags-sql.test.ts`, `tests/transactions-dal.test.ts`) in the full-suite run; and human-verify steps 5-6. |
| T-70-05 | Tampering (IDOR) | `resolveOwnedTagId` -> `app/(app)/transactions/page.tsx` | medium | mitigate | Deleting the ownership guard along with its dashboard callers would drop the belt-and-suspenders IDOR check on the transactions page. Mitigated by task 2(e) restricting the edit to the docstring, plus grep gates asserting both the export and its transactions-page call site still exist, plus the surviving guard tests in `tests/tags-dal.test.ts`. |
| T-70-06 | Tampering (IDOR) | dashboard `?tag=` -> DAL | low | mitigate | Surface eliminated: the parameters no longer exist, so no dashboard query can be narrowed by client input. Grep gate proves zero references. |
| T-70-07 | Information Disclosure | dashboard aggregates | low | accept | Removing an optional filter widens each dashboard aggregate back to the session user's full data set — data the same user already sees unfiltered by default. No cross-user exposure; `verifySession()` scoping is unchanged in every touched query. |

No package-manager installs in this phase (no `## Package Legitimacy Audit` required).
</threat_model>

<verification>
All gates use the DIRECT binaries and `/usr/bin/grep` — this repo's RTK shell hook rewrites `grep`
and `npx`, returning summarised/falsified output that must not be trusted for a gate decision.

Removal gates (must return nothing):
- `! /usr/bin/grep -rnE "TagFilterSelect|buildTagFilterSearch|parseTagIdParam|no-data-for-tag" app lib components tests`
- `! /usr/bin/grep -rn "tagId" lib/dal/overview.ts lib/dal/dashboard.ts`
- `[ ! -f components/dashboard/tag-filter-select.tsx ] && [ ! -f tests/tag-filter-select.test.tsx ]`

Survivor gates (must all hold):
- `[ "$(/usr/bin/grep -rl 'tagScopedTransactions' lib/ | LC_ALL=C sort | tr '\n' ',')" = "lib/dal/tags.ts,lib/dal/transaction-tags-sql.ts,lib/dal/transactions.ts," ]`
  — i.e. definition, transactions.ts import + call, tags.ts comment; nothing else in lib/.
- `git diff --name-only c26b48c26b8feb483bb5b8f36c65a3593aa0083c -- lib/dal/transaction-tags-sql.ts lib/dal/transactions.ts` is EMPTY.
  (Anchored to the phase-base commit, NOT the working tree: GSD commits per task, so a working-tree-only
  check would let a mistaken edit committed in an earlier task pass silently — plan-checker W1.)
- `/usr/bin/grep -q "export async function resolveOwnedTagId" lib/dal/tags.ts`
- `/usr/bin/grep -q "resolveOwnedTagId" "app/(app)/transactions/page.tsx"`

Suite gates:
- `./node_modules/.bin/vitest run` — full suite green, including `tests/transaction-tags-sql.test.ts`
  and the `tests/transactions-dal.test.ts` tag-narrowing case (the automated half of the regression
  check), with NO edits to surviving assertions (D3).
- `./node_modules/.bin/tsc --noEmit` — clean.
- `./node_modules/.bin/eslint <touched files>` — clean.
- `yarn check:language` — passes.

Human gate: task 3 (blocking) — legacy `?tag=` degradation on all three dashboard pages, plus the
`/transactions?tag=` and toolbar tag-filter regression.
</verification>

<success_criteria>
- No dashboard DAL function accepts a tag parameter and neither dashboard DAL module imports the
  tag-scoping predicate (TAG-13).
- The tag-select component, its search helper, `parseTagIdParam` and the tag empty-state variant have
  zero references across app/lib/components/tests; their tests are deleted, not skipped (D4).
- `tagScopedTransactions` survives in exactly three `lib/` files and its definition file plus
  `lib/dal/transactions.ts` are byte-unchanged; `/transactions?tag=` and the TAG-14 toolbar filter
  still filter (automated + human-verified).
- `resolveOwnedTagId` survives with only a docstring change and is still called by the transactions
  page.
- A dashboard URL carrying a legacy `?tag=` renders the normal all-transactions dashboard — no
  redirect, no error, no tag empty state (D1) — and dashboard numbers equal the pre-existing
  unfiltered values with no surviving assertion edited (D3).
- `/dashboard/tags` and `/tags/[id]` are unchanged and still reachable.
</success_criteria>

<output>
Create `.planning/phases/70-dashboard-tag-filter-removal/70-02-SUMMARY.md` when done.
</output>
