---
phase: 41-collapsible-sidebar
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - components/layout/app-shell.tsx
  - components/layout/sidebar.tsx
  - app/(app)/layout.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, D-13]
must_haves:
  truths:
    - "The app shell wraps chrome in SidebarProvider and the <aside> width follows collapsed state (md:w-16 collapsed / md:w-60 expanded)"
    - "The sidebar has a chevron toggle at the top that flips collapsed state and persists it"
    - "Collapsed sidebar shows icon-only nav with tooltips; expanded shows icon + label"
    - "User avatar + dropdown (name/email, Profilo link, Logout) is anchored at the bottom of the sidebar"
    - "The layout no longer imports or renders Topbar"
  artifacts:
    - path: "components/layout/app-shell.tsx"
      provides: "Client shell reading SidebarContext to drive <aside> width"
      exports: ["AppShell"]
    - path: "components/layout/sidebar.tsx"
      provides: "Collapsible icon-rail sidebar with toggle, nav, user controls"
      exports: ["Sidebar"]
    - path: "app/(app)/layout.tsx"
      provides: "RSC layout wrapping AppShell in SidebarProvider, no Topbar"
  key_links:
    - from: "components/layout/app-shell.tsx"
      to: "useSidebarCollapsed"
      via: "context read for aside width class"
      pattern: "useSidebarCollapsed"
    - from: "components/layout/sidebar.tsx"
      to: "signOutAction"
      via: "Logout dropdown item onClick"
      pattern: "signOutAction"
---

<objective>
Refactor the app shell into a single-zone collapsible sidebar (D-01, D-03, D-04, D-07, D-08, D-13).
Introduce an `AppShell` client component that reads `SidebarContext` to set the `<aside>` width, wire
`SidebarProvider` into the RSC layout, drop the Topbar import (D-01/D-09), and rewrite `sidebar.tsx`
to add the chevron toggle, icon-only collapsed mode with tooltips, and the user avatar/dropdown
migrated 1:1 from the topbar to the bottom of the sidebar (D-02/D-07/D-08).

Purpose: this is the visible heart of the phase — the topbar disappears and the sidebar owns all chrome.
Output: `components/layout/app-shell.tsx` (new), rewritten `components/layout/sidebar.tsx`, refactored
`app/(app)/layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-CONTEXT.md
@.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md
@docs/adr/0011-collapsible-sidebar-no-topbar.md
@components/layout/sidebar-provider.tsx
@components/ui/tooltip.tsx
@components/layout/topbar.tsx
@components/layout/sidebar.tsx
@app/(app)/layout.tsx
@lib/auth-client.ts
@lib/actions/auth.ts
@lib/routes.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AppShell client component and wire SidebarProvider into the RSC layout</name>
  <files>components/layout/app-shell.tsx, app/(app)/layout.tsx</files>
  <read_first>
    - app/(app)/layout.tsx (current shell, guard logic, data-sidebar attribute, onboarding bypass)
    - components/layout/sidebar-provider.tsx (SidebarProvider, useSidebarCollapsed from Plan 01)
    - .planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md (Pattern 2, Open Question 2, Pitfall 5)
  </read_first>
  <action>
    Create a "use client" component at components/layout/app-shell.tsx exporting `AppShell({ children }: { children: React.ReactNode })`.
    It reads `const { collapsed } = useSidebarCollapsed()` from '@/components/layout/sidebar-provider'
    and renders the flex shell currently inline in layout.tsx, MINUS the Topbar:

      <div className="flex h-screen overflow-hidden">
        <aside data-sidebar className={cn('hidden border-r border-border md:flex md:shrink-0 md:flex-col transition-[width] duration-200 ease-in-out overflow-hidden', collapsed ? 'md:w-16' : 'md:w-60')}>
          <Sidebar />
        </aside>
        <div className="flex flex-1 flex-col overflow-hidden">
          <main className="flex-1 overflow-y-auto p-6 pb-20 md:pb-6">{children}</main>
          <BottomNav className="md:hidden" />
        </div>
      </div>

    Keep the `data-sidebar` attribute (used by tests/layout.spec.ts — Pitfall 5/anti-pattern). The width
    classes are the ONLY allowed widths per D-03: `md:w-16` collapsed, `md:w-60` expanded. Use the
    Claude's-discretion transition `transition-[width] duration-200 ease-in-out` (D-03 discretion item).
    Import `cn` from '@/lib/utils', `Sidebar` from '@/components/layout/sidebar', `BottomNav` from
    '@/components/layout/bottom-nav'. There is NO Topbar import here (D-01).

    Then refactor app/(app)/layout.tsx: remove the line `import { Topbar } from "@/components/layout/topbar"`
    (D-01) and remove the inline `<Topbar />` usage. Add `import { SidebarProvider } from "@/components/layout/sidebar-provider"`
    and `import { AppShell } from "@/components/layout/app-shell"`. Keep ALL existing guard logic intact
    (verifySession, headers x-pathname/x-search, isExempt onboarding/settings/import guard, isOnboarding
    chrome bypass returning `<>{children}</>`). Replace the final returned JSX with:

      return (
        <SidebarProvider>
          <AppShell>{children}</AppShell>
        </SidebarProvider>
      )

    Do not touch the onboarding redirect or bypass behavior — only the chrome rendering changes.
    English identifiers/comments per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - SOURCE: app/(app)/layout.tsx has NO occurrence of 'topbar' or 'Topbar' (grep -i topbar returns 0 matches)
    - SOURCE: app/(app)/layout.tsx imports SidebarProvider and AppShell (grep -E "SidebarProvider|AppShell" returns >=2 matches)
    - SOURCE: app/(app)/layout.tsx still contains 'verifySession', 'x-pathname', 'isOnboarding' (guard logic preserved)
    - SOURCE: components/layout/app-shell.tsx contains exactly the strings 'md:w-16' and 'md:w-60' and 'data-sidebar' and 'transition-[width]'
    - SOURCE: app-shell.tsx contains NO 'Topbar' reference
    - BEHAVIOR: `yarn build` compiles without error in app/(app)/layout.tsx or app-shell.tsx
  </acceptance_criteria>
  <verify>
    <automated>! grep -i topbar "app/(app)/layout.tsx" && grep -q "SidebarProvider" "app/(app)/layout.tsx" && grep -q "md:w-16" components/layout/app-shell.tsx && grep -q "md:w-60" components/layout/app-shell.tsx</automated>
  </verify>
  <done>layout.tsx wraps AppShell in SidebarProvider with no Topbar; AppShell drives the data-sidebar aside width between md:w-16 and md:w-60 from context; guard logic unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite Sidebar with toggle, collapsible nav + tooltips, and bottom user controls</name>
  <files>components/layout/sidebar.tsx</files>
  <read_first>
    - components/layout/sidebar.tsx (current nav structure, topNavItems/bottomNavItems, ClientMountIcon usage)
    - components/layout/topbar.tsx (avatar + DropdownMenu + signOutAction pattern to migrate 1:1)
    - components/layout/sidebar-provider.tsx (useSidebarCollapsed)
    - components/ui/tooltip.tsx (Tooltip/TooltipTrigger/TooltipContent/TooltipProvider)
    - lib/routes.ts (APP_ROUTES.settings, APP_ROUTES.profileSettings)
    - lib/actions/auth.ts (signOutAction)
    - lib/auth-client.ts (authClient.useSession)
    - .planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md (Pattern 3/4, Pitfall 2/6, specifics)
    - docs/adr/0011-collapsible-sidebar-no-topbar.md (D-04, D-06, D-07, D-08)
  </read_first>
  <action>
    Rewrite the "use client" Sidebar component. Read `const { collapsed, setCollapsed } = useSidebarCollapsed()`
    and `const pathname = usePathname()`. Track a `mounted` flag (useState(false) + useEffect queueMicrotask)
    mirroring ClientMountIcon, so tooltips only render after mount (Pitfall 6).

    Imports to add: `ChevronLeft, ChevronRight, LogOut, User, LayoutDashboard, List, Receipt, Upload` from
    'lucide-react'; `useSidebarCollapsed` from '@/components/layout/sidebar-provider'; `Tooltip, TooltipTrigger,
    TooltipContent, TooltipProvider` from '@/components/ui/tooltip'; Avatar primitives from
    '@/components/ui/avatar'; DropdownMenu primitives from '@/components/ui/dropdown-menu'; `authClient` from
    '@/lib/auth-client'; `signOutAction` from '@/lib/actions/auth'. Keep ClientMountIcon, Separator, cn,
    Link, APP_ROUTES. Remove `Settings`/Impostazioni from the sidebar nav — Impostazioni stays reachable via
    bottom user controls + (mobile) BottomNav; the sidebar's bottom slot is now the user control, not a settings link.

    Structure the `<nav>` (keep `flex h-full w-full flex-col bg-secondary`, padding `py-4` and horizontal padding
    `px-3` when expanded / `px-2` when collapsed):

    1. TOP SLOT (D-04): a header row. When expanded: the "Sparter" wordmark on the left
       (`text-lg font-semibold tracking-tight`) and a toggle button on the right. When collapsed: ONLY the
       toggle button centered (acts as logo placeholder). The toggle is a button calling
       `setCollapsed(!collapsed)` rendering `ChevronLeft` when expanded and `ChevronRight` when collapsed (D-06),
       with `aria-label={collapsed ? 'Espandi barra laterale' : 'Comprimi barra laterale'}` (specifics).

    2. NAV LIST: map topNavItems = [{dashboard,'Dashboard',LayoutDashboard}, {transactions,'Transazioni',List},
       {expenses,'Spese',Receipt}, {import,'Importazioni',Upload}] (keep current set, exclude /categories and Impostazioni).
       Each item is a Link with active state `pathname === href || pathname.startsWith(href + '/')` using the
       existing active classes. When expanded: render icon (ClientMountIcon) + label span. When collapsed: render
       icon-only, centered, and wrap the Link in <Tooltip><TooltipTrigger asChild>...Link...</TooltipTrigger>
       <TooltipContent side="right">{label}</TooltipContent></Tooltip>. Wrap the whole nav list in a single
       <TooltipProvider> (Pitfall 2). Only render Tooltip wrappers when `collapsed && mounted` (Pitfall 6);
       otherwise render the bare Link.

    3. BOTTOM SLOT (D-07/D-08): `<div className="mt-auto">` with a Separator, then the user control migrated
       from topbar.tsx. Read `const { data: session } = authClient.useSession()`, `const email = session?.user?.email ?? ''`,
       `const fallback = email.charAt(0).toUpperCase() || 'U'`. Render a DropdownMenu whose DropdownMenuTrigger has
       `aria-label="Menu utente"` (KEEP this exact label — used by tests/profile.spec.ts) wrapping an Avatar
       (h-8 w-8, AvatarImage when session.user.image, AvatarFallback with `fallback`). When expanded (D-08), the
       trigger row also shows name + email text beside the avatar (name = `session?.user?.name || email || 'Utente'`,
       email muted). When collapsed (D-08), the trigger is the avatar circle only. DropdownMenuContent (align="end")
       mirrors topbar EXACTLY: DropdownMenuLabel with name + email, separator, DropdownMenuItem asChild Link to
       APP_ROUTES.profileSettings with User icon + 'Profilo', DropdownMenuItem (text-destructive) onClick
       `() => signOutAction()` with LogOut icon + 'Logout'.

    Italian is allowed for these user-facing strings ('Espandi barra laterale', 'Comprimi barra laterale',
    'Menu utente', 'Profilo', 'Logout', nav labels, 'Utente') per CLAUDE.md Language Convention (product surface).
    Identifiers and comments stay English. No monetary values — no Decimal.
  </action>
  <acceptance_criteria>
    - SOURCE: sidebar.tsx imports useSidebarCollapsed from '@/components/layout/sidebar-provider'
    - SOURCE: sidebar.tsx renders both ChevronLeft and ChevronRight (grep -E "ChevronLeft|ChevronRight" returns 2 names referenced)
    - SOURCE: toggle button calls setCollapsed (grep "setCollapsed(" present)
    - SOURCE: toggle aria-labels include exact strings 'Espandi barra laterale' and 'Comprimi barra laterale'
    - SOURCE: a DropdownMenuTrigger has aria-label="Menu utente" (exact string preserved for profile.spec.ts)
    - SOURCE: dropdown contains a Link to APP_ROUTES.profileSettings and an onClick calling signOutAction (grep "signOutAction" present)
    - SOURCE: nav uses TooltipProvider and Tooltip from '@/components/ui/tooltip' (grep "@/components/ui/tooltip" present)
    - SOURCE: sidebar.tsx no longer references APP_ROUTES.settings as a nav Link (Impostazioni removed from sidebar nav)
    - BEHAVIOR: `yarn build` compiles without error in sidebar.tsx
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useSidebarCollapsed" components/layout/sidebar.tsx && grep -q "Menu utente" components/layout/sidebar.tsx && grep -q "signOutAction" components/layout/sidebar.tsx && grep -q "Comprimi barra laterale" components/layout/sidebar.tsx && grep -q "@/components/ui/tooltip" components/layout/sidebar.tsx</automated>
  </verify>
  <done>Sidebar renders a top chevron toggle (D-04/D-06), icon-only-with-tooltips collapsed nav (D-03), and a bottom avatar dropdown (D-07/D-08) mirroring the old topbar with Profilo + Logout, all reading collapse state from context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Logout triggers `signOutAction` from a client onClick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Spoofing | `signOutAction` invocation from sidebar dropdown | accept | Next.js Server Actions carry implicit CSRF token; behavior unchanged from topbar, no new surface |
| T-41-03 | Information disclosure | session email/name rendered in sidebar | accept | Same data the topbar already rendered to the authenticated owner; no new exposure |
</threat_model>

<verification>
- `yarn build` green
- grep gates confirm: no Topbar in layout/app-shell, context wired, toggle icons + aria-labels, preserved 'Menu utente' label, signOutAction wired, tooltip import
- `data-sidebar` attribute retained on the aside
</verification>

<success_criteria>
- Topbar no longer imported or rendered anywhere in the app shell (D-01/D-09)
- `<aside>` width animates between md:w-16 (collapsed) and md:w-60 (expanded) driven by context (D-03)
- Sidebar top toggle flips/persists collapse with ChevronLeft/ChevronRight + correct aria-labels (D-04/D-06)
- Bottom avatar dropdown mirrors the old topbar (Profilo link + Logout) and adapts to collapsed/expanded (D-07/D-08)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `components/layout/app-shell.tsx` — client shell reading SidebarContext, drives `<aside>` width (md:w-16/md:w-60), no Topbar
- `components/layout/sidebar.tsx` (rewritten) — collapsible icon rail: top chevron toggle, tooltip nav, bottom avatar dropdown
- `app/(app)/layout.tsx` (refactored) — wraps AppShell in SidebarProvider, Topbar import removed
</artifacts_produced>

<output>
Create `.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-02-SUMMARY.md` when done
</output>
