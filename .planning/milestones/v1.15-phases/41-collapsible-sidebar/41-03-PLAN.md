---
phase: 41-collapsible-sidebar
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - components/layout/bottom-nav.tsx
  - components/settings/settings-hub.tsx
  - components/layout/topbar.tsx
  - tests/app-layout-guard.test.ts
  - tests/profile.spec.ts
  - tests/layout.spec.ts
autonomous: true
requirements: [D-10, D-11, D-12, D-01]
must_haves:
  truths:
    - "BottomNav has a 5th rightmost entry 'Impostazioni' linking to /settings with the Settings icon"
    - "SettingsHub renders an 'Aspetto' section containing the ThemeToggle component, reused unchanged"
    - "components/layout/topbar.tsx no longer exists"
    - "Unit + E2E tests reference the sidebar instead of the deleted topbar and pass"
  artifacts:
    - path: "components/layout/bottom-nav.tsx"
      provides: "5-entry bottom nav including Impostazioni"
      contains: "APP_ROUTES.settings"
    - path: "components/settings/settings-hub.tsx"
      provides: "Aspetto section with ThemeToggle"
      contains: "ThemeToggle"
  key_links:
    - from: "components/settings/settings-hub.tsx"
      to: "components/theme-toggle.tsx"
      via: "import ThemeToggle, render in Aspetto section"
      pattern: "ThemeToggle"
    - from: "components/layout/bottom-nav.tsx"
      to: "/settings"
      via: "Impostazioni nav entry"
      pattern: "APP_ROUTES.settings"
---

<objective>
Close the phase: add the mobile path to settings (BottomNav 'Impostazioni' 5th entry, D-10), relocate
the theme toggle to the settings page (SettingsHub 'Aspetto' section reusing ThemeToggle as-is, D-11/D-12),
delete the now-orphaned topbar (D-01), and update the test suite so unit + E2E reference the sidebar
instead of the deleted topbar.

Purpose: after Plan 02 the topbar is no longer rendered but the file still exists and tests still mock it;
mobile users lost the only path to settings/theme. This plan makes settings reachable on mobile, surfaces
the theme toggle in its new home, and removes the dead code + stale test references.

Output: updated `bottom-nav.tsx`, `settings-hub.tsx`; deleted `topbar.tsx`; updated three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-CONTEXT.md
@.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md
@docs/adr/0011-collapsible-sidebar-no-topbar.md
@components/layout/bottom-nav.tsx
@components/settings/settings-hub.tsx
@components/theme-toggle.tsx
@tests/app-layout-guard.test.ts
@tests/profile.spec.ts
@tests/layout.spec.ts
@lib/routes.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Impostazioni to BottomNav and Aspetto/ThemeToggle to SettingsHub</name>
  <files>components/layout/bottom-nav.tsx, components/settings/settings-hub.tsx</files>
  <read_first>
    - components/layout/bottom-nav.tsx (current navItems array, ClientMountIcon, active-state pattern)
    - components/settings/settings-hub.tsx (current HUB_ITEMS grid, Card structure)
    - components/theme-toggle.tsx (reused as-is — handles its own mounted state)
    - .planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md (Code Examples "SettingsHub con sezione Aspetto", A1)
    - docs/adr/0011-collapsible-sidebar-no-topbar.md (D-10, D-11)
  </read_first>
  <action>
    BottomNav (D-10): import `Settings` from 'lucide-react' (add to existing lucide import). Append a 5th
    entry to the `navItems` array as the LAST element (rightmost): `{ href: APP_ROUTES.settings, label: 'Impostazioni', icon: Settings }`.
    Do not change the existing 4 entries, the `md:hidden` behavior, the `data-bottom-nav` attribute, or the
    flex-1 sizing — the new entry inherits the same map rendering. Active state already uses
    `pathname === href || pathname.startsWith(href + '/')` which works for /settings.

    SettingsHub (D-11/D-12): import `{ ThemeToggle }` from '@/components/theme-toggle' (reused unchanged — D-12;
    no `dynamic()` wrapper needed per research A1, the component manages its own mounted state). Below the
    existing HUB_ITEMS grid, wrap the component output so it returns a vertical stack: keep the existing grid,
    then add an 'Aspetto' section. The section is a heading `Aspetto` (`text-sm font-medium text-muted-foreground`)
    plus a Card (reuse Card/CardContent/CardHeader/CardTitle already imported, or a bordered row
    `rounded-lg border p-4 flex items-center justify-between`) containing a label block ('Tema' title +
    'Chiaro o scuro' muted description) on the left and `<ThemeToggle />` on the right. Wrap the whole return
    in a parent `<div className="space-y-6">` so the grid and the Aspetto section stack vertically.

    Note: SettingsHub stays a Server Component; ThemeToggle is a client island (it is 'use client') — this is
    valid (RSC rendering a client child). Italian user-facing strings ('Impostazioni', 'Aspetto', 'Tema',
    'Chiaro o scuro') are allowed product surfaces per CLAUDE.md. Identifiers/comments English.
  </action>
  <acceptance_criteria>
    - SOURCE: bottom-nav.tsx imports Settings from lucide-react (grep "Settings" in the lucide import line)
    - SOURCE: bottom-nav.tsx navItems contains an entry with label 'Impostazioni' and href APP_ROUTES.settings, and it is the LAST array element
    - SOURCE: settings-hub.tsx imports ThemeToggle from '@/components/theme-toggle' (grep "@/components/theme-toggle")
    - SOURCE: settings-hub.tsx renders the exact string 'Aspetto' and `<ThemeToggle />`
    - SOURCE: theme-toggle.tsx is unchanged (git diff components/theme-toggle.tsx is empty — D-12)
    - BEHAVIOR: `yarn build` compiles both files clean
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Impostazioni" components/layout/bottom-nav.tsx && grep -q "APP_ROUTES.settings" components/layout/bottom-nav.tsx && grep -q "ThemeToggle" components/settings/settings-hub.tsx && grep -q "Aspetto" components/settings/settings-hub.tsx && git diff --quiet components/theme-toggle.tsx</automated>
  </verify>
  <done>BottomNav has Impostazioni as the rightmost 5th entry to /settings with the Settings icon; SettingsHub shows an Aspetto section rendering the unchanged ThemeToggle.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete topbar.tsx and update unit + E2E tests to reference the sidebar</name>
  <files>components/layout/topbar.tsx, tests/app-layout-guard.test.ts, tests/profile.spec.ts, tests/layout.spec.ts</files>
  <read_first>
    - components/layout/topbar.tsx (file to delete — confirm nothing else imports it)
    - tests/app-layout-guard.test.ts (vi.mock for topbar/sidebar; APP_ROUTES mock)
    - tests/profile.spec.ts (PROF-04 describe + test names referencing topbar; 'Menu utente' selector)
    - tests/layout.spec.ts (existing sidebar/bottom-nav E2E)
    - .planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md (Pitfall 3/4, Wave 0 Gaps, Phase Requirements → Test Map)
  </read_first>
  <action>
    First confirm no remaining imports of topbar: `grep -rn "components/layout/topbar" app components lib tests`
    must return only the test file references you are about to fix (Plan 02 already removed the layout import).
    Then DELETE the file components/layout/topbar.tsx (use rm via Bash, not Write).

    tests/app-layout-guard.test.ts (Pitfall 3): remove the `vi.mock('@/components/layout/topbar', ...)` block
    entirely. Add `vi.mock('@/components/layout/sidebar-provider', () => ({ SidebarProvider: ({ children }) => children, useSidebarCollapsed: () => ({ collapsed: false, setCollapsed: () => {} }) }))`
    and `vi.mock('@/components/layout/app-shell', () => ({ AppShell: ({ children }) => children }))` so the
    refactored layout.tsx import chain resolves under Vitest. Keep all existing onboarding-gate assertions and
    the existing bottom-nav/sidebar mocks. The APP_ROUTES mock may need `import: '/import'` added if the layout
    guard references it (it does — isExempt checks APP_ROUTES.import); add `import: '/import'` to the mocked
    APP_ROUTES object so the guard logic does not read undefined.

    tests/profile.spec.ts (Pitfall 4): rename the describe block from
    `'Profile - PROF-04: topbar navigation'` to `'Profile - PROF-04: sidebar user controls'` and the test name
    from `'PROF-04 topbar profile dropdown navigates to /profile'` to
    `'PROF-04 sidebar profile dropdown navigates to /profile'`. The selectors stay identical: the avatar trigger
    keeps `getByRole('button', { name: 'Menu utente' })` and the 'Profilo' menuitem — both preserved by Plan 02.
    Do not change assertions.

    tests/layout.spec.ts (Wave 0 Gaps): add a new test inside the `(app) route group` describe verifying the
    collapse persistence (D-05) and width (D-03):
      - viewport 1280x800, staging header, goto /dashboard
      - locate '[data-sidebar]', assert visible and assert it has class 'md:w-60' OR computed width ~240px (expanded default)
      - click the toggle: `page.getByRole('button', { name: 'Comprimi barra laterale' }).click()`
      - assert localStorage: `await page.evaluate(() => localStorage.getItem('sparter-sidebar-collapsed'))` equals 'true'
      - reload and assert the toggle is now `'Espandi barra laterale'` (state restored from localStorage)
    Also add a test that BottomNav (mobile 375x812) contains an Impostazioni link to /settings:
      `await expect(bottomNav.locator('a[href="/settings"]')).toHaveCount(1)` (D-10).
    Keep all existing layout.spec.ts tests unchanged.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: components/layout/topbar.tsx does not exist (test -f returns non-zero)
    - SOURCE: no source/test file imports '@/components/layout/topbar' (grep -rn returns 0 matches across app/components/lib/tests)
    - SOURCE: tests/app-layout-guard.test.ts has NO vi.mock for topbar and HAS vi.mock for sidebar-provider and app-shell
    - SOURCE: tests/profile.spec.ts describe/test names contain 'sidebar' not 'topbar' for PROF-04; still uses 'Menu utente' selector
    - SOURCE: tests/layout.spec.ts references 'sparter-sidebar-collapsed' and a 'a[href="/settings"]' assertion
    - BEHAVIOR: `yarn vitest run tests/app-layout-guard.test.ts` passes (all onboarding-gate assertions green)
    - BEHAVIOR: `yarn build` succeeds (no dangling topbar import)
  </acceptance_criteria>
  <verify>
    <automated>! test -f components/layout/topbar.tsx && [ "$(grep -rn 'components/layout/topbar' app components lib tests | grep -v '^#' | grep -c .)" -eq 0 ] && yarn vitest run tests/app-layout-guard.test.ts</automated>
  </verify>
  <done>topbar.tsx is deleted with no remaining importers; app-layout-guard unit test passes with sidebar-provider/app-shell mocks; profile PROF-04 renamed to sidebar; layout.spec.ts asserts collapse persistence + BottomNav Impostazioni.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final build + accessibility + language gate</name>
  <files>components/layout/sidebar.tsx, components/layout/bottom-nav.tsx</files>
  <read_first>
    - components/layout/sidebar.tsx (verify aria-labels on toggle + 'Menu utente' trigger after Plan 02)
    - .planning/milestones/v1.15-phases/41-collapsible-sidebar/41-RESEARCH.md (Validation Architecture, Sampling Rate)
  </read_first>
  <action>
    Run the full validation gate and fix any fallout. Concretely:
    1. `yarn build` must be green. If it fails, fix the reported file (do not weaken types).
    2. `yarn vitest run` — full unit suite green.
    3. `yarn check:language` — must pass; the only Italian strings introduced are intentional product surfaces
       (nav labels, 'Aspetto', 'Tema', 'Chiaro o scuro', 'Comprimi/Espandi barra laterale', 'Menu utente',
       'Impostazioni', 'Profilo', 'Logout'). If the checker flags a developer-facing string, move it to English;
       leave product surfaces as-is.
    4. Accessibility spot-check (source assertion, not manual): confirm the sidebar toggle button has a dynamic
       `aria-label` ('Comprimi barra laterale' / 'Espandi barra laterale'), the avatar trigger keeps
       `aria-label="Menu utente"`, and collapsed nav items expose their label via Tooltip (TooltipContent text).
       If any aria-label is missing, add it in the relevant component.

    Do not introduce new features here — this task only verifies and patches regressions from the rewrite.
  </action>
  <acceptance_criteria>
    - BEHAVIOR: `yarn build` exits 0
    - BEHAVIOR: `yarn vitest run` exits 0 (all unit tests pass)
    - BEHAVIOR: `yarn check:language` exits 0
    - SOURCE: sidebar.tsx contains aria-label 'Comprimi barra laterale' and 'Espandi barra laterale' and 'Menu utente'
  </acceptance_criteria>
  <verify>
    <automated>yarn build && yarn vitest run && yarn check:language && grep -q "Espandi barra laterale" components/layout/sidebar.tsx</automated>
  </verify>
  <done>Build, unit suite, and language gate are all green; sidebar toggle + avatar trigger carry correct aria-labels; collapsed nav items are labelled via tooltips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan deletes code and adds a nav link + a settings island; no new untrusted input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Tampering | test mocks for deleted topbar | mitigate | Remove stale `vi.mock('@/components/layout/topbar')` and add sidebar-provider/app-shell mocks so the suite reflects real imports (Pitfall 3) |
| T-41-SC | Tampering | npm installs | mitigate | No installs in this plan; ThemeToggle and Settings icon reuse existing deps |
</threat_model>

<verification>
- `yarn build` green; `yarn vitest run` green; `yarn check:language` green
- `test -f components/layout/topbar.tsx` is false (deleted)
- grep confirms no remaining topbar importers, BottomNav Impostazioni, SettingsHub Aspetto+ThemeToggle, unchanged theme-toggle.tsx
- E2E layout.spec.ts asserts collapse persistence (localStorage 'sparter-sidebar-collapsed') and BottomNav /settings link
</verification>

<success_criteria>
- BottomNav reaches /settings as the rightmost mobile entry (D-10)
- ThemeToggle lives in SettingsHub 'Aspetto', component file unchanged (D-11/D-12)
- topbar.tsx deleted, no dangling imports (D-01)
- Unit + E2E tests updated to the sidebar model and green
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `components/layout/bottom-nav.tsx` (updated) — 5th 'Impostazioni' entry → /settings (D-10)
- `components/settings/settings-hub.tsx` (updated) — 'Aspetto' section rendering ThemeToggle (D-11/D-12)
- `components/layout/topbar.tsx` — DELETED (D-01)
- `tests/app-layout-guard.test.ts`, `tests/profile.spec.ts`, `tests/layout.spec.ts` (updated) — sidebar-model test references
</artifacts_produced>

<output>
Create `.planning/milestones/v1.15-phases/41-collapsible-sidebar/41-03-SUMMARY.md` when done
</output>
