---
phase: 54-reusable-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/import/[fileId]/suggestions/page.tsx
  - components/import/suggestion-section.tsx
  - tests/import-suggestions-page.test.tsx
autonomous: true
requirements: [TRIG-02]
must_haves:
  truths:
    - "The /import/[fileId]/suggestions page sources its data from discoverRegexCandidates (platform-scoped), not the legacy detectPatternSuggestions detector."
    - "A platform whose uncategorized history is 8 identical 'EUR deposit' rows produces 0 regex candidates on the suggestions page (at most one single-categorization suggestion)."
    - "The suggestions page renders BOTH discovery.candidates (regex) and discovery.singleCategorizationSuggestions (identical groups)."
    - "The notFound() guards on a missing/unimported file and a null platformId are preserved."
    - "detectPatternSuggestions has no production consumer except analyzeFile (Phase 55 scope); no UI/flow calls it after this plan."
  artifacts:
    - path: "app/(app)/import/[fileId]/suggestions/page.tsx"
      provides: "Unified-service-backed suggestions page (regex + single lists)"
      contains: "discoverRegexCandidates"
    - path: "components/import/suggestion-section.tsx"
      provides: "Render path for both candidate lists"
  key_links:
    - from: "app/(app)/import/[fileId]/suggestions/page.tsx"
      to: "lib/services/regex-discovery.ts"
      via: "discoverRegexCandidates({ userId, scope: { platformId } }) after getPlatformIdForUserFile"
      pattern: "discoverRegexCandidates\\(\\{\\s*userId"
    - from: "app/(app)/import/[fileId]/suggestions/page.tsx"
      to: "lib/dal/files.ts"
      via: "getPlatformIdForUserFile ownership guard resolves platformId from fileId"
      pattern: "getPlatformIdForUserFile"
---

<objective>
Migrate the suggestions page from the legacy `detectPatternSuggestions()` detector to the unified `discoverRegexCandidates` service (D-04), making the page platform-scoped and rendering both service lists. This is the foundation both Phase 54 entry points navigate to: TRIG-01's import CTA and TRIG-02's on-demand re-check both land on this page, so it must produce unified, correct results first.

Purpose: Retiring the divergent detector on this page is what satisfies TRIG-02's "same underlying service — no parallel/divergent implementation," and it fixes the Phase 53 UAT bug for free (8× identical "EUR deposit" rows route to singleCategorizationSuggestions, never to regex candidates — RDISC-02).
Output: A unified-service-backed suggestions page, a SuggestionSection that renders both lists, updated page tests, and an explicit decision on legacy-detector retirement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-reusable-trigger/54-CONTEXT.md
@.planning/phases/54-reusable-trigger/54-PATTERNS.md
@CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (54-01) introduces / changes the following symbols. Downstream plans (54-02, 54-03) and Phase 55 depend on these:

- **`app/(app)/import/[fileId]/suggestions/page.tsx`** — now calls `discoverRegexCandidates({ userId, scope: { platformId } })`; renders `discovery.candidates` (regex) and `discovery.singleCategorizationSuggestions` (identical groups). Empty-state when both lists are empty.
- **`components/import/suggestion-section.tsx`** — extended to accept and render single-categorization suggestions in addition to regex candidates (new optional prop `singleSuggestions`). The regex-candidate render path is unchanged (`PatternSuggestionWithMeta extends PatternSuggestion`, so `candidates` is type-compatible with the existing `suggestions` prop).
- **Legacy-detector retirement decision:** `detectPatternSuggestions()` (in `lib/utils/pattern-suggestions.ts`) is NOT deleted this phase — it remains a production consumer in `analyzeFile` (the analyze-screen preview, `lib/services/import.ts` ~line 300, `// TODO Phase 55: remove`). This plan removes the suggestions-page consumer; the function + its tests are flagged for Phase 55 cleanup. No new flow may call it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the suggestions page to discoverRegexCandidates (D-04)</name>
  <files>app/(app)/import/[fileId]/suggestions/page.tsx</files>
  <read_first>
    - app/(app)/import/[fileId]/suggestions/page.tsx (current legacy consumer — full file, 73 lines)
    - .planning/phases/54-reusable-trigger/54-PATTERNS.md (section "app/(app)/import/[fileId]/suggestions/page.tsx — migrate from legacy detector" — exact imports to remove/add and replacement data fetch)
    - lib/services/regex-discovery.ts (the service: signature `discoverRegexCandidates({ userId, scope: { platformId } })`, return shape DiscoveryResult with `candidates`, `singleCategorizationSuggestions`, `platformId`)
    - lib/dal/files.ts (getPlatformIdForUserFile — ownership guard)
    - .planning/phases/54-reusable-trigger/54-CONTEXT.md (D-04: platform-wide scope is intentional; D-03 consistency)
  </read_first>
  <action>
    Rewrite the data-fetch section of `SuggestionsPage`. PRESERVE the two existing guards verbatim (do not change them): the `getFileForUser` + `fileRow.status !== 'imported'` → `notFound()` guard (current lines 23-26) and the `getPlatformIdForUserFile` → null → `notFound()` guard (current lines 28-31). REMOVE the imports `getUncategorizedTransactionsByFileId` (from `@/lib/dal/transactions`), `loadActivePatterns` (from `@/lib/services/categorization`), `detectPatternSuggestions`/`PatternDetectorRow` (from `@/lib/utils/pattern-suggestions`), and `normalizeDescription` (from `@/lib/utils/import`). ADD the import `discoverRegexCandidates` from `@/lib/services/regex-discovery`. REPLACE the `Promise.all([...])` block + `detectorRows` map + `detectPatternSuggestions(...)` + `.sort().slice(0,5)` (current lines 33-50) with a single `Promise.all([discoverRegexCandidates({ userId, scope: { platformId } }), getCategories()])` destructured to `[discovery, categories]`. Keep `getCategories` imported from `@/lib/dal/categories`. The empty-state condition becomes `discovery.candidates.length === 0 && discovery.singleCategorizationSuggestions.length === 0`. Pass `discovery.candidates` (regex) and `discovery.singleCategorizationSuggestions` (single) plus `categories` and `fileId` to `SuggestionSection` (the prop wiring is defined in Task 2). Keep the existing page heading/intro copy. NOTE per D-04: scope is now platform-wide (the whole platform's uncategorized history), not file-only — this is intentional and matches the apply path.
  </action>
  <verify>
    <automated>grep -q "discoverRegexCandidates" "app/(app)/import/[fileId]/suggestions/page.tsx" && ! grep -q "detectPatternSuggestions" "app/(app)/import/[fileId]/suggestions/page.tsx" && yarn tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/(app)/import/[fileId]/suggestions/page.tsx` imports `discoverRegexCandidates` from `@/lib/services/regex-discovery` and does NOT import `detectPatternSuggestions`.
    - Source assertion: both `notFound()` guards (missing/unimported file; null platformId) remain present and unchanged.
    - Source assertion: the empty-state check is `discovery.candidates.length === 0 && discovery.singleCategorizationSuggestions.length === 0`.
    - Behavior assertion: the page resolves `platformId` via `getPlatformIdForUserFile({ userId, fileId })` BEFORE calling the service, and calls `discoverRegexCandidates({ userId, scope: { platformId } })`.
    - Command: `yarn tsc --noEmit` reports no new type errors for this file.
  </acceptance_criteria>
  <done>The suggestions page sources data from discoverRegexCandidates (platform-scoped), preserves both notFound() guards, and no longer references detectPatternSuggestions.</done>
</task>

<task type="auto">
  <name>Task 2: Render both candidate lists in SuggestionSection (minimal presentation)</name>
  <files>components/import/suggestion-section.tsx, app/(app)/import/[fileId]/suggestions/page.tsx</files>
  <read_first>
    - components/import/suggestion-section.tsx (current Props: `suggestions: PatternSuggestion[]`, `categories`, `fileId`; maps to SuggestionCard)
    - components/import/suggestion-card.tsx (consumes `suggestion.pattern`, `suggestion.matchCount`, `suggestion.sampleDescriptions` — all present on PatternSuggestionWithMeta)
    - lib/services/regex-discovery.ts (SingleCategorizationSuggestion shape: `normalizedDescription`, `sampleDescriptions`, `matchCount`, `descriptionHashes` — NO `pattern` field, so it is not a SuggestionCard)
    - .planning/phases/54-reusable-trigger/54-CONTEXT.md (D-04: "Minimal presentation here; the polished visual separation is Phase 55")
  </read_first>
  <action>
    Extend `SuggestionSection`'s `Props` with a new optional prop `singleSuggestions?: SingleCategorizationSuggestion[]` (import the type from `@/lib/services/regex-discovery`). Keep the existing `suggestions` prop typed as `PatternSuggestion[]` — `discovery.candidates` (`PatternSuggestionWithMeta[]`) is assignable to it because `PatternSuggestionWithMeta extends PatternSuggestion`. The regex-candidate render path (mapping `suggestions` to `SuggestionCard`) is UNCHANGED. Add a SECOND, minimal block that renders `singleSuggestions` as a plain read-only list (Italian heading + each item's `sampleDescriptions[0]` or `normalizedDescription` and its `matchCount`) — NO promote form, NO SuggestionCard (single suggestions have no regex `pattern` to promote). The early-return guard must change so the component still renders when only `singleSuggestions` is non-empty: return null only when BOTH `suggestions.length === 0 && (singleSuggestions?.length ?? 0) === 0`. Keep all user-facing copy Italian; keep the existing regex section heading. Do not build visual separation polish — that is Phase 55 (SUMUI-02). Update the page (Task 1) to pass `singleSuggestions={discovery.singleCategorizationSuggestions}`.
  </action>
  <verify>
    <automated>grep -q "singleSuggestions" components/import/suggestion-section.tsx && yarn tsc --noEmit 2>&1 | tail -5 && yarn check:language 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `SuggestionSection` Props includes `singleSuggestions?: SingleCategorizationSuggestion[]` imported from `@/lib/services/regex-discovery`.
    - Source assertion: the existing regex-candidate render path (`suggestions.map(... SuggestionCard ...)`) is unchanged.
    - Source assertion: the early-return guard returns null only when both lists are empty.
    - Behavior assertion: single suggestions render as a plain list (no SuggestionCard / no promote form).
    - Command: `yarn tsc --noEmit` passes; `yarn check:language` passes (no English in product-facing strings).
  </acceptance_criteria>
  <done>SuggestionSection renders both regex candidates and single-categorization suggestions; single suggestions are a minimal read-only list; component renders when either list is non-empty.</done>
</task>

<task type="auto">
  <name>Task 3: Update suggestions-page tests for the unified service + retirement note</name>
  <files>tests/import-suggestions-page.test.tsx</files>
  <read_first>
    - tests/import-suggestions-page.test.tsx (current mocks: detectPatternSuggestions, getUncategorizedTransactionsByFileId, loadActivePatterns — these are removed by the migration)
    - lib/services/regex-discovery.ts (DiscoveryResult shape to mock: `candidates`, `singleCategorizationSuggestions`, `totalUncategorized`, `platformId`)
    - app/(app)/import/[fileId]/suggestions/page.tsx (the migrated page from Task 1)
    - .planning/phases/54-reusable-trigger/54-CONTEXT.md (verification anchor: 8 identical "EUR deposit" rows → 0 regex candidates)
  </read_first>
  <action>
    Rewrite the test mocks to match the migrated page: REMOVE the `detectPatternSuggestions`, `getUncategorizedTransactionsByFileId`, and `loadActivePatterns` mocks. ADD a mock for `@/lib/services/regex-discovery` exposing `discoverRegexCandidates` as a `vi.fn()`. Keep the `notFound`, `verifySession`, `getFileForUser`, `getPlatformIdForUserFile`, and `getCategories` mocks. Preserve the existing notFound-guard test cases (missing/unimported file → notFound; null platformId → notFound) — update only the data-source mock, not the guard assertions. REPLACE the suggestion-rendering tests so they drive `discoverRegexCandidates.mockResolvedValue({ ... })`: (a) one test where `candidates` is empty and `singleCategorizationSuggestions` has one entry → page renders the single-suggestion list, no regex SuggestionCard; (b) the verification-anchor test: `discoverRegexCandidates.mockResolvedValue({ candidates: [], singleCategorizationSuggestions: [{ normalizedDescription: 'eur deposit', sampleDescriptions: ['EUR deposit'], matchCount: 8, descriptionHashes: [...] }], totalUncategorized: 8, platformId: 1 })` → assert the rendered markup contains 0 regex candidates (no SuggestionCard / no promotable pattern) and exactly the single suggestion; (c) the empty/empty case → empty-state message. Assert `discoverRegexCandidates` was called with `{ userId, scope: { platformId } }`. Add a top-of-file comment noting that `detectPatternSuggestions` retirement (deleting the function + `tests/pattern-suggestion-detector.test.ts`) is deferred to Phase 55 because `analyzeFile` still consumes it.
  </action>
  <verify>
    <automated>yarn vitest run tests/import-suggestions-page.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Command: `yarn vitest run tests/import-suggestions-page.test.tsx` passes.
    - Behavior assertion: a test mocks `discoverRegexCandidates` with `candidates: []` + a single "EUR deposit" suggestion (matchCount 8) and asserts the page renders 0 regex candidates (no SuggestionCard) and the single suggestion.
    - Behavior assertion: a test asserts `discoverRegexCandidates` is called with `{ userId, scope: { platformId } }`.
    - Source assertion: the test file no longer mocks `detectPatternSuggestions`, `getUncategorizedTransactionsByFileId`, or `loadActivePatterns`.
    - Source assertion: a comment documents the Phase 55 deferral of detectPatternSuggestions deletion.
  </acceptance_criteria>
  <done>Suggestions-page tests pass against the unified service, encode the EUR-deposit anchor (0 regex candidates), and document the legacy-detector Phase 55 deferral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RSC route (`/import/[fileId]/suggestions`) | `fileId` is a user-controlled URL segment; it must not allow reading another user's platform data |
| RSC → service/DAL | `userId` is resolved server-side via `verifySession`; never trust a client-supplied userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure / Elevation | `suggestions/page.tsx` resolving `platformId` from user-supplied `fileId` (IDOR) | mitigate | Resolve `platformId` ONLY via `getPlatformIdForUserFile({ userId, fileId })` (ownership guard, scoped by the verifySession userId); on null → `notFound()`. Preserve the existing `getFileForUser` status guard. Service is then called with the server-resolved `platformId`, never a client value. |
| T-54-02 | Spoofing | userId source for the page | mitigate | `userId` comes from `verifySession()` (server-side session); the page never accepts a userId from the request. |
| T-54-03 | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (reuses existing service + components). No legitimacy gate required. |
</threat_model>

<verification>
- `yarn tsc --noEmit` passes with no new errors.
- `yarn vitest run tests/import-suggestions-page.test.tsx` passes, including the EUR-deposit anchor (0 regex candidates).
- `yarn check:language` passes for the touched page + component.
- Manual/grep: `grep -rn "detectPatternSuggestions\b" app/ components/` returns NO matches in `app/` or `components/` (the only remaining production consumer is `lib/services/import.ts` analyzeFile, deferred to Phase 55).
- IDOR guard: `getPlatformIdForUserFile` is the sole path from `fileId` to `platformId`; null → `notFound()`.
</verification>

<success_criteria>
The suggestions page renders both unified-service lists, the EUR-deposit anchor produces 0 regex candidates, both notFound() guards are preserved, the IDOR mitigation (ownership-guarded file→platform resolution) holds, and no UI/component flow calls the legacy detector.
</success_criteria>

<output>
Create `.planning/phases/54-reusable-trigger/54-01-SUMMARY.md` when done.
</output>
