---
phase: 53-retroactive-application
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/dal/regex-discovery.ts
  - lib/dal/files.ts
  - lib/services/pattern-application.ts
  - tests/pattern-application.test.ts
  - tests/regex-discovery-dal.test.ts
autonomous: true
requirements: [APPLY-02]
tags: [retroactive-apply, platform-scope, dal, pattern-application, set-b, APPLY-02, tdd]

must_haves:
  truths:
    - "Platform-scoped apply scans only uncategorized expenses on the same platform (expense → file → importFormatVersion → platform join), never user-wide"
    - "Already-categorized expenses (Set A, subCategoryId IS NOT NULL) are never selected or updated"
    - "applyNewPatternToPlatformExpenses returns { updatedCount, notUpdatedCount } where notUpdatedCount = scannedInScope − updatedCount"
    - "Matcher tests both full normalized title and numeric-stripped form (Tier-1 fidelity preserved)"
    - "getPlatformIdForUserFile returns platformId only when file belongs to userId; null otherwise"
    - "applyNewPatternToExpenses (legacy user-wide) remains unchanged for createPatternAction"
  artifacts:
    - path: "lib/dal/regex-discovery.ts"
      provides: "getUncategorizedExpensesForPlatformApply(userId, platformId) — platform Set B for writes"
      exports: ["getUncategorizedExpensesForPlatformApply"]
      contains: "getUncategorizedExpensesForPlatformApply"
    - path: "lib/dal/files.ts"
      provides: "getPlatformIdForUserFile({ userId, fileId })"
      exports: ["getPlatformIdForUserFile"]
      contains: "getPlatformIdForUserFile"
    - path: "lib/services/pattern-application.ts"
      provides: "applyNewPatternToPlatformExpenses + PatternApplyResult"
      exports: ["applyNewPatternToPlatformExpenses", "PatternApplyResult"]
      contains: "applyNewPatternToPlatformExpenses"
    - path: "tests/pattern-application.test.ts"
      provides: "Platform boundary, matcher dual-test, count semantics unit tests"
      contains: "applyNewPatternToPlatformExpenses"
  key_links:
    - from: "applyNewPatternToPlatformExpenses"
      to: "getUncategorizedExpensesForPlatformApply"
      via: "DAL fetch before matcher loop"
      pattern: "getUncategorizedExpensesForPlatformApply"
    - from: "getUncategorizedExpensesForPlatformApply WHERE"
      to: "getUncategorizedExpensesForDiscovery WHERE"
      via: "identical join chain + eq(platform.id) + isNull(subCategoryId)"
      pattern: "eq\\(platform\\.id"
    - from: "applyNewPatternToPlatformExpenses matcher"
      to: "normalizeDescription + numeric-strip dual test"
      via: "regex.test(normalized) || regex.test(stripped)"
      pattern: "regex\\.test\\(stripped\\)"
---

<objective>
Add the platform-scoped retroactive apply foundation (APPLY-02, locked to entire platform uncategorized history per Phase 53 CONTEXT — aligns with Phase 51 D-03 read scope):

1. **DAL** — `getUncategorizedExpensesForPlatformApply` mirroring `getUncategorizedExpensesForDiscovery` join/WHERE but selecting `{ id, title, totalAmount }` for the apply scan; `getPlatformIdForUserFile` resolving `file → importFormatVersion → platform` with ownership guard.
2. **Service** — new sibling `applyNewPatternToPlatformExpenses` returning `PatternApplyResult { updatedCount, notUpdatedCount }`; leave legacy `applyNewPatternToExpenses` untouched for `createPatternAction`.

Purpose: Fix cross-platform retroactive writes and provide structured counts Plan 02 (`promoteSuggestionAction`) consumes.
Output: Two DAL exports, one service export + type, and unit tests proving platform boundary, Set A protection, matcher fidelity, and count semantics.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/53-retroactive-application/53-CONTEXT.md
@.planning/phases/53-retroactive-application/53-RESEARCH.md
@.planning/phases/53-retroactive-application/53-PATTERNS.md
@.planning/phases/51-discovery-pipeline-reorder/51-CONTEXT.md

@lib/dal/regex-discovery.ts
@lib/dal/files.ts
@lib/services/pattern-application.ts
@tests/regex-discovery-dal.test.ts
</context>

<artifacts_this_phase_produces>
This plan produces the following NEW symbols (Plan 02 consumes them):
- `getUncategorizedExpensesForPlatformApply(userId: string, platformId: number): Promise<{ id: string; title: string; totalAmount: string }[]>` — NEW platform-scoped Set B query for writes (APPLY-02)
- `getPlatformIdForUserFile(input: { userId: string; fileId: string }, database?: DbOrTx): Promise<number | null>` — NEW file→platform resolver with ownership (APPLY-02)
- `PatternApplyResult` type — `{ updatedCount: number; notUpdatedCount: number }`
- `applyNewPatternToPlatformExpenses(database, input): Promise<PatternApplyResult>` — NEW platform-scoped apply (APPLY-02); legacy `applyNewPatternToExpenses` unchanged
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for platform apply service + DAL WHERE contract</name>
  <files>tests/pattern-application.test.ts, tests/regex-discovery-dal.test.ts</files>
  <read_first>
    - lib/services/pattern-application.ts (lines 16-101): existing `applyNewPatternToExpenses` matcher loop (lines 52-61), UPDATE + `writeClassificationHistory` loop — the behavior to mirror in the sibling function.
    - lib/dal/regex-discovery.ts (lines 34-56): `getUncategorizedExpensesForDiscovery` join/WHERE — the contract `getUncategorizedExpensesForPlatformApply` must mirror exactly.
    - tests/regex-discovery-dal.test.ts (lines 116-171): structural WHERE assertion style for the discovery query — extend with parallel assertions for the apply query.
    - 53-RESEARCH.md §Pattern 1 (platform Set B DAL) and §Validation Architecture Wave 0 gaps.
    - 53-PATTERNS.md §`tests/pattern-application.test.ts` (hoisted mock harness for DAL + history).
  </read_first>
  <acceptance_criteria>
    - New file `tests/pattern-application.test.ts` imports `applyNewPatternToPlatformExpenses` and fails because the export does not exist.
    - Service tests mock `getUncategorizedExpensesForPlatformApply` and `writeClassificationHistory` (no real DB).
    - At least one test asserts platform boundary: expenses from platform A are updated; platform B fixtures in the mock are never passed to UPDATE/history.
    - At least one test asserts numeric-stripped dual match (pattern matches only after stripping pure-numeric tokens).
    - At least one test asserts `{ updatedCount: 2, notUpdatedCount: 1 }` when 3 scanned, 2 match.
    - At least one test asserts invalid regex returns `{ updatedCount: 0, notUpdatedCount: scanned }` without throwing.
    - `tests/regex-discovery-dal.test.ts` gains structural tests for `getUncategorizedExpensesForPlatformApply`: 3 `leftJoin`s, `eq(expense.userId)`, `eq(platform.id)`, `isNull(expense.subCategoryId)`.
    - RED commit uses `test(53-01):` prefix.
  </acceptance_criteria>
  <action>
    Create `tests/pattern-application.test.ts` following the hoisted-mock pattern from 53-PATTERNS.md: mock `server-only`, mock `@/lib/dal/regex-discovery` (`getUncategorizedExpensesForPlatformApply`), mock `@/lib/dal/classification-history` (`writeClassificationHistory`), use real `normalizeDescription`. Build fixtures with two platform IDs (e.g. platform 1 Fineco rows vs platform 2 Revolut rows) — only platform 1 rows are returned by the mocked DAL when `platformId=1`. Assert `applyNewPatternToPlatformExpenses` calls UPDATE path only for matching ids from that scan. Add numeric-strip fixture title like `***** 114 data operazione` with a suggestion-style pattern. Extend `tests/regex-discovery-dal.test.ts` with a describe block for `getUncategorizedExpensesForPlatformApply` using the existing Drizzle-chain mock — mirror the discovery WHERE assertions. Run `yarn test tests/pattern-application.test.ts tests/regex-discovery-dal.test.ts`; suite MUST fail (exports missing). Commit RED.
  </action>
  <verify>
    <automated>yarn test tests/pattern-application.test.ts tests/regex-discovery-dal.test.ts 2>&1 | grep -Eiq "fail|error|is not a function|applyNewPatternToPlatformExpenses|getUncategorizedExpensesForPlatformApply" && echo RED_OK || (echo RED_MISSING && exit 1)</automated>
  </verify>
  <done>Test files reference new exports; suite fails; RED commit made with `test(53-01):` prefix.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement platform DAL helpers + applyNewPatternToPlatformExpenses</name>
  <files>lib/dal/regex-discovery.ts, lib/dal/files.ts, lib/services/pattern-application.ts</files>
  <read_first>
    - lib/dal/files.ts (lines 56-67): `getFileForUser` ownership WHERE pattern for `getPlatformIdForUserFile`.
    - lib/dal/imports.ts (lines 168-173): file → importFormatVersion → platform join precedent.
    - lib/services/pattern-application.ts (lines 52-98): matcher + UPDATE + history — copy verbatim into sibling; only change expense source to DAL.
    - 53-CONTEXT.md locked decision "Retroactive write scope (APPLY-02)" and "Matcher fidelity".
    - 53-RESEARCH.md §Pattern 2 (structured apply result single pass).
  </read_first>
  <acceptance_criteria>
    - `getUncategorizedExpensesForPlatformApply` uses identical join/WHERE as `getUncategorizedExpensesForDiscovery` (per APPLY-02 / Phase 51 D-03 alignment).
    - `getPlatformIdForUserFile` enforces `eq(file.userId, userId)` AND `eq(file.id, fileId)`; returns `null` when platform join missing.
    - `applyNewPatternToPlatformExpenses` accepts `{ userId, platformId, patternId, patternString, subCategoryId, confidence }` and returns `PatternApplyResult`.
    - `notUpdatedCount = uncategorized.length - matchingIds.length` (single pass).
    - `applyNewPatternToExpenses` body is UNCHANGED (grep confirms no `platformId` added to legacy function).
    - All RED tests pass; GREEN commit uses `feat(53-01):` prefix.
  </acceptance_criteria>
  <action>
    Per locked APPLY-02 scope (platform entire uncategorized history, not file-only):
    1. In `lib/dal/regex-discovery.ts`, export `getUncategorizedExpensesForPlatformApply(userId, platformId)` selecting `{ id, title, totalAmount }` with the same `leftJoin` chain and `and(eq(expense.userId), eq(platform.id, platformId), isNull(expense.subCategoryId))` as `getUncategorizedExpensesForDiscovery`. Add doc comment noting write-path Set B mirror.
    2. In `lib/dal/files.ts`, export `getPlatformIdForUserFile(input, database = db)` using `select({ platformId: platform.id }).from(file).leftJoin(importFormatVersion, ...).leftJoin(platform, ...).where(and(eq(file.id), eq(file.userId))).limit(1)`; return `rows[0]?.platformId ?? null`. Import `importFormatVersion`, `platform` from schema; accept `DbOrTx`.
    3. In `lib/services/pattern-application.ts`, export `PatternApplyResult` and `applyNewPatternToPlatformExpenses`. Import `getUncategorizedExpensesForPlatformApply` from DAL. Copy matcher lines 52-61 verbatim. On zero matches return `{ updatedCount: 0, notUpdatedCount: uncategorized.length }`. On invalid regex return `{ updatedCount: 0, notUpdatedCount: uncategorized.length }` (after scan). Keep `writeClassificationHistory` with `source: 'user_pattern'`. Do NOT modify `applyNewPatternToExpenses`.
    Run `yarn test tests/pattern-application.test.ts tests/regex-discovery-dal.test.ts`; all green. Commit GREEN.
  </action>
  <verify>
    <automated>yarn test tests/pattern-application.test.ts tests/regex-discovery-dal.test.ts 2>&1 | tail -5 | grep -Eiq "pass|✓| 0 failed| no failures" && grep -q "applyNewPatternToPlatformExpenses" lib/services/pattern-application.ts && grep -q "getUncategorizedExpensesForPlatformApply" lib/dal/regex-discovery.ts && grep -q "getPlatformIdForUserFile" lib/dal/files.ts && echo GREEN_OK || (echo GREEN_FAIL && exit 1)</automated>
  </verify>
  <done>Platform DAL + service exports exist; legacy apply untouched; all unit tests pass; GREEN commit made with `feat(53-01):` prefix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → DAL | Apply service receives authenticated `userId` from caller; DAL enforces row scope in WHERE. |
| DAL → Postgres | Read platform Set B; UPDATE only ids from scoped scan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | Cross-platform retroactive writes | mitigate | Platform-scoped DAL WHERE (`eq(platform.id, platformId)`) before match; structural DAL test + service boundary test assert isolation (APPLY-02). |
| T-53-02 | Tampering | Set A re-categorization | mitigate | `isNull(expense.subCategoryId)` in SELECT; UPDATE only `inArray(expense.id, matchingIds)` from that scan. |
| T-53-03 | Denial of Service | ReDoS in regex compile during apply | accept | Inherited from existing apply — patterns are validated at create time; invalid regex returns zero updates. |
| T-53-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (53-RESEARCH Package Legitimacy Audit: none). No checkpoint needed. |
</threat_model>

<verification>
- `yarn test tests/pattern-application.test.ts tests/regex-discovery-dal.test.ts` — all green.
- `yarn check:language` passes (English dev code; Italian only in fixture titles if any).
- `grep -c "platformId" lib/services/pattern-application.ts` inside `applyNewPatternToExpenses` function body returns 0 (legacy path unchanged).
</verification>

<success_criteria>
- Platform-scoped apply never scans user-wide uncategorized expenses (APPLY-02).
- Structured `{ updatedCount, notUpdatedCount }` returned from platform apply path.
- Matcher dual-test preserved (numeric-stripped titles).
- RED and GREEN are separate commits.
</success_criteria>

<output>
Create `.planning/phases/53-retroactive-application/53-01-SUMMARY.md` when done.
</output>
