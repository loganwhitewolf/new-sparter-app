---
phase: 53-retroactive-application
plan: "03"
type: execute
wave: 3
depends_on: ["53-02"]
files_modified:
  - app/(app)/import/[fileId]/suggestions/page.tsx
  - components/import/suggestion-section.tsx
  - components/import/suggestion-card.tsx
  - components/import/suggestion-promote-form.tsx
  - tests/suggestion-card.test.tsx
  - tests/suggestion-promote-form.test.tsx
  - tests/import-suggestions-page.test.tsx
autonomous: true
requirements: [APPLY-01]
tags: [suggestion-ui, inline-feedback, applyResult, fileId, ROADMAP-SC-2, APPLY-01]

must_haves:
  truths:
    - "Suggestions page resolves platformId from fileId and passes fileId + platformId to SuggestionSection"
    - "Promote form includes hidden fileId input submitted with pattern + subCategoryId"
    - "After successful promote, card shows 'Pattern creato' badge AND inline count copy (updated · not updated)"
    - "Apply counts persist in card state while page is open (no auto-dismiss, card not removed)"
    - "onPromoted fires only after form submit when state.applyResult is present (submittedRef guard preserved)"
    - "Page returns notFound when platformId cannot be resolved"
  artifacts:
    - path: "app/(app)/import/[fileId]/suggestions/page.tsx"
      provides: "platformId resolution + props threading"
      contains: "getPlatformIdForUserFile"
    - path: "components/import/suggestion-promote-form.tsx"
      provides: "hidden fileId + applyResult consumption"
      contains: 'name="fileId"'
    - path: "components/import/suggestion-card.tsx"
      provides: "inline Italian apply count display after promote"
      contains: "categorizzate"
    - path: "tests/suggestion-card.test.tsx"
      provides: "count copy visibility assertion"
      contains: "categorizzate"
  key_links:
    - from: "suggestions/page.tsx"
      to: "SuggestionSection"
      via: "fileId + platformId props"
      pattern: "platformId="
    - from: "SuggestionPromoteForm"
      to: "promoteSuggestionAction"
      via: "hidden fileId + useActionState"
      pattern: 'name="fileId"'
    - from: "SuggestionPromoteForm useEffect"
      to: "SuggestionCard applyResult state"
      via: "onPromoted(applyResult)"
      pattern: "state\\.applyResult"
---

<objective>
Deliver observable inline apply feedback on the suggestion card (ROADMAP SC-2, APPLY-01 user-visible outcome):

1. Resolve `platformId` on the RSC suggestions page and thread `fileId` through section → card → form.
2. Extend client components to submit `fileId`, consume `ActionState.applyResult`, and render persistent Italian count copy on the card after promote.
3. Extend UI/page tests proving hidden `fileId`, count copy, and `notFound` when platform missing.

Purpose: User sees exactly how many platform-scoped uncategorized expenses were categorized vs left without match — at the action point, without toast or separate summary page (locked UX decisions).
Output: Wired suggestions page + three client components + three test file extensions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/53-retroactive-application/53-CONTEXT.md
@.planning/phases/53-retroactive-application/53-RESEARCH.md
@.planning/phases/53-retroactive-application/53-PATTERNS.md

@app/(app)/import/[fileId]/suggestions/page.tsx
@components/import/suggestion-section.tsx
@components/import/suggestion-card.tsx
@components/import/suggestion-promote-form.tsx
@tests/suggestion-card.test.tsx
@tests/suggestion-promote-form.test.tsx
@tests/import-suggestions-page.test.tsx

@.planning/phases/53-retroactive-application/53-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces the following UI wiring (Phase 54 reuses fileId/platformId threading pattern):
- `SuggestionSection` props: `fileId: string`, `platformId: number`
- `SuggestionCard` props: `fileId: string` (+ internal `applyResult` state for inline counts)
- `SuggestionPromoteForm` props: `fileId: string`; `onPromoted: (applyResult: PatternApplyResult) => void`
- RSC page calls `getPlatformIdForUserFile` after file guard; `notFound()` when null
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire suggestions page + section + card + form fileId threading</name>
  <files>app/(app)/import/[fileId]/suggestions/page.tsx, components/import/suggestion-section.tsx, components/import/suggestion-card.tsx, components/import/suggestion-promote-form.tsx</files>
  <read_first>
    - app/(app)/import/[fileId]/suggestions/page.tsx (lines 20-61): existing file guard + `SuggestionSection` render — add platform resolution after line 26.
    - components/import/suggestion-section.tsx (lines 7-27): props + map to `SuggestionCard`.
    - components/import/suggestion-promote-form.tsx (lines 13-34, 60-61): Props, `useActionState`, hidden inputs, `submittedRef` guard.
    - 53-CONTEXT.md "Feedback placement" and "Feedback persistence" locked decisions.
    - 53-PATTERNS.md §suggestion-promote-form (extend onPromoted signature + fileId hidden input).
    - 53-02-SUMMARY.md — confirm `ActionState.applyResult` shape.
  </read_first>
  <acceptance_criteria>
    - Page imports `getPlatformIdForUserFile` from `@/lib/dal/files`; calls after `getFileForUser` guard; `notFound()` when `platformId == null`.
    - `SuggestionSection` accepts and forwards `fileId` and `platformId` to each `SuggestionCard`.
    - `SuggestionCard` accepts `fileId: string`; passes `fileId` to `SuggestionPromoteForm`; `handlePromoted` signature is `(applyResult: PatternApplyResult) => void` (Task 2 adds applyResult display state).
    - `SuggestionPromoteForm` accepts `fileId: string`; renders `<input type="hidden" name="fileId" value={fileId} />`.
    - `onPromoted` signature becomes `(applyResult: PatternApplyResult) => void`.
    - `useEffect` calls `onPromoted(state.applyResult)` only when `submittedRef.current && state.error === null && state.applyResult` (Pitfall 3 — no mount false positive).
    - Initial `useActionState` seed remains `{ error: null }` (applyResult omitted/null).
  </acceptance_criteria>
  <action>
    Per locked UX placement (inline at action point) and server-side platform resolution (53-RESEARCH Pattern 4):
    1. In `suggestions/page.tsx`, after imported file guard, `const platformId = await getPlatformIdForUserFile({ userId, fileId })`; if null call `notFound()`. Pass `fileId` and `platformId` to `SuggestionSection`.
    2. Extend `SuggestionSection` Props with `fileId: string` and `platformId: number`; pass both to `SuggestionCard` (platformId available for Phase 54 surfaces even if card does not use it directly yet).
    3. In `SuggestionCard`, add `fileId` prop; pass to `SuggestionPromoteForm`. Change `handlePromoted` to `(applyResult: PatternApplyResult) => { setPromoted(true) }` only — defer `applyResult` display state to Task 2.
    4. In `SuggestionPromoteForm`, add `fileId` prop and hidden input. Import `PatternApplyResult` from validations (or service re-export). Change `onPromoted` to accept `applyResult`. Update `useEffect` guard to require `state.applyResult` before calling `onPromoted(state.applyResult)`.
    Do NOT render count copy on card yet (Task 2). Run `yarn check:language` after edits.
  </action>
  <verify>
    <automated>yarn test tests/suggestion-promote-form.test.tsx 2>&1 | tail -8 | grep -Eiq "fail" && (echo FAIL && exit 1) || yarn check:language && grep -q 'name="fileId"' components/import/suggestion-promote-form.tsx && echo TASK1_OK</automated>
  </verify>
  <done>Page resolves platformId; fileId threads through section → card → form hidden input; onPromoted receives applyResult; language check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Inline apply counts on suggestion card + UI/page tests</name>
  <files>components/import/suggestion-card.tsx, tests/suggestion-card.test.tsx, tests/suggestion-promote-form.test.tsx, tests/import-suggestions-page.test.tsx</files>
  <read_first>
    - components/import/suggestion-card.tsx (lines 15-19, 35-42, 70-76): promoted state, badge, form wrapper — extend for applyResult display.
    - tests/suggestion-promote-form.test.tsx (lines 50-67): hidden input regex assertions for pattern/subCategoryId — mirror for fileId.
    - tests/suggestion-card.test.tsx: existing static render pattern.
    - tests/import-suggestions-page.test.tsx (lines 25-27, 57-80): mocks for `getFileForUser` — add `getPlatformIdForUserFile` mock.
    - 53-RESEARCH.md inline count Italian copy example: `{updatedCount} categorizzate · {notUpdatedCount} ancora senza match`.
  </read_first>
  <acceptance_criteria>
    - `SuggestionCard` stores `applyResult` in state; `handlePromoted` accepts and sets it.
    - When `applyResult` set, renders Italian count line below/alongside "Pattern creato" badge.
    - Card remains visible with `opacity-50 pointer-events-none` on form when promoted (NOT removed — rejected alternative).
    - `tests/suggestion-promote-form.test.tsx` asserts hidden `fileId` input with expected value.
    - `tests/suggestion-card.test.tsx` asserts count copy visible when applyResult state is set (client render with `@testing-library/react` if static SSR insufficient).
    - `tests/import-suggestions-page.test.tsx` mocks `getPlatformIdForUserFile`; asserts `notFound` when null; asserts `fileId`/`platformId` passed to section when present.
    - Phase 53 targeted test slice passes.
  </acceptance_criteria>
  <action>
    Per locked feedback persistence (counts stay until navigation):
    1. In `SuggestionCard`, add `fileId` prop. Change state to hold `applyResult: PatternApplyResult | null`. Update `handlePromoted` to `(result) => { setPromoted(true); setApplyResult(result) }`. Pass `fileId` to `SuggestionPromoteForm`. When `applyResult` is non-null, render `<p className="text-sm text-muted-foreground">{applyResult.updatedCount} categorizzate · {applyResult.notUpdatedCount} ancora senza match</p>` near the success badge (Italian product copy per discretion).
    2. Extend `tests/suggestion-promote-form.test.tsx`: pass `fileId="file-abc"`, assert hidden input regex for `name="fileId"` and `value="file-abc"`.
    3. Extend `tests/suggestion-card.test.tsx`: test count copy appears when simulating promoted state with applyResult (use client render + state update OR export a minimal test-only prop `initialApplyResult` only if needed — prefer testing rendered output after callback).
    4. Extend `tests/import-suggestions-page.test.tsx`: mock `getPlatformIdForUserFile` returning `2` or `notFound` on null; update `vi.mock('@/lib/dal/files')` to export both helpers.
    Run `yarn test tests/suggestion-card.test.tsx tests/suggestion-promote-form.test.tsx tests/import-suggestions-page.test.tsx` then full `yarn test`.
  </action>
  <verify>
    <automated>yarn test tests/suggestion-card.test.tsx tests/suggestion-promote-form.test.tsx tests/import-suggestions-page.test.tsx 2>&1 | tail -5 | grep -Eiq "pass|✓| 0 failed| no failures" && grep -q "categorizzate" components/import/suggestion-card.tsx && echo UI_OK || (echo UI_FAIL && exit 1)</automated>
  </verify>
  <done>Card shows persistent inline counts; all UI/page tests pass; Italian copy on card only; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 53 regression slice + language gate</name>
  <files>(verification only)</files>
  <read_first>
    - 53-VALIDATION.md §Sampling Rate (per-task and per-wave commands).
    - CLAUDE.md Language Convention.
  </read_first>
  <acceptance_criteria>
    - `yarn test tests/pattern-application.test.ts tests/pattern-actions.test.ts tests/suggestion-card.test.tsx tests/suggestion-promote-form.test.tsx tests/import-suggestions-page.test.tsx` passes.
    - Full `yarn test` passes with 0 failures.
    - `yarn check:language` passes.
  </acceptance_criteria>
  <action>
    Run the Phase 53 regression slice from 53-VALIDATION.md, then full suite and language gate. Fix any failures within Phase 53 scope only. Record pass counts in SUMMARY.
  </action>
  <verify>
    <automated>yarn test tests/pattern-application.test.ts tests/pattern-actions.test.ts tests/suggestion-card.test.tsx tests/suggestion-promote-form.test.tsx tests/import-suggestions-page.test.tsx 2>&1 | tail -3 | grep -Eiq "fail" && (echo SLICE_FAIL && exit 1) || (yarn check:language && echo PHASE53_OK)</automated>
  </verify>
  <done>Phase 53 slice + full suite + language gate green; counts recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC page → client components | `fileId` and `platformId` are server-resolved props; client only echoes `fileId` in hidden field for action correlation. |
| client → Server Action | Hidden `fileId` is untrusted; action re-validates via Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Information disclosure | platformId exposed to client | accept | platformId is non-secret taxonomy metadata; authorization enforced server-side on apply. |
| T-53-08 | Spoofing | Client omits fileId | mitigate | Action rejects missing fileId (Plan 02); form always renders hidden input when wired. |
| T-53-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. No checkpoint needed. |
</threat_model>

<verification>
- `yarn test tests/suggestion-card.test.tsx tests/suggestion-promote-form.test.tsx tests/import-suggestions-page.test.tsx` — hidden fileId + count copy + page platform guard.
- Full `yarn test` green.
- `yarn check:language` passes.
- Manual UAT (optional): promote Fineco suggestion → counts visible on card, cross-platform expenses untouched.
</verification>

<success_criteria>
- User observes separated updated/not-updated counts inline on the promoted suggestion card (ROADMAP SC-2).
- Counts persist while page is open; card not removed on success.
- fileId/platformId threading ready for Phase 54 discovery surfaces.
</success_criteria>

<output>
Create `.planning/phases/53-retroactive-application/53-03-SUMMARY.md` when done.
</output>
