---
phase: 55-import-summary-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/import/import-preview.tsx
  - tests/import-preview-ui.test.tsx
autonomous: true
requirements:
  - SUMUI-01

must_haves:
  truths:
    - "ImportPreview renderizza al massimo 10 righe di sampleRows anche quando il service ne passa 25"
    - "SuggestionSection non è più renderizzata nell'analyze page pre-import"
    - "Il resto della UI ImportPreview (summary tiles, format override, warnings, errors, confirm button) rimane invariato"
    - "I test di ImportPreview coprono il cap a 10 righe e l'assenza di SuggestionSection"
  artifacts:
    - path: "components/import/import-preview.tsx"
      provides: "ImportPreview senza SuggestionSection; sampleRows.slice(0, 10) in JSX"
    - path: "tests/import-preview-ui.test.tsx"
      provides: "Test per cap a 10 righe; fixture senza patternSuggestions"
  key_links:
    - from: "components/import/import-preview.tsx"
      to: "tests/import-preview-ui.test.tsx"
      via: "fixture baseResult non include più patternSuggestions; test SUMUI-01 verifica max 10 rows"
      pattern: "sampleRows.slice"
---

<objective>
Applicare il cap di 10 righe al campione di transazioni in ImportPreview (SUMUI-01, D-05) e rimuovere SuggestionSection dalla analyze page pre-import (D-08). Aggiornare i test correlati.

Purpose: L'analyze page mostra al massimo 10 transazioni di esempio (non 25), e non mostra più suggerimenti pattern pre-import.
Output: import-preview.tsx senza SuggestionSection; slice(0,10) applicato a sampleRows; test green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-import-summary-ux/55-CONTEXT.md
@.planning/phases/55-import-summary-ux/55-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cap sampleRows a 10 e rimuovere SuggestionSection da ImportPreview (SUMUI-01, D-05, D-08)</name>
  <files>components/import/import-preview.tsx</files>

  <read_first>
    - components/import/import-preview.tsx (intero file — capire struttura del component, posizione del map su sampleRows, posizione del SuggestionSection JSX e del suo import)
    - .planning/phases/55-import-summary-ux/55-PATTERNS.md (sezione "import-preview.tsx" — posizioni esatte delle righe da modificare)
  </read_first>

  <action>
    In components/import/import-preview.tsx, eseguire le seguenti modifiche atomiche:

    1. Cap sampleRows (SUMUI-01, D-05): trovare il blocco result.sampleRows.map((row) => ( nella sezione della tabella di campionamento e aggiungere .slice(0, 10) prima di .map — risultato: result.sampleRows.slice(0, 10).map((row) => (. Non modificare il tipo di result né la struttura del component; è una modifica di 1 token a render time.

    2. Rimuovere SuggestionSection (D-08): 
       a. Eliminare la riga di import di SuggestionSection (import { SuggestionSection } from './suggestion-section' — riga ~28). Verificare prima con grep che SuggestionSection non sia usata altrove nel file oltre al punto da rimuovere.
       b. Eliminare il blocco JSX che renderizza SuggestionSection (la riga con <SuggestionSection suggestions={result.patternSuggestions} ... /> — riga ~214), incluso l'eventuale commento descrittivo sopra (blocco REV-01 menzionato nei PATTERNS).
       c. Verificare che nessun'altra prop o variabile sia diventata inutilizzata dopo la rimozione (es. categories prop usata solo da SuggestionSection — se non ha altri consumer, rimuoverla dal Props type e dalla firma del component).

    Dopo le modifiche, eseguire: npx tsc --noEmit per verificare zero errori.
  </action>

  <verify>
    <automated>grep -c "SuggestionSection" components/import/import-preview.tsx | grep -q "^0$" && echo "PASS: SuggestionSection rimossa" && grep -c "slice(0, 10)" components/import/import-preview.tsx | grep -q "^1$" && echo "PASS: slice(0,10) presente" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo "PASS: TypeScript clean"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "SuggestionSection" components/import/import-preview.tsx restituisce 0 (né import né JSX)
    - grep -c "patternSuggestions" components/import/import-preview.tsx restituisce 0
    - grep -c "slice(0, 10)" components/import/import-preview.tsx restituisce 1 (esattamente nel punto del map)
    - npx tsc --noEmit esce con codice 0
  </acceptance_criteria>

  <done>ImportPreview renderizza max 10 righe di esempio e non contiene più SuggestionSection né riferimenti a patternSuggestions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggiornare tests/import-preview-ui.test.tsx per SUMUI-01 e D-08 (per D-06, D-07, D-08)</name>
  <files>tests/import-preview-ui.test.tsx</files>

  <read_first>
    - tests/import-preview-ui.test.tsx (intero file — capire struttura della suite, fixture baseResult, test REV-01 su patternSuggestions, test esistenti su sampleRows)
    - components/import/import-preview.tsx (versione già modificata nel Task 1 — capire quali props e comportamenti sono ora testabili)
  </read_first>

  <behavior>
    - Test rimosso: il test che verifica che SuggestionSection venga renderizzata quando patternSuggestions è non-vuoto (test REV-01)
    - Test rimosso: il test che verifica che SuggestionSection non venga renderizzata quando patternSuggestions è vuoto
    - Test aggiunto/aggiornato SUMUI-01: con un baseResult che ha 25 sampleRows, ImportPreview deve renderizzare esattamente 10 righe nella tabella (non 25)
    - Test aggiornato: la fixture baseResult non include più la proprietà patternSuggestions
  </behavior>

  <action>
    In tests/import-preview-ui.test.tsx:

    1. Fixture baseResult (riga ~31): rimuovere la proprietà patternSuggestions: [] dal costruttore della fixture. Il tipo ImportAnalysisResult non include più quel campo dopo il Plan 01.

    2. Blocco test REV-01 (righe ~97–130): eliminare i test che verificano la presenza/assenza di SuggestionSection in funzione di patternSuggestions (entrambi i test — quello con patternSuggestions vuoto e quello con patternSuggestions non-vuoto). Ricercare con "Suggerimenti pattern section" o "patternSuggestions" per localizzarli.

    3. Test SUMUI-01 (aggiungere o aggiornare): aggiungere un test che:
       - Costruisce un baseResult con sampleRows: Array.from({ length: 25 }, (_, i) => ({ /* riga minimale compatibile col tipo SampleRow */ description: `DESC-${i}`, amount: '10.00', date: '2024-01-01', rawColumns: [] }))
       - Renderizza <ImportPreview result={...} categories={[]} onConfirm={vi.fn()} />
       - Verifica che il numero di righe renderizzate nella tabella di campionamento sia 10 (non 25) — ad esempio con screen.getAllByRole('row') e aspettandosi length === 11 (10 data rows + 1 header), oppure queryAllByTestId se già presenti.
       - Nome suggerito: 'SUMUI-01: renders at most 10 sample rows even when result has 25'

    Eseguire yarn test tests/import-preview-ui.test.tsx dopo le modifiche.
  </action>

  <verify>
    <automated>yarn test tests/import-preview-ui.test.tsx 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "patternSuggestions" tests/import-preview-ui.test.tsx restituisce 0
    - Il test "SUMUI-01: renders at most 10 sample rows" (o equivalente) è presente e passa
    - yarn test tests/import-preview-ui.test.tsx esce con codice 0 (tutti i test passano)
  </acceptance_criteria>

  <done>tests/import-preview-ui.test.tsx non ha più test su patternSuggestions; copre il cap a 10 righe; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nessuna nuova superficie | Modifica UI read-only; nessun nuovo input utente, endpoint o autenticazione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Tampering | ImportPreview — rimozione prop categories | accept | Verificare con tsc --noEmit che nessuna prop inutilizzata residua causi warning |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | Nessun pacchetto installato in questa phase |
</threat_model>

<verification>
yarn test tests/import-preview-ui.test.tsx deve essere green.
npx tsc --noEmit deve uscire con zero errori dopo le modifiche.
Aprire /import/[fileId] in browser (se server disponibile) e verificare che la tabella mostri max 10 righe e non compaia la sezione Suggerimenti pattern pre-import.
</verification>

<success_criteria>
- ImportPreview renderizza max 10 righe di sampleRows anche con 25 disponibili — per SUMUI-01 e D-05
- SuggestionSection rimossa dall'analyze page pre-import — per D-08
- Test suite per import-preview-ui green senza riferimenti a patternSuggestions
</success_criteria>

## Artifacts this phase produces

**Symbols modified (non creati):**
- `ImportPreview` component in `components/import/import-preview.tsx` — rimosso rendering di SuggestionSection; aggiunto slice(0,10) su sampleRows

**Symbols removed:**
- Import `SuggestionSection` da `components/import/import-preview.tsx`
- Prop `categories` da `components/import/import-preview.tsx` (se usata esclusivamente da SuggestionSection — verificare nel Task 1)

<output>
Create `.planning/phases/55-import-summary-ux/55-02-SUMMARY.md` when done
</output>
