---
phase: 42-overview-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["42-01", "42-02"]
files_modified:
  - lib/dal/overview.ts
  - tests/overview-dal.test.ts
  - CONTEXT.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "getOverview(year) returns four KPI totals plus YTD-vs-prior-YTD deltas"
    - "getYearsWithData() returns distinct years with at least one transaction, DESC"
    - "getMonthOverMonthCategoryChanges(year, monthIndex?, limit?) returns OUT-only movers above the €15 |Δ€| threshold, sorted by |Δ€| desc, isNew when prev=0, year-crossing"
    - "getOverviewChart(year) returns per-month income {recurring,extraordinary} + out per-nature segments"
    - "Every exported overview DAL function calls verifySession() before querying"
    - "CONTEXT.md redefines Reference Period as 'last month with data' and documents MonthOverMonthChange (variazione stays banned)"
  artifacts:
    - path: "lib/dal/overview.ts"
      provides: "getYearsWithData, getOverview, getMonthOverMonthCategoryChanges, getOverviewChart + MonthOverMonthChange/OverviewChartPoint types"
      min_lines: 120
      exports: ["getYearsWithData", "getOverview", "getMonthOverMonthCategoryChanges", "getOverviewChart"]
    - path: "CONTEXT.md"
      provides: "Reference Period redefinition + MonthOverMonthChange canonical term"
      contains: "MonthOverMonthChange"
  key_links:
    - from: "lib/dal/overview.ts"
      to: "verifySession"
      via: "first await in every cache(async …) function"
      pattern: "verifySession\\(\\)"
    - from: "lib/dal/overview.ts getOverview"
      to: "lib/dal/dashboard.ts getOverviewAmountTotals / getUncategorizedCount"
      via: "import + Promise.all over current + prior YTD spans"
      pattern: "getOverviewAmountTotals"
---

<objective>
Write the new year-scoped DAL file `lib/dal/overview.ts` with all four query functions (DATA-01/02/03), turning the failing `tests/overview-dal.test.ts` scaffold GREEN, and update the project glossary `CONTEXT.md` (DATA-04).

Purpose: This is the data-contract payload of the phase. The existing preset-based functions in `lib/dal/dashboard.ts` stay intact (D-09) — these are parallel year-scoped functions Phase 43 will adopt. `getOverviewChart` consumes the live `income_extraordinary` value applied in 42-02. Glossary edits codify `MonthOverMonthChange` as canonical and redefine Reference Period (glossary only — the Deviation engine is NOT changed this phase, D-12).

Output: `lib/dal/overview.ts`, a green `tests/overview-dal.test.ts`, and the CONTEXT.md glossary update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-overview-data-layer/42-CONTEXT.md
@.planning/phases/42-overview-data-layer/42-RESEARCH.md
@.planning/phases/42-overview-data-layer/42-PATTERNS.md
@.planning/phases/42-overview-data-layer/42-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (42-03) produces:
- New file `lib/dal/overview.ts` exporting `getYearsWithData`, `getOverview`, `getMonthOverMonthCategoryChanges`, `getOverviewChart`
- New types `MonthOverMonthChange` ({ categoryId, name, delta, isNew }) and `OverviewChartPoint` ({ month, label, income: {recurring, extraordinary}, out: Record<OutNature,string> })
- Filled `tests/overview-dal.test.ts` (now GREEN)
- CONTEXT.md glossary: Reference Period redefined, MonthOverMonthChange documented
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/dal/overview.ts (four year-scoped functions) and turn the scaffold green</name>
  <files>lib/dal/overview.ts, tests/overview-dal.test.ts</files>
  <read_first>
    - lib/dal/overview.ts (file being created — does not exist yet)
    - tests/overview-dal.test.ts (the failing scaffold this task must satisfy)
    - lib/dal/months-with-data.ts (header + getYearsWithData DISTINCT TO_CHAR pattern, alias yr not ym)
    - lib/dal/dashboard.ts (getOverviewAmountTotals line 431, getUncategorizedCount line 407, buildOverviewData, computeSavingsRate, computeDeltaPercent, getCategoryDeviations lines 969-1044, getMonthlyTrendByNature lines 1253-1295, DASHBOARD_TOTAL_EXPENSE_STATUSES line 239, notTransferCategory/notExcludedFromTotals/dateScopedTransactions lines 383-397)
    - lib/utils/decimal.ts (toDecimal, toDbDecimal)
    - lib/utils/date.ts (monthsBetween for zero-fill buckets)
    - app/proto/overview/mock-data.ts (intended return shapes: income {recurring,extraordinary}, USCITE_NATURES, Mover, noise floor)
    - .planning/phases/42-overview-data-layer/42-PATTERNS.md (lib/dal/overview.ts section — all four query bodies + types)
    - .planning/phases/42-overview-data-layer/42-RESEARCH.md (Patterns 2-5 + Pitfalls 3/4/5)
  </read_first>
  <behavior>
    - getYearsWithData(): SELECT DISTINCT TO_CHAR(occurred_at,'YYYY') AS yr … ORDER BY yr DESC; returns string[] DESC; [] on no data.
    - getOverview(year): pre-fetch last month with data in the year (MAX(TO_CHAR(occurred_at,'YYYY-MM'))); currentTo = last day of that month, currentFrom = Jan 1; prior-year span uses the SAME end-month index (equal-span YTD). Calls getOverviewAmountTotals + getUncategorizedCount twice via Promise.all, feeds buildOverviewData. Four KPI totals + deltas.
    - getMonthOverMonthCategoryChanges(year, monthIndex?, limit?): two windows — current month + previous CALENDAR month (prevYear = monthIndex===0 ? year-1 : year; prevMonthIndex = monthIndex===0 ? 11 : monthIndex-1), each via its own dateScopedTransactions (NOT year-bounded). Category-level SUM(abs(amount)) for category.type='out', DASHBOARD_TOTAL_EXPENSE_STATUSES, notTransferCategory, notExcludedFromTotals. Post-query: delta = Decimal(curr).minus(Decimal(prev)); isNew = prev==='0.00' && curr>0; filter abs(delta) >= 15; sort abs(delta) DESC; slice limit. Returns MonthOverMonthChange[].
    - getOverviewChart(year): per-month × coalesce(userSubcategoryOverride.nature, subCategory.nature) aggregation; income.recurring = nature 'income', income.extraordinary = nature 'income_extraordinary'; out = the 6 OUT natures. 12 zero-filled month buckets via monthsBetween. Returns OverviewChartPoint[].
    - All four: import 'server-only'; cache(async …); verifySession() first; DECIMAL as string; Decimal.js for arithmetic; try/catch → typed empty fallback.
  </behavior>
  <action>
    Create `lib/dal/overview.ts` starting with `import 'server-only'`, `cache` from react, drizzle operators, schema tables, `verifySession`, `toDecimal`/`toDbDecimal`, and the now-exported `getOverviewAmountTotals`, `getUncategorizedCount`, `buildOverviewData` plus `DASHBOARD_TOTAL_EXPENSE_STATUSES` and `notExcludedFromTotals` from `@/lib/dal/dashboard`. Inline the private helpers `notTransferCategory` and `dateScopedTransactions` (acceptable per 42-PATTERNS Shared Patterns — overview.ts is a separate concern file). Implement the four functions per `<behavior>`, each gating on `verifySession()` as the first await and wrapping its query in try/catch → empty fallback. Define and export the `MonthOverMonthChange` ({ categoryId: number; name: string; delta: string; isNew: boolean }) and `OverviewChartPoint` types. For movers: apply the €15 threshold on `toDecimal(delta).abs()` and sort by `toDecimal(delta).abs()` DESC in TypeScript (not SQL), mirroring buildDeviationDataset. For the chart: zero-fill all months with `monthsBetween(from, to)` then fill from rows. Use the Italian month label per mock-data.ts. Never native JS arithmetic on amounts. Then fill `tests/overview-dal.test.ts` assertions so the scaffold passes against the real implementation.
  </action>
  <verify>
    <automated>yarn test tests/overview-dal.test.ts && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - getYearsWithData() returns distinct years DESC; empty array on no data
    - getOverview(year) YTD bound = last month with data; prior-year compare uses the same month span
    - movers: OUT-only, |Δ€| >= 15 filter, sort by |Δ€| desc, isNew=true when prev=0, Jan 2026 compares against Dec 2025
    - getOverviewChart returns income.recurring/income.extraordinary + per-nature out, zero-filled missing months
    - every exported function's first await is verifySession()
    - yarn test tests/overview-dal.test.ts exits 0; yarn build exits 0
  </acceptance_criteria>
  <done>The four functions exist with verifySession gating; the overview-dal test suite is green and build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONTEXT.md glossary — Reference Period redefinition + MonthOverMonthChange (DATA-04)</name>
  <files>CONTEXT.md</files>
  <read_first>
    - CONTEXT.md (§ Dashboard e analisi — current Reference Period, Baseline, Deviation, Noise Threshold definitions and the _Avoid_ discipline)
    - .planning/phases/42-overview-data-layer/42-PATTERNS.md (CONTEXT.md MODIFY section — exact target wording)
    - .planning/phases/42-overview-data-layer/42-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Edit `CONTEXT.md` §"Dashboard e analisi": (1) redefine **Reference Period** as "l'ultimo mese per cui esistono transazioni importate per l'utente" (determined by query, not calendar), with a note that the Deviation engine `getDeviationDateRanges` still uses "ultimo mese di calendario completo" — a documented drift pending a deferred migration (D-12, glossary only — do NOT change the Deviation engine). (2) Add **MonthOverMonthChange** after the Deviation definition: variazione categoria vs the previous calendar month, query `getMonthOverMonthCategoryChanges`, UI copy "Rispetto al mese scorso" / "Dove hai speso di più" / "Dove hai risparmiato", with an `_Avoid_: "variazione" (riservato-deprecato)` line (D-13). Preserve the existing `_Avoid_` lines. Italian domain copy is intentional here (product glossary surface).
  </action>
  <verify>
    <automated>grep -q "MonthOverMonthChange" CONTEXT.md && grep -q "ultimo mese" CONTEXT.md && grep -q "getMonthOverMonthCategoryChanges" CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTEXT.md Reference Period reads "last month with data" with the documented Deviation-engine drift note
    - CONTEXT.md documents MonthOverMonthChange (canonical) with the query name and UI copy
    - "variazione" remains marked _Avoid_; existing _Avoid_ lines preserved
    - The Deviation engine code is NOT changed (glossary-only edit)
  </acceptance_criteria>
  <done>The glossary reflects the new terms; no code change to the Deviation engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DAL | Phase 43 RSC pages will call these functions; the only untrusted input is `year`/`monthIndex`/`limit` (numbers). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Information Disclosure | every getOverview*/getYearsWithData/getMonthOverMonthCategoryChanges/getOverviewChart | mitigate | Each function calls `verifySession()` as its first await; all queries are scoped to the returned `userId` (V4 Access Control, existing pattern maintained). This is THE real control to verify for this phase. |
| T-42-06 | Tampering | numeric params (year/monthIndex/limit) | accept | Numbers used only in date construction and LIMIT; no string interpolation into SQL beyond parameterized drizzle/`sql` templates; out-of-range values yield empty results, not access escalation |

Per 42-RESEARCH §Security Domain: no new auth/input/network surfaces; the maintained control is `verifySession()`-first on every new DAL function.
</threat_model>

<verification>
- `yarn test tests/overview-dal.test.ts` green (scaffold satisfied).
- `yarn test` full suite green; `yarn build` exits 0.
- Every exported function in `lib/dal/overview.ts` matches `verifySession()` as first await.
- CONTEXT.md contains MonthOverMonthChange + redefined Reference Period.
- `yarn check:language` clean (Italian only in the intentional CONTEXT.md glossary surface).
</verification>

<success_criteria>
- All four DATA requirements satisfied: getOverview, getMonthOverMonthCategoryChanges, getYearsWithData, glossary.
- Existing dashboard.ts preset functions remain intact (D-09).
- overview-dal test suite green; build green.
</success_criteria>

<output>
Create `.planning/phases/42-overview-data-layer/42-03-SUMMARY.md` when done.
</output>
