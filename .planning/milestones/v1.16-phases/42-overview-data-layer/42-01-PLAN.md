---
phase: 42-overview-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - lib/utils/nature-labels.ts
  - lib/dal/dashboard.ts
  - scripts/seed-extras.ts
  - drizzle/migrations/
  - tests/nature-labels.test.ts
  - tests/dashboard-dal.test.ts
  - tests/dashboard-charts.test.tsx
  - tests/overview-dal.test.ts
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "FlowNature union has 9 members (income_extraordinary added after income)"
    - "NATURE_LABELS relabels income to 'Entrate ricorrenti' and adds income_extraordinary 'Straordinaria'"
    - "A single-statement ADD VALUE migration file exists for income_extraordinary"
    - "getOverviewAmountTotals and getUncategorizedCount are exported from dashboard.ts"
    - "yarn build exits 0 and yarn test passes except the new overview-dal failing scaffold"
  artifacts:
    - path: "lib/utils/nature-labels.ts"
      provides: "FlowNature union + labels/order/colors including income_extraordinary"
      contains: "income_extraordinary"
    - path: "lib/db/schema.ts"
      provides: "flowNatureEnum with income_extraordinary"
      contains: "income_extraordinary"
    - path: "tests/overview-dal.test.ts"
      provides: "Failing test scaffold for the four overview DAL functions"
      contains: "getYearsWithData"
    - path: "drizzle/migrations/"
      provides: "Single-statement ALTER TYPE ADD VALUE migration"
      contains: "income_extraordinary"
  key_links:
    - from: "lib/dal/dashboard.ts"
      to: "lib/dal/overview.ts (next plan)"
      via: "exported getOverviewAmountTotals + getUncategorizedCount"
      pattern: "export (async )?function (getOverviewAmountTotals|getUncategorizedCount)"
---

<objective>
Lay the type-safe substrate for the income split and the test scaffolding for the new DAL. Extend the `flowNatureEnum` (D-02), fix the entire TypeScript blast-radius of the new `income_extraordinary` member so `yarn build` stays green, generate the standalone enum migration SQL, export the two private dashboard helpers the new DAL will reuse, and create/update the Wave 0 tests.

Purpose: Adding a member to the `FlowNature` union breaks every `Record<FlowNature | 'unclassified', …>` literal in the codebase. This plan absorbs that blast-radius in one wave (per the execution-order constraint in 42-PATTERNS.md) and produces a failing test scaffold so the DAL plan can run RED→GREEN. No DB is applied here and no `lib/dal/overview.ts` is written yet.

Output: Updated enum + labels + dashboard exports, a generated migration file, the new `tests/overview-dal.test.ts` failing scaffold, and three updated existing test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-overview-data-layer/42-CONTEXT.md
@.planning/phases/42-overview-data-layer/42-RESEARCH.md
@.planning/phases/42-overview-data-layer/42-PATTERNS.md
@.planning/phases/42-overview-data-layer/42-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (42-01) produces, toward the phase total:
- New enum value `income_extraordinary` in `flowNatureEnum` (`lib/db/schema.ts`) and the `FlowNature` union (`lib/utils/nature-labels.ts`)
- Relabel: `income` → "Entrate ricorrenti"; new label `income_extraordinary` → "Straordinaria"
- New migration file `drizzle/migrations/00XX_*.sql` (single `ADD VALUE` statement)
- Newly-exported symbols: `getOverviewAmountTotals`, `getUncategorizedCount` (from `lib/dal/dashboard.ts`)
- New test file `tests/overview-dal.test.ts` (failing scaffold)
Subsequent plans add: the seed STEP (42-02), and `lib/dal/overview.ts` with `getOverview`, `getYearsWithData`, `getMonthOverMonthCategoryChanges`, `getOverviewChart`, the `MonthOverMonthChange` type, plus the CONTEXT.md glossary edit (42-03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing overview-dal test scaffold + update three existing tests for the 9-member union</name>
  <files>tests/overview-dal.test.ts, tests/nature-labels.test.ts, tests/dashboard-dal.test.ts, tests/dashboard-charts.test.tsx</files>
  <read_first>
    - tests/months-with-data-dal.test.ts (mock pattern source: vi.mock for db, server-only, react, verifySession)
    - tests/nature-labels.test.ts (file being modified — current 9-key / 8-non-null assertions)
    - tests/dashboard-dal.test.ts (file being modified — buildMonthlyNatureTrendData sorted segment-keys assertion)
    - tests/dashboard-charts.test.tsx (file being modified — NATURE_ORDER non-null count assertion)
    - .planning/phases/42-overview-data-layer/42-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/42-overview-data-layer/42-PATTERNS.md (test sections for tests/overview-dal.test.ts and the three updates)
  </read_first>
  <behavior>
    - tests/overview-dal.test.ts: one describe block per function — getYearsWithData (distinct years DESC, empty array on no data), getOverview (KPI strings, YTD bound = last month with data, equal-span prior-year compare), getMonthOverMonthCategoryChanges (OUT-only, €15 |Δ€| threshold, isNew when prev=0, year-crossing Jan→Dec, sort by |Δ€| DESC), getOverviewChart (per income-type/per-nature split, zero-fill missing months). Tests import from '@/lib/dal/overview' and FAIL because that module does not exist yet (RED scaffold).
    - tests/nature-labels.test.ts: FlowNature key count 9 → 10; ALL_NATURE_KEYS / EXPECTED_LABELS gain income_extraordinary; income expected label 'Entrate' → 'Entrate ricorrenti'; NATURE_ORDER length 9 → 10; non-null nature count 8 → 9; NATURE_COLORS keys 9 → 10.
    - tests/dashboard-dal.test.ts: buildMonthlyNatureTrendData sorted expected segment keys gains 'income_extraordinary'.
    - tests/dashboard-charts.test.tsx: NATURE_ORDER non-null count assertion 8 → 9.
  </behavior>
  <action>
    Create `tests/overview-dal.test.ts` mirroring the `vi.mock` header from `tests/months-with-data-dal.test.ts` (mock `server-only` → `{}`, `react` → `{ cache: (fn) => fn }`, `@/lib/db` → `{ db: { execute: vi.fn(), select: vi.fn() } }`, `@/lib/dal/auth` → `verifySession` resolving `{ userId: 'test-user' }`). Write one describe block per exported function with the cases listed in `<behavior>`; assert against the `MonthOverMonthChange` and `OverviewChartPoint` shapes from 42-PATTERNS.md. These tests reference `@/lib/dal/overview` which does not exist yet — this is the intentional RED scaffold that plan 42-03 turns GREEN. Then update the three existing test files per `<behavior>`, changing the exact numeric assertions (9→10 keys, 8→9 non-null natures) and adding `income_extraordinary` / the `'Entrate ricorrenti'` relabel to the fixtures. Comments and test names in English per the language convention.
  </action>
  <verify>
    <automated>yarn test tests/overview-dal.test.ts 2>&1 | grep -q "Cannot find module\|failed\|FAIL" && echo "RED scaffold confirmed" || (echo "scaffold should fail until 42-03" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - tests/overview-dal.test.ts exists with one describe block per overview DAL function and currently FAILS (module not found / assertions red)
    - tests/nature-labels.test.ts asserts FlowNature has 10 keys, NATURE_ORDER length 10, 9 non-null natures, NATURE_COLORS 10 keys, income label 'Entrate ricorrenti', income_extraordinary label 'Straordinaria'
    - tests/dashboard-dal.test.ts segment-keys array includes 'income_extraordinary'
    - tests/dashboard-charts.test.tsx non-null count assertion is 9
  </acceptance_criteria>
  <done>The new scaffold fails by design; the three updated test files assert the post-change 9/10 counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend flowNatureEnum + FlowNature union/labels/order/colors + dashboard blast-radius, then generate migration</name>
  <files>lib/db/schema.ts, lib/utils/nature-labels.ts, lib/dal/dashboard.ts, scripts/seed-extras.ts, drizzle/migrations/</files>
  <read_first>
    - lib/db/schema.ts (flowNatureEnum at line 52 — the values array being extended)
    - lib/utils/nature-labels.ts (FlowNature union line 1, NATURE_LABELS line 11, NATURE_ORDER line 23, NATURE_COLORS line 35 — all modified)
    - lib/dal/dashboard.ts (getUncategorizedCount line 407, getOverviewAmountTotals line 431 — add export; emptySegments line 664 — add key)
    - scripts/seed-extras.ts (NATURE_SLUGS Record<FlowNature, string[]> at line 55 — TypeScript requires the new key for build-green)
    - drizzle/migrations/0013_brief_pride.sql (single-statement ADD VALUE precedent)
    - drizzle/migrations/0016_chunky_pet_avengers.sql (multi ADD VALUE, no consumers precedent)
    - app/proto/overview/mock-data.ts (INCOME_COLORS.extraordinary = '#a7f3d0' — color source)
    - .planning/phases/42-overview-data-layer/42-RESEARCH.md (Pitfall 1 + Pitfall 2 — migration isolation + union exhaustiveness)
  </read_first>
  <behavior>
    - flowNatureEnum (schema.ts) values array gains 'income_extraordinary' positioned after 'income'.
    - FlowNature union gains '| income_extraordinary' after 'income'.
    - NATURE_LABELS: income → 'Entrate ricorrenti'; new income_extraordinary → 'Straordinaria'.
    - NATURE_ORDER: 'income_extraordinary' inserted immediately after 'income'.
    - NATURE_COLORS: income_extraordinary → '#a7f3d0'.
    - dashboard.ts emptySegments() gains income_extraordinary: ZERO_AMOUNT after income.
    - dashboard.ts getUncategorizedCount and getOverviewAmountTotals gain the export keyword.
    - scripts/seed-extras.ts NATURE_SLUGS gains an income_extraordinary key (value [] is acceptable here — the slug list is populated in 42-02) so the exhaustive Record over FlowNature still type-checks.
    - drizzle/migrations/00XX_*.sql contains exactly one statement: ALTER TYPE "public"."flow_nature" ADD VALUE IF NOT EXISTS 'income_extraordinary' AFTER 'income'.
  </behavior>
  <action>
    Edit `lib/db/schema.ts` `flowNatureEnum` to insert `"income_extraordinary"` after `"income"`. Edit `lib/utils/nature-labels.ts`: add the union member, relabel `income` to `'Entrate ricorrenti'`, add `income_extraordinary: 'Straordinaria'` to NATURE_LABELS, insert `'income_extraordinary'` after `'income'` in NATURE_ORDER, add `income_extraordinary: '#a7f3d0'` to NATURE_COLORS (lighter green per mock-data.ts INCOME_COLORS.extraordinary — ASSUMED A2, cosmetic, correctable in Phase 43). Edit `lib/dal/dashboard.ts`: add `export` to `getUncategorizedCount` (line 407) and `getOverviewAmountTotals` (line 431) per D-09 reuse plan; add `income_extraordinary: ZERO_AMOUNT` to `emptySegments()` after `income`. Edit `scripts/seed-extras.ts` NATURE_SLUGS Record: add `income_extraordinary: []` key so the `Record<FlowNature, string[]>` stays exhaustive (the slug membership is populated by 42-02; an empty list keeps build green now). Then run `yarn drizzle-kit generate` and HAND-VERIFY the emitted `drizzle/migrations/00XX_*.sql` contains ONLY the single `ALTER TYPE … ADD VALUE IF NOT EXISTS 'income_extraordinary' AFTER 'income'` statement (per Pitfall 1 — never combine ADD VALUE with a consuming statement). If drizzle-kit emits extra statements in that file, hand-edit to leave only the ADD VALUE. Do NOT run `yarn db:migrate` here and NEVER `drizzle-kit push`.
  </action>
  <verify>
    <automated>yarn build && yarn test tests/nature-labels.test.ts tests/dashboard-dal.test.ts tests/dashboard-charts.test.tsx && grep -rl "ADD VALUE IF NOT EXISTS 'income_extraordinary'" drizzle/migrations/ | head -1</automated>
  </verify>
  <acceptance_criteria>
    - FlowNature union has 9 members; NATURE_LABELS.income === 'Entrate ricorrenti'; NATURE_LABELS.income_extraordinary === 'Straordinaria'
    - flowNatureEnum values array contains 'income_extraordinary' after 'income'
    - getOverviewAmountTotals and getUncategorizedCount are exported from dashboard.ts
    - A migration file under drizzle/migrations/ contains the single ADD VALUE statement and no statement referencing the new value
    - yarn build exits 0; the three updated test files pass
  </acceptance_criteria>
  <done>yarn build is green, the three updated tests pass, and the standalone migration SQL exists and is hand-verified single-statement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits types, a migration file, and tests. No new auth/input/network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | drizzle/migrations/*.sql ADD VALUE | mitigate | Single-statement file, hand-verified; `IF NOT EXISTS` makes re-runs idempotent (Pitfall 1) |
| T-42-02 | Information Disclosure | dashboard.ts exported helpers | accept | `getOverviewAmountTotals`/`getUncategorizedCount` are server-only and still take a verified `userId` from the calling DAL function; exporting changes visibility only, not the auth gate |

Per 42-RESEARCH §Security Domain: no new auth/input/network surfaces in this phase. The one real control (every new overview DAL function gates on `verifySession()` first) is exercised in plan 42-03, not here.
</threat_model>

<verification>
- `yarn build` exits 0 (full TypeScript blast-radius absorbed).
- `yarn test tests/nature-labels.test.ts tests/dashboard-dal.test.ts tests/dashboard-charts.test.tsx` passes.
- `yarn test tests/overview-dal.test.ts` FAILS by design (RED scaffold for 42-03).
- Migration file is a single `ADD VALUE` statement.
- `yarn check:language` clean for the touched test/comment strings.
</verification>

<success_criteria>
- FlowNature has 9 members; all three Record literals + emptySegments updated.
- `income` relabeled to "Entrate ricorrenti"; `income_extraordinary` labeled "Straordinaria".
- Standalone enum migration generated and hand-verified.
- Two dashboard helpers exported for reuse.
- Wave 0 tests in place (1 new failing scaffold + 3 updated green).
</success_criteria>

<output>
Create `.planning/phases/42-overview-data-layer/42-01-SUMMARY.md` when done.
</output>
