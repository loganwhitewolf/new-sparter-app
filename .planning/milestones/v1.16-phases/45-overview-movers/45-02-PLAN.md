---
phase: 45-overview-movers
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - components/dashboard/overview/overview-chart.tsx
  - components/dashboard/overview/overview-movers-panel.tsx
  - components/dashboard/overview/overview-movers-section.tsx
  - app/(app)/dashboard/overview/page.tsx
autonomous: true
requirements: [MOVE-01, MOVE-02, MOVE-04, MOVE-05]
must_haves:
  truths:
    - "Clicking any month's bar highlights that month's Entrate and Uscite bars (full opacity) and dims the rest to 0.4"
    - "Clicking a bar updates the inline movers panel to that month's data"
    - "The panel is visible on initial load, defaulted to the last month with transactions (not December)"
    - "The panel shows 'Dove hai speso di più' (red) and 'Dove hai risparmiato' (green); an empty section is hidden"
    - "Selecting the first available month (no prior month) shows an empty state"
    - "Chart highlight and panel month always agree (single shared selectedMonth state)"
  artifacts:
    - path: "components/dashboard/overview/overview-movers-section.tsx"
      provides: "Client parent owning selectedMonth; wraps OverviewChart + OverviewMoversPanel"
      contains: "use client"
    - path: "components/dashboard/overview/overview-movers-panel.tsx"
      provides: "Inline two-section movers panel with empty state and loading state"
      contains: "use client"
    - path: "components/dashboard/overview/overview-chart.tsx"
      provides: "Controlled chart: selectedMonth + onMonthSelect props, D-03 Cell highlight on both Bars"
    - path: "app/(app)/dashboard/overview/page.tsx"
      provides: "deriveDefaultMonthIndex + initial movers prefetch, renders OverviewMoversSection"
  key_links:
    - from: "app/(app)/dashboard/overview/page.tsx"
      to: "components/dashboard/overview/overview-movers-section.tsx"
      via: "props: year, defaultMonthIndex, initialMovers, chart"
      pattern: "OverviewMoversSection"
    - from: "components/dashboard/overview/overview-movers-section.tsx"
      to: "lib/actions/overview.ts"
      via: "fetchMovers in useTransition on month change"
      pattern: "fetchMovers"
    - from: "components/dashboard/overview/overview-movers-panel.tsx"
      to: "components/dashboard/overview/overview-movers-format.ts"
      via: "formatMoverLine + splitMovers"
      pattern: "formatMoverLine|splitMovers"
    - from: "components/dashboard/overview/overview-chart.tsx"
      to: "selectedMonth highlight"
      via: "Cell fillOpacity on Entrate and Uscite Bars"
      pattern: "fillOpacity"
---

<objective>
Wire the interactive movers drill-down. Promote `OverviewChart` from self-owned `selectedMonth` state to a controlled component (`selectedMonth` + `onMonthSelect` props), activate the D-03 Cell highlight scaffold on BOTH the Uscite and Entrate Bars, build the inline `OverviewMoversPanel`, and introduce a thin client parent `OverviewMoversSection` that owns the single shared `selectedMonth` so chart highlight and panel month never drift. Finally extend `OverviewDataSection` in `page.tsx` to compute the real `defaultMonthIndex` (D-04) and prefetch the initial movers (D-03).

Purpose: This is the phase's load-bearing architectural decision — lift `selectedMonth` to a shared parent rather than duplicating state. One source of truth keeps MOVE-01 (click → highlight + panel) and MOVE-04 (default + highlight) consistent.

Output: A controlled chart, the movers panel, the shared section parent, and the server-side default-month derivation + prefetch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-overview-movers/45-CONTEXT.md
@.planning/phases/45-overview-movers/45-PATTERNS.md

# Files modified in this plan (current state)
@components/dashboard/overview/overview-chart.tsx
@app/(app)/dashboard/overview/page.tsx

# Contracts created in Plan 01 (consumed here)
@lib/actions/overview.ts
@components/dashboard/overview/overview-movers-format.ts

# Client-component analog: useState + useTransition + isPending loading block
@components/expenses/expense-transactions-dialog.tsx

# DAL types + signatures (read-only)
@lib/dal/overview.ts

# Money display + decimal helper
@components/dashboard/overview/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make OverviewChart controlled + activate D-03 highlight on both Bars</name>
  <files>components/dashboard/overview/overview-chart.tsx</files>
  <read_first>
    - components/dashboard/overview/overview-chart.tsx (current: self-owned selectedMonth at line 39; onMonthSelect optional at line 33; Uscite Cell scaffold at lines 154–162; Entrate Bar has NO Cell array; onClick stubs at lines 120, 140)
    - .planning/phases/45-overview-movers/45-CONTEXT.md (D-06: fillOpacity selected=1 / unselected=0.4, cursor pointer; both Entrate and Uscite)
    - .planning/phases/45-overview-movers/45-PATTERNS.md (lines 195–242: props shape change, Cell activation, Entrate parallel Cell array, onClick wiring on both Bars)
  </read_first>
  <action>
    Change `OverviewChartProps` so the component is controlled: add `selectedMonth: number` and make `onMonthSelect: (monthIndex: number) => void` required (drop the `?`). Keep the existing `data` and the filter-chip internals untouched.

    Remove the internal `const [selectedMonth, setSelectedMonth] = useState(() => data.length - 1)` (line 39) entirely — `selectedMonth` now comes from props, the parent owns it. Remove the now-unused `useState` import only if no other state uses it (the filter-chip `includedIncome`/`includedOut` still need `useState`, so keep the import).

    On BOTH Bars (Entrate at line 113, Uscite at line 134) change `onClick={(_, index) => setSelectedMonth(index)}` to `onClick={(_, index) => onMonthSelect(index)}`.

    Activate the Uscite Cell scaffold (lines 154–162): change `fillOpacity={1}` to `fillOpacity={i === selectedMonth ? 1 : 0.4}` and `cursor="default"` to `cursor="pointer"`.

    Add a parallel Cell array INSIDE the Entrate `<Bar dataKey="entrate">` (it currently has only a `<LabelList>`, no Cell map). Map over `rows` rendering `<Cell key={i} fill="var(--color-entrate)" fillOpacity={i === selectedMonth ? 1 : 0.4} cursor="pointer" />`, mirroring the Uscite Cell block. This makes the Entrate bar highlight in lockstep with Uscite (D-06).

    Leave the per-Bar `cursor="default"` prop on the `<Bar>` elements as-is — the per-Cell `cursor="pointer"` overrides it (noted in PATTERNS.md line 241).
  </action>
  <verify>
    <automated>grep -n "i === selectedMonth ? 1 : 0.4" components/dashboard/overview/overview-chart.tsx | wc -l; grep -c "onMonthSelect(index)" components/dashboard/overview/overview-chart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "i === selectedMonth ? 1 : 0.4" components/dashboard/overview/overview-chart.tsx` returns 2 (Entrate Cell + Uscite Cell both highlighted)
    - `grep -c "onMonthSelect(index)" components/dashboard/overview/overview-chart.tsx` returns 2 (both Bars wired)
    - `grep -c "setSelectedMonth" components/dashboard/overview/overview-chart.tsx` returns 0 (internal state removed; now controlled)
    - `grep -c 'cursor="pointer"' components/dashboard/overview/overview-chart.tsx` returns 2 (both Cell arrays)
    - `selectedMonth: number` and required `onMonthSelect: (monthIndex: number) => void` are in `OverviewChartProps`
  </acceptance_criteria>
  <done>OverviewChart is controlled by props; clicking either bar calls onMonthSelect; both Entrate and Uscite bars dim unselected months to 0.4 and show pointer cursor.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewMoversPanel (inline two-section panel) + OverviewMoversSection (shared-state parent)</name>
  <files>components/dashboard/overview/overview-movers-panel.tsx, components/dashboard/overview/overview-movers-section.tsx</files>
  <read_first>
    - components/expenses/expense-transactions-dialog.tsx (lines 30–45 useState+useTransition; lines 79–90 isPending Loader2 loading block + empty-state branch)
    - lib/actions/overview.ts (fetchMovers signature from Plan 01: (year, monthIndex) => Promise<{ movers, error }>)
    - components/dashboard/overview/overview-movers-format.ts (formatMoverLine + splitMovers from Plan 01)
    - components/dashboard/overview/overview-chart.tsx (now-controlled props shape from Task 1)
    - lib/dal/overview.ts (MonthOverMonthChange type)
    - .planning/phases/45-overview-movers/45-CONTEXT.md (D-01 inline placement; D-02 heading + subsection labels; D-07 empty state; D-08 line format; specifics: red var(--total-out), green var(--total-in), empty-state example copy)
    - .planning/phases/45-overview-movers/45-PATTERNS.md (lines 65–148 panel structure + loading + humanized format)
  </read_first>
  <action>
    Create `components/dashboard/overview/overview-movers-section.tsx` (`'use client'`). This is the shared-state parent — the phase's key architectural decision. Props: `{ data: OverviewChartPoint[]; year: number; defaultMonthIndex: number; initialMovers: MonthOverMonthChange[] }`. It owns ALL the shared interaction state:
    - `const [selectedMonth, setSelectedMonth] = useState(defaultMonthIndex)`
    - `const [movers, setMovers] = useState<MonthOverMonthChange[]>(initialMovers)`
    - `const [isPending, startTransition] = useTransition()`
    - A `handleMonthSelect(monthIndex: number)` callback that sets `selectedMonth` immediately (so the chart highlight is instant) and inside `startTransition` calls `await fetchMovers(year, monthIndex)`, setting `movers` only when `!result.error`. Follow the useTransition pattern from the expense dialog analog.

    Render the chart `<section>` (move the heading "Entrate e uscite per mese" wrapper here OR keep it in page.tsx — see Task 3; keep the chart inside this section) passing `<OverviewChart data={data} selectedMonth={selectedMonth} onMonthSelect={handleMonthSelect} />`, immediately followed by `<OverviewMoversPanel year={year} selectedMonth={selectedMonth} movers={movers} isPending={isPending} data={data} />`. The panel renders inline below the chart (D-01).

    Create `components/dashboard/overview/overview-movers-panel.tsx` (`'use client'`). Props: `{ year: number; selectedMonth: number; movers: MonthOverMonthChange[]; isPending: boolean; data: OverviewChartPoint[] }`. It is presentational (no fetching — state lives in the section parent). Implement:
    - Heading (D-02): `{monthName} {year} vs {prevMonthName} {prevYear}`. Derive month names from a local Italian month-name array (e.g. `['Gennaio','Febbraio',...]`) indexed by `selectedMonth` and `selectedMonth - 1`; when `selectedMonth === 0` the previous month is `Dicembre` and previous year is `year - 1` (year-crossing, mirrors the DAL guard). Example: `Maggio 2025 vs Aprile 2025`.
    - Loading: when `isPending`, render the Loader2 spinner block from the analog (lines 79–82) inside the panel body — subtle, panel-scoped, NOT a full-page loader (D-05 / specifics).
    - Sections: call `splitMovers(movers)` → `{ increases, savings }`. Render the red subsection (`<p>` label "Dove hai speso di più" with `className` using `text-[var(--total-out)]`) ONLY when `increases.length > 0`; render the green subsection (label "Dove hai risparmiato", `text-[var(--total-in)]`) ONLY when `savings.length > 0` (MOVE-02 / D-02 hide-empty). Each list item is `formatMoverLine(m)` keyed by `m.categoryId`.
    - Empty state (MOVE-05 / D-07): when `!isPending && movers.length === 0`, render the contextual Italian message `Nessuna variazione significativa — mese di partenza o nessuna spesa sopra €15.` This covers the first-month case (no prior month) and the all-below-€15 case.
    - Wrap in `<section aria-labelledby="movers-heading" className="space-y-3">` with the heading carrying `id="movers-heading"`, mirroring the chart section's a11y shape.

    All user-facing strings Italian; identifiers/comments English. Do NOT call `fetchMovers` from the panel (the section owns fetching). Do NOT touch the `?year=` URL.
  </action>
  <verify>
    <automated>yarn build 2>&1 | grep -iE "overview-movers|error TS" | head -20; grep -c "splitMovers\|formatMoverLine" components/dashboard/overview/overview-movers-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files start with `'use client'`
    - `grep -c "useState\|useTransition" components/dashboard/overview/overview-movers-section.tsx` ≥ 2 (section owns state)
    - `grep -c "useState\|useTransition\|fetchMovers" components/dashboard/overview/overview-movers-panel.tsx` returns 0 (panel is presentational; no fetching/state)
    - `grep -c "splitMovers" components/dashboard/overview/overview-movers-panel.tsx` ≥ 1 and `grep -c "formatMoverLine" components/dashboard/overview/overview-movers-panel.tsx` ≥ 1
    - `grep -c "Dove hai speso di più\|Dove hai risparmiato" components/dashboard/overview/overview-movers-panel.tsx` returns 2 (both section labels)
    - `grep -c "Nessuna variazione significativa" components/dashboard/overview/overview-movers-panel.tsx` returns 1 (empty state)
    - `grep -c "var(--total-out)\|var(--total-in)" components/dashboard/overview/overview-movers-panel.tsx` returns 2 (red/green accents)
    - `grep -c "increases.length > 0\|savings.length > 0" components/dashboard/overview/overview-movers-panel.tsx` returns 2 (empty sections hidden)
    - `yarn build` shows no TS error in either new file
  </acceptance_criteria>
  <done>The section parent owns the single selectedMonth + movers state and refetches via fetchMovers/useTransition; the presentational panel renders the heading, two hide-when-empty colored sections, the loading spinner, and the empty state — all in Italian.</done>
</task>

<task type="auto">
  <name>Task 3: Wire defaultMonthIndex derivation + initial movers prefetch into OverviewDataSection</name>
  <files>app/(app)/dashboard/overview/page.tsx</files>
  <read_first>
    - app/(app)/dashboard/overview/page.tsx (current: OverviewDataSection lines 23–45; chart section heading lines 37–42; toDecimal import line 10; isYearWithNoData helper lines 18–20)
    - lib/dal/overview.ts (OverviewChartPoint shape: income {recurring, extraordinary} as strings, out is Record<OutNature, string> — an OBJECT, use Object.values; getMonthOverMonthCategoryChanges signature)
    - components/dashboard/overview/overview-movers-section.tsx (props expected: data, year, defaultMonthIndex, initialMovers — from Task 2)
    - .planning/phases/45-overview-movers/45-CONTEXT.md (D-03 prefetch server-side; D-04 default = last month with activity, never data.length-1)
    - .planning/phases/45-overview-movers/45-PATTERNS.md (lines 152–191 deriveDefaultMonthIndex + prefetch; NOTE: PATTERNS shows p.out.reduce which is WRONG — out is an object, see action below)
  </read_first>
  <action>
    Extend the DAL import on line 2 to include `getMonthOverMonthCategoryChanges` alongside the existing `getOverview, getOverviewChart, getYearsWithData`.

    Add a module-level helper `deriveDefaultMonthIndex(chart: OverviewChartPoint[]): number` (D-04). Iterate from the last index down to 0. For each point compute total activity using `toDecimal` (already imported, line 10): start from `toDecimal(p.income.recurring).plus(toDecimal(p.income.extraordinary))`, then add each OUT value. IMPORTANT: `p.out` is `Record<OutNature, string>` (an object), NOT an array — use `Object.values(p.out).reduce((acc, v) => acc.plus(toDecimal(v)), <accumulator>)`. The PATTERNS.md snippet shows `p.out.reduce(...)` which would throw; use `Object.values(p.out)`. Return the first index (scanning downward) where the total is not zero (`!total.isZero()`); if none, return `0`. Import the `OverviewChartPoint` type from `@/lib/dal/overview` for the helper signature.

    In `OverviewDataSection`, after the existing `Promise.all` resolves `[overview, chart]`, compute `const defaultMonthIndex = deriveDefaultMonthIndex(chart)` then `const initialMovers = await getMonthOverMonthCategoryChanges(year, defaultMonthIndex)` (sequential after chart — it depends on the derived index).

    Replace the existing chart `<section>` (lines 37–42, which renders the heading + `<OverviewChart data={chart} />`) with `<OverviewMoversSection data={chart} year={year} defaultMonthIndex={defaultMonthIndex} initialMovers={initialMovers} />`. Move the "Entrate e uscite per mese" heading + chart `<section>` wrapper INTO `OverviewMoversSection` (Task 2) so the chart and the movers panel share one client boundary and one selectedMonth; the page just renders the section. Remove the now-unused direct `OverviewChart` import from page.tsx and add the `OverviewMoversSection` import. Keep `OverviewNudge` and `KpiRow` exactly as they are.

    Do not change `DashboardOverviewPage`, `resolveYear`, the Suspense boundary, or the empty-state branches.
  </action>
  <verify>
    <automated>yarn build 2>&1 | grep -iE "page.tsx|error TS" | head -20; grep -c "Object.values(p.out)\|OverviewMoversSection\|deriveDefaultMonthIndex" "app/(app)/dashboard/overview/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deriveDefaultMonthIndex" "app/(app)/dashboard/overview/page.tsx"` returns 2 (definition + call)
    - `grep -c "Object.values(p.out)" "app/(app)/dashboard/overview/page.tsx"` returns 1 (object iterated correctly, not `.reduce` on object)
    - `grep -c "data.length - 1" "app/(app)/dashboard/overview/page.tsx"` returns 0 (D-04: never the naive last index)
    - `grep -c "getMonthOverMonthCategoryChanges" "app/(app)/dashboard/overview/page.tsx"` returns ≥1 (initial prefetch)
    - `grep -c "OverviewMoversSection" "app/(app)/dashboard/overview/page.tsx"` returns ≥1 (rendered) and `grep -c "import { OverviewChart }" "app/(app)/dashboard/overview/page.tsx"` returns 0 (direct chart import removed)
    - `grep -c "toDecimal" "app/(app)/dashboard/overview/page.tsx"` ≥ 2 (Decimal used for the default-month sum, not native arithmetic)
    - `yarn build` passes (no TS error in page.tsx)
  </acceptance_criteria>
  <done>OverviewDataSection derives the real last-month-with-data index via Decimal helpers (using Object.values on the out record), prefetches that month's movers, and renders OverviewMoversSection with the initial data so the panel is populated on first paint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (bar click) → `fetchMovers` Server Action | `monthIndex` from client state crosses to the server; bounded in Plan 01's action |
| server component → client component props | `initialMovers`, `defaultMonthIndex` serialized from the authenticated server render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | monthIndex from client bar click | mitigate | Bounded server-side in `fetchMovers` (Plan 01, Number.isInteger 0–11); the client can only emit indices 0–11 from the chart anyway |
| T-45-02 | Information Disclosure | initial movers prefetch in OverviewDataSection | mitigate | Runs inside the already-authenticated server page; DAL scopes by `userId` via verifySession |
| T-45-04 | Tampering | XSS via mover category `name` rendered in panel | mitigate | React escapes text content by default; `formatMoverLine` output is rendered as a string child, never via dangerouslySetInnerHTML |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; nothing to verify |
</threat_model>

<verification>
- `yarn build` — chart, panel, section, and page all type-check; controlled props flow correctly
- `yarn check:language` — Italian only in UI strings; identifiers/comments English
- grep gates above confirm: both bars highlight, single shared state, hide-empty sections, empty state present, default index via Decimal
</verification>

<success_criteria>
- Clicking a month's bar highlights both bars for that month (others 0.4) and updates the panel (MOVE-01)
- Panel splits into red "speso di più" / green "risparmiato"; empty section hidden (MOVE-02)
- Panel visible on load defaulted to last month with data, that month highlighted (MOVE-04)
- First-available-month selection yields the empty state (MOVE-05)
- Chart highlight and panel month share one selectedMonth (no drift)
</success_criteria>

<output>
Create `.planning/phases/45-overview-movers/45-02-SUMMARY.md` when done
</output>
