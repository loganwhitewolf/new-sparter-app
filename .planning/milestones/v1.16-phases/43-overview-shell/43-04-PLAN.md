---
phase: 43-overview-shell
plan: 04
type: execute
wave: 3
depends_on: ["43-03"]
files_modified:
  - components/dashboard/kpi-cards.tsx
  - components/dashboard/kpi-card.tsx
  - components/dashboard/entrate-uscite-chart.tsx
  - components/dashboard/bilancio-bars-chart.tsx
  - components/dashboard/overview-filters.tsx
  - components/dashboard/overview-skeleton.tsx
  - components/dashboard/trend-skeleton.tsx
  - app/proto/overview/
autonomous: true
requirements: [CHART-03, KPI-04]
must_haves:
  truths:
    - "The superseded overview components are deleted and yarn build stays green"
    - "The redesigned overview tab still renders correctly after deletion"
    - "No dead import references the deleted components"
  artifacts:
    - path: "app/(app)/dashboard/overview/page.tsx"
      provides: "Still renders the new shell; no longer imports any deleted component"
      contains: "OverviewChart"
  key_links:
    - from: "build graph"
      to: "deleted components"
      via: "zero remaining importers"
      pattern: "kpi-cards|entrate-uscite-chart|bilancio-bars-chart|overview-filters"
---

<objective>
Delete the superseded shipped overview components (D-02), confirmed consumed only by the old
`overview/page.tsx` (now rewritten in Plan 03): `KpiCards`, `KpiCard`, `EntrateUsciteChart`,
`BilancioBarsChart`, `OverviewFilters`, plus the obsolete skeletons `OverviewSkeleton` and
`TrendSkeleton`. Also delete the throwaway proto route `app/proto/overview/`. Do NOT touch the
Categories-tab components (`category-breakdown-chart`, `category-detail-trend-chart`, etc.) or
`dashboard-tab-nav`. `yarn build` and `yarn check:language` must stay green.

Purpose: Removes the dead code the redesign supersedes (D-02), keeping the codebase honest. Ordered
AFTER the new shell renders (Plan 03) so the build is never broken between the new and old surfaces.

Output: deleted files; green build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-overview-shell/43-CONTEXT.md
@.planning/phases/43-overview-shell/43-03-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify zero external importers, then delete superseded components + proto route</name>
  <files>components/dashboard/kpi-cards.tsx, components/dashboard/kpi-card.tsx, components/dashboard/entrate-uscite-chart.tsx, components/dashboard/bilancio-bars-chart.tsx, components/dashboard/overview-filters.tsx, components/dashboard/overview-skeleton.tsx, components/dashboard/trend-skeleton.tsx, app/proto/overview/</files>
  <read_first>
    - app/(app)/dashboard/overview/page.tsx (Plan 03 rewrite — confirm it no longer imports any of the deletion targets, and that it does NOT depend on overview-skeleton.tsx / trend-skeleton.tsx)
    - .planning/phases/43-overview-shell/43-CONTEXT.md (D-02 deletion list + the "Categories pages use different components, untouched" guard)
    - components/dashboard/kpi-card.tsx, components/dashboard/kpi-cards.tsx (confirm kpi-cards is the only importer of kpi-card, and nothing else imports either)
  </read_first>
  <action>
    First, GUARD: grep the whole repo (`app`, `lib`, `components`, excluding the files being deleted
    and the proto route) for any importer of each deletion target. The verified scope: only
    `overview/page.tsx` (now rewritten) and the targeted files themselves referenced these. If grep
    finds ANY other importer (e.g. a test, a barrel export, the Categories tab), STOP and report —
    do not delete that file.
    Then delete: `components/dashboard/kpi-cards.tsx`, `components/dashboard/kpi-card.tsx`,
    `components/dashboard/entrate-uscite-chart.tsx`, `components/dashboard/bilancio-bars-chart.tsx`,
    `components/dashboard/overview-filters.tsx`, `components/dashboard/overview-skeleton.tsx`,
    `components/dashboard/trend-skeleton.tsx`. Also delete the throwaway proto directory
    `app/proto/overview/` in full (variant-a..e, header-variants, kpi-row, kpi-card-reading,
    mock-data, shared, year-select, prototype-switcher, uncategorized-banner, page.tsx, NOTES.md).
    Do NOT touch `category-*` components or `dashboard-tab-nav.tsx`. If `kpi-card.tsx` exports a type
    or helper still referenced elsewhere (grep proved it is not), preserve only that symbol — but the
    verified scope says delete the whole file.
    After deletion run `yarn build` and `yarn check:language`; both must pass. Fix any dangling import
    by removing it (there should be none given the guard).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! ls components/dashboard/kpi-cards.tsx components/dashboard/kpi-card.tsx components/dashboard/entrate-uscite-chart.tsx components/dashboard/bilancio-bars-chart.tsx components/dashboard/overview-filters.tsx components/dashboard/overview-skeleton.tsx components/dashboard/trend-skeleton.tsx 2>/dev/null && ! test -d app/proto/overview && ! grep -rEq "kpi-cards|dashboard/kpi-card'|entrate-uscite-chart|bilancio-bars-chart|overview-filters|overview-skeleton|trend-skeleton|proto/overview" app lib components --include="*.ts" --include="*.tsx" && yarn build > /tmp/p43-04-build.log 2>&1 && yarn check:language > /tmp/p43-04-lang.log 2>&1 && echo CLEANUP_OK || { tail -20 /tmp/p43-04-build.log; tail -20 /tmp/p43-04-lang.log; }</automated>
  </verify>
  <acceptance_criteria>
    - All seven listed components are deleted; `app/proto/overview/` no longer exists.
    - Repo-wide grep finds zero remaining references to any deleted module path (no dead imports).
    - `category-*` components and `dashboard-tab-nav.tsx` are untouched (Categories tab unaffected).
    - `yarn build` exits 0 and `yarn check:language` passes (prints CLEANUP_OK).
  </acceptance_criteria>
  <done>Superseded overview components and the proto route are gone; build + language gate green; the redesigned tab still renders.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

Deleted files (no new symbols):
- `components/dashboard/kpi-cards.tsx`, `components/dashboard/kpi-card.tsx`
- `components/dashboard/entrate-uscite-chart.tsx`, `components/dashboard/bilancio-bars-chart.tsx`
- `components/dashboard/overview-filters.tsx`
- `components/dashboard/overview-skeleton.tsx`, `components/dashboard/trend-skeleton.tsx`
- `app/proto/overview/` (entire throwaway prototype route)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Deletion-only plan; introduces no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Denial of Service (build break) | deleted components with lingering importers | mitigate | Pre-deletion repo-wide grep guard + post-deletion `yarn build` gate; if any external importer exists the task STOPs before deleting. |
| T-43-SC | Tampering | npm installs | accept | No new package installs — deletion only. |
</threat_model>

<verification>
- `yarn build` exits 0; `yarn check:language` passes (CLEANUP_OK).
- Zero remaining references to the deleted module paths.
- Categories tab and dashboard tab nav untouched.
</verification>

<success_criteria>
- Dead overview components and proto route removed (D-02).
- Build and language gate green.
- Redesigned overview tab unaffected by the deletion.
</success_criteria>

<output>
Create `.planning/phases/43-overview-shell/43-04-SUMMARY.md` when done.
</output>
