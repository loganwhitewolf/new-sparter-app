---
phase: 43-overview-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/overview/overview-chart.tsx
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03]
must_haves:
  truths:
    - "User sees one bar group per month for the selected year, with side-by-side green (Entrate) and red (Uscite) bars"
    - "Each bar shows an always-on compact k-notation value label; the exact value remains in the tooltip"
    - "The chart never stacks bars by nature and never plots a balance series"
  artifacts:
    - path: "components/dashboard/overview/overview-chart.tsx"
      provides: "Grouped Entrate/Uscite bar chart (variant A) consuming OverviewChartPoint[], with the D-03 P44/P45 scaffold rendering nothing inert"
      exports: ["OverviewChart"]
  key_links:
    - from: "components/dashboard/overview/overview-chart.tsx"
      to: "lib/dal/overview.ts OverviewChartPoint"
      via: "data prop typed OverviewChartPoint[]"
      pattern: "OverviewChartPoint"
    - from: "components/dashboard/overview/overview-chart.tsx"
      to: "recharts Bar entrate/uscite"
      via: "two side-by-side Bar series, no stackId"
      pattern: "dataKey=\"(entrate|uscite)\""
---

<objective>
Port the PO-approved proto hero chart (variant A) into a production
`OverviewChart` component, STRIPPING the P44/P45 bits (FilterBar, MoversList,
useFilters, the movers panel, "Tocca un mese per il dettaglio" hint). Keep the
D-03 internal scaffold — `selectedMonth` state, per-bar `<Cell>` opacity hook,
`onClick` wiring, optional filter/movers props — but render NOTHING inert/non-functional
in P43 (no visible filter chips, no visible highlight or clickable affordance until the
P45 panel exists). The chart consumes `OverviewChartPoint[]` and collapses the income
split + 6 OUT natures into one green and one red bar per month.

Purpose: Delivers the hero chart (CHART-01..03) — the third UI piece of the shell — as a
self-contained presentation component. The D-03 seams let P44 wire filter chips and P45 wire
the movers drill-down without restructuring this file.

Output: `components/dashboard/overview/overview-chart.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-overview-shell/43-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverviewChart — port variant A, strip P44/P45, keep D-03 scaffold</name>
  <files>components/dashboard/overview/overview-chart.tsx</files>
  <read_first>
    - app/proto/overview/variant-a.tsx (the full reference: BarChart, two Bar series, LabelList compact labels, per-bar Cell opacity, onClick(setSelected), config with var(--total-in)/var(--total-out). STRIP: the `useFilters`/`FilterBar`/`MoversList` imports and JSX, the "Tocca un mese per il dettaglio ↓" hint, the bottom movers `<div>`, and the section `<h2>` if the page provides the heading)
    - app/proto/overview/shared.tsx (to confirm what FilterBar/MoversList/useFilters are — everything imported from './shared' is P44/P45 and must NOT be ported)
    - lib/dal/overview.ts (lines 47-52 — OverviewChartPoint: `{ month, label, income: { recurring, extraordinary }, out: Record<OutNature,string> }`; all amounts DECIMAL strings; zero-filled to 12 buckets)
    - components/ui/chart.tsx (ChartContainer/ChartTooltip/ChartTooltipContent/ChartLegend/ChartLegendContent/ChartConfig — reused as-is)
    - components/dashboard/overview/format.ts (formatEur for the YAxis tickFormatter + tooltip; formatEurCompact for the always-on bar labels — from Plan 01)
    - lib/utils/decimal.ts (toDecimal — sum the income split and the 6 OUT natures per month using Decimal, NEVER native + on the DECIMAL strings)
  </read_first>
  <action>
    Create `components/dashboard/overview/overview-chart.tsx` as a `'use client'` component:
    `OverviewChart({ data }: { data: OverviewChartPoint[] })` importing `OverviewChartPoint`
    from `@/lib/dal/overview`.
    Derive the per-month bar rows: for each point, `entrate` = `toDecimal(income.recurring).plus(toDecimal(income.extraordinary))` and `uscite` = sum of the 6 `out` natures via `toDecimal(...).plus(...)`
    reduced (per CONTEXT chart-input rule — collapse to ONE green + ONE red bar; no stacking).
    Convert each to `Number()` only at the recharts boundary (recharts needs numbers); keep the
    Decimal math for the sums. Map `label` to the X axis dataKey.
    Port the recharts structure verbatim: `ChartContainer` with config
    `{ entrate: { label: 'Entrate', color: 'var(--total-in)' }, uscite: { label: 'Uscite', color: 'var(--total-out)' } }`,
    `BarChart` with `barGap`/`barCategoryGap`, `CartesianGrid vertical=false`, `XAxis dataKey="label"`,
    `YAxis` with `tickFormatter={(v) => formatEur(v)}`, `ChartTooltip`/`ChartTooltipContent`,
    `ChartLegend`/`ChartLegendContent`, two `Bar` series (`entrate` fill `var(--color-entrate)`,
    `uscite` fill via `<Cell>`). Each Bar carries a `LabelList position="top"` with
    `formatter={(v) => formatEurCompact(Number(v))}` (CHART-02 always-on compact labels).
    Do NOT add a `stackId` to either Bar and do NOT add a balance series (CHART-03).
    D-03 scaffold — KEEP but render inert: retain internal `selectedMonth` state
    (`useState(() => data.length - 1)`), the `onClick={(_, index) => setSelectedMonth(index)}`
    on both Bars, and the per-bar `<Cell>` map on the uscite Bar. In P43, set every Cell's
    `fillOpacity` to a constant `1` (NOT the `i === selected ? 1 : 0.4` highlight) and use
    `cursor="default"` so there is NO visible clickable/highlight affordance until P45 activates it
    — leave a `// D-03: P45 will switch this to selected-month highlight + cursor:pointer` comment.
    Accept optional props for the future chips/movers (e.g. `onMonthSelect?`, `hiddenIncome?`,
    `hiddenOut?`) typed but unused/no-op in P43 so P44/P45 wire them without a rewrite — but render
    NO filter UI and NO movers panel. STRIP the proto's `FilterBar`, `MoversList`, `useFilters`,
    the detail-hint span, and the section heading (the page owns headings). Remove the
    "PROTOTYPE — wipe me" comment.
  </action>
  <verify>
    <automated>grep -q "export function OverviewChart" components/dashboard/overview/overview-chart.tsx && grep -q "OverviewChartPoint" components/dashboard/overview/overview-chart.tsx && grep -q 'dataKey="entrate"' components/dashboard/overview/overview-chart.tsx && grep -q 'dataKey="uscite"' components/dashboard/overview/overview-chart.tsx && grep -q "LabelList" components/dashboard/overview/overview-chart.tsx && grep -q "formatEurCompact" components/dashboard/overview/overview-chart.tsx && ! grep -Eq "FilterBar|MoversList|useFilters|from './shared'|stackId" components/dashboard/overview/overview-chart.tsx && ! grep -qi "PROTOTYPE" components/dashboard/overview/overview-chart.tsx && echo "chart-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `OverviewChart` exported, `'use client'`, typed `{ data: OverviewChartPoint[] }` importing `OverviewChartPoint` from `@/lib/dal/overview`.
    - Two side-by-side Bar series `entrate` (var(--total-in)) and `uscite` (var(--total-out)); neither has a `stackId`; no balance series exists (CHART-01, CHART-03).
    - Each bar has a `LabelList` rendering `formatEurCompact` k-notation; the tooltip uses `ChartTooltipContent` (exact value) (CHART-02).
    - Income split + 6 OUT natures are summed with `toDecimal` (no native `+` on DECIMAL strings) into one green + one red value per month.
    - D-03 scaffold present: `selectedMonth` state, `onClick` handlers, per-Cell map all exist, but every Cell renders `fillOpacity={1}` and `cursor="default"` (no visible highlight/clickable affordance).
    - No `FilterBar`/`MoversList`/`useFilters`/`./shared` import, no movers panel JSX, no "PROTOTYPE" comment.
  </acceptance_criteria>
  <done>Grouped green/red bars with always-on compact labels render from OverviewChartPoint[]; no stacking, no balance series, no inert P44/P45 UI; D-03 seams present.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `components/dashboard/overview/overview-chart.tsx`

Exported symbols:
- `OverviewChart` (component; required prop `{ data: OverviewChartPoint[] }`; optional no-op P44/P45 props `onMonthSelect?`, `hiddenIncome?`, `hiddenOut?`)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client component | `OverviewChartPoint[]` (DAL-derived, parameterized, user-scoped) passed as a prop; no direct user input reaches this component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Injection (XSS) | OverviewChart labels/tooltip | accept | All rendered values are numeric formatter output; `label` is a fixed month abbreviation from the DAL. React escapes by default; no `dangerouslySetInnerHTML`. |
| T-43-04 | Tampering | bar `onClick` setSelectedMonth | accept | `index` is a recharts-provided array index bounded by `data.length`; in P43 the state is rendered inert (no UI effect). No security impact. |
| T-43-SC | Tampering | npm installs | accept | No new package installs — recharts and shadcn chart wrappers already present. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the new file.
- Bars are side-by-side (no `stackId`), green/red via existing color tokens.
- Always-on compact labels present; tooltip shows the exact value.
</verification>

<success_criteria>
- Chart renders one bar group per month with grouped Entrate/Uscite bars and compact labels.
- No nature stacking, no balance series, no visible P44/P45 affordances.
- D-03 scaffold present for downstream activation.
</success_criteria>

<output>
Create `.planning/phases/43-overview-shell/43-02-SUMMARY.md` when done.
</output>
