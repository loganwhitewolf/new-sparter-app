---
phase: 44-overview-interactions
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - components/dashboard/overview/overview-nudge.tsx
  - app/(app)/dashboard/overview/page.tsx
  - tests/overview-interactions.test.tsx
autonomous: true
requirements: [NUDGE-01, NUDGE-02, NUDGE-03, NUDGE-04]
must_haves:
  truths:
    - "When the selected year has uncategorized OUT expenses, an inline amber nudge appears on the title row"
    - "The nudge is absent when the uncategorized count is zero"
    - "The nudge offers a 'Categorizza ora' link to uncategorized transactions and an X dismiss control"
    - "Dismissal persists in localStorage (year-scoped, lastSeenCount) and never writes to the database"
    - "The nudge reappears when the uncategorized count rises above the last dismissed count"
  artifacts:
    - path: "components/dashboard/overview/overview-nudge.tsx"
      provides: "Client nudge island with localStorage lastSeenCount dismissal + shouldShowNudge helper"
      exports: ["OverviewNudge", "shouldShowNudge"]
      min_lines: 40
    - path: "app/(app)/dashboard/overview/page.tsx"
      provides: "Wires uncategorizedCount + year into the nudge on the title row"
      contains: "OverviewNudge"
  key_links:
    - from: "app/(app)/dashboard/overview/page.tsx"
      to: "components/dashboard/overview/overview-nudge.tsx"
      via: "renders OverviewNudge with overview.uncategorizedCount and year props"
      pattern: "OverviewNudge"
    - from: "components/dashboard/overview/overview-nudge.tsx"
      to: "/transactions"
      via: "Categorizza ora Link with status=uncategorized + selected-year months"
      pattern: "status=uncategorized"
---

<objective>
Add the inline amber "uncategorized" nudge to the overview title row, persisting dismissal in localStorage only (never the database), and reappearing when new uncategorized OUT expenses arrive (lastSeenCount semantics). Delivers NUDGE-01..04 (D-02, D-03, D-10).

Purpose: The redesign dropped the 5th "Da categorizzare" KPI card in favor of this inline nudge. It must be invitational, count-free in copy, year-scoped, and presentation-only — dismissal is a client convenience, not a business decision, so it stays in localStorage.

Output: `components/dashboard/overview/overview-nudge.tsx` (client island), the page wiring that threads `overview.uncategorizedCount` + `year` into it, and appended nudge tests in the shared Phase 44 test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-overview-interactions/44-CONTEXT.md
@.planning/phases/44-overview-interactions/44-RESEARCH.md
@.planning/phases/44-overview-interactions/44-PATTERNS.md
@.planning/phases/44-overview-interactions/44-VALIDATION.md

@app/(app)/dashboard/overview/page.tsx
@components/dashboard/overview/overview-header.tsx
@components/layout/sidebar-provider.tsx
@lib/dal/dashboard.ts
@lib/validations/transactions.ts
@lib/routes.ts
@tests/deviation-badge.test.tsx
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- File `components/dashboard/overview/overview-nudge.tsx`
- Exported component `OverviewNudge` (props: `{ uncategorizedCount: number; year: number }`)
- Exported pure function `shouldShowNudge(count, stored)` → `boolean`
- localStorage key pattern `sparter-overview-nudge-{year}` storing JSON `{ lastSeenCount: number }`
- New tests appended to `tests/overview-interactions.test.tsx` (nudge + lastSeenCount cases)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the OverviewNudge client island + shouldShowNudge helper</name>
  <files>components/dashboard/overview/overview-nudge.tsx</files>
  <read_first>
    - components/layout/sidebar-provider.tsx (SSR-safe localStorage: default state in useState, read in useEffect after mount — NEVER read localStorage in a useState initializer; PATTERNS.md "SSR-Safe localStorage")
    - components/dashboard/overview/overview-header.tsx (title-row layout context this nudge sits beside; client component conventions)
    - lib/dal/dashboard.ts (lines 31-44: `OverviewData.uncategorizedCount: number` is the OUT-only count source — display only)
    - lib/validations/transactions.ts (lines ~171-176: `/transactions` accepts `status=uncategorized` and comma-separated `months=YYYY-MM,...`)
    - lib/routes.ts (`APP_ROUTES.transactions` = `/transactions`)
    - .planning/phases/44-overview-interactions/44-CONTEXT.md (D-02, D-03, D-10 — copy is invitational and count-free; CTA = "Categorizza ora")
  </read_first>
  <behavior>
    - shouldShowNudge(0, null) === false (zero count → hidden, NUDGE-04)
    - shouldShowNudge(5, null) === true (count > 0, never dismissed → shown, NUDGE-01)
    - shouldShowNudge(5, { lastSeenCount: 5 }) === false (dismissed at current count → hidden, NUDGE-03)
    - shouldShowNudge(8, { lastSeenCount: 5 }) === true (new uncategorized arrived → reappears, NUDGE-03)
    - shouldShowNudge(3, { lastSeenCount: 5 }) === false (count fell, still under last seen → hidden)
    - With count 0 the component renders nothing
    - Dismiss writes JSON `{ lastSeenCount: <current count> }` to key `sparter-overview-nudge-{year}` and hides the nudge
  </behavior>
  <action>
    Create `components/dashboard/overview/overview-nudge.tsx` with `'use client'`. Export pure `shouldShowNudge(count: number, stored: { lastSeenCount: number } | null): boolean` — return `false` if `count <= 0`, else `!stored || count > stored.lastSeenCount`. Export `function OverviewNudge({ uncategorizedCount, year }: { uncategorizedCount: number; year: number })`. Use `const [visible, setVisible] = useState(false)` (SSR-safe default hidden). In a `useEffect` keyed on `[uncategorizedCount, year]`, build the key `` `sparter-overview-nudge-${year}` ``, read+JSON.parse localStorage, and `setVisible(shouldShowNudge(uncategorizedCount, stored))`. Provide a `dismiss()` that writes `localStorage.setItem(key, JSON.stringify({ lastSeenCount: uncategorizedCount }))` then `setVisible(false)`. If `!visible` return `null`. Render an inline amber container with `role="status"` (Tailwind amber classes consistent with the project palette), invitational Italian copy with NO count (e.g. `Hai movimenti da categorizzare` + a short secondary line such as `Completarli rende la panoramica più precisa.` — exact copy is discretion but must not include the number), a `next/link` `Link` to `` `/transactions?status=uncategorized&months=${monthsForYear}` `` where `monthsForYear` is the 12 `YYYY-MM` tokens for `year` joined by commas (build via a small local loop / `URLSearchParams`; use `APP_ROUTES.transactions` for the base path), with link text `Categorizza ora`, and a dismiss `<button type="button" aria-label="Chiudi avviso">` containing a lucide `X` icon. Comments/identifiers English; user-facing strings Italian. Do NOT write any dismissal state to the database and do NOT add a server action.
  </action>
  <verify>
    <automated>yarn test tests/overview-interactions.test.tsx -t "lastSeenCount"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "export function (OverviewNudge|shouldShowNudge)|export const (OverviewNudge|shouldShowNudge)" components/dashboard/overview/overview-nudge.tsx` returns 2 matches
    - localStorage read happens in useEffect, not useState: `grep -n "useState(() *=> *localStorage" components/dashboard/overview/overview-nudge.tsx` returns nothing
    - Year-scoped key present: `grep -c "sparter-overview-nudge-" components/dashboard/overview/overview-nudge.tsx` is >= 1
    - CTA contract present: `grep -c "status=uncategorized" components/dashboard/overview/overview-nudge.tsx` is >= 1
    - Dismiss is accessible: `grep -c "aria-label" components/dashboard/overview/overview-nudge.tsx` is >= 1
    - No DB/server-action import: `grep -nE "use server|@/lib/actions|@/lib/db" components/dashboard/overview/overview-nudge.tsx` returns nothing
    - Copy is count-free: the rendered JSX contains no `{uncategorizedCount}` / `{count}` interpolation in visible text (`grep -nE ">[^<]*\{[^}]*[Cc]ount[^}]*\}[^<]*<" components/dashboard/overview/overview-nudge.tsx` returns nothing)
  </acceptance_criteria>
  <done>`OverviewNudge` renders an inline amber count-free nudge with a year-scoped `/transactions?status=uncategorized&months=...` CTA and an accessible dismiss; `shouldShowNudge` implements lastSeenCount show/hide/reappear; no DB persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire OverviewNudge into the overview page title row + add nudge tests</name>
  <files>app/(app)/dashboard/overview/page.tsx, tests/overview-interactions.test.tsx</files>
  <read_first>
    - app/(app)/dashboard/overview/page.tsx (lines 22-64: `OverviewDataSection` already fetches `overview` via `getOverview(year)`; `OverviewHeader` renders eagerly on the title row; `KpiRow data={overview}` must stay byte-for-byte unchanged)
    - components/dashboard/overview/overview-nudge.tsx (the island created in Task 1; props `{ uncategorizedCount, year }`)
    - components/dashboard/overview/overview-header.tsx (title-row container the nudge sits inline with / adjacent to)
    - tests/deviation-badge.test.tsx (renderToStaticMarkup analog; Pitfall 4: do not assert portal content)
    - .planning/phases/44-overview-interactions/44-VALIDATION.md (per-task test map: NUDGE tests filter on `-t nudge` and `-t lastSeenCount`)
  </read_first>
  <action>
    Edit `app/(app)/dashboard/overview/page.tsx`: import `OverviewNudge` from `@/components/dashboard/overview/overview-nudge`. Render `<OverviewNudge uncategorizedCount={overview.uncategorizedCount} year={year} />` inline on the title row — pass `overview.uncategorizedCount` (NUDGE-01/04 source, already on `OverviewData`) and the resolved `year`. Because the nudge needs `overview` (only available inside the async `OverviewDataSection`) while `OverviewHeader` renders one level up, place the nudge inside `OverviewDataSection` adjacent to the title context (e.g. immediately above `KpiRow` or as a sibling row directly under the header). Leave `KpiRow data={overview} year={year}` and `OverviewChart data={chart}` exactly as they are — chip/nudge state must not reach `KpiRow`. Do not change `getOverview`/`getOverviewChart` calls. Then append to `tests/overview-interactions.test.tsx`: a `describe('overview nudge (NUDGE-01..04, NUDGE-03)')` with `renderToStaticMarkup` cases whose names include **nudge** — assert that `OverviewNudge` with `uncategorizedCount={0}` renders empty string (NUDGE-04) and that a `shouldShowNudge`-based unit case whose name includes **lastSeenCount** covers show(count>0,null)/hide(count===lastSeen)/reappear(count>lastSeen). Import `OverviewNudge`/`shouldShowNudge` via top-level `await import('@/components/dashboard/overview/overview-nudge')` like the analog. Note (Pitfall 4 / NUDGE-01): a count>0 static render may still render nothing because `visible` defaults to `false` before the client `useEffect` runs — assert NUDGE-01's "shown when count>0 and not dismissed" via `shouldShowNudge(5, null) === true` at the unit level rather than via static markup. All identifiers/test names English; Italian only in any asserted UI copy.
  </action>
  <verify>
    <automated>yarn test tests/overview-interactions.test.tsx -t "nudge" && yarn test tests/overview-interactions.test.tsx -t "lastSeenCount"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OverviewNudge" "app/(app)/dashboard/overview/page.tsx"` is >= 2 (import + render)
    - `grep -c "overview.uncategorizedCount" "app/(app)/dashboard/overview/page.tsx"` is >= 1
    - KPI independence preserved: `grep -c "KpiRow data={overview}" "app/(app)/dashboard/overview/page.tsx"` is >= 1 and no chip/nudge state is passed to `KpiRow`
    - `yarn test tests/overview-interactions.test.tsx -t "nudge"` passes (>= 1 test)
    - `yarn test tests/overview-interactions.test.tsx -t "lastSeenCount"` passes (>= 1 test)
    - `yarn test tests/overview-interactions.test.tsx` exits 0 (Plan 01 filter tests still green)
    - `yarn check:language` passes
  </acceptance_criteria>
  <done>The page renders `OverviewNudge` on the title row fed by `overview.uncategorizedCount` + `year`; `KpiRow` is untouched; nudge and lastSeenCount tests pass alongside the existing filter tests; language check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (RSC) → client island | `overview.uncategorizedCount` (number) and `year` (number) cross from the server page into `OverviewNudge`; both are serializable primitives. |
| client → browser localStorage | Dismissal state (`{ lastSeenCount }`) is read/written in the browser only. |
| client → `/transactions` route | CTA navigation carries `status` + `months` query params parsed/validated by the destination route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | localStorage dismissal (`sparter-overview-nudge-{year}`) | mitigate | Dismissal is presentation-only — it gates a UI hint, never authorization or persisted business state. Tampering only re-shows/hides a nudge for that browser; the canonical count comes from the user-scoped DAL (`getOverview` → `verifySession()`), not localStorage. No PII stored (only an integer `lastSeenCount`). |
| T-44-01 | Information Disclosure | uncategorized-count query feeding the nudge | mitigate | Count is sourced from `getOverview(year)` which runs under `verifySession()` and user-scoped queries (DAL); the page passes only a number to the client. No new fetch path or cross-user exposure introduced. |
| T-44-02 | Tampering | `Categorizza ora` CTA URL | mitigate | Build the href from `APP_ROUTES.transactions` plus only canonical `status=uncategorized` and well-formed `YYYY-MM` month tokens (via URLSearchParams); the `/transactions` route re-validates `status`/`months` (`parseStatus`/`parseMonths`), so injected params are dropped. No `dangerouslySetInnerHTML`. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: "No install planned"). |
</threat_model>

<verification>
- `yarn test tests/overview-interactions.test.tsx` green (filter + nudge + lastSeenCount cases)
- `yarn check:language` green
- Manual (per VALIDATION.md): seed/mock a year with uncategorized OUT expenses, open `/dashboard/overview?year=YYYY`, confirm inline amber nudge fits the title row, "Categorizza ora" lands on year-scoped uncategorized transactions, X dismisses, reload keeps it dismissed, and it reappears after count rises
</verification>

<success_criteria>
- Inline amber nudge appears on the title row when `uncategorizedCount > 0`, hidden when `0` (NUDGE-01, NUDGE-04)
- Nudge offers count-free invitational copy, a `Categorizza ora` link to `/transactions?status=uncategorized&months=<year months>`, and an accessible X dismiss (NUDGE-02, D-10)
- Dismissal stored only in localStorage under a year-scoped key with `lastSeenCount`; reappears when count exceeds last seen; never written to DB (NUDGE-03)
- `KpiRow` remains independent of nudge state
</success_criteria>

<output>
Create `.planning/phases/44-overview-interactions/44-02-SUMMARY.md` when done
</output>
