---
phase: 44-overview-interactions
plan: 03
type: execute
wave: 3
depends_on: ["44-01", "44-02"]
files_modified:
  - components/dashboard/overview/overview-chart-filters.tsx
  - components/dashboard/overview/overview-chart.tsx
  - tests/overview-interactions.test.tsx
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, EDU-01, EDU-02]
must_haves:
  truths:
    - "User can toggle income chips (Ricorrenti / Straordinarie) to include or exclude those buckets from the green Entrate bar"
    - "User can toggle the six expense-nature chips to include or exclude those buckets from the red Uscite bar"
    - "The chart stays two bars per month with no stacking and no balance series; default state shows all buckets"
    - "KPI totals never change in response to chip state"
    - "An ⓘ legend popover sits next to the Entrate and Uscite filter groups, and each chip shows a one-line tooltip"
  artifacts:
    - path: "components/dashboard/overview/overview-chart-filters.tsx"
      provides: "Income + expense filter chip groups with group popovers and per-chip tooltips"
      exports: ["OverviewChartFilters"]
      min_lines: 60
    - path: "components/dashboard/overview/overview-chart.tsx"
      provides: "Filter-aware chart that owns chip state and reduces only selected buckets"
      contains: "deriveFilteredBarRow"
  key_links:
    - from: "components/dashboard/overview/overview-chart.tsx"
      to: "components/dashboard/overview/overview-chart-utils.ts"
      via: "deriveFilteredBarRow over included income/out keys"
      pattern: "deriveFilteredBarRow"
    - from: "components/dashboard/overview/overview-chart.tsx"
      to: "components/dashboard/overview/overview-chart-filters.tsx"
      via: "renders OverviewChartFilters and owns the included-keys state"
      pattern: "OverviewChartFilters"
    - from: "components/dashboard/overview/overview-chart-filters.tsx"
      to: "components/ui/popover"
      via: "group ⓘ legend popover (EDU-01)"
      pattern: "Popover"
    - from: "components/dashboard/overview/overview-chart-filters.tsx"
      to: "components/ui/tooltip"
      via: "per-chip TooltipProvider/Tooltip (EDU-02)"
      pattern: "Tooltip"
---

<objective>
Activate the chart filter chips and FlowNature education on the overview chart: income chips (recurring / extraordinary), expense chips (six OUT natures), an ⓘ legend popover per filter group, and a one-line tooltip per chip. Chips slice only the chart bars client-side; KPI totals are untouched. Delivers FILT-01..03 (D-04, D-06, D-07, D-08) and EDU-01..02 (D-05).

Purpose: Phase 43 shipped the grouped two-bar chart with an inert `hiddenIncome`/`hiddenOut` props seam. This plan makes the reduction filter-aware (using the Plan 01 helper), adds the chip UI + contextual education, and proves chip state never reaches the KPIs. No DAL or schema work — the rich `getOverviewChart(year)` payload already exposes every bucket.

Output: `components/dashboard/overview/overview-chart-filters.tsx` (chip groups + popovers + tooltips), a filter-aware rewrite of `overview-chart.tsx`, and appended education/render tests in the shared Phase 44 test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-overview-interactions/44-CONTEXT.md
@.planning/phases/44-overview-interactions/44-RESEARCH.md
@.planning/phases/44-overview-interactions/44-PATTERNS.md
@.planning/phases/44-overview-interactions/44-VALIDATION.md

@components/dashboard/overview/overview-chart.tsx
@components/dashboard/overview/overview-chart-utils.ts
@components/dashboard/overview/overview-header.tsx
@components/ui/popover.tsx
@components/ui/tooltip.tsx
@components/ui/button.tsx
@lib/utils/nature-labels.ts
@lib/dal/overview.ts
@tests/deviation-badge.test.tsx
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- File `components/dashboard/overview/overview-chart-filters.tsx`
- Exported component `OverviewChartFilters` (props include income/out included-key sets + toggle callbacks + optional reset)
- Modified `OverviewChart`: now owns `includedIncome`/`includedOut` state (initialised to all keys), reduces via `deriveFilteredBarRow`, renders `OverviewChartFilters`
- New tests appended to `tests/overview-interactions.test.tsx` (education trigger + chip tooltip render cases)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build OverviewChartFilters — chip groups, group ⓘ popovers, per-chip tooltips</name>
  <files>components/dashboard/overview/overview-chart-filters.tsx</files>
  <read_first>
    - components/dashboard/overview/overview-header.tsx (client component shell pattern in this folder)
    - components/ui/popover.tsx (local Radix wrapper: `Popover`, `PopoverTrigger`, `PopoverContent`; content renders via portal)
    - components/ui/tooltip.tsx (local wrapper: `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger`; `TooltipProvider` MUST wrap the chip subtree)
    - components/ui/button.tsx (compact control styling foundation for chips)
    - lib/utils/nature-labels.ts (lines 12-23: `NATURE_LABELS` — six OUT labels; `income` = "Entrate ricorrenti", `income_extraordinary` = "Straordinaria"; PATTERNS.md says shorten income chips to "Ricorrenti" / "Straordinarie")
    - components/dashboard/overview/overview-chart-utils.ts (`OUT_KEYS`, `INCOME_KEYS`, `OutKey`, `IncomeKey` — drive chip rendering from these constants)
    - .planning/phases/44-overview-interactions/44-CONTEXT.md (D-04 chip→bucket mapping, D-05 contextual education only — no glossary screen, D-07 inclusive toggles, D-08 all-off allowed)
  </read_first>
  <action>
    Create `components/dashboard/overview/overview-chart-filters.tsx` with `'use client'`. Export `OverviewChartFilters` taking the current included income keys + included out keys (as `Set<IncomeKey>` / `Set<OutKey>` or arrays — your choice, but keep state owned by the parent chart, this is a controlled component), an `onToggleIncome(key)` and `onToggleOut(key)` callback, and optionally an `onReset()` affordance (D-08 lightweight reset). Render two labelled groups: an **Entrate** group iterating `INCOME_KEYS` (chip text "Ricorrenti" for `recurring`, "Straordinarie" for `extraordinary`) and an **Uscite** group iterating `OUT_KEYS` (chip text from `NATURE_LABELS` for each OUT nature). Each chip is a `<button type="button" aria-pressed={included}>` styled as a toggle pill (use button.tsx classes / cn). Wrap the chip rows in a single `TooltipProvider`; wrap each chip's button in `Tooltip`/`TooltipTrigger asChild` + `TooltipContent` carrying a concise one-line Italian definition derived from `NATURE_LABELS` + Phase 42 income split (EDU-02). Next to each group label render an ⓘ `Popover`: `PopoverTrigger asChild` is a `<button type="button" aria-label="Informazioni sul gruppo Entrate">` (and "...Uscite") with a lucide `Info` icon; `PopoverContent` holds a one-line group summary (EDU-01). Drive all labels/definitions from `NATURE_LABELS`; do NOT introduce friendlier replacement labels (EDU-FUT-01 is deferred). Identifiers/comments English; chip labels, tooltips, popover copy Italian.
  </action>
  <verify>
    <automated>yarn test tests/overview-interactions.test.tsx -t "education" && yarn test tests/overview-interactions.test.tsx -t "tooltip"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function OverviewChartFilters\|export const OverviewChartFilters" components/dashboard/overview/overview-chart-filters.tsx` is >= 1
    - Uses local primitives: `grep -c "@/components/ui/popover" components/dashboard/overview/overview-chart-filters.tsx` is >= 1 and `grep -c "@/components/ui/tooltip" components/dashboard/overview/overview-chart-filters.tsx` is >= 1
    - `grep -c "TooltipProvider" components/dashboard/overview/overview-chart-filters.tsx` is >= 1 (chip subtree wrapped)
    - Chips are accessible toggles: `grep -c "aria-pressed" components/dashboard/overview/overview-chart-filters.tsx` is >= 1
    - Group info triggers labelled: `grep -c "aria-label=\"Informazioni sul gruppo" components/dashboard/overview/overview-chart-filters.tsx` is >= 2
    - Labels sourced from canonical map: `grep -c "NATURE_LABELS" components/dashboard/overview/overview-chart-filters.tsx` is >= 1
    - No friendlier-label drift: `grep -nE "Sfizi|Extra/Sfizi" components/dashboard/overview/overview-chart-filters.tsx` returns nothing
    - `yarn check:language` passes
  </acceptance_criteria>
  <done>`OverviewChartFilters` renders an Entrate income group and an Uscite expense group of `aria-pressed` toggle chips, each with a one-line tooltip, plus a per-group ⓘ popover with an accessible label; labels come from `NATURE_LABELS`; no taxonomy rename.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make OverviewChart filter-aware and mount the filters; add education render tests</name>
  <files>components/dashboard/overview/overview-chart.tsx, tests/overview-interactions.test.tsx</files>
  <read_first>
    - components/dashboard/overview/overview-chart.tsx (current: private `deriveBarRow` sums all buckets; inert `hiddenIncome?/hiddenOut?` props seam lines 54-62; internal `selectedMonth` state lines 65-67 must stay for P45; two-bar grouped layout CHART-01/03)
    - components/dashboard/overview/overview-chart-utils.ts (`deriveFilteredBarRow`, `OUT_KEYS`, `INCOME_KEYS`, `OutKey`, `IncomeKey`)
    - components/dashboard/overview/overview-chart-filters.tsx (the `OverviewChartFilters` controlled component from Task 1)
    - tests/deviation-badge.test.tsx (renderToStaticMarkup analog; Pitfall 4: Tooltip/Popover content is portaled and will NOT appear in static markup — assert triggers/aria-labels only)
    - .planning/phases/44-overview-interactions/44-CONTEXT.md (D-06 default all-on, D-07 inclusive toggles, D-08 empty bars OK / empty chart NOT, D-09 client state — no URL/localStorage for chips)
  </read_first>
  <behavior>
    - Default render includes all income + all out keys → entrate/uscite equal the legacy all-bucket totals (D-06)
    - Excluding `extraordinary` income drops the green bar total by exactly that bucket; excluding an OUT nature drops the red bar by exactly that bucket (D-07)
    - Disabling all chips in a group leaves that bar at 0 while the chart still renders (no separate empty-state panel) (D-08)
    - Chip state lives in chart-local React state only (not URL, not localStorage) (D-09)
    - The chart still renders exactly two Bars (entrate, uscite); no stacked nature bars, no balance series (CHART-03)
  </behavior>
  <action>
    Edit `components/dashboard/overview/overview-chart.tsx`: import `deriveFilteredBarRow, OUT_KEYS, INCOME_KEYS, type OutKey, type IncomeKey` from `./overview-chart-utils` and `OverviewChartFilters` from `./overview-chart-filters`. Replace the private `deriveBarRow` usage: add chart-local state `const [includedIncome, setIncludedIncome] = useState<Set<IncomeKey>>(() => new Set(INCOME_KEYS))` and `const [includedOut, setIncludedOut] = useState<Set<OutKey>>(() => new Set(OUT_KEYS))` (default all-on, D-06). Compute `rows = data.map((p) => deriveFilteredBarRow(p, [...includedIncome], [...includedOut]))`. Provide toggle handlers that add/remove a key from the corresponding set (inclusive toggles, D-07) and a reset handler that restores all keys. Render `<OverviewChartFilters ... />` above the `ChartContainer`, passing the included sets + toggle/reset callbacks. Keep the existing `selectedMonth` state and the two `<Bar>` elements / `LabelList` / `Cell` structure intact (P45 seam, CHART-01/03) — only the row values become filter-aware. Remove or repurpose the now-redundant inert `hiddenIncome`/`hiddenOut` props (chip state is now internal). Keep `Number()` only at the Recharts boundary (handled inside `deriveFilteredBarRow`). Then append to `tests/overview-interactions.test.tsx`: a `describe('overview chart education (EDU-01, EDU-02)')` using `renderToStaticMarkup(<OverviewChartFilters .../>)` with a fully-on state, with `it` names including **education** (assert both group ⓘ trigger `aria-label`s "Informazioni sul gruppo Entrate"/"...Uscite" appear) and **tooltip** (assert each chip trigger label, e.g. "Ricorrenti"/"Essenziale", appears as a trigger — NOT the portaled tooltip body, per Pitfall 4). Import `OverviewChartFilters` via top-level `await import('@/components/dashboard/overview/overview-chart-filters')`. English identifiers/test names; Italian only in asserted UI strings.
  </action>
  <verify>
    <automated>yarn test tests/overview-interactions.test.tsx && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - Filter-aware reduction wired: `grep -c "deriveFilteredBarRow" components/dashboard/overview/overview-chart.tsx` is >= 1
    - Default all-on state: `grep -c "new Set(INCOME_KEYS)\|new Set(OUT_KEYS)" components/dashboard/overview/overview-chart.tsx` is >= 1
    - Filters mounted: `grep -c "OverviewChartFilters" components/dashboard/overview/overview-chart.tsx` is >= 1
    - Still two bars, no stacking: `grep -c "dataKey=\"entrate\"\|dataKey=\"uscite\"" components/dashboard/overview/overview-chart.tsx` is 2 (or each present once) and no new `stackId` introduced (`grep -c "stackId" components/dashboard/overview/overview-chart.tsx` is 0)
    - P45 seam preserved: `grep -c "selectedMonth" components/dashboard/overview/overview-chart.tsx` is >= 1
    - Chip state not persisted: `grep -nE "localStorage|useSearchParams|router.replace" components/dashboard/overview/overview-chart.tsx` returns nothing
    - `yarn test tests/overview-interactions.test.tsx -t "education"` and `-t "tooltip"` pass
    - `yarn test tests/overview-interactions.test.tsx` exits 0 (all prior Plan 01/02 cases still green)
    - `yarn build` succeeds and `yarn check:language` passes
  </acceptance_criteria>
  <done>`OverviewChart` owns default-all-on income/out included-key state, reduces bars via `deriveFilteredBarRow`, and mounts `OverviewChartFilters`; toggling chips slices only that side's bar while the chart keeps two bars and the P45 seam; chip state is chart-local only; education/tooltip render tests pass and `yarn build` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (RSC) → client chart | `getOverviewChart(year)` payload (`OverviewChartPoint[]`, serializable strings) crosses into `OverviewChart`; chip state stays entirely client-side. |
| (none new persisted) | Chip state is transient React state — not URL, not localStorage, not DB (D-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Tampering | chip state leaking into KPI totals | mitigate | Chip state lives inside `OverviewChart` only; `KpiRow data={overview}` (in the page) never receives it, and `deriveFilteredBarRow` returns only chart-row fields. FILT-03 is asserted by the Plan 01 "KPI" unit test plus the unchanged `KpiRow` wiring from Plan 02. |
| T-44-04 | Information Disclosure / Tampering | XSS via tooltip/popover/chip copy | mitigate | All education copy is static React text from `NATURE_LABELS` / literals; no `dangerouslySetInnerHTML`. Acceptance grep ensures labels come from the canonical map. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: "No install planned"); reuses installed `recharts`, `radix-ui`, `lucide-react`, `decimal.js`. |
</threat_model>

<verification>
- `yarn test tests/overview-interactions.test.tsx` green (filter + nudge + education/tooltip cases)
- `yarn build` green; `yarn check:language` green
- Manual (per VALIDATION.md): open `/dashboard/overview`, toggle income and expense chips and confirm only the corresponding bar total changes while KPI cards stay fixed; open both ⓘ popovers and hover/focus each chip tooltip at desktop and mobile widths
</verification>

<success_criteria>
- Income chips (Ricorrenti / Straordinarie) and six expense-nature chips toggle their bucket in/out of the matching bar (FILT-01, FILT-02)
- Chart stays two bars per month, default all-on, no stacking, no balance series; all-off leaves a zero bar without a competing empty-state panel (D-06, D-07, D-08, CHART-03)
- KPI totals never react to chip state (FILT-03)
- Each filter group has an accessible ⓘ legend popover and each chip a one-line tooltip (EDU-01, EDU-02), labels from `NATURE_LABELS`, no taxonomy rename
- Chip state is chart-local React state only; P45 `selectedMonth` seam preserved; `yarn build` green
</success_criteria>

<output>
Create `.planning/phases/44-overview-interactions/44-03-SUMMARY.md` when done
</output>
