---
phase: 59-import-wizard-attach-format
plan: 03
type: execute
wave: 3
depends_on:
  - 59-01
  - 59-02
files_modified:
  - components/import/import-format-wizard.tsx
  - app/(app)/import/[fileId]/configure/page.tsx
  - tests/import-format-wizard-ui.test.tsx
autonomous: true
requirements:
  - PLAT-04
must_haves:
  truths:
    - "Step 1 lists the attachable platforms as selectable rows with a 'Crea una nuova platform' entry always at the bottom"
    - "Selecting 'Crea nuova platform' reveals an inline Input for the new platform name"
    - "Selecting an existing platform advances to step 2, which shows that platform's name as a read-only header"
    - "When the attachable list is empty, step 1 is skipped and the wizard opens directly on step 2 in create-new mode"
    - "The form submits existingPlatformId (when a platform is selected) and a populated platformName so validation and logging stay intact"
    - "The onboarding flow still works through the two-step wizard"
  artifacts:
    - "components/import/import-format-wizard.tsx: step 1 UI, currentStep + selectedPlatformId state, attachablePlatforms prop, hidden existingPlatformId/platformName inputs"
    - "app/(app)/import/[fileId]/configure/page.tsx: listAttachablePlatformsAction call + attachablePlatforms prop"
    - "tests/import-format-wizard-ui.test.tsx: step 1 render, empty-list skip, attach-mode submit, onboarding regression"
  key_links:
    - "The hidden platformName input is populated with the selected platform's name in attach mode so validateWizardFields passes unchanged"
    - "The page passes attachablePlatforms from listAttachablePlatformsAction; empty list initializes currentStep='columns' and selectedPlatformId='new'"
---

<objective>
Turn the single-step format wizard into the two-step attach flow (D-01..D-05) and wire the RSC page to preload the platform list (D-02).

Purpose: This is the user-facing half of PLAT-04 — step 1 lets the user pick an existing Platform (reuse, no duplicate) or create a new one; step 2 keeps the existing column configuration with a read-only platform header.
Output: the extended `ImportFormatWizard` component with step state and step 1 UI, the updated configure page passing `attachablePlatforms`, and UI test coverage for the new flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-import-wizard-attach-format/59-CONTEXT.md
@.planning/phases/59-import-wizard-attach-format/59-RESEARCH.md
@docs/adr/0015-platform-global-moderated-format-private.md
@lib/dal/import-formats.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `components/import/import-format-wizard.tsx` → new `attachablePlatforms: AttachablePlatform[]` prop; new state `currentStep: 'platform' | 'columns'` and `selectedPlatformId: number | 'new' | null`; step 1 render block; hidden inputs `existingPlatformId` and `platformName`; read-only platform header on step 2
- `app/(app)/import/[fileId]/configure/page.tsx` → `listAttachablePlatformsAction()` call and `attachablePlatforms` prop passed to the wizard
- `tests/import-format-wizard-ui.test.tsx` → step 1 render test, empty-list skip test, attach-mode submit test, onboarding regression
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add step 1 platform selection UI and step state to ImportFormatWizard</name>
  <files>components/import/import-format-wizard.tsx</files>
  <read_first>
    - components/import/import-format-wizard.tsx (the `Props` type; the `useState` declarations around lines 137-147; the `SelectField` sub-component; the existing hidden inputs block around lines 230-240; the `platformName` Input around lines 273-288; `validateWizardFields` which reads `values.platformName`; `readFormValues`; `handleSubmit`; the `createdFormatVersionId` redirect useEffect; the `isOnboardingFlow` handling; the shadcn imports for `Card`, `Button`, `Input`, `Alert`)
    - lib/dal/import-formats.ts (the `AttachablePlatform` type to import for the new prop)
    - .planning/phases/59-import-wizard-attach-format/59-RESEARCH.md (Pattern 4, Code Examples: empty-list initial state and hidden-input wiring; Pitfalls 2, 3, 6)
    - .planning/phases/59-import-wizard-attach-format/59-CONTEXT.md (D-01..D-05; Claude's Discretion on row UX and empty-list behaviour)
  </read_first>
  <action>
    Extend the `Props` type with `attachablePlatforms: AttachablePlatform[]` (import the type from `@/lib/dal/import-formats`). Add state: `currentStep` of type `'platform' | 'columns'` and `selectedPlatformId` of type `number | 'new' | null`, plus a controlled `platformNameInput` string state for the create-new field. Compute initial state from the prop: when `attachablePlatforms.length === 0`, initialize `currentStep` to `'columns'` and `selectedPlatformId` to `'new'` (skip step 1, Pitfall 3); otherwise `currentStep` `'platform'` and `selectedPlatformId` `null`. Render step 1 when `currentStep === 'platform'`: a list of the platforms as selectable radio-style rows (reuse `Card`/`Input`/`Button` and the existing visual language; exact row treatment is the planner's discretion per CONTEXT), each row setting `selectedPlatformId` to the platform id; always render a visually separated final row "Crea una nuova platform" (D-04) that sets `selectedPlatformId` to `'new'`; when `selectedPlatformId === 'new'`, render an inline labeled `Input` bound to `platformNameInput` for the new name (D-05). Add a "Continua" Button that advances `currentStep` to `'columns'`, disabled until a selection is made (and, for `'new'`, until `platformNameInput.trim()` is non-empty). Render the existing column form only when `currentStep === 'columns'`. On step 2, when a numeric platform is selected, show a read-only header "Configura il formato per <platform name>" (D-03) using `attachablePlatforms.find(p => p.id === selectedPlatformId)?.name`; remove the user-editable `platformName` Input from step 2 (the name now comes from step 1). Add two hidden inputs inside the step-2 form: `existingPlatformId` (rendered only when `typeof selectedPlatformId === 'number'`, value = `String(selectedPlatformId)`) and `platformName` whose value is the selected platform's name when attaching, or `platformNameInput` when creating new — so `validateWizardFields`/`readFormValues` keep reading a non-empty `platformName` and need no change (Pitfall 2). Keep the redirect `useEffect`, `handleSubmit`, onboarding copy, and back-link behaviour unchanged. All developer-facing identifiers/comments in English; user-facing copy in Italian.
  </action>
  <verify>
    <automated>yarn test --run tests/import-format-wizard-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'attachablePlatforms' components/import/import-format-wizard.tsx` returns at least 3 (prop type, destructure, usage)
    - Source: `grep -c "currentStep\|selectedPlatformId" components/import/import-format-wizard.tsx` returns at least 4
    - Source: a hidden input with `name="existingPlatformId"` is rendered conditionally on a numeric selection
    - Behavior: rendering with a non-empty `attachablePlatforms` shows the "Crea una nuova platform" entry; rendering with `attachablePlatforms={[]}` renders the column form directly (no step 1)
    - Test command exits 0
  </acceptance_criteria>
  <done>The wizard renders step 1 (platform list + create-new entry + inline name input), advances to the unchanged step 2 with a read-only platform header, skips step 1 on an empty list, and submits existingPlatformId + a populated platformName.</done>
</task>

<task type="auto">
  <name>Task 2: Preload attachable platforms in the configure RSC page and pass to the wizard</name>
  <files>app/(app)/import/[fileId]/configure/page.tsx</files>
  <read_first>
    - app/(app)/import/[fileId]/configure/page.tsx (the current RSC: `await params`, `await searchParams`, the `loadImportFormatWizardContextAction(formData)` call, the error-state early return, and the `<ImportFormatWizard context={result.data} from={from} />` render)
    - lib/actions/import.ts (the `listAttachablePlatformsAction` signature added in Plan 02 — returns `ImportActionState<AttachablePlatform[]>`)
    - .planning/phases/59-import-wizard-attach-format/59-RESEARCH.md (Pattern 6)
  </read_first>
  <action>
    In `app/(app)/import/[fileId]/configure/page.tsx`, after the existing `loadImportFormatWizardContextAction` call and its error-state early return, call `await listAttachablePlatformsAction()` (import it from `@/lib/actions/import`). Pass `attachablePlatforms={platformsResult.data ?? []}` to `<ImportFormatWizard />` alongside the existing `context` and `from` props. If the platforms action returns an error, fall back to an empty array (the wizard then opens directly in create-new mode — Pitfall 3) — do not block the page on this read. Keep the existing error-state markup for the context-load failure unchanged.
  </action>
  <verify>
    <automated>yarn test --run tests/import-format-wizard-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'listAttachablePlatformsAction' 'app/(app)/import/[fileId]/configure/page.tsx'` returns at least 1
    - Source: `grep -c 'attachablePlatforms' 'app/(app)/import/[fileId]/configure/page.tsx'` returns 1
    - Behavior: the `ConfigureImportFormatPage` test still renders the safe error state on context-load failure (regression)
    - Test command exits 0
  </acceptance_criteria>
  <done>The page preloads the attachable platform list server-side and passes it to the wizard, falling back to an empty list (create-new mode) on a read error.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the UI test suite for the two-step attach flow</name>
  <files>tests/import-format-wizard-ui.test.tsx</files>
  <read_first>
    - tests/import-format-wizard-ui.test.tsx (the `renderToStaticMarkup` pattern; the `vi.mock('@/lib/actions/import', …)` block that mocks `loadImportFormatWizardContextAction`/`createPrivateImportFormatAction`/`completeOnboardingPrivateImportAction`; the `context` fixture; the `validFields` fixture and `validateWizardFields` assertions; the `ConfigureImportFormatPage` describe block; how `ImportFormatWizard` is invoked via `createElement` with props)
    - components/import/import-format-wizard.tsx (the new `attachablePlatforms` prop and step 1 markup strings to assert against)
  </read_first>
  <action>
    Extend `tests/import-format-wizard-ui.test.tsx`. Add `listAttachablePlatformsAction` to the `@/lib/actions/import` mock and have it resolve a small platform list by default. Update existing `ImportFormatWizard` render calls to pass an `attachablePlatforms` prop (a non-empty array for step-1 tests, `[]` for the empty-list test). Add tests: (1) with a non-empty `attachablePlatforms`, the rendered markup contains the platform name(s) and the "Crea una nuova platform" entry; (2) with `attachablePlatforms={[]}`, the markup renders the column form directly (e.g. contains "Modalità importo") and does NOT render step-1-only copy; (3) the onboarding flow regression still asserts "Salva formato e importa" and the onboarding back link. Update the `ConfigureImportFormatPage` test setup so `listAttachablePlatformsAction` is mocked (resolving `{ error: null, data: [] }`) and the existing context-load-failure assertion still passes. Keep all `validateWizardFields` unit assertions; if step 2 no longer renders a free-text platformName Input, adjust only the markup assertions that referenced that Input, not the `validateWizardFields` contract.
  </action>
  <verify>
    <automated>yarn test --run tests/import-format-wizard-ui.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: a test asserts the step 1 markup contains "Crea una nuova platform"
    - Behavior: a test renders with `attachablePlatforms={[]}` and asserts the column form ("Modalità importo") renders without step 1
    - Behavior: the onboarding regression test still asserts "Salva formato e importa"
    - Test command exits 0
  </acceptance_criteria>
  <done>UI tests cover step 1 render, empty-list skip to step 2, and the onboarding regression; full file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → server action | The wizard form submits existingPlatformId/platformName; authorization is enforced server-side (Plan 02) |
| RSC page → action | listAttachablePlatformsAction resolves userId via verifySession; the page never sees a client-supplied id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-06 | Tampering | existingPlatformId hidden input | high | mitigate | The client value is only a convenience; the server (Plan 02) validates it via Zod and a TOCTOU SELECT, and the precached list already excludes other users' pending platforms (ASVS V4/V5) |
| T-59-07 | Information Disclosure | step 1 platform list | medium | mitigate | The list comes from listAttachablePlatformsAction → DAL WHERE clause (Plan 01) which never exposes other users' pending platforms |
</threat_model>

<verification>
- `yarn test --run tests/import-format-wizard-ui.test.tsx` green
- Full wave verification: `yarn test --run tests/import-format-wizard-ui.test.tsx tests/import-format-wizard-actions.test.ts tests/import-private-formats-dal.test.ts lib/validations/__tests__/import.test.ts` green
- `yarn tsc --noEmit` clean for the modified files
- `yarn check:language` clean (Italian only in user-facing copy)
</verification>

<success_criteria>
- SC1: On failed detection the wizard offers existing Platforms before creating a new one (step 1 list)
- SC2/SC3: selecting an existing platform attaches (reuse); "Crea nuova" mints a pending platform only when chosen
- SC4: the redirect/onboarding flow still imports the file immediately after save
- Empty list gracefully degrades to direct create-new (step 2 only)
</success_criteria>

<output>
Create `.planning/phases/59-import-wizard-attach-format/59-03-SUMMARY.md` when done
</output>
