---
phase: 77-amortization-schema-and-activation
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - lib/services/transaction-detach.ts
  - lib/actions/amortization.ts
  - lib/validations/amortization.ts
  - lib/dal/transactions.ts
  - components/transactions/remove-amortization-dialog.tsx
  - components/transactions/transaction-table.tsx
  - components/transactions/transaction-detail-client.tsx
  - tests/amortization-undo.test.ts
autonomous: true
requirements: [AMORT-01, AMORT-02]

must_haves:
  truths:
    - "Removing an amortization plan (rimuovi ammortamento) atomically deletes the plan and all its instalment rows and reverses the detach, re-attaching the transaction to its shared per-merchant Expense (D-09)."
    - "The reverse-detach recomputes the transaction's ORIGINAL descriptionHash from its (immutable) description via computeDescriptionHash — never from a stored snapshot, since none exists — and either merges into an existing shared Expense with that hash or creates a fresh one."
    - "The abandoned Standalone Expense is cleaned up via the SAME reconcileExpensesAfterTransactionRemoval() call already used by the forward detach path, deleting it when it has zero remaining transactions (unless it carries manual/override classification history, in which case it is preserved empty) — no bespoke cleanup logic is invented."
    - "The undo confirmation dialog states the consequence (plan + instalments deleted, transaction re-attached to the shared expense) before the destructive confirm, shows a spinner and disables the confirm button while the atomic teardown runs, and on failure shows an error toast while leaving the source plan/transaction untouched."
    - "The transaction detail page offers the same 'Ammortizza' entry point (reusing ActivateAmortizationDialog and the row's guard logic) as the transaction row, and offers 'Rimuovi ammortamento' whenever the transaction currently has an active plan — matching the Entry Point Visibility Matrix (active plan -> activation disabled+tooltip, undo shown)."
  prohibitions:
    - requirement_id: AMORT-02
      category: safety
      statement: "Undo (rimuovi ammortamento) MUST NOT leave the transaction orphaned (expenseId null) or permanently attached under the synthetic detach hash — the atomic reverse-detach must always resolve to a real shared or newly-created Expense."
      status: unverified
      flagged: true
  artifacts:
    - lib/services/transaction-detach.ts (reverseDetachTx)
    - lib/actions/amortization.ts (removeAmortizationPlan)
    - components/transactions/remove-amortization-dialog.tsx
    - lib/dal/transactions.ts (TransactionDetailRow.amortizationPlanId, reused via transactionListSelect)
    - tests/amortization-undo.test.ts
  key_links:
    - "row/detail 'Rimuovi ammortamento' -> RemoveAmortizationDialog -> removeAmortizationPlan Server Action -> reverseDetachTx (load plan+tx -> compute original hash -> find-or-create shared Expense -> re-point transaction.expenseId -> reconcileExpensesAfterTransactionRemoval on [target, abandoned] -> delete amortization_plan row, cascading instalments)"
    - "transaction-detail-client.tsx -> ActivateAmortizationDialog / RemoveAmortizationDialog (same components the row action uses, no duplicate dialog implementation)"
---

<objective>
Ship the D-09 "rimuovi ammortamento" undo path (reverse-detach: delete the plan+instalments,
re-attach the transaction to its shared Expense by its recomputed original descriptionHash) and
extend the transaction detail page with both the "Ammortizza" and "Rimuovi ammortamento" entry
points, reusing the dialogs Plan 77-01 built for the transaction row.

Purpose: gives a user a clean, immediate recovery from a mis-activation (AMORT-01's third stated
truth requires this be available "immediately", per CONTEXT.md D-09), and closes AMORT-01's
"detail page" entry point.
Output: a working "Rimuovi ammortamento" action on both the row and the detail page, and
"Ammortizza" now also reachable from the detail page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-amortization-schema-and-activation/77-CONTEXT.md
@.planning/phases/77-amortization-schema-and-activation/77-UI-SPEC.md
@lib/services/transaction-detach.ts
@lib/services/amortization-activation.ts
@lib/services/expense-reconciliation.ts
@lib/utils/import.ts
@lib/dal/transactions.ts
@components/transactions/activate-amortization-dialog.tsx
@components/transactions/detach-expense-dialog.tsx
@components/transactions/transaction-detail-client.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Reverse-detach service + removeAmortizationPlan action (D-09)</name>
  <precondition>lib/services/amortization-activation.ts::activatePlanTx and the amortization_plan/amortization_instalment tables exist (Plan 77-01 completed).</precondition>
  <files>lib/services/transaction-detach.ts, lib/actions/amortization.ts, lib/validations/amortization.ts, tests/amortization-undo.test.ts</files>
  <read_first>
    lib/services/transaction-detach.ts (applyDetachCleanupTx's full body — the forward operation this reverses; syntheticDescriptionHash; the single-vs-multi-transaction-source branching this reverse path does NOT need, since undo always targets exactly the one amortized transaction), lib/services/expense-reconciliation.ts (reconcileExpensesAfterTransactionRemoval — a full recompute from currently-linked transactions, reused unmodified here for both the target and abandoned expense ids), lib/utils/import.ts (computeDescriptionHash — normalizes+SHA256es the description; the transaction's description is immutable per ADR 0019 SS3, so this always recovers the true original hash), lib/actions/amortization.ts (createAmortizationPlan's structure — removeAmortizationPlan is the sibling action in the same file), lib/db/schema.ts (amortizationPlan's onDelete:'cascade' FK from amortizationInstalment — deleting the plan row cascades the instalments automatically, no manual instalment delete needed).
  </read_first>
  <behavior>
    lib/services/transaction-detach.ts::reverseDetachTx, test-first in tests/amortization-undo.test.ts
    (real-Postgres harness, reuses seedAmortizationPlan from Plan 77-01):
    - Undoing a plan whose transaction's description hash matches NO existing expense creates a
      new shared Expense (title = transaction.description, descriptionHash = computeDescriptionHash
      (description), subCategoryId/status carried forward from the abandoned Standalone Expense's
      current values) and re-points transaction.expenseId to it.
    - Undoing a plan whose transaction's description hash matches an EXISTING shared Expense (a
      second transaction with the same description was imported after activation) merges the
      transaction into that expense (re-points expenseId; does NOT alter that expense's existing
      subCategoryId/status/title).
    - After undo, the abandoned Standalone Expense has zero remaining transactions and is deleted
      (no manual/override classification history exists for it in this scenario).
    - After undo, the amortization_plan row and all its amortization_instalment rows are gone
      (verified by a zero-row count for that planId).
    - Undoing a plan for a transactionId/userId that does not match the caller's session throws
      (ownership check), and writes nothing.
  </behavior>
  <action>
    lib/services/transaction-detach.ts: add `export async function reverseDetachTx(tx: DbOrTx,
    input: {userId: string; transactionId: string; planId: string}): Promise<{expenseId: string}>`.
    Steps: (1) SELECT the amortizationPlan row scoped by id=planId AND userId=userId AND
    transactionId=transactionId (all three must match — throws DetachTransactionError-style if not
    found, reusing that error class). (2) SELECT the transaction row (description, expenseId =
    the current Standalone Expense id) scoped by id+userId. (3) SELECT that Standalone Expense's
    current subCategoryId and status (to carry forward if a new shared Expense must be created).
    (4) originalHash = computeDescriptionHash(transaction.description) (import from
    '@/lib/utils/import'). (5) SELECT an existing expense by (userId, descriptionHash=originalHash)
    excluding the Standalone Expense's own id. (6a) If found: targetExpenseId = found.id. (6b) If
    not found: INSERT a new expense (id=crypto.randomUUID(), userId, title=transaction.description,
    descriptionHash=originalHash, subCategoryId=<step 3 value>, status=<step 3 value>,
    totalAmount=transaction.amount (toDbDecimal), transactionCount=1,
    firstTransactionAt=lastTransactionAt=transaction.occurredAt, importedFromFileId=null);
    targetExpenseId = the new id. (7) UPDATE transaction SET expenseId=targetExpenseId WHERE
    id=transactionId AND userId=userId. (8) DELETE FROM amortizationPlan WHERE id=planId (cascades
    amortizationInstalment automatically via the FK). (9) await
    reconcileExpensesAfterTransactionRemoval(tx, {userId, affectedExpenseIds: [targetExpenseId,
    <the abandoned Standalone Expense id from step 2>]}) — recomputes both (idempotent no-op on the
    freshly-inserted new-expense path; correctly folds the transaction into an existing expense's
    aggregate on the merge path; deletes/preserves the abandoned expense per its own existing rule).
    Return {expenseId: targetExpenseId}.

    lib/actions/amortization.ts: add `export async function removeAmortizationPlan(input:
    {planId: string}): Promise<{error: string | null}>`. Validates planId via a new
    RemoveAmortizationPlanSchema (z.object({planId: z.string().uuid()})) in
    lib/validations/amortization.ts, calls verifySession(), loads the plan's transactionId first
    (a small SELECT scoped by id+userId — needed as reverseDetachTx's input), wraps
    db.transaction(tx => reverseDetachTx(tx, {userId, transactionId, planId})) in try/catch
    (Italian fallback: 'Si è verificato un errore. Riprova tra qualche secondo.'), calls
    revalidateCategorizationSurfaces() on success.
  </action>
  <reversibility rating="costly">Reverting a detach is a reconciliation operation touching two
  Expense rows and cascading a delete across two tables, not a simple row delete — matches
  CONTEXT.md D-09's own "costly" rating; the reverse-detach invariant (recompute, never store, the
  original hash) must be correct in this foundation phase since Phase 78+ has no other undo
  path.</reversibility>
  <verify>
    <automated>node_modules/.bin/vitest run tests/amortization-undo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    tests/amortization-undo.test.ts exits 0 covering all 5 <behavior> cases against a real Postgres
    harness. After removeAmortizationPlan succeeds: amortization_plan and amortization_instalment
    have zero rows for that plan/transaction; the transaction's expenseId points at an expense whose
    descriptionHash equals computeDescriptionHash(transaction.description); the abandoned Standalone
    Expense row is gone (or preserved-empty if it had manual classification history).
  </acceptance_criteria>
  <done>A plan can be atomically removed, always resolving the transaction back to a real shared or
  freshly-created Expense — never orphaned, never left under the synthetic detach hash.</done>
</task>

<task type="auto">
  <name>Wire "Rimuovi ammortamento" (row + detail) and "Ammortizza" on the detail page</name>
  <files>components/transactions/remove-amortization-dialog.tsx, components/transactions/transaction-table.tsx, components/transactions/transaction-detail-client.tsx, lib/dal/transactions.ts</files>
  <read_first>
    components/transactions/transaction-table.tsx lines 658-676 and 860-940 (the "Spesa a sé"
    DropdownMenuItem + dialog-render-at-bottom pattern this task mirrors for "Rimuovi
    ammortamento"; the amortizationPlanId row field Plan 77-01 added), components/transactions/detach-expense-dialog.tsx
    (confirmation-dialog structure to mirror), components/transactions/transaction-detail-client.tsx
    full file (useState dialog-open pattern, existing "Spesa a sé"/reimbursement action buttons to
    place the two new buttons alongside), lib/dal/transactions.ts (transactionListSelect's
    amortizationPlanId field from Plan 77-01, and getTransactionForDetail's existing reuse-from-
    transactionListSelect pattern for pairedWithId at line 740), .planning/phases/77-amortization-schema-and-activation/77-UI-SPEC.md
    (Undo Confirmation Dialog copy block, Entry Point Visibility Matrix).
  </read_first>
  <action>
    components/transactions/remove-amortization-dialog.tsx (new, mirrors detach-expense-dialog.tsx's
    structure): Dialog, title "Rimuovi ammortamento", description "Questa azione eliminerà la
    pianificazione e tutte le rate associate. La transazione tornerà nella spesa condivisa per
    descrizione.", destructive Button "Rimuovi ammortamento" (spinner+disabled while pending, calls
    removeAmortizationPlan), ghost Button "Annulla". On success: toast.success('Ammortamento
    rimosso. Transazione ripristinata.'), onOpenChange(false), call the passed onSuccess callback.
    On error: toast.error(`Errore nel rimuovere l'ammortamento: ${result.error}`), dialog stays open.

    lib/dal/transactions.ts: extend getTransactionForDetail's select with
    `amortizationPlanId: transactionListSelect.amortizationPlanId` (same reuse pattern as
    `pairedWithId: transactionListSelect.pairedWithId` at line 740) and add the matching
    `amortizationPlanId: string | null` field to TransactionDetailRow.

    components/transactions/transaction-table.tsx: add a "Rimuovi ammortamento" DropdownMenuItem
    (Trash2 icon, destructive styling matching the UI-SPEC), shown only when
    `transaction.amortizationPlanId` is non-null (mirrors the existing conditional-render pattern
    for pairedWithId/expenseId at lines 628-676), opening RemoveAmortizationDialog for that
    row's amortizationPlanId. Render the dialog once near the existing DetachExpenseDialog/other
    dialog renders at the bottom of the table.

    components/transactions/transaction-detail-client.tsx: add two action buttons near the existing
    "Spesa a sé" action — "Ammortizza" (opens ActivateAmortizationDialog from Plan 77-01, reusing it
    unmodified; gated by the SAME five-guard client-side eligibility logic from Plan 77-01's row
    wiring, computed from this page's already-loaded `transaction.categoryType`, `transaction.groupId`,
    `transaction.amortizationPlanId`, `transaction.amount`, and reimbursement-involvement derived
    as `reimbursementPanelData !== undefined || refundMembership !== undefined` — both already
    passed as props, no new query needed) and "Rimuovi ammortamento" (opens
    RemoveAmortizationDialog, shown only when `transaction.amortizationPlanId` is non-null). Follow
    the Entry Point Visibility Matrix exactly: an active plan disables "Ammortizza" (tooltip
    "Questa transazione ha già una pianificazione attiva. Rimuovila prima di crearne una nuova.")
    and shows "Rimuovi ammortamento"; no plan hides "Rimuovi ammortamento" and gates "Ammortizza" on
    the other four guards.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <automated>node_modules/.bin/vitest run tests/amortization-undo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    A transaction row and its detail page both show "Rimuovi ammortamento" if and only if
    amortizationPlanId is non-null, and both show "Ammortizza" gated by the same five guard reasons
    used on the row. Confirming removal on the detail page redirects/refreshes to show the
    transaction re-attached to its shared expense (no plan/instalment rows remain).
  </acceptance_criteria>
  <done>Both entry points (row, detail page) offer activation and undo consistently, matching the
  Entry Point Visibility Matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> removeAmortizationPlan Server Action | Untrusted planId crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-77-06 | Elevation of Privilege | removeAmortizationPlan | high | mitigate | verifySession() supplies userId; reverseDetachTx's plan lookup is scoped `id=planId AND userId=userId AND transactionId=transactionId` — a foreign planId resolves to "not found", never another user's plan |
| T-77-07 | Tampering | reverseDetachTx atomicity | high | mitigate | expenseId re-point, plan/instalment delete, and both reconcile calls run inside one passed-in `tx`; any failure rolls back the whole db.transaction, so the transaction can never end up pointed at a deleted expense or with an orphaned expenseId |
| T-77-08 | Repudiation | removeAmortizationPlan | low | accept | No dedicated audit-log entry is written for an undo (matches the forward detach path, which also writes no explicit audit row today — expenseClassificationHistory only records categorization events, not detach/reverse-detach) |

</threat_model>

<verification>
tests/amortization-undo.test.ts exits 0. `node_modules/.bin/tsc --noEmit` is clean. Manual: activate
amortization on a transaction, then remove it from both the row menu and the detail page (two
separate transactions), confirming both re-attach correctly and the standalone expense is cleaned
up.
</verification>

<success_criteria>
A user can remove an amortization plan from the row or the detail page, atomically, always ending
in a real shared or freshly-created Expense. The detail page offers both "Ammortizza" and "Rimuovi
ammortamento" consistent with the row's guard logic.
</success_criteria>

<output>
Create `.planning/phases/77-amortization-schema-and-activation/77-02-SUMMARY.md` when done
</output>
