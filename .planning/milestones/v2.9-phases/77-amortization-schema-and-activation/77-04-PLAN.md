---
phase: 77-amortization-schema-and-activation
plan: 04
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - lib/dal/dashboard.ts
  - tests/reimbursement-regression.test.ts
autonomous: true
requirements: [LENS-03]

must_haves:
  truths:
    - "getCategoriesBreakdown, getCategoryRanking, and getCategoryDeviations all read their per-row amount from ledger_entry_cash.amount and no longer call effectiveAmount() or isNotSecondary() directly, including the extra effectiveAmount() reference inside getCategoryRanking's own ORDER BY clause (D-11)."
    - "getCategoryDetail's trend and subcategory-breakdown sub-queries migrate the same way; its top-transactions sub-query keeps DISPLAYING each transaction's raw (un-netted) amount exactly as before — only the ORDER BY ranking switches to the netted ledger_entry_cash.amount, via an added join keyed on transaction id, never by renaming the SELECTed amount field itself (a behavior-preserving distinction, not a refactor shortcut)."
    - "getMonthlyTrendByNature migrates the same way while preserving its LEFT JOIN chain (transactions with a null expenseId must still appear, unlike the INNER JOIN functions above)."
    - "All five functions' pre-existing regression assertions in tests/reimbursement-regression.test.ts stay green unmodified — the seam migration changes the SQL, not the observable output (LENS-03)."
  artifacts:
    - lib/dal/dashboard.ts (getCategoriesBreakdown, getCategoryRanking, getCategoryDeviations, getCategoryDetail, getMonthlyTrendByNature — all migrated to ledger_entry_cash)
  key_links:
    - "getCategoriesBreakdown/getCategoryRanking/getCategoryDeviations/getCategoryDetail/getMonthlyTrendByNature -> ledger_entry_cash view -> byte-identical outputs"
---

<objective>
Complete the ledger_entry seam migration (D-11) for the five remaining `lib/dal/dashboard.ts`
aggregation functions Plan 77-01 did not touch (it migrated only `getOverviewAmountTotals` as the
tracer's proof). Every function stops calling `effectiveAmount()`/`isNotSecondary()` directly and
reads `ledger_entry_cash.amount` instead, per the established Plan 77-01 pattern.

Purpose: LENS-03 requires ALL cash-lens aggregation call sites — not just one — to be
byte-identical once amortization data exists; this plan (with its sibling 77-05) delivers the
other 9 of the 10 gated functions.
Output: dashboard.ts fully migrated; per-function regression proof extended for each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/dal/dashboard.ts
@lib/dal/dashboard-filters.ts
@lib/db/schema.ts
@tests/reimbursement-regression.test.ts
@tests/fixtures/reimbursement-seed.ts
</context>

<tasks>

<task type="auto">
  <name>Migrate getCategoriesBreakdown, getCategoryRanking, getCategoryDeviations to ledger_entry_cash</name>
  <precondition>lib/db/schema.ts exports ledgerEntryCash and lib/dal/dashboard-filters.ts exports the generalized dateScopedTransactions (Plan 77-01 completed).</precondition>
  <files>lib/dal/dashboard.ts</files>
  <read_first>
    lib/dal/dashboard.ts lines 973-1200 (the three functions this task migrates, verbatim — every
    `.from(transactionTable)`, `effectiveAmount()`, `isNotSecondary()`, and `dateScopedTransactions(
    userId, ...)` call site in each), lib/dal/dashboard.ts lines 458-522 (getOverviewAmountTotals —
    Plan 77-01's already-migrated reference implementation; this task applies the IDENTICAL
    transformation to the three functions here), lib/db/schema.ts (ledgerEntryCash's exact exported
    column names: id, userId, occurredAt, expenseId, amount).
  </read_first>
  <action>
    For each of getCategoriesBreakdown (973-1032), getCategoryRanking (1034-1092), and
    getCategoryDeviations (1094-1200, both the reference and baseline sub-queries): change
    `.from(transactionTable)` to `.from(ledgerEntryCash)`; change every join currently anchored on
    `transactionTable.expenseId` (e.g. `.innerJoin(expense, eq(transactionTable.expenseId,
    expense.id))`) to `eq(ledgerEntryCash.expenseId, expense.id)`; replace every
    `${effectiveAmount()}` reference (including getCategoryRanking's ORDER BY at line 1085, which
    repeats the same expression outside the SELECT list — do not miss it) with
    `${ledgerEntryCash.amount}`; remove `isNotSecondary()` from every WHERE clause (the view's own
    definition already excludes refund rows); update every `dateScopedTransactions(userId, from,
    to)` call to `dateScopedTransactions(ledgerEntryCash, userId, from, to)` (the generalized
    signature from Plan 77-01). getCategoryRanking's `monthSql` (`to_char(${transactionTable
    .occurredAt}, 'YYYY-MM')`) becomes `to_char(${ledgerEntryCash.occurredAt}, 'YYYY-MM')`. Leave
    every other join (subCategory, category, userSubcategoryOverride, nature, direction), GROUP BY,
    and the surrounding try/catch/empty-fallback structure completely unchanged — this is a pure
    row-source swap, not a query rewrite.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <automated>node_modules/.bin/vitest run tests/reimbursement-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Zero remaining occurrences of `effectiveAmount()` or `isNotSecondary()` inside
    getCategoriesBreakdown, getCategoryRanking, or getCategoryDeviations (grep-verifiable:
    `grep -n "effectiveAmount\|isNotSecondary" lib/dal/dashboard.ts` shows no hits between lines
    973-1200). tests/reimbursement-regression.test.ts's pre-existing assertions for
    getCategoriesBreakdown/getCategoryRanking/getCategoryDeviations remain green unmodified.
  </acceptance_criteria>
  <done>Three of the five remaining dashboard.ts aggregation sites read from ledger_entry_cash.</done>
</task>

<task type="auto">
  <name>Migrate getCategoryDetail (with the top-transactions dual-join case) and getMonthlyTrendByNature</name>
  <precondition>The previous task in this plan is complete (same file, sequential within this plan).</precondition>
  <files>lib/dal/dashboard.ts, tests/reimbursement-regression.test.ts</files>
  <read_first>
    lib/dal/dashboard.ts lines 1202-1428 (getCategoryDetail's three sub-queries — trend, subcategory
    breakdown, and the special top-transactions query at lines 1361-1406 whose SELECT list at
    1362-1373 reads `amount: transactionTable.amount` — the RAW, un-netted amount, deliberately
    different from the aggregate `amount` fields elsewhere — while its ORDER BY at line 1405 sorts by
    `abs(${effectiveAmount()})`, the NETTED value), lib/dal/dashboard.ts lines 1430-1485
    (getMonthlyTrendByNature — note the LEFT JOIN chain, not INNER JOIN, and its two non-amount
    aggregate fields totalNc/totalIgn which are unaffected by this migration).
  </read_first>
  <action>
    getCategoryDetail's trend sub-query and subcategory-breakdown sub-query (including its own
    `orderBy(desc(sql\`coalesce(abs(sum(${effectiveAmount()})), 0)\`), ...)` at line 1360): apply the
    IDENTICAL transformation from the previous task (`.from(ledgerEntryCash)`, join via
    ledgerEntryCash.expenseId, replace effectiveAmount() with ledgerEntryCash.amount, drop
    isNotSecondary(), generalize dateScopedTransactions).

    getCategoryDetail's top-transactions sub-query is a SPECIAL CASE — do NOT apply the same
    from-swap, because its SELECT list displays the transaction's RAW amount (not netted) while only
    its ranking should use the netted value; renaming `amount: transactionTable.amount` to
    `ledgerEntryCash.amount` would silently change the DISPLAYED figure (a real LENS-03 regression).
    Instead: keep `.from(transactionTable)` as the query root unchanged, and ADD
    `.innerJoin(ledgerEntryCash, eq(ledgerEntryCash.id, transactionTable.id))` (an id-to-id join —
    ledgerEntryCash's cash-lens rows map 1:1 onto transaction rows, since no instalment row ever
    appears in ledgerEntryCash). This join has two effects, both needed: it replaces `isNotSecondary()`
    in the WHERE clause (drop that call — a refund transaction simply has no matching
    ledgerEntryCash row, so the INNER JOIN itself excludes it, identical to today's NOT EXISTS
    filter), and it supplies `ledgerEntryCash.amount` for the ORDER BY at line 1405 (`desc(abs(
    ${ledgerEntryCash.amount}))` replacing `desc(abs(${effectiveAmount()}))`). The SELECT list's
    `amount: transactionTable.amount` stays exactly as it is today — untouched.

    getMonthlyTrendByNature: same transformation as the earlier task's three functions
    (`.from(ledgerEntryCash)`, `eq(ledgerEntryCash.expenseId, expense.id)` — keep this join LEFT, not
    INNER, matching the existing `.leftJoin(expense, ...)`; replace `${effectiveAmount()}` in the
    `amount` SELECT expression with `${ledgerEntryCash.amount}`; drop `isNotSecondary()`; generalize
    dateScopedTransactions; `monthSql` reads `${ledgerEntryCash.occurredAt}`). Leave totalNc/totalIgn
    (both `expense`-only aggregates, no amount involved) untouched.

    tests/reimbursement-regression.test.ts: extend the amortization describe block Plan 77-01 added
    with two more assertions confirming getCategoriesBreakdown, getCategoryRanking,
    getCategoryDeviations, getCategoryDetail, and getMonthlyTrendByNature all stay byte-identical
    (same essentialCategoryId amount / same top-transaction raw amount / same monthly-trend amount)
    between the pre-amortization and post-amortization snapshots captured in Plan 77-01's beforeAll —
    mirroring the existing per-function assertion style already used for the N=1 reimbursement
    scenario earlier in the same file.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <automated>node_modules/.bin/vitest run tests/reimbursement-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Zero remaining occurrences of `effectiveAmount()`/`isNotSecondary()` inside getCategoryDetail's
    trend and subcategory-breakdown sub-queries (top-transactions keeps neither call — both are
    structurally replaced by the ledgerEntryCash join). getMonthlyTrendByNature has zero remaining
    occurrences. tests/reimbursement-regression.test.ts's pre-existing getCategoryDetail assertion
    (top-transaction amount is the RAW un-netted figure) remains green — proving the dual-join
    special case did not change the displayed value. The new amortization-scenario assertions for
    all five functions pass.
  </acceptance_criteria>
  <done>All five remaining dashboard.ts aggregation sites migrated; getCategoryDetail's raw-vs-netted
  amount distinction preserved exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| ledger_entry_cash view -> five aggregation functions | No new user input crosses here (all inputs are already-validated filters/date ranges from the existing dashboard read paths) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-77-11 | Tampering | getCategoryDetail top-transactions dual join | medium | mitigate | The join is keyed on `ledgerEntryCash.id = transactionTable.id` (both primary-key-shaped, no user input in the join predicate); the raw-amount SELECT is verified unchanged by the existing regression assertion, closing the "silently netted a displayed value" risk this task explicitly calls out |
| T-77-12 | Information Disclosure | ledger_entry_cash join scope | low | accept | Every migrated function retains its existing `dateScopedTransactions(..., userId, ...)` and expense-status WHERE predicates unchanged — the seam swap does not touch the user-scoping boundary |

</threat_model>

<verification>
`node_modules/.bin/tsc --noEmit` is clean. tests/reimbursement-regression.test.ts exits 0, all
pre-existing assertions plus the new per-function amortization assertions for all five functions.
`grep -n "effectiveAmount\|isNotSecondary" lib/dal/dashboard.ts` shows no hits between lines
973-1485 (getUncategorizedCount at 436-456 is explicitly out of scope and untouched).
</verification>

<success_criteria>
All five remaining dashboard.ts aggregation sites are migrated to ledger_entry_cash with
byte-identical output, including the top-transactions raw-amount preservation.
</success_criteria>

<output>
Create `.planning/phases/77-amortization-schema-and-activation/77-04-SUMMARY.md` when done
</output>
