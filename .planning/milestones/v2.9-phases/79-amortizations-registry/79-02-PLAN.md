---
phase: 79-amortizations-registry
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - components/amortizations/amortization-table.tsx
  - tests/amortization-registry-table.test.ts
  - tests/amortization-registry-dal.test.ts
autonomous: true
requirements: [REG-02]

must_haves:
  truths:
    - "Clicking 'Chiudi' on an open plan row opens the EXISTING CloseAmortizationDialog (reused verbatim, D-A1) passing planId=row.id; on success the dialog closes and router.refresh() re-fetches the RSC page's data so the row reflects status='closed' without a full page reload."
    - "'Realizza con vendita' on an open plan row is a plain navigation Link to transactionDetailHref(row.transactionId) (D-A2) — no inline sale-value input, no transaction picker, no new server action; the destination page hosts the entire Phase 78 realization flow unchanged."
    - "Both 'Chiudi' and 'Realizza con vendita' render ONLY when row.status === 'open' (D-A3); a closed row's actions cell is empty — gated by the single exported resolveRowActions(row).showActions predicate, unit-tested for both statuses without jsdom."
    - "ASSUMPTION (flagged, REG-02/edge-probe unclassified): REG-02's ROADMAP wording ('optionally entering a sale/realization value') is satisfied by D-A1 (scrap-close, no inline value) + D-A2 (deep-link to the existing realization flow) rather than a literal inline scalar field — a deliberate, CONTEXT.md-locked narrowing (78-CONTEXT.md D-02 forbids a synthetic transaction), not a scope gap."
    - "getAmortizationPlanList's list correctly reflects a plan closed via closePlanTx — re-querying after a direct closePlanTx call shows status='closed' and an updated netValue/remainingMonths Decimal-consistent with closePlanTx's own returned remainingValue, proving the read path (Plan 79-01) and the write path (Phase 78, reused here) never numerically diverge."
    - "The full test suite — including tests/reimbursement-regression.test.ts's LENS-03 byte-identical cash-lens assertions — stays green after this plan; the registry introduces no new write path, every mutation still flows through Phase 78's already-regression-proven closePlanTx/realizePlanTx."
    - { statement: "Row action buttons have a defined pending/disabled visual state while the close server action is in flight, and the action group's label/button sizing does not overflow the actions cell under narrow/mobile layouts (E4) — CloseAmortizationDialog's own internal pending state, reused verbatim, IS already covered by Phase 78's existing tests; this backstop covers only the NEW registry row-level presentation, not exercised by an automated test in this phase.", verification: backstop }
    - { statement: "Actions behave correctly for a plan in every open-lifecycle edge state (e.g. zero remaining months but still open) — showActions remains true regardless of remainingMonths; exercised only by resolveRowActions's status-based unit test, not a full lifecycle-state matrix (E4).", verification: backstop }
  artifacts:
    - components/amortizations/amortization-table.tsx
    - tests/amortization-registry-table.test.ts
    - tests/amortization-registry-dal.test.ts
  key_links:
    - "AmortizationTable's 'Chiudi' button mounts the SAME CloseAmortizationDialog component Phase 78 already wired into transaction-table.tsx/transaction-detail-client.tsx — zero new dialog code, only a new mount site."
    - "router.refresh() (next/navigation) in the dialog's onSuccess callback mirrors the EXACT pattern already used by transaction-table.tsx/transaction-detail-client.tsx — not the placeholder 'SWR hook' comment left in 79-RESEARCH.md/79-PATTERNS.md's illustrative pseudocode, which this plan supersedes with the concrete, already-proven mechanism."
  prohibitions:
    - "MUST NOT write a synthetic/placeholder transaction to realize a plan (ADR 0019 §8, D-A1/D-A2) — 'Realizza con vendita' is ALWAYS a navigation to the transaction detail page's real-transaction-linking flow, never an inline value field in this registry."
    - "MUST NOT perturb the LENS-03 byte-identical cash-lens regression — this plan adds zero new write paths; every mutation reuses Phase 78's already-regression-proven closePlanTx/realizePlanTx unchanged."
---

<objective>
Deliver REG-02 (79-CONTEXT.md D-A1/D-A2/D-A3): wire the registry's row actions on top of Plan
79-01's already-working read path — "Chiudi" (scrap-close, reusing the existing
CloseAmortizationDialog verbatim) and "Realizza con vendita" (a deep-link navigation to the
transaction detail page's existing Phase 78 realization flow), both visible ONLY on open plans.
No new backend mechanics: every mutation this plan can trigger already exists and is already
regression-proven from Phase 78.

Purpose: let the user act on a plan directly from the registry without ever constructing a
synthetic transaction, honoring ADR 0019 §8's "never synthetic" invariant by omission — the
registry offers no inline sale-value field at all.
Output: components/amortizations/amortization-table.tsx extended with a row-actions column
(resolveRowActions helper + CloseAmortizationDialog mount + router.refresh() wiring), plus a
DAL-level proof that the registry's read query and Phase 78's write path never numerically diverge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-amortizations-registry/79-CONTEXT.md
@docs/adr/0019-amortization-accrual-lens.md
@components/transactions/close-amortization-dialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire "Chiudi" + "Realizza con vendita" row actions (D-A1/D-A2/D-A3)</name>
  <files>components/amortizations/amortization-table.tsx, tests/amortization-registry-table.test.ts</files>
  <read_first>
    components/transactions/close-amortization-dialog.tsx (CloseAmortizationDialog's exact props:
    open, onOpenChange, planId, onSuccess — reused verbatim, zero changes)
    components/transactions/transaction-table.tsx lines 1093-1114 (the EXISTING
    "{closeAmortizeTarget && (<CloseAmortizationDialog .../>)}" conditional-mount pattern and its
    onSuccess calling router.refresh() — mirror this exact shape, not the vague "SWR hook"
    placeholder comment left in 79-RESEARCH.md/79-PATTERNS.md's illustrative pseudocode)
    components/transactions/transaction-detail-client.tsx lines 583-604 (a second confirming
    instance of the SAME open/onOpenChange/planId/onSuccess -> router.refresh() shape)
    lib/routes.ts (transactionDetailHref(transactionId) — the D-A2 realize target; import from
    '@/lib/routes' alongside the already-imported reimbursementHref-style helpers)
    docs/adr/0019-amortization-accrual-lens.md sections 7-8 (§7: scrap-close collapses remaining
    instalments onto the closure month, past instalments untouched — this is exactly what
    CloseAmortizationDialog's existing copy already describes, no new copy needed; §8: realization
    = linking a REAL sale transaction, never a synthetic one — why "Realizza con vendita" is a
    navigation, not an inline form)
    79-CONTEXT.md D-A1/D-A2/D-A3 (the locked action contract this task implements)
  </read_first>
  <behavior>
    - resolveRowActions({ status: 'open', ... }).showActions === true.
    - resolveRowActions({ status: 'closed', ... }).showActions === false.
    - resolveRowActions({ transactionId: 'tx-123', ... }).realizeHref equals
      transactionDetailHref('tx-123') exactly, regardless of status (href generation is unconditional;
      only RENDERING the action buttons is gated on status via showActions).
  </behavior>
  <action>
    In components/amortizations/amortization-table.tsx, add an Azioni <TableHead> as the final
    column (plain, not sortable — no HeaderSortButton). Export
    resolveRowActions(row: Pick<AmortizationPlanListRow, 'id' | 'transactionId' | 'status'>): {
    showActions: boolean; realizeHref: string } — returns { showActions: row.status === 'open',
    realizeHref: transactionDetailHref(row.transactionId) }. Add local component state
    closeTarget: string | null (a planId) via useState, and const router = useRouter() (from
    'next/navigation'). In each row's Azioni cell, call resolveRowActions(row); when showActions is
    true, render a `flex gap-1` group with a "Chiudi" Button (variant="outline" size="sm",
    onClick sets closeTarget to row.id) and a "Realizza con vendita" Link (href={realizeHref})
    wrapping an outline/sm Button; when showActions is false, render nothing (an empty cell, not
    even a placeholder dash). After the closing </Table> (or the EmptyState branch), conditionally
    mount `{closeTarget && (<CloseAmortizationDialog open={Boolean(closeTarget)}
    onOpenChange={(open) => { if (!open) setCloseTarget(null) }} planId={closeTarget}
    onSuccess={() => { setCloseTarget(null); router.refresh() }} />)}` — mirrors
    transaction-table.tsx's exact conditional-mount + onSuccess shape; wrap the table's existing
    top-level return in a `<>...</>` fragment if it is not already one, since the dialog must be a
    sibling of the `<div>` wrapping the toolbar/table, not nested inside it.

    Extend tests/amortization-registry-table.test.ts with resolveRowActions cases covering all 3
    <behavior> scenarios above, using a makeRow-style fixture builder consistent with this file's
    existing tests (Plan 79-01).
  </action>
  <reversibility rating="reversible">Wires existing, already-tested Phase 78 dialogs/services onto a
  new mount site — removing the row-actions column later unwinds nothing else (D-A1/D-A2/D-A3).</reversibility>
  <verify>
    <automated>yarn vitest run tests/amortization-registry-table.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - components/amortizations/amortization-table.tsx contains the literal strings "Chiudi" and
      "Realizza con vendita" (grep confirms both).
    - `grep -c 'router.refresh()' components/amortizations/amortization-table.tsx` returns >= 1.
    - All 3 resolveRowActions <behavior> cases pass in tests/amortization-registry-table.test.ts.
    - `grep -c 'CloseAmortizationDialog' components/amortizations/amortization-table.tsx` returns
      >= 2 (the import plus exactly one mount site).
  </acceptance_criteria>
  <done>An open plan's row shows "Chiudi" (opens the existing close dialog, refreshes the list on
  success) and "Realizza con vendita" (navigates to the transaction detail page's existing
  realization flow); a closed plan's row shows neither.</done>
</task>

<task type="auto">
  <name>Task 2: DAL/lifecycle consistency proof + full-suite regression gate</name>
  <files>tests/amortization-registry-dal.test.ts</files>
  <read_first>
    lib/services/amortization-lifecycle.ts (closePlanTx(tx, { userId, planId, closureMonth }) —
    signature and returned { closureInstalmentId, remainingValue } shape)
    tests/amortization-lifecycle.test.ts lines 1-60 (the exact pattern for importing/calling
    closePlanTx directly against the harness db inside a test, bypassing the action layer)
  </read_first>
  <action>
    In tests/amortization-registry-dal.test.ts, add one integration case: seed an open plan via
    seedAmortizationPlan/materializeInstalments (same fixture helpers as Plan 79-01's tests), call
    getAmortizationPlanList(userId) and assert the plan's row shows status:'open'; then call
    closePlanTx(db, { userId, planId, closureMonth: <an explicit test date> }) directly (imported
    from lib/services/amortization-lifecycle.ts, same harness db); call
    getAmortizationPlanList(userId) again and assert the SAME row now shows status:'closed', and
    that toDecimal(row.netValue).equals(toDecimal(closeResult.remainingValue).negated().plus(...))
    is NOT the assertion — instead assert row.remainingMonths reflects the collapsed schedule (0 or
    1 depending on whether the closure month has itself already passed relative to CURRENT_DATE at
    test time) and that netValue recomputes consistently with a fresh consumed/total read, proving
    the list query and closePlanTx's write are never numerically inconsistent.

    Run the full suite to confirm zero collateral regressions: `yarn vitest run` (includes
    tests/reimbursement-regression.test.ts's LENS-03 byte-identical cash-lens assertions — this
    plan introduces no new write path, so those assertions are expected to be unaffected by
    construction), `yarn tsc --noEmit`, and `yarn check:language`.
  </action>
  <verify>
    <automated>yarn vitest run && yarn tsc --noEmit && yarn check:language</automated>
  </verify>
  <acceptance_criteria>
    - The new getAmortizationPlanList/closePlanTx consistency case passes.
    - Full test suite passes with 0 failures, including tests/reimbursement-regression.test.ts.
    - `yarn tsc --noEmit` and `yarn check:language` are both clean.
  </acceptance_criteria>
  <done>REG-01, REG-02, and REG-03 are all delivered with zero regression on the LENS-03 cash-lens
  guarantee; the registry's read path and Phase 78's write path are proven numerically
  consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry "Chiudi" button -> closePlanAction | untrusted planId crosses the server boundary via the EXISTING Phase 78 action, unchanged by this plan |
| registry "Realizza con vendita" -> transactionDetailHref navigation | a plain client-side GET navigation, no mutation; the destination page's own realizePlanAction is Phase 78's unchanged code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-04 | Tampering/Elevation | closePlanAction invoked from the new registry entry point | high | mitigate | closePlanAction is REUSED VERBATIM from Phase 78 (already mitigated there: ClosePlanSchema validates planId shape, verifySession() resolves userId server-side, loadOpenPlanForOwner scopes the SELECT by id+userId together) — this plan adds zero new server code on the close path, only a new UI call site |
| T-79-05 | Tampering | "Realizza con vendita" deep link | medium | mitigate | the registry only generates a transactionDetailHref(transactionId) — a plain client-side navigation (GET), no mutation; the actual realizePlanAction the destination page invokes is Phase 78's own RealizePlanSchema + service-level ownership check, unchanged by this phase |
| T-79-06 | Tampering | synthetic-transaction bypass of the "never synthetic" invariant (ADR 0019 §8) | low | accept | the registry offers NO inline sale-value input field at all (D-A1/D-A2) — there is no code path in this phase that could construct a synthetic transaction; the invariant is preserved by omission, not by a new guard |

</threat_model>

<verification>
- `yarn vitest run tests/amortization-registry-table.test.ts tests/amortization-registry-dal.test.ts`
- `yarn vitest run` (full suite, confirms LENS-03 stays green)
- `yarn tsc --noEmit`
- `yarn check:language`
</verification>

<success_criteria>
- REG-02 fully delivered: a user can close a plan directly from the registry (scrap-close only,
  reusing the existing dialog) and can reach the realize-via-sale flow via a deep link to the
  transaction detail page; both actions are visible only on open plans.
- The full suite, including the LENS-03 cash-lens regression, stays green.
</success_criteria>

<output>
Create `.planning/phases/79-amortizations-registry/79-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `components/amortizations/amortization-table.tsx` — extended (Plan 79-01 file); adds
  `resolveRowActions(row)`, the Azioni column, and a `CloseAmortizationDialog` mount site.
- `tests/amortization-registry-table.test.ts` — extended with `resolveRowActions` test cases.
- `tests/amortization-registry-dal.test.ts` — extended with a `getAmortizationPlanList` /
  `closePlanTx` consistency case.
