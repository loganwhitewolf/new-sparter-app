---
phase: 80-dashboard-accrual-lens
plan: 04
type: execute
wave: 3
depends_on: ["80-01", "80-03"]
files_modified:
  - app/(app)/dashboard/overview/page.tsx
  - components/dashboard/dashboard-tab-nav.tsx
  - tests/dashboard-filters.test.ts
autonomous: true
requirements: [LENS-01, LENS-04, LENS-05]

must_haves:
  truths:
    - "/dashboard/overview fully reflects the lens: KPI totals (80-01), the bar chart, and the movers drill-down (80-03) all use the same resolved ledgerRowSource"
    - "The year selector on /dashboard/overview offers years that exist only under competenza, and falls back sanely (D-10 clamp) when the user flips lenses on a period that does not exist in the new lens"
    - "Switching tabs (Overview <-> Categorie <-> Tag) preserves ?lens= exactly like preset/type/sort/tag already do"
  artifacts:
    - "app/(app)/dashboard/overview/page.tsx fetches getYearsWithData for BOTH lenses and resolves the year via the extended resolveYear(requested, yearsForActiveLens, yearsForOtherLens)"
    - "buildDashboardTabHref (components/dashboard/dashboard-tab-nav.tsx) preserves a lens searchParam across tab navigation"
  key_links:
    - "app/(app)/dashboard/overview/page.tsx: parseLensParam(searchParams.lens) -> resolveLedgerRowSource -> getOverview/getOverviewChart/getMonthOverMonthCategoryChanges(..., ledgerRowSource)"
    - "app/(app)/dashboard/overview/page.tsx: getYearsWithData(lens) + getYearsWithData(otherLens) -> resolveYear(params.year, yearsForActiveLens, yearsForOtherLens)"
    - "DashboardTabNav -> buildDashboardTabHref preserves ?lens= across /dashboard/overview <-> /dashboard/categories <-> /dashboard/tags"
---

<objective>
Finish wiring `/dashboard/overview` end-to-end: thread the resolved `ledgerRowSource` into
`getOverviewChart`/`getMonthOverMonthCategoryChanges` (migrated in Plan 80-03) alongside the KPI
totals already wired in Plan 80-01, fetch `getYearsWithData` for BOTH lenses so the extended
`resolveYear` (Plan 80-03) can clamp a cross-lens period mismatch, and make `buildDashboardTabHref`
preserve `?lens=` across tab navigation — the last piece of D-03's "one global switch, shared
identically across all four dashboard sub-routes."

Purpose: this is the route where LENS-04 (whole year including future instalment months) and
LENS-05 (year selector offers an instalment-only year) become user-visible, and where the D-10
cross-lens clamp actually protects a real navigation path.
Output: /dashboard/overview is fully lens-aware; the tab nav carries the lens between routes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-dashboard-accrual-lens/80-CONTEXT.md
@.planning/phases/80-dashboard-accrual-lens/80-01-SUMMARY.md
@.planning/phases/80-dashboard-accrual-lens/80-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire /dashboard/overview's chart + movers + year selector to the resolved lens</name>
  <files>app/(app)/dashboard/overview/page.tsx</files>
  <read_first>
    app/(app)/dashboard/overview/page.tsx (current lens wiring from Plan 80-01 — parseLensParam/
    resolveLedgerRowSource already threaded into getOverview; this task extends it to the chart/
    movers calls and the year resolution);
    lib/dal/overview.ts (getYearsWithData(lens), getOverviewChart(year, ledgerRowSource),
    getMonthOverMonthCategoryChanges(..., ledgerRowSource) — all migrated in Plan 80-03);
    components/dashboard/overview/resolve-year.ts (the extended resolveYear signature from Plan 80-03).
  </read_first>
  <behavior>
    - Visiting /dashboard/overview?lens=competenza&year=2030 where 2030 exists only via an
      instalment -> renders the 2030 accrual dashboard (year selector includes 2030).
    - Visiting the SAME URL then flipping to cassa (?lens=cassa, year unset by the switch) -> the
      page recomputes yearsForActiveLens=cash years, and resolveYear clamps 2030 to the latest cash
      year instead of rendering an empty state.
  </behavior>
  <action>
    In app/(app)/dashboard/overview/page.tsx: fetch `const yearsForCassa = await
    getYearsWithData('cassa')` and `const yearsForCompetenza = await getYearsWithData('competenza')`
    (both calls, unconditionally — needed for the cross-lens clamp regardless of which lens is
    active). Set `const yearsForActiveLens = lens === 'competenza' ? yearsForCompetenza :
    yearsForCassa` and `const yearsForOtherLens = lens === 'competenza' ? yearsForCassa :
    yearsForCompetenza`. Replace the current `resolveYear(params.year, years)` call with
    `resolveYear(params.year, yearsForActiveLens, yearsForOtherLens)`. Pass `yearsForActiveLens` (not
    the old single `years`) to `OverviewHeader`'s `years` prop and to `OverviewDataSection`. Inside
    `OverviewDataSection` (or its call site), thread the SAME `ledgerRowSource` already resolved for
    `getOverview` into `getOverviewChart(year, ledgerRowSource)` and each
    `getMonthOverMonthCategoryChanges(year, defaultMonthIndex, direction, 10, ledgerRowSource)` call
    (currently three parallel calls for 'in'/'out'/'allocation' at lines 84-86) — same
    `ledgerRowSource` value for all three, never re-derive it per-call. Also thread `ledgerRowSource`
    into the `getOverviewChart(year - 1)` prior-year call used for YoY deltas (currently line 68) —
    both years must read the same lens for the delta comparison to be meaningful.
  </action>
  <acceptance_criteria>
    - `page.tsx` fetches `getYearsWithData('cassa')` and `getYearsWithData('competenza')` unconditionally
    - `resolveYear` is called with three arguments (requested, active, other)
    - `getOverviewChart` and all three `getMonthOverMonthCategoryChanges` calls receive the same
      `ledgerRowSource` value the KPI totals use
  </acceptance_criteria>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
    /dashboard/overview's chart, movers, and year selector all honor the active lens and clamp
    correctly on a cross-lens period mismatch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve ?lens= across dashboard tab navigation</name>
  <files>components/dashboard/dashboard-tab-nav.tsx, tests/dashboard-filters.test.ts</files>
  <read_first>
    components/dashboard/dashboard-tab-nav.tsx lines 14-42 (buildDashboardTabHref — the existing
    preset/type/sort/tag preservation pattern this task extends with lens);
    tests/dashboard-filters.test.ts lines 102-150 (the existing buildDashboardTabHref test suite
    this task extends with a lens test case, following the exact `?tag=` precedent at lines 131-141).
  </read_first>
  <behavior>
    - buildDashboardTabHref('/dashboard/categories', new URLSearchParams({lens:'competenza'})) ->
      '/dashboard/categories?lens=competenza'.
    - buildDashboardTabHref('/dashboard/tags', new URLSearchParams({preset:'last-3-months'})) (no
      lens present) -> '/dashboard/tags?preset=last-3-months' (lens omitted, not forced).
  </behavior>
  <action>
    In components/dashboard/dashboard-tab-nav.tsx's `buildDashboardTabHref` (line 14), add
    `const lens = searchParams.get('lens')` alongside the existing `preset`/`type`/`sort`/`tag`
    reads (lines 19-22), and `if (lens) { params.set('lens', lens) }` alongside the existing
    conditional sets (lines 24-38) — mirror the `tag` precedent exactly (line 22 and 36-38).
  </action>
  <acceptance_criteria>
    - `buildDashboardTabHref` preserves a present `lens` searchParam and omits it when absent, for
      all three tab hrefs (overview, categories, tags)
  </acceptance_criteria>
  <verify>
    <automated>yarn test tests/dashboard-filters.test.ts</automated>
  </verify>
  <done>
    The lens survives a tab switch exactly like preset/type/sort/tag already do (D-03).
  </done>
</task>

</tasks>

## Artifacts this phase produces

- `app/(app)/dashboard/overview/page.tsx` — full lens wiring (both-lens year fetch, cross-lens
  clamp via the extended `resolveYear`, `ledgerRowSource` threaded to every widget on the page)
- `buildDashboardTabHref` (components/dashboard/dashboard-tab-nav.tsx) — preserves `?lens=` across
  tab navigation, mirroring the existing preset/type/sort/tag preservation

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> RSC | `?lens=`/`?year=` on `/dashboard/overview` remain untrusted input, already validated by `parseLensParam`/`resolveYear` from prior plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-08 | Tampering | Chart/movers reading a DIFFERENT ledgerRowSource than the KPI totals on the same page (drift bug, not an attack) | medium | mitigate | Task 1's action requires the SAME resolved `ledgerRowSource` variable be threaded to every call site on the page — never re-derived per call — closing a class of "half the page updated" bugs |

</threat_model>

<verification>
`node_modules/.bin/tsc --noEmit && yarn test tests/dashboard-filters.test.ts` — green. Manually visit
/dashboard/overview, flip the lens, confirm chart + movers + KPIs + year selector all update together.
</verification>

<success_criteria>
/dashboard/overview is fully lens-consistent across every widget; the lens survives tab navigation.
</success_criteria>

<output>
Create `.planning/phases/80-dashboard-accrual-lens/80-04-SUMMARY.md` when done
</output>
