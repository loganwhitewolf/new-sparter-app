---
phase: 81-inline-net-display-for-paired-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/transactions/transaction-table.tsx
  - components/transactions/paired-reduction-badge.tsx
  - tests/transaction-table-paired-net-display.test.tsx
autonomous: true
requirements: [AMORT-05]

must_haves:
  truths:
    - "A transactions-table row that is the outflow ANCHOR of a pairing (pairedWithId set AND its own amount is negative — D-N1's single reimbursement/reimbursement_refund path used by both amortization-sale realization and v2.8 reimbursements) renders pairedNetAmount as the primary amount-cell figure with the gross transaction.amount struck-through and opaque directly beneath it (D-N2)."
    - "A transactions-table row that is the COUNTERPART of a pairing (pairedWithId set AND its own amount is positive) renders a 'riduzione di …' badge in the title row, in place of ReimbursementRowIndicator, linking to transactionDetailHref(pairedWithId) — the ANCHOR transaction, never /reimbursements/[id] — and its own amount cell is rendered attenuated instead of in the normal inflow tone (D-N3)."
    - "An unpaired row (pairedWithId === null) renders exactly as before this phase — single gross amount tone-colored by its own sign, ReimbursementRowIndicator only when reimbursementId is set — zero visual regression for the common unpaired case."
    - "Anchor-vs-counterpart role is resolved by a single client-side function (resolvePairRole) reading only pairedWithId and the sign of amount — both the amount-cell split and the title-badge swap consult the SAME resolved role, so they can never disagree on a given row."
    - "lib/dal/transactions.ts is NOT modified by this phase — the sign-based role discriminator is safe because assertOutflowAnchorAmount/assertInflowRefundAmount (lib/services/reimbursement-invariant.ts) already enforce, at write time, that every reimbursement anchor is negative and every linked refund is positive, for both createPairTx call sites (D-N1 zero-DAL-change goal)."
    - "effectiveAmount(), every dashboard/lens total, and tests/reimbursement-regression.test.ts's LENS-03 byte-identical assertions are unaffected — this phase touches only components/transactions/transaction-table.tsx and one new sibling component, never lib/dal/transaction-pairs-sql.ts or any aggregation query (D-N4)."
  artifacts:
    - components/transactions/transaction-table.tsx
    - components/transactions/paired-reduction-badge.tsx
    - tests/transaction-table-paired-net-display.test.tsx
  key_links:
    - "transaction-table.tsx computes `const pairRole = resolvePairRole(transaction)` once per row (alongside the existing isTransfer/amountColorClass derivations) and both Task 1's amount-cell branch and Task 2's title-row badge branch read that same value — a single source of truth for anchor-vs-counterpart."
    - "PairedReductionBadge's Link href is transactionDetailHref(transaction.pairedWithId) (lib/routes.ts) — the ANCHOR's own detail page — distinct from ReimbursementRowIndicator's reimbursementHref(reimbursementId) target, which keeps managing the /reimbursements deep link for every non-counterpart paired row."
  prohibitions:
    - "MUST NOT modify lib/dal/transactions.ts, lib/dal/transaction-pairs-sql.ts, or any dashboard/lens aggregation query — this phase is presentational only (D-N4)."
    - "MUST NOT introduce a new DB column, migration, or write path — zero data-layer change (D-N1 zero-DAL-change goal, D-N4 invariant)."
---

<objective>
Deliver the v2.9 UAT closure item (81-CONTEXT.md, no new REQ-ID; closes readability on AMORT-05): in
the transactions table, a paired outflow anchor (amortization-sale realization OR v2.8
reimbursement — one code path, D-N1) shows its `pairedNetAmount` as the primary amount-cell figure
with the gross `amount` struck-through/opaque beneath it (D-N2), and the paired counterpart row
carries a "riduzione di …" badge linking to its anchor transaction with an attenuated amount
(D-N3) — so a user reading the table alone understands the real net without opening the detail
page. Purely presentational (D-N4): zero changes to `lib/dal/transactions.ts`, `effectiveAmount()`,
netting, or any total — role discrimination reuses data `transactionListSelect` already exposes.

This is the phase's TRACER (Task 1): the thinnest end-to-end slice — a pure role-resolution
function reading already-fetched row fields, wired straight into the amount `<TableCell>`, verified
via a real render test — proving the zero-DAL-change, sign-based anchor/counterpart discriminator
before any further UI is built on top of it. Task 2 expands the SAME resolved role into the
title-row badge swap and closes with the full-suite + language-check gate (D-N4).

Purpose: close the non-blocking UAT gap flagged in `.planning/v2.9-MILESTONE-AUDIT.md` (Phase 78
item) — the detail page and dashboard already net correctly; the transactions table row does not.
Output: `resolvePairRole` + the split amount-cell render in `transaction-table.tsx`,
`components/transactions/paired-reduction-badge.tsx` (new), and a real-Postgres-free render test
covering anchor, counterpart, and unpaired rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-inline-net-display-for-paired-transactions/81-CONTEXT.md
@lib/dal/transactions.ts
@components/transactions/transaction-table.tsx
@components/transactions/reimbursement-row-indicator.tsx
@lib/routes.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Anchor row — net-primary amount with struck-through gross (D-N2)</name>
  <files>components/transactions/transaction-table.tsx, tests/transaction-table-paired-net-display.test.tsx</files>
  <read_first>
    lib/dal/transactions.ts lines 80-258 (pairedCounterpartIdExpr/pairedReimbursementIdExpr doc
    comments — the anchor/refund role rules this task's client-side discriminator must match;
    transactionListSelect's pairedWithId/pairedNetAmount/pairedAmount/pairedDescription/
    reimbursementId fields — ALL already selected, nothing to add) and lines 266-303
    (TransactionListRow type — the exact field names/types available on every row)
    lib/services/reimbursement-invariant.ts (assertOutflowAnchorAmount/assertInflowRefundAmount —
    proves an anchor's own `amount` is ALWAYS negative and a refund/counterpart's own `amount` is
    ALWAYS positive, enforced at write time for every pairing; this is what makes a pure
    sign-of-amount discriminator safe with zero DAL change)
    lib/services/transaction-pairs.ts lines 147-162 ("Resolve anchor/refund by SIGN, not |amount|
    magnitude" — the same convention this task's resolvePairRole mirrors on the read side)
    components/transactions/transaction-table.tsx lines 121-155 (computeAmortizationEligibility —
    the exact placement/style convention for a pure, row-derived helper function reading only
    TransactionListRow fields, declared above the component) and lines 552-556 (isTransfer/
    amountColorClass — the per-row derived-value block inside the transactions.map callback where
    pairRole must be computed alongside them) and lines 603-610 (the amount TableCell — the
    primary edit site, currently a single `{formatAmount(transaction.amount, transaction.currency)}`
    text node with the color class on the TableCell itself)
    lib/utils/amount-tone.ts (amountToneClass signature — categoryType param only overrides for
    'transfer'/'allocation', otherwise reads the value's own sign; used for the net figure's tone)
    lib/utils/format-amount.ts (formatAbsoluteAmount / the local `formatAmount` wrapper already in
    transaction-table.tsx — reuse both, do not add a third formatter)
    tests/transaction-table-menu.test.tsx (the exact render-test harness to mirror: `next/navigation`
    mocked to stub useRouter/useSearchParams/usePathname, `@/components/ui/dropdown-menu` mocked to a
    flat non-portal stub, `renderToStaticMarkup` from react-dom/server, a local `makeTransaction`
    fixture builder covering every TransactionListRow field with sensible defaults, a local `render`
    helper passing the full TransactionTable prop set)
  </read_first>
  <behavior>
    - resolvePairRole(transaction) returns null when pairedWithId is null (the common unpaired case).
    - resolvePairRole(transaction) returns 'anchor' when pairedWithId is set AND the row's own amount
      is negative (toDecimal(amount).isNegative()).
    - resolvePairRole(transaction) returns 'counterpart' when pairedWithId is set AND the row's own
      amount is positive (not negative) — the refund/sale/inflow leg.
    - An anchor-role transaction (amount '-133.00', pairedWithId set, pairedNetAmount '-33.00')
      renders BOTH the formatted net figure ('33,00 €', from pairedNetAmount) AND the formatted
      gross figure ('133,00 €', from amount) in the amount cell's markup, with a `line-through`
      class present only on the gross figure's element.
    - An unpaired transaction (pairedWithId null) renders ONLY its own formatted gross amount in the
      amount cell — no `line-through` class present anywhere in that row's markup, identical output
      shape to the pre-existing single-figure render.
  </behavior>
  <action>
    Above the TransactionTable component (near computeAmortizationEligibility), add a pure function
    resolvePairRole(transaction: TransactionListRow): 'anchor' | 'counterpart' | null — returns null
    when transaction.pairedWithId is null; otherwise returns 'anchor' when
    toDecimal(transaction.amount).isNegative() is true, else 'counterpart'. Document inline (English
    comment) that this is safe with zero DAL change because assertOutflowAnchorAmount/
    assertInflowRefundAmount already guarantee the sign invariant at write time for every pairing —
    amortization-sale realization and v2.8 reimbursements both flow through the same createPairTx
    path (D-N1), so there is no separate branch to add here.

    Inside the transactions.map callback, immediately after the existing `const amountColorClass = …`
    line, add `const pairRole = resolvePairRole(transaction)`.

    Restructure the amount `<TableCell>` (currently `className={cn('text-right font-mono
    tabular-nums', amountColorClass)}` wrapping a single `{formatAmount(...)}` text node): keep the
    TableCell's own className as the neutral `'text-right font-mono tabular-nums'` (drop
    amountColorClass from the cell itself — it moves onto the inner element per branch, so total
    visual output for every non-anchor row is unchanged, only the markup nesting differs). Inside the
    cell: when `pairRole === 'anchor' && transaction.pairedNetAmount`, render a `flex flex-col
    items-end leading-tight` wrapper containing (1) a span with
    className={amountToneClass(transaction.pairedNetAmount, transaction.categoryType)} showing
    formatAmount(transaction.pairedNetAmount, transaction.currency) as the primary net figure, and
    (2) a span with className `text-xs text-muted-foreground line-through opacity-60` showing
    formatAmount(transaction.amount, transaction.currency) as the struck-through gross figure
    beneath it. In every OTHER case (pairRole is null, or pairRole is 'anchor' but pairedNetAmount is
    unexpectedly null as a defensive fallback), render a single span with className={amountColorClass}
    showing formatAmount(transaction.amount, transaction.currency) — byte-for-byte the same visual
    result as today for every unpaired row. Do NOT branch on pairRole === 'counterpart' in this task
    — the counterpart-row amount attenuation is Task 2's change, so a counterpart row still shows its
    plain toned amount after this task alone (an accepted intermediate state within this same plan).

    Create tests/transaction-table-paired-net-display.test.tsx, mirroring
    tests/transaction-table-menu.test.tsx's mocks/harness exactly (next/navigation mock,
    dropdown-menu mock, renderToStaticMarkup, a local makeTransaction fixture covering every
    TransactionListRow field). Cover every <behavior> case above: an unpaired row's markup contains
    no `line-through` class; an anchor row's markup contains both the net-figure text and the
    struck-through gross-figure text with `line-through` present.
  </action>
  <reversibility rating="reversible">D-N2 is render-layer only over fields transactionListSelect
  already exposes — reverting removes the extra span and resolvePairRole, no data model or write
  path changes (per 81-CONTEXT.md D-N1..D-N4: "Reversibility: reversible, render-layer only").</reversibility>
  <verify>
    <automated>yarn vitest run tests/transaction-table-paired-net-display.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/transactions/transaction-table.tsx defines resolvePairRole (grep confirms the
      identifier) returning 'anchor' | 'counterpart' | null purely from pairedWithId and the sign of
      amount — no new field read from TransactionListRow beyond what's already typed.
    - The amount TableCell's `line-through` class (grep confirms it appears in
      transaction-table.tsx) is reachable only through the anchor branch.
    - `yarn vitest run tests/transaction-table-paired-net-display.test.tsx` passes.
  </acceptance_criteria>
  <done>An anchor row (paired, outflow) shows pairedNetAmount as the primary amount-cell figure with
  the gross amount struck-through/opaque beneath it; an unpaired row's amount cell renders exactly as
  it did before this task, proven by a real render test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counterpart row — reduction badge + attenuated amount, then full-suite gate (D-N3/D-N4)</name>
  <files>components/transactions/paired-reduction-badge.tsx, components/transactions/transaction-table.tsx, tests/transaction-table-paired-net-display.test.tsx</files>
  <read_first>
    components/transactions/reimbursement-row-indicator.tsx (the existing icon-only Link+Badge
    pattern this new component's shape mirrors: a Next.js Link wrapping a shadcn Badge, `shrink-0`
    placement so it survives a truncated title on the same flex row, an Italian aria-label + title
    tooltip)
    lib/routes.ts (transactionDetailHref(id) — the anchor-transaction link target D-N3 requires,
    distinct from reimbursementHref(id) which ReimbursementRowIndicator still uses)
    components/ui/badge.tsx (badgeVariants: 'outline' is `border-border text-foreground` with
    `[a&]:hover:bg-accent`; the base class already includes `overflow-hidden whitespace-nowrap` so a
    `truncate` + `max-w-*` className on the Badge itself is sufficient, no extra wrapping span needed)
    components/transactions/transaction-table.tsx lines 590-600 (the exact title-row spot where
    `{transaction.reimbursementId != null && <ReimbursementRowIndicator .../>}` renders today,
    alongside TransactionTagsChip on the same flex line) and the amount TableCell from Task 1 (the
    non-anchor branch this task narrows further for the counterpart sub-case)
    81-CONTEXT.md "Claude's Discretion" section (the two link intents to reconcile: the existing
    ReimbursementRowIndicator manages a reimbursement via /reimbursements/[id]; the new counterpart
    badge must instead point at the anchor TRANSACTION's own detail page — an amortization sale does
    not warrant the /reimbursements deep link the way a reimbursement does, so this task SWAPS the
    title-row badge for counterpart rows rather than showing both side by side)
  </read_first>
  <behavior>
    - PairedReductionBadge({ anchorTransactionId, anchorLabel }) renders a link whose href is
      transactionDetailHref(anchorTransactionId), with visible text containing "riduzione di"
      followed by a truncated form of anchorLabel (falling back to a generic Italian label when
      anchorLabel is null).
    - A counterpart-role row (pairRole === 'counterpart') renders PairedReductionBadge in the
      title-row badge slot INSTEAD of ReimbursementRowIndicator, even though reimbursementId is also
      non-null on that row — the swap is exclusive, never both badges on the same row.
    - An anchor-role row (pairRole === 'anchor') still renders ReimbursementRowIndicator unchanged in
      the title row — this task narrows only the counterpart branch, never the anchor branch.
    - A counterpart-role row's amount cell renders its own gross amount with an attenuated
      (muted/opacity) class instead of the normal inflow tone class.
    - tests/reimbursement-regression.test.ts (LENS-03 byte-identical assertions) still passes
      unmodified — this task never touches lib/dal/transaction-pairs-sql.ts or any aggregation query.
  </behavior>
  <action>
    Create components/transactions/paired-reduction-badge.tsx exporting function
    PairedReductionBadge({ anchorTransactionId, anchorLabel }: { anchorTransactionId: string;
    anchorLabel: string | null }): resolve `const label = anchorLabel?.trim() || 'transazione
    collegata'`; truncate for the badge's own compact width (e.g. slice to ~18 chars + an ellipsis
    when longer, mirroring transactionRowLabel's own slice-and-ellipsis convention already in this
    file); render a Next.js Link with href={transactionDetailHref(anchorTransactionId)} wrapping a
    Badge variant="outline" className="shrink-0 max-w-40 cursor-pointer truncate
    text-muted-foreground hover:bg-muted", aria-label={`Riduzione di ${label}`}, title={`Apri la
    transazione collegata: ${label}`}, with visible text "riduzione di {truncated label}" (D-N3's
    exact locked wording, lowercase). Document inline (English comment) why this is a NEW sibling
    component rather than an extension of ReimbursementRowIndicator: the link target and semantic
    intent differ (anchor transaction vs. reimbursement management page), and reusing one component
    for both would need a branching prop anyway — a plain sibling is simpler and keeps
    ReimbursementRowIndicator's existing call sites/tests untouched.

    In transaction-table.tsx, import PairedReductionBadge. At the title-row badge spot (the
    `{transaction.reimbursementId != null && <ReimbursementRowIndicator .../>}` block), change the
    condition to branch on pairRole first: when `pairRole === 'counterpart' && transaction.pairedWithId`,
    render `<PairedReductionBadge anchorTransactionId={transaction.pairedWithId}
    anchorLabel={transaction.pairedDescription} />`; otherwise (pairRole is 'anchor', null, or the
    pairedWithId guard fails defensively) keep the existing
    `transaction.reimbursementId != null && <ReimbursementRowIndicator reimbursementId={…} />` branch
    completely unchanged.

    In the amount TableCell's non-anchor branch (Task 1's single-span fallback), narrow the
    className: when pairRole === 'counterpart', use `text-muted-foreground opacity-60` (mirrors Task
    1's struck-gross opacity value for visual consistency between the two "de-emphasized" pairing
    states) instead of amountColorClass; every other row (pairRole null) keeps amountColorClass
    exactly as before.

    Extend tests/transaction-table-paired-net-display.test.tsx: a counterpart-role fixture (amount
    '100.00', pairedWithId set to an anchor id, pairedDescription set, reimbursementId set) renders
    the badge text "riduzione di" and a link `href` equal to transactionDetailHref(the anchor id),
    and does NOT render ReimbursementRowIndicator's own aria-label for that same row (confirming the
    swap, not an addition) — assert this by checking the row's rendered fragment lacks that specific
    aria-label string while still containing the new badge's own aria-label. An anchor-role fixture
    from Task 1 still renders ReimbursementRowIndicator's aria-label unchanged (regression guard for
    the non-counterpart branch).

    Close the task with the full verification gate: run `node_modules/.bin/tsc --noEmit` (direct
    binary — RTK intercepts npx per project memory, and this project has no dedicated tsc script),
    `yarn check:language`, and `yarn vitest run tests/reimbursement-regression.test.ts
    tests/transaction-table-paired-net-display.test.tsx` together — confirming the LENS-03
    byte-identical regression assertions and every new render assertion pass in the same run.
  </action>
  <reversibility rating="reversible">D-N3 is render-layer only, swapping which badge component
  renders for a counterpart row — reverting restores ReimbursementRowIndicator unconditionally, no
  data model or write path changes (per 81-CONTEXT.md D-N1..D-N4: "Reversibility: reversible,
  render-layer only").</reversibility>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && yarn check:language && yarn vitest run tests/reimbursement-regression.test.ts tests/transaction-table-paired-net-display.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/transactions/paired-reduction-badge.tsx exports PairedReductionBadge, importing
      transactionDetailHref from lib/routes (grep confirms both identifiers).
    - transaction-table.tsx's title-row conditional references both PairedReductionBadge and
      ReimbursementRowIndicator as mutually exclusive branches (grep confirms both identifiers are
      present in the file).
    - `node_modules/.bin/tsc --noEmit` exits clean.
    - `yarn check:language` passes.
    - `yarn vitest run tests/reimbursement-regression.test.ts tests/transaction-table-paired-net-display.test.tsx`
      passes in full — the LENS-03 byte-identical assertions are unaffected by this phase.
  </acceptance_criteria>
  <done>A counterpart row shows a "riduzione di …" badge linking to its anchor transaction with an
  attenuated amount, replacing the generic reimbursement-management badge on that row only; the
  anchor row's own title-row badge is unchanged; the full type-check, language check, and the
  byte-identical LENS-03 regression suite all stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC transactions page -> transaction-table.tsx (client) | pairedWithId/pairedNetAmount/pairedDescription/reimbursementId are already fetched server-side, userId-scoped by the unmodified transactionListSelect/DAL query — this phase reads no new field and adds no new server round-trip |
| transaction-table.tsx -> PairedReductionBadge Link href | pairedWithId is a same-user-owned transaction id, already resolved server-side by the pre-existing, unmodified pairedCounterpartIdExpr(); rendered into a same-origin internal route href via transactionDetailHref, never an external URL or raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-01 | Information Disclosure | PairedReductionBadge anchor link | low | accept | pairedWithId is already resolved server-side by the pre-existing, unmodified pairedCounterpartIdExpr() (itself scoped by the transaction table's own userId ownership joins); the badge's Link target is a route (transactionDetailHref) the same user could already reach today via the existing pair popover/ReimbursementRowIndicator — this phase adds a second navigation path to an already-reachable page, no new disclosure surface |
| T-81-02 | Tampering | client-side resolvePairRole (sign of transaction.amount) | low | accept | pairRole is a pure display classification with no write effect; a mislabeled badge/strikethrough on a single row is the worst-case outcome — it can never mutate a total, an aggregate, or effectiveAmount() (D-N4 keeps every total untouched, and the sign invariant it reads is itself enforced at write time by assertOutflowAnchorAmount/assertInflowRefundAmount) |
| T-81-03 | Repudiation | none — no new write path | n/a | accept | this phase has no server action, no mutation, and no new audit-relevant event; there is nothing to repudiate |

</threat_model>

<verification>
- `yarn vitest run tests/transaction-table-paired-net-display.test.tsx tests/reimbursement-regression.test.ts` — the new render test plus the byte-identical LENS-03 regression suite, together
- `node_modules/.bin/tsc --noEmit` — typecheck via the direct binary (RTK intercepts npx per project memory; this project has no dedicated tsc script)
- `yarn check:language` — new identifiers/comments stay English; the "riduzione di …" UI copy is Italian by design (product surface) and is exempt as a quoted string, per the checker's own convention
- `yarn vitest run` — full suite, run once at the end of the phase as the overall closing gate (D-N4: every existing test must stay green, not just the two files touched above)
</verification>

<success_criteria>
1. A paired outflow anchor (amortization closed-for-sale OR v2.8 reimbursement) renders in the
   transactions table with pairedNetAmount as the primary figure and the gross amount
   struck-through/opaque beneath it — for all pairing types, one code path (D-N1/D-N2).
2. The counterpart row shows a "riduzione di …" badge that links to its anchor transaction and
   renders its amount attenuated, so it no longer reads as a plain asset/inflow (D-N3).
3. No change to any total, effectiveAmount() result, netting math, or dashboard/lens figure — the
   full test suite, including tests/reimbursement-regression.test.ts's LENS-03 byte-identical
   assertions, stays green (D-N4). lib/dal/transactions.ts is unmodified.
</success_criteria>

<output>
Create `.planning/phases/81-inline-net-display-for-paired-transactions/81-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `components/transactions/transaction-table.tsx` — modified (existing file); adds
  `resolvePairRole`, the split anchor amount-cell render, the counterpart amount attenuation, and
  the title-row badge swap.
- `components/transactions/paired-reduction-badge.tsx` — new file; exports `PairedReductionBadge`.
- `tests/transaction-table-paired-net-display.test.tsx` — new file; render tests covering anchor,
  counterpart, and unpaired rows.
</output>
