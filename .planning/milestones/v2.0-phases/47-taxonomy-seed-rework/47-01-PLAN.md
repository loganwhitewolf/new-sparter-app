---
phase: 47-taxonomy-seed-rework
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/seed-taxonomy.test.ts
  - tests/fixtures/v2-taxonomy-manifest.ts
autonomous: true
requirements: [TAX-01]
tags: [seed, taxonomy, vitest, wave-0, tdd-scaffold]

must_haves:
  truths:
    - "Vitest contract test file exists and imports categories/subCategories/natures from seed-data"
    - "Tests assert 23 active categories, every sub has natureId 1-8, and dissolved wrapper slugs are absent from active set"
    - "Running yarn test tests/seed-taxonomy.test.ts fails RED before seed-data v2 lands (Wave 1+)"
  artifacts:
    - path: "tests/seed-taxonomy.test.ts"
      provides: "Static TAX-01/02 contract gate for seed-data v2"
      contains: "seed taxonomy contract"
    - path: "tests/fixtures/v2-taxonomy-manifest.ts"
      provides: "Explicit slug manifest from nature-remapping-WORKING.md"
      contains: "V2_CATEGORY_SLUGS"
  key_links:
    - from: "tests/seed-taxonomy.test.ts"
      to: "scripts/seed-data.ts"
      via: "import { categories, subCategories, natures }"
      pattern: "from '../scripts/seed-data'"
---

<objective>
Creare lo scaffold Vitest RED per il contratto tassonomia v2 (TAX-01/TAX-02) prima di modificare `seed-data.ts`.

Purpose: Wave 0 Nyquist — ogni piano successivo ha un gate automatico (`yarn test tests/seed-taxonomy.test.ts`). Il manifest esplicito di slug deriva da `.planning/nature-remapping-WORKING.md` (D-01), non dal round number "~65".

Output: `tests/seed-taxonomy.test.ts` + `tests/fixtures/v2-taxonomy-manifest.ts` (RED fino a Plan 02).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md
@.planning/phases/47-taxonomy-seed-rework/47-RESEARCH.md
@.planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md
@.planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md
@.planning/nature-remapping-WORKING.md
@scripts/seed-data.ts
@tests/import-detector.test.ts
@tests/nature-labels.test.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

- **`tests/fixtures/v2-taxonomy-manifest.ts`** — manifest esplicito (D-01):
  - `V2_CATEGORY_SLUGS`: 23 slug attivi (4 IN + 16 OUT + 2 ALLOCATION + 1 TRANSFER) da working doc sezioni IN/OUT/ALLOCATION/TRANSFER
  - `V2_SUBCATEGORY_MANIFEST`: array `{ slug, natureCode }` per ogni sottocategoria finale (conteggio derivato, non gated su "~65")
  - `DISSOLVED_CATEGORY_SLUGS`: wrapper da asserire assenti/inattivi (`abbonamenti`, `assicurazioni`, `famiglia`, `libri-e-media`, `tempo-libero`, `sconti-rimborsi-e-cashback`, `vendite-e-dismissioni`, `movimenti-di-liquidita`, `bonifici-e-rimborsi`)
  - `DROPPED_SUBCATEGORY_SLUGS`: slug pruned (overtime, rimborso-*, store splits, ecc.) — assenti da active set
  - `NATURE_ID_BY_CODE`: map 1-8 allineata a `natures` array Phase 46 (non modificare lookup blocks)

- **`tests/seed-taxonomy.test.ts`** — describe `seed taxonomy contract (TAX-01/02/03)`:
  - 23 categorie attive
  - ogni sub in `subCategories` ha `natureId` 1-8
  - manifest slug/nature match (TAX-01)
  - dissolved cats assenti da active set (TAX-02 baseline)
  - commento: test RED fino a wholesale replace Plan 02
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Author v2 taxonomy slug manifest fixture from working doc</name>
  <read_first>
    - .planning/nature-remapping-WORKING.md (sezioni IN/OUT/ALLOCATION/TRANSFER — FINAL remap bullets)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-01, D-02, D-03)
    - scripts/seed-data.ts (natures array L1579+ — IDs 1-8 codes only; DO NOT edit directions/natures blocks per D-04)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (nature ID map table)
  </read_first>
  <action>
    Create `tests/fixtures/v2-taxonomy-manifest.ts` exporting typed constants sourced ONLY from `.planning/nature-remapping-WORKING.md` (D-01 — do not re-derive merges).

    `V2_CATEGORY_SLUGS` — exactly 23 Italian slug strings for active system categories:
    IN (4): income-da-lavoro, pensioni-e-sussidi, rendite, entrate-straordinarie
    OUT (16): trasporti, spesa, salute, utenze, casa, imposte-e-oneri, servizi-professionali, formazione, vacanze, regali-e-donazioni, ristorazione, shopping, cultura-e-tempo-libero, benessere, animali, rate-e-finanziamenti
    ALLOCATION (2): risparmio, investimenti
    TRANSFER (1): trasferimenti

    `V2_SUBCATEGORY_MANIFEST` — one entry per final subcategory bullet in working doc with `{ slug: string; natureCode: 'income' | 'income_extraordinary' | 'essential' | 'discretionary' | 'debt' | 'transfer' | 'savings' | 'investment' }`. Include all IN (16), OUT (~52), ALLOCATION (9), TRANSFER (3) slugs from working doc lines 59-159. Use merged slug names (e.g. `spesa-quotidiana`, `dividendi`, `trasferimento-tra-conti`).

    `DISSOLVED_CATEGORY_SLUGS` — v1 wrapper cats to assert inactive/absent in v2 baseline (D-02): abbonamenti, assicurazioni, famiglia, libri-e-media, tempo-libero, sconti-rimborsi-e-cashback, vendite-e-dismissioni, movimenti-di-liquidita, bonifici-e-rimborsi

    `DROPPED_SUBCATEGORY_SLUGS` — pruned slugs (D-03): overtime, all rimborso-* variants listed in working doc, store-type splits (discount, negozio-di-quartiere, mercato-rionale, prodotti-freschi, prodotti-non-alimentari), altri-abbonamenti, spesa-online, bonifico-in-uscita, bonifico-in-entrata, ricariche-conti (folded into transfer), etc.

    `NATURE_ID_BY_CODE` — static map matching seed-data natures ids 1-8 (income=1, income_extraordinary=2, essential=3, discretionary=4, debt=5, transfer=6, savings=7, investment=8). Developer comments in English; slug strings Italian.
  </action>
  <verify>
    <automated>test -f tests/fixtures/v2-taxonomy-manifest.ts && grep -q 'V2_CATEGORY_SLUGS' tests/fixtures/v2-taxonomy-manifest.ts && grep -q 'V2_SUBCATEGORY_MANIFEST' tests/fixtures/v2-taxonomy-manifest.ts && node --experimental-strip-types -e "import('./tests/fixtures/v2-taxonomy-manifest.ts').then(m=>{if(m.V2_CATEGORY_SLUGS.length!==23)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/fixtures/v2-taxonomy-manifest.ts
    - V2_CATEGORY_SLUGS has length 23
    - V2_SUBCATEGORY_MANIFEST has one entry per working-doc final subcategory slug with natureCode
    - DISSOLVED_CATEGORY_SLUGS and DROPPED_SUBCATEGORY_SLUGS populated from D-02/D-03
    - NATURE_ID_BY_CODE maps all 8 nature codes to integers 1-8
    - No edits to scripts/seed-data.ts in this task
  </acceptance_criteria>
  <done>Explicit v2 slug manifest fixture exists as single source for test assertions (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Add RED seed-taxonomy contract tests (Wave 0)</name>
  <read_first>
    - tests/fixtures/v2-taxonomy-manifest.ts (from Task 1)
    - tests/import-detector.test.ts (import pattern from seed-data)
    - tests/nature-labels.test.ts (it.each static assertions)
    - scripts/seed-data.ts (current exports — expect FAIL until Plan 02)
    - .planning/phases/47-taxonomy-seed-rework/47-VALIDATION.md (Wave 0 gate)
  </read_first>
  <action>
    Create `tests/seed-taxonomy.test.ts` importing `{ categories, subCategories, natures }` from `../scripts/seed-data` and manifest from `./fixtures/v2-taxonomy-manifest`.

    Tests (all must FAIL RED against current v1 baseline):
    1. `has 23 active system categories` — filter isActive !== false, length 23 (TAX-01)
    2. `every system subcategory has natureId 1-8` — loop subCategories; assert natureId defined and maps to valid natures[].id (TAX-01, D-11)
    3. `active category slugs match v2 manifest` — active slugs equal set of V2_CATEGORY_SLUGS (TAX-01)
    4. `subcategory slugs and nature codes match manifest` — for each V2_SUBCATEGORY_MANIFEST entry, find sub by slug, assert natureId matches NATURE_ID_BY_CODE[natureCode] (TAX-01)
    5. `dissolved wrapper categories are not active` — none of DISSOLVED_CATEGORY_SLUGS appear in active category slugs (TAX-02 baseline)
    6. `dropped subcategory slugs are not active` — none of DROPPED_SUBCATEGORY_SLUGS in active sub slugs (D-03)

    Use vitest describe/it/expect. Developer comments English. Do NOT modify seed-data.ts in this task.
  </action>
  <verify>
    <automated>yarn test tests/seed-taxonomy.test.ts 2>&1 | tee /tmp/seed-taxonomy-red.log; grep -qE 'FAIL|failed|AssertionError' /tmp/seed-taxonomy-red.log && test -f tests/seed-taxonomy.test.ts && echo PASS_RED</automated>
  </verify>
  <acceptance_criteria>
    - tests/seed-taxonomy.test.ts exists with at least 6 test cases covering TAX-01 and TAX-02 baseline assertions
    - yarn test tests/seed-taxonomy.test.ts exits non-zero (RED) against current seed-data
    - Tests import manifest fixture, not hardcoded duplicate slug lists in test file body
    - scripts/seed-data.ts unchanged
  </acceptance_criteria>
  <done>Wave 0 RED scaffold ready; Plan 02 turns tests GREEN (per D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only plan; no DB, no operator scripts executed, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Integrity | v2-taxonomy-manifest | mitigate | Manifest copied verbatim from locked working doc (D-01); reviewed in Plan 02 implementation |

T-47-SC: N/A — no package installs.
</threat_model>

<verification>
`yarn test tests/seed-taxonomy.test.ts` runs and fails RED. Manifest fixture present. No seed-data edits.
</verification>

<success_criteria>
- tests/fixtures/v2-taxonomy-manifest.ts and tests/seed-taxonomy.test.ts exist
- Contract tests fail against v1 baseline (expected RED)
- TAX-01 test scaffold covers category count, natureId, slug manifest
</success_criteria>

<output>
Create `.planning/phases/47-taxonomy-seed-rework/47-01-SUMMARY.md` when done.
</output>
