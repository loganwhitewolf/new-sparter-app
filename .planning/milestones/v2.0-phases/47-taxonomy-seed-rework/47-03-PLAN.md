---
phase: 47-taxonomy-seed-rework
plan: 03
type: execute
wave: 2
depends_on: ["47-02"]
files_modified:
  - scripts/seed-data.ts
  - scripts/seed.ts
autonomous: true
requirements: [TAX-01, TAX-02]
tags: [seed, patterns, sign-agnostic, excludeFromTotals]

must_haves:
  truths:
    - "categorizationPatterns retargeted to v2 subcategory slugs without amountSign"
    - "seed.ts passes natureId through subCategory insert"
    - "excludeFromTotals updated for v2 TRANSFER slugs"
    - "Missing-slug guard passes for all pattern subCategorySlug values"
  artifacts:
    - path: "scripts/seed-data.ts"
      provides: "sign-agnostic categorizationPatterns"
    - path: "scripts/seed.ts"
      provides: "natureId pass-through + v2 excludeFromTotals slugs"
  key_links:
    - from: "seed.ts subCategory insert"
      to: "seed-data subCategories[].natureId"
      via: "inferInsert cast"
      pattern: "db.insert(subCategory)"
---

<objective>
Aggiornare `categorizationPatterns` per slug v2 sign-agnostic (D-10) e collegare `seed.ts` al pass-through `natureId` + slugs TRANSFER per `excludeFromTotals` (D-13).

Purpose: TAX-01 pattern targets allineati; TAX-02 merge slug retarget in baseline. Fresh install dopo Phase 48 avrà pattern + natureId corretti.

Output: patterns + seed.ts wiring compile; taxonomy tests still GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-taxonomy-seed-rework/47-02-SUMMARY.md
@.planning/phases/47-taxonomy-seed-rework/47-RESEARCH.md (pattern slug retarget map)
@.planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md
@scripts/seed-data.ts
@scripts/seed.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

- **`scripts/seed-data.ts` — `categorizationPatterns`:**
  - Remove `AmountSign` type export and all `amountSign` fields from pattern literals (D-10)
  - Retarget `subCategorySlug` per RESEARCH.md pattern map (e.g. supermercato→spesa-quotidiana, cene-fuori→ristoranti, bonifico-*→trasferimento-tra-conti)
  - Single bonifico pattern row for `trasferimento-tra-conti` (dedupe IN+OUT — Pitfall 3)
  - Deactivate or drop patterns targeting pruned slugs (negozio-di-quartiere, treno, altri-abbonamenti)

- **`scripts/seed.ts`:**
  - Confirm `natureId` passes through `db.insert(subCategory).values(subCategories as ...)` (Plan 02 shape)
  - Update `excludeFromTotals` slug list to v2 TRANSFER subs: `trasferimento-tra-conti`, `addebito-carta-di-credito`, `contante` (replace ricariche-conti/prelievo-contante slugs)
  - Missing-slug guard must pass for all pattern subCategorySlug values
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Retarget categorizationPatterns to v2 slugs (sign-agnostic)</name>
  <read_first>
    - scripts/seed-data.ts (categorizationPatterns L1271+, AmountSign type L1269)
    - .planning/phases/47-taxonomy-seed-rework/47-RESEARCH.md (Pattern slug → v2 retarget map table)
    - scripts/seed.ts (pattern insert L142-150 — fields consumed)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-10)
  </read_first>
  <action>
    Update `export const categorizationPatterns` in seed-data.ts per D-10 and RESEARCH pattern map.

    Remove `export type AmountSign` and every `amountSign` property from pattern objects. Each pattern retains: pattern, subCategorySlug, confidence, priority, description.

    Retarget subCategorySlug to v2 slugs that exist in Plan 02 subCategories. Key merges: supermercato→spesa-quotidiana, cene-fuori→ristoranti, colazioni-e-snack→bar-caffe-e-snack, carburante→carburante-e-ricarica, streaming-*→streaming, farmaci-generici→farmaci, sport→sport-e-fitness, servizi-telefonici-e-internet→telefono-e-internet, bonifico-in-uscita+bonifico-in-entrata→single pattern on trasferimento-tra-conti, commissioni-bancarie→commissioni-e-canone-conto, take-away→take-away-e-delivery, pedaggi-autostradali→pedaggi-e-parcheggi.

    Remove patterns for dropped slugs or retarget to nearest v2 catch-all per research notes.

    Ensure no duplicate (pattern regex, subCategorySlug) pairs that would violate unique (pattern, subCategoryId) after insert — especially bonifico dedupe.

    Do NOT modify categories/subCategories/directions/natures arrays in this task unless a slug typo blocks pattern resolution.
  </action>
  <verify>
    <automated>grep -v '^#' scripts/seed-data.ts | grep -c 'amountSign' | xargs test 0 -eq && yarn test tests/seed-taxonomy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No amountSign / AmountSign in seed-data.ts (filtered grep count 0)
    - Every categorizationPatterns[].subCategorySlug exists in subCategories slugs
    - At most one bonifico-related pattern targeting trasferimento-tra-conti
    - yarn test tests/seed-taxonomy.test.ts still GREEN
  </acceptance_criteria>
  <done>Sign-agnostic patterns retargeted to v2 slugs (D-10, TAX-02 pattern aspect).</done>
</task>

<task type="auto">
  <name>Task 2: Wire seed.ts natureId pass-through and v2 excludeFromTotals slugs</name>
  <read_first>
    - scripts/seed.ts (subCategory insert L89-91, excludeFromTotals L93-97, pattern guard L125-137)
    - scripts/seed-data.ts (subCategories with natureId from Plan 02)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (excludeFromTotals slug list)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-05 no DB apply, D-13)
  </read_first>
  <action>
    Verify/update scripts/seed.ts for v2 wiring:

    1. Subcategory insert: ensure `subCategories as Array<typeof subCategory.$inferInsert>` includes natureId from literals — add English comment if pass-through is implicit. No map-out of natureId.

    2. Replace excludeFromTotals slug array (L93-97) with v2 TRANSFER subs per D-13: `trasferimento-tra-conti`, `addebito-carta-di-credito`, `contante`. Remove obsolete slugs `ricariche-conti`, old `trasferimento` if present.

    3. Confirm missing-slug guard runs against updated categorizationPatterns — logic unchanged; must not throw when run against in-memory slug set (static check: every pattern subCategorySlug in subCategories export).

    Do NOT run yarn db:seed (D-05). Do NOT modify directions/natures insert blocks (D-04).

    Run yarn test tests/seed-taxonomy.test.ts and yarn tsc --noEmit (or yarn build if faster in env).
  </action>
  <verify>
    <automated>grep -q 'trasferimento-tra-conti' scripts/seed.ts && grep -q 'contante' scripts/seed.ts && node --experimental-strip-types -e "import { subCategories, categorizationPatterns } from './scripts/seed-data.ts'; const slugs=new Set(subCategories.map(s=>s.slug)); const missing=categorizationPatterns.map(p=>p.subCategorySlug).filter(s=>!slugs.has(s)); if(missing.length) { console.error(missing); process.exit(1); }" && yarn test tests/seed-taxonomy.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - excludeFromTotals uses v2 transfer slug triple
    - All pattern subCategorySlug values resolve in subCategories
    - seed.ts compiles; no edits to directions/natures blocks
    - No db:seed / migrate executed (D-05)
  </acceptance_criteria>
  <done>seed.ts wired for natureId + v2 transfer excludeFromTotals (D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator script | seed.ts runs only when operator invokes db:seed — not in Phase 47 gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Integrity | pattern→subCategorySlug | mitigate | Static missing-slug check in verify |

T-47-SC: N/A
</threat_model>

<verification>
yarn test tests/seed-taxonomy.test.ts GREEN. Pattern slugs resolve. amountSign absent. tsc clean on seed scripts.
</verification>

<success_criteria>
- Sign-agnostic patterns target v2 slugs
- seed.ts excludeFromTotals + natureId pass-through correct
- TAX-01/TAX-02 pattern baseline satisfied
</success_criteria>

<output>
Create `.planning/phases/47-taxonomy-seed-rework/47-03-SUMMARY.md` when done.
</output>
