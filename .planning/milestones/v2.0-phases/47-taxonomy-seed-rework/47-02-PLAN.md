---
phase: 47-taxonomy-seed-rework
plan: 02
type: execute
wave: 1
depends_on: ["47-01"]
files_modified:
  - scripts/seed-data.ts
autonomous: true
requirements: [TAX-01, TAX-02]
tags: [seed-data, taxonomy, natureId, wholesale-replace]

must_haves:
  truths:
    - "seed-data.ts exports 23 active categories matching working doc"
    - "seed-data.ts exports ~65-80 subcategories each with natureId 1-8"
    - "Dissolved wrapper categories absent or isActive false in baseline"
    - "yarn test tests/seed-taxonomy.test.ts passes GREEN"
  artifacts:
    - path: "scripts/seed-data.ts"
      provides: "v2 categories + subCategories wholesale baseline"
      contains: "natureId"
  key_links:
    - from: "scripts/seed-data.ts subCategories[].natureId"
      to: "scripts/seed-data.ts natures[].id"
      via: "explicit integer FK 1-8"
      pattern: "natureId:"
---

<objective>
Sostituire wholesale gli array `categories` e `subCategories` in `seed-data.ts` con la tassonomia v2 certificata (D-07), aggiungendo `natureId` su ogni sottocategoria di sistema (D-11).

Purpose: TAX-01 fresh-install baseline + TAX-02 dissolutions/renames nel literal seed. Non toccare blocchi `directions`/`natures`/`platforms`/`categorizationPatterns` (Plan 03).

Output: `categories` (23 attive) + `subCategories` (manifest completo) GREEN vs Wave 0 tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-taxonomy-seed-rework/47-01-SUMMARY.md
@.planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md
@.planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md
@.planning/nature-remapping-WORKING.md
@tests/fixtures/v2-taxonomy-manifest.ts
@tests/seed-taxonomy.test.ts
@scripts/seed-data.ts
</context>

<artifacts_produced>
## Artifacts this phase produces

- **`scripts/seed-data.ts` — `categories` array (wholesale replace, D-07):**
  - 23 active system categories per manifest; reuse explicit `id` where row survives v1→v2 (research A3); new IDs for 🆕 cats (`pensioni-e-sussidi`, `servizi-professionali`, `animali`, `cultura-e-tempo-libero`, `entrate-straordinarie` if new slug)
  - Renamed cats: `bollette-e-utilita`→`utenze`, `income-finanziari`→`rendite`, `tasse-imposte-e-commissioni`→`imposte-e-oneri`, `regali`→`regali-e-donazioni`, `ignore`/cat 32→`trasferimenti`
  - Dissolved wrappers: omit from active set OR `isActive: false` (planner discretion — tests assert not in active slugs per D-02)
  - Keep legacy `type` field on literals for human reference (stripped at insert in seed.ts per Phase 46 pattern)

- **`scripts/seed-data.ts` — `subCategories` array (wholesale replace):**
  - Every entry includes `natureId: 1..8` per working doc nature assignment (D-11)
  - Italian `name`/`slug`; merged subs per working doc (e.g. `spesa-quotidiana`, `dividendi`, `farmaci`, `streaming`)
  - Transfer subs: `trasferimento-tra-conti`, `addebito-carta-di-credito`, `contante` with `natureId: 6` (D-13)
  - No `excludeFromTotals` in literals — set post-insert in seed.ts Plan 03

**NOT modified:** `export const directions`, `export const natures`, `categorizationPatterns`, `platforms`, `AmountSign` type (Plan 03).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Replace categories array with v2 23-category baseline</name>
  <read_first>
    - .planning/nature-remapping-WORKING.md (category lists per direction; dissolutions D-02)
    - tests/fixtures/v2-taxonomy-manifest.ts (V2_CATEGORY_SLUGS, DISSOLVED_CATEGORY_SLUGS)
    - scripts/seed-data.ts (current categories L3-220 — id reuse pattern, type field)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-01, D-02, D-04, D-07)
    - .planning/phases/47-taxonomy-seed-rework/47-RESEARCH.md (category ID reuse table)
  </read_first>
  <action>
    Replace the entire `export const categories = [...]` block in `scripts/seed-data.ts` with v2 taxonomy (D-07 wholesale replace).

    Author exactly 23 active categories matching V2_CATEGORY_SLUGS from manifest. For each category object keep shape: `{ id, name, slug, type, displayOrder, isActive }` matching existing literal idiom.

    Direction assignment via legacy `type` field (human reference only, stripped at insert): IN cats `type: "in"`, OUT `type: "out"`, ALLOCATION `type: "out"` or document as allocation in comment, TRANSFER cat `type: "system"` or equivalent legacy — match surrounding convention.

    Reuse explicit numeric IDs where semantic row survives (e.g. id 1 risparmio, 12 investimenti, 7 trasporti, 32 trasferimenti). Assign new IDs for net-new categories without colliding with deactivated v1 IDs (gaps 6,16,17,20,29-31 usable).

    Dissolved v1 wrapper categories (D-02): do NOT include in active 23-count set. Either omit entirely from array or include with `isActive: false` for deployed-DB reference — must pass test `dissolved wrapper categories are not active`.

    Do NOT modify `directions`, `natures`, `subCategories`, or `categorizationPatterns` in this task.
  </action>
  <verify>
    <automated>yarn test tests/seed-taxonomy.test.ts -t "23 active"</automated>
  </verify>
  <acceptance_criteria>
    - categories array replaced with v2 content; exactly 23 entries with isActive !== false
    - All V2_CATEGORY_SLUGS present in active category slugs
    - No DISSOLVED_CATEGORY_SLUGS in active set (TAX-02)
    - directions/natures blocks untouched (D-04)
    - Legacy type field retained on category literals
  </acceptance_criteria>
  <done>23 active v2 categories authored in seed-data.ts (D-07, TAX-01).</done>
</task>

<task type="auto">
  <name>Task 2: Replace subCategories array with v2 manifest + natureId on every sub</name>
  <read_first>
    - .planning/nature-remapping-WORKING.md (every sub bullet with nature code)
    - tests/fixtures/v2-taxonomy-manifest.ts (V2_SUBCATEGORY_MANIFEST, NATURE_ID_BY_CODE, DROPPED_SUBCATEGORY_SLUGS)
    - scripts/seed-data.ts (current subCategories shape; natures id map L1579+)
    - .planning/phases/47-taxonomy-seed-rework/47-PATTERNS.md (natureId idiom D-11)
    - .planning/phases/47-taxonomy-seed-rework/47-CONTEXT.md (D-03 pruning, D-11, D-13)
  </read_first>
  <action>
    Replace entire `export const subCategories = [...]` with v2 entries sourced from V2_SUBCATEGORY_MANIFEST / working doc (D-07).

    Each subcategory object: `{ categoryId, name, slug, natureId, displayOrder, isActive: true }`. Use explicit `natureId` integers 1-8 matching NATURE_ID_BY_CODE / natures array (D-11) — never omit natureId on system subs.

    Map categoryId to the v2 category ids authored in Task 1. Italian names/slugs per working doc.

    Include all final merged subs; exclude DROPPED_SUBCATEGORY_SLUGS from active set (D-03). Transfer subs get natureId 6 (D-13).

    Run `yarn test tests/seed-taxonomy.test.ts` — must pass all manifest tests (GREEN). If count drift vs "~65", tests use manifest length not summary round number (research A1).

    Do NOT edit categorizationPatterns, directions, natures, platforms.
  </action>
  <verify>
    <automated>yarn test tests/seed-taxonomy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every subCategories entry has natureId 1-8
    - All V2_SUBCATEGORY_MANIFEST slugs present with correct natureId
    - DROPPED_SUBCATEGORY_SLUGS not in active sub slug set
    - yarn test tests/seed-taxonomy.test.ts exits 0 (GREEN)
    - directions/natures/categorizationPatterns unchanged
  </acceptance_criteria>
  <done>v2 subCategories with natureId complete; Wave 0 contract tests GREEN (TAX-01, TAX-02 baseline).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static seed literals only; no DB apply (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02 | Integrity | natureId FK literals | mitigate | Tests assert every natureId resolves to natures[].id; manifest cross-check |

T-47-SC: N/A
</threat_model>

<verification>
yarn test tests/seed-taxonomy.test.ts GREEN. No directions/natures edits. No DB commands.
</verification>

<success_criteria>
- 23 active categories + full subcategory manifest with natureId 1-8
- TAX-01 and TAX-02 baseline assertions pass
- D-07 wholesale replace complete for cat/sub only
</success_criteria>

<output>
Create `.planning/phases/47-taxonomy-seed-rework/47-02-SUMMARY.md` when done.
</output>
