---
phase: 49-dashboard-and-surfaces
plan: 04
type: execute
wave: 2
depends_on: ["49-02"]
files_modified:
  - app/globals.css
  - components/dashboard/overview/kpi-card-reading.tsx
  - components/dashboard/overview/kpi-row.tsx
  - components/dashboard/overview/overview-chart.tsx
  - components/dashboard/overview/overview-chart-filters.tsx
  - components/dashboard/overview/overview-movers-section.tsx
  - components/dashboard/overview/overview-movers-panel.tsx
  - app/(app)/dashboard/overview/page.tsx
autonomous: false
requirements: [DASH-01, DASH-03, DASH-04]
must_haves:
  truths:
    - "The overview chart shows a 3rd 'Accantonato' grouped bar per month, always rendered (even at zero), in purple --total-allocation"
    - "Clicking a bar selects month AND direction; Entrate→IN movers, Uscite→OUT movers, Accantonato→allocation movers (per-nature)"
    - "A 5th KPI card 'Accantonato' shows totalAllocation with YTD-vs-prior delta and a sentiment reading line"
    - "Allocation movers render per nature (Risparmio / Investimento) with signed Δ€; empty month shows 'Nessun accantonamento in {mese}.'"
    - "Transfer is never shown on the dashboard chart, KPIs, or movers"
  artifacts:
    - path: "components/dashboard/overview/overview-chart.tsx"
      provides: "3-bar grouped chart with direction-aware onMonthSelect(index, direction)"
      contains: "accantonato"
    - path: "components/dashboard/overview/kpi-row.tsx"
      provides: "5th Accantonato ReadingKpiCard; grid-cols-5"
      contains: "Accantonato"
    - path: "app/globals.css"
      provides: "--total-allocation / --total-transfer CSS tokens"
      contains: "--total-allocation"
  key_links:
    - from: "components/dashboard/overview/overview-movers-section.tsx"
      to: "lib/actions/overview.ts fetchMovers"
      via: "fetchMovers(year, monthIndex, selectedDirection)"
      pattern: "fetchMovers\\("
    - from: "components/dashboard/overview/kpi-row.tsx"
      to: "lib/dal/dashboard.ts OverviewData.totalAllocation"
      via: "data.totalAllocation read into 5th card"
      pattern: "totalAllocation"
    - from: "app/(app)/dashboard/overview/page.tsx"
      to: "lib/dal/overview.ts getMonthOverMonthCategoryChanges"
      via: "initial movers fetch defaults to 'out' direction"
      pattern: "getMonthOverMonthCategoryChanges"
---

<objective>
Build the 4-direction dashboard presentation on top of the Plan 02 data layer (DASH-01, DASH-03, DASH-04; decisions D-01 through D-06). Extend `OverviewChart` to a 3-bar grouped layout with the always-present "Accantonato" bar (D-01) and direction-aware click (D-02); add the 5th "Accantonato" KPI card (D-05); make `OverviewMoversSection`/`OverviewMoversPanel` direction-aware with per-nature allocation movers (D-02, D-03, D-04); add the allocation nature-filter group; and add the `--total-allocation` / `--total-transfer` CSS tokens. Transfer stays hidden everywhere on the dashboard.

Purpose: the data is correct after Plan 02, but the user cannot see allocation until the chart, KPI row, and movers surface it. This plan delivers the visible 4-direction view the phase goal demands.
Output: extended overview components + globals.css + the overview page wiring; a human-verify checkpoint confirms the visual contract.
</objective>

<artifacts_produced>
## Artifacts this phase produces (declared in this plan)

NEW symbols/UI created by this plan (drift verification: treat as newly created):

- `--total-allocation` and `--total-transfer` CSS custom properties in `app/globals.css` (light + dark blocks)
- Third Recharts `<Bar dataKey="accantonato">` in `OverviewChart`; `chartConfig.accantonato` entry
- `onMonthSelect(monthIndex, direction)` signature (direction param NEW) on `OverviewChart` and `handleMonthSelect` in `OverviewMoversSection`
- `selectedDirection` state in `OverviewMoversSection`
- 5th `<ReadingKpiCard label="Accantonato" tone="allocation">` in `KpiRow`; `allocationReading()` helper
- `'allocation'` added to the `Tone` union in `kpi-card-reading.tsx`; `text-[var(--total-allocation)]` value color
- `direction` prop on `OverviewMoversPanel`; per-direction heading + per-nature allocation rows + allocation empty state
- Accantonamento nature-filter group (Risparmio / Investimento) in `OverviewChartFilters`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dashboard-and-surfaces/49-UI-SPEC.md
@.planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md
@.planning/phases/49-dashboard-and-surfaces/49-CONTEXT.md
@.planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md
@CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add allocation CSS tokens + extend chart to 3 bars with direction-aware click</name>
  <files>app/globals.css, components/dashboard/overview/overview-chart.tsx, components/dashboard/overview/overview-chart-filters.tsx</files>
  <read_first>
    - app/globals.css (existing --total-in / --total-out / --balance token blocks in :root and .dark — mirror their structure)
    - components/dashboard/overview/overview-chart.tsx (Recharts Bar/BarChart/ChartContainer pattern lines 1-26, chartConfig lines 23-26, NatureTooltip, the LabelList compact-format, fillOpacity selected/unselected logic, onMonthSelect handler)
    - components/dashboard/overview/overview-chart-filters.tsx (existing Entrate/Uscite nature filter groups — the chip group pattern to mirror for Accantonamento)
    - components/dashboard/overview/overview-chart-utils.ts (deriveFilteredBarRow / deriveNatureBreakdown — must consume the new OverviewChartPoint.allocation bucket)
    - lib/dal/overview.ts (OverviewChartPoint after Plan 02 — the allocation bucket shape this chart renders)
    - .planning/phases/49-dashboard-and-surfaces/49-UI-SPEC.md (Chart Contract: bar order, zero-bar always rendered, fillOpacity, tooltip allocation section, onMonthSelect signature; Color section new tokens; Copywriting Contract chart labels)
    - .planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md (overview-chart.tsx section: chartConfig + NatureTooltip + direction-aware click)
  </read_first>
  <action>
    (1) In `app/globals.css`, add `--total-allocation` (light + dark: `#a78bfa`, the seeded `direction.color` for allocation) and `--total-transfer` (`#94a3b8`) alongside the existing `--total-in`/`--total-out` tokens, mirroring the existing `:root` and `.dark` block structure. Source the values from the seeded `direction.color` (already `#a78bfa` / `#94a3b8` in seed-data).
    (2) In `overview-chart.tsx`: add a 3rd `<Bar dataKey="accantonato">` after entrate/uscite, fill `var(--total-allocation)`, always rendered even at zero-height (do NOT conditionally hide on empty months — D-01); add `accantonato: { label: 'Accantonato', color: 'var(--total-allocation)' }` to `chartConfig`. Apply the same selected/unselected `fillOpacity` (1.0 / 0.4) and the compact-k LabelList as the existing bars. Extend `NatureTooltip` with an Accantonamenti section (Risparmio + Investimento rows) mirroring the existing income/out sections. Change `onMonthSelect` to `(monthIndex: number, direction: 'in' | 'out' | 'allocation')`: each bar's onClick passes its mapped direction (entrate→'in', uscite→'out', accantonato→'allocation'). A zero-height allocation bar is still clickable (cursor pointer) per D-04. Update `overview-chart-utils.ts` derive functions to read the new `allocation` bucket. Do NOT reintroduce per-nature OUT stacking — allocation is a separate grouped bar, not a stack on Uscite (CONTEXT.md specifics).
    (3) In `overview-chart-filters.tsx`: add an "Accantonamento" nature-filter group with Risparmio + Investimento chips, mirroring the existing Entrate/Uscite chip-group pattern. Labels from `nature.label_it` (Risparmio / the UI-SPEC permits "Investimento" override for clarity).
    Run `yarn check:language` after — Italian copy is intentional product surface; ensure no developer-facing strings leaked.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -15 && grep -c 'total-allocation' app/globals.css && grep -c 'accantonato' components/dashboard/overview/overview-chart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` declares `--total-allocation` in both the light (`:root`) and dark (`.dark`) blocks
    - `overview-chart.tsx` contains a Bar with `dataKey="accantonato"` and `chartConfig.accantonato`
    - `overview-chart.tsx` `onMonthSelect` (or equivalent prop) carries a `direction` argument
    - Behavior: the allocation bar renders at zero height on an empty month and is still clickable (verified at the human-verify checkpoint)
    - `yarn build` exits 0; `yarn check:language` clean
  </acceptance_criteria>
  <done>The chart shows a 3rd always-present allocation bar with direction-aware click; allocation nature filter group present; CSS tokens added.</done>
</task>

<task type="auto">
  <name>Task 2: 5th KPI card + direction-aware movers section/panel + page wiring</name>
  <files>components/dashboard/overview/kpi-card-reading.tsx, components/dashboard/overview/kpi-row.tsx, components/dashboard/overview/overview-movers-section.tsx, components/dashboard/overview/overview-movers-panel.tsx, app/(app)/dashboard/overview/page.tsx</files>
  <read_first>
    - components/dashboard/overview/kpi-card-reading.tsx (Tone union line 14, valueColor switch lines 22-28)
    - components/dashboard/overview/kpi-row.tsx (4-card grid lines 49-96, savingsReading/balanceReading helper shapes, ReadingKpiCard usage)
    - components/dashboard/overview/overview-movers-section.tsx (shared-state architecture lines 1-59: useState selectedMonth/movers, useTransition, handleMonthSelect calling fetchMovers)
    - components/dashboard/overview/overview-movers-panel.tsx (heading lines 38-53, empty-state line 59 block, splitMovers/takeTopMovers, list item layout)
    - app/(app)/dashboard/overview/page.tsx (lines 51-78: getOverview/getOverviewChart Promise.all, getMonthOverMonthCategoryChanges initial movers, KpiRow + OverviewMoversSection wiring)
    - lib/dal/dashboard.ts (OverviewData.totalAllocation + deltas.totalAllocation after Plan 02)
    - lib/actions/overview.ts (fetchMovers direction param after Plan 02)
    - lib/dal/overview.ts (MonthOverMonthChange.natureCode after Plan 02)
    - .planning/phases/49-dashboard-and-surfaces/49-UI-SPEC.md (Component Inventory KpiRow/ReadingKpiCard/Movers; Allocation movers panel layout; Copywriting Contract — exact Italian strings for KPI label/readings, movers headings, empty states; Color sentiment rules)
    - .planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md (kpi-row.tsx allocationReading helper, kpi-card-reading.tsx Tone extension, overview-movers-section.tsx selectedDirection state, overview-movers-panel.tsx direction-aware heading + allocation grain)
  </read_first>
  <action>
    (1) `kpi-card-reading.tsx`: add `'allocation'` to the `Tone` union; in the `valueColor` switch add `if (tone === 'allocation') return 'text-[var(--total-allocation)]'`.
    (2) `kpi-row.tsx`: change the grid to `grid-cols-2 md:grid-cols-5`; add a 5th `<ReadingKpiCard label="Accantonato" value={formatEur(data.totalAllocation)} tone="allocation" delta={data.deltas.totalAllocation} goodWhenPositive={true} prevYear={prevYear} reading={allocationReading(data.deltas.totalAllocation)} className="min-h-0" />`. Add an `allocationReading(delta)` helper mirroring `savingsReading`/`balanceReading`: positive→"Stai accantonando più del {prevYear}" (good), negative→"Stai accantonando meno del {prevYear}" (warn), null→"Nessun confronto con il {prevYear}" (neutral) — exact copy from the UI-SPEC Copywriting Contract. "More allocated = good" sentiment (D-05).
    (3) `overview-movers-section.tsx`: add `const [selectedDirection, setSelectedDirection] = useState<'in'|'out'|'allocation'>('out')`; change `handleMonthSelect(monthIndex, direction)` to set both states and call `fetchMovers(year, monthIndex, direction)`; pass `selectedDirection` down to `OverviewMoversPanel`. Wire the chart's new direction-aware `onMonthSelect` to this handler. Preserve the useTransition + error/empty handling exactly.
    (4) `overview-movers-panel.tsx`: add a `direction: 'in'|'out'|'allocation'` prop; render the per-direction heading (IN: "Entrate di {mese} rispetto a {mese prec.}", OUT: existing "Spese di…", allocation: "Accantonamenti di {mese} rispetto a {mese prec.}"); for allocation, render per-nature rows (Risparmio/Investimento, max 2) using `m.name` (the panel already renders `m.name`; for allocation the DAL populates name = nature label / natureCode). Signed Δ colors: +€ in `text-[var(--total-in)]`, −€ in `text-[var(--total-out)]`. Add the direction-specific empty states: allocation→"Nessun accantonamento in {mese}." (D-04), IN→"Nessuna variazione significativa nelle entrate.", OUT→existing string unchanged. Add `aria-label="Caricamento in corso"` to the loading spinner per the UI-SPEC accessibility note.
    (5) `app/(app)/dashboard/overview/page.tsx`: the initial `getMonthOverMonthCategoryChanges(year, defaultMonthIndex)` call now defaults direction='out' (backward compatible — no change needed unless an explicit arg is wanted); pass `data` (which now carries totalAllocation) to `KpiRow` unchanged; the `OverviewMoversSection` props are unchanged except it now owns selectedDirection internally. Confirm the page still compiles with the new component signatures.
    Run `yarn check:language` after — verify Italian product copy is intact and no developer strings leaked.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -15 && grep -c 'totalAllocation' components/dashboard/overview/kpi-row.tsx && grep -c "selectedDirection" components/dashboard/overview/overview-movers-section.tsx && yarn test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `kpi-card-reading.tsx` `Tone` union includes `'allocation'` and valueColor returns `text-[var(--total-allocation)]` for it
    - `kpi-row.tsx` uses `md:grid-cols-5`, reads `data.totalAllocation`, and contains the label string `Accantonato` and an `allocationReading` helper
    - `overview-movers-section.tsx` declares `selectedDirection` state and calls `fetchMovers(year, monthIndex, ...)` with a direction argument
    - `overview-movers-panel.tsx` accepts a `direction` prop and contains the strings `Accantonamenti di` (allocation heading) and `Nessun accantonamento in` (allocation empty state)
    - `yarn build` exits 0; `yarn test` no new failures; `yarn check:language` clean
  </acceptance_criteria>
  <done>5th KPI card surfaces totalAllocation; movers section+panel are direction-aware with per-nature allocation grain and correct Italian copy; page wiring compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of the 4-direction dashboard</name>
  <what-built>
    The overview dashboard now shows: a 3rd "Accantonato" grouped bar (always present, purple) alongside Entrate/Uscite; a 5th "Accantonato" KPI card with delta + reading line; direction-aware bar clicks (Entrate→IN movers, Uscite→OUT movers, Accantonato→per-nature allocation movers); transfer hidden everywhere on the dashboard.
  </what-built>
  <how-to-verify>
    1. Run the app (`yarn dev`) and open the overview dashboard at `/dashboard/overview` for a year with data (and ideally a month with savings/investment activity).
    2. Confirm each month group shows THREE bars: Entrate (green), Uscite (orange), Accantonato (purple) — and that the Accantonato bar is rendered even on a month with no allocation (zero height, still clickable). It must NOT be a stack on Uscite.
    3. Confirm the KPI row shows FIVE cards; the new "Accantonato" card shows a monetary value, a delta badge (when prior-year data exists), and a reading line in Italian.
    4. Click the Entrate bar → movers panel heading reads "Entrate di {mese} rispetto a…"; click Uscite → "Spese di…"; click Accantonato → "Accantonamenti di…" with per-nature rows (Risparmio / Investimento), and on an empty month "Nessun accantonamento in {mese}.".
    5. Confirm transfer does NOT appear as a bar, KPI, or mover anywhere on the dashboard.
    6. Confirm savings/investment amounts appear ONLY in the allocation bar/card, NOT inflating Uscite.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client chart click → server action | The chart passes a `direction` (and month) to `fetchMovers`; validation is enforced server-side in Plan 02 |
| authenticated user → rendered dashboard | KPI/chart/movers data is already user-scoped by the DAL; the UI only renders it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Tampering | direction value sent from chart click to `fetchMovers` | mitigate | The direction is constrained to the three chart bars client-side AND re-validated against the closed enum in `fetchMovers` (Plan 02 T-49-02-01); the UI does not trust client state for authorization |
| T-49-04-02 | Information Disclosure | rendered totals/movers | accept | Pure presentation of already user-scoped DAL output; no new data path; low risk |

No package installs (all shadcn blocks already present per UI-SPEC); no `T-49-SC` checkpoint needed.
</threat_model>

<verification>
- `yarn build` passes; `yarn test` no new failures; `yarn check:language` clean.
- Human-verify checkpoint approved: 3 bars, 5 KPI cards, direction-aware movers, transfer hidden, allocation not inflating Uscite.
</verification>

<success_criteria>
- The dashboard presents IN/OUT plus a visible Accantonato block; transfer excluded and hidden (DASH-01).
- Allocation surfaced as its own KPI measure with delta + reading (DASH-04).
- Refund/divestment netting from Plan 02 is visible in the real amounts and netted bars (DASH-03).
- Direction-aware drill-down works for all three visible directions (D-02, D-03, D-04).
</success_criteria>

<output>
Create `.planning/phases/49-dashboard-and-surfaces/49-04-SUMMARY.md` when done.
</output>
