---
phase: 49-dashboard-and-surfaces
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/dashboard-dal.test.ts
  - tests/overview-dal.test.ts
  - tests/cascade-options.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, CAT-01, CAT-02]
must_haves:
  truths:
    - "A failing test asserts a + refund under an OUT subcategory lowers totalOut (algebraic sum), not raises it"
    - "A failing test asserts savings/investment land in the allocation bucket and contribute 0 to totalOut"
    - "A failing test asserts a transfer-direction transaction contributes 0 to totalIn/totalOut/totalAllocation"
    - "A failing test asserts savings rate stays (in - out)/in with out excluding allocation+transfer"
    - "A failing test asserts buildDirectionNatureMap returns non-empty options keyed by direction code"
  artifacts:
    - path: "tests/dashboard-dal.test.ts"
      provides: "RED tests for algebraic-sum netting, allocation bucket, transfer exclusion, savings rate"
      contains: "totalAllocation"
    - path: "tests/overview-dal.test.ts"
      provides: "RED test for getOverviewChart allocation bucket / OUT bucket excludes savings+investment"
      contains: "allocation"
    - path: "tests/cascade-options.test.ts"
      provides: "RED test for buildDirectionNatureMap with direction-code categories"
      contains: "buildDirectionNatureMap"
  key_links:
    - from: "tests/dashboard-dal.test.ts"
      to: "lib/dal/dashboard.ts"
      via: "import getOverviewAmountTotals / buildOverviewData"
      pattern: "buildOverviewData|getOverviewAmountTotals"
    - from: "tests/cascade-options.test.ts"
      to: "lib/utils/cascade-options.ts"
      via: "import buildDirectionNatureMap"
      pattern: "buildDirectionNatureMap"
---

<objective>
Wave 0 test-first slice. Author the RED automated tests that pin the four money-correctness behaviors ADR 0012 locks (algebraic-sum refund netting, allocation-bucket isolation, transfer exclusion, unchanged savings rate) plus the cascade-options direction-keying contract. These tests are expected to FAIL until Plans 02/03 land — they are the executable acceptance contract for DASH-02, DASH-03, DASH-04, and CAT-01.

Purpose: money-correctness is the critical risk of this phase. A `+` refund under an OUT subcategory must LOWER the OUT total (the inverse of the old sign-split which always added). Without a committed failing test, a shallow rewrite could silently preserve the old behavior and ship a regression.
Output: extended `tests/dashboard-dal.test.ts`, extended `tests/overview-dal.test.ts`, new `tests/cascade-options.test.ts` — all RED at commit time.
</objective>

<artifacts_produced>
## Artifacts this phase produces (declared in this plan)

These symbols are NEW in Phase 49 — created by later plans, asserted-against here. Drift verification must treat them as newly created, not pre-existing:

- `OverviewAggregateRow.totalAllocation` (string) — new field on the dashboard aggregate row type (Plan 02)
- `OverviewData.totalAllocation` (string) + `OverviewData.deltas.totalAllocation` (number | null) — new KPI field (Plan 02)
- `OverviewChartPoint.allocation` (`{ savings: string; investment: string }`) — new chart bucket (Plan 02)
- `buildDirectionNatureMap` — renamed/reworked from `buildTypeNatureMap` in `lib/utils/cascade-options.ts` (Plan 05)
- `MonthOverMonthChange.natureCode` (string | null, optional) — new field for allocation grain (Plan 02)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md
@.planning/phases/49-dashboard-and-surfaces/49-CONTEXT.md
@docs/adr/0012-direction-derived-from-nature-allocation.md
@tests/fixtures/v2-taxonomy-manifest.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED money-correctness tests in dashboard-dal + overview-dal</name>
  <files>tests/dashboard-dal.test.ts, tests/overview-dal.test.ts</files>
  <read_first>
    - tests/dashboard-dal.test.ts (current mocks + assertions — see what schema fields are mocked)
    - tests/overview-dal.test.ts (current getOverviewChart / getOverview test setup)
    - lib/dal/dashboard.ts (buildOverviewData line 455, OverviewData type ~line 95-133, getOverviewAmountTotals line 436, computeSavingsRate import line 27)
    - lib/dal/overview.ts (OverviewChartPoint type line 50, OUT_NATURES line 39, getOverviewChart line 317)
    - .planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md (## Validation Architecture → Key Correctness Assertions block has the exact expected values)
    - tests/fixtures/v2-taxonomy-manifest.ts (slug→nature oracle: the 3 transfer slugs and savings/investment slugs)
    - docs/adr/0012-direction-derived-from-nature-allocation.md (algebraic-sum netting + allocation-as-direction contract)
  </read_first>
  <behavior>
    - Test DASH-02 refund netting: a fixture with €100 OUT spending + a +€30 refund under the SAME OUT subcategory → expect totalOut === '70.00' (NOT '100.00', NOT '130.00').
    - Test DASH-03 allocation isolation: a -€500 savings deposit (cash leaving account, nature savings → direction allocation) → expect totalOut === '0.00' AND totalAllocation === '500.00'.
    - Test DASH-02 net divestment: a +€800 investment deposit and a +€300 divestment in the same month net algebraically per direction → expect the allocation total reflects sum(amount), not abs-summed double-count (assert the netted value, e.g. allocation bar shows the algebraic sum not 800+300).
    - Test DASH-01 transfer exclusion: a transfer-direction transaction contributes 0 to all three totals → expect totalIn === '0.00', totalOut === '0.00', totalAllocation === '0.00'.
    - Test DASH-04 savings rate unchanged: totalIn=3000, totalOut=2000 (spending only, no allocation/transfer) → expect savingsRate === 33 (computeSavingsRate rounds). Assert allocation does NOT enter the savings-rate denominator/numerator.
    - Test DASH-02 overview chart bucket: getOverviewChart routes savings/investment to the new `allocation` bucket and OUT bucket contains only essential/discretionary/debt (assert `allocation.savings` / `allocation.investment` populated, assert `out` has no savings/investment keys).
  </behavior>
  <action>
    Extend `tests/dashboard-dal.test.ts` with describe blocks for the five DASH behaviors above, importing the real `buildOverviewData` and (where the function exists) `getOverviewAmountTotals` from `lib/dal/dashboard.ts`. For SQL-backed functions, follow the existing mock-db pattern in the file but update mocked rows to carry `direction.code` and `direction.includedInTotals` (NOT `subCategory.excludeFromTotals` / `subCategory.nature` — those mocks are stale per Pitfall 6). The `OverviewData.totalAllocation` field does not exist yet; reference it in assertions so the test fails to compile/run RED until Plan 02 adds it. Extend `tests/overview-dal.test.ts` with the getOverviewChart allocation-bucket assertion referencing the new `OverviewChartPoint.allocation` shape. Use the exact expected string values from RESEARCH.md Key Correctness Assertions ('70.00', '500.00', savingsRate 33, transfer zeros). Do NOT implement the production code — these tests must be RED. Commit RED with message `test(49-01): add failing money-correctness tests for direction algebraic sum`.
  </action>
  <verify>
    <automated>yarn test -- dashboard-dal overview-dal 2>&1 | tail -30; echo "EXPECTED: RED (failures or compile errors referencing totalAllocation / allocation bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard-dal.test.ts` contains the string `totalAllocation` in at least one assertion
    - `tests/dashboard-dal.test.ts` contains an assertion `'70.00'` (refund-netting expected value) and `'500.00'` (allocation isolation)
    - `tests/overview-dal.test.ts` contains an assertion referencing `.allocation` bucket on a chart point
    - `yarn test -- dashboard-dal overview-dal` exits NON-zero (RED) at commit time — failing because the production code/types do not yet provide allocation outputs
    - No mock in the edited tests references `subCategory.excludeFromTotals` or `subCategory.nature` (stale schema — Pitfall 6); transfer/allocation exclusion is mocked via `direction.includedInTotals`
  </acceptance_criteria>
  <done>The four DASH behaviors and the chart-bucket behavior have committed RED tests with exact expected values; stale schema mocks removed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED cascade-options direction-keying test (CAT-01)</name>
  <files>tests/cascade-options.test.ts</files>
  <read_first>
    - tests/cascade-options.test.ts (current tests for buildTypeNatureMap / buildCategorySubcategoryMap)
    - lib/utils/cascade-options.ts (buildTypeNatureMap lines 37-39 skip condition, buildOptions helper lines 59-69, buildCategorySubcategoryMap line 102)
    - lib/dal/categories.ts (CategoryWithSubCategories.type line 14 — will become 'in'|'out'|'allocation'|'transfer'|null)
    - lib/utils/nature-labels.ts (NATURE_ORDER, NATURE_LABELS used by buildOptions)
    - .planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md (cascade-options.ts section — the rename + skip-condition change)
  </read_first>
  <behavior>
    - Test CAT-01: given category fixtures whose `type` is a real direction code ('in'/'out'/'allocation'/'transfer') with subcategories carrying effectiveNature, `buildDirectionNatureMap(categories)` returns a record keyed by direction code where each bucket holds the natures belonging to that direction (e.g. result['allocation'] contains savings + investment options; result['out'] contains essential/discretionary/debt).
    - Test CAT-01 regression guard: when a category has `type === null` it is skipped (no crash), but categories with valid direction codes still produce non-empty buckets — the old bug returned `{}` because ALL categories had null type after Phase 46.
    - Test: a `type === 'system'` category is still skipped (system categories never appear as a direction chip).
  </behavior>
  <action>
    Create `tests/cascade-options.test.ts`. Import `buildDirectionNatureMap` from `lib/utils/cascade-options.ts` — this symbol does not exist yet (it is the renamed `buildTypeNatureMap` per Plan 05), so the import fails RED. Build fixtures matching `CategoryWithSubCategories` with `type` set to direction codes and subcategories carrying `effectiveNature` values drawn from the v2 manifest (savings/investment under an 'allocation' category, essential/discretionary under 'out'). Assert the keyed-by-direction output shape and the non-empty allocation bucket. Assert the `'system'`-skip and `null`-skip behavior. Do NOT touch `lib/utils/cascade-options.ts` — keep the test RED. Commit RED with message `test(49-01): add failing buildDirectionNatureMap test for CAT-01`.
  </action>
  <verify>
    <automated>yarn test -- cascade-options 2>&1 | tail -20; echo "EXPECTED: RED (buildDirectionNatureMap not exported yet)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/cascade-options.test.ts` exists and imports `buildDirectionNatureMap` from `@/lib/utils/cascade-options`
    - The test asserts `result['allocation']` is a non-empty array (savings + investment options)
    - The test asserts a `type === 'system'` category is excluded from output
    - `yarn test -- cascade-options` exits NON-zero (RED) — `buildDirectionNatureMap` is not yet exported
  </acceptance_criteria>
  <done>A committed RED test pins the direction-keyed cascade-options contract; references the new `buildDirectionNatureMap` export name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only authors tests; no runtime trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | Test fixtures encoding money values | accept | Tests are dev-only; fixtures use the v2-taxonomy-manifest oracle so expected values trace to the locked contract, not to ad-hoc numbers |

No package installs in this plan; no `T-49-SC` supply-chain checkpoint needed.
</threat_model>

<verification>
- `yarn test -- dashboard-dal overview-dal cascade-options` runs and shows RED for the new assertions (expected at Wave 0).
- The RED failures reference allocation/totalAllocation/buildDirectionNatureMap (proving the tests target the new contract, not a typo).
</verification>

<success_criteria>
- Three test files carry committed RED tests for the five money-correctness behaviors + the cascade-options contract.
- Expected values match RESEARCH.md Key Correctness Assertions exactly.
- No stale `excludeFromTotals` / `subCategory.nature` mocks remain in the edited test files.
</success_criteria>

<output>
Create `.planning/phases/49-dashboard-and-surfaces/49-01-SUMMARY.md` when done.
</output>
