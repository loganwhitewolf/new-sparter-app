---
phase: 49-dashboard-and-surfaces
plan: 05
type: execute
wave: 2
depends_on: ["49-03"]
files_modified:
  - lib/utils/cascade-options.ts
  - components/categorization/subcategory-picker.tsx
  - app/(app)/transactions/transactions.table.ts
  - app/(app)/expenses/expenses.table.ts
  - components/categories/category-settings-panel.tsx
  - components/transactions/transaction-table.tsx
  - lib/utils/pattern-suggestions.ts
  - lib/validations/category.ts
  - lib/actions/categories.ts
autonomous: true
requirements: [CAT-01, CAT-02]
must_haves:
  truths:
    - "cascade-options derives type→nature buckets from the nature→direction mapping; buildDirectionNatureMap returns non-empty options keyed by direction code"
    - "The SubcategoryPicker exposes all 4 direction chips (Entrate / Uscite / Accantonamenti / Trasferimenti)"
    - "Transaction and Expense table filters use a 'direction' key (4 options) with the nature cascade dependsOn 'direction'"
    - "The category settings panel groups categories by the 4 directions, not by the removed category.type"
    - "Assigning a subcategory nature persists a real natureId (not null) via the lookup table"
    - "No remaining reference to the removed category.type across categorization surfaces"
  artifacts:
    - path: "lib/utils/cascade-options.ts"
      provides: "buildDirectionNatureMap keyed by direction code; system-only skip"
      contains: "buildDirectionNatureMap"
    - path: "components/categorization/subcategory-picker.tsx"
      provides: "4 direction chips incl Accantonamenti"
      contains: "allocation"
    - path: "app/(app)/transactions/transactions.table.ts"
      provides: "direction filter (4 options) + nature dependsOn direction"
      contains: "direction"
  key_links:
    - from: "lib/utils/cascade-options.ts"
      to: "lib/dal/categories.ts CategoryWithSubCategories.type"
      via: "iterates cat.type direction code"
      pattern: "cat\\.type"
    - from: "app/(app)/transactions/transactions.table.ts"
      to: "lib/dal/transactions.ts TransactionFilters.direction"
      via: "filter key 'direction'"
      pattern: "direction"
    - from: "lib/actions/categories.ts"
      to: "lib/dal/categories.ts upsertSubcategoryNatureOverride"
      via: "resolve nature code → natureId, persist real id"
      pattern: "natureId"
---

<objective>
Re-point every categorization surface to the nature→direction model (CAT-01, CAT-02; decisions D-07, D-08, D-09). Rewrite `cascade-options.ts` (`buildTypeNatureMap` → `buildDirectionNatureMap`, fix the skip condition that currently drops every category), add the 4th "Accantonamenti" direction chip to the `SubcategoryPicker` (D-07), swap the table filter `type`→`direction` (4 options) with the nature cascade re-pointed to `dependsOn: 'direction'` (D-08), restore direction-based grouping in the category settings panel, fix the broken nature-assignment write path (currently persists `natureId: null`), and clean up the remaining `transaction-table.tsx` / `pattern-suggestions.ts` stubs.

Purpose: this is the categorization-assignment side of the phase. Even though the dashboard hides transfer and separates allocation, the user MUST be able to assign any of the 4 directions and filter tables by all of them (D-07, D-08). This plan consumes the restored `CategoryWithSubCategories.type` from Plan 03.
Output: rewritten cascade-options + picker + table configs + settings panel + nature write path + the remaining UI/util stubs; Plan 01 cascade-options test GREEN.
</objective>

<artifacts_produced>
## Artifacts this phase produces (declared in this plan)

NEW / CHANGED symbols (drift verification: treat as newly created/renamed):

- `buildDirectionNatureMap` — renamed from `buildTypeNatureMap` in `lib/utils/cascade-options.ts`; output keyed by `'in'|'out'|'allocation'|'transfer'|''`
- 4th `TYPE_FILTERS` chip `{ key: 'allocation', label: 'Accantonamenti' }` in `subcategory-picker.tsx`; `defaultType` extended to include `'allocation'`
- `DIRECTION_LABELS` const (renamed from `TYPE_LABELS`, gains `allocation`) in both `transactions.table.ts` and `expenses.table.ts`; filter key `'direction'` (was `'type'`); nature filter `dependsOn: 'direction'`
- Direction-based `TYPE_LABELS`/`TYPE_ORDER` extended with `allocation` in `category-settings-panel.tsx`
- `natureId` resolution in `setSubcategoryNatureAction` (`lib/actions/categories.ts`) + re-added `natureId` field in `CreateSubcategorySchema` (`lib/validations/category.ts`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md
@.planning/phases/49-dashboard-and-surfaces/49-UI-SPEC.md
@.planning/phases/49-dashboard-and-surfaces/49-CONTEXT.md
@.planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md
@CONTEXT.md
@CLAUDE.md
@tests/fixtures/v2-taxonomy-manifest.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite cascade-options to buildDirectionNatureMap (CAT-01)</name>
  <files>lib/utils/cascade-options.ts</files>
  <read_first>
    - lib/utils/cascade-options.ts (buildTypeNatureMap lines 37-39 skip condition, buildOptions helper lines 59-69, buildCategorySubcategoryMap line 102, the Record output + allOptions union)
    - lib/dal/categories.ts (CategoryWithSubCategories.type after Plan 03 — now a direction code; effectiveNature on subcategories)
    - lib/utils/nature-labels.ts (NATURE_ORDER, NATURE_LABELS used by buildOptions)
    - tests/cascade-options.test.ts (the Plan 01 RED test expecting buildDirectionNatureMap keyed by direction code)
    - .planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md (cascade-options.ts section: rename + skip-condition change; Preserve buildOptions)
    - .planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md (cascade-options full rewrite section + Pitfall 5)
  </read_first>
  <action>
    Rename `buildTypeNatureMap` to `buildDirectionNatureMap`. Change the skip condition from `if (cat.type === null || cat.type === 'system') continue` to `if (cat.type === 'system') continue` — the null-check is removed because `cat.type` is now a real direction code (Plan 03). Key the output Record by direction code: `'in' | 'out' | 'allocation' | 'transfer' | ''` (add the `allocation` bucket). Preserve the `buildOptions()` inner helper (NATURE_ORDER filter + NATURE_LABELS map) and the `allOptions` union bucket unchanged. In `buildCategorySubcategoryMap`, apply the same skip-condition fix (`cat.type === 'system'` only). Update any consumers of the old function name (`grep -rn buildTypeNatureMap` across lib/components/app) to call `buildDirectionNatureMap`. Keep the function a pure utility — no `server-only` import, no DB access (RESEARCH Architectural Responsibility Map: pure utility tier).
  </action>
  <verify>
    <automated>yarn test -- cascade-options 2>&1 | tail -20 && grep -rc 'buildTypeNatureMap' lib app components 2>/dev/null | grep -v ':0' || echo "no stale buildTypeNatureMap refs"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/utils/cascade-options.ts` exports `buildDirectionNatureMap` and contains no `buildTypeNatureMap`
    - The skip condition is `cat.type === 'system'` only (no `cat.type === null` guard)
    - The output Record includes an `allocation` key bucket
    - No file under lib/, app/, or components/ still references `buildTypeNatureMap`
    - Behavior: `yarn test -- cascade-options` exits 0 (Plan 01 RED test now GREEN)
  </acceptance_criteria>
  <done>cascade-options derives buckets from direction codes, no longer drops all categories; Plan 01 CAT-01 test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: 4th direction chip in SubcategoryPicker + table filter direction swap (D-07, D-08, D-09)</name>
  <files>components/categorization/subcategory-picker.tsx, app/(app)/transactions/transactions.table.ts, app/(app)/expenses/expenses.table.ts</files>
  <read_first>
    - components/categorization/subcategory-picker.tsx (TYPE_FILTERS lines 72-77, TypeKey alias line 38, defaultType prop line 58, allowedCategoryTypes, PickerBody filter `c.type === type`, rail filter)
    - lib/categorization/subcategory-options.ts (buildCategoryOptions filtering by categoryType, allowedCategoryTypes — confirm allocation/transfer categories are NOT additionally blocked by a system-only guard; resolves RESEARCH Open Question 2)
    - app/(app)/transactions/transactions.table.ts (TYPE_LABELS lines 4-9, type filter lines 51-56, nature filter dependsOn 'type' lines 58-64, full TableConfig shape)
    - app/(app)/expenses/expenses.table.ts (identical structure)
    - lib/dal/transactions.ts / lib/dal/expenses.ts (after Plan 03 — TransactionFilters.direction / ExpenseFilters.direction the configs must match)
    - lib/utils/nature-labels.ts (NATURE_LABELS for nature cascade chip labels)
    - scripts/seed-data.ts (directions array lines 1244-1289 — allocation label_it = "Accantonamenti"; nature labelIt for savings="Straordinario"/investment="Finanziario" — note the seed label vs UI-SPEC display preference)
    - .planning/phases/49-dashboard-and-surfaces/49-UI-SPEC.md (Direction Chip Contract; Table Filter Contract; Copywriting Contract — allocation chip "Accantonamenti", and the nature cascade label note where the planner may override "Straordinario"→"Risparmio" and "Finanziario"→"Investimento" if the seed label reads poorly)
    - .planning/phases/49-dashboard-and-surfaces/49-PATTERNS.md (subcategory-picker.tsx section; transactions.table.ts mechanical swap; expenses.table.ts)
  </read_first>
  <action>
    In `subcategory-picker.tsx`: extend `TYPE_FILTERS` to 4 direction chips by inserting `{ key: 'allocation', label: 'Accantonamenti' }` between Uscite and Trasferimenti (label from seeded `direction.label_it`, D-09). Extend `defaultType` prop type to include `'allocation'`. `TypeKey = CategoryWithSubCategories['type']` auto-extends from Plan 03 — confirm no manual literal-union edit is also needed elsewhere. Verify `buildCategoryOptions` in `lib/categorization/subcategory-options.ts` does not block allocation/transfer with a stray `type === 'system'`-only guard (Open Question 2); if it does, fix it so all 4 directions surface. The `PickerBody`/rail `c.type === type` logic needs no change once types are restored.
    In `transactions.table.ts` and `expenses.table.ts` (apply identically to both): rename `TYPE_LABELS` to `DIRECTION_LABELS` and add `allocation: 'Accantonamenti'`; change the filter object `key: 'type'`→`key: 'direction'`, `label: 'Tipo'`→`label: 'Direzione'`, add the `allocation` select option (4 options: in/out/allocation/transfer with labels from `direction.label_it`), update `toChip` to `Direzione: ${DIRECTION_LABELS[v] ?? v}`; change the nature filter's `dependsOn: 'type'`→`dependsOn: 'direction'`. For the nature cascade chip labels, use `NATURE_LABELS`; per the UI-SPEC, if the seeded `nature.labelIt` reads poorly for savings/investment ("Straordinario"/"Finanziario"), override the display to "Risparmio"/"Investimento" in the table config. Preserve the rest of the declarative `TableConfig` shape.
    Run `yarn check:language` after.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -15 && grep -c 'allocation' components/categorization/subcategory-picker.tsx && grep -c "key: 'direction'\|key: \"direction\"" app/(app)/transactions/transactions.table.ts app/(app)/expenses/expenses.table.ts</automated>
  </verify>
  <acceptance_criteria>
    - `subcategory-picker.tsx` `TYPE_FILTERS` has a chip with `key: 'allocation'` labelled "Accantonamenti"
    - `transactions.table.ts` and `expenses.table.ts` each have a filter with `key: 'direction'` (label "Direzione"), 4 select options, and the nature filter `dependsOn: 'direction'`
    - No remaining `key: 'type'` filter or `dependsOn: 'type'` in either table config
    - Behavior: the direction filter offers In/Out/Accantonamenti/Trasferimenti and the nature cascade enables only after a direction is chosen (verified via build + config inspection)
    - `yarn build` exits 0; `yarn check:language` clean
  </acceptance_criteria>
  <done>The picker exposes all 4 direction chips; both table configs filter by direction with the nature cascade re-pointed; labels sourced from seed direction.label_it.</done>
</task>

<task type="auto">
  <name>Task 3: Settings-panel direction grouping + nature write-path fix + remaining stubs</name>
  <files>components/categories/category-settings-panel.tsx, components/transactions/transaction-table.tsx, lib/utils/pattern-suggestions.ts, lib/validations/category.ts, lib/actions/categories.ts</files>
  <read_first>
    - components/categories/category-settings-panel.tsx (TYPE_LABELS lines 30-35, TYPE_ORDER line 37, CategorySidebar grouping lines 39-53)
    - components/transactions/transaction-table.tsx (lines 315-320: categoryType placeholder + isTransfer hardcoded false)
    - lib/utils/pattern-suggestions.ts (lines 19, 138: detected-sign display TODO — patterns are sign-agnostic)
    - lib/validations/category.ts (CreateSubcategorySchema line ~74 — natureId removed as TODO)
    - lib/actions/categories.ts (createSubcategoryAction line 126, setSubcategoryNatureAction lines 143-164 — passes natureId: null)
    - lib/dal/categories.ts (upsertSubcategoryNatureOverride signature lines 258-260 — accepts natureId: number | null)
    - lib/utils/nature-labels.ts (FlowNature codes → for resolving nature code → natureId; tests/fixtures/v2-taxonomy-manifest.ts NATURE_ID_BY_CODE map line 223)
    - .planning/phases/49-dashboard-and-surfaces/49-CONTEXT.md (D-07 all 4 directions assignable; D-09 labels from seed)
  </read_first>
  <action>
    (1) `category-settings-panel.tsx`: extend `TYPE_LABELS` with `allocation: 'Accantonamenti'` and add `'allocation'` to `TYPE_ORDER` (place it per display order: out, in, allocation, transfer, system). The `categories.filter((c) => c.type === type)` grouping now works because Plan 03 restored real direction codes. Remove the TODO(Phase 49) comment.
    (2) `transaction-table.tsx`: replace the `categoryType` placeholder usage and `const isTransfer = false` hardcode — derive `isTransfer` from `row.categoryType === 'transfer'` now that `TransactionListRow.categoryType` is a direction code (Plan 03). Remove the two TODO(Phase 49) comments.
    (3) `pattern-suggestions.ts`: drop the detected-sign display logic at both TODO sites — patterns are sign-agnostic (ADR 0012 supersedes ADR 0008); remove the sign indicator from the suggestion display and the TODO comments.
    (4) `lib/validations/category.ts` + `lib/actions/categories.ts`: re-add `natureId` (number, nullable) to `CreateSubcategorySchema` and fix `setSubcategoryNatureAction` — it currently calls `upsertSubcategoryNatureOverride({..., natureId: null})`, discarding the chosen nature. Resolve the nature CODE from the input to its natureId via the lookup (use the `NATURE_ID_BY_CODE` map from the v2-taxonomy-manifest, or a small DB lookup of `nature.code → nature.id`) and pass the real `natureId`. Wire `createSubcategoryAction` to pass the parsed `natureId` through to `createUserSubcategory`. Preserve the `verifySession()`, visibility check, and `revalidateCategorizationSurfaces()` shape. Remove the TODO comments.
    Run `yarn check:language` after.
  </action>
  <verify>
    <automated>yarn build 2>&1 | tail -15 && for f in components/categories/category-settings-panel.tsx components/transactions/transaction-table.tsx lib/utils/pattern-suggestions.ts lib/validations/category.ts lib/actions/categories.ts; do echo "$f: $(grep -c 'TODO(Phase 49)' "$f")"; done && yarn test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `category-settings-panel.tsx` `TYPE_ORDER` includes `'allocation'` and `TYPE_LABELS` maps it to "Accantonamenti"
    - `transaction-table.tsx` derives `isTransfer` from `categoryType === 'transfer'` (no hardcoded `false`)
    - `setSubcategoryNatureAction` resolves and passes a real `natureId` (not `null`) to `upsertSubcategoryNatureOverride`
    - `CreateSubcategorySchema` includes a `natureId` field
    - `grep -c 'TODO(Phase 49)'` returns 0 for all five files
    - `yarn build` exits 0; `yarn test` no new failures; `yarn check:language` clean
  </acceptance_criteria>
  <done>Settings panel groups by direction; nature assignment persists a real natureId; transaction-table transfer detection restored; sign-agnostic pattern display; remaining stubs cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → setSubcategoryNatureAction / createSubcategoryAction | nature/natureId crosses from the categorization UI into a DB write |
| client filter select → table DAL | the `direction` filter value crosses into the list query (validated in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05-01 | Tampering | natureId resolved from a client-supplied nature code in `setSubcategoryNatureAction` | mitigate | Resolve nature code against the closed lookup set (NATURE_ID_BY_CODE / `nature.code` table) — an unknown code resolves to no id and the write is rejected; Zod `SetSubcategoryNatureSchema` already validates the input shape; `isSubCategoryVisibleToUser` scopes the target to the user |
| T-49-05-02 | Elevation of Privilege | subcategory nature override write | mitigate | Existing `verifySession()` + visibility check preserved; the override is keyed by `userId`; no cross-user write path introduced |
| T-49-05-03 | Tampering | `direction` filter value in table config → DAL | mitigate | Closed select options (4 direction codes) client-side; parameterized `eq(direction.code, ...)` server-side (Plan 03 T-49-03-01); arbitrary values match zero rows |

No package installs; no `T-49-SC` supply-chain checkpoint needed.
</threat_model>

<verification>
- `yarn test -- cascade-options` GREEN (Plan 01 CAT-01 test satisfied).
- `yarn build` passes; full `yarn test` no new failures; `yarn check:language` clean.
- `grep -rc 'category.type\|buildTypeNatureMap' lib app components` shows no stale references to the removed model.
</verification>

<success_criteria>
- cascade-options + picker derive type chips from nature→direction; all 4 directions assignable (CAT-01, D-07).
- Both table filters operate on direction + nature consistently with the new model (CAT-02, D-08, D-09).
- Settings panel groups by direction; nature assignment persists a real natureId (write-path bug fixed).
- No reference to the removed `category.type` remains across categorization surfaces.
</success_criteria>

<output>
Create `.planning/phases/49-dashboard-and-surfaces/49-05-SUMMARY.md` when done.
</output>
