---
phase: 49-dashboard-and-surfaces
plan: 06
type: execute
wave: 3
depends_on: ["49-02", "49-03"]
files_modified:
  - lib/db/schema.ts
  - drizzle/migrations
  - tests/dashboard-dal.test.ts
autonomous: false
requirements: [DASH-02]
must_haves:
  truths:
    - "sub_category.exclude_from_totals is removed from the schema and from the database via a generated migration"
    - "No application code references subCategory.excludeFromTotals (the column-drop is safe because all references were removed in Waves 1-2)"
    - "The migration follows the Phase 48 operator caution: pg_dump snapshot before apply, guarded apply, no hand-written down-migration"
    - "direction.included_in_totals is the single source of truth for spending-total exclusion (transfer + allocation excluded)"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "subCategory table with excludeFromTotals removed"
      contains: "nature_id"
    - path: "drizzle/migrations"
      provides: "Generated migration dropping exclude_from_totals"
  key_links:
    - from: "lib/db/schema.ts"
      to: "drizzle/migrations"
      via: "drizzle-kit generate from the schema diff"
      pattern: "DROP COLUMN"
---

<objective>
Drop the now-redundant `sub_category.exclude_from_totals` column (D-10, D-11, D-12, DASH-02). After Plans 02 and 03 removed `notExcludedFromTotals()` and every `subCategory.excludeFromTotals` reference (replacing them with `direction.included_in_totals`), the column carries no information `direction.included_in_totals` does not already encode — research confirmed semantic equivalence via the v2-taxonomy-manifest (the 3 transfer slugs + the 2 allocation natures map exactly to `included_in_totals = false`). This plan removes the column from `schema.ts`, generates the migration with `drizzle-kit generate`, and applies it via the project flow with the Phase 48 operator caution.

Purpose: complete the single-source-of-truth cleanup — leaving a dead, no-longer-read column is tech debt the user rejected (D-11). This MUST be the final wave because dropping the column while any wave still references it would throw a DB error (the critical sequencing constraint from RESEARCH.md and PATTERNS.md).
Output: `excludeFromTotals` removed from `schema.ts`; a generated SQL migration; test mocks cleaned; the migration applied via the operator-guarded flow.
</objective>

<artifacts_produced>
## Artifacts this phase produces (declared in this plan)

NEW artifacts created by this plan (drift verification: treat as newly created):

- A new generated migration file under `drizzle/migrations/` (e.g. `0019_*.sql`) containing `ALTER TABLE "sub_category" DROP COLUMN "exclude_from_totals"` — exact filename assigned by `drizzle-kit generate`
- Updated `drizzle/migrations/meta/_journal.json` + snapshot (drizzle-kit managed)

REMOVED (intentional): `excludeFromTotals` field from the `subCategory` pgTable in `lib/db/schema.ts`; any `subCategory.excludeFromTotals` mock in `tests/dashboard-dal.test.ts`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md
@.planning/phases/49-dashboard-and-surfaces/49-CONTEXT.md
@.planning/phases/48-sql-migration-recategorization/48-CONTEXT.md
@docs/adr/0012-direction-derived-from-nature-allocation.md
@CLAUDE.md
@tests/fixtures/v2-taxonomy-manifest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove excludeFromTotals from schema + clean test mocks, then generate migration</name>
  <files>lib/db/schema.ts, tests/dashboard-dal.test.ts, drizzle/migrations</files>
  <read_first>
    - lib/db/schema.ts (subCategory table — excludeFromTotals at line 176; confirm no other table references it)
    - tests/dashboard-dal.test.ts (any mock still referencing subCategory.excludeFromTotals — Pitfall 6 false-green risk)
    - .planning/phases/49-dashboard-and-surfaces/49-RESEARCH.md (## exclude_from_totals Column Drop: Verification Data — the 3 transfer slugs + allocation natures = included_in_totals false; the migration flow block; Pitfall 6 test mock)
    - .planning/phases/48-sql-migration-recategorization/48-CONTEXT.md (D-13/D-14 operator protocol: pg_dump, guarded apply, no down-migration)
    - CLAUDE.md (Drizzle migrations rule: drizzle-kit generate + scripts/migrate.ts; NEVER drizzle-kit push)
    - package.json (db:generate / db:migrate / db:verify scripts)
  </read_first>
  <action>
    PRECONDITION GREP (run first, fail loudly if non-zero): `grep -rn 'excludeFromTotals' lib app components` MUST return zero application references — Plans 02 and 03 removed them all. If any reference remains, STOP and report which file still uses it (the column drop is unsafe until that reference is gone — this is the critical sequencing constraint).
    Then: remove the `excludeFromTotals` field from the `subCategory` pgTable in `lib/db/schema.ts` (line 176). Remove any `subCategory.excludeFromTotals` mock from `tests/dashboard-dal.test.ts` (Pitfall 6 — a stale mock shields the test from DB reality, a false green). Generate the migration with `yarn db:generate` (drizzle-kit generate) — this produces a new `drizzle/migrations/0019_*.sql` with `ALTER TABLE "sub_category" DROP COLUMN "exclude_from_totals"`. Do NOT write a hand-crafted down-migration (D-12 — restore-from-dump is the rollback path). Do NOT run `drizzle-kit push`. Inspect the generated SQL to confirm it is a single DROP COLUMN and contains no unintended drops (drizzle-kit can over-generate; verify the diff matches only the excludeFromTotals removal).
  </action>
  <verify>
    <automated>echo "app refs to excludeFromTotals (must be 0):" && grep -rc 'excludeFromTotals' lib app components 2>/dev/null | grep -v ':0' || echo "0 references — safe to drop" ; echo "--- generated migration ---" && ls -t drizzle/migrations/*.sql 2>/dev/null | head -1 && yarn build 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'excludeFromTotals' lib app components` returns no non-zero matches (no application reference remains)
    - `lib/db/schema.ts` no longer declares `exclude_from_totals` on the subCategory table
    - A new migration file exists under `drizzle/migrations/` whose SQL contains `DROP COLUMN "exclude_from_totals"` and NO other unintended schema change
    - `tests/dashboard-dal.test.ts` contains no mock referencing `subCategory.excludeFromTotals`
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>The column is removed from schema + test mocks; a clean single-column-drop migration is generated; no app code references the column.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator-guarded migration apply (pg_dump snapshot → db:migrate → db:verify)</name>
  <what-built>
    Task 1 generated a single-column-drop migration (`DROP COLUMN exclude_from_totals`) from the schema change. Per D-12 / the Phase 48 operator protocol, applying it to a live database requires a pg_dump snapshot first and a guarded apply — it cannot run unattended in CI without the snapshot step. There is no hand-written down-migration; the rollback path is restore-from-dump.
  </what-built>
  <how-to-verify>
    1. Take a snapshot BEFORE applying: `pg_dump $DATABASE_URL > backup-pre-phase49-$(date +%Y%m%d).dump` (run against the target environment; staging first per the Phase 48 staging-first gate).
    2. Apply the migration: `yarn db:migrate` — confirm it runs to completion with no errors and applies only the new `0019_*` migration.
    3. Verify: `yarn db:verify` (the Phase 48 verification harness) — confirm no assertion fails; spending-total exclusion now sources from `direction.included_in_totals` with no semantic gap.
    4. Sanity-check the dashboard against the migrated DB: spending totals still exclude transfer + allocation (the 3 transfer slugs + savings/investment), matching pre-migration behavior.
    5. Rebuild the app artifact cache if stale: `rm -rf .next` (the column change invalidates the build cache; Vercel preview rebuilds automatically).
    6. If anything fails: restore from the dump taken in step 1 — do NOT attempt a hand-written reverse migration.
  </how-to-verify>
  <resume-signal>Type "applied" once the migration is applied and `yarn db:verify` is green on the target environment, or describe the failure (and whether a dump-restore was needed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live database | the column-drop migration mutates production/staging schema and is irreversible without the dump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06-01 | Denial of Service / data loss | `DROP COLUMN exclude_from_totals` migration | mitigate | `pg_dump` snapshot taken BEFORE apply (D-12); staging-first gate (Phase 48 protocol); restore-from-dump is the rollback path; the dropped column is DB-internal (never exposed in API responses or user settings — RESEARCH Runtime State Inventory) so no user-facing data is lost |
| T-49-06-02 | Tampering | aggregation correctness after column drop | mitigate | The drop only proceeds after the Task 1 precondition grep proves zero app references remain; `direction.included_in_totals` proven semantically equivalent via the v2-taxonomy-manifest oracle (3 transfer slugs + allocation natures); `yarn db:verify` confirms post-migration integrity |
| T-49-06-03 | Tampering | unintended over-generated DDL from drizzle-kit | mitigate | Task 1 inspects the generated SQL to confirm a single DROP COLUMN and no unintended drops before any apply; `drizzle-kit push` is forbidden (CLAUDE.md hard rule) |

No package installs; no `T-49-SC` supply-chain checkpoint needed.
</threat_model>

<verification>
- Task 1 precondition grep proves zero `excludeFromTotals` app references (the sequencing constraint is satisfied — this wave runs AFTER Plans 02 + 03).
- Generated migration is a single clean DROP COLUMN; `yarn build` passes.
- Operator checkpoint: pg_dump taken, `yarn db:migrate` clean, `yarn db:verify` green, dashboard spending totals unchanged.
</verification>

<success_criteria>
- `sub_category.exclude_from_totals` is gone from schema and DB (DATA-06 / D-10 closed).
- `direction.included_in_totals` is the sole source of spending-total exclusion (D-11).
- Migration applied via the project flow (drizzle-kit generate + scripts/migrate.ts), never push; pg_dump rollback path honored (D-12).
- No false-green test mock for the dropped column remains.
</success_criteria>

<output>
Create `.planning/phases/49-dashboard-and-surfaces/49-06-SUMMARY.md` when done.
</output>
