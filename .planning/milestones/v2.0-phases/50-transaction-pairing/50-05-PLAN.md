---
phase: 50-transaction-pairing
plan: 05
type: execute
wave: 3
depends_on: ["50-03", "50-04"]
files_modified:
  - components/transactions/counterpart-picker-dialog.tsx
  - components/transactions/transaction-pair-popover.tsx
  - components/transactions/transaction-table.tsx
  - app/(app)/transactions/page.tsx
autonomous: false
requirements: [PAIR-01, PAIR-02, PAIR-03]
tags: [transaction-pairing, ui, dialog, popover, transactions]

must_haves:
  truths:
    - "A 'Collega rimborso' row action opens a counterpart picker dialog; 'Scollega' replaces it when the row is already paired"
    - "The picker lists only eligible counterparts (opposite sign, ±90 days, not already paired) and confirming creates the pair"
    - "Each paired row shows an inline link badge with the signed net amount; clicking it opens a popover with counterpart details + 'Vai alla transazione'"
    - "Paired rows stay in natural chronological position (no re-ordering or grouping)"
  artifacts:
    - path: "components/transactions/counterpart-picker-dialog.tsx"
      provides: "CounterpartPickerDialog — searchable opposite-sign counterpart picker mirroring TransactionFormDialog"
      contains: "createTransactionPairAction"
    - path: "components/transactions/transaction-pair-popover.tsx"
      provides: "TransactionPairPopover — link badge + counterpart popover"
      contains: "Popover"
    - path: "components/transactions/transaction-table.tsx"
      provides: "Collega/Scollega row actions + inline pair badge + pairTarget dialog state"
      contains: "Collega rimborso"
    - path: "app/(app)/transactions/page.tsx"
      provides: "Picker dialog wiring (data passed to the client TransactionTable)"
      contains: "TransactionTable"
  key_links:
    - from: "components/transactions/transaction-table.tsx"
      to: "components/transactions/counterpart-picker-dialog.tsx"
      via: "pairTarget state opens the dialog"
      pattern: "CounterpartPickerDialog"
    - from: "components/transactions/counterpart-picker-dialog.tsx"
      to: "lib/actions/transaction-pairs.ts"
      via: "useActionState(createTransactionPairAction)"
      pattern: "createTransactionPairAction"
    - from: "components/transactions/transaction-table.tsx"
      to: "lib/actions/transaction-pairs.ts"
      via: "deleteTransactionPairAction on Scollega"
      pattern: "deleteTransactionPairAction"
---

<objective>
Deliver the PAIR-01/PAIR-02/PAIR-03 user surface: the "Collega rimborso"/"Scollega" row actions (D-09–D-11), the searchable counterpart picker dialog (D-12–D-14), and the inline pair badge + popover (D-15, D-16). All Italian copy is intentional user-facing product surface; everything else stays English.

Purpose: Wire the Plan 03 actions/DAL and the Plan 04 list fields into the transaction list so a user can link, see the net, and unlink without leaving the page.
Output: two new components, plus row-action + badge + wiring edits to the table and page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-transaction-pairing/50-RESEARCH.md
@.planning/phases/50-transaction-pairing/50-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `CounterpartPickerDialog` (open/onOpenChange/transactionId/transactionAmount/transactionOccurredAt props) — components/transactions/counterpart-picker-dialog.tsx.
- `TransactionPairPopover` (link badge + popover with counterpart description/amount/date/net + "Vai alla transazione") — components/transactions/transaction-pair-popover.tsx.
- `Collega rimborso` / `Scollega` dropdown items, inline pair badge, and `pairTarget` client state — components/transactions/transaction-table.tsx.
- Picker dialog wiring — app/(app)/transactions/page.tsx.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CounterpartPickerDialog + TransactionPairPopover components</name>
  <files>components/transactions/counterpart-picker-dialog.tsx, components/transactions/transaction-pair-popover.tsx</files>
  <read_first>
    - components/transactions/transaction-form-dialog.tsx — the exact Dialog shell, `useActionState` + `submittedRef` + `useEffect` success-close + toast, and `Alert` error-display pattern to mirror (50-PATTERNS.md counterpart-picker-dialog section quotes the lines)
    - components/ui/popover.tsx + components/ui/badge.tsx + components/ui/input.tsx + components/ui/dialog.tsx — the primitives to compose (do not hand-roll a positioned div — 50-RESEARCH.md Don't Hand-Roll)
    - lib/actions/transaction-pairs.ts (Plan 03) — `createTransactionPairAction` signature consumed via `useActionState`
    - lib/dal/transaction-pairs.ts (Plan 03) — `getEligibleCounterparts` / `CounterpartRow` shape the picker list renders; the picker loads counterparts via a server action or server-fetched prop (server-safe split: data server-side, state client-side)
    - lib/utils/decimal.ts — `toDecimal` for formatting the signed net amount in the badge/popover (project hard rule)
    - lib/routes.ts — the canonical transactions route constant for the "Vai alla transazione" link (do not hardcode `/transactions`)
    - .planning/phases/50-transaction-pairing/50-RESEARCH.md §5.2/§5.3 (picker + badge), Open Question 1 (same signed net on both legs)
  </read_first>
  <action>
    Create `components/transactions/counterpart-picker-dialog.tsx` (`'use client'`) mirroring `transaction-form-dialog.tsx`: props `{ open, onOpenChange, transactionId, transactionAmount, transactionOccurredAt }`. Use `useActionState(createTransactionPairAction, { error: null })` + a `submittedRef`; on success (`state.error === null` after submit) close the dialog and `toast.success('Transazione collegata.')`. Render a `Dialog`/`DialogContent` titled "Collega rimborso" with: an `Input` search field (filters the eligible list by description/customTitle), two `Input type="date"` controls for the date range defaulted to ±90 days around `transactionOccurredAt` (D-13 — user can widen), and a scrollable list of eligible counterparts. The eligible list is fetched via a server action that calls `getEligibleCounterparts` (opposite sign + date range + not-already-paired, D-14) — pass `referenceId`, `referenceAmount`, the date range; re-fetch when the range changes. Selecting a counterpart sets a hidden `counterpartId` field; the form `action` sets `submittedRef.current = true` then calls `formAction`. Show `state.error` in a destructive `Alert`. Italian UI copy intentional; identifiers/comments English.
    Create `components/transactions/transaction-pair-popover.tsx` (`'use client'`): props `{ pairedWithId, netAmount, currency, pairedDescription, pairedAmount, pairedOccurredAt }`. Render a `Badge` trigger showing a `Link2` icon + the signed net formatted via `toDecimal(netAmount)` (e.g. "🔗 €-50" / "🔗 +€50" — the SAME signed net on both legs, Open Question 1; never native arithmetic). Wrap it in a `Popover`; `PopoverContent` shows counterpart description, counterpart amount (formatted via Decimal), counterpart date, the net effect, and a "Vai alla transazione" link to `${APP_ROUTES.transactions}?q=${pairedWithId}` (D-16 — no navigation away/scroll-jump beyond the query anchor). Reuse `Badge` exactly as the table's existing status chips.
  </action>
  <verify>
    <automated>yarn lint components/transactions/counterpart-picker-dialog.tsx components/transactions/transaction-pair-popover.tsx && yarn check:language components/transactions/counterpart-picker-dialog.tsx components/transactions/transaction-pair-popover.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `counterpart-picker-dialog.tsx` contains `'use client'`, `useActionState(createTransactionPairAction`, a `submittedRef`, `Dialog`, `Input`, and a hidden `counterpartId` field; references `getEligibleCounterparts` (directly or via a load action).
    - Source assertion: `transaction-pair-popover.tsx` contains `Popover`, `Badge`, `Link2`, `toDecimal(` for the net formatting (no `Number(`/`parseFloat(`/native `+ - * /` on amounts), and a "Vai alla transazione" link built from the routes constant (not a hardcoded `/transactions`).
    - Behavior assertion (PAIR-02 visual/interaction — 50-VALIDATION.md Manual-Only row): the badge shows link icon + signed net; the popover shows counterpart description/amount/date + net + link.
    - Command: `yarn lint` + `yarn check:language` clean on both files.
  </acceptance_criteria>
  <done>Both components compile and lint clean; picker mirrors the dialog pattern and loads eligible counterparts; badge/popover render link + Decimal-formatted net + counterpart details; language check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Row actions + inline badge in TransactionTable + page wiring</name>
  <files>components/transactions/transaction-table.tsx, app/(app)/transactions/page.tsx</files>
  <read_first>
    - components/transactions/transaction-table.tsx — the existing `DropdownMenuItem` actions block (50-RESEARCH.md §5.1 lines ~437–495), the `categorizeTarget`/`ExpenseCategorizeDialog` state pattern to mirror for `pairTarget`, and the description/title `TableCell` where the badge mounts; the component is already `'use client'`
    - components/transactions/counterpart-picker-dialog.tsx + transaction-pair-popover.tsx (Task 1) — the components mounted here
    - lib/actions/transaction-pairs.ts (Plan 03) — `deleteTransactionPairAction` for the "Scollega" action
    - lib/dal/transactions.ts (Plan 04) — `TransactionListRow` now carries `pairedWithId`/`pairedNetAmount`/`pairedDescription`/`pairedOccurredAt`; the badge reads these
    - app/(app)/transactions/page.tsx — the RSC page; confirm dialog state lives in the client `TransactionTable` (page passes data, not dialog state), mirroring the existing categorize orchestration
    - .planning/phases/50-transaction-pairing/50-PATTERNS.md (transaction-table.tsx + page.tsx sections — exact dropdown + badge + wiring snippets)
  </read_first>
  <action>
    transaction-table.tsx: add a `pairTarget` client state `useState<{ id: string; amount: string; occurredAt: Date } | null>(null)` mirroring `categorizeTarget`. In the row-action dropdown, before the existing `<DropdownMenuSeparator />`, add: when `transaction.pairedWithId` is set, a `<DropdownMenuItem>` "Scollega" that submits `deleteTransactionPairAction` (form/action invocation) for `transaction.id` (D-11, PAIR-03); otherwise a `<DropdownMenuItem>` "Collega rimborso" that calls `setPairTarget({ id, amount: transaction.amount, occurredAt: transaction.occurredAt })` (D-09, PAIR-01). In the description/title `TableCell`, after the existing title content, render `<TransactionPairPopover ... />` when `transaction.pairedWithId && transaction.pairedNetAmount` are present, passing `netAmount={transaction.pairedNetAmount}`, `currency`, `pairedDescription`, `pairedAmount={transaction.pairedNetAmount}`, `pairedOccurredAt` (D-15 inline, natural position — do NOT re-sort or group rows). Mount `<CounterpartPickerDialog open={pairTarget !== null} onOpenChange={(o) => { if (!o) setPairTarget(null) }} transactionId={pairTarget?.id ?? ''} transactionAmount={pairTarget?.amount ?? ''} transactionOccurredAt={pairTarget?.occurredAt ?? new Date()} />` alongside the existing `ExpenseCategorizeDialog`.
    page.tsx: confirm the RSC passes the transaction rows (now including the paired fields from the Plan 04 select) to `TransactionTable`; no new server data fetch is required since counterpart lookup is lazy inside the dialog. If `TransactionTable` gained required props, thread them from the page. Keep the page a Server Component; all dialog/popover state stays in the client table.
    Italian copy ("Collega rimborso", "Scollega") is intentional product surface; identifiers/comments English. Run `yarn check:language` (touches components — required by CLAUDE.md).
  </action>
  <verify>
    <automated>yarn build && yarn check:language</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `transaction-table.tsx` contains `Collega rimborso`, `Scollega`, `deleteTransactionPairAction`, `CounterpartPickerDialog`, `TransactionPairPopover`, and a `pairTarget` state; the badge renders only when `transaction.pairedWithId` is set (D-15) with no re-sort/group of rows.
    - Source assertion: `app/(app)/transactions/page.tsx` still has no `'use client'` (remains RSC) and passes rows to `TransactionTable`.
    - Behavior assertion (PAIR-01 link entry / PAIR-03 unlink toggle — 50-VALIDATION.md Manual-Only rows): unpaired row shows "Collega rimborso" → opens picker; paired row shows "Scollega" → removes the pair; badge appears inline on paired rows.
    - Command: `yarn build` succeeds and `yarn check:language` is clean.
  </acceptance_criteria>
  <done>Row actions toggle by pair state, the picker opens from "Collega rimborso", "Scollega" removes the pair, the inline badge renders on paired rows in natural order, and the page stays RSC; build + language check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Visual + interaction verification of the pairing flow</name>
  <files>components/transactions/transaction-table.tsx, components/transactions/counterpart-picker-dialog.tsx, components/transactions/transaction-pair-popover.tsx</files>
  <action>
    Operator-run verification (no code changes): after Tasks 1–2 build green, the operator exercises the end-to-end pairing flow in a running app per the steps in <how-to-verify> and confirms PAIR-01/PAIR-02/PAIR-03 behavior, including the dashboard netting and baseline-on-unlink. This surface has no headless render harness (50-VALIDATION.md Manual-Only), so human confirmation is the gate.
  </action>
  <verify>Operator confirms: (1) "Collega rimborso" opens the picker showing only opposite-sign, in-range, unpaired counterparts; (2) confirming pairs both rows and the 🔗 badge with the same signed net appears in natural order; (3) the popover shows counterpart details + working "Vai alla transazione"; (4) the dashboard nets the primary's month and excludes the secondary while unrelated unpaired transactions are unchanged; (5) "Scollega" removes the badge and restores independent aggregation.</verify>
  <what-built>
    The full transaction-pairing UX: "Collega rimborso"/"Scollega" row actions, the counterpart picker dialog (opposite-sign, ±90-day, excludes already-paired), the inline pair badge with signed net, and the popover with counterpart details + "Vai alla transazione". Backed by the Plan 03 actions and the Plan 04 netting. These surfaces have no headless render harness (50-VALIDATION.md Manual-Only) so a human confirms them.
  </what-built>
  <how-to-verify>
    1. Start the app and open the transactions list. Pick two opposite transactions in the same period (e.g. a -100 expense and a +50 credit, the dinner/Satispay scenario from 50-CONTEXT.md).
    2. On the larger one, open the row dropdown → "Collega rimborso". Confirm the picker opens, lists the +50 (and other opposite-sign, in-range, unpaired transactions), excludes same-sign and already-paired ones, and that widening the date range loads more. Select the +50 and confirm.
    3. Confirm both rows now show the 🔗 badge with the SAME signed net (€-50) and remain in their natural chronological position (no re-ordering/grouping). Click a badge → popover shows the counterpart description, amount, date, net effect, and a working "Vai alla transazione" link.
    4. Open the dashboard/overview: confirm the +50 is no longer counted as IN and the OUT/ristoranti total for the primary's month reflects the -50 net (not the full -100). Confirm an unrelated unpaired +100 credit still appears as IN independently (baseline unchanged).
    5. Back in the list, on either paired row open the dropdown → "Scollega". Confirm the badge disappears from both rows and the dashboard returns to treating both transactions independently (algebraic-sum baseline).
  </how-to-verify>
  <resume-signal>Type "approved" if the link/badge/popover/unlink flow and the dashboard netting behave as described, or describe the issue.</resume-signal>
  <acceptance_criteria>
    - Behavior assertion (PAIR-01): pairing two opposite transactions from "Collega rimborso" persists and both rows show the badge.
    - Behavior assertion (PAIR-02): the badge shows the signed net and the popover shows counterpart details + working link; rows stay in natural order.
    - Behavior assertion (PAIR-03): "Scollega" restores both transactions to independent aggregation; unrelated unpaired transactions are unaffected throughout.
  </acceptance_criteria>
  <done>Operator confirms the link → badge/popover → unlink flow and the dashboard netting (primary nets, secondary excluded, baseline restored on unlink) all behave correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → server action | The picker/badge invoke `createTransactionPairAction` / `deleteTransactionPairAction`; the UI is convenience only — all authorization is enforced server-side in Plan 03 (the client cannot bypass it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Information Disclosure / Tampering (IDOR) | picker list + pair/unlink invocations | mitigate | The picker is populated only by the `verifySession`-scoped `getEligibleCounterparts` (Plan 03), so no other user's transactions are ever shown; create/delete actions re-validate ownership server-side regardless of client input. The UI surface adds no new trust — it cannot forge ownership. Acceptance: picker uses the session-scoped DAL; actions are the Plan 03 ones. |
| T-50-02 | Tampering (double-link) | picker exclusion | mitigate | The picker excludes already-paired transactions (D-14) so the UX never offers a double-link; the server constraint + service guard remain the backstop. Acceptance: already-paired counterparts absent from the list. |
| T-50-08 | Injection / output handling | counterpart description/amount in popover | mitigate | Counterpart text is rendered through React (auto-escaped) in the Popover; amounts are formatted via Decimal, not interpolated as HTML. Acceptance: no `dangerouslySetInnerHTML` in the popover. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs (50-RESEARCH.md §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `yarn lint components/transactions/counterpart-picker-dialog.tsx components/transactions/transaction-pair-popover.tsx components/transactions/transaction-table.tsx` — clean.
- `yarn build` — succeeds.
- `yarn check:language` — clean (components touched).
- Operator checkpoint (Task 3) — link/badge/popover/unlink + dashboard netting confirmed.
</verification>

<success_criteria>
PAIR-01 (link from row action via picker), PAIR-02 (inline badge + net + popover, natural order), and PAIR-03 (unlink restores baseline) are fully usable in the transaction list; the page stays RSC with client dialog state; build + language checks clean; operator confirms the end-to-end flow and the dashboard netting.
</success_criteria>

<output>
Create `.planning/phases/50-transaction-pairing/50-05-SUMMARY.md` when done.
</output>
