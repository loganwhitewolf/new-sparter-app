---
phase: 50-transaction-pairing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/transaction-pairs-service.test.ts
  - tests/transaction-pairs-dal.test.ts
  - tests/dashboard-dal.test.ts
  - tests/transactions-dal.test.ts
autonomous: true
requirements: [PAIR-01, PAIR-02, PAIR-03]
tags: [transaction-pairing, netting, testing, drizzle]

must_haves:
  truths:
    - "A failing (RED) test suite asserts createPair ownership validation, primary resolution, and double-link rejection before any production code exists"
    - "A failing test asserts the dashboard netting contract: paired secondary excluded, net attributed to primary's direction/month"
    - "A failing test asserts TransactionListRow exposes pairedWithId + pairedNetAmount"
    - "A regression test pins the ADR 0004 algebraic-sum baseline for unpaired transactions"
  artifacts:
    - path: "tests/transaction-pairs-service.test.ts"
      provides: "RED unit tests for createPair / deletePairByTransactionId (ownership, primary resolution, double-link guard, unlink-restores-baseline)"
      contains: "createPair"
    - path: "tests/transaction-pairs-dal.test.ts"
      provides: "RED unit tests for getEligibleCounterparts filter logic"
      contains: "getEligibleCounterparts"
    - path: "tests/dashboard-dal.test.ts"
      provides: "Extended netting scenarios on buildOverviewData / getOverviewAmountTotals"
      contains: "isNotSecondary"
    - path: "tests/transactions-dal.test.ts"
      provides: "Extended select-shape assertions for pairedWithId / pairedNetAmount"
      contains: "pairedWithId"
  key_links:
    - from: "tests/transaction-pairs-service.test.ts"
      to: "lib/services/transaction-pairs.ts"
      via: "import { createPair, deletePairByTransactionId }"
      pattern: "from '@/lib/services/transaction-pairs'"
    - from: "tests/transaction-pairs-dal.test.ts"
      to: "lib/dal/transaction-pairs.ts"
      via: "import { getEligibleCounterparts }"
      pattern: "from '@/lib/dal/transaction-pairs'"
---

<objective>
Author the Wave 0 RED test scaffolds for transaction pairing so every downstream task in Phase 50 has an automated verification target before its production code exists. This satisfies Nyquist Dimension 8 (no task ships without a test) and locks the behavioral contract for the service layer, the counterpart-picker DAL, the dashboard netting helpers, and the transaction-list select shape.

Purpose: Tests fail RED now (modules under test do not exist yet) and turn GREEN as Plans 02–05 land. They are the executable acceptance criteria for PAIR-01/PAIR-02/PAIR-03.
Output: Two new test files + extensions to two existing test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-transaction-pairing/50-RESEARCH.md
@.planning/phases/50-transaction-pairing/50-VALIDATION.md
@.planning/phases/50-transaction-pairing/50-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces ONLY test files. It references (does not create) the symbols the rest of the phase will produce:
- `createPair`, `deletePairByTransactionId` (lib/services/transaction-pairs.ts — Plan 03)
- `getEligibleCounterparts` (lib/dal/transaction-pairs.ts — Plan 03)
- `isNotSecondary`, `effectiveAmount` (lib/dal/transaction-pairs-sql.ts — Plan 02)
- `pairedWithId`, `pairedNetAmount`, `pairedDescription`, `pairedOccurredAt` on `TransactionListRow` (lib/dal/transactions.ts — Plan 04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED service + counterpart-picker DAL test scaffolds</name>
  <files>tests/transaction-pairs-service.test.ts, tests/transaction-pairs-dal.test.ts</files>
  <read_first>
    - tests/expense-actions.test.ts — the mock-`db.select`/`db.insert` fixture-injection pattern to replicate for service-layer tests (per 50-RESEARCH.md §Validation Architecture, "mock db.select and db.insert similar to tests/expense-actions.test.ts")
    - tests/transactions-dal.test.ts — the DAL mocking style (`vi.mock('@/lib/db')`, `vi.mock('@/lib/dal/auth')`) to replicate for the counterpart-picker test
    - .planning/phases/50-transaction-pairing/50-RESEARCH.md §6 (Server Action Ownership Validation) — the exact createPair / deletePairByTransactionId signatures and primary-resolution rules these tests assert against
    - .planning/phases/50-transaction-pairing/50-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements) — the requirement→behavior rows this file must cover
  </read_first>
  <action>
    Create `tests/transaction-pairs-service.test.ts` importing `createPair` and `deletePairByTransactionId` from `@/lib/services/transaction-pairs` (module does not exist yet — import resolves once Plan 03 lands; until then the suite is RED). Mock `@/lib/db` with a controllable `db.select`/`db.insert`/`db.delete` chain and inject transaction-row fixtures (id, amount as DECIMAL string, occurredAt, userId).
    Cover these cases mapped to PAIR-01/PAIR-03 (T-50-01, T-50-02):
    (a) createPair throws "Non sei autorizzato..." when either transaction's userId !== session userId (IDOR / T-50-01);
    (b) createPair resolves the PRIMARY as the larger `|amount|` using Decimal.js (`toDecimal(a).abs()` vs `toDecimal(b).abs()`) and inserts it as `transactionAId`; assert with fixtures e.g. amount "-100.00" vs "+50.00" → primary is the -100.00 row;
    (c) tie-break: when `|amount|` is equal, the earlier `occurredAt` becomes primary;
    (d) createPair surfaces the unique-constraint / already-paired rejection as a thrown Error (T-50-02 double-link guard);
    (e) deletePairByTransactionId validates ownership before delete and removes the pair where the transaction appears on either FK side;
    (f) after delete, the helper issues a delete with an `or(eq(a_id), eq(b_id))` predicate (unlink-restores-baseline contract, PAIR-03).
    Use Decimal-string fixtures throughout — never native JS arithmetic in assertions; assert exact string/Decimal equality for the primary-resolution math.
    Create `tests/transaction-pairs-dal.test.ts` importing `getEligibleCounterparts` from `@/lib/dal/transaction-pairs`. Mock `@/lib/db` and `@/lib/dal/auth` (`verifySession` → `{ userId }`). Assert the WHERE predicate composition: eq(userId), opposite-sign filter (reference amount negative → counterpart amount > 0 and vice versa), date range gte/lte, ne(self id), and the NOT EXISTS already-paired exclusion (D-13, D-14). Since the query is mocked, assert the call captured the expected filter conditions (inspect the captured `.where` argument or the SQL fragment), not live rows.
    Do NOT place implementation code in this plan — only tests. Italian strings appearing in assertions are user-facing error copy and are allowed; keep all test names, comments, and identifiers in English (run `yarn check:language`).
  </action>
  <verify>
    <automated>yarn test tests/transaction-pairs-service.test.ts tests/transaction-pairs-dal.test.ts 2>&1 | grep -qiE "Cannot find module|failed|fail" && echo "RED-OK (modules absent / assertions fail as expected)" || echo "UNEXPECTED-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/transaction-pairs-service.test.ts` contains `from '@/lib/services/transaction-pairs'` and references both `createPair` and `deletePairByTransactionId`.
    - Source assertion: `tests/transaction-pairs-dal.test.ts` contains `from '@/lib/dal/transaction-pairs'` and references `getEligibleCounterparts`.
    - Behavior assertion (maps to 50-VALIDATION.md rows PAIR-01 ownership / primary-resolution / double-link, PAIR-03 unlink): test names exist for ownership rejection, `|amount|` primary resolution, tie-break by occurredAt, double-link rejection, and unlink ownership + or-predicate delete.
    - Command: the two suites are RED (modules not yet implemented) — `yarn check:language` passes on both new files.
  </acceptance_criteria>
  <done>Both new test files exist, import the not-yet-created modules, encode the PAIR-01/PAIR-03 behavioral contract, and run RED. `yarn check:language` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend dashboard-dal + transactions-dal tests with netting + select-shape contract</name>
  <files>tests/dashboard-dal.test.ts, tests/transactions-dal.test.ts</files>
  <read_first>
    - tests/dashboard-dal.test.ts — the existing `buildOverviewData` / aggregation builder test style to extend (do not rewrite existing assertions; the ADR 0004 baseline cases here become the regression guard)
    - tests/transactions-dal.test.ts — the existing `transactionListSelect` / `TransactionListRow` shape assertions to extend
    - .planning/phases/50-transaction-pairing/50-RESEARCH.md §3 (Netting SQL Strategy) and §4 (Transaction List Integration) — the `isNotSecondary()` / `effectiveAmount()` contract and the four new select fields these tests assert
    - .planning/phases/50-transaction-pairing/50-PATTERNS.md (lib/dal/transaction-pairs-sql.ts section) — exact helper signatures the netting test references
  </read_first>
  <action>
    Extend `tests/dashboard-dal.test.ts`: import `isNotSecondary` and `effectiveAmount` from `@/lib/dal/transaction-pairs-sql` (module absent until Plan 02 — keeps the new cases RED). Add a `describe('transaction pairing netting')` block asserting (PAIR-03): the netting helpers produce SQL fragments containing the expected tokens — `isNotSecondary()` yields a `NOT EXISTS` fragment referencing `transaction_pair` / `transaction_b_id`; `effectiveAmount()` yields a `CASE WHEN EXISTS` fragment that adds the secondary's amount (`transaction_a_id` correlation, `::numeric`). Assert by inspecting the compiled `.sql` of the returned Drizzle fragment (the helpers are pure fragment factories per 50-RESEARCH.md §Validation Architecture). Add a scenario-level case for `buildOverviewData` (or the existing builder under test) showing that when a primary/secondary fixture pair is present, `totalOut` reflects the algebraic net (primary.amount + secondary.amount) and the secondary does not contribute to its own month — pin expected values with Decimal-string fixtures (e.g. cena -100 + ricarica +50 → net -50). Keep ALL existing unpaired-transaction assertions intact as the ADR 0004 regression guard (PAIR-03 "unpaired unaffected").
    Extend `tests/transactions-dal.test.ts`: add assertions that `transactionListSelect` exposes keys `pairedWithId`, `pairedNetAmount`, `pairedDescription`, `pairedOccurredAt`, and that `TransactionListRow` type permits them as nullable (PAIR-02). Assert the `pairedNetAmount` select fragment contains a `::numeric` addition (signed Decimal net via SQL). These reference fields that Plan 04 adds — RED until then.
    Tests only; no production edits. English identifiers/comments; `yarn check:language` clean.
  </action>
  <verify>
    <automated>yarn test tests/dashboard-dal.test.ts tests/transactions-dal.test.ts 2>&1 | grep -qiE "Cannot find module|failed|fail|pairedWithId|isNotSecondary" && echo "RED-OK (new netting/select cases fail pending impl)" || echo "CHECK-OUTPUT"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/dashboard-dal.test.ts` contains `isNotSecondary` and `effectiveAmount` imported from `@/lib/dal/transaction-pairs-sql`.
    - Source assertion: `tests/transactions-dal.test.ts` references all four new keys `pairedWithId`, `pairedNetAmount`, `pairedDescription`, `pairedOccurredAt`.
    - Behavior assertion (50-VALIDATION.md rows PAIR-03 netting + PAIR-02 select-shape): a netting scenario pins `totalOut` to the algebraic net of a primary/secondary fixture pair, and the pre-existing unpaired baseline assertions remain unchanged (regression guard).
    - Command: `yarn check:language` passes; the newly added cases are RED while the pre-existing baseline cases stay GREEN.
  </acceptance_criteria>
  <done>Both existing test files gain netting + select-shape cases that fail RED pending Plans 02/04, while their pre-existing ADR 0004 baseline assertions remain GREEN. `yarn check:language` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test-only plan) | This plan adds no runtime surface; it encodes the security assertions the production code must satisfy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Information Disclosure / Tampering (IDOR) | `createPair` / `deletePairByTransactionId` ownership | mitigate | This plan authors the RED test asserting ownership rejection (both tx.userId must equal session userId); Plan 03 makes it GREEN |
| T-50-02 | Tampering (double-link integrity) | `transaction_pair` uniqueness | mitigate | This plan authors the RED test asserting createPair rejects an already-paired transaction; Plans 02 (unique constraint) + 03 (service guard) make it GREEN |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (50-RESEARCH.md §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `yarn test tests/transaction-pairs-service.test.ts tests/transaction-pairs-dal.test.ts` — RED (modules absent).
- `yarn test tests/dashboard-dal.test.ts tests/transactions-dal.test.ts` — new cases RED, pre-existing baseline GREEN.
- `yarn check:language` — clean on all four files.
</verification>

<success_criteria>
All four test files exist and encode the PAIR-01/PAIR-02/PAIR-03 behavioral contract; new cases run RED; pre-existing baseline assertions stay GREEN; `yarn check:language` passes. Nyquist Dimension 8 satisfied for every downstream task.
</success_criteria>

<output>
Create `.planning/phases/50-transaction-pairing/50-01-SUMMARY.md` when done.
</output>
