---
phase: 46-direction-nature-schema
plan: 03
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - scripts/seed-data.ts
  - scripts/seed.ts
  - scripts/seed-extras.ts
autonomous: true
requirements: [DATA-01, DATA-02]
tags: [seed, lookup-table, nature, direction, build-survival]

must_haves:
  truths:
    - "scripts/seed-data.ts exports a `directions` array with exactly 4 rows (in, out, allocation, transfer) carrying the locked attribute semantics"
    - "scripts/seed-data.ts exports a `natures` array with exactly 8 rows, each mapped to a direction (income, income_extraordinary → in; essential, discretionary, debt → out; transfer → transfer; savings, investment → allocation)"
    - "The new `directions`/`natures` baseline blocks live in seed-data.ts (D-07) and adding them does NOT violate the additive-seed rule because they are brand-new tables, not already-shipped shapes (D-08)"
    - "scripts/seed.ts imports the direction/nature tables and the directions/natures arrays and inserts directions BEFORE natures via onConflictDoNothing()"
    - "scripts/seed.ts compiles against the new schema — no reference to the removed amountSign / category.type columns survives in the insert value objects"
    - "scripts/seed-extras.ts compiles against the new schema — the existing dedupe predicate at ~L334 no longer references categorizationPattern.amountSign (sign-agnostic, ADR 0012); NO new nature_id-backfill STEP is added (D-09)"
    - "No drizzle-kit generate / push and no migrate or DB apply is run (D-06)"
  artifacts:
    - path: "scripts/seed-data.ts"
      provides: "directions (4) + natures (8) baseline lookup arrays"
      contains: "export const directions"
    - path: "scripts/seed.ts"
      provides: "FK-ordered insert of directions then natures; build-survival repair of removed-column writes"
      contains: "db.insert(direction)"
    - path: "scripts/seed-extras.ts"
      provides: "build-survival repair of the existing pattern-dedupe predicate (drops amountSign); no new backfill STEP"
      contains: "sign-agnostic"
  key_links:
    - from: "scripts/seed.ts directions/natures insert"
      to: "lib/db/schema.ts direction / nature tables"
      via: "db.insert(...).values(... as Array<typeof X.$inferInsert>).onConflictDoNothing()"
      pattern: "db\\.insert\\(nature\\)"
    - from: "scripts/seed-data.ts natures[].directionId"
      to: "scripts/seed-data.ts directions[].id"
      via: "literal id reference (1=in, 2=out, 3=allocation, 4=transfer)"
      pattern: "directionId"
---

<objective>
Author the static `direction` (4 rows) and `nature` (8 rows) baseline lookup data as new arrays in `scripts/seed-data.ts`, wire `scripts/seed.ts` to insert them in FK order (directions before natures) via `onConflictDoNothing()`, and repair BOTH seed scripts (`scripts/seed.ts` AND `scripts/seed-extras.ts`) so they compile against the new schema authored in plan 46-01 (the seed runner currently writes the removed `category.type` and `categorizationPattern.amountSign` columns, and `seed-extras.ts` has a live SQL dedupe predicate referencing the removed `categorizationPattern.amountSign`).

Purpose: DATA-01 (4 directions) and DATA-02 (8 natures, each with a direction) require the canonical reference rows to be DEFINED in every environment's baseline seed. They are foundational lookup data — present everywhere, idempotent — not a backfill of existing rows (D-07). This plan also owns the seed-side of Phase 46's "green build" definition-of-done: 46-02 Task 3 runs the repo-wide `yarn build`, but the union build is green only once BOTH seed scripts stop referencing the removed columns — `tsconfig.json` (`include: **/*.ts`) puts `scripts/seed-extras.ts` in typecheck scope, so its stale `amountSign` reference at ~L334 would fail `yarn tsc --noEmit` / `yarn build`. That repair lives here, alongside the `seed.ts` repair, so all seed-script build-survival edits stay together.

Output: `scripts/seed-data.ts` with new `directions`/`natures` arrays; `scripts/seed.ts` importing and inserting them in FK order and compiling clean; `scripts/seed-extras.ts` with its existing pattern-dedupe predicate made sign-agnostic and compiling clean. NO migration generated, NO DB apply (D-06) — the rows physically materialize when Phase 48 runs migrate + seed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-direction-nature-schema/46-CONTEXT.md
@.planning/phases/46-direction-nature-schema/46-PATTERNS.md
@.planning/phases/46-direction-nature-schema/46-01-PLAN.md
@docs/adr/0012-direction-derived-from-nature-allocation.md
@.planning/nature-remapping-WORKING.md
@lib/db/schema.ts
@lib/utils/nature-labels.ts
@scripts/seed-data.ts
@scripts/seed.ts
@scripts/seed-extras.ts

## D-09 boundary clarification (READ before touching seed-extras.ts)

D-09 reserves `scripts/seed-extras.ts` for **Phase 47's nature_id backfill on existing rows** — i.e. ADDING a NEW backfill STEP that UPDATEs `sub_category.nature_id` / `user_subcategory_override.nature_id` is Phase 47 work and is FORBIDDEN here.

D-09 does NOT forbid **repairing an existing step's reference to a column that 46-01 removed**. The dedupe predicate at ~`seed-extras.ts:334` references `categorizationPattern.amountSign` (and `amount_sign` in its SQL subquery). After 46-01 drops that column, this is a hard typecheck break — fixing it is a **D-05 build-survival edit**, the same class as the `seed.ts` removed-column repair already in this plan, and the same class as the `seed.ts` `amountSign` deletion. The executor MUST make this edit and MUST NOT skip the file citing D-09. The line is explicit: no new STEP is added to the `STEPS` array; an existing predicate is corrected in place.
</context>

<artifacts_produced>
## Artifacts this plan produces (seed authorship + seed-script build-survival — plan 03)

**New baseline lookup arrays in `scripts/seed-data.ts` (D-07, D-08 — new tables, NOT shipped shapes, so the additive-seed rule is satisfied):**

- `directions` — 4 rows, explicit `id`, mirroring the `categories`/`platforms` literal-array idiom (`seed-data.ts:3+`, `seed-data.ts:1141+`). Columns mirror the `direction` pgTable authored in 46-01: `id`, `code`, `labelIt`, `netWorthEffect`, `includedInTotals`, `shownSeparately`, `hidden`, `displayOrder`, `color`. The locked attribute matrix (D, CONTEXT.md L100-101):

  | id | code | netWorthEffect | includedInTotals | shownSeparately | hidden | displayOrder |
  |----|------|----------------|------------------|-----------------|--------|--------------|
  | 1 | in | increase | true | false | false | 0 |
  | 2 | out | decrease | true | false | false | 1 |
  | 3 | allocation | neutral | false | true | false | 2 |
  | 4 | transfer | neutral | false | false | true | 3 |

- `natures` — 8 rows (D-01, NOT 9), explicit `id`, each carrying `directionId` referencing a `directions[].id`. Columns mirror the `nature` pgTable: `id`, `code`, `directionId`, `labelIt`, `color`, `displayOrder`. The locked code→direction mapping (CONTEXT.md L99, D-01):

  | code | directionId (→ direction code) |
  |------|--------------------------------|
  | income | 1 (in) |
  | income_extraordinary | 1 (in) |
  | essential | 2 (out) |
  | discretionary | 2 (out) |
  | debt | 2 (out) |
  | transfer | 4 (transfer) |
  | savings | 3 (allocation) |
  | investment | 3 (allocation) |

  `labelIt` / `color` cross-checked against `lib/utils/nature-labels.ts` `NATURE_LABELS` / `NATURE_COLORS` for the 8 v2.0 codes — note the rename mapping: `savings` reuses the old `extraordinary` label/color, `investment` reuses the old `financial` label/color (per `nature-remapping-WORKING.md`). Uncategorized is `nature_id NULL` — there is NO 9th row (D-02).

**Modified seed runner — `scripts/seed.ts`:**
- New imports: `direction`, `nature` (from `../lib/db/schema`) and `directions`, `natures` (from `./seed-data`).
- New insert steps: `db.insert(direction)...onConflictDoNothing()` then `db.insert(nature)...onConflictDoNothing()` (FK order — natures reference directions).
- Build-survival repair: the existing `db.insert(category)` value mapping must no longer write the removed `category.type` column, and the existing `db.insert(categorizationPattern)` value mapping must no longer write the removed `amountSign` column (`seed.ts:132`). The `excludeFromTotals` UPDATE step (`seed.ts:79-83`) is RETAINED (D-10).

**Modified additive-seed script — `scripts/seed-extras.ts` (build-survival ONLY, NO new STEP — D-05 not D-09):**
- The existing pattern-dedupe predicate at ~`seed-extras.ts:334` references the removed `categorizationPattern.amountSign` column inside a `sql\`...\`` template: `(${categorizationPattern.pattern}, ${categorizationPattern.amountSign}) IN (SELECT pattern, amount_sign FROM categorization_pattern WHERE sub_category_id = ${negozioDiQuartiereId})`. Per ADR 0012 the pattern unique key is now `(pattern, subCategoryId)`, so the dedupe must become sign-agnostic: drop the `amountSign` component (and `amount_sign` from the SQL subquery), leaving the predicate matching on `pattern` within the already-scoped target subcategory.
- The stale comment at `seed-extras.ts:324-325` (which describes the 3-column unique constraint incl. `amountSign`) is updated to the 2-column key, and an English inline comment `// Phase 46: patterns are sign-agnostic (amount_sign removed, ADR 0012)` is added.
- NO new STEP is added to the `STEPS` array. The nature_id backfill on existing rows remains Phase 47 (D-09).

**NOT a new backfill STEP:** the `seed-extras.ts` edit is a build-survival repair of an existing step's reference to a removed column (D-05), NOT the Phase 47 nature_id population (which is still untouched here, per D-09). No migration files. No `lib/db/schema.ts` (owned by 46-01).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Define the directions (4) + natures (8) baseline lookup arrays in seed-data.ts</name>
  <read_first>
    - scripts/seed-data.ts (the file being modified — `categories` literal-array idiom L3-30, `platforms` L1141+, the `as <Type>` cast idiom used on `amountSign` L1269/L1276 — model the new arrays on these)
    - .planning/phases/46-direction-nature-schema/46-01-PLAN.md (artifacts_produced section — the exact column set + mapped db names for the `direction` and `nature` pgTables your row objects must match: direction has code/labelIt/netWorthEffect/includedInTotals/shownSeparately/hidden/displayOrder/color; nature has code/directionId/labelIt/color/displayOrder)
    - .planning/phases/46-direction-nature-schema/46-CONTEXT.md (D-01 the 8 codes + direction mapping L99; D-02 null=uncategorized; specifics L100-101 attribute semantics)
    - docs/adr/0012-direction-derived-from-nature-allocation.md (the 4 directions, 8 natures, net_worth_effect / included_in_totals / shown_separately / hidden semantics per direction)
    - .planning/nature-remapping-WORKING.md (final nature codes + direction mapping + the financial→investment / extraordinary→savings rename, for label/color sourcing)
    - lib/utils/nature-labels.ts (NATURE_LABELS L12 + NATURE_COLORS L38 — source labelIt/color for the 8 codes; apply the rename: savings ← extraordinary label/color, investment ← financial label/color)
  </read_first>
  <action>
    Add two new exported arrays near the top of `scripts/seed-data.ts`, modeled on the existing `categories` literal-array shape (plain array of row objects with explicit `id`, `as const`-style literals; introduce a narrow `NetWorthEffect = "increase" | "decrease" | "neutral"` type and use the `... as NetWorthEffect` cast idiom mirroring the existing `amountSign: "negative" as AmountSign` pattern if narrowing is needed for the seed-runner cast). Add a single-line English developer comment block header above each array (mirror the `// CATEGORIZATION PATTERNS` comment style at L1265).

    `directions` — exactly 4 rows, in this id/code/attribute matrix (locked by CONTEXT.md L100-101 and ADR 0012):
    - id 1, code `in`, netWorthEffect `increase`, includedInTotals `true`, shownSeparately `false`, hidden `false`, displayOrder 0
    - id 2, code `out`, netWorthEffect `decrease`, includedInTotals `true`, shownSeparately `false`, hidden `false`, displayOrder 1
    - id 3, code `allocation`, netWorthEffect `neutral`, includedInTotals `false`, shownSeparately `true`, hidden `false`, displayOrder 2
    - id 4, code `transfer`, netWorthEffect `neutral`, includedInTotals `false`, shownSeparately `false`, hidden `true`, displayOrder 3
    `labelIt` for the directions are Italian product surfaces — use: in → `Entrate`, out → `Uscite`, allocation → `Accantonamenti`, transfer → `Trasferimenti` (mirroring 46-PATTERNS.md L137-140; adjust only if nature-labels.ts dictates a more canonical string). `color` is a nullable hex string — source a sensible value per direction or leave null; not load-bearing for DATA-01.

    `natures` — exactly 8 rows (NOT 9 — D-01), explicit `id` 1-8, each with a `directionId` referencing the directions above by id. The code→directionId mapping is LOCKED: `income` → 1, `income_extraordinary` → 1, `essential` → 2, `discretionary` → 2, `debt` → 2, `transfer` → 4, `savings` → 3, `investment` → 3. For each row, source `labelIt` and `color` from `lib/utils/nature-labels.ts` for that code, applying the documented renames (`savings` takes the old `extraordinary` label `Straordinario` / color `#fbbf24`; `investment` takes the old `financial` label `Finanziario` / color `#a78bfa`); the other six codes already exist as keys in NATURE_LABELS/NATURE_COLORS. Set `displayOrder` ascending 0-7 in the order listed. Do NOT add an uncategorized/sentinel 9th row — uncategorized is represented by `nature_id NULL` (D-02).

    Leave every existing array (`categories`, `subCategories`, `platforms`, `categorizationPatterns`) and the `AmountSign` type untouched in this task — Task 2 owns any seed-runner-side adjustments. Domain values (codes, Italian labels, hex colors) are intentional product/data surfaces; developer comments stay English (CLAUDE.md language convention).
  </action>
  <verify>
    <automated>grep -q 'export const directions' scripts/seed-data.ts && grep -q 'export const natures' scripts/seed-data.ts && [ "$(node -e "const s=require('fs').readFileSync('scripts/seed-data.ts','utf8'); const m=s.match(/export const directions\s*=\s*\[([\s\S]*?)\];/); process.stdout.write(String((m[1].match(/code:/g)||[]).length))")" = "4" ] && [ "$(node -e "const s=require('fs').readFileSync('scripts/seed-data.ts','utf8'); const m=s.match(/export const natures\s*=\s*\[([\s\S]*?)\];/); process.stdout.write(String((m[1].match(/code:/g)||[]).length))")" = "8" ] && [ "$(node -e "const s=require('fs').readFileSync('scripts/seed-data.ts','utf8'); const m=s.match(/export const natures\s*=\s*\[([\s\S]*?)\];/); process.stdout.write(String((m[1].match(/directionId:/g)||[]).length))")" = "8" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-data.ts contains `export const directions` with exactly 4 row objects (4 `code:` keys inside the array literal)
    - scripts/seed-data.ts contains `export const natures` with exactly 8 row objects (8 `code:` keys, 8 `directionId:` keys)
    - the 4 direction codes are exactly `in`, `out`, `allocation`, `transfer` with the locked attribute matrix (in/out includedInTotals=true; allocation shownSeparately=true & includedInTotals=false; transfer hidden=true & includedInTotals=false)
    - the 8 nature codes are exactly `income`, `income_extraordinary`, `essential`, `discretionary`, `debt`, `transfer`, `savings`, `investment` — no `operational`, `financial`, `extraordinary`, or `uncategorized` row
    - each nature's `directionId` matches the locked mapping (income/income_extraordinary→1, essential/discretionary/debt→2, savings/investment→3, transfer→4)
    - column keys on direction/nature rows match the 46-01 pgTable property names (camelCase: labelIt, netWorthEffect, includedInTotals, shownSeparately, displayOrder, directionId)
    - no existing array (categories/subCategories/platforms/categorizationPatterns) was modified in this task
  </acceptance_criteria>
  <done>scripts/seed-data.ts exports a 4-row `directions` array and an 8-row `natures` array with the locked codes, attribute semantics, and direction mapping; uncategorized has no row; existing arrays are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire seed.ts to insert directions then natures (FK order) and repair removed-column writes</name>
  <read_first>
    - scripts/seed.ts (the file being modified — schema import L9, seed-data import block L18-23, the category insert L72, the excludeFromTotals UPDATE L79-83, the platform setval idiom L90, the categorizationPattern insert mapping L125-139 incl. the `amountSign: patternSeed.amountSign` write at L132)
    - scripts/seed-data.ts (the new `directions`/`natures` arrays from Task 1; the `categories` array still carries a `type` field at L8 etc.; `categorizationPatterns` still carries `amountSign` at L1276)
    - .planning/phases/46-direction-nature-schema/46-01-PLAN.md (confirms `category.type` and `categorizationPattern.amountSign` columns are REMOVED from schema.ts; the new `direction`/`nature` tables and their column names)
    - .planning/phases/46-direction-nature-schema/46-PATTERNS.md (`scripts/seed.ts — consume the new arrays` section L150-156: insert idiom `db.insert(X).values(... as Array<typeof X.$inferInsert>).onConflictDoNothing()`, directions before natures, optional setval like platform_id_seq)
    - .planning/phases/46-direction-nature-schema/46-CONTEXT.md (D-06 no DB apply; D-09 no NEW seed-extras backfill STEP; D-10 excludeFromTotals retained)
  </read_first>
  <action>
    Extend the imports: add `direction` and `nature` to the schema import at `scripts/seed.ts:9` (the `from '../lib/db/schema'` line), and add `directions` and `natures` to the `./seed-data` import block at `scripts/seed.ts:18-23`.

    Add two new insert steps inside the `seed()` function, placed BEFORE the existing categories insert (these lookups are referenced by later phases; FK order requires directions before natures since `nature.directionId` references `direction.id`). Mirror the `seed.ts:72` idiom exactly:
    - `await db.insert(direction).values(directions as Array<typeof direction.$inferInsert>).onConflictDoNothing()` with a `console.log('Seeding directions...')` and a count log following the established style.
    - `await db.insert(nature).values(natures as Array<typeof nature.$inferInsert>).onConflictDoNothing()` with matching logs.
    Optionally reset the id sequences after insert with the `setval` idiom used for `platform_id_seq` at `seed.ts:90` (`direction_id_seq`, `nature_id_seq`) — recommended since the rows use explicit ids; this is Claude's discretion.

    Repair the build-survival breakage introduced by 46-01 (this is part of the seed-side of the wave-2 green-build gate that 46-02 Task 3 asserts repo-wide; the `seed-extras.ts` half is Task 3 of this plan):
    - In the `db.insert(categorizationPattern)` value mapping (`seed.ts:125-139`), DELETE the `amountSign: patternSeed.amountSign` line (`seed.ts:132`) — the column no longer exists. Add an English inline comment `// Phase 46: patterns are sign-agnostic (amount_sign column removed, ADR 0012)`. Leave every other field (pattern, subCategoryId, confidence, priority, description, isActive, userId) intact.
    - In the `db.insert(category)` step (`seed.ts:72`), the `categories` literal still carries a `type` field that no longer maps to a column. Ensure the inserted value objects do not write `type`: either map the categories array to objects omitting `type` before insert, or remove the now-dead `type` field from the `categories` literal in `seed-data.ts`. Choose the approach that keeps `yarn tsc --noEmit` clean for `scripts/seed.ts` against `typeof category.$inferInsert`; prefer mapping-out at the insert site to keep the literal array byte-stable for other consumers/tests if any depend on it (grep for `categories` importers first — Claude's discretion).

    Do NOT touch the `excludeFromTotals` UPDATE step (`seed.ts:79-83`) — the column is RETAINED this phase (D-10). Do NOT add any new STEP to `scripts/seed-extras.ts` (D-09) — the build-survival repair of its existing predicate is Task 3, not a new backfill step. Do NOT run `drizzle-kit generate`, `drizzle-kit push`, `scripts/migrate.ts`, `yarn db:migrate`, `yarn db:seed`, or any command that touches a live database (D-06) — this task edits source only.
  </action>
  <verify>
    <automated>grep -q 'db.insert(direction)' scripts/seed.ts && grep -q 'db.insert(nature)' scripts/seed.ts && [ "$(grep -n 'db.insert(direction)' scripts/seed.ts | cut -d: -f1)" -lt "$(grep -n 'db.insert(nature)' scripts/seed.ts | cut -d: -f1)" ] && [ "$(grep -cE 'amountSign|amount_sign' scripts/seed.ts)" = "0" ] && grep -q 'excludeFromTotals' scripts/seed.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed.ts imports `direction` and `nature` from `../lib/db/schema` and `directions`/`natures` from `./seed-data`
    - scripts/seed.ts contains `db.insert(direction)` and `db.insert(nature)`, with the direction insert appearing on an earlier line than the nature insert (FK order)
    - both new inserts use `.onConflictDoNothing()` and the `... as Array<typeof X.$inferInsert>` cast idiom
    - scripts/seed.ts contains NO `amountSign` / `amount_sign` reference (count == 0)
    - the category insert no longer writes the removed `type` column (no `type:` survives in the category insert value objects)
    - scripts/seed.ts STILL contains the `excludeFromTotals` UPDATE step (D-10 retained)
    - scripts/seed-extras.ts is NOT given a new STEP in this task (the existing-predicate repair is Task 3 — D-09 forbids only a NEW backfill STEP)
    - no `drizzle-kit` / migrate / db:seed command was run (D-06)
  </acceptance_criteria>
  <done>scripts/seed.ts imports and inserts directions-then-natures via onConflictDoNothing, no longer writes the removed amountSign/category.type columns, retains the excludeFromTotals UPDATE — without any DB apply or migration.</done>
</task>

<task type="auto">
  <name>Task 3: Build-survival repair of the existing seed-extras.ts pattern-dedupe predicate (drop amountSign — NO new STEP)</name>
  <read_first>
    - scripts/seed-extras.ts (the file being modified — the imports near the top incl. `categorizationPattern`, `sql`, `and`, `eq`; the pattern-migration block ~L323-346; specifically the stale comment ~L324-325 describing the 3-column unique constraint, and the live dedupe predicate at ~L334 `sql\`(${categorizationPattern.pattern}, ${categorizationPattern.amountSign}) IN (SELECT pattern, amount_sign FROM categorization_pattern WHERE sub_category_id = ${negozioDiQuartiereId})\``; the surrounding `database.delete(categorizationPattern).where(and(eq(categorizationPattern.subCategoryId, prodottiFreschiId), <predicate>))`)
    - .planning/phases/46-direction-nature-schema/46-01-PLAN.md (confirms `categorizationPattern.amountSign` is REMOVED and the unique key becomes `(pattern, subCategoryId)`)
    - docs/adr/0012-direction-derived-from-nature-allocation.md (patterns become sign-agnostic; unique key `(pattern, subCategoryId)`)
    - .planning/phases/46-direction-nature-schema/46-CONTEXT.md (D-05 build-survival edits allowed; D-06 no DB apply; D-09 — the boundary: NO new nature_id backfill STEP, but repairing an existing step's reference to a removed column IS allowed; D-10 unrelated)
    - the "D-09 boundary clarification" block in this plan's <context> (read it before editing — it states why this file MUST be touched despite D-09)
  </read_first>
  <action>
    D-09 BOUNDARY (do NOT skip this file): D-09 forbids adding a NEW nature_id-backfill STEP to `seed-extras.ts` (that is Phase 47). It does NOT forbid repairing an EXISTING step's reference to a column 46-01 removed — that is a D-05 build-survival edit, identical in class to the `seed.ts` removed-column repair in Task 2. `tsconfig.json` (`include: **/*.ts`) puts `scripts/seed-extras.ts` in typecheck scope, so the stale `categorizationPattern.amountSign` reference at ~L334 WILL fail `yarn tsc --noEmit` / `yarn build` (the wave-2 definition-of-done gate) if left unrepaired. Make the edit.

    Edit ONLY the existing pattern-dedupe predicate (~`seed-extras.ts:329-336`, the `database.delete(categorizationPattern).where(and(...))` inside the prodotti-freschi → negozio-di-quartiere migration block):
    - Drop the `amountSign` component from the `sql\`...\`` dedupe predicate so it becomes sign-agnostic. The unique key is now `(pattern, subCategoryId)` (ADR 0012), and the outer `eq(categorizationPattern.subCategoryId, prodottiFreschiId)` already scopes the delete to one subcategory — so the dedupe only needs to match on `pattern` against the target subcategory's patterns. Rewrite the predicate to the equivalent of: `sql\`${categorizationPattern.pattern} IN (SELECT pattern FROM categorization_pattern WHERE sub_category_id = ${negozioDiQuartiereId})\``. Remove the `, ${categorizationPattern.amountSign})` tuple wrapping and the `, amount_sign` column from the subquery SELECT.
    - Update the stale comment at ~L324-325 (currently `unique("categorization_pattern_unique").on(pattern, subCategoryId, amountSign)`) to reflect the new 2-column key `(pattern, subCategoryId)`.
    - Add an English inline comment `// Phase 46: patterns are sign-agnostic (amount_sign removed, ADR 0012)` at the predicate.
    - If `categorizationPattern.amountSign` is referenced nowhere else in the file after this edit, no further change is needed; if an unused import surfaces, leave imports as-is unless tsc/lint flags them (do not over-edit).

    Do NOT add any new entry to the `STEPS` array. Do NOT add a nature_id backfill (Phase 47, D-09). Do NOT touch any other step (the expense-migration steps L303-321, the deactivation step L348-354, the Trasferimenti/Rimborsi reorg at L357+) — they do not reference removed columns. Do NOT run `drizzle-kit generate`, `drizzle-kit push`, `scripts/migrate.ts`, `yarn db:migrate`, `yarn db:seed`, `yarn db:seed-extras`, or any live-DB command (D-06) — this edits source only.
  </action>
  <verify>
    <automated>[ "$(grep -cE 'amountSign|amount_sign' scripts/seed-extras.ts)" = "0" ] && grep -q 'sign-agnostic' scripts/seed-extras.ts && grep -q 'db.insert(direction)' scripts/seed.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-extras.ts contains NO `amountSign` / `amount_sign` reference (count == 0)
    - the dedupe predicate now matches on `(pattern, subCategoryId)` only (sign-agnostic) — the `categorizationPattern.amountSign` term and the `amount_sign` subquery column are gone
    - the stale 3-column-unique comment is corrected to the 2-column key, and an English `// Phase 46: patterns are sign-agnostic (amount_sign removed, ADR 0012)` comment is present
    - NO new STEP was added to the `STEPS` array (D-09 — only an existing predicate was repaired)
    - no other step in seed-extras.ts was modified
    - `yarn tsc --noEmit` reports NO error originating in `scripts/seed-extras.ts` (the file compiles clean against the new schema)
    - no `drizzle-kit` / migrate / db:seed / db:seed-extras command was run (D-06)
  </acceptance_criteria>
  <done>The existing pattern-dedupe predicate in scripts/seed-extras.ts is repaired to be sign-agnostic (no `amountSign`/`amount_sign`), the stale unique-constraint comment is corrected, no new STEP is added (D-09 honored), and seed-extras.ts typechecks clean against the new schema — without any DB apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan authors static reference data in `scripts/seed-data.ts` and edits the operator-run seed scripts `scripts/seed.ts` / `scripts/seed-extras.ts`. No request handler, no user-supplied input, no auth path, no network surface is added. The seed scripts are operator-invoked (`yarn db:seed` / `yarn db:seed-extras`) and are NOT executed in this phase (D-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-07 | Tampering / data integrity (FK order) | `natures[].directionId` → `directions[].id` | mitigate | The seed runner inserts `directions` BEFORE `natures` so the NOT NULL `nature.direction_id` FK (authored in 46-01 with `onDelete: restrict`) always resolves. The directionId literal mapping is asserted against the locked matrix in Task 1 verify. Physically enforced when Phase 48 applies the migration + runs seed. |
| T-46-08 | Tampering / idempotency | re-running `yarn db:seed` against an existing DB | accept | Both new inserts use `.onConflictDoNothing()` on the unique `code`, matching the established baseline-seed contract (`seed.ts:72`). Re-seeding is a no-op for these tables; explicit ids + optional `setval` keep the sequences consistent. This is the project's standard additive-seed posture (CLAUDE.md). |
| T-46-09 | Correctness / removed-column reference | `category.type` / `amountSign` writes in `seed.ts` + `amountSign` dedupe predicate in `seed-extras.ts` | mitigate | Both seed scripts currently reference columns removed by 46-01; left unrepaired they break `yarn tsc --noEmit` / `yarn build`. Task 2 strips the `seed.ts` writes; Task 3 makes the `seed-extras.ts` dedupe sign-agnostic. The typecheck gate asserts both seed scripts compile clean. No data exposure — these are taxonomy strings and a migration dedupe predicate. The dedupe semantic change (sign-agnostic) is the locked ADR 0012 intent; its physical re-run is Phase 48, not this phase. |

**No HIGH-severity threats** in this plan. Rationale (ASVS L1): it produces static Italian-taxonomy lookup data and edits operator-only seed scripts; it executes no SQL in this phase (D-06), opens no network or auth surface, and accepts no user input. The realistic surface — FK insert ordering, idempotency, and removed-column references — is addressed above and physically exercised only when Phase 48 runs migrate + seed. No blocking checkpoint required.

T-46-SC (supply-chain): N/A — this plan installs no npm/pip/cargo packages.
</threat_model>

<verification>
After all three tasks, `scripts/seed-data.ts` defines the 4 direction + 8 nature baseline rows, `scripts/seed.ts` inserts them in FK order and compiles against the new schema, and `scripts/seed-extras.ts`'s existing pattern-dedupe predicate is sign-agnostic and compiles clean. This plan's own gate is source-level: the array-count greps (Task 1), the `amountSign`-absent + FK-order greps (Tasks 2-3), and `yarn tsc --noEmit` reporting no error in the seed scripts. The repo-wide `yarn build` / `yarn tsc --noEmit` / `yarn check:language` are the SHARED Wave-2 gate owned by sibling plan 46-02 Task 3 — that union build is green only once BOTH this plan (seed.ts AND seed-extras.ts no longer reference the removed `amountSign`) and 46-02 (the ~18 app/DAL call sites) have landed; execute-phase runs the union gate. NO `drizzle-kit generate`, NO `drizzle-kit push`, NO migrate, NO `db:seed` / `db:seed-extras` against a live DB (D-06).
</verification>

<success_criteria>
- `scripts/seed-data.ts` exports a `directions` array with exactly 4 rows carrying the locked attribute matrix (DATA-01).
- `scripts/seed-data.ts` exports a `natures` array with exactly 8 rows, each with the locked `directionId` mapping; no 9th/uncategorized row (DATA-02; uncategorized = `nature_id NULL` per D-02).
- `scripts/seed.ts` imports and inserts `directions` then `natures` (FK order) via `onConflictDoNothing()`.
- `scripts/seed.ts` no longer writes the removed `category.type` or `categorizationPattern.amountSign` columns and typechecks clean.
- `scripts/seed-extras.ts`'s existing pattern-dedupe predicate is sign-agnostic (no `amountSign`/`amount_sign`), the stale comment is corrected, NO new backfill STEP is added (D-09), and it typechecks clean (build-survival seed-side repair, D-05).
- `scripts/seed.ts` retains the `excludeFromTotals` UPDATE (D-10).
- NO migration generated, NO DB apply, NO live seed run (D-06).
</success_criteria>

<output>
Create `.planning/phases/46-direction-nature-schema/46-03-SUMMARY.md` when done. Record: the exact `directions` (4) and `natures` (8) rows authored (codes, directionId mapping, attribute matrix, any label/color rename sourcing decisions), the seed.ts insert ordering, how the removed `category.type` / `amountSign` writes were repaired in seed.ts (map-out vs literal edit), how the `seed-extras.ts` dedupe predicate was made sign-agnostic, explicit confirmation that `excludeFromTotals` was retained (D-10), that NO new STEP was added to `seed-extras.ts` (D-09 — only an existing-predicate build-survival repair), and that NO migration / DB apply / live seed was run (D-06). Note that the repo-wide green build is the shared Wave-2 gate completed jointly with 46-02.
</output>
