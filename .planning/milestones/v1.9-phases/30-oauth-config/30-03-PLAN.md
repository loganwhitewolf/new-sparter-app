---
phase: 30-oauth-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - docs/deploy/vercel-supabase-r2.md
autonomous: true
requirements:
  - ENV-03
tags:
  - docs
  - env
  - deploy
  - oauth

must_haves:
  truths:
    - "`.env.example` contains 4 commented OAuth env vars (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET) with registration instructions"
    - "`.env.example` contains zero references to REGISTRATION_ENABLED"
    - "`docs/deploy/vercel-supabase-r2.md` contains all 4 OAuth env vars in the runtime variables table"
    - "`docs/deploy/vercel-supabase-r2.md` contains the canonical callback URL pattern `{BETTER_AUTH_URL}/api/auth/callback/{provider-id}`"
    - "`docs/deploy/vercel-supabase-r2.md` contains zero references to REGISTRATION_ENABLED or registration_disabled"
    - "`yarn check:language` passes"
  artifacts:
    - path: ".env.example"
      provides: "Env template with OAuth section and no registration toggle"
      contains: "GOOGLE_CLIENT_ID"
    - path: "docs/deploy/vercel-supabase-r2.md"
      provides: "Production deploy runbook with OAuth env contract and callback URLs"
      contains: "/api/auth/callback/google"
  key_links:
    - from: ".env.example OAuth section"
      to: "auth.ts socialProviders"
      via: "matching env var names — GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET"
      pattern: "GOOGLE_CLIENT_ID"
    - from: "docs/deploy/vercel-supabase-r2.md runtime variables table"
      to: "auth.ts socialProviders + Google/GitHub OAuth app registration"
      via: "documented var names + callback URL pattern"
      pattern: "GITHUB_CLIENT_ID"
---

<objective>
Update `.env.example` and `docs/deploy/vercel-supabase-r2.md` to (a) remove all REGISTRATION_ENABLED guidance per REG-01 / D-02 and (b) document the 4 OAuth env vars plus callback URL pattern per ENV-03 / D-05 / D-06.

Purpose: An operator preparing a production deploy must be able to read the runbook and `.env.example` and correctly configure Google + GitHub OAuth without consulting source code. No stale guardrail references should remain in operator-facing documentation.

Output:
- `.env.example` — REGISTRATION_ENABLED block deleted; OAuth provider section added after the Auth block.
- `docs/deploy/vercel-supabase-r2.md` — all REGISTRATION_ENABLED / disabled-signup / registration_disabled references deleted; 4 OAuth env var rows added to the runtime variables table; callback URL note added.
</objective>

<execution_context>
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andreabernardini/ai-projects/new-sparter-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-oauth-config/30-CONTEXT.md
@.planning/phases/30-oauth-config/30-RESEARCH.md
@.planning/phases/30-oauth-config/30-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Current state references. Read each file before editing. -->

From `.env.example` (89 lines):
- Lines 50–54 = REGISTRATION_ENABLED comment block (5 lines incl. blank separators). Delete.
- Line 47 = `# AUTH_DEBUG=1` (last line of Auth section). Insert OAuth section after this.
- Line 56 = `# -----------------------------------------------------------------------------` (start of "Cloudflare R2 storage" section).

From `docs/deploy/vercel-supabase-r2.md` (270 lines):
- Line 10 (Step 2): contains `, and \`REGISTRATION_ENABLED\``
- Line 15 (Step 7): contains `With \`REGISTRATION_ENABLED\` unset or true-like and after redeploy,`
- Line 16 (Step 8): entire line = "Disable registration and redeploy..."
- Line 17 (Step 9): entire line = "Check direct disabled-signup rejection..."
- Line 19 (Step 11): entire line = "Re-enable registration if desired..."
- Line 41 (browser smoke table): row for `PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED`
- Lines 46–48 (recommended phase sequence): step 2 (disabled-registration Playwright)
- Lines 61, 62 (evidence table): `disabled direct signup` and `preserved login` rows
- Lines 74–75 (failure tree): item 5 about `REGISTRATION_ENABLED`
- Line 102 (Vercel runtime variables table): `REGISTRATION_ENABLED` row
- Lines 106–125: entire `## Registration toggle smoke and recovery` section
- Line 182 (small-file R2 import smoke): paragraph mentioning "S04 registration guardrail"
- Line 254 (no-secret verification): `grep -q "REGISTRATION_ENABLED" .env.example` line

Canonical OAuth env var section for `.env.example` (per RESEARCH.md §4):
```
# -----------------------------------------------------------------------------
# OAuth providers (optional — activate by adding both vars for a provider)
# -----------------------------------------------------------------------------
# Register at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {BETTER_AUTH_URL}/api/auth/callback/google
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Register at https://github.com/settings/applications/new
# Authorization callback URL: {BETTER_AUTH_URL}/api/auth/callback/github
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
```

Canonical Vercel runtime variables table rows for `docs/deploy/vercel-supabase-r2.md` (per PATTERNS.md):
```
| `GOOGLE_CLIENT_ID` | No | Optional | Google OAuth client ID. When set (together with `GOOGLE_CLIENT_SECRET`), activates the Google social login provider. Register at https://console.cloud.google.com/apis/credentials. Callback URL: `{BETTER_AUTH_URL}/api/auth/callback/google`. |
| `GOOGLE_CLIENT_SECRET` | No | Optional (paired with ID) | Google OAuth client secret. Must be set together with `GOOGLE_CLIENT_ID`. |
| `GITHUB_CLIENT_ID` | No | Optional | GitHub OAuth client ID. When set (together with `GITHUB_CLIENT_SECRET`), activates the GitHub social login provider. Register at https://github.com/settings/applications/new. Callback URL: `{BETTER_AUTH_URL}/api/auth/callback/github`. |
| `GITHUB_CLIENT_SECRET` | No | Optional (paired with ID) | GitHub OAuth client secret. Must be set together with `GITHUB_CLIENT_ID`. |
```

Canonical callback URL note paragraph:
```
OAuth callback URLs follow the pattern `{BETTER_AUTH_URL}/api/auth/callback/{provider-id}`. For Google: `.../callback/google`. For GitHub: `.../callback/github`. `BETTER_AUTH_URL` must be set to the correct production HTTPS origin for OAuth redirects to work.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .env.example — remove REGISTRATION_ENABLED, add OAuth section</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (full file — confirm lines 50–54 hold the REGISTRATION_ENABLED block and line 47 ends with `# AUTH_DEBUG=1`)
  </read_first>
  <action>
    Apply two edits to `.env.example`:

    **Edit A — Delete the REGISTRATION_ENABLED block.** Remove these five exact lines (currently 50–54):
    ```
    # Server-side production registration guardrail. Registration is enabled by
    # default when this is unset, blank, or malformed. Set to false, 0, no, or off
    # to block new signup server-side while keeping existing-user login available.
    # Vercel environment changes require a redeploy before runtime behavior changes.
    # REGISTRATION_ENABLED=true
    ```
    Also remove the blank separator line immediately before this block (currently line 49) so two blank lines don't pile up.

    **Edit B — Insert the OAuth providers section.** Insert the following block after the `# AUTH_DEBUG=1` line (currently line 47), separated from preceding/following content by one blank line on each side. The block must be inserted BEFORE the `# -----------------------------------------------------------------------------` header that opens the "Cloudflare R2 storage" section.

    Exact block to insert (verbatim — keep the 79-character `# ----...` rule lines):
    ```
    # -----------------------------------------------------------------------------
    # OAuth providers (optional — activate by adding both vars for a provider)
    # -----------------------------------------------------------------------------
    # Register at https://console.cloud.google.com/apis/credentials
    # Authorized redirect URI: {BETTER_AUTH_URL}/api/auth/callback/google
    # GOOGLE_CLIENT_ID=
    # GOOGLE_CLIENT_SECRET=

    # Register at https://github.com/settings/applications/new
    # Authorization callback URL: {BETTER_AUTH_URL}/api/auth/callback/github
    # GITHUB_CLIENT_ID=
    # GITHUB_CLIENT_SECRET=
    ```

    Do NOT uncomment any of the four OAuth env vars (they remain `# VAR=` to signal "optional"). Do NOT modify any other section of `.env.example`.
  </action>
  <verify>
    <automated>grep -c "REGISTRATION_ENABLED" .env.example | grep -q '^0$' && grep -c "^# GOOGLE_CLIENT_ID=$" .env.example | grep -q '^1$' && grep -c "^# GITHUB_CLIENT_ID=$" .env.example | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REGISTRATION_ENABLED" .env.example` equals 0
    - `grep -c "^# GOOGLE_CLIENT_ID=$" .env.example` equals 1
    - `grep -c "^# GOOGLE_CLIENT_SECRET=$" .env.example` equals 1
    - `grep -c "^# GITHUB_CLIENT_ID=$" .env.example` equals 1
    - `grep -c "^# GITHUB_CLIENT_SECRET=$" .env.example` equals 1
    - `grep -c "OAuth providers (optional" .env.example` equals 1
    - `grep -c "console.cloud.google.com/apis/credentials" .env.example` equals 1
    - `grep -c "github.com/settings/applications/new" .env.example` equals 1
    - `grep -c "/api/auth/callback/google" .env.example` equals 1
    - `grep -c "/api/auth/callback/github" .env.example` equals 1
    - `grep -c "^DATABASE_URL=" .env.example` equals 1 (untouched section preserved)
    - `grep -c "^R2_ACCOUNT_ID=" .env.example` equals 1 (untouched section preserved)
    - `grep -c "^BETTER_AUTH_SECRET=" .env.example` equals 1 (untouched)
  </acceptance_criteria>
  <done>.env.example shows the OAuth section between the Auth block and the R2 block; REGISTRATION_ENABLED appears zero times.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/deploy/vercel-supabase-r2.md — purge REGISTRATION_ENABLED, add OAuth runtime vars + callback note</name>
  <files>docs/deploy/vercel-supabase-r2.md</files>
  <read_first>
    - docs/deploy/vercel-supabase-r2.md (full 270-line file — confirm each location enumerated in the interfaces block is present at the documented line)
  </read_first>
  <action>
    Apply the following edits in order. Each removal targets a specific span; each addition specifies exact location and verbatim content. Use `grep -n` to anchor against shifting line numbers as you edit.

    **REMOVALS (REGISTRATION_ENABLED purge per D-02):**

    R1. In the Step 2 list item (find with `grep -n "Configure Vercel runtime variables" docs/deploy/vercel-supabase-r2.md`): change the sentence so it no longer mentions `REGISTRATION_ENABLED`. The original ends `"...optional Better Stack variables, and \`REGISTRATION_ENABLED\` in Vercel Production."` — replace the segment `", and \`REGISTRATION_ENABLED\` in Vercel Production"` with ` in Vercel Production` (so it now reads `"...optional Better Stack variables in Vercel Production."`). Keep the rest of the sentence unchanged.

    R2. In Step 7 (find with `grep -n "Smoke enabled signup and login" docs/deploy/vercel-supabase-r2.md`): replace `"With \`REGISTRATION_ENABLED\` unset or true-like and after redeploy, create or use a disposable smoke account through the UI, then confirm the authenticated app loads."` with `"After redeploy, create or use a disposable smoke account through the UI, then confirm the authenticated app loads."` Keep the surrounding evidence-recording sentence intact.

    R3. Delete entire Step 8 (the bullet starting "Disable registration and redeploy..."). Locate with `grep -n "Disable registration and redeploy"`. Renumber subsequent steps so the list stays contiguous (original 9 → 8, 10 → 9, 11 → 10, 12 → 11, 13 → 12).

    R4. Delete entire Step 9 ("Check direct disabled-signup rejection..."). Already covered by the renumber in R3.

    R5. Delete entire Step 11 ("Re-enable registration if desired..."). Already covered by the renumber in R3.

    R6. In the optional browser smoke table (find with `grep -n "PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED"`): delete the entire markdown table row for `PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED`.

    R7. In the recommended phase sequence (find with `grep -n "Recommended phase sequence"`): delete the second numbered item ("After redeploying with `REGISTRATION_ENABLED=false`, add `PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED=1`..."). Renumber the third item to "2."

    R8. In the safe evidence table (find with `grep -n "disabled direct signup"`): delete both rows — `| disabled direct signup |...` and `| preserved login |...`. Keep all other table rows.

    R9. In the failure decision tree (find with `grep -n "Registration behavior does not match"`): delete the entire item 5 ("Registration behavior does not match `REGISTRATION_ENABLED`..."). Renumber subsequent items so the list stays contiguous.

    R10. In the Vercel runtime variables table (find with `grep -n "REGISTRATION_ENABLED" docs/deploy/vercel-supabase-r2.md` — should now point at the table row): delete the entire `| \`REGISTRATION_ENABLED\` | ... |` row.

    R11. Delete the entire `## Registration toggle smoke and recovery` section, from its `## ` heading line through the last item of its "Minimal no-secret smoke checklist" (the line `"4. Re-enable registration only when intended..."`). Locate with `grep -n "^## Registration toggle smoke"` and `grep -n "^## Cloudflare R2 production setup"` — delete everything between them (inclusive of the Registration section heading, exclusive of the Cloudflare heading).

    R12. In the small-file R2 import smoke section (find with `grep -n "S04 registration guardrail"`): the sentence currently ends `"...and rely on the S04 registration guardrail to avoid public signup amplification of quota usage."` — replace the segment `", and rely on the S04 registration guardrail to avoid public signup amplification of quota usage"` with `""` (empty string), so the sentence ends `"...Cloudflare R2 Free storage/operation usage and Supabase Free row usage."`

    R13. In the no-secret verification commands block (find with `grep -n "grep -q \"REGISTRATION_ENABLED\""`): delete the single line `grep -q "REGISTRATION_ENABLED" .env.example`. Keep the surrounding bash block intact.

    **ADDITIONS (OAuth documentation per D-05):**

    A1. In the Vercel runtime variables table, AFTER the row for `BETTER_AUTH_SECRET` and BEFORE the row for `R2_ACCOUNT_ID`, insert these four rows verbatim:
    ```
    | `GOOGLE_CLIENT_ID` | No | Optional | Google OAuth client ID. When set (together with `GOOGLE_CLIENT_SECRET`), activates the Google social login provider. Register at https://console.cloud.google.com/apis/credentials. Callback URL: `{BETTER_AUTH_URL}/api/auth/callback/google`. |
    | `GOOGLE_CLIENT_SECRET` | No | Optional (paired with ID) | Google OAuth client secret. Must be set together with `GOOGLE_CLIENT_ID`. |
    | `GITHUB_CLIENT_ID` | No | Optional | GitHub OAuth client ID. When set (together with `GITHUB_CLIENT_SECRET`), activates the GitHub social login provider. Register at https://github.com/settings/applications/new. Callback URL: `{BETTER_AUTH_URL}/api/auth/callback/github`. |
    | `GITHUB_CLIENT_SECRET` | No | Optional (paired with ID) | GitHub OAuth client secret. Must be set together with `GITHUB_CLIENT_ID`. |
    ```

    A2. Immediately AFTER the runtime variables table (after the line `"Do not create client-prefixed versions of database, R2, Better Stack, or auth secret variables. Only variables intentionally named with \`NEXT_PUBLIC_\` are exposed to browser code."`), insert this paragraph as a new paragraph (preceded and followed by one blank line):
    ```
    OAuth callback URLs follow the pattern `{BETTER_AUTH_URL}/api/auth/callback/{provider-id}`. For Google: `.../callback/google`. For GitHub: `.../callback/github`. `BETTER_AUTH_URL` must be set to the correct production HTTPS origin for OAuth redirects to work.
    ```

    Do NOT modify any other section, table, code block, or paragraph. Do NOT translate to Italian (developer-facing docs are English per CLAUDE.md Language Convention).
  </action>
  <verify>
    <automated>grep -cE "REGISTRATION_ENABLED|registration_disabled|disabled-signup|disabled direct signup|PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED|Disable registration|Re-enable registration|S04 registration guardrail|Registration toggle smoke" docs/deploy/vercel-supabase-r2.md | grep -q '^0$' && grep -c "GOOGLE_CLIENT_ID" docs/deploy/vercel-supabase-r2.md | grep -q '^[1-9]' && grep -c "/api/auth/callback/{provider-id}" docs/deploy/vercel-supabase-r2.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "REGISTRATION_ENABLED|registration_disabled|disabled-signup|disabled direct signup" docs/deploy/vercel-supabase-r2.md` equals 0
    - `grep -cE "PLAYWRIGHT_SMOKE_EXPECT_REGISTRATION_DISABLED|Disable registration|Re-enable registration" docs/deploy/vercel-supabase-r2.md` equals 0
    - `grep -c "S04 registration guardrail" docs/deploy/vercel-supabase-r2.md` equals 0
    - `grep -c "^## Registration toggle smoke and recovery" docs/deploy/vercel-supabase-r2.md` equals 0
    - `grep -c "Registration behavior does not match" docs/deploy/vercel-supabase-r2.md` equals 0
    - `grep -c "GOOGLE_CLIENT_ID" docs/deploy/vercel-supabase-r2.md` is at least 1 (table row + possibly inline references)
    - `grep -c "GOOGLE_CLIENT_SECRET" docs/deploy/vercel-supabase-r2.md` is at least 1
    - `grep -c "GITHUB_CLIENT_ID" docs/deploy/vercel-supabase-r2.md` is at least 1
    - `grep -c "GITHUB_CLIENT_SECRET" docs/deploy/vercel-supabase-r2.md` is at least 1
    - `grep -c "/api/auth/callback/google" docs/deploy/vercel-supabase-r2.md` is at least 1
    - `grep -c "/api/auth/callback/github" docs/deploy/vercel-supabase-r2.md` is at least 1
    - `grep -c "/api/auth/callback/{provider-id}" docs/deploy/vercel-supabase-r2.md` equals 1
    - `grep -c "^## Cloudflare R2 production setup" docs/deploy/vercel-supabase-r2.md` equals 1 (next heading preserved)
    - `grep -c "^## Vercel runtime environment variables" docs/deploy/vercel-supabase-r2.md` equals 1 (table heading preserved)
    - `grep -c "yarn db:migrate" docs/deploy/vercel-supabase-r2.md` is at least 1 (migration section untouched)
  </acceptance_criteria>
  <done>Runbook contains zero REGISTRATION_ENABLED-era content; OAuth env vars documented in the runtime variables table with the canonical callback URL note immediately below.</done>
</task>

<task type="auto">
  <name>Task 3: Run language gate</name>
  <files>(verification only — no source modification)</files>
  <read_first>
    - .env.example (post-edit)
    - docs/deploy/vercel-supabase-r2.md (post-edit)
  </read_first>
  <action>
    Per CLAUDE.md Language Convention: any change touching docs or env templates must pass `yarn check:language`. Run:

    ```bash
    yarn check:language
    ```

    Expected: exit code 0 with no findings against the new OAuth content (all new English; provider URLs are external, not localized).

    If `check:language` flags any line introduced by Tasks 1 or 2, return to that task and adjust the wording — do NOT silence the check via ignore lists.
  </action>
  <verify>
    <automated>yarn check:language</automated>
  </verify>
  <acceptance_criteria>
    - `yarn check:language` exits with code 0
  </acceptance_criteria>
  <done>Language gate passes; all new operator-facing docs are English-compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → committed docs / env templates | Documentation drives operator configuration of secret-class env vars in Vercel; misleading docs could cause misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-12 | Information disclosure (I) | `.env.example` OAuth comments | mitigate | All four OAuth env vars remain COMMENTED OUT (`# VAR=`) with no example values — operator cannot accidentally commit a real secret by copy-paste. The registration URL hints point to provider consoles, not embedded secrets. |
| T-30-13 | Repudiation (R) / operator error | runbook OAuth setup instructions | mitigate | Callback URL pattern `{BETTER_AUTH_URL}/api/auth/callback/{provider-id}` is documented with explicit per-provider examples; provider registration URLs are linked. Documentation removes ambiguity that could lead to mis-registered OAuth apps (e.g. wrong redirect URI). |
| T-30-14 | Tampering (T) | runbook integrity after large deletion (Section "Registration toggle smoke and recovery") | mitigate | Deletion scope is precisely enumerated (R1–R13 in Task 2) with grep anchors at each step — no surrounding content (migration, R2 CORS, free-tier constraints, redeploy guidance) is modified. Acceptance criteria explicitly verify preserved sections (`Cloudflare R2 production setup`, `Vercel runtime environment variables`, `yarn db:migrate`). |
| T-30-15 | Information disclosure (I) | accidental leak of REGISTRATION_ENABLED stale guidance | mitigate | Final grep audit (`grep -cE "REGISTRATION_ENABLED|registration_disabled..." == 0`) enforces zero residual references — operators cannot follow stale guidance to set a non-existent toggle. |
</threat_model>

<verification>
- `grep -c "REGISTRATION_ENABLED" .env.example` equals 0
- `grep -cE "REGISTRATION_ENABLED|registration_disabled|disabled-signup|disabled direct signup" docs/deploy/vercel-supabase-r2.md` equals 0
- `grep -c "GOOGLE_CLIENT_ID" .env.example` equals 1; same for `GITHUB_CLIENT_ID`
- `grep -c "GOOGLE_CLIENT_ID" docs/deploy/vercel-supabase-r2.md` is at least 1; same for `GITHUB_CLIENT_ID`
- `grep -c "/api/auth/callback/{provider-id}" docs/deploy/vercel-supabase-r2.md` equals 1
- `yarn check:language` exits 0
</verification>

<success_criteria>
1. ENV-03 acceptance: an operator reading `.env.example` and `docs/deploy/vercel-supabase-r2.md` can correctly configure all 4 OAuth env vars and the OAuth provider apps without consulting source code.
2. D-02 acceptance: zero references to `REGISTRATION_ENABLED` (or its UI/test mirror identifiers) remain in either file.
3. D-05 acceptance: runbook documents all 4 OAuth env vars and both callback URL paths.
4. D-06 acceptance: `.env.example` documents the 4 OAuth env vars (commented out) with provider registration URLs and callback URI hints.
5. `yarn check:language` exits 0 after the edits.
</success_criteria>

<output>
After completion, create `.planning/phases/30-oauth-config/30-03-SUMMARY.md` capturing: (a) the exact lines/sections removed from each file, (b) the exact lines added, (c) the final residual-reference grep counts.
</output>
