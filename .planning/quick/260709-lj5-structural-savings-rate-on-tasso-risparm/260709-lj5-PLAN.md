# Quick Task 260709-lj5: Structural savings rate on the Tasso risparmio card

## Goal

Same treatment as Entrate (lan) and Bilancio (leg): the Tasso risparmio card shows the
recurring-only savings rate as a breakdown row under the total headline.

Structural rate = `computeSavingsRate(totalInRecurring, totalOut)` — same formula
`(in − out)/in × 100`, same zero-income guard, recurring income only.

**Labels are provisional** ("Ricorrente") — the user explicitly wants to revisit the
breakdown labels across all three cards right after this lands (follow-up discussion,
same session).

## Tasks

### 1. DAL (lib/dal/dashboard.ts)
- `OverviewData.structuralSavingsRate: number | null` — computed in `buildOverviewData`
  from `totalInRecurring` (null when unknown). No delta.

### 2. kpi-row.tsx
- Tasso risparmio card: `breakdown=[{ label: 'Ricorrente', value: `${structuralSavingsRate}%` }]`
  when non-null.

### 3. Tests
- dashboard-dal: extraordinary-heavy fixture → structural rate negative
  ((1500−2600)/1500 = −73.3); all-recurring → structural = total; null → null.
- overview-interactions KpiRow render: percentage row present / absent.

## Verify
- Full suite green; tsc clean; check:language clean.

## Out of scope
- Final label wording (follow-up discussion, all three cards together).
- Savings-rate reading thresholds unchanged (still evaluated on the total rate).
