---
quick_id: 260630-h1j
slug: nome-file-cliccabile-in-transazioni-filt
status: ready
---

# Quick Plan: File name link → import filter

1. Add `fileId` UUID filter to `parseImportFilters` + `getImportRows`.
2. Wire import page active-filter / table key.
3. Link file name in `transaction-table.tsx` to `/import?fileId=…`.
4. Tests for parser + DAL filter.
