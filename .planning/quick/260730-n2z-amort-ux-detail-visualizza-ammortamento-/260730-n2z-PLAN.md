---
id: 260730-n2z
title: Amort UX — Visualizza, vendita/rimborso copy, Tutti=all, transactionId filter
status: ready
date: 2026-07-30
---

# Quick Plan: 260730-n2z — Amort UX polish

## Goal

1. Transaction detail: replace Chiudi + Rimuovi with **Visualizza ammortamento** → `/amortizations?transactionId=<uuid>`.
2. Registry: filter by `transactionId` query param; chip clearable.
3. Copy: **Chiudi con vendita/rimborso** (table CTA + dialog radio + over-residual user message).
4. Status **Tutti**: show open + closed (`null`/`all` → no status filter).

## Context

Locked in `260730-n2z-CONTEXT.md`. Stack on branch `gsd/quick-260730-m2x-amort-dashboard-fixes`.

## Tasks

### Task 1: Status Tutti + transactionId filter

**Files:**
- `components/amortizations/amortization-table.tsx`
- `lib/utils/amortizations-table-config.ts`
- `lib/routes.ts`
- `tests/amortization-registry-table.test.ts`

**Action:**
1. Change `resolveEffectiveStatusFilter` return type to `'open' | 'closed' | null`. Return `null` when param is `null`, `''`, or `'all'`; return `'open'` / `'closed'` for those literals; unrecognized → `null` (show all, same as Tutti — do not coerce to open).
2. In `AmortizationTable` filter loop: if `effectiveStatus !== null`, require `row.status === effectiveStatus`; also if `transactionId = searchParams.get('transactionId')` is non-empty, require `row.transactionId === transactionId`.
3. Add filter field to `AMORTIZATIONS_TABLE_CONFIG`: `{ key: 'transactionId', label: 'Transazione', type: 'text', toChip: () => 'Transazione collegata' }` so chips + Cancella tutto clear it. (Text input in Filtri sheet is acceptable; deep-link is the primary entry.)
4. Add `amortizationsByTransactionHref(transactionId: string)` in `lib/routes.ts` → `${APP_ROUTES.amortizations}?transactionId=${encodeURIComponent(transactionId)}`.
5. Update unit tests for `resolveEffectiveStatusFilter`; add a small pure test for transactionId filtering if extracted, or test filter composition via a tiny exported helper `matchesAmortizationFilters(row, { status, q, transactionId })` — prefer extracting the filter predicate so it is unit-testable without jsdom.

**Verify:** `yarn vitest run tests/amortization-registry-table.test.ts`

**Done when:** Tutti shows open+closed; `?status=open|closed` still narrows; `?transactionId=` isolates the UNIQUE plan; chip clears the param.

### Task 2: Detail Visualizza + copy vendita/rimborso

**Files:**
- `components/transactions/transaction-detail-client.tsx`
- `components/amortizations/amortization-table.tsx`
- `components/transactions/amortization-reimburse-dialog.tsx`
- `lib/services/amortization-lifecycle.ts` (user-facing over-residual string only)
- `tests/amortization-lifecycle.test.ts` (assertion string update)
- Optionally extend an existing detail/registry test if one asserts CTA labels

**Action:**
1. On transaction detail, when `amortizationPlanId != null`: remove Chiudi/Rimuovi buttons, dialogs, and related state (`closeAmortizeOpen`, `removeAmortizeOpen`, unused imports). Add a Link/Button to `amortizationsByTransactionHref(transaction.id)` labeled **Visualizza ammortamento** (CalendarClock icon is fine). Keep Ammortizza eligibility UI unchanged. Keep AmortizationReimburseDialog on detail only if still used elsewhere on that page — if it was only for close-via-detail path, remove that entry too (detail no longer closes). Check reimbursement/amortization panels on the same page.
2. Table button label: `Realizza con vendita` → `Chiudi con vendita/rimborso`. Update comments in that file.
3. Dialog radio: `Chiudi per vendita` → `Chiudi con vendita/rimborso`. Keep reduce label as-is.
4. Over-residual error in `reducePlanTx`: replace `"chiudi per vendita"` with `"chiudi con vendita/rimborso"` in the thrown message; update the lifecycle test that asserts that substring.

**Verify:** `yarn vitest run tests/amortization-registry-table.test.ts tests/amortization-lifecycle.test.ts` (+ any new/updated UI static tests)

**Done when:** amortized detail offers only Visualizza (plus Ammortizza when eligible / Elimina etc.); registry CTA and dialog copy match; over-residual message consistent.

## Out of scope

- Transactions **table** row Chiudi/Rimuovi (CONTEXT: leave as-is).
- `/amortizations/[id]` plan detail page (D-D1).
- Changing realize vs reduce dialog behavior (copy only).

## must_haves

- Detail amortized: Visualizza link with `transactionId` param; no Chiudi/Rimuovi on detail.
- Registry Tutti = all statuses; open/closed still work.
- `transactionId` filter + clearable chip.
- User-visible vendita copy includes rimborso where locked.
