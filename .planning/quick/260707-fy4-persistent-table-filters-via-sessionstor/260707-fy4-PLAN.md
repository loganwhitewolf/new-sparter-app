---
phase: quick-260707-fy4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/data-table/use-table-url.ts
  - tests/use-table-url-persistence.test.ts
  - .planning/table-filter-sort-DECISIONS.md
autonomous: true
requirements: [QUICK-FY4]
user_setup: []

must_haves:
  truths:
    - "Leaving a filtered table page and returning via a bare nav link restores the last filters/sort for that table (per-tab, sessionStorage)"
    - "A URL that already carries search params (shared link, refresh, back/forward) is never overridden by the saved state"
    - "Clearing all filters then re-entering bare shows the cleared (unfiltered) view, not stale filters"
    - "Each table (transactions, expenses, files) persists independently under its own key"
    - "Reopening the browser starts clean (sessionStorage lifetime, no staleness handling)"
  artifacts:
    - "components/data-table/use-table-url.ts — save-on-write + restore-on-mount wiring plus exported pure helpers"
    - "tests/use-table-url-persistence.test.ts — unit tests for the pure helpers, no jsdom"
    - ".planning/table-filter-sort-DECISIONS.md — amended URL-source-of-truth line"
  key_links:
    - "useTableUrl.replaceWith is the single funnel for every URL write (updateParam, updateParams, useToolbarSort) — the save hook there covers all three tables and sort with one implementation"
    - "Restore fires only from the mount effect with an empty query string, so back/forward navigation (history entry WITH params, v2.5 smart-back) never triggers it"
---

<objective>
Persist table filters/sort per tab so that bare re-entry into `/transactions`, `/expenses`, or `/import` restores the last view. URL stays the single source of truth; sessionStorage is only a restore layer for bare navigation (locked decisions 1-4). Amend the one superseded line in `table-filter-sort-DECISIONS.md` (locked decision 5).

Purpose: today, navigating away from a filtered table and coming back through the nav loses the filters — an ephemeral-by-design choice from the 2026-06-04 grill that the user has now reversed for the bare-navigation case only.
Output: updated `components/data-table/use-table-url.ts` (shared hook — all three tables get it with one implementation), new unit test file, one-line doc amendment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@components/data-table/use-table-url.ts
@components/data-table/DataTableToolbar.tsx
@tests/data-table-toolbar.test.tsx
@tests/detail-page-shell.test.tsx
@.planning/table-filter-sort-DECISIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sessionStorage save/restore in useTableUrl — pure helpers + hook wiring + unit tests</name>
  <files>components/data-table/use-table-url.ts, tests/use-table-url-persistence.test.ts</files>
  <behavior>
    Pure helpers (exported from use-table-url.ts, unit-testable without jsdom — precedent: attachPopstateRefresh / hasInAppHistory in components/detail-pages/detail-page-shell.tsx):
    - Test 1: tableUrlStorageKey('/transactions') returns 'table-url:transactions' (leading slash stripped); same shape for '/expenses' and '/import' (locked decision 3 key format).
    - Test 2: saveTableQuery(storage, key, 'q=coop&sort=amount') calls storage.setItem(key, 'q=coop&sort=amount').
    - Test 3: saveTableQuery(storage, key, '') saves the empty string — "cleared" must be distinguishable from "never touched" (locked decision 3).
    - Test 4: saveTableQuery(null, key, q) and saveTableQuery with a setItem that throws are both silent no-ops (storage unavailable / quota / private mode).
    - Test 5: readRestorableQuery(storage, key, '') returns the stored string when it is non-empty.
    - Test 6: readRestorableQuery(storage, key, 'status=categorized') returns null — current URL already has params, saved state never wins (locked decision 1). This is also the pin for the v2.5 smart-back path: router.back() lands on a history entry WITH params, so restore must not fire.
    - Test 7: readRestorableQuery returns null when the stored value is '' (cleared state — bare URL is already correct) and when the key is absent.
    - Test 8: readRestorableQuery(null, key, '') returns null and does not throw.
  </behavior>
  <action>
    In components/data-table/use-table-url.ts, add three exported pure helpers and wire them into the hook. Per locked decisions 1-4; comments and identifiers in English.

    Helpers (module scope, above the hook):
    - tableUrlStorageKey(route: string): string — returns 'table-url:' + route with the leading '/' stripped. The hook already receives route ('/transactions' | '/expenses' | '/import' via APP_ROUTES), so the key derives from it with no per-table forking.
    - safeSessionStorage(): Storage | null — returns window.sessionStorage, or null when typeof window is 'undefined' or the property access throws (storage disabled). Wrap the access in try/catch.
    - saveTableQuery(storage: Pick&lt;Storage, 'setItem'&gt; | null, key: string, query: string): void — no-op on null storage; try/catch around setItem (quota/private mode). Always writes, including query === '' (decision 3: cleared must overwrite the previous save).
    - readRestorableQuery(storage: Pick&lt;Storage, 'getItem'&gt; | null, key: string, currentQuery: string): string | null — returns null when storage is null, when currentQuery is non-empty (URL wins, decision 1), when getItem throws, when the key is absent, or when the stored value is ''. Otherwise returns the stored query string.

    Hook wiring inside useTableUrl(route):
    - Compute the key once: const storageKey = tableUrlStorageKey(route).
    - Save: in replaceWith, after computing query and before/alongside the startTransition call, invoke saveTableQuery(safeSessionStorage(), storageKey, query). replaceWith is the single funnel for updateParam, updateParams, and useToolbarSort, so every filter/sort change — and "Cancella tutto" — is captured with one line (decision 3).
    - Restore: add a mount-only useEffect (empty dependency array; add the eslint-disable-next-line react-hooks/exhaustive-deps comment). Body: const restored = readRestorableQuery(safeSessionStorage(), storageKey, searchParams.toString()); if restored is truthy, call startTransition wrapping router.replace(route + '?' + restored, { scroll: false }). Call router.replace directly — do NOT route through replaceWith, so "save" semantics stay strictly on user-initiated writes. The brief unfiltered first render before the replace is an accepted tradeoff (decision 4).
    - Update the hook's JSDoc header to document the sessionStorage restore layer: URL remains source of truth; restore fires only on bare mount.

    Notes for the executor:
    - Multiple components call useTableUrl on the same page (DataTableToolbar, useToolbarSort in the table, ExpenseUncategorizedCta). Each instance's mount effect may fire; all read the same key and issue an idempotent router.replace to the same URL. Harmless — do not add cross-instance coordination.
    - sessionStorage must never be touched during render — only inside the effect and inside replaceWith (event-handler context). The existing renderToStaticMarkup tests in tests/data-table-toolbar.test.tsx run in Node without window and act as the SSR regression guard; they must keep passing unchanged.
    - Do not modify DataTableToolbar.tsx, the per-table configs, or expense-uncategorized-cta.tsx — the change lives entirely in the shared hook (implementation note: no per-table forking).

    Tests in tests/use-table-url-persistence.test.ts (new file): plain vitest, no jsdom, no react rendering. Mock storage as a minimal object with vi.fn() getItem/setItem (same pattern as the attachPopstateRefresh test in tests/detail-page-shell.test.tsx). Cover every case in the behavior block. Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>yarn vitest run tests/use-table-url-persistence.test.ts tests/data-table-toolbar.test.tsx</automated>
  </verify>
  <done>New helper tests pass (all 8 behaviors); existing data-table-toolbar tests pass unchanged (SSR-safety regression); the hook saves on every replaceWith including empty query, and restores only on bare mount via direct router.replace.</done>
</task>

<task type="auto">
  <name>Task 2: amend table-filter-sort-DECISIONS.md URL-source-of-truth line</name>
  <files>.planning/table-filter-sort-DECISIONS.md</files>
  <action>
    Per locked decision 5, amend the Architecture bullet that currently reads: "URL = single source of truth." followed by the ephemeral/no-localStorage clause. Replace only that bullet, keeping the doc's terse bullet style, with wording that states: URL = single source of truth (unchanged); sessionStorage is a per-tab restore layer for bare navigation only (quick task 260707-fy4) — saved on every filter/sort change per table, restored via router.replace only when the URL has no search params; a URL with params (shared link, refresh, back/forward) always wins; localStorage still rejected. Keep it to 1-2 lines in the existing style. Do not touch any other line in the file.
  </action>
  <verify>
    <automated>grep -c "sessionStorage" /Users/andreabernardini/ai-projects/new-sparter-app/.planning/table-filter-sort-DECISIONS.md</automated>
  </verify>
  <done>The Architecture section reflects the new semantics (URL source of truth + sessionStorage bare-navigation restore); the rest of the document is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage → URL | A tampered/stale stored query string is injected into router.replace on mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fy4-01 | Tampering | readRestorableQuery → router.replace | low | accept | Restored value becomes ordinary URL searchParams — identical trust level to a user-typed URL; server-side Zod validation of searchParams in the DAL path already sanitizes arbitrary params (ADR 0010 architecture, unchanged) |
| T-fy4-02 | Denial of service | saveTableQuery (quota/private mode) | low | mitigate | try/catch around all storage access; feature degrades to current ephemeral behavior, never breaks the page |
</threat_model>

<verification>
- `yarn test` — full suite green (no regressions in data-table-toolbar, detail-page-shell, or table tests).
- `yarn check:language` — new comments/tests are English-only (project language convention).
- Manual (post-execute, developer): filter /transactions, navigate to /expenses via nav, return to /transactions via nav → filters restored; press "Cancella tutto", leave, return → unfiltered; open a detail page and use "Indietro" (smart back) → filters intact via history entry, no restore interference.
</verification>

<success_criteria>
- All three tables persist and restore filters/sort per tab with a single shared-hook implementation.
- Restore never fires when the URL already has params (shared links, refresh, back/forward, smart-back all unaffected) — pinned by unit test.
- Cleared state persists as cleared (empty-string save) — pinned by unit test.
- SSR/render paths never touch sessionStorage — pinned by existing renderToStaticMarkup tests.
- DECISIONS.md no longer claims filters are ephemeral; amendment is minimal.
</success_criteria>

<output>
Create `.planning/quick/260707-fy4-persistent-table-filters-via-sessionstor/260707-fy4-SUMMARY.md` when done.
</output>
