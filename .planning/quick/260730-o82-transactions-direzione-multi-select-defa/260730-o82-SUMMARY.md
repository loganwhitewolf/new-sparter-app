---
id: 260730-o82
title: Transactions direction multi-select with transfer-off default
status: complete
date: 2026-07-30
---

# Quick Task 260730-o82 Summary

## What changed

1. **Implicit default** — bare `/transactions` applies `directions = in,out,allocation,unclassified` (no transfer). URL stays clean; **no Direzione chips** until the user changes the filter.
2. **Multi-select UI** — Direzione is a checkbox list; selecting the default set again clears the URL param; enabling Trasferimenti writes `?direction=…,transfer`.
3. **DAL** — `inArray` / OR with unclassified (`natureId IS NULL`); empty list → match nothing.
4. **Cascade** — nature/category options = union of selected directions; with implicit default, transfer natures/categories stay out until transfer is checked.
5. **Expenses** — unchanged (single-select).

## Commits

- (this wrap-up) feat/fix for parse, DAL, toolbar, config, tests

## Tests

- `tests/transaction-filters-direction.test.ts` (new)
- `tests/transactions-dal.test.ts` (map update)
- `tests/cascade-options.test.ts` (`unionFilterOptions`)
- `yarn check:language` passed

## Deviations

- Planner skipped (orchestrator wrote PLAN from locked CONTEXT).
- Empty multi-select selection clears URL → back to implicit default (avoids a dead empty list).
