---
id: 260731-hhv-03
wave: 3
title: Taxonomy split + pattern copy + amort UI rename
status: ready
date: 2026-07-31
depends_on: [260731-hhv-02]
requirements: [2.3, 3.4, 3.10]
---

# Wave 03: Taxonomy split 2.3 + pattern copy 3.4 + amort UI rename 3.10

## Goal

Spezzare "carburante e ricarica" in **Carburante** + **Ricarica auto elettrica** (D-04); riscrivere copy pattern senza "regex" (D-03); rinominare Ammortamenti in UI consumer "Spese dilazionate" senza toccare route/slug (D-02).

## Context

Seeds are additive (`CLAUDE.md`): append STEPS in `scripts/seed-extras.ts`; never edit `seed-data.ts` shapes. Patterns: update `scripts/seed-patterns-data.ts` then `yarn db:seed-patterns` replaces system patterns. Historical v2 merge already folded `carburante` + `elettricita-per-auto` into `carburante-e-ricarica` — do **not** restore slug `elettricita-per-auto`.

## Tasks

### Task 1: Split subcategory carburante-e-ricarica (2.3 / D-04) — seed-extras + data migrate

**Files:**
- `scripts/seed-extras.ts` — append new STEP(s) only; mirror helpers like `insertPacchettoVacanze` / `migrateSubcategoryMerge` / `renameSubcategoryGuarded`
- Optionally a small test if STEP_NAMES registry is asserted elsewhere
- Do **not** modify `scripts/seed-data.ts` row shapes

**Action:**
1. Append an idempotent step (name e.g. `split-carburante-e-ricarica`) that:
   - Ensures system subcategories exist under the same parent category as today (`categoryId` 7 / transport category): **Carburante** slug `carburante`, **Ricarica auto elettrica** slug `ricarica-auto-elettrica` (derive via project slug convention; locked names per D-04).
   - Uses nature consistent with the old `carburante-e-ricarica` row (essential transport nature as currently seeded).
   - Deactivates or retires `carburante-e-ricarica` after migration (do not leave two live synonyms).
2. **Migrate linked rows** from `carburante-e-ricarica` → new IDs:
   - Expenses / transactions / categorization history / user patterns pointing at the old subcategory id.
   - **Ambiguity policy (locked for executor):** rows that cannot be classified by description keywords → assign to **Carburante** (fuel is the majority historical bucket). Deterministic EV keywords (e.g. ricarica elettrica, enel x, tesla supercharger, evcharge — use sensible IT/EN merchant tokens present in data) → **Ricarica auto elettrica**. Document counts in SUMMARY.
3. Idempotent: re-run skips when both new slugs exist and old slug already inactive/migrated.
4. Never edit `seed-data.ts` for this split.

**Verify:**
- `yarn db:seed-extras` (or dry-run unit if STEP is imported in tests) against local DB
- Query: no active system sub with slug `carburante-e-ricarica`; both new slugs active

**Done when:** Additive step creates Carburante + Ricarica auto elettrica, migrates FKs per ambiguity policy, deactivates old merged slug; `seed-data.ts` untouched.

### Task 2: Split system patterns fuel vs EV (2.3 / D-04) — seed-patterns-data

**Files:**
- `scripts/seed-patterns-data.ts` — pattern currently targeting `subCategorySlug: "carburante-e-ricarica"` (fuel stations regex)
- Runbook note: `yarn db:seed-patterns` after data change (full replace of system patterns; user patterns preserved)

**Action:**
1. Point the existing fuel/gas-station pattern at slug `carburante`.
2. Add a separate system pattern (or split tokens) for EV charging → `ricarica-auto-elettrica` with appropriate Italian/merchant keywords; keep confidence/priority coherent with nearby transport patterns.
3. Ensure no remaining `carburante-e-ricarica` references in `seed-patterns-data.ts`.
4. Do not put pattern rows into `seed-extras.ts` (patterns belong in seed-patterns-data per CLAUDE.md).

**Verify:**
- `rg 'carburante-e-ricarica' scripts/seed-patterns-data.ts` → no matches
- `yarn db:seed-patterns` succeeds locally (or pattern validation script if used in CI)

**Done when:** System patterns split fuel vs EV onto the two new slugs; old slug absent from patterns data.

### Task 3: Pattern UI copy — natural Italian, no user-facing "regex" (3.4 / D-03)

**Files:**
- `components/patterns/create-pattern-dialog.tsx` — "regola regex", "Pattern regex", `/netflix/i` examples
- `components/patterns/pattern-actions.tsx` — "Aggiorna la regex…", "Pattern regex", `/pattern/i` help
- `components/categories/category-pattern-panel.tsx` — default description "Regole regex…", table head "Pattern (regex)"
- `app/(app)/patterns/page.tsx` — page blurb if it exposes technicism
- `app/(app)/import/[fileId]/suggestions/page.tsx` / suggestion cards — only if user-facing "regex" remains
- Validation error strings in `lib/validations/pattern.ts` — rewrite user-visible messages to avoid the word "regex" while keeping technical accuracy for developers in comments/logs

**Action:**
1. Per **D-03**, replace user-facing copy with natural Italian. Draft direction (polish OK):
   - Section/panel: "Regole automatiche" (not "Regole regex")
   - Edit dialog: "Aggiorna la regola…" (not "Aggiorna la regex…")
   - Field label: e.g. "Testo da riconoscere" / "Regola" (not "Pattern regex")
   - Examples: show `netflix` plain — do not expose `/netflix/i` slash-flag form in help text
2. Table column header: drop "(regex)".
3. Grep product UI for user-visible `regex` / `Regex` in these surfaces and clear them. Keep internal identifiers (`pattern`, DAL, `safe-regex`) unchanged.
4. Italian only for product strings; developer comments stay English.

**Verify:**
- `rg -n 'regex|Regex' components/patterns components/categories/category-pattern-panel.tsx app/(app)/patterns` — no user-visible copy left (code comments / import paths OK if clearly non-UI)
- `yarn check:language` if docs/comments touched
- Spot-check Patterns page + create/edit dialogs

**Done when:** No user-facing "regex" on pattern surfaces; draft natural-IT copy shipped per D-03.

### Task 4: Ammortamenti → Spese dilazionate UI only (3.10 / D-02)

**Files:**
- `components/layout/sidebar.tsx` — nav label `Ammortamenti`
- `app/(app)/amortizations/page.tsx` — `metadata.title` + `<h1>`
- `components/amortizations/amortization-table.tsx` — empty state "Nessun ammortamento trovato"
- `components/amortizations/*` — any remaining consumer-facing "Ammortament*" headings/empty copy
- Transaction row actions / detail copy that say "ammortamento" in user-facing menus where the contract expects consumer naming — prefer consistent "spesa dilazionata" / "Spese dilazionate" phrasing; do not rename domain types or toast keys that break tests unnecessarily — update tests that assert visible labels
- `tests/sidebar-sections.test.tsx` — expect new label; href stays `/amortizations`
- **Do not change** `APP_ROUTES.amortizations = '/amortizations'` in `lib/routes.ts`

**Action:**
1. Per **D-02**, rename user-visible "Ammortamenti" / "ammortamento" labels to consumer **"Spese dilazionate"** (or singular "Spesa dilazionata" where grammar requires). Keep consistency across sidebar, page title, empty states.
2. Leave routes, DB tables, `amortizationPlan` identifiers, and feature gates unchanged.
3. If a string is developer-only (code comment, ADR), leave English domain term "amortization".
4. Update snapshot/assertions that check visible Italian labels.

**Verify:**
- `yarn vitest run tests/sidebar-sections.test.tsx tests/amortization-registry-table.test.ts` (and any failing label assertions fixed)
- Confirm `lib/routes.ts` still exports `/amortizations`
- Grep UI: sidebar shows Spese dilazionate; URL path unchanged

**Done when:** Consumer UI says Spese dilazionate consistently; `/amortizations` route unchanged; no feature-gate hide.

## must_haves

- Carburante + Ricarica auto elettrica live; old `carburante-e-ricarica` retired via additive seed-extras (D-04).
- System patterns point at the two new slugs (D-04).
- Pattern surfaces use natural IT copy with no user-facing "regex" (D-03).
- Ammortamenti UI renamed; routes/slugs unchanged (D-02).

## Out of scope / Deferred

- **2.4** subcategory hover/selected / Più usate IA
- **3.8** dashboard visual hierarchy redesign
- **3.9** Bilancio tasso/20% domain definition
- Editing `seed-data.ts` shapes
- Restoring historical slug `elettricita-per-auto`
- Changing `APP_ROUTES.amortizations` or hiding amortizations behind a plan gate
