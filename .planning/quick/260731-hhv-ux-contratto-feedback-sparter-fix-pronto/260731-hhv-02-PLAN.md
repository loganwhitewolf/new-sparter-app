---
id: 260731-hhv-02
wave: 2
title: Welcome/Dashboard PRONTO + bug Crea categoria
status: ready
date: 2026-07-31
depends_on: [260731-hhv-01]
requirements: [3.7, 3.1, 3.2, 3.3, 3.5, 3.6]
---

# Wave 02: Welcome/Dashboard PRONTO + bug 3.7

## Goal

Sbloccare "Crea categoria" (bug 3.7), poi chiudere Benvenuto/Dashboard PRONTO: unica CTA welcome, tag uncategorized come link (D-01), nascondere box suggerimenti vuoto, filtro periodo su Spese, card KPI Entrate/Uscite cliccabili.

## Context

Locked: **D-01** (3.2 → link, not remove). Bug 3.7 is highest priority — Task 1 must run before cosmetic dashboard items. Scout note: `overview-nudge.tsx` already wraps "Movimenti da categorizzare" in a `Link` to `/transactions?status=uncategorized&months=…` plus dismiss — verify first; only change if action/link is missing.

## Tasks

### Task 1: Reproduce + fix "Crea categoria" (3.7) — FIRST

**Files:**
- `components/categories/category-mutation-dialogs.tsx` — `CreateCategoryDialog` / submit "Crea categoria"
- `lib/actions/categories.ts` — `createCategoryAction`
- `lib/validations/category.ts` — `CreateCategorySchema` / `deriveCategorySlug`
- `lib/dal/categories.ts` — `createUserCategory`
- `tests/category-actions.test.ts` (extend if new failure mode)
- Any related client helper used by `useDialogAction` in the same dialogs file

**Action:**
1. Reproduce the failure path: open Settings → categories → "Nuova categoria" / "Crea categoria" with a valid name; capture the exact error (UI `ActionError`, server log, Zod, DAL duplicate, missing slug, form `name` field mismatch, etc.).
2. Fix the root cause in the correct layer (validation → action → DAL). Keep `"use server"` thin; money N/A. Preserve session `userId` (never trust form `userId`).
3. Ensure happy path returns `{ error: null }`, revalidates categorization surfaces, closes dialog / toasts success as today.
4. Add or update a unit test that locks the reproduced failure (if it was untested) so the bug cannot regress.
5. Do not redesign the dialog UI beyond what the fix requires.

**Verify:**
- `yarn vitest run tests/category-actions.test.ts`
- Manual: create a personal category successfully; duplicate name shows safe Italian error.

**Done when:** "Crea categoria" succeeds for a new unique name; known errors map to Italian messages; tests cover the fixed path.

### Task 2: Welcome single CTA (3.1) + nudge link verify (3.2 / D-01) + hide empty suggestions (3.3)

**Files:**
- `app/(app)/onboarding/_components/step-5-outro.tsx` — currently TWO CTAs: "Vai alla dashboard" + "Personalizza le categorie"
- `components/dashboard/overview/overview-nudge.tsx` — already Link + dismiss (verify)
- `tests/overview-interactions.test.tsx` — nudge link assertions if needed
- `app/(app)/import/[fileId]/suggestions/page.tsx` — empty branch renders "Nessun suggerimento trovato…" box
- `components/import/suggestion-section.tsx` — already returns `null` when both suggestion arrays empty (confirm no empty box leak)

**Action:**
1. **3.1:** Remove the secondary "Personalizza le categorie" button and any alternate actions. Leave a single primary CTA: "Vai alla dashboard" (keep hard `<a href={APP_ROUTES.dashboard}>` navigation reason in comments).
2. **3.2 / D-01:** Verify `OverviewNudge` already links to uncategorized movements via existing query (`status=uncategorized` + year months). If link+href correct → document verify-done in SUMMARY and skip code change. If the tag is dismiss-only without navigation, or href wrong → make the label a navigable link (keep dismiss X). Do not remove the tag.
3. **3.3:** When discovery has zero regex candidates and zero single suggestions, do **not** show an empty "Suggerimenti pattern" / "Nessun suggerimento trovato" box. Hide that empty UI; keep a useful path forward (e.g. `ProceedToImportsCta` and/or tag suggestions section if it has content). When suggestions exist, keep `SuggestionSection` as today.
4. Optionally soften page title/description when both pattern and tag suggestions are empty — discretionary, as long as the empty pattern box is gone.

**Verify:**
- `yarn vitest run tests/overview-interactions.test.tsx`
- Grep/spot-check: `step-5-outro.tsx` has exactly one CTA href to dashboard; suggestions empty branch has no user-visible "Nessun suggerimento trovato" box.

**Done when:** Welcome has only "Vai alla dashboard"; nudge is a working uncategorized link (or verified already); empty pattern suggestions UI is hidden.

### Task 3: Expenses period filter — month-multi (3.5)

**Files:**
- `app/(app)/expenses/expenses.table.ts` — comment locks **D-11** "NO month-multi"; **override per this contract** (document the override in the file comment)
- `app/(app)/expenses/page.tsx` — wire `months` into filters + pass `monthsWithData` into `ExpensesToolbar` (toolbar already accepts the prop)
- `app/(app)/expenses/ExpensesToolbar.tsx` — already forwards `monthsWithData`
- `lib/validations/expense.ts` — `parseExpenseFilters` / `ParsedExpenseFilters` (add `months`)
- `lib/dal/expenses.ts` — apply months filter in `getExpenses`
- `lib/dal/months-with-data.ts` — extend if expenses need their own month list; or reuse a coherent source (see Action)
- `lib/utils/search-params.ts` — reuse `parseMonths`
- Tests: add/update expense filter parse + DAL predicate tests as appropriate (new file OK)

**Action:**
1. Add `month-multi` filter field `months` to `expensesTableConfig`, mirroring `app/(app)/transactions/transactions.table.ts` (same control UX).
2. Explicitly update the D-11 comment: contract feedback 3.5 / quick 260731-hhv overrides the prior "no temporal filter" lock for the expenses toolbar.
3. Parse `months` CSV via `parseMonths` in `parseExpenseFilters`; pass into DAL.
4. DAL: when `months` non-empty, restrict expenses to those months. Prefer filtering by member transaction `occurredAt` month (domain-correct for spending period). If join cost is prohibitive, document chosen date field in SUMMARY — but prefer transaction-linked months over raw `expense.createdAt` when feasible.
5. Provide `monthsWithData` to the toolbar (extend `getMonthsWithData` for expenses, or derive distinct months from expense-linked transactions — pick one and keep UX coherent with Transactions).
6. Include `months` in `hasActiveExpenseFilters` / table key so empty-state and remount behave correctly.

**Verify:**
- Automated: vitest for parse months on expenses + any new DAL unit test
- Manual: `/expenses` shows Mesi filter; selecting a month narrows the list; clearing restores all-time default

**Done when:** Spese has the same month-multi temporal filter pattern as Transactions; D-11 comment documents the contract override; absent `months` = all-time.

### Task 4: KPI Entrate/Uscite cards clickable (3.6)

**Files:**
- `components/dashboard/overview/kpi-row.tsx` — Entrate / Uscite `ReadingKpiCard` instances (Bilancio / Accantonato stay non-required by contract)
- `components/dashboard/overview/kpi-card-reading.tsx` — add optional `href` / link wrapper if needed (keep card anatomy)
- `lib/routes.ts` — reuse `buildDashboardCategoriesHref({ type: 'in' | 'out', … })` or equivalent existing detail routes; do not invent a new status param

**Action:**
1. Make Entrate and Uscite KPI cards navigable to the corresponding dashboard detail (categories breakdown with `type=in` / `type=out`, preserving current year/preset/lens query params from the overview context if already available to `KpiRow`).
2. Entire card (or clear hit target covering the card) is clickable; keyboard-accessible (`Link` / focusable). Cursor pointer + accessible name.
3. Do not change Bilancio savings-rate copy (3.9 deferred). Accantonato clickability is optional/out of scope unless trivial with the same helper.
4. Avoid nested interactive elements breaking composition bars — wrap card with `Link` carefully or use a single navigational surface.

**Verify:**
- Typecheck / existing dashboard overview tests if any touch KPI row
- Manual: overview → click Entrate → categories (or detail) filtered to income; Uscite → out

**Done when:** Entrate and Uscite cards navigate to the matching detail; Bilancio copy untouched.

## must_haves

- Crea categoria works (3.7).
- Welcome: only "Vai alla dashboard" (3.1).
- Uncategorized nudge is a link to existing uncategorized transactions route (D-01 / 3.2) — verified or fixed.
- Empty pattern-suggestions box hidden (3.3).
- Expenses month-multi filter works; D-11 overridden in comments (3.5).
- Entrate/Uscite KPI cards clickable to detail (3.6).

## Out of scope / Deferred

- **3.4** pattern copy (wave 03)
- **3.8** dashboard visual hierarchy redesign
- **3.9** Bilancio tasso / 20% domain + copy
- **3.10** Ammortamenti rename (wave 03)
- **2.4** Categorizza hover/Più usate
