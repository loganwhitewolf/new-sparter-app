---
quick_id: 260630-gy0
slug: bulk-categorizzazione-massiva-nella-vist
status: ready
---

# Quick Plan: Bulk categorizzazione in Transazioni

## Goal

Parity with `/expenses`: floating bulk bar gets **Categorizza (N)** alongside Elimina.

## Approach

- Reuse `BulkCategorizeDialog` + existing `bulkCategorize` action (transactions categorize via linked expense).
- Derive unique `expenseId`s from selected transaction rows; enable button only when ≥1 expense is present.
- Optimistically update all rows sharing those expense ids after success.

## Tasks

1. Extend `TransactionBulkActionBar` with categorize CTA (mirror `BulkActionBar`).
2. Wire `transaction-table.tsx`: dialog state, expense-id mapping, optimistic UI.
3. Optional toast noun in `BulkCategorizeDialog` for "transazioni" vs "spese".
4. Unit tests for bulk bar + table wiring.
