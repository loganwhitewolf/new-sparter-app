---
phase: quick-260629-m9i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/expense-reconciliation.ts
  - lib/services/transaction-deletion.ts
  - lib/services/transaction-detach.ts
  - lib/actions/transactions.ts
  - lib/validations/transactions.ts
  - lib/dal/transactions.ts
  - lib/dal/classification-history.ts
  - components/transactions/detach-expense-dialog.tsx
  - components/transactions/transaction-table.tsx
  - lib/services/import.ts
  - tests/transaction-detach-service.test.ts
  - tests/transaction-detach-action.test.ts
  - tests/import-service.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "From /transactions, a row on an aggregated expense (2+ linked transactions) shows menu action Separa in spesa dedicata"
    - "Confirming detach with a non-empty title moves the transaction to a new expense with synthetic descriptionHash and opens categorization on that expense"
    - "Source expense totals and transactionCount are recalculated; empty non-manual expenses are deleted per existing deletion policy"
    - "Re-importing a file does not change subCategoryId or status on an expense that was manually categorized (status 3, latest history source manual)"
    - "New import rows with the same bank descriptionHash still attach to the original expense; detached transactions stay on their synthetic-hash expense"
  artifacts:
    - path: lib/services/transaction-detach.ts
      provides: detachTransactionToDedicatedExpense service
    - path: lib/services/expense-reconciliation.ts
      provides: shared expense aggregate reconciliation after transaction membership changes
    - path: components/transactions/detach-expense-dialog.tsx
      provides: title confirmation dialog before detach
    - path: lib/dal/classification-history.ts
      provides: latest classification source lookup for import manual-lock
  key_links:
    - from: components/transactions/transaction-table.tsx
      to: lib/actions/transactions.ts
      via: detachTransaction server action
    - from: lib/services/transaction-detach.ts
      to: lib/services/expense-reconciliation.ts
      via: reconcile source expense after expenseId move
    - from: lib/services/import.ts
      to: lib/dal/classification-history.ts
      via: skip subCategoryId overwrite when manually locked
---

<objective>
Implement "Separa in spesa dedicata": detach one transaction from an aggregated expense into a new dedicated expense with its own title, then immediately categorize it. In the same quick task, lock manually categorized expenses against subCategoryId overwrite on re-import.

Purpose: Support mixed-purpose P2P transfers (e.g. recurring streaming vs one-off lunch reimbursement) without CSV pre-splitting, while keeping manual categorization stable across re-imports.

Output: detach service + action + transaction-table UI flow; shared expense reconciliation helper; import upsert manual-lock guard; Vitest coverage for detach, action smoke, and re-import preserve.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@CONTEXT.md
@.planning/quick/260629-m9i-separa-transazione-in-spesa-dedicata-det/260629-m9i-CONTEXT.md

@lib/services/transaction-deletion.ts
@lib/services/import.ts
@lib/db/schema.ts
@components/transactions/transaction-table.tsx
@components/expenses/expense-categorize-dialog.tsx
@lib/actions/expenses.ts
@lib/dal/classification-history.ts
@lib/utils/decimal.ts
@lib/utils/import.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detach transaction to dedicated expense (service, action, UI)</name>
  <files>lib/services/expense-reconciliation.ts, lib/services/transaction-deletion.ts, lib/services/transaction-detach.ts, lib/actions/transactions.ts, lib/validations/transactions.ts, lib/dal/transactions.ts, components/transactions/detach-expense-dialog.tsx, components/transactions/transaction-table.tsx, tests/transaction-detach-service.test.ts, tests/transaction-detach-action.test.ts</files>
  <behavior>
    - Detach rejects when transaction missing, wrong user, no expenseId, or source expense has transactionCount &lt;= 1
    - Detach creates new expense: title from input (trimmed, max 120, non-empty), descriptionHash = SHA-256 of detached:{transactionId}, status 1, subCategoryId null, totals from the single moved transaction
    - Detach moves transaction.expenseId to new expense (no duplicate row); source expense reconciled via same rules as delete (manual/override preserved empty, others deleted when zero txs)
    - Action returns { newExpenseId, newExpenseTitle } for UI to open ExpenseCategorizeDialog
    - UI shows Separa in spesa dedicata only when expenseId present and expenseTransactionCount &gt; 1; after success opens categorize on new expense
  </behavior>
  <action>
Implement end-to-end detach per locked CONTEXT decisions (UI entry, post-detach categorize, title dialog).

1. Extract reconciliation (Claude's Discretion — avoid duplicating deletion logic):
   - Create lib/services/expense-reconciliation.ts exporting the aggregate load, manual/override detection, buildReconcilePlan, and applyExpenseReconciliation logic currently private in transaction-deletion.ts.
   - Refactor transaction-deletion.ts to import from expense-reconciliation.ts with identical behavior (no rule changes).

2. Service lib/services/transaction-detach.ts:
   - Export detachTransactionToDedicatedExpense({ userId, transactionId, title }).
   - Run inside db.transaction.
   - Load transaction joined to source expense; enforce userId scope on both (IDOR). Fail with a clear domain error if not found, no expenseId, or source expense.transactionCount &lt;= 1.
   - Compute synthetic descriptionHash with node:crypto createHash sha256 on the literal prefix detached: concatenated with transactionId (do NOT use computeDescriptionHash — not a bank description).
   - Insert new expense: user-provided title slice 0-120, synthetic descriptionHash, status 1, subCategoryId null, totalAmount = transaction.amount (Decimal helpers), transactionCount 1, first/lastTransactionAt = transaction.occurredAt, importedFromFileId null.
   - Update transaction set expenseId = new expense id (keep original transaction.descriptionHash unchanged for import dedup).
   - Call reconcileExpensesAfterTransactionRemoval (or equivalent exported helper) for [sourceExpenseId] only.
   - Return { newExpenseId, newExpenseTitle: title }.

3. Validation lib/validations/transactions.ts:
   - Add DetachTransactionSchema: transactionId uuid/string, title z.string().trim().min(1).max(120).

4. Action lib/actions/transactions.ts:
   - Add detachTransaction server action: verifySession, parse schema, call service, revalidatePath for /transactions (and /expenses if already used by sibling actions). Map domain errors to Italian user-facing messages; developer logs/comments in English.

5. DAL lib/dal/transactions.ts:
   - Add expenseTransactionCount: expense.transactionCount to transactionListSelect and TransactionListRow type (needed to gate menu item on aggregated expenses only).

6. UI components/transactions/detach-expense-dialog.tsx:
   - Client dialog: title input pre-filled from customTitle if present else description (slice 120), required non-empty, Italian copy (e.g. titolo dialog + confirm/cancel). Calls detachTransaction on confirm; surfaces errors via toast.

7. UI components/transactions/transaction-table.tsx:
   - In row ⋮ menu (D-UI entry point only — not expense details dialog): add DropdownMenuItem Separa in spesa dedicata with a distinct icon (e.g. Split or Copy — not the existing Unlink pair icons).
   - Visible only when transaction.expenseId and transaction.expenseTransactionCount &gt; 1.
   - On success: close menu, toast success, setCategorizeTarget({ id: newExpenseId, title: newExpenseTitle }) so existing ExpenseCategorizeDialog opens immediately (D-FLOW). Categorize flow already sets status 3 + manual history on save — no change needed there.
   - Optimistically update local row state: point expenseId/title/status to new expense (status 1 / da categorizzare labels) or refetch via router.refresh if simpler.

8. Tests (write first per tdd):
   - tests/transaction-detach-service.test.ts: mock db transaction; happy path moves expenseId, synthetic hash, reconciles source; rejects single-tx expense; preserves manual-empty source expense.
   - tests/transaction-detach-action.test.ts: smoke — session + schema + service delegate.

No schema migration. No bulk detach. No entry from expense details dialog (deferred/YAGNI).
  </action>
  <verify>
    <automated>yarn vitest run tests/transaction-detach-service.test.ts tests/transaction-detach-action.test.ts</automated>
  </verify>
  <done>Detach service and action pass tests. Transaction table shows Separa in spesa dedicata only on multi-transaction expenses; confirming title creates dedicated expense and opens categorization dialog on the new expense with status 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-import preserve manual categorization lock</name>
  <files>lib/dal/classification-history.ts, lib/services/import.ts, tests/import-service.test.ts</files>
  <behavior>
    - When upserting an existing expense by bank descriptionHash: if expense.status is 3 AND latest expense_classification_history row for that expenseId has source manual, do not overwrite subCategoryId or status even when categorizePipeline returns a different match
    - Still update totalAmount, transactionCount, lastTransactionAt on that expense; new transactions with same bank descriptionHash still link to this expense
    - When manual-locked, skip writeClassificationHistory for the pipeline result on that upsert
    - Detached expenses (synthetic descriptionHash) are unaffected — different hash bucket
  </behavior>
  <action>
Implement re-import manual lock per D-REIMPORT (locked CONTEXT decision).

1. DAL lib/dal/classification-history.ts:
   - Add getLatestClassificationSource(database, { userId, expenseId }) returning the source of the most recent history row (order by createdAt desc, id desc limit 1) or null.

2. Import lib/services/import.ts — in the existing expense upsert branch (when existing row found by userId + bank descriptionHash):
   - After loading existing, compute isManuallyLocked = existing.status === '3' AND getLatestClassificationSource(...) === 'manual'. Only manual counts — not override (per locked decision).
   - If isManuallyLocked: update only totalAmount (Decimal plus), transactionCount increment, lastTransactionAt, updatedAt. Do NOT set subCategoryId or status from catResult.
   - Else: keep current behavior (subCategoryId and status from catResult / existing).
   - Wrap writeClassificationHistory: skip when isManuallyLocked even if catResult is non-null.
   - Transaction linking loop unchanged — same descriptionHash still maps to same expenseId.

3. Test tests/import-service.test.ts:
   - Add case: existing expense status 3, latest history source manual, categorizePipeline returns different subCategoryId — after importFile upsert, expense keeps original subCategoryId and status 3; totals still increase.

Do not change categorizePipeline itself. No migration.
  </action>
  <verify>
    <automated>yarn vitest run tests/import-service.test.ts -t "manual"</automated>
  </verify>
  <done>Re-import upsert preserves subCategoryId and status on manually categorized expenses while still aggregating new transactions; vitest manual-lock case passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Untrusted transactionId and title from authenticated user |
| Service → Database | expenseId / transactionId must be user-scoped to prevent IDOR |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m9i-01 | Spoofing | detachTransaction action | medium | mitigate | verifySession before any DB read |
| T-m9i-02 | Tampering | transaction-detach.ts | high | mitigate | All reads/writes filter by userId; reject cross-user transaction or expense |
| T-m9i-03 | Tampering | import.ts upsert | medium | mitigate | Manual-lock check uses expense owned by import userId; history query scoped by userId |
| T-m9i-04 | Denial | detach on single-tx expense | low | accept | Reject — detach meaningless; UI hidden when count &lt;= 1 |
</threat_model>

<verification>
After both tasks:
- yarn vitest run tests/transaction-detach-service.test.ts tests/transaction-detach-action.test.ts tests/import-service.test.ts -t "manual"
- yarn tsc --noEmit
- yarn check:language (routes/comments touched)
Manual smoke: on /transactions, open ⋮ on a multi-tx expense row → Separa in spesa dedicata → edit title → confirm → categorize sheet opens on new expense; re-import same file → original expense category unchanged, detached row stays separate.
</verification>

<success_criteria>
- User can detach one transaction from an aggregated expense via transaction table menu only, with Italian label Separa in spesa dedicata.
- New dedicated expense uses synthetic descriptionHash, status 1 until categorized; categorize flow sets status 3 with manual history.
- Source expense totals reconcile correctly; empty non-manual expenses deleted per existing policy.
- Re-import does not overwrite manual categorization on locked expenses; new same-hash transactions still aggregate.
- Tests pass; no schema migration; Decimal.js for money; db.transaction for multi-step detach.
</success_criteria>

<output>
Create `.planning/quick/260629-m9i-separa-transazione-in-spesa-dedicata-det/260629-m9i-SUMMARY.md` when done.
</output>
